접근을 전환한다. wireframe_plan.md를 저장소에 편입하고
docs/wireframe_plan.md 로 두어라.

stage_v_spec.md와 prior_fill_plan.md를 대체한다. 두 문서는
docs/archive/ 로 옮기고 폐기 사유를 명시한다.

════════════════════════════════════════
전환 요지
════════════════════════════════════════

이전: 완성된 스케치를 해석해 형태를 추론한다.
이후: 카메라를 먼저 세우고, 그 위에서 투시 작도로 3D를 직접 구축한다.

SketchUp의 선 도구와 메커니즘이 사실상 같다. 유일한 차이는
카메라의 출처다 — SketchUp은 3D 앱이라 처음부터 알고,
여기서는 초반 입력이 그것을 정한다.

지금까지 막혔던 문제들은 전부 미지수가 관측보다 많아서 생겼다.
카메라가 확정되면 커서 픽셀 하나가 3D 점 하나를 유일하게 결정한다.
추론이 사라지고 계산만 남는다.

════════════════════════════════════════
W-A. 전환 정리 (먼저)
════════════════════════════════════════

a. wireframe_plan.md §2.3의 폐기 항목을 코드에서 제거한다.
   1-bis 불확실성 전파, 사전분포 채움, 도메인 상수 테이블,
   2D 각도 축 클러스터링, footprint 압출, 매스/보이드 분류기,
   다중 뷰 정합, axisToggle.

   **측정 결과는 보존한다.** 접근을 바꾼 근거이므로
   progress.md와 reports/에 남기고, 코드만 제거한다.

b. §2.1 유지 항목이 온전한지 확인한다.
   특히 카메라 수학(6.2/6.3/6.4/6.5)이 stage_perspective/에서
   분리 가능한지 본다. W-1이 이것을 직접 쓴다.

c. assumptions.md를 §10에 따라 정리한다.
   무효화된 항목은 삭제하지 말고 "접근 전환으로 무효"로 표시한다.

d. 리뷰어 서브에이전트(.claude/agents/reviewer.md)를 만든다.
   읽을 수 있는 것: progress.md, assumptions.md, checkpoints.md,
     reports/*, docs/*_plan.md, docs/perspective_theory.md, 직전 보고문
   읽을 수 없는 것: 소스 코드, 테스트 코드

   점검 항목:
     1. 이번 보고의 수치가 이전 보고와 모순되는가
     2. 이번 결과가 assumptions의 어느 항목을 반증하는가
     3. 수치가 지나치게 좋은가 (정확히 1.0, 0.0, 기계 엡실론)
     4. 결론이 근거보다 넓은가
     5. 이론서와 어긋나는가
     6. 같은 유형의 결함이 다른 곳에도 있을 가능성
        (지표 맹점, 프레임 문제, 비결정 시드, 자기참조 검증)

   출력은 지적 목록. 승인/반려가 아니라 지적만.
   각 W 단계 완료 시 호출하고, 지적이 나오면 대응 후 재호출.
   2회 반복 후에도 남으면 checkpoints.md에 올리고 진행.

e. 분기 결정 규칙
   - 측정이 가리키는 방향을 우선한다
   - 사전 예상과 어긋나면 측정을 따르고 예상을 assumptions에
     반증으로 기록한다
   - 선택 근거와 기각 사유를 progress.md에 남긴다
   - 다음만 사람에게 올린다: 자료구조 변경, 범위의 축소·확대,
     재산출 범위가 한 단계를 넘을 때, 제품 방향 판단

════════════════════════════════════════
W-0. 획 → 엣지 매핑 측정 (선행, 판단 근거)
════════════════════════════════════════

이 방식에서 유일하게 원리적 미확인 항목이다.
나머지를 만들기 전에 판단이 서야 한다.

기존 실획 데이터(Quick,Draw raw, 세션 JSON)로 측정한다.

a. 한 획이 한 엣지로 매핑되는 비율
b. 덧그림 병합 정확도 (같은 위치 재방문 검출)
c. 획 분절 오류율 (한 획이 여러 엣지여야 하는 경우)
d. 지나침 빈도 (교차점을 넘어 그은 경우)

비율이 낮으면 6.2 처리 설계를 그에 맞게 강화한다.
결과를 progress.md에 기록하고 W-1로 진행한다.

════════════════════════════════════════
W-1. 카메라 확정 UI + 제약 누산기
════════════════════════════════════════

wireframe_plan.md §3 전체.

입력 방식을 고정하지 않는다. 점 찍기 / 선 긋기 / 지평선 긋기 /
렌즈 슬라이더가 모두 같은 제약 누산기로 수렴한다.
이론서 5장의 자유도 회계가 자료구조가 된다.

해 계산은 이론서 6.2/6.3/6.4를 그대로 쓴다. 1-ter에서 검증됨.
과잉 결정 시 최소제곱, 잔차를 표시한다.

유효성 검사는 실시간으로:
  3점 예각 조건(6.5) 위반 → f² < 0, 즉시 붉게 표시
  화각(18.4) d < 0.5W → 경고

부분 확정 상태에서도 그릴 수 있어야 한다. 1점에서 f가 자유여도
폭·높이는 실척으로 읽히므로(7.7절) 그 축은 즉시 배치된다.

카메라 확정 후 투시 그리드(지면 격자 + 축 가이드)를 캔버스에 깐다.
프리셋: 실내 1점 24mm / 실내 2점 35mm / 외부 2점 50mm / 외부 3점 앙각.

════════════════════════════════════════
W-2. 추론 엔진
════════════════════════════════════════

§4.1~4.3.

방향 분류는 점-직선 거리로 한다. 선을 연장해 소실점을 지나는지
검사하는 것이 각도 추정보다 강건하다.
분류 대상: VP1/VP2/VP3, 화면평행(c=0), 면 위 사선, 미분류.

**제약 재적합**이 핵심이다. 분류된 선을 정확히 그 VP를 지나도록
다시 적합하면 자유도가 2에서 1로 줄고 손떨림이 제거된다.
직교 스냅이 평면에서 하던 일의 투시 판이다.
코너 오차 감소량을 측정할 것.

위치는 앵커 체인으로 전파한다. 씨앗(지면 접촉점) → 광선-직선
최근접점 → 교차점이 새 앵커. 연결된 것만 배치된다.

스냅 대상: 끝점/중점/엣지 위/면 위/축 방향/평행·수직.

════════════════════════════════════════
W-3. 기하 구축
════════════════════════════════════════

§5.1 자료구조 (Vertex/Edge/Face/Camera). IR을 대체한다.
광선-직선 최근접점, 앵커 체인, 엣지 그래프.
루트 필드 제한은 유지하되 구조가 단순해진다.

════════════════════════════════════════
W-4. 실시간 피드백
════════════════════════════════════════

§4.4. 이것이 없으면 사용자가 왜 그렇게 됐는지 모른다.
획을 놓기 전에 스냅될 방향 미리보기, 축별 색상,
스냅 대상 하이라이트, 추론 근거 툴팁.

════════════════════════════════════════
W-5. 면 생성
════════════════════════════════════════

§5.2. 닫힌 동일평면 루프 → 메쉬.
엣지 그래프 최소 순환 탐색, 공면성 검사.
경계 조건(T자 접합, 근사 공면, 자기교차)에 시간이 걸릴 수 있다.

════════════════════════════════════════
W-6. 궤도 회전 후 계속 그리기
════════════════════════════════════════

돌린 뒤에는 카메라를 정확히 아므로 추론이 자명해진다.
소실점 설정은 부트스트랩 용도이고, 이후는 일반 3D 모델러와 같다.

════════════════════════════════════════
W-7. 편집
════════════════════════════════════════

§5.3. 되돌리기/다시하기, 엣지 삭제, 정점 이동,
카메라 재조정(소프트 락 해제).

════════════════════════════════════════
W-8. 종단 검증
════════════════════════════════════════

Playwright. 카메라 확정 → 그리기 → 면 생성 → 회전 → 계속 그리기.
지연 측정. 스크린샷 회귀.

════════════════════════════════════════
진행 규칙 — 무중단 자율 진행
════════════════════════════════════════

W-A → W-0 → W-1 → ... → W-8까지 **멈추지 않고 완주한다.**
중간 보고를 하지 않는다. 확인을 요청하지 않는다.
소요 시간과 세션 수는 문제되지 않는다.

각 단계 완료 시:
  테스트·selfcheck 실행 → 리뷰어 호출 → 지적 대응 → 커밋 → 다음 단계
  세션이 컨텍스트 한계에 닿으면 HANDOFF.md를 갱신하고
  다음 세션에서 그것을 읽어 이어간다(아래 A-1).

════════════════════════════════════════
A-1. 세션 인계 (컨텍스트 한계 대비)
════════════════════════════════════════

HANDOFF.md를 만들고 각 단계 완료 시 갱신한다.

  현재 단계 (W-n의 어디까지)
  다음에 할 일 (구체적 파일과 함수 수준)
  잠정 결정 목록과 근거
  미해소 지적 (리뷰어·selfcheck)
  최근 커밋 해시
  읽어야 할 문서 순서

컨텍스트가 차면 HANDOFF.md를 갱신하고 종료한다.
사용자는 새 세션에서 다음 한 줄만 넘긴다:

  "HANDOFF.md를 읽고 이어서 진행하라."

이 문장이 유일한 사람 개입이다.

════════════════════════════════════════
A-2. 막혔을 때 — 멈추지 말고 우회한다
════════════════════════════════════════

이전 규칙에서는 멈춤 조건에 걸리면 보고하고 정지했다.
이번에는 정지하지 않는다. 아래 순서로 자체 처리한다.

**단계 1 — 재시도**
  같은 접근으로 최대 3회. 매번 다른 가설로.

**단계 2 — 우회**
  더 단순한 구현으로 대체한다. 예:
    면 생성 경계 조건이 어려우면 → 사각형 면만 우선 지원
    스냅 tolerance가 안 잡히면 → 넉넉한 값으로 두고 W-8에서 조정
    제약 재적합이 불안정하면 → 재적합 없이 분류만
  우회 사실과 원래 목표를 DEFERRED.md에 기록한다.

**단계 3 — 격리**
  해당 기능을 비활성화하고 나머지를 진행한다.
  전체가 그것에 의존하면 최소 대체물을 만든다.

**단계 4 — 다음 단계로**
  어떤 경우에도 다음 W 단계로 넘어간다.
  W-8까지 도달하는 것이 목표이며, 완성도는 그 다음이다.

**중단하는 유일한 경우**
  W-0에서 획→엣지 매핑 비율이 0.5 미만일 때.
  이 방식 전체의 전제가 무너지므로 보고하고 멈춘다.
  그 외에는 어떤 이유로도 멈추지 않는다.

════════════════════════════════════════
A-3. 판단 규칙 (사람에게 묻지 않는다)
════════════════════════════════════════

  - 측정이 가리키는 방향을 우선한다
  - 사전 예상과 어긋나면 측정을 따르고, 예상을 assumptions에
    반증으로 기록한다
  - 선택지가 여럿이면 **가장 단순한 것**을 고른다.
    복잡한 대안은 DEFERRED.md에 남긴다
  - 계획 문서와 실제가 어긋나면 실제를 따르고 문서를 갱신한다
  - 범위를 넓히지 않는다. 넓히고 싶으면 DEFERRED.md에 적는다
  - 선택 근거와 기각 사유를 progress.md에 남긴다

**사람에게 올릴 것은 없다.** 판단이 필요한 모든 항목을
잠정 결정하고 DECISIONS.md에 근거와 함께 기록한다.
프로토타입 완성 후 한 번에 검토한다.

════════════════════════════════════════
A-4. 품질 하한 (여기는 타협하지 않는다)
════════════════════════════════════════

우회하고 단순화하되 다음은 유지한다.

  - 기존 테스트가 깨진 채로 다음 단계에 가지 않는다
  - 새 기능마다 테스트를 쓴다. §13 규칙 3(반례 테스트) 포함
  - selfcheck를 매 단계 돌린다
  - 리뷰어를 매 단계 호출한다. 지적을 2회까지 대응하고
    남으면 DEFERRED.md에 옮긴 뒤 진행한다
  - 커밋을 잘게 나눈다. 되돌릴 수 있어야 한다
  - progress.md를 매 단계 갱신한다

════════════════════════════════════════
A-5. 완주 조건
════════════════════════════════════════

W-8이 끝나면 다음을 만들고 종료한다.

  PROTOTYPE.md
    - 실행 방법 (npm run dev, iPad 접속 절차)
    - 되는 것 / 안 되는 것 목록
    - 알려진 결함
    - 측정된 지표 (매핑 비율, 지연, 코너 오차 등)
  DEFERRED.md  — 우회·단순화·미룬 항목 전체
  DECISIONS.md — 잠정 결정과 근거 전체
  HANDOFF.md   — 완료 표시

그 뒤에 보고한다. 그것이 첫 보고이자 마지막 보고다.

════════════════════════════════════════
신규 assumptions
════════════════════════════════════════

- "한 획이 한 엣지에 대응한다" — W-0이 측정. 이 방식 전체의 전제
- "제약 재적합이 코너 오차를 줄인다" — W-2에서 확인
- "카메라 확정이 사용자에게 부담이 아니다" — 점 두세 개로 끝나므로
  낮을 것으로 예상하나 미검증
- "면 생성 경계 조건이 관리 가능하다" — W-5에서 확인
- "면 위 사선 처리로 비직교 범위가 회수된다" — 이전 측정에서
  비직교 52.6%가 범위 밖이었다. 얼마나 회수되는지 W-2에서 측정
