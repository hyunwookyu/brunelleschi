# web2 인계

## 🆕🆕🆕🆕🆕🆕🆕 web2-19 (2026-08-28) — 제도판: 흑연·종이 탭·띠 넷·아이콘

**어디까지 배포됐는가: (마감 절차 진행 중 — 병합·푸시·deploy:check 후 이 줄이 정본이 된다.)**

**게이트(2부 ⛔⛔ — 2-d ①②⑤): 전부 통과 — 값으로**: ① v2·**v3** → sheets 3
[「작도」·「종이 2」·「종이 3」]·포즈 전 성분 일치·v1 oy −5→−205(sheets.test — v3 판 실제
태움·2차 리뷰 [1]로 강화) ② v4 왕복 toEqual + 직렬화 문자열 자기 왕복 ⑤ 획 수 단위 2→2 ·
e2e 3→3(원장 views_thumb `gate_5_delete`). 서서 **배포하지 않고 3부로**(지시 문면).

지시 원문: [`docs/instructions/web2-19.md`](../docs/instructions/web2-19.md)(작업 전 커밋
d104b1d — 대응표 `docs/DRAFTING-MAP.md`·아이콘 정본 `docs/instrument-icons.md` 포함).
기록: NOTES 「2026-08-28 — web2-19」 절 전부(착수 대조·D 적용·1~4부·마감).

- **1부(획은 흑연이다)**: render2d constructing 갈래 삭제 — 몸체는 언제나 재료색(수리 전
  재현: vp 정의선 미리보기에 파랑 픽셀 168 → 0). 예고는 **생길 자리의 파선 ✕**(draft.vp =
  classifyNext 좌표 그대로 — 커밋 후 vps[0] 일치는 **구성 보장**·배선 회귀 채널이다 — 2차 [8]㉰). draftBrushed의 label
  매개변수 삭제. D-3 상주 반증(diag.forceConstructing). 1-c 분포는 **측정만**:
  `graphite_web2.json` — 손 대역(2~5px 처짐) 가로 66/144칸이 소실점 갈래(임계 불변 —
  다음 회차 판단. DEFERRED).
- **2부(종이 탭)**: `Doc.sheets`(배열 0 = 작도 종이 · id 0 예약 · 나머지 nextId 한 통) ·
  `.brnl` **v4**(1~4 수용·5 거부 · savedViews[i]→sheets[i+1] 「종이 N」 · 깨진 종이만 강등) ·
  띠(#paperbar — 탭·「+」·두 번 탭 이름·길게 눌러 썸네일·삭제 확인) · #topleft 세로 기둥
  (**web2-20이 아래에 종속 탭 줄을 붙인다** — 높이 하드코딩 없음, ⑦ 팔이 줄을 실제로 붙여
  잰다). App.savedViews·saveView·gotoView·deleteView는 없어졌다(addSheet·gotoSheet·
  deleteSheet·renameSheet). views.spec → paperbar.spec.
- **3부(띠 넷·전체 화면)**: 종이(위왼)·눈(위오른 — 작도 시점·전체 화면·표시 팝업)·손(우측 —
  되돌리기/구분선·자(삼각자=스냅 입구·축 스냅 자리만 비움)·연필 접힘·펜·지우개·면·치수)·
  서랍(파일+종이 질감). 설정 자루 해체 — own3d는 진단 곁 #diagctl(열쇠 b2-own3d 불변).
  전체 화면 body.fs(뼈대 유지·#fs-exit·세션 한정). zones.spec 5팔.
- **4부(아이콘·이름)**: 이름 넷만(다른 각도에서는 숨긴다 · 소수점 그대로 · brush/classic
  화면 언어 0 · own3d→진단). Phosphor light 이식(ruler·arrows-out·eye·eye-slash·
  compass-tool·grid-four·plus — path만·MIT LICENSE 줄). **굵기 light 확정 — 실측**
  (icon_weight_web2: 잉크량 최근접 = light. ⚠ 첫 판 32px 문턱은 분해능 0 — #71 ㉢ 재발).

**다음 사람이 알아야 할 것**:
- **web2-20(겹)은 이 회차의 `Doc.sheets`·`#topleft`를 가리킨다** — 종속 탭 줄은 #topleft
  안 둘째 줄로 붙인다(paperbar.spec ⑦이 그 자리를 잰다). **종이 삭제 규약이 바뀐다**
  (겹이 획을 소유하면 종이 삭제 = 획 삭제 — DEFERRED 행이 그 예고다).
- 옐로 롤 아이콘(색 예외 #e9d98a)·트레이싱지 롤은 `docs/instrument-icons.md` 정본에 있고
  **아직 화면에 없다**(web2-20이 쓴다).
- **PITFALLS #72 신설**(같은 선택자 CSS 두 자리 = 순서 의존 · 요소 수 팔은 목록으로) ·
  #71 ㉢ 재발 부기(새 척도에는 분해능 단언을 팔에 박는다).
- 가정 AS-C61~C66 신설 · 결정 D-W6 · 실기기 표(DEFERRED web2-19 — 파선 ✕·수평 의도·탭 셋·네 자리·연필통·전체 화면·자 팝업·아이콘 판독·(예비) 옐로 롤).
- 1-c 분포표가 다음 임계 판단의 출발점이다(«수평 의도» 실기기 행과 함께).

**검증(마감)**: 단위 **511 passed / 0 failed / 511**(56파일) · 타입 0오류 · 빌드 2.0s ·
전량 e2e 「**234 passed (7.8m)**」 — 234/0/234(30파일 · `--list` 대조) · selfcheck 새
플래그는 원장 안 해명. 원장 신설: graphite_web2 · icon_weight_web2{,_dpr2} ·
views_thumb_web2(paperbar가 승계).

## 🆕🆕🆕🆕🆕🆕 web2-18 (2026-08-27) — 잉크가 위에 온다 · 연장선은 획득한다 · 돌려도 흑연이 남는다

**어디까지 배포됐는가: 배포 ①(1·2부) = 원격 main `ed6d33f635432c7c706699ea0fdf97c4e0b1e921`,
`deploy:check` 「✓ 화면 빌드 식별자 == 배포 커밋 / 배포됨」 — 화면 빌드 식별자 `ed6d33f`.
배포 ②(3부 + 1차 리뷰 대응) = 원격 main
`e56b4ed7bb8aba06997870eed27a6773d9869373`, `deploy:check` 3회째 「✓ 화면 빌드 식별자 ==
배포 커밋 / 배포됨」 — **화면 빌드 식별자 `e56b4ed`**.**
⚠ 뒤에 문서 커밋이 붙으면 화면 해시가 한 칸 민다(web2-07 규약 — 정본은 위 해시).

**게이트: 3-d 통과(도는 동안의 프레임 기준)** — 400획 궤도 1프레임 합의 **중앙값**이
140~184 ms → **1.6~1.8 ms**(예산 16.7). ⚠⚠ **최악 프레임은 반대로 늘었다**(275~307 ms —
제스처를 «잡는» 한 프레임의 굽기다. 수리 전 191~278). 사람이 보고한 증상이 「**돌리는데**
느려진다」이므로 그 프레임으로 판정했고, 잡는 순간은 **다른 증상**으로 `DEFERRED.md`에 있다.
**4부 문턱(200획 osnap 2 ms)은 안 넘었다**(실측 0.87~0.90) — 안 열었다.

지시 원문: [`docs/instructions/web2-18.md`](../docs/instructions/web2-18.md).
기록: NOTES 「2026-08-27 — web2-18」 절 전부(착수 대조·D 적용·0~4부·**1차 리뷰 대응 19건**·마감).

- **0부(비용 원장)**: `cost18_web2{,_before,_dpr2}.json`. 획 50/100/200/400 × rawIn 0/50/100%,
  손 오차 태움(#68). **판정 넷**: ①전량 흑연은 O(n)이고 400획에서 예산의 열 배 밖 ·
  ②syncStrokes는 가장 싸고 **궤도 중엔 안 돈다** · ③궤도 프레임의 **99% 넘는 몫이 흑연 겹** ·
  ④osnap은 초선형이고 그 안의 `intersections3`이 O(n²)(기울기 31~47).
  ⚠ **PITFALLS #38을 «닫았다»고 적지 않는다**(1차 리뷰어 [17]) — 비용 원장은 새로 냈지만
  이 회차 자체가 원장 밖 수치를 남겼다(`pivot` 좌표·xint 뒤집힘은 단위 팔에만 있다).
- **1부**: 잉크 확정선 몸체를 `#gl`(Line2) → **`#ink`**. 비 0.149 → 1.052(연필 상자 해시는 불변).
  기각한 두 길은 `DECISIONS.md` D-W5.
- **2부**: `ext`가 **획득식**이 됐다(`core/extacq.ts` — 400 ms 머무름 · 최대 2 LRU ·
  상한 2배 · 확정 시 비움 · 무채색 십자 표식). 허공 855칸에서 ext **75 → 0**, near 35 → 38.
  진단 패널이 「마지막 획 스냅 / 지금 호버 스냅 / 연장선 획득」을 말한다.
- **3부**: ㉠ 화면 밖 잘라내기(여유 24px — 실측 6px 위의 동작점. ⚠ 이 픽스처에서는
  **잘라낸 획이 0**이라 ㉠의 효과는 미측정이다) + **㉢ 획별 질감 캐시**
  (`#brushc`를 아틀라스로 한 번 굽고 제스처 중 `#brushsnap`이 아핀 변환해 붙인다).
  ㉡은 **측정이 기각**했다(D-4 — `pressureProfile`이 이미 12점으로 재표본).
- **4부**: 안 열었다. 조건·표는 `DEFERRED.md`.

**다음 사람이 알아야 할 것**:
- **PITFALLS #71 신설** — 「시간·픽셀을 재는 팔은 어떤 조건에서 재는가가 값의 절반이다」
  (워커 수·`afterAll` 기록·임계가 분해능 아래·격자가 실패 불가·겹을 옮기면 판독 자리도).
  ⚠ **비용 원장은 `--workers=1`로 돌린다** — 안 그러면 전/후 비교가 무효다.
- **겹의 소유가 한 자리에 모였다**: `docs/line_plan_r3_draft.md` §9.6(무엇이 어느 캔버스에
  사는가 + 규칙 셋). 종전에는 세 파일 주석에 흩어져 있었다.
- 가정 **AS-C54~C58** 신설(전부 동작점·스윕 없음). 결정 **D-W5**.
- 실기기 표: `DEFERRED.md` 「web2-18 실기기 확인 표」 7행 — **「돌리는 중 질감」 행이
  사람의 구속**이다(질감이 사라지면 즉시 보고).
- ⚠ **web2-17 실기기 표 7행은 아직 사람 몫이다**(그대로 남는다).

## 🆕🆕🆕🆕🆕 web2-17 (2026-08-27) — 지평선은 이미 있다 (새 진입로) (**전부 완료 — 배포 ①·② 확인은 아래 줄이 정본**)

**어디까지 배포됐는가: 전부.** 배포 ①(1~3부) = `efe6202` 화면 확인 · 배포 ②(4·5부) =
원격 main **`99309d57e8c648a663c9cf01cd397e60bad3bd42`**, `deploy:check` 「✓ 화면 빌드
식별자 == 배포 커밋 / 배포됨」 — **화면 빌드 식별자 `99309d5`**. ⚠ 이 뒤 문서 커밋이
붙으면 화면 해시가 한 칸 민다(web2-07 규약 — 정본은 위 해시). **게이트(지시 ⛔⛔): 1-e ①~⑥ · 2-c ①~④ · 3-d ①~⑤ 전부 통과**(원장
horizon_equiv_web2 · entry17_web2 · drawview_web2 — 리뷰 1차 17건·2차 20건 전건 대응).

지시 원문: [`docs/instructions/web2-17.md`](../docs/instructions/web2-17.md).
기록: NOTES 「2026-08-27 — web2-17」 절 전부(착수 대조·D 적용·리뷰 대응 1·2차·마감 수치).

- **1부**: 지평선 = 프레임 상수(H/2 — `horizonDocY` 한 자리). Role 'horizon'·수평 강제
  삭제, 빈 문서부터 카메라 상시(축 H·V). **핵심 결정(지시에 없던 한 줄)**: 지평선 위
  «따라긋기» 획(양 끝 |y−H/2| ≤ OSNAP_RADIUS_PX)은 퇴화 — 아무것도 선언하지 않는다.
  이것이 1-e ⑥(기존 손버릇 → 2점 수렴)과 기존 팔 전체를 살린 열쇠다(경계 8/9px 플립 실측).
  1-c: waitWhy(aboveHorizon·onHorizon — 진단 패널).
- **2부**: .brnl v2. v1은 읽으며 변환(첫 획=지평선 검증 가드 · 전체 평행이동 · savedViews
  oy 보정 · scaleRef 방어). v2는 옛 파서가 **거부**(의도 — 2-a). **자동 저장 열쇠 분리**
  (`b2-autosave2` — 옛 PWA가 새 자동 저장을 지우는 소실 경로 차단. 옛 열쇠는 읽기 이행만).
  오라클 `test/legacy_web2_16.json`(옛 코드 622e9ac 실측 — 평행이동 동치·왕복·own3 재사영
  1.05e-12px). 평행이동 동치: 정수 격자 0(구성) · 비정수 격자 <1e-9 · 반증 dy=0은 >0.5.
- **3부**: `drawView`(첫 획 순간의 view — App/BrnlData 층·Doc 밖) · 선언 전 줌 차단
  (`dollyBy`) · `composeView` 합성(fitViewToFrame — 덮어쓰기 반증 팔). 안내 문구가 줌
  차단을 설명한다(「줌은 첫 획 뒤에 열린다」).
- **4부**: 지면 규칙 확대 — **연결 패스 소진 뒤** 모델 전체가 heightless(<1e-6)이고
  **대기가 전부 소실점 축**일 때, 소실점 축 선은 지면선(하나 올릴 때마다 재연결).
  두 판별자 모두 기존 팔이 강제했다(중간 판정 = 문서 순서 의존 · 판별자 없음 = 사슬
  끊긴 높이 획의 조용한 오배치). waitWhy hasHeight·mixedWait. 잔차 스윕: 0 아님 9/40 ·
  최대 2.2e-16 — 임계가 잔차와 9.7자릿수·실제 높이와 5.9자릿수 사이(ground_rule_web2).
- **5부**: `horizonPref`(null=자동 — **첫 소실점(vp0)이 화면 안**이면 숨김 · vpOnScreen
  한 함수 = ✕ 컬링과 공유) · fadeView 제스처 동결(왕복 팬 표시 변화 0회·대조군 >1) ·
  체크박스가 실제 상태를 비춤(change=사람 판별) · 숨은 상태 둘째 탭 진입로 팔.
  ⚠ 2점에서 vp0만 나가면 지평선이 다시 뜬다(AS-C53 알려진 대가 — 문면 준수).

**다음 사람이 알아야 할 것**:
- **잠복 결함 둘을 발견·처리했다**: ① 뷰 팝업 top 클램프(이미지 디코드 전 offsetHeight —
  load마다 재클램프로 수리 · AS-C7 네 번째 사례) ② draft 스냅샷의 파선 행 ±10ch 반올림
  (WebGL 픽셀 복사 — snapghost 관측창을 옮겨 우회 · DEFERRED 행·되살림 재실행으로 ㉢ 닫음).
- 실기기 표: DEFERRED 「web2-17 실기기 확인 표」(1~3부 5행 + 4·5부 2행) — 팬 눈높이·
  8px 대역·선언 전 줌·작도 시점·옛 자동 저장·자동 숨김 드나듦·지면 규칙 순서.
- 가정: AS-C51·C52·C53 신설 · **AS-C5 제자리 정정**(주점 y가 데이터→프레임 상수 가정) ·
  AS-C7 사례 추가. 결정: D-W4(+부기). PITFALLS 새 등재 없음(1·2차 대응 절에 판정문).
- 개정 3 초안 §9.5 「진입」 상태표 신설(승인은 여전히 대기 — 현행 계획서 불변).
- **web2-16 4부는 폐기됐다**(지시 ⛔⛔ — ㉯ 삭제. 4-a·4-b만 이 회차가 이었다).

검증(4·5부 마감): 단위 **483 passed / 0 failed / 483**(51파일) · 타입 · 빌드(1.97s~2.05s) ·
e2e **190 passed / 0 failed / 190**(23파일·95팔 — --list 대조) · selfcheck 새 플래그는
원장 안 해명. 원장 신설 6종: horizon_equiv · entry17 · drawview · ground_rule ·
horizon_hide (+legacy_web2_16 오라클 픽스처) · test_cost 현행화(history에 1~3부 보존).


## 🆕🆕🆕🆕 web2-16 (2026-08-27) — e2e 선행 실패 · 축 사영 · 대기선 파선 (**1~3부 완료·배포 ①② 확인 · 4부 미착수**)

**어디까지 배포됐는가: 배포 ①(1·2부) = `e3cbb1b` 화면 확인 · 배포 ②(3부) = 원격 main
`97e27ac7c87397da7e5e8f7357ffd798247f00e1` — 화면 확인은 NOTES 「배포 ②」 줄이 정본(이
줄이 그보다 먼저 쓰였으면 그 줄을 본다). ⚠⚠ 게이트(지시 ⛔⛔ — 3부 끝 판정): 2부 회귀
팔 ①~⑤ 전부 통과 — 리뷰어 판정 포함(NOTES 2부 「지시 ①~⑤ 대응 표」). 4부(소실점을
놓고 시작한다)는 컨텍스트 소진으로 미착수 — 다음 회차 몫이다.**

지시 원문: [`docs/instructions/web2-16.md`](../docs/instructions/web2-16.md).
기록: NOTES 「2026-08-27 — web2-16」 절(착수 PITFALLS 대조·1~3부·리뷰 대응 전건).

- **1부**: dim.spec 「인식기 감지」 2케이스의 원인은 인식기가 아니라 **진단 패널의 포인터
  삼킴**(e224d82부터 — 상단이 필기 시작점을 1.4px 덮음). 수리 `#diagpanel{pointer-events:
  none}` + diag.spec 3×3 격자 팔. **PITFALLS #70 등재**(잔존 dev 서버 + reuseExistingServer
  가 «과거 재현»을 현재 측정으로 바꾼다 — web2-15의 380c3f9 워크트리 증거도 오염 무효,
  결론만 재확인) + 부기(cwd 위생·파일 수 회계). 원장 `diag_occlusion_web2.json` 신설.
  web2-14의 «전량 통과» 보고는 e224d82부터 거짓(형태 판별 불가 — misreports_found).
- **2부**: 축 걸린 획의 **2D(대기) 특징점** 오스냅을 조준선에 사영(수선의 발 —
  draft.ts resolveEnd ①. 3D 특징점은 #63 유지: 양 끝 3D라 축 없이 승격 — 면 팔 10개가
  경계를 그었다). 무산 계수 전 경로(aNot3d·noCam — own3d.ts·진단 패널). edge_band 세
  국면(w14 98 · w15 52 재현 · 현행 3 — 죽은 칸 전부 계수 증분·조용한 죽음 0). 표시:
  기호↔축선 끝 사이 수선 연결선(#555·0.8px — e2e axisproj.spec 픽셀 팔·분리 32px).
  팔 axisproj.test 11(반증 두 겹 실행 — 밀림원은 off=4 상수·4/L이 가른다).
- **3부**: 대기 획 몸체 = **#brushc 흑연 파선**(C.WAIT_DASH_* 5·4 — classic도 같은 상수) ·
  waitFadeFactor **이진**(창 30° 안 1·밖 0 — 페이드·팬 0.3 폐지. AS-C44 갱신). 문서 불변 ·
  제스처 동결 유지(brush 겹 기준). ⚠ «tail이 4 failed를 잘라 176을 전량으로 읽을 뻔» —
  --list 총수 대조(㉤)가 잡았다(NOTES — flow·brush 팔 둘이 겹 이사로 깨져 있었다).
- **검증(3부 마감)**: 단위 437/0/437(46파일) · 타입 · 빌드 · e2e **180/0/180**(22파일 —
  «180 passed (4.7m)») · selfcheck 새 플래그 없음. 리뷰어 여섯 차례(1부 12+11 · 2부 18+13 ·
  3부 14) 전건 대응.

**다음 사람이 알아야 할 것**:
- **4부 지시(소실점을 놓고 시작한다)는 통째로 남았다** — 지시 원문 4-a~4-f. 이 세션의
  사전 조사(코드 지형): ㉮ 빈 화면 탭 → `resolveCommit`이 hz===null이면 지금은 null(잡음) —
  거기가 입구다(탭점을 0길이로 받고 classifyNext에 «hz 없음 + 탭 → vp» 갈래·analyze에서
  그 vp의 y가 horizonY가 된다). ㉯ 틱 → `resolveCommit`에서 «높이 없음 + 지평선 교차 +
  L < C.VP_MIN_LEN_RATIO·diag(기존 임계 재사용 — 새 숫자 없음)»이면 0길이 vp로 변환.
  두 번째 소실점 찍기는 **이미 있다**(classifyNext 탭 갈래 — f²>0 검사 포함). 4-a 지면
  규칙: lift.ts의 `(isFirstLine || makesVp)` 조건에 «heightless(모든 3D가 Y=0) && vp축»을
  더한다(수직선이 서면 저절로 꺼진다). ⚠ vp에서 시작한 획은 시작이 무한원이라 유한화가
  필요 — 이 세션의 잠정안: 커밋에서 시작을 지평선 아래 8px(OSNAP_RADIUS 재사용)로 다듬는다
  (잉크 심판 예외를 안 만드는 길 — 검증 안 됨, 다음 세션이 판단).
- 실기기 표: DEFERRED 「web2-16 — 실기기 확인 표」(수선 연결선·흑연 파선·즉시 끊김 —
  길이 축·팬 채널 소실 병기) + 1부의 시각 가림 행.
- 원장 신설·재구성: diag_occlusion_web2 · xint_web2(edge_band 세 국면) · test_cost_web2
  (passed/failed 필드 — ㉤ 형식) · wait_freeze_web2(3부 재배선 서술).


## 🆕🆕🆕 web2-15 (2026-08-27) — 겉보기 교차 스냅 · 지우개 끝 (**완료·배포 확인됨**)

**어디까지 배포됐는가: 전부(2차 리뷰 대응까지).** 원격 main =
**`bc923ead8234152215380409fcf479162b6cd251`**, `deploy:check`
「✓ 화면 빌드 식별자 == 배포 커밋 / 결과: **배포됨**」 — **화면 빌드 식별자 `bc923ea`**.
_(앱 코드가 든 기능 커밋은 `a3f5ece` 하나이고 그것도 화면으로 확인됐다. `1fd63d3`·
`bc923ea`는 리뷰 대응 — 원장·문서·측정 하네스·팔 주석만이다.)_ ⚠ 이 뒤에 문서 커밋이
더 붙으면 화면 해시가 한 칸 밀린다(web2-07 규약 — 정본은 위 해시).

⚠⚠ **리뷰는 2회로 닫았다(§4 규약).** 남은 것은 DEFERRED 「web2-15 — 코드 몫 이월」
다섯 행이고 **그 첫 행이 지금 사는 결함**이다(아래 ⚠⚠).

지시 원문: [`docs/instructions/web2-15.md`](../docs/instructions/web2-15.md)
기록: NOTES 「2026-08-27 — web2-15」 절(표식·수리·수치·임계 목록·안 잰 것).

### 1번 — 소실점 선을 다른 선으로 정의하기 (세 번째 판, 이번엔 손에서도 서야 한다)

증상은 그대로였다: 「스냅이 안 잡힌다」. web2-14가 대기선 몸통을 `near` 후보로 열었는데
손에서 여전히 안 됐다. **표식(D-1)이 낸 기전은 둘이고, 지시가 준 후보 셋 중 맞은 것이
없었다**(㉮ 「결과 타입이 3D 점 하나뿐」은 사실이 아니다 — `p3`는 이미 `V3 | null`이다):

- ① **정의가 죽는 기전** — `near`가 획을 축선에서 밀어내 축을 잃게 한다 → A 자신이 안
  올라감 → 정의가 **조용히** 무산(무산 계수조차 안 오른다). 죽은 38칸 중 37칸이 이 형태.
- ② **표식이 안 뜨는 기전**(정의 실패의 기전이 **아니다** — 1차 리뷰 [7]로 갈랐다):
  `ext`·`perp`가 `near`를 가린다. 다른 3D 선에서 **이어** 그으면 그 연장이 조준 경로
  내내 이긴다 — 후보 분포 ext 220 · near 44 · 없음 11인데 정의는 237이 섰다(ext가
  이긴 칸은 전부 섰다). **사람이 말한 「스냅이 잡히지 않는다」를 재현하는 것이 ②다**
  (275칸 중 220 = 80%에서 대기선에 아무 표식도 안 뜬다).

수리: **겉보기 교차**(`xint` — AutoCAD apparent intersection · Rhino 선례 그대로, A-3).
축스냅된 획이 대기선을 만나는 자리는 하나이므로 「붙인다」가 아니라 「만나는 데까지
늘린다」. 구멍은 커서가 아니라 **축스냅된 끝**에서 잰다(#68: 오차가 안 실리는 값으로).
원장 `stage0/out/xint_web2.json` — **몸통 대역** 275칸에서 정의 **237 → 275**,
반경 밖 8 → 22/55. ⚠ 인용에 **「몸통 대역 한정」**을 붙인다.

⚠⚠ **2차 리뷰가 이 회차의 결론 셋을 뒤집었다**(NOTES 「리뷰 대응 (2차)」가 정본):
- **끝점·중점 대역에서 기전 ①은 살아 있다.** 길이를 흔들고 세 대역을 다 훑으니
  `edge_band` **297칸 중 52칸이 축을 잃고 죽는다**(L40 28 · L70 19 · L110 5 /
  반대끝 30 · 중점 18 · 시작끝 4). 죽은 칸의 이기는 종류는 `end` 34 · `mid` 18이고
  **`xint`는 0**이다. → web2-15가 한 것은 「①을 없앴다」가 아니라 **「①의 통로 하나
  (`near`)를 막았다」**이다. **안 고쳤다 — DEFERRED에 수치와 함께 올렸다.**
  ✅ **web2-16 2부가 고쳤다** — 2D 특징점을 축선에 사영(수선의 발). 재실행에서
  축 손실 52 → **0**, 죽음 3(off=20 — 의도가 갈린 자리·계수에 잡힘). DEFERRED 행 닫힘.
- **「여유의 대가 0」은 틀렸다.** 대기선을 둘로 늘리니 **20칸 중 1칸에서 «안 겨눈»
  선이 이기고 그 선이 정의된다**(`distractor` · gap 10). gap 20 이상은 0.
- **「220/275 = 80%가 사람 증상의 재현율」은 철회한다.** 죽은 칸도 비-ext 칸도 dx가
  전부 8이라 그 비율은 **격자가 정한 상수**다(#46). 남는 정확한 문장: 「기존 3D 선에서
  이어 긋는 구도에서 손 오차가 8px 미만이면 **언제나** ext가 이겨 표식이 안 뜬다」.
- 새로 **닫힌** 것: 문값이 조준선 방향으로 **정확히 8에서 갈린다**
  (`boundary_along_aim` — dy6·7 전부 통과 / dy8 6/11 / dy9~11 전부 거부).

### 2번 — Pro Pen 3E 지우개 끝

사람이 답을 줬다(진단 패널 관측): 지우는 **내내** `buttons 32`, **호버에는 없음**.
`assumptions.md` **AS-C48·AS-C49**로 올렸고 DEFERRED 「지우개 끝 신호」 행을 닫았다.

**도구를 안 바꾼다** — `pointerdown`에서 비트가 서 있으면 **그 획 하나**를 지우개
경로로 보낸다(`App.tipErase`, 매 접촉에서 다시 정해진다). 끝은 **연필 지우개**(흑연만).

> ⚠⚠ **다음 사람에게: 「호버 커서가 안 뜬다」는 결함이 아니다.**
> 기기가 지우개 끝을 **호버에 안 알린다**(AS-C49 — 사람이 실기기로 확인했고 다시
> 재지 않는다). 그래서 지우개 커서도 판정도 **닿는 순간부터**다. 호버 미리보기는
> 이 기기·브라우저 조합에서 **불가능하다** — 고치려 들지 말 것.

### 검증(마감)

- 단위 **426**(45파일) · 타입 · 빌드 통과.
- e2e **174케이스**(87팔 × dpr 둘 — `npx playwright test --list` 출력이 정본) 중
  **172 통과 · 2 실패**.
  ⚠ **실패 1팔 × 2dpr = `dim.spec.ts` 「인식기 감지」 — 선행 결함이다.**
  근거: `git worktree`로 **web2-14 마감 커밋 `380c3f9`을 그대로 체크아웃**해 돌렸고
  같은 2케이스가 같은 자리(60s 타임아웃 — `#dim-read`가 모의 인식기의 «38»에 도달
  못 함)에서 실패했다.
  > ⚠⚠ **[web2-16 정정] 위 워크트리 재현은 증거로 무효다**(PITFALLS **#70** — 잔존
  > dev 서버 5301을 `reuseExistingServer`가 재사용해 실제로는 web2-15 자신의 코드를
  > 재고 있었다). 결론(그 코드에서 실패)만 web2-16이 깨끗한 서버로 다시 세웠다
  > (c0faaaf 실측 실패 + 380c3f9=문서만 커밋 — `diag_occlusion_web2.json`). 원인은
  > 인식기가 아니라 **진단 패널의 포인터 삼킴**(e224d82부터)이었고 **web2-16 1부가
  > 수리했다**(`pointer-events:none` · e2e 176/0/176). ⚠⚠ **모순은 2차 리뷰에서 닫혔다** — `380c3f9`에서
  `npx playwright test --list`가 **「Total: 166 tests」**다. web2-14가 적은 164 =
  166 − 2 = **지금 실패하는 그 2케이스**다. → **web2-14의 마감 보고가 틀렸다.**
  ⚠ [web2-16 정정] 형태는 판별 불가다 — passed-만-보고(166−2)와 팔 오산(«82팔»×2)이
  같은 164를 낸다(misreports_found). 전량 보고가 거짓인 것만 확정. 저장소 밖 변화 가설은 필요 없다.
  ⚠ 그 회차는 **e2e 2케이스가 깨진 채 「전량 통과」로 보고하고 배포했다** — A-4의
  예외가 기록 없이 지나갔다. DEFERRED에 행으로 세웠다.
- selfcheck: `xint_web2.json`에 플래그 여섯 — 전부 원장 안 `flags_explained`가 해명한다.
  ⚠ 2차 리뷰의 「중첩 미스캔이라 안 걸린 것 아니냐」는 **확인해 보니 틀렸다** — selfcheck는
  중첩을 훑는다(4단 배열 안의 값도 잡았다). 안 걸린 값은 더 이상 0/단일이 아니어서다.

### 남은 사람 몫

- **DEFERRED 「web2-15 실기기 확인 표」 셋** — ⊠ 기호가 손에 읽히는가 · 8px 대역이
  실제 손에 맞는가 · 지우개 끝이 실제로 지우고 **잘못 지운 적이 한 번이라도 있는가**.
- ~~**선행 결함** `dim.spec` 인식기 감지(위) — 그리고 위 「모순」의 판정.~~
  ✅ **web2-16 1부가 해소** — 원인은 진단 패널의 포인터 삼킴(e224d82부터 · 수리
  `pointer-events:none` · e2e 176/0/176). 모순의 판정: **둘 다 «전량 통과 보고가 거짓»만 확정**이고 형태는 판별 불가다(a1b5bf8은 passed-만 또는 팔 오산(82팔×2=164), e224d82는 형태 미상 — `diag_occlusion_web2.json` misreports_found. 2부 2차 [2]가 초판의 «passed-만 확정»을 하향).
- ~~⚠⚠ **[살아 있는 결함] 대기선의 끝점·중점 오스냅이 획을 축선에서 밀어 정의를 조용히
  죽인다** — `edge_band` 297칸 중 **52칸**(짧은 획일수록 심하다: L40에서 28).
  web2-15는 `near` 통로만 막았다. **범위를 안 넓히려고 안 고쳤다** — DEFERRED 참조.~~
  ✅ **web2-16 2부가 해소**(사영 — DEFERRED 그 행에 수치·경로. 축 손실 52 → 0).
- **리뷰 대응 전건**은 NOTES 「리뷰 대응 (1차)」·「리뷰 대응 (2차)」 절.
  ⚠ **2차가 1차의 결론 셋을 뒤집었다**(위 ⚠⚠) — 1차 절만 읽으면 틀린 수를 인용한다.
  안 고치고 넘긴 것: #43 카운터(앱 진단) · #12(왜 8인가) · 위 [살아 있는 결함].
- 개정 3 초안 승인(여전히 대기 — 문서 교체 안 함, 지시 3 ⛔).
- web2-14 3차 리뷰 잔여 표(아래 옛 절) · web2-14 실기기 표 넷 중 미회신분.
- NOTES 「사람 손이 닿는 임계 — 훑은 목록」의 나머지 행(이번엔 목록만 남겼다 —
  `TAP_MAX_PX` 2px · 대기선 끝점·중점 오스냅의 축 밀어냄 · `ERASER_PX` 체감 크기).


## 🆕🆕 web2-14 (2026-08-27) — 기본값 전환 · 교점 정의 수리 · 감쇠 동결 · 고스트 수리 (**완료·배포 확인됨**)

**어디까지 배포됐는가: 전부.** 원격 main = **`c0faaafb1042f2335435c24870ca058e056e3451`**,
`deploy:check` 「✓ 화면 빌드 식별자 == 배포 커밋 / 결과: **배포됨**」 — **화면 빌드
식별자 `c0faaaf`**. ⚠ 이 기록 커밋이 다시 배포되므로 화면 해시가 한 칸 밀린다(web2-07
규약 — 그 뒤 커밋은 문서만이다. 정본은 위 해시). 남은 사람 몫: **DEFERRED 「web2-14 —
실기기 확인 표」 넷**(고스트 소멸+dpr 회신 · 교점 잇기(δ 가정) · 감쇠 셋 · 자립 기본) ·
3차 리뷰 잔여 표 · 개정 3 초안 승인(여전히 대기 — 문서 교체는 안 했다, 지시 5 ⛔).

넷 다 됐다(지시 순서 판단: 4번 먼저 — 질감이 이미 기본이라 모든 사람이 겪는 회귀):

- **4번(고스트)**: `#brushsnap`에 CSS 크기가 없어 dpr>1에서 스냅샷 겹이 dpr배로 깔렸다
  (canvas는 대체 요소 — `inset:0`이 못 늘린다). 수리 `fitSnap` CSS 명시. 회귀 팔
  `snapghost.spec`(원격/증인 상자 × 뷰변환 둘 × dpr 둘 — 원장 `snap_ghost_web2*.json`,
  수리 전 dpr2 실패 실행). ⚠ **실기기 dpr 미측정** — «사람 증상의 원인» 판정은 DEFERRED
  web2-14 표(dpr 회신)가 최종.
- **1번(기본값)**: own3d 기본 **켜짐**(localStorage 'off'만 끔 — 복원 전 읽기), 질감은
  web2-11부터 이미 기본(AS-C36 — 변경 없음). 팔 재구성: **켜짐 = 전량 정본**(단위·e2e가
  기본 그대로 돈다) · 꺼짐 = 명시 대체 팔 · **이행 팔**(own3 없는 옛 파일 → 열며 굳음).
  기본 전환이 드러낸 회귀 수리: 굳은 획의 나중 치수 입력이 무시됨 → lift 씨앗에 dim
  대체(멱등). DEFERRED 「기본값 전환」 행 닫음. 팔 구성 표는 NOTES 1번 절.
- **2번(교점 정의)**: 재현 — 손 오차 3px에서 ①A 3D ✓ ②닿음 ✓ **③왕복 문(0.5px)이 손
  오차를 거부**(missed.roundtrip — 표식). 수리 (a) 대기 획 그린 구간 near 오스냅(가시성
  = 기존 기호) (b) P = A의 3D 직선 ∩ B의 해석면(사영이 구성상 B 잉크 위). 진단
  「교점(마지막 획)」 단계 줄(①~④). `vptouch.test` 4팔·반증 둘. **PITFALLS #68 등재**
  (이상적 손 픽스처의 함정 — 손 오차를 심는다).
- **3번(감쇠 떨림)**: 갈래 ㉮ — 제스처(궤도·팬) 동안 판정 포즈 동결(`fadePose` ·
  `fadeRef` 단일 출처), 놓으면 재판정. 왕복 표시 변화 0회(단위 `navhold.test` ·
  e2e 배선 팔 — **반증 실제 실행**: 배선 제거 시 실패). 감쇠 자체 불변(3-a 팔 통과).
  알려진 대가: 드래그 중 한시 미끄러짐(NOTES 3번 절 — 근거 포함). AS-C44 부분 반증 기록.

지시 원문: [`docs/instructions/web2-14.md`](../docs/instructions/web2-14.md)
(⚠ 디스크 초판에 4번 절이 빠져 있어 채팅 전문으로 갱신하고 시작했다).
기록: NOTES 「web2-14」 절 넷(4→1·2→3 순) + 「2차 리뷰 대응」 절. 리뷰: 4번 12건 ·
1·2번 18건 · 3번 11건 — 전부 대응(재현·대조군·반증은 실제 실행. 부수 발견: 사슬 굳힘의
잉크 심판 둘째 예외 — AS-C47). **PITFALLS #68 등재** + selfcheck 등록부가 새 형식
(`### #NN.`)을 읽도록 강화. 실기기 판정 표: DEFERRED 「web2-14 — 실기기 확인 표」
(고스트+dpr 회신 · 교점 잇기(δ 가정) · 감쇠 셋 · 자립 기본).

검증(마감): 단위 **416**(43파일) · 타입 · 빌드 · e2e **164케이스**(82팔 × dpr 둘) 전량 —
**기본 = 자립 켜짐에서**. selfcheck: 새 원장 구조적 스냅샷 플래그(공통 형태) 외 없음.
원장: snap_ghost_web2* · wait_freeze_web2* 신설 · test_cost_web2 현행화 ·
own3d_invariant 재실행(값 동일 대역).


## 🆕 web2-13 (2026-08-27) — 개정 3 초안 · 승격 측정 · 구현 셋 · 자립 깃발 (**진행 중**)

**어디까지: 1~4부 전부 완료(리뷰 1~4차 대응 포함 · 배포 ① `daf3745` 화면 확인됨).
다음: 배포 ② 화면 확인 → 회차 마감. 남은 사람 몫: 실기기 판정(DEFERRED web2-13 두 표) ·
개정 3 초안 승인 · 자립 깃발 기본값 판단.**

- 4부 산출(전부 깃발 뒤·기본 꺼짐): `core/own3d.ts`(잉크 심판 own3Deviation·
  OWN3_TOL_PX 0.01 · camSig · defineByTouch) · `Stroke.own3` 선택 필드(.brnl 자동 —
  파서는 모양 틀리면 필드만 버림) · lift 씨앗(liftAll(doc, useOwn)) · recompute의
  굳힘(닫힘=constructionDone)·승격 사건(서명 변화 → 버리고 재굳힘) · 설정 «실험»
  체크박스+진단 「3D 경로」 · own3d.test 14팔(4-a/b/c/d/g — 반증 전부 실행) ·
  own3d.spec. **깃발 꺼짐 전후 동일**: 단위·e2e 전량 통과 + e2e가 다시 쓰는 값
  원장들이 4부 전후 바이트 동일(움직인 것은 시간 필드뿐 — 4차 [47] 증거).
  기본값은 안 켰다 — DEFERRED 「기본값 전환」 표가 사람 판정 대기.
  가정: **AS-C46**(닫힘 판정)·**AS-C47**(잉크 심판 허용 오차 — 예외 전수 미실시).
  원장 신설: `own3d_invariant_web2.json`(국면 넷 여유). 4차 리뷰(11건) 대응: 조각
  own3 승계([46])·교점 무산 계수([42] — 진단 「3D 경로」 줄)·알려진 대가 팔([40]).

- 3부 산출: 3-a 대기 획 시점 감쇠(core/waitfade.ts · 설정 토글 «대기 획은 그린
  시점에서만» · AS-C44) · 3-b 잘못 찍힌 점 문(isStray · C.STRAY_MIN_PX=6 실측
  `stray_gate_web2.json` · 진단 패널 카운터 · AS-C45) · 3-c btn-brush를 설정 «렌더러»
  절로(id·배선 불변). 검증: 단위 384/40 · 타입 · 빌드 · e2e 154/77×2 · selfcheck
  새 플래그 없음. DEFERRED 실기기 표 다섯 행.

- 2부 결과: `stage0/out/promote_freeze_web2.json` — **굳힌 3D는 승격을 못 살아남는다**
  (승격이 주점·f를 함께 옮긴다 — 작도 시점 어긋남이 판정선 2px의 백 배 대역).
  4-c 갈래 확정: 승격 가능 동안 사슬 · 카메라 닫힌 뒤 굳힘 · 승격 나면 다시 올려 굳힘.
  ⚠ 잰 승격은 P1→P2다(web2의 유일한 승격 — P2→P3 문면과의 갈림은 NOTES 2부 절).
  하네스: `web2/test/promote_freeze.test.ts`(오라클 둘·반증 셋 — 반증 설계 교훈 포함).

- 지시 원문: [`docs/instructions/web2-13.md`](../docs/instructions/web2-13.md)(작업 전 커밋 87e9d18).
  갈래 `web2-13`. **5부 구성·배포 둘**(3부 뒤 ① · 4부 뒤 ②). 4부는 깃발 뒤·기본
  꺼짐 — **기본값은 사람만 켠다.**
- 1부 산출물: [`docs/line_plan_r3_draft.md`](../docs/line_plan_r3_draft.md)(**초안 —
  승인 대기.** 현행은 여전히 개정 2) · DECISIONS.md **D-W2**(제도 매체 — 실재한 적
  없던 참조 자리를 채움)·**D-W3**(개정 3 요약) · AS-C41 ⚠ 병기 · NOTES 「작도 획
  지우개」 되돌릴 조건 발동 표시.
- 초안 §9.1은 **2부가 채웠다**(판정선은 1부 커밋 4d04f85에 사전 등록 — 측정 커밋
  52bd484보다 먼저. «안 맞는다 → 자립은 카메라 닫힘 후» · §9.2 닫힘 판정 · §9.1
  끝의 ⚠⚠ 알려진 대가(사슬 끊긴 굳힘 × 승격)까지가 현행 내용이다).
- 기록: NOTES 「web2-13 · 1부」 절(착수 PITFALLS 대조 · 리뷰어 13건 — 12 대응·1 반박
  [11] · 날조 미수 자기 기록 포함).



**어디까지 배포됐는가**: ① `7be179e`(1부 — 잉크 소실 수리·미리보기 brush) ·
② `2cc161e`(2부 — 세로바 한 규칙·비우기 확인 곁·뷰 썸네일·연필통) — 둘 다 화면 빌드
식별자 확인됨. **③(3부 — 지평선 토글·모서리 넘김·잉크 번짐·종이 결)**의 해시·확인은
NOTES 「배포 ③ 확인」 절이 정본이다(이 줄이 그 절보다 먼저 쓰였으면 그 절을 본다).
지시 원문: [`docs/instructions/web2-12.md`](../docs/instructions/web2-12.md).
기록: [NOTES.md](NOTES.md)의 web2-12 절 전부(리뷰 3회분 대응 포함).

**다음 사람이 알아야 할 것**:
- **1번의 진짜 원인은 «흰 장막»이었다**(#67): p5.brush의 규약 위반 알파가 #gl 전체를
  덮었다 — 수리는 straight alpha(`withStraightAlpha`). 합성 화면을 재는 팔(materials.spec)
  과 겹 알파 규약 단언이 재발을 지킨다.
- **미리보기가 확정과 같은 렌더다**(D-W1 개정): 질감 #brushc(draft 전용 모드 — 확정은
  #brushsnap 스냅샷) + 몸체 Line2(`setDraftLine` 역사영). 뗌 게이트(draftgate.spec)가
  전 재료 0 diff를 지킨다. 시드 = 잠정 id = `app.nextId`.
- **세로바 규칙의 단일 출처는 `:root` 변수 셋**(--ui-scale·--hit-pad·--btn-gap) —
  값·겹침·높이는 `sidebar_layout_web2*.json`. 연필통(가로 눕힘)이 4-e 슬라이더를
  대체했고 옛 경로는 #oldtools(hidden)+TRAY 상수로 남아 있다(A-4 — 사람이 승인하면 지운다).
- **제도 표현은 전부 표현 계층이다**: 넘김(core/overshoot — 만나는 끝만·7px 화면 고정) ·
  잉크 번짐(inkFlow — raw 유도·시드 id) · 종이 한 장(CSS 마스크 — 위상 문서·결 화면).
  기하 불변은 배열 핀 팔·오스냅 판이 지킨다. 치수 틱(11번)은 **대상 부재**(AS-C42).
- **실기기 판정 표**: DEFERRED의 web2-12 표 셋(1부 여섯·2부 일곱·3부 다섯 행).
  가정: AS-C37~C43 신설 · AS-C22·C30·C35·C36 정정.

검증(최종): 단위 **373**(37파일) · 타입 · 빌드 · e2e **146**(73팔 × dpr 둘) · selfcheck
(신설 원장 스냅샷 플래그 — 구조적 — 외 없음). 리뷰어 셋(14+17+15+17건 — 전부 대응, NOTES).
PITFALLS: **#67**. 결정: D-W1 개정 · 뷰 삭제 확인·비우기 확인 곁(confirmNear).


## 🆕 web2-11 (2026-08-26) — 손을 그대로 받고, 종이처럼 그린다 (**1·2부 전부 배포됨**)

**배포 둘 다 확인됨** — ① 입력 계층 `e2e5e91` · ② brush 렌더러 = 최종 원격 main
**`b70b65566e4aca4f445c2928641b2ac5db87851f`**, 화면 빌드 식별자 **`b70b655`**,
`deploy:check` 「배포됨」. 지시 원문:
[`docs/instructions/web2-11.md`](../docs/instructions/web2-11.md). 기록: [NOTES.md](NOTES.md)의 web2-11 절들.

**1부(입력 계층 — `e2e5e91`)**: 화면 불변(판별은 묶음 패치 팔) 하에 데이터만 확장 —
coalesced 수집(`app.coalesce` D-3 손잡이) · tilt/twist 관측 · **점별 필압 `Stroke.rawIn`**
(raw와 나란한 평행 배열·펜만·press 0..8191 `C.PRESS_Q`·`mat.press` 비트 불변) · 지우개 끝
신호 표시만(D-4) · .brnl 새 필드 전부 선택(옛 파서 b6980c9 스냅샷 팔 — ⚠ 옛 앱이 새 파일을
재저장하면 rawIn 소실) · 진단 패널 확장(날값·coalesced 계수·「단계 N·양자화 후 M」·.brnl 바이트).

**2부(brush 렌더러 — `b70b655`)**: p5.brush **2.2.2 표준 빌드**(p5 불요·WebGL2·MIT —
LICENSE 표기)가 `#brushc` 겹(gl↔ink 사이)에서 **획만** 그린다(D-W1: 「2D는 내가 그린 것,
3D는 확정된 기하」 — Line2·작도선·표식·미리보기 불변 ⚠ **미리보기 몫은 web2-12 2번이 뒤집었다**(D-W1 개정 — draft도 brush+Line2로 그린다)). **기본이 brush다**(AS-C36).
되돌리기: 세로바 「종이 질감」 버튼(classic 경로 전부 생존 — grain은 분기로만 꺼짐 2-e ·
진단 패널 「렌더러」 줄). 매핑: 2H/HB/2B/rotring 직결, **H·F·B는 이웃 질감 + MAT 진하기**
(AS-C34 — 렌더 어두움 여섯 급 단조를 e2e가 잰다). 필압: 1부 rawIn → spline 12표본.
계약 셋: 화면 고정 굵기(화면 좌표로 그림) · widthOfMat 단일 출처 · **시드=획 id**(표준
빌드 seed API 실측 — «못 하면 멈춤» 미발화 · 같은 기하·다른 id → 다른 픽셀 반증까지).

**다음 사람이 알아야 할 것**:
- **그리는 중 brush 겹은 «전량» 재그리기 0회**(캐시 카운터 — ⚠ web2-12 2번부터 미리보기가
  이 겹에서 **한 획** 재그리기로 돈다: 확정 획은 스냅샷 겹이 들고, 몸체는 Line2가 그린다.
  «미리보기는 ink 종전 경로»는 그 회차로 뒤집혔다 — D-W1 개정). **궤도·줌 중에는 프레임마다 전량 재그리기** — 500획이면 무겁다(실기기
  「돌릴 때 끊긴다」의 원인 후보 1번 — DEFERRED 2부 표).
- **측정 수치를 문서에서 찾지 마라** — `stage0/out/brush_perf_web2.json`·
  `brush_visual_web2.json`이 정본이고 e2e 실행마다 다시 써진다. 문서에 옮겨 적으면 낡는다
  (#42 ⑥·#47 — 이 회차가 세 번 데었다. 그 형태 자체는 **PITFALLS #66**으로 등재).
- ⚠ **brush 획의 칠해진 픽셀은 전부 불투명**(AS-C35) — 옅은 획도 뒤의 3D를 픽셀 단위로
  가린다(classic grain은 반투명이었다). 화면 판정은 실기기 몫.
- **오프라인은 이 회차 미실측**(sw.js 런타임 캐시 구조 서술만).
- **실기기 판정이 최종이다**(지시 문면): DEFERRED web2-11 두 표(1부 다섯 행 — coalesced
  계수·필압 단계·기울기·지우개 펜 3E·저장 크기 / 2부 다섯 행 — 따라오는가·종이 같은가·
  경도 차·줌·오래 그리기). 도구는 전부 진단 패널(우하단 빌드 식별자 탭).

검증(최종): 단위 **366**(36파일) · 타입 · 빌드 · e2e **116**(58팔 × dpr 둘) · selfcheck
(구조적 12 + 원인 확인 1 외 없음). 리뷰어 넷(1부 12+14 · 2부 18+13 — 전부 대응, NOTES).
가정: **AS-C33~C36 신설**. 결정: **D-W1**. PITFALLS: **#66**.

## 🆕 web2-10 (2026-08-26) — 표시 마감 · 진단 · 색 빼기 · 치수 입력 (**완료**)

**여덟 항목 전부 배포됨** — 최종 원격 main = **`b6980c95bbb5e0cb3d4d55e6cb3b4e2528d9f839`**,
화면 빌드 식별자 **`b6980c9`**, `deploy:check` 「배포됨」. 배포 넷:
① `19a37f4`(지시 1·2·3 — short_name «Brunelleschi»·인라인 로딩화면·크레딧/LICENSE 실명)
② `6c5b6f1`(지시 4~7 — **진단 패널**(빌드 식별자 탭)·세로바 1.5배(시점바 둘째 열·큐브
cx W−110 연쇄 이동)·오스냅 2H·**축 스냅 무채색 파선**(PITFALLS **#65** 등재))
③ `05ea816`(지시 8-a — **숫자 키패드 = 치수의 확정 경로** + 확률 입력(필기·음성)
**스테이징**(자동 적용 폐지 — web2-08 규칙 뒤집힘·AS-C24 필기 ⛔ 반증))
최종 `b6980c9`(지시 8-b — **인식기 교체**: 기능 감지로 내장 API → **digitnet**(784-64-11
MLP·int8 68KB·직접 학습 — 가중치만 CC BY-SA 3.0, LICENSE 예외). $P는 하네스로 남음).

**다음 사람이 알아야 할 것**:
- **인식 결과는 자동 적용되지 않는다** — 키패드 표시로 스테이징되고 «적용»을 눌러야
  실린다(지시 8-a ② — 조용히 틀린 scaleRef를 막는 자리).
- digitnet의 안전 불변은 **오독 0 · 잡음 수용 0**(합성 4시드 팔) — 확신 임계(0.52)는
  잡음 차단이 아니라 **불확신 기권** 손잡이다(분리 부호가 시드에 따라 뒤집힌다 — AS-C32).
  배포 구성의 MNIST 수: 맞음 9483 · 기권 325 · 오독 192 / 10000.
- **short_name을 바꿔도 이미 설치된 앱 라벨은 «Brunel» 그대로다**(설치 시점에 굳는다 —
  재설치해야 새 라벨).
- **web2가 처음으로 `stage0/out`에 쓴다**: digitnet_mnist · digit_accuracy_web2 ·
  test_cost_web2(비용 원장 — DEFERRED 24행 갱신됨). selfcheck의 상수 스냅샷 플래그 6건은
  구조적(web/ 등록부 규약 미적용 — 원인 확인 기록).
- 실기기 확인 목록은 **DEFERRED의 web2-10 표**가 정본(1.5배 손 판정·로딩 흐림·오스냅
  가시성·축 파선 판독·네 축 절대 식별·키패드 크기·digitnet 실필기 — AS-C28~C32).

검증(최종): 단위 **352** · 타입 · 빌드 · e2e **88**(44팔 × dpr 둘, 63s) · selfcheck(구조적
6건 외 없음). 리뷰어 넷(12+17+15+18건 — 전부 대응. 기록은 NOTES 각 절).
지시 원문: [`docs/instructions/web2-10.md`](../docs/instructions/web2-10.md).
**배포 지점이 셋인 회차다**(지시 머리 — 사람이 없는 동안 돌므로 뒤가 터져도 앞은 손에 있게).
지시 원문: [`docs/instructions/web2-10.md`](../docs/instructions/web2-10.md).
기록: [NOTES.md](NOTES.md)의 web2-10 절.

- ⚠ **short_name을 "Brunelleschi"로 바꿨지만(지시 1) 이미 설치된 앱의 홈 화면 라벨은
  «Brunel» 그대로다** — 안드로이드가 설치 시점 라벨을 굳힌다. 「안 먹었다」가 아니다.
  새로 설치해야 새 라벨이 보인다.
- 실기기 관측 회신: web2-09 PWA 판정 넷(설치·전체화면·핀치·아이콘) 전부 통과 —
  DEFERRED web2-09 표의 세 행을 닫았다.

## 🆕 web2-09 (2026-08-26) — PWA 표시를 다듬는다

**배포 확인됨(코드 정본 `d594214`)** — 원격 main = **`d5942146e41812ad19cc9cd76bd24550c6979349`**,
`npm run deploy:check` 「배포됨」(3회째 — 앞 둘은 옛 해시 `2e0474e`, Pages 전파 관행 그대로),
**화면 빌드 식별자 `d594214 2026-08-26 02:26`**(실브라우저로 읽음 — theme-color·매니페스트
링크·html touch-action:none까지 실배포에서 확인). 이 기록 커밋은 **문서만**이라 그 뒤 화면
해시가 한 칸 밀려도 앱은 같다(web2-07 규약).
이 세션의 main 커밋: `71969da`(원문) `ca5cc17`(지시 0) `0d93864`(지시 1·2·3) `60912fe`(지시 4)
`649259c`(기록) `85b6185`(1차 대응) `4629210`(2차 대응) `af9173d`(인계) `d594214`(병합).
지시 원문: [`docs/instructions/web2-09.md`](../docs/instructions/web2-09.md) —
**「원문을 먼저 둔다」 규약을 처음 지킨 회차**다(작업 전 커밋).
기록은 [NOTES.md](NOTES.md)의 「web2-09」 절. PITFALLS 등재 없음(사유는 NOTES —
교훈 둘이 D-2·D-3의 적용 사례라 새 규칙이 아니다). 가정: **AS-C25·C26·C27 신설**.

**된 것**: ① web2-08이 작업 트리로만 남긴 **아이콘 여섯 커밋**(매니페스트 유효·경로·
purpose·실크기 실측) ② **전체화면·표시줄 메타**(theme-color·apple 셋 — iOS는 선언만,
장치 없어 미확인) ③ **브라우저 줌 방어층**: html/body touch-action none(뿌리 겹) +
이벤트 보험(두 손가락 touchmove·gesture*·Ctrl+휠만 preventDefault) ④ **회귀 채널**:
`e2e/pwa.spec.ts` 6팔(선언·유효 사슬 25점·계측기 생존 반증·이벤트 보험·앱 줌 생존
view.s 1→3.111·펜 획 커밋) + `static_deploy`에 배포본 매니페스트 팔(파싱·display·
start_url·scope·name·아이콘 다섯 200·PNG 실크기=선언 sizes) ⑤ 낡은 주석 수정.

**⚠ 다음 사람이 알아야 할 것 셋**:
- **보고된 증상(핀치가 브라우저 줌 · 전체화면 안 됨)은 둘 다 헤드리스에서 재현이 안 됐다.**
  메타·CSS를 다 벗겨도 합성 핀치가 visualViewport.scale을 못 움직인다 — 그 계측은 폐기했고
  (D-3) 이 회차의 수리는 전부 **발화한 적 없는 방어층**이다. 판정은 실기기에서 난다.
- **실기기(MovinkPad Pro 14) 확인 항목의 정본은 DEFERRED.md의 web2-09 표**다(16행 —
  UA·전체화면·핀치·이중 탭·dpr·TAP_MAX_PX·PARALLEL_PX·표식 7px·필압·손가락 면·maskable
  잘림·theme-color·패널 스크롤·iOS). 핀치가 여전히 새면 **접근성 「강제 확대」·비-크롬·
  데스크톱 사이트 모드** 순으로 가른다(AS-C25 — 이 열거의 완전성 근거는 없다).
- **패널 터치 스크롤은 막혀 있다**(pan-y를 관측 없이 열었다가 되돌렸다 — AS-C26).
  내용이 화면 안에 드는 동안 대가 0. 잘린다는 관측이 오면 그 행이 열 조건이다.

**검증**: 단위 348 · 타입 · 빌드 · e2e **60**(46 + 이 회차 14, dpr 둘) · selfcheck(새 플래그
없음). 리뷰어 2회(13건 → 10건, 전부 대응 — [1] 마감만 이 절 아래 배포 확인으로 남았었다).


## 🆕 web2-08 (2026-08-26) — 그리기 도구 UI 넷 (시점 스냅 · 지평선 · 오스냅 · 치수)

**배포 확인됨** — 원격 main = **`105835f072de78e48f2d8c23883fad78d8d7283b`**, **화면 빌드 식별자 `105835f`**,
`npm run deploy:check` 「배포됨」. 지시 원문: [`docs/instructions/web2-08.md`](../docs/instructions/web2-08.md).
기록: [NOTES.md](NOTES.md)의 web2-08 절 넷(지시 3 → 1 → 2 → 4 순서로 진행했다 — 지시의 순서다).

**된 것 넷**:

1. **시점 스냅(지시 3)** — «무조건 접기»(web2-04)를 뒤집었다. 접히는 것은 임계
   `snapAngle(f,W) = min(atan(f/6W), 8.25°)` **안**의 자세뿐이고, 밖(탑뷰·저면·큰 기울기·
   기울여 저장한 시점)은 **머무는 상태**다. 요가 그 차수 프레임의 가로축과 임계 안이면
   **1점으로 정확히 붙는다.** 머무는 자세에서는 그리기·지우개가 열린다(`s.view` 경로 —
   사영 일치 6자리·조각 재리프팅 1e-6 팔). 판정은 `core/level.ts`의
   `foldTarget`/`pitchSnaps`/`snapAngle` 한 자리다. **AS-C21**(임계 미검증 — 실기기 표본 0).
2. **지평선(지시 1)** — 불투명 작도색에서 **경도표 2H 그대로**(색·알파 0.50·1.1px)로.
   e2e가 «띠의 열당 잉크량»으로 잰다(수리 전 382/319 → 128/128 · 상한 250은 h~2h 문면
   전체 통과). **AS-C22**. ⚠ 소실점 ✕는 종전 색 그대로라 지평선보다 진해졌다(`DEFERRED`).
3. **오스냅 기호(지시 2)** — `#1a9c50`(초록) → 무채색 `#555`(`COL.osnap` — 면 미리보기의
   `COL.snap`과 상수를 갈랐다). 종류는 형태(□◆△✕⊥▫○)가 가른다. 채도 픽셀 팔(131 → ≤12).
   **AS-C23**. 축 색 넷·면 미리보기 초록은 문면대로 남겼다.
4. **치수(지시 4)** — 4-1~4-8 전부. **스케일은 파생이다**(원칙 b): 문서 순서상 첫 치수
   획의 `dim ÷ 무치수 풀이 길이`를 `lift.ts`가 2패스로 계산 — 저장하던 초안은 «2500»을
   획마다 쓰는 도중 첫 «2»가 굳었다(e2e가 잡았다). 치수는 시작점·방향만 취하고 길이를
   대체한다(`Stroke.dim`). 길이 계산은 `core/dim.ts` 하나(미리보기 == 확정 3D == 패널 —
   팔·e2e). 우측 접이식 리본: ~~$P 필기 인식~~ **⛔ web2-10에서 교체**(실기기가 $P를 반증 — AS-C24. 지금은 감지 선택: 내장 API → digitnet MLP, `core/handwriting.ts`. $P는 하네스로 남음. 인식 결과는 자동 적용이 아니라 **스테이징**이다 — 8-a ②) · **음성**(Web Speech ko-KR — 역시 스테이징 — 「삼천오백」·「3.5미터」·「이백오십
   밀리」 문면 팔) · 단위 mm/cm/m(문서 저장) · **치수 스냅**(기본 꺼짐 — 켜면 실제 3D
   길이가 눈금 배수) · 무한소수 표기 옵션. **AS-C24**(실필기·실음성 표본 0).

**뒤집힌 옛 규칙** (다음 사람이 옛 문서를 읽을 때):
- web2-04의 「정렬 아닌 포즈는 전부 접힌다 · 기울면 획 금지(그리기도 지우기도)」는
  **임계 안으로 좁혀졌다.** NOTES web2-04 절에 ⛔ 제자리 표시가 있다.
- 「접기가 요를 그대로 두므로 1점은 저절로」(web2-04) → 이제 임계 안이면 **스냅으로
  정확히** 붙는다.
- 뷰 큐브의 비정렬 18시점이 «1.2초짜리»였던 것 → 이제 머문다(DEFERRED에 ⛔ 정정).

**다음 사람이 화면에서 볼 것**:
- **머무는 자세에서 그은 대기 획**은 다른 자세로 돌리면 옛 화면 좌표에 흐림(0.3)으로
  남는다(`own` 규칙 — DEFERRED). 그 자세에서 «작도»(소실점)는 여전히 못 만든다.
- 시점 스냅 임계(≈8.25°, 광각에선 좁아짐)가 손에 맞는가 — AS-C21의 되돌릴 조건 셋.
- 필기 인식이 실펜 글씨를 읽는가 — 못 읽으면 «?»가 뜨고 적용 안 된다(다시 쓰기).
  «?»가 잦으면 AS-C24의 되돌릴 조건(오독부터 잰다).
- 치수 스냅 눈금은 스케일과 같은 자릿수여야 뜻이 있다(1mm 기둥에 10mm 눈금은 끝점을
  소실점 대역까지 민다 — e2e 주석).

**수 회계**: 단위 30 파일·306건(HEAD 실측) → **33 파일·348건**. e2e 17팔 → **23팔**(×2 dpr = 46/46). 리뷰어 셋(지시 3: 1차 15건·2차 17건 — 임계를 min(atan(f/6W), 8.25°)로 간 것이 1차 [1][2]다 · 지시 1·2: 16건 · 지시 4: 20건 — scaleRef가 그 [5]다) 대응 기록은
NOTES 각 절. 수치는 `stage0/out` 원장 밖(#25 — web2 공통 사유. 재실행 통로가 각 절에 있다).

**열린 것**: `DEFERRED.md`의 web2-08 절(✕ 진하기 · 흐린 대기 획 · 작도 불가 노출 ·
치수 실행취소 · 스케일 획 삭제 · 단위 어휘).

## 🆕 web2-07 (2026-08-21) — 면 생성

**배포 확인됨** — 원격 main = **`e1c26cf819ba3b47d855f45903b8910444ed5d08`**,
**화면 빌드 식별자 `e1c26cf`**, `npm run deploy:check` 「배포됨」.
면 생성 코드가 처음 나간 커밋은 `4000831`이고, 그 뒤 `e7fb75f`·`b7dcbbc`는 **문서만**이다.
(⚠ 병합 직후 두세 번은 화면이 옛 해시였다 — Pages가 도는 데 약 1~2분.)
⚠ **이름이 밀렸다** — 사람이 「web2-06」으로 부른 회차가 둘이라(그리기 결함 다섯 · 면 생성)
다른 세션이 먼저 그 이름과 **PITFALLS #61·#62**를 썼다. 이 회차가 **web2-07 · #63·#64**다.
기록은 [NOTES.md](NOTES.md)의 「**web2-07**」 절. 등재: **PITFALLS #63 · #64**.
지시 원문: [`docs/instructions/web2-07.md`](../docs/instructions/web2-07.md).

**된 것**: **면 도구**(세로바 맨 아래, 평행사변형). 둘러싸인 자리를 탭하면 면이 되고
**다시 탭하면 없어진다**(토글 하나). **자동으로 안 만든다** — 닫힌 루프가 생겼다고 면이
아니다. 커서를 올리면 «지금 탭하면 무엇이 될지»가 미리 보인다(만들면 초록·없애면 파랑).

**구조가 셋으로 갈려 있다**(`src/core/face.ts`): **찾기**(평면마다 최소 순환) →
**저장**(획 id의 차례. 좌표를 안 담는다) → **풀기**(이웃한 두 경계의 3D 직선 교점이 정점).
그래서 ① 끝점이 정확히 안 만나도 되고(삐져나온 교차가 모서리다) ② **차수 승격으로 전부
다시 올라가도 면이 따라온다** ③ 곡선이 오면 «풀기»의 두 함수만 는다.

**이 회차가 고친 것 둘 — 둘 다 면 밖의 결함이다**:
1. **`lift.ts`가 사람이 붙인 끝점을 덮어썼다**(#63). 축 풀이가 끝점 매칭보다 앞서서
   오스냅으로 붙인 `(500,520)`이 `(503.409,510.455)`로 밀렸다 → **루프가 안 닫혔다.**
   지금은 `draft.ts`와 같은 순서다(점이 방향을 이긴다). ⚠ **면과 무관한 곳에도 영향이 있다** —
   축 방향 획의 끝이 기존 3D에 붙으면 이제 그 좌표를 쓴다.
2. **가림이 평면 그래프 전제를 깼다**(#64). 세로 기둥이 지면선을 화면에서만 가로질러
   **벽도 바닥도 면이 안 됐다**(마디 10·변 11인데 순환 1개·면적 0). 지금은 **평면마다** 훑는다.

**다음 사람이 화면에서 볼 것**:
- **면 위에 그리면 어떻게 되는가** — 지금은 아무 일도 안 난다(면은 스냅 대상이 아니다).
  `DEFERRED.md`의 「개구부 앱 경로」가 그 자리와 이어진다.
- ⚠ **면끼리 서로를 안 가린다** — 뒤엣면이 비쳐 보이는데 **탭은 앞엣것을 집는다.**
  벽 뒤 바닥이 보이는데 탭하면 벽이 잡힌다. 그 어긋남을 알고 뒀다(`DEFERRED.md`).
- ⚠ **손가락으로는 면을 못 만든다**(손가락 하나는 궤도다). 펜·마우스는 된다. 실기기 미측정.
- **면 색은 무채색 `#8d8880` · 불투명도 0.22**다(상시 표시라 색을 안 준다 · 4-c).
  동작점 하나이고 눈으로만 골랐다.
- **`C.PLANAR_RATIO` 0.01** — **AS-C18**. 값의 근거는 「`INTERSECT_GAP_RATIO`와 같은 물음의
  앞뒤」이고 분모가 같은 양임을 팔이 잰다(장면 10배 → 두 경계 다 10배).
  ⚠⚠ **이 임계는 지금 아무것도 안 거른다** — 탭으로 만든 면은 구성상 한 평면이고 승격도
  안 접었다(이탈 2.0e−17). **방어층이고 한 번도 안 열렸다**(#35).
- **면 색 `#8d8880` · 불투명도 0.22** — **AS-C20**. 눈으로만 골랐고 스윕이 없다.
  겹치면 두 배로 진해진다(면끼리 깊이를 안 쓴다).
- **「겹쳐 보이면 가까운 쪽을 집는다」** — **AS-C19**. A-3(3D 도구 관행)을 따른 것이고
  이 저장소의 관측이 아니다.

**⚠ 다음 사람이 먼저 부딪힐 둘**(2차 리뷰어 [14]):
- **호버 비용을 안 쟀다.** `planesOf`가 마디 차수²를 훑고 평면마다 변을 다시 훑는데
  그것이 **커서를 올릴 때마다** 돈다. 획 수 대 1회 ms를 아무도 안 쟀다(16 ms = 한 프레임).
- **비용 원장이 이 회차 몫만큼 낡았다**(#38) — 단위 26건·e2e 8건을 더하고 `stage0/out`에
  아무것도 안 남겼다.

**열린 것**: `DEFERRED.md`의 web2-07 절 다섯.
가장 큰 것은 **개구부의 앱 경로**다 — 떨어져 있는 안쪽 고리를 3D로 못 올린다(lift의 문제이지
면의 문제가 아니다). 면 쪽 기전은 전부 되어 있고 합성 lift로 잰다.
⚠ **web2-05가 남긴 「접힌 뒤 지평선이 안 그려진다」는 그대로다** — 이 회차가 안 건드렸다.
## 🆕 web2-06 (2026-08-21) — 그리기 결함 다섯

**배포 확인됨(정본)** — 원격 main = `899238f`, **화면 빌드 식별자 `899238f`**,
`npm run deploy:check` exit 0. 그 커밋이 **리뷰어 두 차례 대응까지 포함한 상태**다.
- 다섯 지시만 담은 첫 배포는 `983be24`였고 그것도 화면에서 확인했다(빌드 식별자 `983be24`).
- 두 번 다 **병합 직후 세 번은 옛 해시**가 보였다 — Pages가 도는 데 약 1분. `?reset`이 캐시와 가른다.
- ⚠ 이 기록 커밋 자체가 다시 배포되므로 **화면은 곧 이 커밋 해시를 보인다**(web2-05와 같은 형태).
  정본은 위 `899238f`이고, 그 뒤 커밋은 **문서만** 바꾼다. 지시 원문: [`docs/instructions/web2-06.md`](../docs/instructions/web2-06.md).
기록은 [NOTES.md](NOTES.md)의 「**web2-06** · 다섯」 절(지시 1~5). 등재: **PITFALLS #61·#62**.
가정: **AS-C15 반증** · **AS-C16·AS-C17 신설** · **AS-C14 관측됨(줌 쪽만 열었다)**.

| # | 증상 | 고친 자리 | 실측 |
|---|---|---|---|
| 1 | 소실점에서 뻗는 획이 **지평선 위로 눌린다** | `camera.ts` `vpAt()` + `draft.ts` 갈래 ② | (900,400)→(700,600)이 끝점 **(700,400)** → (700,600) |
| 2 | 지평선을 긋고 **수평선을 스냅으로 못 긋는다** | `VP_FAR_W = 6` (경계 = `min(6W, h·R/1.5)`) | 처짐 2px에 **33W 소실점** → 흡수 1.5px → **11.1px**(h=200·R=400) |
| 3 | **접힌 뒤 지평선이 안 그려진다** | `horizonScreenY()` — 작도 포즈가 아니라 **정렬**이 조건 | 지평선 띠 픽셀 **26 → 2420** |
| 4 | 궤도 중심이 **연필 구축선에 끌려간다** | `orbitPivot` = 펜(잉크)의 **경계 상자 중심** · 펜이 없으면 **종전 무게중심** | 반경 **17.335 → 7.274** |
| 5 | **줌한 궤도 반경이 접으면 사라진다** | `levelPose`가 반지름을 «지금 값»으로 맞춘다 | 줌 3.613 → 접은 뒤 **7.225 → 3.613** |

**규칙이 바뀐 자리 셋** — 다음 사람이 알아야 한다:
- **먼 소실점은 6W까지만 «그어서» 만든다**(그 밖은 지평선 위를 **찍는다**). web2-03이 열어 둔
  8W·10W·20W 대역이 닫혔다 — `farvp.test.ts`의 조항 둘에 ⛔ 표시와 뒤집힌 값이 박혀 있다.
  ⚠ **H로 읽힌 뒤에는 찍기도 안 받는다**(1점 잠금) — 순서가 먼저다.
- **접기가 새로 쓰는 값이 둘**이다: 요 + **pivot까지의 거리**. 피치·롤·베어링은 앵커 그대로이고,
  ⚠ **눈높이는 앵커 그대로가 아니다** — 반경에 걸린 배율을 함께 탄다(`pivot.y + (anchor.y−pivot.y)·k`).
  줌이 없으면 k = 1이라 그대로다(2차 리뷰어 [U4]가 이 줄과 아래 줄이 서로 반대인 것을 잡았다).
- **궤도 중심이 펜에 붙는다.** ⚠ **펜이 없을 때는 종전 규칙(무게중심) 그대로**다 —
  초판은 그때도 경계 상자였고 반경이 17.335 → **52.087**(3배)이 됐다. 그 방향은 사람이
  보고한 증상과 같아서 1차 리뷰어 [9]가 잡았고, 「연필로 **대신한다**」를 «종전 규칙으로
  돌아간다»로 읽었다(`test/pivot.test.ts`가 두 수를 다 낸다).

**다음 사람이 화면에서 볼 것**:
- **소실점에서 뻗어 그린 선은 3D로 안 올라간다**(시작점이 무한원이라 리프팅할 점이 없다).
  연필 구축선으로 남는 것이 맞는데, **화면에서 그것이 «실패»로 보이지 않는지** 봐야 한다.
- **수평선을 그을 때 어디까지 붙는가** — 흡수 한계가 `h·R/(6W)` px다. 지평선 가까이서 짧게
  그으면(h=40·R=300 → 1.67px) **여전히 안 붙는다.** 그 구멍은 팔에 박혀 있다.
- **접힌 뒤 지평선**이 그림과 안 싸우는가(무채색 얇은 선 하나가 화면을 가로지른다).
- **펜을 처음 한 획 그은 순간 궤도 중심이 튄다** — 연필 전체에서 펜 하나로 옮겨간다.
  그 전환이 놀랍지 않은지.
- **줌해서 맞춘 거리가 접힌 뒤에도 남는다** — 그때 **눈높이는 pivot 기준으로 같은 배율**을
  탄다(`pivot.y + (anchor.y − pivot.y)·k`). pivot이 눈높이에 있으면 그대로이고, 아니면
  다가간 만큼 pivot 쪽으로 움직인다. **정렬 포즈에서 그냥 줌해도 같은 일이 난다** —
  그래서 #60의 재발이 아니다(팔 있음). 실기기 확인은 없다.

**안 만든 것 / 안 연 것**:
- 지시 5에 **새 컨트롤을 안 만들었다** — 조절 수단(휠·두 손가락)은 이미 있었고 없던 것은
  «남는 것»이었다. 되돌릴 조건: 손가락으로 반경을 못 맞춘다는 관측.
- **궤도 중의 «팬»은 여전히 접기가 되돌린다** — 줌과 달리 불변량 논증이 안 선다.
  새는 몫만 수로 적었다 — 팬 2.133에 반경 **7.225 → 7.012**, 그리고 **눈높이 1.600 → 1.562**
  (200px 팬이면 1.879까지). **«안 열었다»가 «안 샌다»가 아니다.** `DEFERRED.md`.
- **기운 포즈의 지평선**은 여전히 안 그린다(직선의 사영이라 수평 방향 둘의 소실점을 이어야 한다).

**검증**: 단위 **279건**(29파일 — 1차 리뷰어 대응으로 +4) · 타입 · 빌드 · e2e **26건**(dpr 둘 — 이 회차가 **+4**:
지평선 픽셀 · 궤도 반경 · `draw.spec.ts`의 지시 1·2) ·
`fold_measure` 원장(줌 행 아홉 추가) · `python selfcheck.py`(새 플래그 없음 ·
`scan_pitfalls_table_last` 0건).

⚠ **리뷰어를 항목마다 안 부르고 다섯을 한 번에 불렀다**(CLAUDE.md §4는 「각 항목 완료 시」다).
컨텍스트를 아끼려고 그랬고, **규약과 갈린 것이므로 적어 둔다.**

---

## 🆕 web2-05 (2026-08-21) — 접을 때 궤도 전체를 되돌린다 + 소실점 스냅 표식

**배포 확인됨** — 원격 main = `d08f245eb1a376ded0d3fb33b9b536359666e39c`,
**화면 빌드 식별자 `d08f245`**, `npm run deploy:check` exit 0.
(⚠ 병합 직후 세 번은 화면이 `96ab66d`였다 — Pages가 도는 데 약 1분.)
지시 원문: [`docs/instructions/web2-05.md`](../docs/instructions/web2-05.md).
기록은 [NOTES.md](NOTES.md)의 「**web2-05**」 절. 등재: **PITFALLS #60**.

**증상 ①**: 정렬은 되는데 **지평선이 화면 위로 심하게 올라간다.**
표식이 그대로 냈다 — 궤도 전 눈높이 **1.600** → 궤도 후 **6.590** → **접은 뒤에도 6.590**,
거리는 7.191 → 9.926. **궤도로 올라간 높이가 남은 것**이다.

**web2-04가 틀렸다**(사람이 뒤집었다): 「눈높이를 유지한다」로 정했는데 **그 눈높이는
궤도가 만든 부수 효과**다. → **PITFALLS #60**.

**지금 규칙**: 접기 = **앵커를 pivot의 수직축 둘레로 요 차이만큼 돌린 강체 회전**.
앵커 = **정렬 상태를 떠나기 직전의 포즈**(궤도·뷰 큐브·저장 시점이 한 규칙).
높이·거리·피치·롤·베어링이 **구성상** 보존되고 **요만 새 값**이다.
정렬 상태에서 사람이 옮긴 것은 의도이므로 앵커가 따라간다(지시 d).

⛔ **그 대가로 web2-04의 「물러나기」 기전이 통째로 죽었다** — 팔 다섯과 하네스 하나를
**지웠다**(#57·#56). web2-04 절의 수치에 **폐기 표시**를 달았다 — 두 차례에 걸쳐 **여섯 자리**다
(위치·원장·D-5·반증 표·검증·안 잰 것, 그리고 2차 리뷰어가 잡은 둘: 접힌 뒤 `eye.y` 줄과
「눈높이를 지면에서 안 막는다」 절의 전제). ⚠ 「전부 달았다」를 **두 번 적었다가 두 번 다
남은 자리가 있었다** — 다음에 원장을 갈아엎는 사람은 `grep`으로 확인하고 적는다(#42 ⑥).

**증상 ②**: 소실점 스냅. **판정은 돌았고 표식이 없었다** — `render2d`의 `mark()` switch에
`case 'vp'`가 없어 빈 경로에 stroke가 됐다. 픽셀 실측: 소실점 둘레 **80 → 80(차 0)**,
같은 조건 끝점 **10 → 72**. ✕에 겹치는 반지름 7 원을 더했고, **`never` 검사로 구조가
막게** 했다(종류를 더하면 표식을 안 더한 순간 타입이 깨진다 — 실제로 확인).

**보장이 아닌 것**: 「접은 뒤 대상이 화면에 남는다」. 구성상 고정되는 것은 **pivot 한 점**
뿐이고(요 180°에서도 같다), 떨어진 기하는 옮겨간다. 재서 경계를 적었다 — 퍼짐/거리가
**넓은 화각 0.68 · 관행 0.38 · 좁은 화각 0.15**부터 화면 밖으로 나간다(NOTES의 표).

**다음 사람이 화면에서 볼 것**:
- ⚠ **접힌 뒤 지평선이 안 그려진다** — 이 세션이 픽셀로 발견했다. `render2d`가 작도
  포즈에서만 긋기 때문이고 **이 회차가 만든 것은 아니다.** 접힌 포즈는 피치 0이라
  지평선이 실제로 화면 수평선인데도 없다 — **눈높이를 화면에서 못 읽는다.**
  고치는 것은 한 줄이고 안 한 이유는 범위다. `DEFERRED.md`에 함께 볼 것 셋과 함께 있다.
- **접기가 이제 안 느껴져야 한다** — 돌려보기 전과 후가 좌우 각도만 다르다(픽셀로 확인).
- **큰 요로 접었을 때 그림이 화면에 남는가** — 좁은 화각 구도에서는 퍼짐 0.15배부터 나간다.
- 소실점 표식 **반지름 7 px**가 손가락·펜에 맞는가(실기기 미측정).
- **1200 ms**(AS-C11) · **300 ms**(AS-C12) · 소실점 표식 **반지름 7 px**(AS-C13)는
  전부 동작점 하나다(#12).
- ⛔ ~~**「돌려보다 줌해서 맞춘 거리가 접으면 사라지는가」**~~ **→ 그 관측이 왔다(web2-06 지시 5)**.
  **줌은 이제 접기가 지킨다**(궤도가 반경을 구성상 보존하므로 그 변화는 사람의 것이다).
  **팬은 여전히 되돌아간다** — 근거가 안 서서 안 열었다. ⚠ 다만 팬이 반경에 남긴 몫은
  줌으로 읽혀 남고 **눈높이까지 움직인다**(1.600 → 1.562 · 200px 팬이면 1.879).
  이 줄의 옛 사실(「팬·줌 둘 다 되돌린다」)은 **web2-06 이후 틀리다.**

**열린 것**: `DEFERRED.md`의 web2-04 절 중 **셋이 그대로 유효하다**(접힌 포즈에서 작도를
못 끝낸다 · 뷰 큐브 18 시점 · 지우기까지 막은 것). ⛔ **둘은 이번에 무효·재작성됐다** —
「가로(x)」 행은 **「화면 안을 아예 안 잡는다」로 다시 썼고**(대역 실측 포함),
「팬으로 pivot을 지나친 갈래」는 **그 갈래가 코드에서 사라져 무효**다.
⚠ **소실점이 화면 밖이면 ✕도 표식도 안 그린다** — 스냅 목록에는 있으나 커서를 못 가져간다.
그 자리는 아래 「다음 세션이 할 일」의 «화면 밖 소실점 가장자리 표식»이다.

---

---

## 🆕 web2-03 완료 (2026-08-21) — 세 항목

**배포 확인됨** — 원격 main = `acfd3e0778a7e5d8c5b7c46ea673aeedfb4ab27a`,
**화면 빌드 식별자 `acfd3e0`**, `npm run deploy:check` exit 0.
지시 원문: [`web2-03.md`](../docs/instructions/web2-03.md) (⚠ 지시 1이 도중에 개정됐고
초판은 [`web2-03-r1.md`](../docs/instructions/web2-03-r1.md)).
기록은 [NOTES.md](NOTES.md)의 「**web2-03** · 지시 0」~「지시 2」.

1. **`CLAUDE.md` §2에 「D. 진단 규약」** — 착수 명령 바로 아래. A-3·A-4·§5.3의 중복을
   D-3·D-4로 보냈다. **이 회차에 실제로 값을 했다**(지시 0 절의 「§D로 달라진 것」).
2. **임의의 소실점** — 여분은 화면 평행 가로축 `H`였다. `an.axes`가 정규직교 프레임이
   아니었고, 2점에서 H를 빼고 · 1점 주점을 깊이 소실점으로 옮기고 · ✕ 표식을
   `vpMarks()`(사람이 만든 소실점만)로 모았다.
   ⚠ **1-f의 답은 「별개」다** — H를 지워도 깊이축 기울기는 안 사라진다. 수리가 둘이다.
3. **먼 소실점 대역** — 경계가 고정 px가 아니라 `h × 20`이었다(h = 시작점의 지평선 이격).
   「지평선과 평행한가」를 **처짐 px**로, 「기존 소실점에 붙었나」를 **각도**로 바꿨다.

**남은 구멍(이 회차가 못 덮은 것)**:
- **아주 짧게 지평선 바로 아래를 긋는 획**(h=10·R=100)은 경계가 0.56W로 **정상 구도 안**이다.
  ⛔ 개선 배수 `R/30`은 **web2-06 이후 6W 상한 아래에서만** 성립한다(1차 리뷰어 [7]).
- ⛔ ~~**1점 의도가 먼 소실점으로 읽히는 반대 오류**를 안 쟀다~~ **→ web2-06 지시 2가 쟀고 고쳤다**
  (`VP_FAR_W`). **AS-C15가 그때 반증됐다** — 대역을 정하는 것은 포인터 잡음이 아니라
  손 겨냥 오차다. 아래 「`PARALLEL_PX`가 실기기 잡음 대역에 맞는지 모른다」도 **그 물음이
  아니게 됐다**(그 상수는 이제 경계의 뒷 항이고, 앞 항이 거의 언제나 먼저 걸린다).
- **3점이 도달 불가**라 1-d의 「1점·2점·3점 어디서나」 중 3점이 공란이다.
- **보조 소실점을 만드는 길이 없다** — 2점에서 화상면 평행 가로 방향으로 그으려면 그 기능이 서야 한다.
- **`PARALLEL_PX = 1.5`가 실기기 잡음 대역에 맞는지 모른다**(마우스 기준으로 골랐다).

---

## (이전) web2-02 완료 (2026-08-21) — 네 항목

**배포 확인됨** — 원격 main = `1dc89e804395556e26923be38a8066d2bb694edf`,
**화면 빌드 식별자 `1dc89e8`**, `npm run deploy:check` exit 0.
(⚠ 병합 직후 두 번은 화면이 `c4a4b4d`였다 — Pages가 도는 데 약 1분. `?reset`이 캐시와 가른다.)

**둘째 깊이선 · 1점 잠금 · 격자 기본 꺼짐 · 그리기 도구 UI.**
지시 원문: [`docs/instructions/web2-02.md`](../docs/instructions/web2-02.md).
작업 기록은 [NOTES.md](NOTES.md)의 「**web2-02** · 지시 1」~「지시 4」 절 넷.
⚠ **절 제목에 회차 표식(`web2-01`/`web2-02`)이 붙었다** — 날짜가 다 같아서 못 갈렸다.

무엇이 바뀌었나:
1. **소실점을 만드는 선은 지면이다** — 둘째 깊이선이 첫 것과 안 닿아도 3D가 된다.
   지면 앵커가 첫 획에만 걸려 있던 것을 넓혔다. 연결이 있으면 연결이 이긴다.
2. **화면 수평축이 선언되면 1점으로 잠근다** — 그 뒤 어떤 획도(찍기 포함) 두 번째
   수평 소실점을 못 만든다. `Analysis.p1Locked`는 **계산이고 저장하지 않는다**.
3. **격자 기본 꺼짐**. 토글은 설정에 남는다. 기본 표시 전수 대조표는 NOTES.
4. **UI 전면 교체** — 도구 넷(연필·펜·지우개 둘), SVG 아이콘, 테두리·박스·상태 표시 제거.
   `Tool`·`activeGrade()`·`isEraser()`·`widthOfMat()`가 각각 단일 출처다.

**다음 세션이 정할 것**: `progress.md`와 이 관행이 갈려 있다 — CLAUDE.md §2는
「착수 시 읽은 번호를 `progress.md`에 적는다」인데 web2 라인은 그것을 `NOTES.md` 각 절의
「착수 시 PITFALLS 대조」에 두고 있다. **둘 중 하나로 맞춘다**(문면을 고치든 한 줄을 남기든).
이 세션은 범위를 안 넓히려고 안 고쳤다.

**다음 사람이 화면에서 볼 것**(이 세션이 픽셀을 못 봤다 — #22):
- 새 아이콘이 조잡하지 않은가(4-g). 좌표와 클래스만 쟀다.
- **이 회차의 셋이 겹친 화면** — 격자 꺼짐 + 소실점 ✕가 무채색 + 「소실점 N」·「대기한다」
  알림 삭제. 그러고도 「내 선이 어디로 수렴하는가」와 「이 획이 대기인가」가 읽히는가.
- 연필 몸통 밀기(4-e)의 10px/칸이 손가락에 맞는가.
- 잠긴 1점 상태에서 뜨는 뷰 큐브가 이상하지 않은가(좌표로는 정규직교다).

---

**(이전) 지시 web2-01(일곱 항목)의 1~5 완료 · 6 부분 · 7 완료.**
2026-08-21, 갈래 `fix-draw-order` → main 병합. **배포 확인됨** —
원격 main = `b1189a6f4db37f44a810929dd734ab17740cacf0`,
화면 빌드 식별자 `b1189a6 2026-08-21 02:15`, Pages run #91 success.
지시 원문: [`docs/instructions/web2-01.md`](../docs/instructions/web2-01.md).
작업 기록·수치·판단 근거는 전부 [NOTES.md](NOTES.md)의 「지시 1」~「지시 6」 절.

## ⛳ 마감 절차 — **명령이 판정한다** (PITFALLS #58 · 지시 7)

```
갈래 커밋 → 검증 → main 병합·푸시 → npm run deploy:check → 그 출력을 보고에 붙인다
```

`web2/tools/deploy-check.mjs`가 ① `git ls-remote origin main`을 **실제로 실행해 출력을 찍고**
② 원격 main 해시 == 내 커밋 해시 ③ 배포된 `sw.js`의 빌드 식별자 == 그 해시를 본다.
다르면 **exit 1**. 사람이 「확인했다」고 적는 자리를 없앴다.

⚠ **Pages run success는 판정자가 아니다** — 작업 갈래에서 돌면 배포되는 것은 main의 상태다.
⚠ 두 세션 연속으로 보고에 적힌 커밋(`2af3bd6`·`9d8f6f5`)이 **로컬에도 원격에도 없었다**
(reflog·전 갈래에 없고 GitHub API가 둘 다 422). 어느 명령의 출력에서도 나올 수 없는 값이었다.
**어느 명령을 안 불렀는지는 기록으로 못 가린다** — 값이 존재한 적 없다는 것만 확인됐다.

## 다음 세션이 할 일 — 지시 6의 남은 목록 (6-m대로 우선순위 순)

1. **렌즈 조절**(6-e) — mm ↔ 화각, **정렬·확인 시점에서만**. 작도 중에는 소실점이 f를 정하므로
   조절 대상이 아니다. 조절해도 그린 선의 3D 좌표는 안 움직인다.
2. **화면 밖 소실점 가장자리 표식**(6-h) — 지금은 화면 밖이면 안 그린다. 작게.
3. **방사선(가이드) 표시와 「선 우선순위」**(6-h) — 격자는 넣었다. 방사선이 남았다.
   사용자가 그린 선이 가장 눈에 띄어야 한다.
4. **큐브 상대 90° 화살표 · 절대 정렬 탭 · 비행 애니메이션**(6-e).
   좌우 화살표 = 상대 90°, 탭 = 절대 정렬, 보고 있는 특징을 다시 탭하면 가장 가까운 1점.
5. **「이 선이 두 소실점 어디도 향하지 않습니다 · 지우기 · 무시」 경로**(6-c) —
   `notice.ask`는 이미 있다. 그 문구를 낼 자리가 없다.
6. ~~**지평선 표시 토글**~~ — **닫힘(web2-12 7번)**: 설정에 토글(기본 켜짐) + 작도
   대역으로 더 옅게(COL.horizon — 잉크량 실측은 level.spec 「지평선이 옅다」).
7. **좌우 전환 설정**(6-b) — 「둘지는 판단한다」. **지금은 안 둔다**(근거 없음).
   되돌릴 조건: 왼손잡이 사용이 관측되면.
   ⚠ **web2-12 6번으로 «무엇을 뒤집어야 하는지»가 구체화됐다**(오른손잡이가 화면 형태에
   굳었다 — 지시 문면): ① 세로바 전체를 왼쪽으로(`#sidebar`의 right → left · 패널들의
   right 좌표 네 곳) ② **연필통 행의 방향**(지우개 왼쪽·심 오른쪽 — `pencilRowSvg`/
   `penRowSvg`의 x 좌표를 미러) ③ 선택 표시의 이동 방향(`.tool.on svg`의 translateX 부호)
   ④ 팝오버들(확인·뷰 썸네일)의 «버튼 왼쪽» 배치(`confirmNear`·`openViewsPop`의 right 계산)
   ⑤ 굵기 막대 자리(`#thick`의 right). 만들 때는 CSS 논리 속성이나 미러 플래그 하나로
   묶는 쪽이 답이다 — 지금 흩어 두는 이유는 관측 0이기 때문(A-3).

**판단 못 한 것**: web/의 「선택」 도구(`edit`)를 web2에 넣을지. 지시 6의 논의 목록에도,
6-l의 폐기 목록에도 없다 — 원문만으로 못 정한다. 사람에게 물어야 한다.

## 열린 위험 · 안 잰 것

- **실기기(펜) 확인이 아직 없다.** 특히 **탭 = 소실점 찍기**(지시 4)가 펜에서 2px
  (`C.TAP_MAX_PX`) 안에 들어오는지 모른다. 안 들어오면 그 값을 올린다.
- **1점 상태에서 궤도할 수 있게 됐다**(web2-01 지시 1). 그 상태의 f는 임의 게이지이므로,
  두 번째 소실점이 서면 **궤도 시점에서 형상이 291.534 px 움직인다**
  (⚠ web2-03에서 다시 움직였다: 248.276 → **291.534**. 1점 주점이 `W/2`에서 깊이 소실점으로
   옮겨가 1점 기하가 바뀌었다. **되돌리지 않은 근거와 되돌릴 조건은 NOTES의 web2-03 지시 1 절**)
  (⚠ 여기 적혀 있던 **206 px는 이 픽스처의 값이 아니었다** — web2-02에서 같은 하네스로
  기준 커밋 `c4a4b4d`와 web2-02 HEAD 양쪽을 다시 읽어 둘 다 248.276을 얻었다. #47: 원장이 정본).
  `promote.test.ts`가 범위(100~400)와 **실측값**(`toBeCloseTo(291.534, 2)`)을 함께 박는다
  — 범위만으로는 규칙이 바뀌어도 안 깨지고 죽는다(#56). 작도 시점은 0.000000 px다.
- **이 세션의 브라우저 창이 숨겨져 있어 픽셀을 못 읽었다**(`document.hidden=true`,
  rAF 정지 — PITFALLS #22). 픽셀 증거는 전부 헤드리스 e2e가 댄다.
  ⚠ 그 상태에서는 **CSS 트랜지션도 멈춘다**(web2-02에서 확인) — 계산값이 시작값에 얼어 있어
  「규칙이 안 먹는다」로 오독하기 쉽다. `transition:none`을 얹어 보면 갈린다.
  ~~격자는 e2e 픽셀 팔이 아직 없다~~ → **web2-02에서 넣었다**(첫 화면 `inkPixels === 0`).
  ⚠ 다만 **「켰을 때 칠해진다」는 여전히 안 잰다** — 양성 채널이 빈 자리다.
- 웹2 측정이 전부 **`stage0/out` 원장 밖**이다(PITFALLS #25). 수치가 NOTES 산문에만 있다.

## 검증 명령

```
npm test            단위 243건 (24파일)
npx vitest run test/fold_measure.test.ts   접기 원장(화각 셋 × 궤도 넷 — 궤도 전/후/접은 뒤)
npm run typecheck
npm run build       sw.js·빌드 식별자
npm run e2e         종단 18건 (dpr1/dpr2) — dist/ 가 있어야 정적 배포 팔이 돈다
npm run deploy:check  배포 판정자
```

⚠ **「e2e 18건」은 판별력의 근거가 아니다** — 그 수는 `flow` 1 + `static_deploy` 5 +
`level` 3을 dpr 둘로 돌린 것이다. 무엇을 새로 판정하는지는 **단언 단위**로
NOTES에 적는다(web2-02 지시 4 절의 표가 그 형식이다).

브라우저가 이미 깔린 환경이면 `PW_CHROMIUM_EXE=/경로/chromium`.

---

## (이전) 1~7단계 구현 완료 기록

**상태: 지시서의 1~7단계 전부 구현·검증 완료** (2026-08-21, 갈래 `web2` → main 병합).

**그 뒤 「비우기」와 파일 묶음 대조**(2026-08-21, 갈래 `claude/file-bundle-clear-feature-4h54uj`).

- 검증: 단위 **82건**(`npm test`) · 종단 흐름 dpr1/2 **12건**(`npm run e2e`, dev 서버 5301) ·
  타입(`npm run typecheck`) · 프로덕션 빌드(sw.js·빌드 식별자 확인).
  ⚠ 정적 배포 e2e는 `dist/`가 있어야 돈다 — 없으면 **건너뛴다**(`npm run build`를 먼저).
  브라우저가 이미 깔린 환경이면 `PW_CHROMIUM_EXE=/경로/chromium`으로 그것을 쓴다.
- 결정·측정·게이지·함정은 전부 [NOTES.md](NOTES.md)에 있다. 문서는 그것 하나다.

**배포는 web2다** (2026-08-21 전환 완료·확인됨, `pages.yml` working-directory = `web2`).
확인 방법: 화면 우하단의 빌드 식별자가 배포된 커밋 해시와 같으면 새 것이 나간 것이다.
안 맞으면 아직 안 퍼졌거나(배포망 세대 차 — 몇 분) 캐시에 묶인 것이다. `?reset`이 둘을 가른다.
주소 그대로 `https://hyunwookyu.github.io/brunelleschi/`. `web/`은 안 지웠고 배포만 안 된다.
전환 설계(옛 워커 인계·`l.html` 리다이렉트·`?reset` 탈출구)와 남은 위험은 NOTES의
「배포 전환」 절에 있다.

## ⛳ 마감 절차 — **배포까지가 한 항목이다** (PITFALLS #58)

```
갈래 커밋 → 검증 → **main 병합·푸시** → Pages 초록 → **화면 빌드 식별자 == 그 커밋** → 보고
```

갈래 푸시는 중간 상태다. 2026-08-21에 비우기 작업이 갈래에만 올라간 채 «완료»로 보고됐고,
사람이 연 화면은 `760ba38`(작업 전)이었다. **완료 보고에는 화면에서 읽은 배포 커밋 해시를 적는다.**

## 다음 세션이 할 일

1. **실기기 확인**(아직 안 됐다 — 전환의 유일한 남은 위험). ~~아이패드~~ **실기기는
   Wacom MovinkPad Pro 14(안드로이드 15)로 확정됐다**(web2-09 지시 머리 — **판정 기준을
   크롬으로 정한 것**이고 실제 쓰는 브라우저·UA는 미확인이다, DEFERRED 표 첫 행).
   펜으로 지평선→소실점 둘→획→지우개까지 해 본다. 관측 목록·방법은 **DEFERRED.md의
   web2-09 표가 정본**이다(dpr·TAP_MAX_PX·PARALLEL_PX·표식 7px·필압·손가락 면·전체화면·핀치). 안 되면 NOTES 「되돌리기」로 즉시 되돌린다.
   화면이 옛 앱으로 보이면 `?reset`을 붙여 연다.
2. ⚠ ~~**지시 원문이 저장소에 없다**~~ (web2-01·web2-02는 이제 `docs/instructions/`에 있다.
   ⚠ 둘 다 **작업 중·작업 뒤에** 넣었다 — 「먼저 둔다」는 아직 한 번도 안 지켜졌다) —
   web2 세션(1~7단계)도 이번 비우기 지시도
   `docs/instructions/`에 안 들어왔다(그 폴더의 README가 회차별 원문 보관을 규약으로 둔다).
   그래서 이번 「5단계 대조」는 **살아남은 기록**(NOTES 5단계 절·커밋 `babdfa1` 메시지)만
   상대로 했다 — 원문에만 있고 그 둘에 안 남은 항목은 못 잡는다. 다음 지시부터는 원문을 먼저 둔다.
3. 남은 다듬기(전부 동작에는 지장 없음):
   - 저장 파일명이 늘 `drawing.brnl` 고정 · 저장·내보내기 성공 알림 없음 ·
     옛 앱 `.brnl.json`(`s2s-doc/2`)은 못 연다 (NOTES 「비우기 · 파일 묶음 대조」의 「남은 것」)
   - 프리핸드 표현(raw 보존은 되어 있음 — 표현 계층만 남음)
   - 큐브 전환 애니메이션(현재 즉시), 뷰 큐브 면 라벨
   - 필압의 획별 본체 반영(현재 입자 층에만 — 경도별 공유 재질이라)
   - 오스냅 반경·종류 설정의 저장(현재 세션 한정)
4. 회귀 주의: 리프팅 연결은 **방향이 없다**(lift.ts — 6단계 수리). 건드리면
   `lift.test.ts`의 「연결은 방향이 없다」·e2e 선따기 단계가 지킨다.

## 구조 요약 (자세한 것은 NOTES)

- `src/core/camera.ts` — 소실점·카메라·사영의 **단일 출처**(정적 검사로 강제)
- `src/core/lift.ts` — 광선-직선 리프팅·대기·연쇄 (문서의 순수 함수)
- `src/core/{osnap,pieces,material,file,export,viewcube}.ts` — 각 단계 코어
- 「비우기」는 `state.ts`의 `clearAll()` 하나 — 확인(최상단 한 줄의 밑줄 단어,
  `notice.ts`의 `ask()`)을 받고 자동 저장까지 지운다. **실행취소 대상이 아니다**
- `src/app/` — 상태(op 실행취소)·입력(펜/터치/마우스)·렌더(2D 오버레이 + Line2)
- 문서(Doc)에는 획(2D 좌표·뷰 포즈·재료)만 있다 — 카메라·차수·조각은 전부 계산.
