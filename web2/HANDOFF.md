# web2 인계

> ## 📌 이 파일이 「다음에 무엇을 하는가」의 **유일한 출처**다 (2026-08-30 사람 지시)
>
> 그동안 순서가 **두 곳**에 있었다 — 이 파일과 사용자의 채팅. 둘이 갈렸다.
> 앞으로는 이 파일이 정본이고, 그것을 지키는 규칙이 셋이다:
>
> 1. **새 지시문 파일을 저장할 때 반드시 같은 커밋에서 아래 §2 대기열을 갱신한다.**
> 2. **라운드를 마칠 때도 같은 커밋에서 §2 대기열을 갱신한다.**
> 3. **채팅에서 받은 순서와 이 파일이 어긋나면 보고하고 판단을 구하라** — 조용히
>    한쪽을 고르지 마라. (⚠ 저장소의 **사실**과 어긋나는 것은 다르다 — 그때는
>    저장소를 따르고 §2에 그 정정을 적는다. 2026-08-30에 34가 그 경우였다.)
>
> §3 아래의 「회차 기록」은 **과거의 기록**이다. 그 안에 적힌 「다음에 할 일」은
> 전부 **무효**이고, 유효한 것은 §1·§2뿐이다.

---

## 1. 다음 세션에 이대로 붙일 것 (사람이 복사만 하면 된다)

```
저장소 /home/user/brunelleschi · 배포되는 앱은 web2/다(pages.yml의 working-directory가 정본).

**59·60이 끝났고 배포됐다(main 46543b1 — §2 마감 블록 · NOTES 「마감 — web2-59 · web2-60」).**
**60의 ⚑(사람 눈 판정 대기)는 2026-09-04 사람 지시가 닫았다** — 값을 당기는 대신 **엔진을
바꾼다**: 「칠 엔진을 직접 쓰지 않는다. **61 = p5.brush(MIT)** → 배포 → **62 = libmypaint(ISC)+
mypaint-brushes(CC0) 이식** — 둘 다 한다」(docs/instructions/web2-61.md·web2-62.md · DECISIONS).
**지금 회차 = web2-61**(칠을 p5.brush로 — 이 라운드 하나만 · 62는 61 배포 뒤 새 세션).

착수 시:
· web2/HANDOFF.md §1·§2를 읽어라 — 실행 상태의 유일한 출처다
· docs/DIRECTION.md 를 읽고 착수 표에 이 라운드의 축을 적어라
· 60 마감 블록의 착수 표 이월 항목을 옮겨 적어라(#42 ⑩ — #103·#104 · 프레임 게이트의 부하 조건 열쇠 ·
  글꼴 의존 반증 · 원장 def 문장은 값 뒤에)
· tail -40 PITFALLS.md 를 실제로 읽고 번호를 web2/NOTES.md 「착수 시 PITFALLS 대조」에
  적어라(안 적으면 안 읽은 것으로 본다) · CLAUDE.md §2의 D 다섯 적용 내용을 적어라
· 떠 있는 개발 서버를 내리고 새로 띄운다(#70) · 갈래를 먼저 딴다
마감은 `cd web2 && node tools/deploy-check.mjs` — 갈래 푸시는 중간 상태다(#58 · #81).
⚠ 원장 갱신은 `LEDGER=1` — 없으면 조용히 안 써진다(#90 · #94). 원장 실행은 워커 1(#99).
  ⚠⚠ web2 팔은 **web2/ 안에서** 돌려라(#101). ⚠⚠ **e2e가 도는 동안 web2/ 아래 파일을 고치지 마라**(#104).
⚠ 상한 없는 대기 ⛔(#95) · 마감 보고에 「도는 백그라운드 없음」을 값으로.
시험은 54의 셋: e2e:green / e2e:night(전량 검증) / e2e:ledger.
⚠ 이 컨테이너(리눅스 SW GL)의 밤은 빨강이 십수 개 뜬다 — 전부 부하·환경 귀속이었다(60 마감 블록 표 ·
  #80의 단독 재실행이 가른다). ui34 ④′는 환경 결정적(글꼴 · DEFERRED web2-60 마지막 행) · rep49 ⑤ dpr1은 환경이되 값이 흔들린다(수리 전 트리 3회 전부 문 밖 · 60 마감 표).
⚠ 칠 엔진의 계약(D-W27): 획 안은 최대값 합집합(도장 브러시) · 붓은 흐름+캡(젖은 매체) · 획 사이는 누적 ·
  결은 면 고정(굵기 사다리 2^(1/4)) · 미리보기 == 확정본(같은 함수·같은 캔버스). 값은 실험실(60-4)이 자리.

(0-f) **⛳ #42 ⑩ — 56이 세운 규칙 셋 · 53«이 설 때» 첫 착수 표다(57이 보존 — 2026-09-03)**:
  · **내보내기는 접합 이동표를 같은 자로 읽어야 한다** — 두께 기하의 정점은
    `app.joints.shifts`(면 id → 정점 열쇠 vkey → 앞/뒤 이동)가 옮긴다. 내보내기가
    중심선 정점(rf.outer + slotOffsets)만 읽으면 **화면과 다른 솔리드**(계단 코너)가
    나간다 — 렌더의 j56 헬퍼(render3d)가 그 자의 소비 선례다.
  · **복합 구석(세 두꺼운 면이 정점을 나눔)은 접합이 기각돼 계단이 남는다**(DEFERRED
    web2-56 첫 행 · e2e(join56.spec) ⑤ rejects 2 실측) — 내보내기도 그 모양 그대로
    나가는 것이 옳다(화면=파일). 「내보낸 벽이 안 붙어 있다」 보고가 오면 그 행부터.
  · **t=0 픽셀 항등의 부모 트리 대조 도구가 생겼다**(`web2/tools/pretree56.mjs` —
    비상주 · LEDGER=1 · 워크트리+junction 절차는 NOTES 56 2차 [11]) — 렌더 경로를
    건드리는 라운드는 이것으로 픽셀 무변을 값으로 댈 수 있다.
```

---

## 2. 대기열 — **이것이 정본이다**

```
완료·배포   26 · 27 · 28 · 29 · 30 · 31 · 32 · 34 · 35 · 37(1·2·4·6) · 41(1·2·3·4) · 39 · 40 · 42 · 43 · 44 · 45 · 46 · 47 · 48 · 49 · **50**
            (**50은 af6994b** — deploy-check 화면 식별자 일치 · 전량 e2e 624/0(1.0h) ·
             마감 블록은 NOTES 「마감 — web2-50」. 칠의 정본이 면 위 좌표(uv)·표시가
             면 텍스처+곱이 됐다 — D-W20 · 여섯 증상 게이트는 paint50_web2_*.json)
            (44는 **a639718** · 45는 **81c56c3** · 46은 **50e5a73** · 47은 **822eec0** ·
             48은 **711b800** · **49는 cd16dda**로 그 조건을 채웠다 — deploy-check 화면
             식별자 일치 · 마감 블록은 `web2/NOTES.md`의 「마감 — web2-44/45/46/47/48/49」.
             ⚠⚠ **49의 마감은 사슬 2 세션이 했다** — 49 세션이 갈래에서 멈췄다(#58의 그
             형태 · 전량 e2e 미실행 상태였다). 마감 세션이 전량 608/0(1.0h)을 돌리고
             병합·배포했다 — 마감 블록의 ⚠⚠ 절이 그 기록이다)
            ⚠ 「완료·배포」는 **아래 마감 블록의 화면 빌드 식별자가 그 커밋과 같을 때만** 적는다
              (#58 · #89 — 2차 리뷰어가 41 초판에서 그 순서가 뒤집힌 것을 잡았다).
              41은 **c48b9e4**로, 39는 **5afb498**로, 40은 **2a15097**로, **42는 77027f3**로
              그 조건을 채웠다(마감 블록들 — 42의 것은 `web2/NOTES.md` 끝의 「마감 — web2-42」다).

▶ **사슬 2(CHAIN2 — 50 ✓ → 51 ✓ → 52 ✓ · Fable 한 세션)** ← **완주**(2026-09-02) ·
     50 `af6994b` · 51 `6b452e3`(리뷰어 3왕복 — «원근 축척» 철회) · **52 `7c699d0`**
     (재료 여덟 · 무늬→면 텍스처 · 프리셋 기기 저장 · 전량 655/0+1skip 1.0h · 리뷰어
     2왕복 — 마감 블록은 NOTES 「마감 — web2-52」·「사슬 2 완주」). ⚑ 갈래 결정: 조절 UI
     안 넣음(2/13 형태 실패 — 실기기 ⑳ 대기). 실기기 몫 ⑳㉑㉒㉓㉔.
     ⚠ 52는 갈래 없이 main 직행(자백 — 마감 블록) — 다음 회차는 갈래를 먼저 딴다.
     `docs/instructions/CHAIN2.md` · `web2-50.md`(자국의 뿌리) · `web2-51.md`(자국의 질) ·
     `web2-52.md`(재료). 멈춤 조건 셋은 CHAIN2 원문. 50이 전부다 — 51·52는 그 위에 얹는다.
     **재번호**(50 지시문의 명령): 50=자국의 뿌리 · 51=자국의 질 · 52=재료 ·
     **53=내보내기와 별칭**(종전의 50). 착수 전에 **49의 완주 보고**(NOTES 「web2-49」 절)를
     읽는다 — 49의 무늬 생성기가 52의 재료가 되고 49의 ⚑ 갈래(원점·방향)가 52에서 정해진다.
     ⚠ 사슬이 읽을 것 둘: ① **48-1의 «흰 장막»이 출하 상태에 남아 있다** — 올바른 수리의
     설계가 DEFERRED(«칠은 제 겹을 가져야 한다»)에 있고, **50의 구조(면 텍스처 + 곱 합성)가
     그 행을 닫는 자리다** ② palette 다섯 ↔ 재료 여섯의 집합 차 — 52-2가 정한다(D-W19 [2])
▶ **사슬 3(CHAIN3 — 54 ✓ → 55 ✓ → 56)** ← **55에서 절단**(2026-09-03 사람 지시 —
     시험 팔 추격 중단·55 마감·배포 후 세션 종료. 「한 세션에서 54→55→56」 조항은 여기서
     끊었다 — **56은 새 세션이 깨끗한 컨텍스트로 시작한다**).
     **55 완료·배포 — `main 4eecab5`**(deploy-check 화면 식별자 일치 · Pages attempt 1 ·
     마감 블록은 NOTES 「마감 — web2-55」). 두께와 분류가 섰다: 분류 다섯(ClsDef
     t/off/pri/core/mat · BIM형) · 면 예외 ex{t} · 세 슬롯(앞/뒤/테두리 — 테두리 uv=(s,u)
     **세계 단위**) · **t=0 저장 바이트 동일**(pre_tree sha 두 dpr 일치) · KEY_ORDER 84 ·
     손글씨 두께(thick/thickEx · «2»→«25» 한 걸음 취소) · 밤 684칸(+14) · 단위 1038.
     ⚠ 56 착수 표에 옮길 것: **pre_tree 재측정 의무**(56이 저장을 건드리면) · **칠통 세로
     여유 13px**(775/788) · **stair 값**(thick55 ledger stair {buried 75, step 100} — 접합
     게이트 「계단 픽셀 0」의 입력) · **두께 20면 dpr2 +18.3ms**(DEFERRED — 실기기 ㉖) ·
     **#93 계열 DEFERRED 행**(dpr2 부하 회전 플레이크 — gesture 3부·roundsave ③·brush51 ⑤
     남음 · 귀속 표는 55 마감 블록 · 54 게이트 1 「불안정 0」이 부하 조건부였음이 드러남).
   (54의 기록: 2026-09-03 완료 시점
     **54 완료·배포 — `main f354fef`**, deploy-check 화면 식별자 일치 · 마감 블록은 NOTES
     「마감 — web2-54」. 실행 셋이 생겼다: **e2e:green**(병렬 4 · 계측 넷 제외 · dpr2는
     목록 — 19.8분) · **e2e:night**(전량 670칸 — 27분) · **e2e:ledger**(LEDGER=1 · 워커 1 ·
     스펙 하나씩 강제 — #99 무변). **§1 게이트 2가 섰다**(gates54_web2.json — 통과 집합
     전 쌍 diff 0 · 사슬 계속). 칠의 자리: 획은 시작한 면에만 · faceSel 고름(도구 넘어
     산다 · 빈 곳 꾹 누름 풀기) · 정면 왕복(칠통에서도 · D-W23 · 실기기 ㉕). ⚠ **㉢(CI
     초록)은 한 번 실측(537/547 — 리눅스 팔 의존 실증) 후 되돌렸다** — 열 조건·⚑ 비용
     판단은 DEFERRED web2-54 절. ⚠ 55·56 착수 표에 옮길 것: **칠통 세로 여유 13px**
     (775/788 — DEFERRED) · **전량은 e2e:night로**(마감 검증) · 원장 스펙은 병합-쓰기.
   **56 완료·배포 — `main 69354ad`**(deploy-check 화면 식별자 일치 · Pages attempt 1 ·
     마감 블록은 NOTES 「마감 — web2-56」)
     (2026-09-03 · 갈래 web2-56 · 검증 트리 cab4179: 단위 1054/1054 · 밤 691/694 —
     빨강 2는 선재 귀속(#80 단독 초록·스펙 바이트 55와 동일) · 리뷰어 2왕복 31건 전건).
     섰다: 병합 걸음의 단일 층 축약(정점 면내 이동 — core/joint.ts) · 마이터=무승부의
     결과(별도 모드 ⛔) · 승부 관통/버트 · T · 치유(#71) · 안정 열쇠 결정론 · 1링
     (29/435) · 접합 끊기(Stroke.nj — KEY_ORDER 85 · 손통 「접합」) · 계단 픽셀 켬 0/끔
     544·2168 · t=0 부모 트리 픽셀 sha 완전 일치(pretree56). ⚠ 복합 구석(세 두꺼운 면이
     정점 공유)은 기각 — 계단이 남는다(DEFERRED · 실기기 ㉗).
   ▷ ⚠ **순서가 2026-09-03 사람 지시로 바뀌었다**(§0 규칙 3의 보고는 NOTES 57 머리):
     `docs/DIRECTION.md`(신규 — 세 축·최소 판매선)가 **A행(자국·면 = 57·58)을 앞**에
     세웠고 57 지시문이 「내보내기(53) 보류」를 명시한다. **53은 뒤로**(§1의 (0-f) 블록은
     53이 설 때 첫 착수 표로 그대로 산다).
   **57 완료·배포 — `main 080846d`**(deploy-check 화면 식별자 일치 · Pages completed/
     success attempt 1 · 마감 블록은 NOTES 「마감 — web2-57」)
     (2026-09-03 · 갈래 web2-57 · 밤 검증 트리 5145a76: 밤 697/698 **빨강 0** · 단위
     1075 · 리뷰 2왕복 36건 전건). 섰다: **면 → 획의 «구간»**(파생 — 자리마다 · 저장 무변
     KEY_ORDER 0) · 지우개 이관(carryFaceEdges — 구간 표 선계산 #100 · op.edgeMoved
     실행취소) · 열림 알림(eraseAt 반환 → notify 한 줄) · 반경 축 실측(크기통 [5,12,27,60] —
     r5·r12 산다 / r27·r60은 이웃 경계를 물어 열린다 · 조건형 게이트) · 잔차 실측(형제
     조각 지움/옮김 — 실기기 ㉘ · AS-C173 · DEFERRED 4행). ⚠ #100·#101 등재(표 갱신 포함).
   **58 완료·배포 — `main aeb8419`**(deploy-check 화면 식별자 일치 · Pages completed/
     success attempt 1 · 마감 블록은 NOTES 「마감 — web2-58」)
     (2026-09-03 · 같은 세션 연속(57→58 — 사람 채팅 지시) · 갈래 web2-58 · 최종 밤 트리
     d7d6bf7: 밤 711/712 **빨강 0** · 단위 1082 · 리뷰 2왕복 27건 전건). 섰다: **51 문면
     철회 둘**(D-W26 — 크기 슬라이더(붓500·마커100·색연필/연필50)·텍스처 팁 해제) ·
     **엔진 하나 브러시는 데이터**(brush58.BrushDef · facetex.paintMark — 제품==실험실 #54 ·
     기본값 = 51 상수(픽셀 무회귀 실측) · 예외 = 마커 팁 0(사람 판정 · 사전 1.374→1.07)) ·
     끝점 D-2(간격은 원래 거리 기반 — **지시 가설 반증 AS-C174** · 정체는 팁) · **브러시
     작업대**(설정에 숨음 · 손잡이 23+곡선 표찰 · mypaint 출발점(CC0 값만) · 「굳힌다」=기기 ·
     굳힘→제품 자국 1.168 실측) · #100·#101·#102 등재. DEFERRED 6행 · 실기기 ㉘ 유지.
   ▶ ⚑ **58의 멈춤은 열린 채다**(사람이 당긴 굳힘 값 미도착) — 2026-09-03 사람 채팅 지시로
     순서가 정해졌다: **59(칠 엔진의 뼈대) → 60(자국의 질)**, 참조 `docs/REFERENCE-FEATHER.md`.
     §0 규칙 3의 보고: 이 파일의 종전 문면(「그 뒤 순서는 사람이 정한다 · 후보 53」)과
     채팅이 갈렸고 **채팅이 정본**이다(사람이 정한 순서 그 자체). 58의 굳힘 절은 60의
     ⚑(사람 눈 판정 · 마감 조건)에 합쳐진다 — 60이 실험실을 넓힌 뒤 같은 자리에서 묻는다.
   **59·60 완료·배포 — `main 46543b1`**(deploy-check 화면 식별자 일치 · 마감 블록은 NOTES
     「마감 — web2-59 · web2-60」 · 문서 최종 6ba7117도 화면 일치 — Pages attempt 2: cost23 ⑥ 러너 요동) · ⚑ **멈춤 — 사람 눈 판정 다섯**(붓의 획 안 누적 · cpBurnish
     출발점 · 결 칸의 뜻 · mypaint 출발점 표 · 값 꺼내기 JSON) — 값이 오면 굳히는 것이 첫 항목.
     (2026-09-03 · 갈래 web2-59-60 · 밤 검증 트리 42c9491: 단위 1088/1088 · 밤 717/736 —
     빨강 18 전건 귀속(부하 15 단독 초록 · 환경 3: rep49 ⑤ dpr1 SW GL · ui34 ④′ 글꼴 2dpr) · 리뷰어
     2왕복 30건 전건 · 원장 판갈이 10스펙 × 2dpr + 2차 뒤 재판갈이). 섰다: **미리보기 == 확정본**
     (같은 함수·같은 캔버스 · diff 0 · 반증 둘) · **스트로크 버퍼**(획 안 최대값 합집합 — 자기
     교차 1.000 ↔ 옛 1.167/1.243 · 붓은 흐름+캡) · **면 고정 결**(위상 상관 1.000 ↔ 획별 시드
     .019 · 굵기 사다리) · coalesced 3.67배 실측 · **mypaint 출발점**(도장 주기 진폭 .0065/.0045 ↔
     옛 .072) · **색연필 문턱↔압력**(빈 몫 .32→.08 단조 · cpBurnish 0이면 불변) · 잉크 경로 1.000 ·
     실험실(붙는 판 · 새 축 다섯 · 값 꺼내기/가져오기). ⚠ #103·#104 등재.
   ▶ **59·60 착수**(2026-09-03 · 갈래 `claude/web2-59-60-completion-pm1fk3` · 한 세션 연속 ·
     지시문 `docs/instructions/web2-59.md`·`web2-60.md` + `docs/REFERENCE-FEATHER.md` 등재 —
     같은 커밋에서 이 대기열 갱신 · §0 규칙 1). 59 = 구조(미리보기==확정본 · 스트로크 버퍼 ·
     결 한 번 · 입력 경로 보고) · 60 = 값의 자리(mypaint 출발점 · 색연필 문턱 · 잉크 경로 ·
     실험실 넓힘) → **60이 서면 멈추고 사람이 눈으로 판정한다**(마감 조건). 그 뒤 순서는
     사람이 정한다(후보: 53 내보내기 — §1의 (0-f)가 그 첫 착수 표).
   ▶ ⚠ **60의 ⚑는 2026-09-04 사람 지시가 닫았다** — 값을 당기는 대신 엔진을 바꾼다(사람의
     물음 「퀄리티 좋은 도구가 많은데 우리가 이 값을 찾아야 하나?」 → 답 「아니다」 · DECISIONS
     2026-09-04): **61 = 칠을 p5.brush(MIT · 이미 의존성)로** — 손으로 짠 도장 루프는 고치지
     않고 버린다 · 칠 렌더러를 «이음매 하나»로(62가 그 뒤만 갈아끼운다) · 산출물에 사진
     (web2/shots/61-<brush>.png — 브러시 넷 × 획 셋). **62 = libmypaint(ISC)+mypaint-brushes
     (CC0) 이식 — 61 배포 뒤 «새 세션»**(지시문 web2-61.md·web2-62.md — 이 커밋이 등재 ·
     §0 규칙 1 같은 커밋 갱신).
   ▶ **61 착수**(2026-09-04 · 갈래 `web2-61` · NOTES 「web2-61」 착수 절 — PITFALLS 대조 ·
     D 다섯 · 이월 다섯). 진행 중.
   보류  36(실기기 ③ 대기) · 38(53 뒤 — 개명은 코드가 다 자란 뒤 · 재번호 2026-09-02) · 37-3(장식) ·
         30-13 · 성능 · 근평면 · 1.74배 잔차 · v1.0 태그
   사람 몫(실기기)  기존 ③⑥⑧ + K1·K2·K3 + 「2500 네 획」 유지 + ~~**⑰ 칠의 곱 합성**~~
                    (✅ **닫힘 2026-09-02 사람 판정** — 흰 장막 「지금은 뜨지 않는다」 ·
                    48-1은 50의 곱 합성으로 닫혔다. 설계대로다 — CHAIN3/web2-54 문면) +
                    **⑱ 색상 휠**(고리·판의 크기가 손가락으로 쓸 만한가) +
                    **⑲ 면의 숨김**(칠 도구를 들 때만 드러나는 것이 자연한가 · 칠한 면의
                    불투명이 작도를 막지 않는가 — AS-C161) +
                    **⑳ 재료 표현의 원점·방향**(web2-49 ⚑ — 자동 규칙(수평 켜·좌하 시작)이
                    틀리게 느껴지는 자리가 있는가: 슬라브 타일의 격자 방향 · 박공 벽의
                    벽돌 시작 위상. 있으면 조절 UI(DEFERRED)가 열린다 — AS-C164) +
                    **㉑ 칠의 자국과 해상도**(web2-50 — 구조만 바꿨다: 캔버스 획이라
                    51 전에는 «밋밋»할 수 있다 · 줌 인에서 칠이 뭉개는가 — 상한 1024,
                    paint50 tex_budget이 근거 · DEFERRED 열 조건) +
                    **㉒ 칠 많은 장면의 궤도 프레임**(web2-50 — dpr2 헤드리스에서 차
                    ~20ms(소프트웨어 GL 의심 · 원장 delta_median_ms) — 실기기 GPU에서
                    무거운가. 무거우면 «칠 안 된 쪽 메시 생략·텍스처 병합»이 첫 후보) +
                    ~~**㉓ 브러시 자국의 질**~~(✅ **닫힘 2026-09-02 사람 판정** —
                    「쓸만하다」. 브러시 엔진 재수술 없음 · 51 ⚑ 조절 UI 안 만듦 —
                    CHAIN3/web2-54 문면 · AS-C166 갱신)(web2-51 ⚑ — 지시 문면 «쓸 만한가는 눈이
                    정한다»: 연필 결·마커 띠·cp 구멍·붓 갈라짐이 건축 표현으로 서는가
                    (AS-C166) · 펜 필압이 실기기에서 농도>굵기로 갈리는가 · 40px 견본의
                    자국 비례 잔차 +8~10%(원인 미상 — brush51 swatch.note_bias)가 눈에
                    걸리는가 · 51 성능의 크기·부호(헤드리스 판정 불능 — perf.note_82) ·
                    압력 플레이크 진폭(3행)이 폭 술어 여유(4행)와 같은 눈금인 것(#14)) +
                    **㉔ 무늬 선의 질**(web2-52 ⚑ — 텍스처에 구운 무늬가 49의 GL 선분과
                    같은 «제도 선»으로 읽히는가(AS-C168): 축소 표집의 뭉갬 · 알파 1 선의
                    날 · 줄눈 대비. 짝: 프리셋 찬 칸을 길게 눌러 «확인 없이 덮이는» 것이
                    실수를 부르는가(34-0 #30 ㉡ — 부르면 되돌리기/확인의 사양이 열린다)) +
                    **㉕ 칠의 자리**(web2-54 §2 — 헤드리스가 값은 냈고 «손맛»이 남았다:
                    획이 시작한 면에만 남는 것이 그리기에 자연한가 · 여러 면을 «꾹 눌러
                    고르고 칠 도구로 바꾸는» 길이 손에 걸리는가(안내는 상태 줄뿐) ·
                    빈 곳 꾹 누름 해제가 발견되는가 · 정면 왕복(다시 누르면 직전 시점)이
                    기대와 맞는가 — 특히 궤도를 돈 뒤 「정면」이 «복귀»가 아니라 «새
                    비행»이 되는 D-W23의 갈래) +
                    **㉘ 구간 잔차**(web2-57 — AS-C173: 다중 조각 구간의 단일 참조 —
                    비참조 형제 조각을 지우면 면이 산 채 경계 잉크에 틈(multi_piece.
                    then_sibling_erase), 옮기면 잉크만 가고 면이 안 따라온다(2.01단위/면 0 —
                    then_sibling_move). 실사용에서 눈에 걸리는가 — 걸리면 다중 참조
                    라운드가 열린다(DEFERRED web2-57 둘째 행)) +
                    **㉖ 두께의 손맛**(web2-55 — AS-C171: 안 칠한 벽의 «안 보이는 두께»가
                    혼란인가 · 모로 보이는 띠(~6px)를 손으로 칠할 수 있는가(안 되면 정면
                    뷰로 돌아 칠하는 흐름이 답인가) · 손통 「두께」+손글씨 mm 입력이
                    발견되는가 · 일괄/예외 재누름이 손에 맞는가 · 축척 미정에서 «값만 남고 안 그려짐»(상태 줄 경고)이 혼란인가(AS-C171 ③) · **두께 20면의 실기기 프레임**(dpr2 SW GL +18.3ms — GPU에서는 몇인가: ㉒의 짝))
```

**48이 끝났고 배포됐다**(2026-09-02 · `main 711b800` — 마감 블록은 NOTES 「마감 — web2-48」).
**다듬기 열셋** — 칠의 크기 트레이·단색 채움·색상 휠(culori) 기본·톤 자동 제거·면 기본
숨김(칠한 면 불투명)·칠의 «면의 쪽»(paint.s — 구조)·궤도 중 칠 유지·툴팁 전수(34-0 표 +
R9)·삼각자 세로 복귀·큐브 톤. ⚠ **다음 세션이 알아야 할 것 셋**:
1. **48-1(흰 장막)은 진단만 남고 수리가 되돌려졌다** — 위 ⑤. 재번호(48=다듬기 · 49=재료 ·
   50=내보내기)는 48 지시문의 명령이었고 실행됐다.
2. **이 라운드는 PITFALLS #98의 사고 회차다** — 라운드 통위임으로 완료 기록을 두 번 잃었다.
   하위 에이전트는 라운드 «안»의 자기 완결 항목에만.
3. **실기기 ⑰⑱⑲**(위 사람 몫)가 48의 눈 판정이다 — 특히 ⑲(면 숨김·불투명)는 AS-C161이 유보의 정본.

**47이 끝났고 배포됐다 — 사슬(44→47)의 마지막 라운드다**(2026-09-01 · 해시는 NOTES
「마감 — web2-47」). **크기가 보이고 세어진다** — 스케일바(1·2·5 유동 자 · 공간에 놓임 ·
축척 미정이면 안 뜸), 사람(표시 「사람 놓기」 — 눈이 지평선에 얹히는 구성 · 스텐실은 설정
에서 그려 기기에 저장 · **기본 그림은 비어 있다 — 사람이 그려 줄 자리**), 면적·부피
(근거 면이 밝아진다 — #61 「숫자는 그림보다 무겁다」가 이 회차의 중심), 실 다이어그램
(벽으로 닫힌 영역 → 버블+연결 · 앱 경로 양성 1). 부수 수리: 평행 사영의 지면 접지 붕괴
(광선 원점 — AS-C156 반증). ⚠ **다음 세션이 알아야 할 것 셋**:
1. **숫자의 신뢰는 축척의 신뢰를 넘지 못한다**(D-W17 · AS-C4) — 1점 문서의 f 감도 스윕이
   DEFERRED 첫 행이다. 48(내보내기)이 치수를 밖으로 낼 때 같은 유보가 따라간다.
2. **사람 스텐실 기본 몇 벌**이 비어 있다 — 사람이 그려 주면 그대로 실린다(형식·판·배선
   완비). 실기기 ⑯이 그 판의 눈 판정.
3. **AS-C156**: 평행 뷰 지면 앵커로 올라간 «옛» 좌표의 정오는 미판정(그 경로 픽스처가
   없다) — 평행 뷰 관련 이상 보고가 오면 그 행부터.

**46이 끝났고 배포됐다**(2026-09-01 · `main 50e5a73` — 마감 블록은 NOTES 「마감 — web2-46」).
**재료가 생겼다** — 칠통(붓 재누름): 재료 다섯(벽돌·콘크리트·유리·나무·금속 = 톤 둘셋 +
해칭 무늬 — 색 목록이 아니다) · 마커(marker46 — 내장 spacing만 0.2로: 겹치면 실제로
진해진다, 원장 대조 팔이 정본) · 색연필. 분류가 톤을 «제안»한다(자동 — 사람이 고르면
그 값이 남는다). 면 재료(손통 「재료」)가 채움 해칭의 무늬·색을 정하고, 이색 해칭이
생기면서 45가 미룬 **깊이 순서 픽셀 판별**이 섰다(다툰 픽셀 · D-3 반증 포함).
저장 +4(paint.m/t/i · Face.mat — 게이트 ② 빨강 확인). ⚠ **다음 세션이 알아야 할 것 셋**:
1. **46의 ⚑는 실기기 ⑮다**(마커·색연필·톤·무늬가 «쓸 만한가» — 눈으로만 갈린다.
   AS-C153·C152가 그 유보의 정본).
2. **마커 띠는 경로에 비대칭이다**(위 ~9px·아래 ~3px — mats46_e2e marker_seam의
   프로파일). 칠 팔을 새로 짤 때 상자를 경로 대칭으로 놓으면 46 2차 [1]을 재밟는다.
3. **paintSel은 세션 상태다**(저장 안 됨 — DEFERRED). 47이 표시(스케일바·사람)를 얹을 때
   같은 물음(기기 저장인가 문서인가)을 만난다 — 사람 스텐실은 지시가 «기기 저장»을 명시했다.

**45가 끝났고 배포됐다**(2026-09-01 · `main 81c56c3` — 마감 블록은 NOTES 「마감 — web2-45」).
**면이 보이게 됐다** — 칠(붓 도구 · 면 위 lift · 흑연 질감)과 채움(해칭 — 화면/면 두 판 ⚑),
법선 분류(슬라브·벽·경사 · 손통 「분류」 정정), 깊이 정렬(차례 의존 → 화가 알고리즘 0/33).
저장 필드 셋(paint·cls·fill — KEY_ORDER·게이트 ② 통과). ⚠ **다음 세션이 알아야 할 것 셋**:
1. **⚑는 해소됐다** — 답 = 화면 고정(2026-09-01 · §2 ✓ 줄과 D-W15 갱신분이 정본).
2. **새 결함 둘이 DEFERRED에 있다**: 스침 병합(2.2px 스침 → 면 짜깁힘) · 공선 겹침(깊이선
   위 격자 → 셀 9→3). 실기기 「면이 이상하다」가 오면 그 두 행부터.
3. **픽셀 순서 판별은 46 몫이다** — 같은 색 반투명은 over 합성이 교환이라 순서가 픽셀에 안
   실린다. 46이 첫 이색 겹을 만들면 setFaceSortForTest(끔)와 픽셀 대조로 팔을 세운다.

**44가 끝났고 배포됐다**(2026-09-01 · `main a639718` — 사슬 44→45→46→47의 첫 라운드 ·
마감 블록은 `web2/NOTES.md` 끝의 「마감 — web2-44」). **선택이 생겼다** — 39의 꾹 누름이 잡기까지
한다(치수숫자>값표찰>선>잠긴선>면 — D-W14가 규약 정본). 잡은 것 위에서 끌면 옮김(축
제한)·끝을 끌면 돌림(수직축)이고 값은 손글씨가 고친다. 손통(핀셋)에 복제·잠금·맺기(R=0
연장만)·정면(면이 정한 뷰 — 평행)이 들었다. `Stroke.lock`이 저장 필드에 늘었다(KEY_ORDER
등재 — 43의 게이트 ②가 실제로 빨개져 잡았다). 띠는 짝 줄 둘로 재편(여유 31→68px@창800).
⚠ **다음 세션이 알아야 할 것 셋**:
1. **짝 줄은 상단 140px(뷰 큐브 대역) 아래에서만** — 초판이 시점 셋을 맨 윗줄에 눕혀
   큐브 가운데(투시) 누름을 덮었다(flow.spec이 잡았다). 손잡이를 더 눕힐 때 그 규칙.
2. **⚑(의존 갈래)는 ㉡+이어잡기로 갔다** — 측정이 가른 것은 «㉢가 정보를 파괴한다»뿐이고
   ㉠ 기각은 설계 근거(D-W8)다(원장 `grip44_web2.json`의 `verdict`가 경계 문면).
3. **소실점을 만든 획은 못 잡는다**(카메라 방어 — D-W14). 벽 밑변이 깊이선이면 밑변 없이
   잡힌다 — 실기기 관측 판정자(DEFERRED).

**43이 끝났다**(2026-09-01 · 마감 블록은 `web2/NOTES.md` 끝의 「마감 — web2-43」).
문서가 **IndexedDB**로 옮겨 갔고(`core/store.ts`) 문서마다 **불변 열쇠와 이름**이 생겼으며
파일 서랍에 **최근 드로잉 목록**(썸네일·이름·마지막 수정)이 들어왔다.
⚠ **다음 세션이 알아야 할 것 셋**:
1. **저장 자리가 바뀌었다** — `localStorage.getItem('b2-autosave2')`를 읽는 팔은 이제
   **아무것도 안 잰다**. 읽고 쓰는 자리는 `e2e/store43.ts` 하나다(`savedText`·`putSaved`·
   `clearStore`·`waitSaved`·`bootDone`). `localStorage.clear()`도 「앱을 비운다」가 아니다.
2. **「비우기」의 뜻이 바뀌었다** — 지금 그림을 **굳히고 새 문서로** 간다(최근 목록에 남는다).
   지우는 자리는 목록의 `×`다. ⚠ 이것은 **지시문에 없는 갈림**이라 사람의 판단을 기다린다.
3. **저장물의 열쇠 차례를 쓰는 쪽이 정한다**(`file.ts`의 `KEY_ORDER`). **새 필드를 더하면
   그 목록에도 적어야 한다** — 안 적으면 저장에서 사라지고, `test/roundtrip43.test.ts`의
   게이트 ②가 그 자리에서 빨개진다.

⚠⚠ **40이 「완료·배포」로 올라온 것은 판정자가 그 조건을 채운 뒤다**(#58 · #89):
`deploy-check.mjs`가 원격 main = `2a15097`이고 **화면 빌드 식별자도 `2a15097`**임을 냈다.
⚠ 그 전 상태에서 이 파일 §1이 「끝났다」로 적혀 §2와 갈렸던 것을 1차 리뷰어 [14]가 잡았다 —
§0의 규칙 2(「라운드를 마칠 때도 같은 커밋에서 §2를 갱신한다」)를 어긴 것이다.
**「끝났다」는 저장소 밖 사실을 딛어야 하는 말이고, §2가 정본이다.**
⚠⚠ **40은 전량 초록이 아니다** — 일곱이 빨간 채로 배포됐고 **일곱 전부 HEAD에서도 같은 값으로
빨갛다**(`git stash`로 확인 · NOTES의 「빨강 귀속」 표). 다음 세션이 더 빠른 기기에서 돌리면
그 상태부터 다시 본다.

**⚠ 이 대기열은 2026-08-31에 사람이 통째로 갈아 끼웠다**(`docs/instructions/web2-39.md` §0㉣).
바뀐 것 둘: ㉠ **37-3은 장식이므로 뒤로 밀렸다**(종전 「다음」이었다) ㉡ **39·40·42가 앞에 섰다** —
39는 사용자가 실사용에서 찾은 **살아 있는 결함**이다(세로 작도선이 「1」로 읽혀 치수가 붙는다).
36·38은 종전 순서 그대로 맨 뒤다. 37-5는 **web2-41 2번이 이미 되살려 넣었다**(역할이
「동점 처리」에서 「애매함을 푸는 유일한 손잡이」로 바뀌었다).

**37은 2026-08-31에 네 절이 끝났다** — 37-1 가상 교차와 우선순위 표 · 37-2 대기는 옅은 청색 ·
37-4 대기 획은 광선이 바뀌면 버린다 · 37-6 오스냅 당김 기준선. 회차 기록은 `web2/NOTES.md`의
네 절이 정본이다. **37-3·37-5는 그 회차에 착수하지 않았다**(RUN.md가 분량이 모자라면 자르라고 했다).
⚠ **37-5는 web2-41 2번이 끝냈다**(역할이 「동점 처리」에서 「애매함을 푸는 유일한 손잡이」로 바뀌었다).

**이 회차가 함께 한 것 둘**(RUN.md §1·§2 — 37과 별개의 지시):

- **원장 쓰기 관문**. `LEDGER=1`이 없으면 `stage0/out`에 한 바이트도 안 쓴다. 판정부는
  `web2/tools/ledgercore.ts` 하나이고 배선이 둘이다(vitest는 `test.alias`, playwright는
  설정 최상단 import — **런타임마다 `node:fs`가 도달하는 길이 다르다**). `selfcheck.py`의
  `scan_ledger_guard`가 그 배선 넷을 지킨다. **PITFALLS #90**.
- **PITFALLS #89 「초록의 범위를 적어라」** 신설 · **#88을 검사 팔 안의 상수까지** 넓혔다.

### 마감 — **web2-39** (2026-08-31)

**도는 백그라운드**(#81 ㉤ — 2026-09-01에 생긴 조항이라 **이 회차는 소급이 아니다**.
아래는 그 조항을 세운 날의 **실측**이고, 다음 회차부터는 마감 시점의 값을 적는다):

```
상한 없는 대기       0   ← web2-39가 남긴 배포 대기(12시간 51분)를 사람이 잡아 끊었다
도는 개발 서버       1   ⚠ localhost:5301 (web2 · 14시간 37분) — **다음 회차가 내리고 새로 띄운다**
```

⚠⚠ **그 서버는 #70이 이름으로 규정한 자리다**(오래 떠 있는 개발 서버가 옛 빌드를 낸다).
playwright의 `reuseExistingServer`가 그것을 **그대로 물려 쓴다** — 다음 세션이 착수 시
`preview_stop` 뒤 새로 띄우지 않으면 옛 빌드를 재고도 초록이 나온다.
⚠ 그리고 이 줄은 **센 값**이지 「없다고 생각한다」가 아니다(#89의 형태).


```
main 5afb498 · cd web2 && node tools/deploy-check.mjs
  $ git ls-remote origin main
  5afb4982c302fa81757ed002bd20d4ffc664b38d  refs/heads/main
  내 커밋   5afb4982c302fa81757ed002bd20d4ffc664b38d
  원격 main 5afb4982c302fa81757ed002bd20d4ffc664b38d      ✓
  Pages 실행 completed / success (attempt 1)
    https://github.com/hyunwookyu/brunelleschi/actions/runs/33363467096
  화면 빌드 식별자 5afb498 == 배포 커밋                    ✓
  결과: **배포됨**
```

⚠ **그 뒤의 커밋은 문서뿐이다** — `git diff --name-only 5afb498..eaabbaa`가
`web2/NOTES.md` · `web2/HANDOFF.md` · `stage0/out/selfcheck.json` 셋이고
**`web2/src`·`e2e`·`test`·`index.html`은 0개**다. 즉 화면에 있는 앱은 이 회차의 제품 코드
그대로이고, 뒤따르는 Pages 실행이 바꾸는 것은 **빌드 식별자 문자열 하나**다.
(그래서 이 블록의 판정 해시는 `5afb498`로 둔다 — 판정자가 확인한 것이 그 앱이다. 41이
같은 자리에서 같은 규약을 썼다.)

**⚠ 무엇이 초록인가**(#89 — 세 줄이 **다른 것**을 말한다):

| 무엇이 돌았나 | 어디서 | 결과 |
|---|---|---|
| 타입 · 단위 · 빌드 · `static_deploy.spec` **하나** | **CI**(`pages.yml`) | 실행 33363467096 success · 화면 식별자 `5afb498` |
| 타입 · 단위 · 빌드 | **사람이 손으로** | 타입 **0** · 단위 **855 / 0**(109파일) · 빌드 통과 |
| **전량 e2e**(dpr1+dpr2 · `--workers=1` · 39.5분) | **사람이 손으로** | **490 / 0** |

⚠ **전량 e2e는 `d88c48c` 트리에서 돌았다** — 그 뒤 배포까지 더해진 것은 **병합 커밋 하나**이고
`git diff --name-only d88c48c 5afb498 -- web2/src web2/e2e web2/index.html`가 **빈 목록**이다.
⚠⚠ **전량을 두 번 돌렸다** — 1회차 488/2의 빨강 둘은 **내 회귀**였다(`zones.spec`의 자리
전수표에 새 손잡이 `rng-whold`가 빠져 있었다). 표를 늘려 고쳤다(#75 ㉣). 2회차가 490/0이다.

**원장 쓰기 관문**(#90 · 판정자는 mtime과 막은 횟수 — #91): 전량 뒤 `git status stage0/out`이
**비어 있었고** 관문이 같은 실행에서 **18번 막았다**고 스스로 적었다.

**리뷰어 2회**(§4): 1차 **15건**(높음 셋) · 2차 **10건**(높음 하나).
2차가 «손으로 옮긴 값»을 잡아 ⑦의 근거를 **e2e가 스스로 쓰는 원장**으로 바꿨다.
남긴 둘은 `DEFERRED.md`.

---

### 마감 — **web2-41** (2026-08-31)

```
main c48b9e4 · cd web2 && node tools/deploy-check.mjs
  $ git ls-remote origin main
  c48b9e44345873396ec4f7fc83e5c50a81adf3c6  refs/heads/main
  내 커밋   c48b9e44345873396ec4f7fc83e5c50a81adf3c6
  원격 main c48b9e44345873396ec4f7fc83e5c50a81adf3c6      ✓
  Pages 실행 completed / success (attempt 1)
    https://github.com/hyunwookyu/brunelleschi/actions/runs/33351231930
  화면 빌드 식별자 c48b9e4 == 배포 커밋                     ✓
  결과: **배포됨**
```

⚠ **그 뒤의 커밋은 문서뿐이다** — `git diff --name-only c48b9e4..` 가 `web2/NOTES.md`·
`web2/HANDOFF.md` 둘이고 **`web2/src` 파일은 0개**다. 즉 화면에 있는 앱은 이 회차의 제품
코드 그대로이고, 뒤따르는 Pages 실행이 바꾸는 것은 **빌드 식별자 문자열 하나**다.
(그래서 이 블록의 판정 해시는 `c48b9e4`로 둔다 — 판정자가 확인한 것이 그 앱이다.)

**⚠ 무엇이 초록인가**(#89 — 두 줄이 **다른 것**을 말한다):

| 무엇이 돌았나 | 어디서 | 결과 |
|---|---|---|
| 타입 · 단위 · 빌드 · `static_deploy.spec` **하나** | **CI**(`pages.yml`) | 실행 33351231930 success · 화면 식별자 `c48b9e4` |
| 타입 · 단위 · 빌드 | **사람이 손으로**(이 세션) | 타입 **0** · 단위 **848 / 0**(109파일) · 빌드 통과 |
| **전량 e2e**(dpr1+dpr2 · `--workers=1` · 38.4분) | **사람이 손으로**(이 세션) | **484 / 0** |

`pages.yml`이 도는 e2e는 `static_deploy.spec.ts` **하나뿐**이다 — 「Pages 초록 = 전량 통과」로
읽지 마라. ⚠ **전량 e2e는 `3895308` 트리에서 돌았다**: 그 뒤 `src/`에 더해진 것은
`xintAmbigFinal` 하나이고 **제품 경로에서 아무도 안 부른다**(`git diff 3895308 9553519 -- web2/src/`가
그 함수와 주석뿐이다). 그 근거를 적지 않고 「전량 통과」라고만 쓰면 #89를 어기는 것이다.

**원장 쓰기 관문**(#90 · 판정자는 mtime과 막은 횟수다 — **#91**): 전량 e2e 뒤
`git status stage0/out`이 **비어 있었다**. LEDGER 없이 돈 실행이 한 바이트도 안 썼다.

**리뷰어 2회**(§4): 1차 18건 · 2차 18건. 2차에서 **주장 둘을 약화시켰고**(게이트 ⑤의
「상한이다」가 추론이었던 것 · 「무해함은 대역이 아니다」가 표보다 센 주장이었던 것)
**남긴 둘은 `DEFERRED.md`로** 올렸다(원칙 d 관문의 판별력이 원장 밖 · 필압 극값의 시드 변동폭).

---

### 마감 — web2-37 (2026-08-31)

```
main dbebc5e · node tools/deploy-check.mjs
  원격 main dbebc5ef268b45f9945fa6cb73e6cff7d74afd45 == 내 커밋
  Pages 실행 (attempt 1) https://github.com/hyunwookyu/brunelleschi/actions/runs/33344051102
  화면 빌드 식별자 dbebc5e == 배포 커밋      → 결과: **배포됨**
```

**⚠ 무엇이 초록인가**(#89 — 이 회차가 등재한 조항이다. 두 줄이 **다른 것**을 말한다):

| 무엇이 돌았나 | 어디서 | 결과 |
|---|---|---|
| 타입 · 단위 · 빌드 · `static_deploy.spec` **하나** | **CI**(`pages.yml`) | Pages 실행 attempt 1 · 화면 식별자 `dbebc5e` |
| **전량 e2e**(dpr1+dpr2 · `--workers=1` · 38.1분) | **사람이 손으로**(이 세션) | **484 / 0** |

`pages.yml`이 도는 e2e는 `static_deploy.spec.ts` **하나뿐**이다 — 「Pages 초록 = 전량 통과」로
읽지 마라. 전량은 사람이 도는 세션에서만 돈다.

검증(병합 전): 타입 **0** · 단위 **832 / 0 / 832**(108파일) · 빌드 통과(2.28s) ·
전량 e2e **484 / 0 / 484**.
⚠ **전량을 두 번 돌렸다** — 1회차 479/5에서 나온 빨강 셋을 고쳤기 때문이다.
셋 다 **내 회귀**였다(`main` 37792bd 트리에서 같은 팔 22/22 통과 — #80대로 갈랐다).
**제품 코드는 안 고쳤다**(전부 팔). 2회차가 위 수치다.
⚠ 2회차 **뒤에** `paper.spec` 하나를 더 고쳐(출하 색 측정을 테스트 끝으로) 그 스펙만
다시 돌렸다 — **8/8**(dpr1+dpr2). 나머지 스펙은 2회차 이후 안 바뀌었다.

**§1 원장 쓰기 관문의 게이트**(#90):
1회차에서 원장 sha256 **200개 전수 동일** · `git status stage0/out` 비어 있음 — 전량이
**한 바이트도 안 썼다**. ⚠ 판정자는 해시가 아니라 **mtime과 막은 횟수**다(AS-C138:
관문 이전의 덮어쓰기에서도 sha256이 전수 같았다).

## 3. 이월 · 보류

- **30-13** 세게 누르면 질감이 준다 — 낮은 우선(`DEFERRED.md`)
- **성능**(200~400획) · **저장·불러오기 왕복** — 사용자가 미뤘다
- **근평면 클리핑** — 헤드리스에서만 나고 실기기 재현 실패. **계측 조건 조사 미착수**
- **원장 오염 산술의 1.74배 잔차** — 미해명
- ✅ **오스냅 시작점 획득 반경 — 37-6이 값을 보고했다**: **8 화면 css px**
  (`constants.ts`의 `OSNAP_RADIUS_PX` · 화면 슬라이더로 4~20). 41획 도면에서 허공 시작을
  **당긴 비율 1274/3600**(기준 시드 이동 중앙 3.82px · 최대 7.999 = 반경이 상한).
  ⚠ 종류 분포 `near` **391 / 568**(end 69 · mid 45 · **int 48** · vertex 15)은 **획득**의 것이다
  (37이 적은 405/563은 낡았다 — `int`가 22 → 48로 배가 됐고 그것이 「3D 교차 83 → 116」과 같은 방향이다) — **당김 칸만의 분포는 원장에 없다**
  (37 회차가 두 축을 갈라 놓고 문장에서 다시 합쳤다 — 리뷰어 [12]). 반경 곡선(당김/1200): 0→0 · 4→178 · 8→**413** · 12→611 · 16→758 · 20→873.
  **값은 안 바꿨다** — 줄일지는 **실기기 확인 ⑧**을 보고 정한다. 원장 `osnappull37_web2.json`
  ⚠⚠ **이 수는 web2-41이 다시 돌린 것이다**(#42 ⑥ · 37이 남긴 1229/3600 · 3.50px · 4→170 ·
  8→394 · 12→593은 **낡았다**). 41-1이 애매한 획을 대기로 돌려 같은 장면의 자립이
  **35 → 29**(대기 6 → 12 · 3D 교차 83 → 116)이 됐고, 후보가 줄면 당김이 **오히려 늘었다** —
  끝점 후보가 사라진 자리에서 `near`가 대신 물기 때문이다. 종류 목록이 반경보다 큰 손잡이라는
  37-6의 관측(D-4)을 이 변화가 한 번 더 가리킨다.
- ⚠ **오스냅 반경 환산이 세 자리에서 갈린다**(37-6이 재다가 찾았고 안 고쳤다 — 범위 밖):
  `state.ts`의 `measureTap`·`pickDimTarget`·`defineByTouch`가 `viewScale` 대신 `view.s`로
  나눠, **보기 렌즈가 걸리면** 그리기 경로와 재기·치수 경로의 실효 반경이 `lensK`만큼 갈린다
- **v1.0 태그와 화면 버전 표시** — 실기기 확인이 끝난 뒤
- `web2` 라인에 **`constantsSnapshot()`/`metric_defs` 기계가 아직 없다** — web2 원장 전부가
  STALE 판정 불가다. 종전부터의 유보다
- ✅ **`ui34.spec ④`의 선재 결함은 이 회차가 닫았다**(마감 리뷰어 [2] — A-4를 한 회차 안에서
  두 기준으로 쓴 것을 잡았다). 전량 1회차에서 **6.839225** ↔ 문 6.601로 빨갰고 `main`
  (`c4932ab`) 트리에서도 똑같이 빨갰다(#80대로 갈랐다 — 31의 회귀가 아니었다).
  **임계를 무르지 않고 둘을 고쳤다**: ㉠ 팔의 문 `6.6`을 **창 `rect`에서 유도**(#88 —
  31-3이 `flow.spec`에 한 것과 같은 수리다) ㉡ 각인이 창에 들게 제품을 고쳤다 →
  **6.599998**. ⚠ **초판 진단이 절반만 맞았다**(D-4): 글꼴만이 아니라 `.tool.on`의
  `scale(1.14)`가 두 번째 원인이었고(같은 글자의 잉크 9.364343 ↔ 9.708766),
  `textLength`는 34-3이 이미 걸어 뒀지만 그것이 묶는 것은 **전진폭**이지 `getBBox()`가
  내는 **잉크**가 아니었다. 덤으로 34-2가 「10에 12.23」이라 적고 넘어간 연필 각인도
  닫혔다(**10.000000** ≤ 10.000000). 원장은 `close31_web2.json` · **AS-C135**
- ⚠ **CI는 전량 e2e를 안 돈다** — `.github/workflows/pages.yml`이 도는 e2e는
  `e2e/static_deploy.spec.ts` **하나**뿐이다(typecheck·`npm test`·build는 돈다).
  그러므로 **전량은 사람이 도는 세션에서만 돈다** — 「초록이니 배포됐다」로 읽지 마라.
  **넣으면 배포마다 34분이 붙는다 — 사용자가 정할 일이다**(RUN.md §8). 정해질 때까지
  마감 보고에 **「무엇이 초록인가」를 두 줄로** 적는다(**PITFALLS #89** — 이 회차가 등재했다)
- **37-3(교차 강조)·37-5(끝의 필압)** — 착수 안 했다. ⚠ **37-5의 근거가 바뀌었다**:
  ⚠⚠ **이 근거는 web2-41이 반증했다**(AS-C140) — 그 3.141은 한 3D 선 «위에서»의 이동이라
  배치를 안 바꾼다. 37-5는 **web2-41 2번이 넣었고**, 그 자리의 값은 이제
  「누름이 애매함을 푼다」(같은 장면 14/14)와 「무릎 1.91 · 문 2.00」이다.
- **`core/waitfade.ts`의 감쇠 판정이 대기 획에 대해 도달 불가다** — 37-4가 시점 변경에서
  대기 획을 버리므로 「다른 시점에서 대기 획을 그리는」 국면이 없다. **코드를 안 지웠다**
  (A-4: 폐기 코드를 지우기 전에 게이트를 통과시킨다). 지울 조건은 `DEFERRED.md`
- **원장 쓰기 관문이 생겼다** — `LEDGER=1` 없이는 `stage0/out`에 **한 바이트도 안 쓴다**.
  새 하네스는 아무것도 안 해도 막힌다(판정부 `web2/tools/ledgercore.ts` 하나). **PITFALLS #90**
- **손 띠(세로바) 예산이 포화됐다** — 31이 돋보기·렌즈 둘을 넣어 여유 105px → **31px**.
  다음에 손잡이를 더하는 항목은 **자리를 새로 내야 한다**(`DEFERRED.md`)
- `DEFERRED.md`의 항목들
- ⚠ **§1의 42-시대 이월 주석((0)~(0-e))은 56 마감이 걷었다** — 그중 아직 사는 것:
  settleSlide 미호출 감시 부재(DEFERRED 그 행) · 40 실기기 G4·G5·G6(docs/DEVICE-CHECK.md)

---

## 4. 사람 몫 — 실기기 (헤드리스가 못 대신하는 자리)

자리는 `docs/DEVICE-CHECK.md`.

① **선따기 → 지우개로 바깥 토막 자르기 → 겹 끄기 → 종이에 선이 몇 개**
   ⚠ 34-3이 지우개를 **크기통 넷**으로 바꿨다(반경 5·12·27·60 px) — 그 크기가 손에 맞는지가
   같이 걸린다. 견본은 1:1이라 화면에서 본 크기가 곧 지우는 크기다.

② **종이에 숫자 쓰기가 실제로 되는가** — 치수선이 되는가 · 획이 흩어지지 않는가 ·
   대상이 맞게 잡히는가 · 사후 수정 · 실행취소 한 번에 손글씨로.

③ **사용자 글씨로 숫자 열 개를 써서 몇 개가 맞는가.**
   ⚠⚠ **합성 87.5%(198/220)는 사람 손을 예측하지 못한다.** 「4」가 20/20인 것도 그 픽스처가
   인식기 원형 표와 **좌표로 같은 획**이라 표본 «안»의 값이다 — 손으로 쓴 4는 미답이다.
   **이 수 하나가 web2-36(자형 등록)의 착수 여부를 정한다.**

④ **(예약 — ⑧로 대체됐다.** RUN.md §9가 그렇게 정정했다. 아래 ⑧을 보라.)

**37이 들어간 뒤**(2026-08-31 · RUN.md §9 — 이 넷이 다음 라운드의 착수 조건이다):

⑤ **허공에서 선을 쫙쫙 그어 교차시켜라. 선이 서는가, 대기로 남는가.**
   ⚠⚠ **물음이 바뀌었다**(web2-41 · 아래 옛 문면은 반증됐다 — AS-C140).
   ~~자가 고른 것이 의도한 것과 갈리는 비율 0.8125 · 깊이 갈림 3.141~~ 그 3.141은
   **한 3D 선 «위에서»의 이동**이라 배치를 안 바꾼다. 41-1이 물을 것은 이제 **반대쪽**이다:
   **「서야 할 선이 자꾸 대기로 남는가」.** 붐비는 장면에서 대기는 이제 정상 거동이고
   (깊이가 갈리는 판에서 23/24가 대기다), 그것이 **손에 답답한지**가 이 칸의 물음이다.
   답답하면 `XINT_AMBIG_RATIO`를 올린다. ⚠ **실제로 훑은 문턱은 0.005 · 0.01 · 0.02 셋**이고
   그 셋에서 「잘못 놓인 선 0」이 선다 — 그 위는 **외삽**이다(문턱을 올리면 획이 서고 장면이
   자라 분모가 바뀐다). 올릴 거면 `xint41` 팔을 그 값으로 다시 돌려라.
   ⚠ 함께 볼 것: 획 끝에서 **한 번 꾹 누르면** 그 교차로 선다(41-2 · 화면에 안내는 없다).

⑥ **대기 선이 옅은 청색으로 보이는가. 정착할 때 흑연으로 바뀌는 게 보이는가.**
   ⚠⚠ **실측 대비가 흑연의 절반 아래다**(web2-37이 출하 색으로 잰 값 · web2-41 4번이 여기 붙였다.
   정본은 `paper.spec`의 출하 색 측정이고 재는 조건은 **「종이 세 장 아래에서의 대비」**다):

   ```
   대기 청색   31.0 (dpr1) / 16.7 (dpr2)
   흑연        70.0 (dpr1) / 58.7 (dpr2)
   ```

   **dpr2에서 흑연의 3분의 1도 안 된다.** 실기기에서 안 보이면 37-2가 하려던 일을 못 한다 —
   대기 획이 「아직 안 섰다」고 말하는 것이 41-1의 대기가 늘어난 뒤로 **더 중요해졌다**
   (붐비는 장면에서 획이 대기로 남는 것이 정상 거동이 됐다). ⛔ **값은 이 라운드에서 안 바꿨다** —
   무엇이 「읽히는가」는 코드가 정할 일이 아니고, 이 칸의 답을 받은 뒤 별도로 정한다(41-4의 문면).
   ⚠ 색은 `#a4dded`(논포토 블루)이고 마우스로는 「보이되 옅다」를 못 잰다.
   되돌릴 손잡이는 `WAIT_INK` 하나다(알파를 건드리면 등급 축과 다시 섞인다).
   ⚠ 정착 전이 300ms 동안 `#gl`(three.js 선)은 이미 흑연이라 **흐린 청색 번쩍임**으로
   읽힐 수 있다 — 그것이 거슬리는지도 함께 보라(37-2가 범위 밖으로 뒀다).

⑦ **시점을 돌렸을 때 대기 선이 사라지는가. 화면을 밀었을 때는 남는가.**
   ⚠⚠ **「민다」의 뜻이 국면마다 다르다**(리뷰어 [13] · AS-C132와 같은 자리): **작도 포즈**의
   팬·줌은 화면평면이 미끄러질 뿐이라 남고, **궤도 뒤**의 팬·줌은 카메라를 3D에서 옮기므로
   **광선이 바뀌어 사라진다**. 지시문 표는 「이동·확대는 따라간다」라고만 적었다 —
   그 좁힘은 기하가 시킨 것이고 **사람 손에서 결함으로 읽히는지가 이 칸의 물음**이다.
   ⚠ 실행취소가 **그 궤도까지** 무른다(획만 되돌리면 다음 시점 변경에서 또 사라져서다 —
   실측 AS-C139). 그 「한 번에 통째로 무른다」가 손에 자연스러운지 보라.

⑧ **시작점이 근처 끝점으로 끌려가서 거슬리는가**(옛 ④).
   기준선: 반경 **8 화면 px** · 41획 도면에서 허공 시작을 당긴 비율 **1274/3600** ·
   기준 시드 이동 중앙 **3.82px**. 거슬리면 오스냅 반경을 줄이는 **별도 라운드**다 —
   반경 곡선이 원장에 있다(4→178 · 8→413 · 12→611 / 1200).
   ⚠ 이 수는 **web2-41이 다시 돌린 것**이다(37의 1229/3600 · 3.50px는 낡았다 — #42 ⑥).

---

⑨ **(34가 새로 만든 자리)** 위 띠에 **파일·설정**이 올라왔고 **작도 시점**이 손 띠로 내려왔다.
   롤 둘이 **롤통**이 됐다. 「그리는 중에 쓰는가」로 가른 것이 손에 맞는지.

---

**40이 들어간 뒤**(2026-08-31 — 셋 다 「사람 눈이 판정한다」가 지시문에 못 박힌 자리다):

⑩ **G4 — 겹(트레이싱지·옐로)의 결이 이제 곱게 읽히는가.**
   사람이 낸 것은 「롤 텍스쳐가 너무 크다」였다. 섬유 **길이 ×0.5 · 개수 ÷0.5**(면적 밀도
   보존 — 진폭은 3.914 → 3.913으로 안 움직였다)이고 **굵기는 안 건드렸다**.
   상관 길이가 **0.743~0.827배**로 줄었다(`paper_grain40_web2.json`).
   ⚠ **0.5는 게이트가 강제한 값이 아니라 «눈»이 고른 값이다**(훑기가 초판의 「더 못 내린다」를
   뒤집었다 — AS-C145): dpr3/dpr1 결 비는 K 1.0 → **1.1073** · 0.5 → **1.1364** · 0.35 →
   **1.1369**로 아래로 갈수록 덜 오르고 문(1.15)까지 여유 **0.0136**이 남는다(같은 K 두 번의
   폭 **0.0000** — #14를 값으로 지났다. ⚠ 이 수는 K 손잡이 경로 `k_sweep.rows`의 것이고,
   제품 경로의 게이트 값 `gate.ratio`는 1.1366으로 0.00025 다르다 — 섞어 인용하지 마라). ⚠ 훑은 것은 **0.35까지**다 — 그 아래는 안 쟀다.
   K를 따라 움직이는 바닥은 **반감 지연**이고 K = 0.2에서 **1.19986으로 문(1.2)을 이미 밑돈다**.
   ⚠⚠ **0.35 칸은 진폭이 +8.2% 뛴다** — 내려가려면 그 칸에서 게이트 넷을 다시 재라(`DEFERRED.md`).
   「너무 잘아졌다」면 `OVERLAY_LEN_K`를 0.6~1.0으로 올린다.

⑪ **G5 — 겹을 얹고 걷을 때의 동작이 손에 맞는가.**
   **왼쪽에서 밀려 들어오고 같은 쪽으로 물러난다 · 300 ms**(`LAY_SLIDE_MS` = `FOLD_ANIM_MS`).
   물음 셋: 길이가 「두 번째부터는 기다림」인가(내린다) · 들어오는 쪽이 왼쪽이 맞는가 ·
   **동작 중에 그어도 걸리는 것이 없는가**(획이 들어오면 창을 즉시 닫는다 — 헤드리스는
   그것을 값으로 재지만 «손에 걸리는 느낌»은 못 잰다).
   ⚠ **걷을 때 그 겹의 획은 함께 물러나지 않는다**(알려진 강등 — `DEFERRED.md`).
   그 어긋남이 눈에 띄는지도 이 칸에서 본다.

⑫ **G6 — 말린 앞 가장자리가 «두께»로 읽히는가.**
   폭 **14 CSS px** · 가장 짙은 알파 **0.18** · 종이가 자리에 놓이며 펴진다(동작의 뒤쪽 58%).
   그늘 띠가 따로 보이면 좁히거나 옅게, 두께가 안 읽히면 반대로. 손잡이는
   `C.LAY_CURL_PX`·`C.LAY_CURL_ALPHA` 둘이다.

---

**44가 들어간 뒤**(2026-09-01):

⑬ **잡기 — 선을 꾹 눌러 잡고 끌어 옮겨 보라.** 볼 것 다섯:
   ⓐ 꾹 누름의 두 뜻(치수 쓰기 ↔ 잡기)이 손에 헷갈리는가 — **잡은 채 숫자를 쓸 때
   첫 획이 옮김으로 먹히는가**(AS-C150이 그 가정이다 — 먹히면 그 행이 값으로 채워진다)
   ⓑ 잡힘 표시(끝점 고리)와 글씨 대상 강조(밑줄 띠)가 **갈려 보이는가** — 색은 같고
   모양만 다르다(44 리뷰어 [6]의 눈 몫)
   ⓒ 벽을 옮겼을 때 딸린 선의 틈(픽스처 실측 58.5px@1.42유닛)이 «고칠 수 있는 상태»로
   읽히는가, 「고장」으로 읽히는가(⚑의 눈 몫) — 답답하면 「이어진 것 잡기」(같은 선 꾹
   한 번 더)가 그 답인지도
   ⓓ 끝점을 끌면 돌아가는 것이 의도와 맞는가(축점 = 반대 끝 · 수직축 둘레만)
   ⓔ 잡기 반경(치수 고르기와 같은 대역)이 손에 맞는가 — 41획 장면 실측 119/123(떨림
   3px)이고 실장면 값은 이 눈이 판정자다(#46)

⑭ **(45가 새로 만든 자리 — 면·칠)** 면을 지정하고 칠·채움을 써 보라. 볼 것 넷:
   ⓐ **⚑ 해칭 두 판** — 표시 팝업 「해칭을 면에 붙인다」를 끄고(화면 고정) 켜서(면 고정)
   시점을 돌려 보라. 어느 쪽이 제도의 눈에 맞는가(**기본값은 임의다** — 이 물음이 45의 ⚑).
   ⓑ 칠(붓)이 면 경계에서 나뉘는 것이 손에 자연스러운가 · 허공에 그은 몫이 사라지는 것이
   놀랍지 않은가(픽스처 실측: 26점 붓 → 2획 + 허공 8점 버림·계수됨)
   ⓒ 분류 자동값(바닥 슬라브·벽 벽)이 실장면에서 맞는가 — 틀리면 손통 「분류」로 돌리고
   그 사례를 남겨 달라(FACE_CLASS_DEG 10°의 실표본 — AS-C151)
   ⓓ 겹친 면들의 앞뒤가 맞아 보이는가(깊이 정렬 — 픽스처는 두 벽 한 배치뿐이다)

⑮ **(46이 새로 만든 자리 — 재료 · 46의 ⚑가 이 항목이다)** 붓을 다시 눌러 칠통을 열고
   마커·색연필로 칠해 보라. **눈으로만 갈리는 물음 넷**(값은 다 섰다 — AS-C153):
   ⓐ **마커가 건축 표현으로 쓸 만한가** — 겹칠수록 진해지는 계단(한 번·두 번·세 번)이
   실물 마커의 손맛인가. 끝·솔기의 잔존이 «마커답게» 읽히는가, 지저분한가.
   ⓑ **색연필** — 마커와 나란히 쓸 때 가늘기·결이 색연필로 읽히는가(레티나에서 상대적으로
   가늘어진다 — DEFERRED 행. «너무 가늘다»면 그 행을 연다).
   ⓒ **재료 다섯의 톤·무늬** — 벽돌·콘크리트·유리·나무·금속이 팔레트에서 그 재료로
   읽히는가. 채움 해칭의 무늬(벽돌 빗금·콘크리트 교차·유리 성김·나무 결·금속 좁은 빗금)가
   갈려 보이는가.
   ⓓ **톤 자동(분류의 제안)** — 슬라브 밝음·벽 그림자가 실장면에서 맞는 제안인가.
   어긋나는 사례가 오면 AS-C152의 표를 고친다.

⑯ **(47이 새로 만든 자리 — 숫자와 표시)** 치수를 하나 매기고 볼 것 넷:
   ⓐ **스케일바**(좌하) — 지도 앱처럼 읽히는가. 시점을 돌릴 때 값·폭의 변화가 «그 깊이의
   자»로 납득되는가(평행 뷰에서는 균일해야 한다).
   ⓑ **사람** — 설정에서 스텐실을 그리고(가로선에 눈을 맞춘다) 표시 「사람 놓기」로
   세워 보라. 눈이 지평선에 얹히는가 · 크기가 공간의 자로 읽히는가 · **그려 준 스텐실
   몇 벌을 주면 기본값으로 싣는다**(지시의 그 자리).
   ⓒ **바닥면적·부피**(표시) — 근거 면이 밝아지는 1.6초가 충분한가 · 값이 도면의 감과
   맞는가(축척이 이상하면 사람·스케일바가 함께 이상해 보인다 — 그것이 신호다).
   ⓓ **실 다이어그램**(표시 토글) — 벽으로 닫힌 방에서 버블이 서는가 · 헛것이 없는가.

---
---

# 회차 기록 (참고)

> ⚠ **아래는 과거의 기록이다.** 그 안에 적힌 「다음 세션이 할 일」·「다음은 무엇」은
> **전부 무효**다 — 유효한 순서는 위 §1·§2뿐이다. 상세는 `web2/NOTES.md`가 정본이다.

## 🆕 web2-35 병합 · web2-34 (2026-08-30) — **규칙을 전수로 쓸어냈다**

### 어디까지 했나

| 단계 | 무엇 | 상태 |
|---|---|---|
| 0 | **web2-35 병합** | ✅ 배포까지 마감 — main **`eca5442`** · 화면 빌드 식별자 `eca5442` |
| 1 | `docs/instructions/web2-36.md` 저장 | ✅ **착수 안 함**(34 · 31이 먼저다) |
| 2 | **web2-34 여덟 항목 + 34-0 대조표** | ✅ 완주 · 리뷰어 12건 대응까지 |
| 3 | web2-31 | ⛔ **미착수** — 위의 「이대로 붙일 것」이 그 지시다 |

### 병합에서 나온 것 — **평행 세션이 같은 번호를 두 번 썼다**

web2-35가 web2-32 5·6·7과 평행으로 돌아 `AS-C117~C120`과 `PITFALLS #82`가 **둘씩** 있었다.
main에 먼저 든 web2-32의 것을 두고 web2-35의 것을 **`AS-C121~C124`·`#83`**으로 옮겼다
(본문·표·㉠~㉨ 인용·원장·하네스 주석까지). **앞으로 평행 세션이 있으면 이 자리를 먼저 본다.**
그리고 **어느 갈래도 합친 트리를 검증한 적이 없었다** — 그 첫 전량이 393/394였고
하나는 `gesture.spec` dpr2의 180s 타임아웃(혼자 돌리면 다섯 팔 전부 통과 — #80대로 갈랐다).

### web2-34가 세운 것 — 화면 규칙 여덟(`DECISIONS.md`가 정본)

R1~R5는 web2-28·30이 세운 것이고 **R6·R7·R8이 이 회차**다. R5는 이 회차 안에서 **한 번 정정됐다**
(아래 「가장 값진 것」). 34-0의 전수 대조표(요소 40 × 규칙 8)가 `web2/NOTES.md`에 있다.

### 항목별 한 줄과 그 수치

| 항목 | 무엇 | 수치 |
|---|---|---|
| 34-1 (R8) | 바탕 종이 결을 겹 아래로 · 설정에서 끈다 | 종이/겹 비 **0.985 → 1.46~1.64**(dpr1) · **0.864 → 1.333**(dpr2) · 끄면 0.000000 |
| 34-2 (R6) | 접힌 펜이 촉을 말한다 | 다섯 촉 `.18~.70` · 각인 높이비 **1.000**(연필과 같은 대역) |
| 34-3 (R1) | 지우개 막대 → 크기통 넷 | 견본 지름 **10/24/54/120 px** · 실제로 지운 반경이 그 안. **옛 막대는 실제의 27.5~112.5%였다** |
| 34-4 (R7) | 열린 통은 바깥을 눌러도 접힌다 | 안 접힘 **8/10 → 0/10** · 삼킴 0 |
| 34-5 | 설정 아이콘을 선 문법 톱니로 | 옛(채운) 아이콘이 새 검사에 **위반 6건** |
| 34-6 | 배치를 「그리는 중에 쓰는가」로 | 여유 **25px → 105px** · 31의 둘 넣으면 13px |
| 34-7 | 어긋남 문턱 | **옛 0.02는 흔들린 칸의 71.3%(719/1009)에서 뜬다.** 새 문턱 fold **2**(경계 아래 1.75 · 위 2.75 · 오탐 59/1229) |
| 34-8 | 잰 값의 정체 | 저장 형태는 **이미 정체였다.** 위약이 값 축 10/12 · 점 축 **24/24** 갈린다 |

### ⚠⚠ 이 회차에서 가장 값진 것 — **전수가 지시문에 없던 것을 넷 찾았다**

개별 증상 보고로는 하나도 안 나왔을 것들이다.

1. **접힌 겹 요약이 「맨 위 겹」을 말하고 있었다**(34-2 전수) — 아래 겹을 골라 두면
   **접힌 통이 틀리게 말한다.** 34-0 표·`DECISIONS.md`·지시문이 **셋 다** 「겹 요약은
   이미 지킨다」로 적고 있었다(AS-C126이 그 반증).
2. **표시 팝업이 치수 리본 뒤로 깔려 있었다**(34-6) — 겹침 **158×103**, web2-19 이래.
3. ⚠⚠ **그리고 통 넷이 통째로 리본 뒤로 깔려 있었다**(리뷰어 [1] → 34-6 후속) —
   연필통·촉통은 **web2-30 이래**, 크기통은 34-3 이래. `elementFromPoint`가 `pad-keys`·
   `dimpanel`을 냈다. 원인은 `#sidebar`의 **자기 쌓임 맥락**이 그 안의 z 12를 삼킨 것이고,
   **선언은 처음부터 옳았다** — 답은 z가 아니라 **맥락에서 꺼내는 것**이다(**PITFALLS #87**).
4. **위 띠를 넓히자 종이 띠가 파고들었다**(전량 e2e) — 예약이 **상수**로 적혀 있어
   옮긴 쪽과 말없이 묶여 있었다. `--topbar-reserve`로 뺐다.

**규칙이 이 과정에서 갈렸다**(`DECISIONS.md`의 「R5 정정」 · AS-C130):
판별 물음이 둘로 늘었다 — ① 동시에 열릴 수 있는가 ② **둘 다 오래 떠 있는가.**
①만 예면 재는 것은 「자리」가 아니라 **「순서」**다(`elementFromPoint`로).

### ⚠ 다음이 알아야 할 함정 · 정정

- **`#84 ㉢`를 같은 회차의 34-6이 어겼다** — 자리를 옮기고 `sidebar_layout_web2.json`을
  안 돌려 두 원장이 세로바 높이를 763 ↔ 683으로 다르게 말했다. **무엇을 옮기면 그 자리를
  재던 원장을 그 자리에서 다시 돌린다.**
- **34-1의 dpr1 진폭은 두 자리까지만 유효하다**(여섯 실행 2.392~2.680 · 폭 12%).
  ⚠⚠ **web2-40이 그 목록을 늘렸다 — 일곱 실행 2.392~2.803 · 폭 17%**(같은 자리가 2.803으로
  읽혔고 `git stash`로 자기 변경 탓이 아님을 확인했다). 그러면 **조항 ①(진폭 > 2.1)의
  여유 0.292가 폭 0.411보다 작다** — 「폭 밖이라 안전하다」고 못 적는다(#14 · `DEFERRED.md`).
  조항 ②는 최댓값에서도 비가 1.396(문 1.2)이라 여유가 있다.
  ⚠ 그 폭의 위험한 갈래 하나는 **닫혀 있다** — 바탕 결은 **로드마다 픽셀로 같다**(해시·sd가
  소수 여섯째 자리까지 동일). 17%는 제품이 아니라 **재는 절차**의 성질이다.
- **34-7의 「자연 분포」는 «재구성 흔들림»이지 손획 표본이 아니다.** 실기기 표본은 여전히 0.
- **34-8의 「값 축 10/12」는 픽스처가 정한 수다** — 네 구도가 전부 축척 기준을 «화면평행
  세로»로 두고 있다. 일반 진술로 인용하지 마라(DEFERRED에 있다).
- `web2` 라인에 `constantsSnapshot()`/`metric_defs` 기계가 **아직 없다** — web2 원장 전부가
  STALE 판정 불가다. 종전부터의 유보이고 이 회차가 만든 것이 아니다.

### 🙋 사람에게 남은 실기기 확인 (헤드리스가 못 대신하는 자리)

1. **선따기 → 지우개로 바깥 토막 자르기 → 겹 끄기 → 종이에 선이 몇 개**
   ⚠ 34-3이 지우개를 **크기통 넷**으로 바꿨다(반경 5·12·27·60 px) — 그 크기가 손에 맞는지가
   같이 걸린다. 견본은 1:1이라 화면에서 본 크기가 곧 지우는 크기다.
2. **종이에 숫자 쓰기가 실제로 되는가** — 치수선이 되는가 · 획이 흩어지지 않는가 ·
   대상이 맞게 잡히는가 · 사후 수정 · 실행취소 한 번에 손글씨로.
3. **사용자 글씨로 숫자 열 개를 써서 몇 개가 맞는가.**
   ⚠⚠ **합성 87.5%(198/220)는 사람 손을 예측하지 못한다.** 「4」가 20/20인 것도 그 픽스처가
   인식기 원형 표와 **좌표로 같은 획**이라 표본 «안»의 값이다 — 손으로 쓴 4는 미답이다.
   이 수 하나가 web2-36(자형 등록)의 착수 여부를 정한다.
4. **(새로 생긴 자리)** 위 띠에 **파일·설정**이 올라왔고 **작도 시점**이 손 띠로 내려왔다.
   롤 둘이 **롤통**이 됐다. 「그리는 중에 쓰는가」로 가른 것이 손에 맞는지.

## 🆕🆕🆕🆕🆕🆕 web2-32 (2) (2026-08-30) — **32-5 · 32-6 · 32-7** · 32는 이것으로 끝났다

**①' 검증 — 확인했다.** 타입 **0** · 단위 **727 / 0 / 727**(93파일 · 26.6s) ·
e2e 전량 **394 / 0 / 394**(`--workers=1` · dpr1+dpr2 · **30.7m**) · 빌드 통과(2.4s).
비용은 `test_cost_web2.json`이 정본이다(팔 +16 단위 · +8 e2e).

**①'' 배포 — 확인했다.** main **`07b92d0`** · `node tools/deploy-check.mjs`:
```
원격 main 07b92d07c1358547088a137b264800b50c122a2a == 내 커밋
Pages 실행 completed / success
화면 빌드 식별자 07b92d0 == 배포 커밋      → 결과: 배포됨
```
(병합 커밋 `629a282`가 먼저 나갔고, 그 뒤 문서 한 줄을 더 밀어 `07b92d0`이 현재 판이다.)
⚠ **배포를 기다리는 동안 또 밀지 마라** — 그때마다 Pages가 처음부터 다시 돈다(약 10분).
이번에도 그렇게 한 번 더 돌았다.

⚠⚠ **전량이 둘을 잡았다** — 하나는 제품, 하나는 팔:
- **제품**: 재기 단추를 세로바에 뒀더니 **800px 화면을 12px 넘쳤다**(812 · `sidebar.spec`
  dpr1·dpr2 + `zones.spec` id 목록). 임계를 안 무르고 **치수 패널로 옮겼다**(→ 763/800 ·
  `zones`의 목록은 손대지 않았다). 선례는 web2-29의 `btn-dim-write`다.
- **팔**: `autosave.spec ②`가 앞 세션의 **판본 게이트(#80)**에 막혀 부하에서만 죽었다 —
  문서를 안 바꾸고 리스너만 재점화하므로 앞선 저장이 이미 나갔으면 게이트가 막는다.
  **통과할 때조차 «재는 것을 재는지»가 운이었다.** 팔이 «다음 저장»을 실제로 만들게
  고쳤다(`docVersion++` — 임계도 대기도 안 건드렸다). 재현·수리는 NOTES 마지막 절.

**① 이 세션의 범위도 사람이 잘랐다** — 「32-5 · 32-6 · 32-7만 한다. 32-1~32-4 · 32-8은
이미 끝났다」. 그리고 **정정 셋을 함께 줬다**(그 셋이 이 회차의 방향을 정했다):

| 정정 | 무엇 |
|---|---|
| 32-5 | 「**만드는 일이 아니라 드러내는 일**이다. `lift.scaleOf` + `doc.scaleRef`로 이미 있다. 새 기제를 만들지 마라」 |
| 32-6 | 「기본값은 **패널에 표시만**이다 — 도면에 아무것도 안 남는다. 숫자를 저장하지 마라. 아이콘은 **이미 그려져 있다**」 |
| 32-7 | 「= web2-30의 **30-6**이다. 치수가 하나일 때 어긋남이 0인 것은 모형의 성질이 아니라 **표본 수의 성질**이었다. **표시하되 고치지 마라**」 |

| 항목 | 무엇 |
|---|---|
| 32-5 | **축척을 화면에** — `scaleOf`가 «고른 획»을 같이 낸다(`LiftResult.scaleId`). 「축척 미정」 ↔ 「**축척 1단위 = 1250 mm** · 기준 2400 mm」(⚠ 「1 : N」이 아니다 — 리뷰어 [8]). 왕복(미정→확정→미정→**고치기**→다음이 정함)을 팔이 잰다 |
| 32-6 | **재기** — `core/measure.ts`. 저장하는 것은 「어느 두 점」뿐이다(획 id + 선분 위 t · **면의 선례**). 기본은 표시만이고 도면이 안 변한다. 축척 미정이면 비율. 삼각 스케일 아이콘 정본 그대로 |
| 32-7 | **어긋남** — 「잰 값」은 **치수 적용 «전»** 길이다(`LiftResult.dimGeom`). 적용 뒤를 재면 **구성상 1**이고 그것이 29-2가 기능을 걷은 자리다(AS-C107 → **AS-C119로 뒤집혔다**) |
| (덤) | 치수 적용이 **세 갈래에 복사돼 있던 것**을 `applyDim` 한 자리로 합쳤다(#54) — 표식을 세 곳에 심으면 그 셋이 갈린다 |

**② ⚠⚠ 이 회차가 뒤집은 것 — 「구성상 0」의 원인은 **둘의 곱**이었다**

⚠ **초판 보고는 「진짜 원인은 «자»였다」고 적었다. 1차 리뷰어가 원장 자신으로 반박했다** —
`skew_first_dim`을 보면 **올바른 자**(적용 전)로 재도 치수가 하나면 비가 **1**이다. 29-2의
장면은 치수가 하나였으므로 두 자가 같은 답을 냈고, 그 관측만으로는 둘을 **가를 수 없었다**.

```
표본이 하나  →  어느 자로 재도 1        (30-6이 잡은 것 · 맞다)
자가 적용 뒤 →  표본이 둘이어도 1        (이 회차가 잡은 것)
둘 다 고쳐야 →  둘째 치수에서 1.5로 갈린다
```

원장 `scale32_web2.json`의 두 열이 그 곱을 낸다 — 세로가 표본 수의 몫, 가로가 자의 몫.
**재발 방지 규칙도 둘이다**: 「표본을 늘려라」**와** 「이 자가 무엇을 보장하는지 물어라」.
⚠⚠ 그리고 **후자를 이 세션이 옆 항목에서 곧바로 어겼다** — 32-6의 정체 왕복 오차(구성상 0)에
임계 `< 1e-9`를 걸었다가 리뷰어에게 잡혔다(세션 내 재발 · NOTES 32-6 절).

**③ 등재 둘**(사람이 지시에 못 박은 것)
- **PITFALLS #82** — 「고정 몫이 있는 환경에서 성능 주장을 **비**로 쓰지 마라. **차**로 써라」
  (`(c+a)/(c+b)`는 c가 크면 1로 가고 `(c+a)−(c+b)=a−b`에서는 c가 지워진다). cost23이 그렇게
  죽어 **배포를 막았다**(#81과 짝). **앞으로 만드는 모든 타이밍 팔에서 다시 만난다.**
- **AS-C117** — 30-8의 가로세로비 상관은 **실재했으나 원인이 아니었다**(정규화 단독 이득
  +15/880 = 1.7%p이고 그 15칸이 전부 자형 하나). **상관을 원인으로 읽지 마라.**

**④ 그리고 이 회차가 등재한 가정 셋 더**: AS-C118(축척은 만들 것이 아니었다) ·
AS-C119(잰 값은 적용 전 길이다) · AS-C120(저장하는 것은 «어느 두 점»이다).

**⑤ ⚠ 병렬 세션** — `docs/instructions/web2-35.md`(궤적 인식)를 **다른 세션이 동시에** 돌고
있다. 공유 문서(NOTES·PITFALLS·assumptions·DECISIONS·DEFERRED)는 이 갈래가 **덧붙이기만**
했다. 예외 하나: **AS-C107 행에 「뒤집혔다 → AS-C119」 표시를 넣었다**(#42 ⑥ — 낡은 인용을
남기면 그 인용이 낡은 것과 같다). 그 행은 32-7의 것이라 35 쪽과 안 겹친다.

---

## web2-32 (2026-08-29) — **32-1 · 32-2 · 32-3 · 32-4 · 32-8 완료** · 32-5·32-6·32-7은 다음

**① 이 세션의 범위는 사람이 잘랐다** — 「32-5(축척)·32-6(재기)·32-7(어긋남)은 이번에 하지 마라.
핵심 사슬은 **32-1 → 32-2 → 32-3**이다」. 그 다섯을 다 했다(32-4 포함).

| 항목 | 무엇 |
|---|---|
| 32-1 | **종이의 글씨를 뭉치로 가른다** — `Stroke.text`(옐로와 **같은 규격**) · 재판정은 «근거가 안 된 획»만 · **닫힌 한 붓이 «찍은 점»으로 버려지던 것**을 고쳤다 |
| 32-2 | **제안을 걷어내고 즉시 치수선** — 실행취소 한 번에 손글씨로 · **사후 수정**(숫자를 눌러 값·대상) |
| 32-3 | **대상은 숫자의 위치가 정한다** — 근접·정렬·선상 위치 셋. 세 항 **137/144** ↔ 옛 규칙 **132/144**(시드 셋) · 대역 밖이면 **후보 0** |
| 32-4 | **가로세로비 정규화**(구제 시야) — 전체 **491 → 506/880** · **오답 2 → 2**(안 늘렸다) · 「4」 **62 → 77/320** · 비 최대 격차 12.7 → **11.4%p** |
| (덤) | 전량 e2e가 잡은 **선재 결함** — 비운 저장소가 부팅 예약 저장에 **되살아났다**. 판본 게이트로 수리(PITFALLS **#80**) |
| 32-8 | 종이/옐로의 갈림을 **코드로** 세웠다(`DECISIONS.md` [4] 갱신) |

**①' 검증·배포 — 확인했다.** 타입 **0** · 단위 **711/0/711**(91파일) · e2e 전량
**378/0/378**(`--workers=1` · dpr1+dpr2 · 45.3m) · 빌드 통과.
main **`79a4fd3`** · 화면 빌드 식별자 **`79a4fd3`** == 배포 커밋 → **배포됨**.
⚠⚠ 그 배포가 **두 번 막혔다**(PITFALLS **#81**): 화면만 긁는 대기를 세 시간 돌렸는데
그 커밋의 Pages 실행이 **build 실패 → deploy skipped**였다. 막은 것은 web2-23의 `cost23`
타이밍 팔이고 원인이 둘이었다(예열 순서 · 「비」가 러너의 고정 몫 3~4ms에 물림). 둘 다
**임계를 안 낮추고** 고쳤고, `deploy-check.mjs`가 이제 **실행 상태를 먼저 읽는다**.

**② ⚠⚠ 다음 세션이 먼저 읽을 정정 — 32-5(축척)는 «만드는 일»이 아니다**

지시문 32-5는 축척을 새로 만드는 것처럼 썼는데 **이미 있다**: `lift.scaleOf` + `doc.scaleRef`가
「문서 순서상 첫 치수 획의 dim ÷ 무치수 풀이 길이」이고 **그것이 곧 32-5의 규칙**이다.
그러므로 그 항목은 **「미정 / 확정」을 화면에 드러내고 팔로 못 박는 일**이다.
**새 기제를 만들지 마라.** (사람이 이 정정을 이번 세션에 못 박았다.)
- 있는 것: 첫 치수가 스케일을 정한다(`setDimension`의 `wasScaled` 갈래) · 바탕 종이만 기준이
  된다(web2-21 1-b) · 첫 치수를 지우면 스케일이 풀린다(파생이므로 자동)
- 없는 것: **화면에 안 보인다** — 「축척 미정」·「1 : N」이 어디에도 안 뜬다. 그리고 팔이
  「미정 → 확정 → 다시 미정」의 왕복을 안 잰다
- ⚠ 32-6(재기)·32-7(어긋남)은 그 위에 선다: 32-7의 픽스처는 **둘째 치수**가 있어야 발화한다
  (30-6이 AS-C107의 「구성상 0」을 뒤집은 근거가 그것이다 — 표본이 하나여서였다)

**③ 이월** — **30-13**(세게 누르면 질감이 준다)은 계속 낮은 우선이다(`DEFERRED.md`).
그리고 **web2-31**(보기 렌즈)은 31-0 판정만 끝난 채로 남아 있다.

---

## web2-30 (2026-08-29) — 손으로 찾은 것들 **열둘 중 열둘 완료** · 32·31은 남았다

**① 앞 회차 마감 확인 — 이 세션의 첫 일이었다.** `cd web2 && node tools/deploy-check.mjs`가
`bfeb063` 배포를 확인했다(원격 main == 내 커밋 == 화면 빌드 식별자). web2-26~29 마감 **완료**.

**② 이 세션이 한 것 — `docs/instructions/web2-30.md`의 열둘.**

| 항목 | 무엇 |
|---|---|
| 30-1 | `fingerPans`에서 `penUsed` 항을 걷었다 — **3D가 서면 한 손가락은 궤도**(H4) |
| 30-2 | 펜 굵기 슬라이더 ⛔ → **촉통 다섯**(0.18~0.7 mm · 0.35 = 기본 1.5 px) |
| 30-3 | 펼침은 **겹쳐 뜬다**(`placeFlyout` 하나) · 빌드 식별자 `pointer-events: none` |
| 30-4 | 되돌릴 수 없는 물음은 **대상을 밝힌다**(종이 삭제·겹 삭제 둘 다) |
| 30-5 | `ext` ≠ `perp` 근거를 **AS-C110**에 등록 |
| 30-7 | 필압 보정 **절차가 화면에 머무른다** · 마치기 전에는 옵션이 안 켜진다 |
| 30-8 | 자형 × **가로세로비** 분해표(`glyph30_web2.json`) |
| 30-9 | **바탕 종이에 결**(`#paperfilm`) — 셋이 같은 함수로 구워진다 |
| 30-10 | 시스템 띠에 **설정(톱니바퀴)** · 필압 보정을 눈에서 옮겼다 |
| 30-11 | 연장선을 **선언된 구속**으로(왕복 제스처) — ⚠ 구조 변경 |
| 30-12 | 툴팁 600 → **300 ms**(+ 머무름 시간을 실제로 재는 팔) |
| 31-0 | **렌즈 슬라이더 폐기 사유 판정** — 지시가 착수 전 조건으로 건 것. **어긋나지 않는다** |

⛔ **30-13**(압력↑ → 질감↓)은 **지시가 낮은 우선으로 뒀고** DEFERRED로 넘겼다.

**③ 이 회차가 뒤집은 것 셋 — 다음 회차의 착수 표로 옮긴다.**
- **PITFALLS #78**(신설) — 헤드리스 획득률이 올랐는데 손에서 안 걸리면 **후보 경쟁**을
  의심하라. 픽스처의 **획 수**가 축이다. 어떤 것은 후보가 아니라 **선언된 구속**이다.
- **26-3의 띠 넓히기는 실패**로 기록했다(값이 아니라 층위).
- **web2-20 3부의 「바탕 종이에는 결이 없다」를 뒤집었다** — 차등 자체가 결함으로 읽혔다.

**④ 검증(마감)**: 타입 **0** · 단위 **703 / 0 / 703**(89파일) ·
e2e **380 / 0 / 380**(`--workers=1` · **28.6m**) · 빌드 통과.

**⑤ 배포 — 확인했다.** main **`69e44ef`** · `node tools/deploy-check.mjs`:
```
원격 main 69e44ef54bbaaffb1ec8e592c1c6e1c920ebe70f == 내 커밋
화면 빌드 식별자 69e44ef == 배포 커밋      → 결과: 배포됨
```

**⑥ ⚠ 전량 e2e가 «제품의» 결함을 하나 잡았다** — 설정 서랍·진단 패널·자립 깃발이 같은
우하단 모서리를 쓰면서 **열린 깃발이 「진단」 단추를 삼켰다**. z만으로는 못 푼다(세로바가
자기 쌓임 맥락이고, 맥락째 올리면 「알림이 리본 위」가 뒤집힌다) — 답은 **자리 다툼을 없애는
것**이었다(`btn-diag`를 명령으로 → 누르면 서랍이 접힌다). 자세한 것은 `web2/NOTES.md` 30-3 절.

**⑦ ⚠ 전량 e2e를 돌릴 때는 로그를 파일로 받는다** — 한 실행이 「12 통과 · 나머지 실패」로
무너졌는데(환경: dev 서버) `tail -30`으로만 받아 **왜인지 못 봤다**. `> e2e_full.log`로
받고, 무너지면 `test-results`를 비우고 dev 서버를 새로 띄운다(#70).

---