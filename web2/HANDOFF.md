# web2 인계

> ## 📌 이 파일이 「다음에 무엇을 하는가」의 **유일한 출처**다 (2026-08-30 사람 지시)
>
> 그동안 순서가 **두 곳**에 있었다 — 이 파일과 사용자의 채팅. 둘이 갈렸다.
> 앞으로는 이 파일이 정본이고, 그것을 지키는 규칙이 셋이다:
>
> 1. **새 지시문 파일을 저장할 때 반드시 같은 커밋에서 아래 §2 대기열을 갱신한다.**
> 2. **라운드를 마칠 때도 같은 커밋에서 §2 대기열을 갱신한다.**
> 3. **채팅에서 받은 순서와 이 파일이 어긋나면 보고하고 판단을 구하라** — 조용히
>    한쪽을 고르지 마라. (⚠ 저장소의 **사실**과 어긋나는 것은 다르다 — 그때는
>    저장소를 따르고 §2에 그 정정을 적는다. 2026-08-30에 34가 그 경우였다.)
>
> §3 아래의 「회차 기록」은 **과거의 기록**이다. 그 안에 적힌 「다음에 할 일」은
> 전부 **무효**이고, 유효한 것은 §1·§2뿐이다.

---

## 1. 다음 세션에 이대로 붙일 것 (사람이 복사만 하면 된다)

```
저장소 /home/user/brunelleschi · 배포되는 앱은 web2/다(pages.yml의 working-directory가 정본).
착수 시 `tail -40 PITFALLS.md`를 실제로 읽고 그 번호를 web2/NOTES.md의 각 절
「착수 시 PITFALLS 대조」에 적어라(안 적으면 안 읽은 것으로 본다).
CLAUDE.md §2의 D 다섯을 그 항목에 적용하고 적용 내용을 적어라.
마감은 `cd web2 && node tools/deploy-check.mjs` — 갈래 푸시는 중간 상태다(PITFALLS #58 · #81).

**web2-37을 완주하라** — `docs/instructions/web2-37.md`가 지시문이다.
「획이 서기 위해 필요한 것」 — 사무소 소장의 실사용 관찰에서 나왔고 37-1이 중심이다.
아래 다섯을 지시문보다 우선한다.

(1) **37-1의 표 하나로 써라.** 「허공 시작 처리」를 특수 분기로 만들면 조합이 늘 때마다
    빠지는 경우가 생긴다. 명시 점(오스냅) 0/1/2 × 축 유무 × 교차 0/1/2+ 의
    **전수 표가 이 항목의 결과물**이다.

(2) **반증 조건을 실제로 실행하라**(D-3). 시작·끝을 **둘 다 물리고** 그 방향이 축과
    **살짝 어긋나는** 픽스처에서 **두 점이 이기고 끝점이 안 떨어진다** — 이것이
    37-1의 반증 조건이다. 축이 이기게 짠 판을 만들어 **거기서 빨개지는 것을 보여라.**
    ⚠ 31이 이 형태를 네 번 했다 — 「구성상 보장」과 「측정」을 원장에서 가르고,
    판별력은 **위약 판**이 준다(PITFALLS #86 · 31-2 NOTES가 본보기다).

(3) **깨끗한 장면으로 재지 마라**(#71 · 지시문이 못 박았다). 오획득률은 **획 40개 이상**의
    장면에서 잰다. 30-11이 같은 이유로 픽스처를 다시 짰다.

(4) **착수 전 확인 둘을 먼저 보고하라** — ㉠ lift가 시작점 앵커 기준이라
    **끝점만 물린 획이 대기로 남고 있는가**(D-2 재현) ㉡ **삐져나오기가 렌더 단계의
    것인가**(저장 좌표를 늘리고 있었으면 원칙 b 위반이라 강조를 얹기 전에 정리한다).

(5) **⚠⚠ 손 띠(세로바)의 자리가 포화됐다.** 31이 돋보기·렌즈 둘을 넣어 여유가
    105px → **31px**로 줄었다(`DEFERRED.md`). 37이 새 손잡이를 더해야 하면
    **자리를 새로 내야 한다** — 34-6의 여유에 그냥 얹을 수 없다.
    그 여유를 **상수로 적지 마라**(PITFALLS #88) · 새 통은 `#app` 직계로(#87).

**그 다음은 web2-36**(실기기 확인 ③의 인식률을 보고 나서) → **web2-38**(개명이라 맨 뒤).
마친 뒤 이 파일 §2 대기열을 **같은 커밋에서** 갱신하라.
```

---

## 2. 대기열 — **이것이 정본이다**

```
완료·배포   26 · 27 · 28 · 29 · 30 · 31 · 32 · 34 · 35

▶ 37  획이 서기 위해 필요한 것          ← 다음
▶ 36  사후 수정이 자형을 등록한다 (실기기 인식률 ③을 보고 나서)
▶ 38  「종이」를 「장면」으로 (개명이므로 맨 뒤)
```

**31은 2026-08-31에 네 항목 전부 끝났다** — 31-1 뷰 큐브(축 기준 90°) · 31-2 보기 렌즈 ·
31-3 돋보기 · 31-4 시점 아이콘. 회차 기록은 `web2/NOTES.md`의 네 절이 정본이다.
31의 착수 조건 셋(31-0 판정 · D-3 함정 · 34-6 자리)은 **전부 밟았고 수치가 그 절에 있다**.

**⚠ 채팅의 대기열과 저장소가 갈린 자리 둘 — 저장소를 따랐다**(위 규칙 3):

| 무엇 | 채팅이 말한 것 | 저장소의 사실 | 처리 |
|---|---|---|---|
| **34** | 「진행 — 병합·마감」 | **끝났다.** main `c4932ab` · `deploy-check.mjs`가 「원격 main == 내 커밋 · Pages completed/success · 화면 빌드 식별자 `c4932ab` == 배포 커밋 → **배포됨**」 | **완료·배포로 옮겼다** |
| **33** | 「`web2-33.md`가 있으면 32-5·6·7과 중복이니 머리에 «32에 흡수됨»을 적어라」 | **그 파일이 없다.** `docs/instructions/`에 web2-33은 존재한 적이 없다 | **할 일 없음.** 32-5·6·7은 `web2-32.md` 안에서 완주됐다 |

**31이 밟은 착수 조건 셋과 그 결과**(다음 회차가 같은 형태를 만나므로 남긴다):

1. **31-2는 조건부다.** 앞 세션의 31-0 판정이 `web2/NOTES.md`에 있다(「31-0 착수 전 판정」).
   결론은 **어긋나지 않는다 · 착수해도 된다**이고, 착수 조건 둘이 붙어 있다 —
   ① `Camera.f`·`fSource`는 **읽기만** ② `fSource`를 **화면에 내지 않는다**(D-L55).
2. **31-1의 시험 방법이 함정이다**(D-3). 「기준 틀은 화면이 아니라 축」은 **초기 화면이 축에서
   돌아가 있는 2점 구도**로만 반증된다 — 정면 구도로 90°를 시험하면 축 기준이든 화면 기준이든
   **결과가 같아 거저 통과한다.** 사용자가 극초기에 잡은 문제가 정확히 그렇게 숨어 있었다.
3. **자리는 34-6이 만들어 뒀다. 사이드바를 다시 재편하지 마라.** 실측(뷰포트 1200×800 ·
   원장 `stage0/out/place34_web2_dpr1.json`): 재편 전 여유 25px → **후 105px**. 31의 둘을
   각 45px로 넣으면 **13px** 남고, `.ico-m`(36px)로 두면 **31px**다(시점 묶음의 다른 단추가
   전부 그 급이라 그쪽이 자연스럽다). ⚠⚠ **새 통을 만들면 `#app`의 직계로 둬라** —
   `#sidebar` 안에 두면 쌓임 맥락에 삼켜져 치수 리본 뒤로 **조용히 깔린다**(**PITFALLS #87**).
   ⚠ 그리고 그 여유를 **상수로 다시 적지 마라**(**PITFALLS #88** — 이 회차가 등재했다).

⚠ **뷰 큐브는 이미 캔버스 그림쇠다** — `app.cubeLayout`(cx W−110 · cy 60 · size 80) ·
`src/core/viewcube.ts`가 기하를 내고 `render2d.ts`가 그리고 `input.ts`가 짚는다.
31-1이 얹는 것은 **축 기준 틀과 90° 화살표**이지 새 단추가 아니다.

---

## 3. 이월 · 보류

- **30-13** 세게 누르면 질감이 준다 — 낮은 우선(`DEFERRED.md`)
- **성능**(200~400획) · **저장·불러오기 왕복** — 사용자가 미뤘다
- **근평면 클리핑** — 헤드리스에서만 나고 실기기 재현 실패. **계측 조건 조사 미착수**
- **원장 오염 산술의 1.74배 잔차** — 미해명
- **오스냅 시작점 획득 반경** — **37-6이 값을 보고한 뒤**, 실기기 확인 ④를 보고 정한다.
  (37-6은 값을 **재기만** 한다 — 이번에 바꾸지 않는다)
- **v1.0 태그와 화면 버전 표시** — 실기기 확인이 끝난 뒤
- `web2` 라인에 **`constantsSnapshot()`/`metric_defs` 기계가 아직 없다** — web2 원장 전부가
  STALE 판정 불가다. 종전부터의 유보다
- ⚠⚠ **`ui34.spec ④`가 이 기기에서 빨갛다 — 선재 결함이고 CI가 안 본다**(2026-08-31 web2-31 마감이
  전량으로 냈다). 실측 **6.839224** ↔ 문 **6.601**. `main`(`c4932ab`)의 트리에서 같은 팔이
  **똑같이** 빨갛다(#80대로 갈랐다 — 31의 회귀가 아니다). 원인은 각인의 `system-ui` 글꼴이
  기기마다 다르게 풀리는 것이고(컨테이너 6.60 ↔ 여기 6.839), 그 문 `6.6`은 **아이콘 창
  `rect`의 폭을 손으로 옮겨 적은 값**이다(**#88 그 자체** · 여유 0.001 · #83 ㉧).
  **임계를 안 물렀다** — 34-3의 문면이라 34-3을 여는 회차의 몫이다(`DEFERRED.md` · **AS-C135**)
- ⚠ **CI는 전량 e2e를 안 돈다** — `.github/workflows/pages.yml`이 도는 e2e는
  `e2e/static_deploy.spec.ts` **하나**뿐이다(typecheck·`npm test`·build는 돈다).
  그러므로 **전량은 사람이 도는 세션에서만 돈다** — 「초록이니 배포됐다」로 읽지 마라
- **손 띠(세로바) 예산이 포화됐다** — 31이 돋보기·렌즈 둘을 넣어 여유 105px → **31px**.
  다음에 손잡이를 더하는 항목은 **자리를 새로 내야 한다**(`DEFERRED.md`)
- `DEFERRED.md`의 항목들

---

## 4. 사람 몫 — 실기기 (헤드리스가 못 대신하는 자리)

자리는 `docs/DEVICE-CHECK.md`.

① **선따기 → 지우개로 바깥 토막 자르기 → 겹 끄기 → 종이에 선이 몇 개**
   ⚠ 34-3이 지우개를 **크기통 넷**으로 바꿨다(반경 5·12·27·60 px) — 그 크기가 손에 맞는지가
   같이 걸린다. 견본은 1:1이라 화면에서 본 크기가 곧 지우는 크기다.

② **종이에 숫자 쓰기가 실제로 되는가** — 치수선이 되는가 · 획이 흩어지지 않는가 ·
   대상이 맞게 잡히는가 · 사후 수정 · 실행취소 한 번에 손글씨로.

③ **사용자 글씨로 숫자 열 개를 써서 몇 개가 맞는가.**
   ⚠⚠ **합성 87.5%(198/220)는 사람 손을 예측하지 못한다.** 「4」가 20/20인 것도 그 픽스처가
   인식기 원형 표와 **좌표로 같은 획**이라 표본 «안»의 값이다 — 손으로 쓴 4는 미답이다.
   **이 수 하나가 web2-36(자형 등록)의 착수 여부를 정한다.**

④ **37이 들어간 뒤: 허공에서 선을 여러 개 쫙쫙 그어 봐라.**
   시작점이 근처 끝점으로 끌려가서 거슬리는가. **안 거슬리면 오스냅 반경은 그대로 둔다.
   거슬리면 별도 라운드로 줄인다.** (37-6이 기준선 수치를 남긴다)

⑤ **(34가 새로 만든 자리)** 위 띠에 **파일·설정**이 올라왔고 **작도 시점**이 손 띠로 내려왔다.
   롤 둘이 **롤통**이 됐다. 「그리는 중에 쓰는가」로 가른 것이 손에 맞는지.

---
---

# 회차 기록 (참고)

> ⚠ **아래는 과거의 기록이다.** 그 안에 적힌 「다음 세션이 할 일」·「다음은 무엇」은
> **전부 무효**다 — 유효한 순서는 위 §1·§2뿐이다. 상세는 `web2/NOTES.md`가 정본이다.

## 🆕 web2-35 병합 · web2-34 (2026-08-30) — **규칙을 전수로 쓸어냈다**

### 어디까지 했나

| 단계 | 무엇 | 상태 |
|---|---|---|
| 0 | **web2-35 병합** | ✅ 배포까지 마감 — main **`eca5442`** · 화면 빌드 식별자 `eca5442` |
| 1 | `docs/instructions/web2-36.md` 저장 | ✅ **착수 안 함**(34 · 31이 먼저다) |
| 2 | **web2-34 여덟 항목 + 34-0 대조표** | ✅ 완주 · 리뷰어 12건 대응까지 |
| 3 | web2-31 | ⛔ **미착수** — 위의 「이대로 붙일 것」이 그 지시다 |

### 병합에서 나온 것 — **평행 세션이 같은 번호를 두 번 썼다**

web2-35가 web2-32 5·6·7과 평행으로 돌아 `AS-C117~C120`과 `PITFALLS #82`가 **둘씩** 있었다.
main에 먼저 든 web2-32의 것을 두고 web2-35의 것을 **`AS-C121~C124`·`#83`**으로 옮겼다
(본문·표·㉠~㉨ 인용·원장·하네스 주석까지). **앞으로 평행 세션이 있으면 이 자리를 먼저 본다.**
그리고 **어느 갈래도 합친 트리를 검증한 적이 없었다** — 그 첫 전량이 393/394였고
하나는 `gesture.spec` dpr2의 180s 타임아웃(혼자 돌리면 다섯 팔 전부 통과 — #80대로 갈랐다).

### web2-34가 세운 것 — 화면 규칙 여덟(`DECISIONS.md`가 정본)

R1~R5는 web2-28·30이 세운 것이고 **R6·R7·R8이 이 회차**다. R5는 이 회차 안에서 **한 번 정정됐다**
(아래 「가장 값진 것」). 34-0의 전수 대조표(요소 40 × 규칙 8)가 `web2/NOTES.md`에 있다.

### 항목별 한 줄과 그 수치

| 항목 | 무엇 | 수치 |
|---|---|---|
| 34-1 (R8) | 바탕 종이 결을 겹 아래로 · 설정에서 끈다 | 종이/겹 비 **0.985 → 1.46~1.64**(dpr1) · **0.864 → 1.333**(dpr2) · 끄면 0.000000 |
| 34-2 (R6) | 접힌 펜이 촉을 말한다 | 다섯 촉 `.18~.70` · 각인 높이비 **1.000**(연필과 같은 대역) |
| 34-3 (R1) | 지우개 막대 → 크기통 넷 | 견본 지름 **10/24/54/120 px** · 실제로 지운 반경이 그 안. **옛 막대는 실제의 27.5~112.5%였다** |
| 34-4 (R7) | 열린 통은 바깥을 눌러도 접힌다 | 안 접힘 **8/10 → 0/10** · 삼킴 0 |
| 34-5 | 설정 아이콘을 선 문법 톱니로 | 옛(채운) 아이콘이 새 검사에 **위반 6건** |
| 34-6 | 배치를 「그리는 중에 쓰는가」로 | 여유 **25px → 105px** · 31의 둘 넣으면 13px |
| 34-7 | 어긋남 문턱 | **옛 0.02는 흔들린 칸의 71.3%(719/1009)에서 뜬다.** 새 문턱 fold **2**(경계 아래 1.75 · 위 2.75 · 오탐 59/1229) |
| 34-8 | 잰 값의 정체 | 저장 형태는 **이미 정체였다.** 위약이 값 축 10/12 · 점 축 **24/24** 갈린다 |

### ⚠⚠ 이 회차에서 가장 값진 것 — **전수가 지시문에 없던 것을 넷 찾았다**

개별 증상 보고로는 하나도 안 나왔을 것들이다.

1. **접힌 겹 요약이 「맨 위 겹」을 말하고 있었다**(34-2 전수) — 아래 겹을 골라 두면
   **접힌 통이 틀리게 말한다.** 34-0 표·`DECISIONS.md`·지시문이 **셋 다** 「겹 요약은
   이미 지킨다」로 적고 있었다(AS-C126이 그 반증).
2. **표시 팝업이 치수 리본 뒤로 깔려 있었다**(34-6) — 겹침 **158×103**, web2-19 이래.
3. ⚠⚠ **그리고 통 넷이 통째로 리본 뒤로 깔려 있었다**(리뷰어 [1] → 34-6 후속) —
   연필통·촉통은 **web2-30 이래**, 크기통은 34-3 이래. `elementFromPoint`가 `pad-keys`·
   `dimpanel`을 냈다. 원인은 `#sidebar`의 **자기 쌓임 맥락**이 그 안의 z 12를 삼킨 것이고,
   **선언은 처음부터 옳았다** — 답은 z가 아니라 **맥락에서 꺼내는 것**이다(**PITFALLS #87**).
4. **위 띠를 넓히자 종이 띠가 파고들었다**(전량 e2e) — 예약이 **상수**로 적혀 있어
   옮긴 쪽과 말없이 묶여 있었다. `--topbar-reserve`로 뺐다.

**규칙이 이 과정에서 갈렸다**(`DECISIONS.md`의 「R5 정정」 · AS-C130):
판별 물음이 둘로 늘었다 — ① 동시에 열릴 수 있는가 ② **둘 다 오래 떠 있는가.**
①만 예면 재는 것은 「자리」가 아니라 **「순서」**다(`elementFromPoint`로).

### ⚠ 다음이 알아야 할 함정 · 정정

- **`#84 ㉢`를 같은 회차의 34-6이 어겼다** — 자리를 옮기고 `sidebar_layout_web2.json`을
  안 돌려 두 원장이 세로바 높이를 763 ↔ 683으로 다르게 말했다. **무엇을 옮기면 그 자리를
  재던 원장을 그 자리에서 다시 돌린다.**
- **34-1의 dpr1 진폭은 두 자리까지만 유효하다**(여섯 실행 2.392~2.680 · 폭 12%).
  ⚠ 그 폭의 위험한 갈래는 **닫혔다** — 바탕 결은 **로드마다 픽셀로 같다**(해시·sd가
  소수 여섯째 자리까지 동일). 12%는 제품이 아니라 **재는 절차**의 성질이다.
- **34-7의 「자연 분포」는 «재구성 흔들림»이지 손획 표본이 아니다.** 실기기 표본은 여전히 0.
- **34-8의 「값 축 10/12」는 픽스처가 정한 수다** — 네 구도가 전부 축척 기준을 «화면평행
  세로»로 두고 있다. 일반 진술로 인용하지 마라(DEFERRED에 있다).
- `web2` 라인에 `constantsSnapshot()`/`metric_defs` 기계가 **아직 없다** — web2 원장 전부가
  STALE 판정 불가다. 종전부터의 유보이고 이 회차가 만든 것이 아니다.

### 🙋 사람에게 남은 실기기 확인 (헤드리스가 못 대신하는 자리)

1. **선따기 → 지우개로 바깥 토막 자르기 → 겹 끄기 → 종이에 선이 몇 개**
   ⚠ 34-3이 지우개를 **크기통 넷**으로 바꿨다(반경 5·12·27·60 px) — 그 크기가 손에 맞는지가
   같이 걸린다. 견본은 1:1이라 화면에서 본 크기가 곧 지우는 크기다.
2. **종이에 숫자 쓰기가 실제로 되는가** — 치수선이 되는가 · 획이 흩어지지 않는가 ·
   대상이 맞게 잡히는가 · 사후 수정 · 실행취소 한 번에 손글씨로.
3. **사용자 글씨로 숫자 열 개를 써서 몇 개가 맞는가.**
   ⚠⚠ **합성 87.5%(198/220)는 사람 손을 예측하지 못한다.** 「4」가 20/20인 것도 그 픽스처가
   인식기 원형 표와 **좌표로 같은 획**이라 표본 «안»의 값이다 — 손으로 쓴 4는 미답이다.
   이 수 하나가 web2-36(자형 등록)의 착수 여부를 정한다.
4. **(새로 생긴 자리)** 위 띠에 **파일·설정**이 올라왔고 **작도 시점**이 손 띠로 내려왔다.
   롤 둘이 **롤통**이 됐다. 「그리는 중에 쓰는가」로 가른 것이 손에 맞는지.

## 🆕🆕🆕🆕🆕🆕 web2-32 (2) (2026-08-30) — **32-5 · 32-6 · 32-7** · 32는 이것으로 끝났다

**①' 검증 — 확인했다.** 타입 **0** · 단위 **727 / 0 / 727**(93파일 · 26.6s) ·
e2e 전량 **394 / 0 / 394**(`--workers=1` · dpr1+dpr2 · **30.7m**) · 빌드 통과(2.4s).
비용은 `test_cost_web2.json`이 정본이다(팔 +16 단위 · +8 e2e).

**①'' 배포 — 확인했다.** main **`07b92d0`** · `node tools/deploy-check.mjs`:
```
원격 main 07b92d07c1358547088a137b264800b50c122a2a == 내 커밋
Pages 실행 completed / success
화면 빌드 식별자 07b92d0 == 배포 커밋      → 결과: 배포됨
```
(병합 커밋 `629a282`가 먼저 나갔고, 그 뒤 문서 한 줄을 더 밀어 `07b92d0`이 현재 판이다.)
⚠ **배포를 기다리는 동안 또 밀지 마라** — 그때마다 Pages가 처음부터 다시 돈다(약 10분).
이번에도 그렇게 한 번 더 돌았다.

⚠⚠ **전량이 둘을 잡았다** — 하나는 제품, 하나는 팔:
- **제품**: 재기 단추를 세로바에 뒀더니 **800px 화면을 12px 넘쳤다**(812 · `sidebar.spec`
  dpr1·dpr2 + `zones.spec` id 목록). 임계를 안 무르고 **치수 패널로 옮겼다**(→ 763/800 ·
  `zones`의 목록은 손대지 않았다). 선례는 web2-29의 `btn-dim-write`다.
- **팔**: `autosave.spec ②`가 앞 세션의 **판본 게이트(#80)**에 막혀 부하에서만 죽었다 —
  문서를 안 바꾸고 리스너만 재점화하므로 앞선 저장이 이미 나갔으면 게이트가 막는다.
  **통과할 때조차 «재는 것을 재는지»가 운이었다.** 팔이 «다음 저장»을 실제로 만들게
  고쳤다(`docVersion++` — 임계도 대기도 안 건드렸다). 재현·수리는 NOTES 마지막 절.

**① 이 세션의 범위도 사람이 잘랐다** — 「32-5 · 32-6 · 32-7만 한다. 32-1~32-4 · 32-8은
이미 끝났다」. 그리고 **정정 셋을 함께 줬다**(그 셋이 이 회차의 방향을 정했다):

| 정정 | 무엇 |
|---|---|
| 32-5 | 「**만드는 일이 아니라 드러내는 일**이다. `lift.scaleOf` + `doc.scaleRef`로 이미 있다. 새 기제를 만들지 마라」 |
| 32-6 | 「기본값은 **패널에 표시만**이다 — 도면에 아무것도 안 남는다. 숫자를 저장하지 마라. 아이콘은 **이미 그려져 있다**」 |
| 32-7 | 「= web2-30의 **30-6**이다. 치수가 하나일 때 어긋남이 0인 것은 모형의 성질이 아니라 **표본 수의 성질**이었다. **표시하되 고치지 마라**」 |

| 항목 | 무엇 |
|---|---|
| 32-5 | **축척을 화면에** — `scaleOf`가 «고른 획»을 같이 낸다(`LiftResult.scaleId`). 「축척 미정」 ↔ 「**축척 1단위 = 1250 mm** · 기준 2400 mm」(⚠ 「1 : N」이 아니다 — 리뷰어 [8]). 왕복(미정→확정→미정→**고치기**→다음이 정함)을 팔이 잰다 |
| 32-6 | **재기** — `core/measure.ts`. 저장하는 것은 「어느 두 점」뿐이다(획 id + 선분 위 t · **면의 선례**). 기본은 표시만이고 도면이 안 변한다. 축척 미정이면 비율. 삼각 스케일 아이콘 정본 그대로 |
| 32-7 | **어긋남** — 「잰 값」은 **치수 적용 «전»** 길이다(`LiftResult.dimGeom`). 적용 뒤를 재면 **구성상 1**이고 그것이 29-2가 기능을 걷은 자리다(AS-C107 → **AS-C119로 뒤집혔다**) |
| (덤) | 치수 적용이 **세 갈래에 복사돼 있던 것**을 `applyDim` 한 자리로 합쳤다(#54) — 표식을 세 곳에 심으면 그 셋이 갈린다 |

**② ⚠⚠ 이 회차가 뒤집은 것 — 「구성상 0」의 원인은 **둘의 곱**이었다**

⚠ **초판 보고는 「진짜 원인은 «자»였다」고 적었다. 1차 리뷰어가 원장 자신으로 반박했다** —
`skew_first_dim`을 보면 **올바른 자**(적용 전)로 재도 치수가 하나면 비가 **1**이다. 29-2의
장면은 치수가 하나였으므로 두 자가 같은 답을 냈고, 그 관측만으로는 둘을 **가를 수 없었다**.

```
표본이 하나  →  어느 자로 재도 1        (30-6이 잡은 것 · 맞다)
자가 적용 뒤 →  표본이 둘이어도 1        (이 회차가 잡은 것)
둘 다 고쳐야 →  둘째 치수에서 1.5로 갈린다
```

원장 `scale32_web2.json`의 두 열이 그 곱을 낸다 — 세로가 표본 수의 몫, 가로가 자의 몫.
**재발 방지 규칙도 둘이다**: 「표본을 늘려라」**와** 「이 자가 무엇을 보장하는지 물어라」.
⚠⚠ 그리고 **후자를 이 세션이 옆 항목에서 곧바로 어겼다** — 32-6의 정체 왕복 오차(구성상 0)에
임계 `< 1e-9`를 걸었다가 리뷰어에게 잡혔다(세션 내 재발 · NOTES 32-6 절).

**③ 등재 둘**(사람이 지시에 못 박은 것)
- **PITFALLS #82** — 「고정 몫이 있는 환경에서 성능 주장을 **비**로 쓰지 마라. **차**로 써라」
  (`(c+a)/(c+b)`는 c가 크면 1로 가고 `(c+a)−(c+b)=a−b`에서는 c가 지워진다). cost23이 그렇게
  죽어 **배포를 막았다**(#81과 짝). **앞으로 만드는 모든 타이밍 팔에서 다시 만난다.**
- **AS-C117** — 30-8의 가로세로비 상관은 **실재했으나 원인이 아니었다**(정규화 단독 이득
  +15/880 = 1.7%p이고 그 15칸이 전부 자형 하나). **상관을 원인으로 읽지 마라.**

**④ 그리고 이 회차가 등재한 가정 셋 더**: AS-C118(축척은 만들 것이 아니었다) ·
AS-C119(잰 값은 적용 전 길이다) · AS-C120(저장하는 것은 «어느 두 점»이다).

**⑤ ⚠ 병렬 세션** — `docs/instructions/web2-35.md`(궤적 인식)를 **다른 세션이 동시에** 돌고
있다. 공유 문서(NOTES·PITFALLS·assumptions·DECISIONS·DEFERRED)는 이 갈래가 **덧붙이기만**
했다. 예외 하나: **AS-C107 행에 「뒤집혔다 → AS-C119」 표시를 넣었다**(#42 ⑥ — 낡은 인용을
남기면 그 인용이 낡은 것과 같다). 그 행은 32-7의 것이라 35 쪽과 안 겹친다.

---

## web2-32 (2026-08-29) — **32-1 · 32-2 · 32-3 · 32-4 · 32-8 완료** · 32-5·32-6·32-7은 다음

**① 이 세션의 범위는 사람이 잘랐다** — 「32-5(축척)·32-6(재기)·32-7(어긋남)은 이번에 하지 마라.
핵심 사슬은 **32-1 → 32-2 → 32-3**이다」. 그 다섯을 다 했다(32-4 포함).

| 항목 | 무엇 |
|---|---|
| 32-1 | **종이의 글씨를 뭉치로 가른다** — `Stroke.text`(옐로와 **같은 규격**) · 재판정은 «근거가 안 된 획»만 · **닫힌 한 붓이 «찍은 점»으로 버려지던 것**을 고쳤다 |
| 32-2 | **제안을 걷어내고 즉시 치수선** — 실행취소 한 번에 손글씨로 · **사후 수정**(숫자를 눌러 값·대상) |
| 32-3 | **대상은 숫자의 위치가 정한다** — 근접·정렬·선상 위치 셋. 세 항 **137/144** ↔ 옛 규칙 **132/144**(시드 셋) · 대역 밖이면 **후보 0** |
| 32-4 | **가로세로비 정규화**(구제 시야) — 전체 **491 → 506/880** · **오답 2 → 2**(안 늘렸다) · 「4」 **62 → 77/320** · 비 최대 격차 12.7 → **11.4%p** |
| (덤) | 전량 e2e가 잡은 **선재 결함** — 비운 저장소가 부팅 예약 저장에 **되살아났다**. 판본 게이트로 수리(PITFALLS **#80**) |
| 32-8 | 종이/옐로의 갈림을 **코드로** 세웠다(`DECISIONS.md` [4] 갱신) |

**①' 검증·배포 — 확인했다.** 타입 **0** · 단위 **711/0/711**(91파일) · e2e 전량
**378/0/378**(`--workers=1` · dpr1+dpr2 · 45.3m) · 빌드 통과.
main **`79a4fd3`** · 화면 빌드 식별자 **`79a4fd3`** == 배포 커밋 → **배포됨**.
⚠⚠ 그 배포가 **두 번 막혔다**(PITFALLS **#81**): 화면만 긁는 대기를 세 시간 돌렸는데
그 커밋의 Pages 실행이 **build 실패 → deploy skipped**였다. 막은 것은 web2-23의 `cost23`
타이밍 팔이고 원인이 둘이었다(예열 순서 · 「비」가 러너의 고정 몫 3~4ms에 물림). 둘 다
**임계를 안 낮추고** 고쳤고, `deploy-check.mjs`가 이제 **실행 상태를 먼저 읽는다**.

**② ⚠⚠ 다음 세션이 먼저 읽을 정정 — 32-5(축척)는 «만드는 일»이 아니다**

지시문 32-5는 축척을 새로 만드는 것처럼 썼는데 **이미 있다**: `lift.scaleOf` + `doc.scaleRef`가
「문서 순서상 첫 치수 획의 dim ÷ 무치수 풀이 길이」이고 **그것이 곧 32-5의 규칙**이다.
그러므로 그 항목은 **「미정 / 확정」을 화면에 드러내고 팔로 못 박는 일**이다.
**새 기제를 만들지 마라.** (사람이 이 정정을 이번 세션에 못 박았다.)
- 있는 것: 첫 치수가 스케일을 정한다(`setDimension`의 `wasScaled` 갈래) · 바탕 종이만 기준이
  된다(web2-21 1-b) · 첫 치수를 지우면 스케일이 풀린다(파생이므로 자동)
- 없는 것: **화면에 안 보인다** — 「축척 미정」·「1 : N」이 어디에도 안 뜬다. 그리고 팔이
  「미정 → 확정 → 다시 미정」의 왕복을 안 잰다
- ⚠ 32-6(재기)·32-7(어긋남)은 그 위에 선다: 32-7의 픽스처는 **둘째 치수**가 있어야 발화한다
  (30-6이 AS-C107의 「구성상 0」을 뒤집은 근거가 그것이다 — 표본이 하나여서였다)

**③ 이월** — **30-13**(세게 누르면 질감이 준다)은 계속 낮은 우선이다(`DEFERRED.md`).
그리고 **web2-31**(보기 렌즈)은 31-0 판정만 끝난 채로 남아 있다.

---

## web2-30 (2026-08-29) — 손으로 찾은 것들 **열둘 중 열둘 완료** · 32·31은 남았다

**① 앞 회차 마감 확인 — 이 세션의 첫 일이었다.** `cd web2 && node tools/deploy-check.mjs`가
`bfeb063` 배포를 확인했다(원격 main == 내 커밋 == 화면 빌드 식별자). web2-26~29 마감 **완료**.

**② 이 세션이 한 것 — `docs/instructions/web2-30.md`의 열둘.**

| 항목 | 무엇 |
|---|---|
| 30-1 | `fingerPans`에서 `penUsed` 항을 걷었다 — **3D가 서면 한 손가락은 궤도**(H4) |
| 30-2 | 펜 굵기 슬라이더 ⛔ → **촉통 다섯**(0.18~0.7 mm · 0.35 = 기본 1.5 px) |
| 30-3 | 펼침은 **겹쳐 뜬다**(`placeFlyout` 하나) · 빌드 식별자 `pointer-events: none` |
| 30-4 | 되돌릴 수 없는 물음은 **대상을 밝힌다**(종이 삭제·겹 삭제 둘 다) |
| 30-5 | `ext` ≠ `perp` 근거를 **AS-C110**에 등록 |
| 30-7 | 필압 보정 **절차가 화면에 머무른다** · 마치기 전에는 옵션이 안 켜진다 |
| 30-8 | 자형 × **가로세로비** 분해표(`glyph30_web2.json`) |
| 30-9 | **바탕 종이에 결**(`#paperfilm`) — 셋이 같은 함수로 구워진다 |
| 30-10 | 시스템 띠에 **설정(톱니바퀴)** · 필압 보정을 눈에서 옮겼다 |
| 30-11 | 연장선을 **선언된 구속**으로(왕복 제스처) — ⚠ 구조 변경 |
| 30-12 | 툴팁 600 → **300 ms**(+ 머무름 시간을 실제로 재는 팔) |
| 31-0 | **렌즈 슬라이더 폐기 사유 판정** — 지시가 착수 전 조건으로 건 것. **어긋나지 않는다** |

⛔ **30-13**(압력↑ → 질감↓)은 **지시가 낮은 우선으로 뒀고** DEFERRED로 넘겼다.

**③ 이 회차가 뒤집은 것 셋 — 다음 회차의 착수 표로 옮긴다.**
- **PITFALLS #78**(신설) — 헤드리스 획득률이 올랐는데 손에서 안 걸리면 **후보 경쟁**을
  의심하라. 픽스처의 **획 수**가 축이다. 어떤 것은 후보가 아니라 **선언된 구속**이다.
- **26-3의 띠 넓히기는 실패**로 기록했다(값이 아니라 층위).
- **web2-20 3부의 「바탕 종이에는 결이 없다」를 뒤집었다** — 차등 자체가 결함으로 읽혔다.

**④ 검증(마감)**: 타입 **0** · 단위 **703 / 0 / 703**(89파일) ·
e2e **380 / 0 / 380**(`--workers=1` · **28.6m**) · 빌드 통과.

**⑤ 배포 — 확인했다.** main **`69e44ef`** · `node tools/deploy-check.mjs`:
```
원격 main 69e44ef54bbaaffb1ec8e592c1c6e1c920ebe70f == 내 커밋
화면 빌드 식별자 69e44ef == 배포 커밋      → 결과: 배포됨
```

**⑥ ⚠ 전량 e2e가 «제품의» 결함을 하나 잡았다** — 설정 서랍·진단 패널·자립 깃발이 같은
우하단 모서리를 쓰면서 **열린 깃발이 「진단」 단추를 삼켰다**. z만으로는 못 푼다(세로바가
자기 쌓임 맥락이고, 맥락째 올리면 「알림이 리본 위」가 뒤집힌다) — 답은 **자리 다툼을 없애는
것**이었다(`btn-diag`를 명령으로 → 누르면 서랍이 접힌다). 자세한 것은 `web2/NOTES.md` 30-3 절.

**⑦ ⚠ 전량 e2e를 돌릴 때는 로그를 파일로 받는다** — 한 실행이 「12 통과 · 나머지 실패」로
무너졌는데(환경: dev 서버) `tail -30`으로만 받아 **왜인지 못 봤다**. `> e2e_full.log`로
받고, 무너지면 `test-results`를 비우고 dev 서버를 새로 띄운다(#70).

---