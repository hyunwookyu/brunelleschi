// 정해진 값 — 지시서 「정해진 값」 절에서 그대로. 근거가 없는 값은 넣지 않는다.
// 임계는 전부 비(比)다. 예외: 오스냅 반경·지우개(포인터 정밀도 문제라 선례가 절대 px).

export const C = {
  /** 소실점 방향 판정 — 대표 직선에서 소실점까지 수직거리 ÷ 획 길이 */
  VP_DIR_RATIO: 0.06,
  /** 화면 평행 판정 — 수평·수직에서 벗어난 정도(길이 대비) ≈ 2.87°.
   *  **축 스냅의 후보 고르기에만 쓴다**(가장 가까운 축). 카메라 판정에는 안 쓴다 — 아래. */
  SCREEN_PARALLEL_RATIO: 0.05,
  /** **지평선과 평행한가**를 가르는 px. 획의 «끝점 처짐»을 그대로 잰다(비가 아니다).
   *
   *  비(2.87°)로 재던 것이 결함이었다(web2-03 지시 2). 그 비가 뜻하는 소실점 거리는
   *  **시작점이 지평선에서 얼마나 떨어졌는가(h)에 비례**한다: 경계 = h ÷ tan(2.87°) ≈ h × 20.
   *  그래서 지평선 가까이서 그으면 경계가 무너진다 — 실측:
   *
   *      h= 40px → 0.7W    h= 80px → 1.3W    h=150px → 2.5W
   *      h=250px → 4.2W    h=400px → 6.7W        (W = 화면 폭)
   *
   *  건축 투시도의 소실점은 보통 화면 폭의 2~3배이고 완만하면 그보다 멀다 — **정상 구도가
   *  통째로 그 밖**이었고, 그 획들이 「수평」으로 읽혀 문서가 1점으로 잠겼다.
   *
   *  px로 재면 h·W와 무관해진다: 처짐 d = h·R/D 이므로(R = 획의 가로 폭, D = 소실점 거리)
   *  «d < σ» 는 곧 «이 획으로는 그 소실점을 무한원과 구별할 수 없다»는 뜻이다.
   *  σ는 포인터 잡음 대역(1px)의 바로 위로 둔다 — 그보다 작은 처짐은 손이 만든 것이 아니다.
   *  축 스냅이 H로 붙인 획은 처짐이 **정확히 0**이므로 언제나 이 안이다(의도한 수평의 길). */
  PARALLEL_PX: 1.5,
  /** **새 소실점이 설 수 있는 최대 거리** — 획의 시작점에서 그 소실점까지, 화면 폭 대비.
   *  그보다 멀면 무한원으로 읽는다(= 화면 평행 축 H).
   *
   *  `PARALLEL_PX`와 **같은 물음의 다른 절반**이다. 그쪽은 「이 획으로 무한원과 구별할 수
   *  있는가」(포인터가 답한다)이고, 이쪽은 **「그 소실점이 작도 대역 안인가」**(구도가 답한다).
   *  둘을 OR로 묶으므로 실제 경계는 `min(6W, h·R/1.5)`이다(h = 시작점의 지평선 이격,
   *  R = 획의 가로 폭). 6W가 거의 언제나 먼저 걸리고, 아주 짧게 지평선 바로 아래를 긋는
   *  획(h·R < 10800)에서만 `PARALLEL_PX`가 먼저 걸린다.
   *
   *  **왜 필요한가**(web2-06 지시 2): 포인터 잡음만 보던 초판은 처짐 **2px**에서 이미
   *  33W 소실점을 만들었다 — 지평선을 긋고 수평선을 그으면 손 겨냥 오차가 그대로
   *  «먼 소실점»으로 읽혀 카메라가 망가졌다(그 획은 작도 획이라 **실행취소도 안 된다**).
   *  수평 의도가 흡수하는 겨냥 오차는 `h·R/(6W)` px다 — h=200·R=400에서 11.1px.
   *
   *  **왜 6인가 — 옳은 값이 아니라 «고른 값»이다**(web2-06 1차 리뷰어 [2]·[12]).
   *  ⛔ 초판은 「사람이 든 대역의 밖 첫 눈금」이라 적었는데 **틀렸다**: 지시 원문은
   *  「완만한 구도면 5000px을 **넘는다**」이므로 4.2W는 **하한**이다. 6W는 사람이 «정상»이라
   *  부른 대역의 밖이 아니라 **그 안 어딘가를 자르는 값**이다.
   *  남는 근거는 **대비표**다(`test/hsnap.test.ts`가 4·6·8·10을 갈아 끼워 매 실행에 낸다).
   *  ⚠ **표는 교환비를 보일 뿐 «어디서 자를지»를 말하지 않는다**(2차 리뷰어 [N1]·[N2]):
   *    · 하한 「1~5W는 산다」는 **앞 회차(web2-03)의 게이트**이고 사람 문면에는 상한이 없다.
   *      K=4는 그 대역을 깨므로 못 쓴다.
   *    · 상한 「6 vs 8」에는 **측정이 없다** — 손 겨냥 오차를 잰 적이 없다(AS-C1: 실획 표본 0).
   *      6은 **보고된 증상(「수평선을 못 긋는다」) 쪽에 여유를 둔 선택**이다(흡수 11.0 vs 8.0px).
   *  참고: 6W는 기본 f(0.87W)에서 축이 화상면과 이루는 각으로 **7.1°~9.9°**다(임계가 시작점
   *  기준이라 주점 기준 각에는 ±W의 폭이 있다). 그 대역에서는 «2점»이 화면에서 1점과
   *  구별되지 않으므로 H로 읽는 것이 틀린 답이 아니다 — **다만 f가 기본값일 때의 말이고**,
   *  이 갈래는 소실점이 0~1개일 때만 도므로 그때 f는 언제나 기본값이다.
   *
   *  ⚠ 대가: 그보다 먼 소실점은 **그어서** 못 만든다(web2-03이 10W·20W까지 열어 뒀던
   *  대역이다 — NOTES의 대비표). 그 자리는 **지평선 위를 찍는 길**이 답한다(지시 4-b).
   *  되돌릴 조건: 6W 밖 소실점을 그어서 만들려는 사용이 관측되면 올린다. */
  VP_FAR_W: 6,

  /** 방향을 믿는 최소 길이 (화면 대각 대비) */
  MIN_DIR_LEN_RATIO: 0.02,
  /** 소실점에 기여하는 최소 길이 = 그 2배 */
  VP_MIN_LEN_RATIO: 0.04,

  /** 오스냅 반경 px (화면 기준, 절대 — 선례 대역 10~15px의 안쪽) */
  OSNAP_RADIUS_PX: 8,
  /** 탭과 드래그를 가르는 이동량 px. 이 안이면 «찍기»다(지시 4-d).
   *  별도 도구를 안 만든다 — 탭은 지금까지 잡음으로 버려지던 자리이고,
   *  클릭 = 점 찍기는 캐드 선례다(A-3: 단순한 쪽). */
  TAP_MAX_PX: 2,
  /** 「잘못 찍힌 점」 문 — 탭도 아니고(끝점 이동 > TAP_MAX_PX) 이보다 작은 raw bbox
   *  대각의 획은 **애초에 만들지 않는다**(web2-13 3-b · 개정 3 초안 §6). 화면 css px.
   *  실측 근거는 `stage0/out/stray_gate_web2.json`(실획 28706 — 원시 Quick,Draw 장치
   *  픽셀): 실획 분포는 ≈0에 뭉친 점 찍힘 무리와 수십 px부터의 의도 획으로 갈리고
   *  3~10px 골짜기가 평평하다 — 6은 탭 문(2)의 3배·의도 획 개시 대역 아래의 골짜기
   *  가운데다. ⚠ 척도는 **bbox 대각**이다 — 끝점 거리로 재면 닫힌 한 붓이 오폐기된다
   *  (원장 첫 실측이 보였다). 버린 수는 진단 패널이 센다 — 크면 이 값이 틀린 것이다.
   *  되돌릴 조건: 실기기에서 「짧은 획이 억울하게 버려진다」(DEFERRED web2-13 표). */
  STRAY_MIN_PX: 6,
  /** 대기 획이 보이는 각도 창(도) — web2-13 3-a · 초안 §5.
   *  ⚠ web2-16 3-b가 **감쇠(그라디언트)를 폐지했다** — 사람이 정했다: 「페이드 같은 거
   *  없이 즉시 화면에서 없애자. 돌아오면 보이도록」. 이제 창 안 = 원 진하기(1),
   *  창 밖 = 안 보임(0)의 **이진**이고 이 값은 그 경계다. 문서(획)는 안 지워진다 —
   *  화면에서만이다(실행취소·저장·.brnl 불변 — 팔이 지킨다).
   *  값의 근거: 시점 스냅 임계(snapAngle ≈8.25° — 그 안은 «같은 시점»으로 접힌다)보다
   *  넉넉히 크고, 90°(측면)보다 훨씬 작은 한 점 — ⚠ 동작점 하나다(#12 · AS-C44).
   *  되돌릴 조건: 실기기에서 「너무 빨리/늦게 사라진다」 관측. */
  WAIT_FADE_DEG: 30,
  /** 대기 획 파선의 화면 px 패턴(그림·빈칸) — web2-16 3-a. 제도에서 파선은 «아직/숨은»의
   *  계열이다(A-3 — 숨은선). 값은 종전 벡터 점선(setLineDash [5,4])의 규격을 그대로
   *  옮겼다 — 숫자를 새로 짓지 않는다. 이제 이 패턴을 brush 흑연(#brushc)이 긋고,
   *  classic 렌더러의 벡터 점선도 같은 상수를 읽는다(단일 출처 #54). */
  WAIT_DASH_ON_PX: 5,
  WAIT_DASH_OFF_PX: 4,
  /** **«모델에 높이가 없다»의 Y 허용**(web2-17 4부) — lifted의 모든 3D 점이 이 안이면
   *  아직 지면 작도 국면이고, 소실점 축의 선은 지면선이다(지면 규칙 확대).
   *  대역 근거(#35 — 아무것도 안 거르는 방어층이 아님을 잰다): 지면 규칙이 놓는 Y는
   *  fp 잔차 대역(≤1e-15 실측 — ground_rule_web2.json)이고, 실제 높이(수직선 하나)는
   *  사람 눈높이 스케일에서 O(1)이다 — 1e-6은 그 사이 아홉 자릿수 골짜기의 값.
   *  규칙이 실제로 꺼지는 것은 lift4.test 반증 팔이 실행으로 확인한다. */
  HEIGHTLESS_Y: 1e-6,
  /** **연장선 획득 머무름 ms**(web2-18 2-b) — 커서가 승격 선분의 끝점 오스냅 반경 안에
   *  이만큼 머물면 그 선분이 획득되고, 그때부터 그 연장이 `ext` 후보가 된다.
   *
   *  왜 획득식인가: 종전에는 모든 승격 선분의 연장이 **상시** 후보였고 길이 상한도 없어
   *  획이 늘수록 화면이 연장선으로 덮였다(사람 관측 「허공에서도 스냅이 잡혀 끌려다닌다」).
   *  AutoCAD Extension·Rhino Ext 둘 다 상시가 아니다 — 선례를 그대로 따른다(A-3).
   *
   *  값 400의 근거: **호버 의도의 관용 대역**이다. 아래는 «지나가는 중»(빠른 조준
   *  이동에서 끝점 위를 스치는 시간)이고, 위는 «멈췄다»로 읽히기 시작하는 자리다 —
   *  참조점으로 웹 툴팁의 관행 지연(300~500 ms)을 쓴다. `FOLD_DELAY_MS`(1200)와 다른
   *  물음이다: 그쪽은 «연속 조작의 손 뗌 간격»이고 이쪽은 «머물렀다의 최소»다.
   *  ⚠ **동작점 하나다 — 스윕이 없다**(#12 · AS-C55). 실기기에서 안 쟀다.
   *  되돌릴 조건: 「획득이 안 된다(너무 오래 기다린다)」면 내리고, 「지나가기만 해도
   *  획득된다」면 올린다. 둘 다 화면에 표식이 있으므로 사람이 그 자리에서 판정한다. */
  EXT_ACQUIRE_MS: 400,
  /** **획득한 끝점에서 연장이 사는 길이** — 그 선분 길이의 몇 배까지인가(web2-18 2-b).
   *  그 밖은 아무것도 아니다(종전에는 무한이었다).
   *  값 2의 근거: 제도에서 «이 선을 늘려 만나는 자리»는 그 선과 비슷한 스케일의 이웃이다 —
   *  선분 하나 몫을 더 가는 데까지가 그 어법의 대역이고, 그보다 멀면 그 선이 아니라
   *  **다른 구속**(소실점·축·교점)이 답할 자리다. ⚠ 동작점 하나다(#12 · AS-C56 — 스윕 없음).
   *  되돌릴 조건: 「연장이 닿아야 할 데까지 안 간다」가 관측되면 올린다. */
  EXT_MAX_RATIO: 2,
  /** 동시에 획득해 두는 끝점 수(web2-18 2-b) — LRU. 둘인 근거는 «두 연장의 교점»이
   *  연장선 오스냅의 표준 용법이라서다(AutoCAD Extension도 둘을 함께 든다). */
  EXT_MAX_ACQUIRED: 2,
  /** 끝점·중점 병합 (기하 크기 대비) */
  MERGE_RATIO: 0.002,
  /** 선분 밖 허용 (선분 길이 대비) */
  SEG_OVERSHOOT_RATIO: 0.02,
  /** 선분 **밖**(연장선) 위의 점을 리프팅이 되찾을 때의 사영 일치 한계 px.
   *  공간 여유가 아니라 **수치 동일성 검사**라서 작다 — 사람이 연장선 오스냅에 붙였다면
   *  확정 2D가 바로 그 점의 사영이므로 왕복 오차가 fp 수준이어야 한다.
   *  안쪽 점에는 MERGE_RATIO를 그대로 쓴다(그쪽은 진짜 공간 여유다). */
  LINE_MATCH_PX: 0.5,
  /** 3D 교차 허용 간격 (기하 크기 대비) */
  INTERSECT_GAP_RATIO: 0.01,
  /** **면의 평면성 허용** (기하 크기 대비). 루프의 정점들이 이 두께 안에 들어와야 면이다.
   *
   *  값을 `INTERSECT_GAP_RATIO`와 **같게** 둔 것이 근거다: 루프의 모서리는 3D 교차로
   *  이어진 것이고, 그 교차가 이미 이 간격까지 «만났다»로 받아들여진다. 면이 그보다
   *  엄하면 «만난 선들로 만든 루프인데 면은 안 된다»가 되고, 그보다 헐거우면 교차가
   *  아닌 것도 면이 된다. 두 임계는 **같은 물음의 앞뒤**라 한 값이다.
   *  ⚠ 동작점 하나다(#12) — 되돌릴 조건: 손으로 그린 정상 루프가 평면성으로 거부되는
   *  것이 관측되면 올린다. 반증은 `face.test.ts`의 「비평면은 거부한다」가 잰다. */
  PLANAR_RATIO: 0.01,

  /** 눈높이 — 지면(Y=0)에서 카메라까지. 단위는 미터로 읽는다.
   *
   *  **f와는 다른 축이다.** f는 깊이 압축률(화각)이고 이것은 **Y 스케일**이다.
   *  스케일이 무차원이어도 값 하나는 필요한데, 임의값보다 의미 있는 값이 낫다:
   *  이 도구는 사람 시점으로 공간을 조직하는 것이므로 눈높이가 사람 눈높이인 것이
   *  그 전제와 일관되고, 나중에 치수를 넣을 때 기준이 이미 서 있다(방을 그리면 대략 실척).
   *
   *  ⚙ **조절 가능하게 하지 않았다** — 지금 이 값을 바꿔야 할 사용자 요구가 없다(A-3).
   *  되돌릴 조건: 실척 치수 입력이 들어오거나, 사람이 눈높이를 바꾸려 하는 것이 관측되면
   *  그때 연다. 열 자리는 여기 하나이므로(원칙 a) 상수를 인자로 바꾸면 된다. */
  EYE_HEIGHT: 1.6,

  /** 1점 상태의 f 기본값 (f/W). 깊이 배율 게이지 — 임의값이되 화각 참고 대역(≈60°) 안.
   *  이론서 18.4: f/W ≥ 0.87이면 60° 이하. */
  DEFAULT_F_RATIO: 0.87,

  /** 지면 격자 — 한 칸 1(눈높이와 같은 단위. 눈높이가 1.6이므로 «1 m 격자»로 읽힌다)
   *  이고 원점 기준 ±GRID_HALF 칸. 화면 각도 균등 분할이 아니라 **공간의 정사각형**이다
   *  (이론서 9.5 — 등간격은 1/(V−uₙ)이 등차수열). */
  GRID_STEP: 1,
  GRID_HALF: 24,

  /** 선 굵기 px — 화면 고정, 거리 무관 */
  LINE_W_RESULT: 1.5,
  LINE_W_GUIDE: 1.3,

  /** 제도펜 니브 굵기 px — 기본값과 조절 대역. 연필과 **재료가 다르므로 도구가 다르다**
   *  (지시 4-h): 연필은 경도(2H~2B)를 고르고, 펜은 니브를 고른다. 실제 홀더펜·제도펜이
   *  그렇게 갈린다. 대역은 로트링 계열의 관용 니브(0.1~2.0 mm)를 화면 px로 읽은 것이다. */
  NIB_PX: 1.5,
  NIB_MIN: 0.4,
  NIB_MAX: 4,

  /** 뷰 썸네일 폭 px(web2-12 5번) — 저장 시점에 화면 합성을 이 폭으로 줄여 굽는다.
   *  높이는 화면 비례. 동작점(#12 — AS-C39): 스윕 없음. 근거는 «펼친 목록에서 장면을
   *  알아볼 수 있는 최소 대역»의 한 점이고, 한 장의 바이트·생성 시간은 원장이 든다
   *  (views_thumb_web2.json). 상한 몫: .brnl 파서가 장당 300KB 위를 강등한다(file.ts). */
  THUMB_W: 160,

  /** 종이 탭 길게 누르기 ms(web2-19 2-c — 썸네일·이름·삭제가 열린다). 동작점(#12 —
   *  스윕 없음): 근거는 모바일 길게 누르기의 관행 대역(안드로이드 기본 ≈400~500)이고
   *  손 표본은 0이다. 이동 허용은 OSNAP_RADIUS_PX 재사용(«누른 자리에서 안 벗어났다»는
   *  같은 물음 — 새 숫자를 안 짓는다). 되돌릴 조건: 실기기에서 「탭이 자꾸 팝업이 된다」
   *  (올린다) / 「길게 눌러도 안 뜬다」(내린다) — DEFERRED 실기기 표. */
  PAPER_LONGPRESS_MS: 500,

  /** **셔터의 번쩍임 ms**(web2-25 3-a) — 「+」가 «찍는 동작»이 됐으므로 누르는 순간
   *  화면이 한 번 짧게 번쩍한다. **짧고 무채색**이다(지시 ⚠ — 색 ⛔).
   *  동작점(#12 · AS-C90): 근거는 순간 피드백의 관행 대역(100~150ms — 그보다 짧으면
   *  프레임 하나에 묻히고, 길면 «화면이 깜빡였다»가 아니라 «가려졌다»로 읽힌다).
   *  되돌릴 조건: 실기기에서 「찍힌 줄 몰랐다」(올린다) / 「눈에 거슬린다」(내린다). */
  SHUTTER_FLASH_MS: 120,

  /** **후행 확정(머무름) — 옐로 전용**(web2-22 2부 · QuickShape 어법 — Procreate 선례:
   *  끝에서 떼지 않고 잠깐 머무르면 반듯해진다. 토글이 아니라 제스처다 ⛔).
   *  600ms 초안 — Procreate 기본(중간 ≈0.8s)보다 조금 짧게 시작(동작점 #12 · AS-C79 —
   *  실기기 「머무름 시간이 손에 맞는가」 행이 판정). */
  HOLD_MS: 600,
  /** 머무름 판정의 이동 허용 화면 px — 손이 «가만히»일 때도 미세하게 떤다. 탭 대역
   *  (TAP_MAX_PX 2)보다 넓게 — 펜 끝 떨림은 탭보다 크다(동작점 · AS-C79). */
  HOLD_JITTER_PX: 4,
  /** 반듯해진 획이 **화면 수평·수직**에 붙는 대역(도) — 직선화가 먼저고 그다음 이 붙임
   *  (지시 2-a 순서). ⛔ 소실점 방향은 안 넣는다 — 그건 작도다(트레이싱지의 일).
   *  5°는 동작점(#12 · AS-C79) — QuickShape의 «가까우면 붙는» 감각 대역. */
  YELLOW_SNAP_DEG: 5,
  /** **옐로 raw 솎기 임계(화면 px)** — web2-24 4-b: 옐로 획은 raw(손 점렬)가 정본
   *  기하이고, 합쳐진 포인터 사건의 수백 점을 확정 시점에 RDP로 솎는다. 임계는
   *  «눈에 안 보이는 선»(0.5px 초안 — AS-C82. ⛔ 크게 잡지 마라: 눈에 보이면 그림이
   *  바뀐 것이다 — 지시 §4). 반증(㉯): 5px로 키우면 이탈이 눈에 보인다 —
   *  yellowraw_measure가 그 변이를 실제로 돌려 원장에 남긴다. */
  RAW_SIMPLIFY_PX: 0.5,

  /** **자동 저장 상한 가정**(web2-22 3부) — localStorage 브라우저 상한은 보통 5~10MB인데
   *  **알아낼 표준 길이 없다**(지시 문면). 보수적으로 5MB를 가정한다(AS-C80 — 실제로
   *  실패하면 그때의 바이트를 원장에 적어 가정을 고친다). 단위는 UTF-16 코드 유닛 수
   *  (= JSON이 거의 ASCII라 문자열 length ≈ 바이트 — 브라우저 quota도 대개 이 단위다). */
  /** **굽기(make2d)가 사람을 기다리게 해도 되는 상한 ms**(web2-23 1-b — 지시의 초안값).
   *  넘으면 진행 표시를 띄운다. ⚠ **프레임 예산이 아니다** — 옐로를 얹을 때 «한 번» 도는
   *  비용이고 기준이 「사람이 기다릴 만한가」다. 실측은 이 값의 ~1% 대역이라 진행 표시를
   *  안 만들었다(AS-C87 — 발화 조건이 없는 UI를 안 만든다). 원장 `cost23_web2.json`이
   *  이 상수를 읽어 초과 칸을 센다: 여기 두는 까닭은 임계가 원장 밖에 있으면 낡음이
   *  안 잡히기 때문이다(D-C4). */
  BAKE_BUDGET_MS: 500,

  AUTOSAVE_LIMIT_BYTES: 5 * 1024 * 1024,
  /** 사전 경고 임계(상한 대비 비율 — 초안 70%, 동작점 AS-C80): **실패하기 전에** 말한다.
   *  autosaveWarned(실패 «후» 알림)는 그대로 남는다 — 이건 그 앞의 문이다(조용한 실패
   *  방지 — 몇 시간 그리다 어느 순간부터 저장이 안 되고 있었다는 걸 나중에 아는 형태). */
  AUTOSAVE_WARN_RATIO: 0.7,

  /** 모서리 넘김 길이 px(web2-12 8번) — **화면 고정**(원칙 e: 줌해도 안 커진다) ·
   *  표현만(기하 불변 — core/overshoot.ts 머리주석이 정본). 동작점(#12 — AS-C41):
   *  스윕 없음. 손 제도의 넘김이 선 굵기의 몇 배 대역이라는 관행에서 한 점. */
  OVERSHOOT_PX: 7,

  /** **접기 지연 ms** — 카메라 조작에서 손을 떼고 이만큼 지나면 정렬로 접힌다.
   *
   *  하한은 «연속 조작의 손 뗌 간격»이다: 여러 각도를 살필 때 한 끌기와 다음 끌기 사이가
   *  그 간격이고, 그 안에서 접히면 살펴보기가 막힌다. 참조점으로 **운영체제의 더블클릭
   *  경계 500 ms**를 쓴다 — 그것이 「한 동작으로 이어진다」의 관용 대역 상한이므로,
   *  의도적인 다시 잡기는 그보다 느리다. 여기는 그 **2.4배**라 다시 잡기가 안 걸린다.
   *  상한은 「반응이 없다」로 읽히는 대역(약 2 s) 아래다 — 접히는 것이 곧 그릴 수 있게
   *  되는 것이므로 지연이 그대로 대기시간이다.
   *
   *  ⚠ **실기기에서 안 쟀다**(마우스로 골랐다). 되돌릴 조건: 기울인 채로 살펴보는 것이
   *  막힌다는 관측이 나오면 올린다. 붙잡고 있는 동안은 무한히 안 접히므로(`held`)
   *  이 값이 짧아도 「살펴보기 자체」는 막히지 않는다 — 막히는 것은 «놓고 보기»뿐이다. */
  FOLD_DELAY_MS: 1200,
  /** 접히는 애니메이션 ms — 갑자기 바뀌면 놀란다. 시점 전환의 관용 대역(250~400 ms) 안이고,
   *  지연(1200)의 4분의 1이라 「기다렸다」와 「접힌다」가 화면에서 안 섞인다. */
  FOLD_ANIM_MS: 300,

  /** 지우개 px */
  ERASER_PX: 12,
  ERASER_MIN: 4,
  ERASER_MAX: 60,

  /** **점별 필압 양자화 분모**(web2-11 1-c) — 저장값 = round(pressure × PRESS_Q), 0..PRESS_Q.
   *  8191인 근거: 실기기 펜(Wacom Pro Pen 3)이 **선언한 단계가 8192**(0 포함 8191 구간)라
   *  이 분모면 하드웨어가 주는 것 이상을 안 버린다(오차 ≤ 1/16382 ≈ 6.1e-5).
   *  되돌릴 조건(도달 가능한 형태 — 1차 리뷰어 [7]이 「8191을 넘으면」의 발화 불가를
   *  잡았다): 진단 패널 「단계 N · 양자화 후 M」에서 **M < N**이 관측되면 — 서로 다른
   *  원시 필압 둘이 같은 저장값으로 접힌 것이므로 — 이 분모가 브라우저 분해능보다 좁다.
   *  그때 올리고 stroke_payload_web2·input_capture_web2 원장을 재실행한다(인용 연쇄). */
  PRESS_Q: 8191,
} as const

/** **시점 스냅 임계의 상한(rad)** — 실제 임계는 `level.ts`의
 *  `snapAngle(f, W) = min(atan(f / (VP_FAR_W·W)), POSE_SNAP_RAD)` 다(web2-08 지시 3).
 *  정렬은 그 안으로 **의도적으로 가져왔을 때만** 걸리고, 밖이면 기울인 채 둔다 —
 *  머무는 상태다. 판정은 `foldTarget`/`pitchSnaps` 한 자리다. 요(2점→1점 축 붙기)도
 *  같은 임계다 — 같은 물음(「정렬과 화면에서 구별되는가」)이라 한 값이다.
 *
 *  **두 물음의 AND다**(#61의 처방 — 임계를 옮기지 않고 빠진 축을 더한다):
 *  ① `atan(f/6W)` — **그 문서의 f**로, 그 기울기의 소실점(피치면 세로 소실점, 요면
 *    화면과 거의 평행해진 가로축의 소실점)이 작도 대역(`VP_FAR_W`=6W) 밖에 있는가.
 *    밖이면 이 앱 자신이 «무한원과 구별 못 한다»로 읽는 대역이다(`classifyNext`의
 *    H 갈래와 같은 물음 — 그쪽 주석의 «기본 f일 때» 한정이 여기서는 f를 실제로
 *    넣으므로 안 걸린다). 지시 문면 「수직선이 화면상 수직에 가까워질 정도」 그대로.
 *    넓은 화각(f=0.323W — 표준 픽스처의 값)에서는 3.08°로 좁아진다 — 거기서 8°
 *    기울기의 세로 소실점은 2.2W라 작도 대역 안(화면에서 모인다)인데, 그것을 접으면
 *    보고된 증상의 재발이다(1차 리뷰어 [1]이 잡았다 — 초판은 기본 f의 고정각 하나였다).
 *    ⚠ 이 6W는 **주점 기준**이고 `classifyNext`의 6W는 **획 시작점 기준**이다 — 같은
 *    대역 «크기»의 차용이지 같은 양의 재사용이 아니다(2차 리뷰어 [N10] · AS-C21).
 *  ② 상한 `POSE_SNAP_RAD = atan(0.87/6) ≈ 8.25°` — 기본 f에서의 ①값. 망원(f≈2.74W —
 *    표준 망원 픽스처)에서 ①이 24.5°까지 벌어지는 것을 자른다: 그 대역의 기울임이
 *    접히면 「무조건 2점으로 찾아간다」가 기울일 일이 가장 많은 구도에서 재발한다.
 *    지시의 둘째 문면 「즉 내려다보거나 올려다보지 않는 정도로」가 이 자세 쪽 물음이다.
 *    ⚠ «몇 도부터 내려다보기인가»의 실측은 없다(AS-C21 — 근거 없는 각도 문구를 안 쓴다).
 *
 *  참고 환산: 궤도 1px = `ORBIT_RAD_PER_PX`(0.005 rad) = 0.286°이므로 상한 8.25°는
 *  29px, 넓은 화각의 3.08°는 11px이다. ⚠ 실기기·실사용 표본 없이 고른 값이다
 *  (assumptions AS-C21). 되돌릴 조건: 임계 바로 밖(예: 상한 근처 기울임)에 «두려는»
 *  사용이 관측되면 좁히고, 정렬로 «못 가져가겠다»는 관측이 나오면 상한 쪽을 넓힌다. */
export const POSE_SNAP_RAD = Math.atan(C.DEFAULT_F_RATIO / C.VP_FAR_W)
