// 정해진 값 — 지시서 「정해진 값」 절에서 그대로. 근거가 없는 값은 넣지 않는다.
// 임계는 전부 비(比)다. 예외: 오스냅 반경·지우개(포인터 정밀도 문제라 선례가 절대 px).

export const C = {
  /** 소실점 방향 판정 — 대표 직선에서 소실점까지 수직거리 ÷ 획 길이 */
  VP_DIR_RATIO: 0.06,
  /** 화면 평행 판정 — 수평·수직에서 벗어난 정도(길이 대비) ≈ 2.87°.
   *  **축 스냅의 후보 고르기에만 쓴다**(가장 가까운 축). 카메라 판정에는 안 쓴다 — 아래. */
  SCREEN_PARALLEL_RATIO: 0.05,
  /** **지평선과 평행한가**를 가르는 px. 획의 «끝점 처짐»을 그대로 잰다(비가 아니다).
   *
   *  비(2.87°)로 재던 것이 결함이었다(web2-03 지시 2). 그 비가 뜻하는 소실점 거리는
   *  **시작점이 지평선에서 얼마나 떨어졌는가(h)에 비례**한다: 경계 = h ÷ tan(2.87°) ≈ h × 20.
   *  그래서 지평선 가까이서 그으면 경계가 무너진다 — 실측:
   *
   *      h= 40px → 0.7W    h= 80px → 1.3W    h=150px → 2.5W
   *      h=250px → 4.2W    h=400px → 6.7W        (W = 화면 폭)
   *
   *  건축 투시도의 소실점은 보통 화면 폭의 2~3배이고 완만하면 그보다 멀다 — **정상 구도가
   *  통째로 그 밖**이었고, 그 획들이 「수평」으로 읽혀 문서가 1점으로 잠겼다.
   *
   *  px로 재면 h·W와 무관해진다: 처짐 d = h·R/D 이므로(R = 획의 가로 폭, D = 소실점 거리)
   *  «d < σ» 는 곧 «이 획으로는 그 소실점을 무한원과 구별할 수 없다»는 뜻이다.
   *  σ는 포인터 잡음 대역(1px)의 바로 위로 둔다 — 그보다 작은 처짐은 손이 만든 것이 아니다.
   *  축 스냅이 H로 붙인 획은 처짐이 **정확히 0**이므로 언제나 이 안이다(의도한 수평의 길). */
  PARALLEL_PX: 1.5,
  /** **새 소실점이 설 수 있는 최대 거리** — 획의 시작점에서 그 소실점까지, 화면 폭 대비.
   *  그보다 멀면 무한원으로 읽는다(= 화면 평행 축 H).
   *
   *  `PARALLEL_PX`와 **같은 물음의 다른 절반**이다. 그쪽은 「이 획으로 무한원과 구별할 수
   *  있는가」(포인터가 답한다)이고, 이쪽은 **「그 소실점이 작도 대역 안인가」**(구도가 답한다).
   *  둘을 OR로 묶으므로 실제 경계는 `min(6W, h·R/1.5)`이다(h = 시작점의 지평선 이격,
   *  R = 획의 가로 폭). 6W가 거의 언제나 먼저 걸리고, 아주 짧게 지평선 바로 아래를 긋는
   *  획(h·R < 10800)에서만 `PARALLEL_PX`가 먼저 걸린다.
   *
   *  **왜 필요한가**(web2-06 지시 2): 포인터 잡음만 보던 초판은 처짐 **2px**에서 이미
   *  33W 소실점을 만들었다 — 지평선을 긋고 수평선을 그으면 손 겨냥 오차가 그대로
   *  «먼 소실점»으로 읽혀 카메라가 망가졌다(그 획은 작도 획이라 **실행취소도 안 된다**).
   *  수평 의도가 흡수하는 겨냥 오차는 `h·R/(6W)` px다 — h=200·R=400에서 11.1px.
   *
   *  **왜 6인가 — 옳은 값이 아니라 «고른 값»이다**(web2-06 1차 리뷰어 [2]·[12]).
   *  ⛔ 초판은 「사람이 든 대역의 밖 첫 눈금」이라 적었는데 **틀렸다**: 지시 원문은
   *  「완만한 구도면 5000px을 **넘는다**」이므로 4.2W는 **하한**이다. 6W는 사람이 «정상»이라
   *  부른 대역의 밖이 아니라 **그 안 어딘가를 자르는 값**이다.
   *  남는 근거는 **대비표**다(`test/hsnap.test.ts`가 4·6·8·10을 갈아 끼워 매 실행에 낸다).
   *  ⚠ **표는 교환비를 보일 뿐 «어디서 자를지»를 말하지 않는다**(2차 리뷰어 [N1]·[N2]):
   *    · 하한 「1~5W는 산다」는 **앞 회차(web2-03)의 게이트**이고 사람 문면에는 상한이 없다.
   *      K=4는 그 대역을 깨므로 못 쓴다.
   *    · 상한 「6 vs 8」에는 **측정이 없다** — 손 겨냥 오차를 잰 적이 없다(AS-C1: 실획 표본 0).
   *      6은 **보고된 증상(「수평선을 못 긋는다」) 쪽에 여유를 둔 선택**이다(흡수 11.0 vs 8.0px).
   *  참고: 6W는 기본 f(0.87W)에서 축이 화상면과 이루는 각으로 **7.1°~9.9°**다(임계가 시작점
   *  기준이라 주점 기준 각에는 ±W의 폭이 있다). 그 대역에서는 «2점»이 화면에서 1점과
   *  구별되지 않으므로 H로 읽는 것이 틀린 답이 아니다 — **다만 f가 기본값일 때의 말이고**,
   *  이 갈래는 소실점이 0~1개일 때만 도므로 그때 f는 언제나 기본값이다.
   *
   *  ⚠ 대가: 그보다 먼 소실점은 **그어서** 못 만든다(web2-03이 10W·20W까지 열어 뒀던
   *  대역이다 — NOTES의 대비표). 그 자리는 **지평선 위를 찍는 길**이 답한다(지시 4-b).
   *  되돌릴 조건: 6W 밖 소실점을 그어서 만들려는 사용이 관측되면 올린다. */
  VP_FAR_W: 6,

  /** 방향을 믿는 최소 길이 (화면 대각 대비) */
  MIN_DIR_LEN_RATIO: 0.02,
  /** 소실점에 기여하는 최소 길이 = 그 2배 */
  VP_MIN_LEN_RATIO: 0.04,

  /** 오스냅 반경 px (화면 기준, 절대 — 선례 대역 10~15px의 안쪽) */
  /** **툴팁이 뜨는 머무름 ms** — web2-28 2번이 지시대로 600으로 뒀고,
   *  **web2-30 12번이 실기기 관측(「뜨긴 하는데 너무 늦다」)으로 300으로 내렸다.**
   *  나머지 규칙은 그대로다(펜에서만 · 그리는 중 안 뜸 · `title` 재사용).
   *  ⚠ 600은 운영체제 툴팁 관행 대역(500~1000ms)의 값이었는데, 그 관행은 **마우스 호버**의
   *  것이다 — 펜은 화면에 닿아 있고 손이 이미 그 단추 위에 있으므로 «지나가다 뜬다»가
   *  일어나지 않는다. 그래서 대역을 그대로 옮길 이유가 없다. */
  TIP_DWELL_MS: 300,
  /** 치수선의 화면 규격(web2-29 1단계) — 전부 **화면 px**(줌에 안 커진다 · 원칙 e 계열).
   *  ⛔ 새 색을 안 짓는다: 치수는 작도 대역(`COL.construction`)이다. */
  /** **손글씨로 읽어 볼 획의 크기 상한**(화면 px · web2-29 2단계) — 이보다 큰 획은
   *  그림이다. 글자 대역의 상한이고, 넘으면 제안 자체가 안 뜬다(기본은 그림이다). */
  DIM_GLYPH_MAX_PX: 60,
  /** 한 묶음으로 볼 획 사이의 간격 — 글자 크기의 몇 배까지 이어 쓴 것으로 보는가 */
  DIM_GROUP_SPAN: 1.2,
  /** 한 묶음의 획 수 상한 — 「2500」이 넷, 두 획 글리프까지 여유(새 숫자를 크게 안 짓는다) */
  DIM_GROUP_MAX: 10,
  /** ── 글씨는 선을 꾹 눌러 시작한다(web2-39 1번) — **진입이 명시적이다** ────────
   *
   *  ⛔⛔ **web2-32 1번의 «손이 가른다» 분류기는 여기서 사라졌다.** 그 층은 「이 획이
   *  글씨인가」를 **추측**했고(짧다·방향 전환·뭉쳐 있다), 사용자가 실사용에서 그 추측이
   *  구조적으로 못 서는 것을 찾아 왔다: **작도선 자체가 숫자와 구별되지 않는다** —
   *  「1」은 세로선이고 짧은 세로선 셋은 「111」이며 제도에서 짧은 평행 세로선은
   *  널려 있다(기둥·문설주·해칭). 궤적을 봐도 1과 세로 작도선은 **같은 궤적**이라
   *  인식을 고쳐서는 못 막는다(AS-C143). 그래서 상수 넷(`TEXT_MIN_STROKES` ·
   *  `TEXT_TURN_RAD` · `TEXT_TURN_SEG_PX` · `TEXT_BASIS_TOL`)이 함께 걷혔다.
   *
   *  남은 규칙은 둘뿐이다: **상태 안 → 모든 획이 글씨 · 상태 밖 → 모든 획이 작도선.**
   *  그래서 여기 새로 나는 값도 둘이다(진입 시간 · 종료 시간). «먼 곳» 판정은
   *  `DIM_GLYPH_MAX_PX × DIM_GROUP_SPAN`을 그대로 쓴다 — 32-1의 뭉치 간격 규칙과
   *  **같은 물음**이라 새 숫자를 안 짓는다(#54). */
  /** **치수를 매길 선을 «꾹 누르는» 시간 ms**(web2-39 1번 — 사용자 요청으로 설정에
   *  노출된다). 기본은 지시문이 준 대역 0.4~0.5s의 가운데다.
   *  동작점(#12 — 스윕 없음): 근거는 모바일 길게 누르기의 관행 대역이고 손 표본은 0이다.
   *  ⚠ **`PAPER_LONGPRESS_MS`(500)를 그대로 쓰지 않았다** — 그쪽은 «탭이 우선인 자리»의
   *  값이라 길어야 하고(짧으면 탭이 팝업이 된다), 여기는 «그리기가 우선인 자리»라
   *  **손이 획을 시작하기 전에** 판정이 나야 한다. 두 자리의 값이 갈리는 것이 규칙이다.
   *  ⚠⚠ 이동 허용은 `HOLD_JITTER_PX` 계열(`hold.driftAllowPx`)을 재사용한다 — 「누른
   *  자리에서 안 벗어났다」는 옐로 머무름과 **같은 물음**이다(새 숫자 ⛔).
   *  되돌릴 조건: 실기기에서 「그으려는데 자꾸 글씨가 된다」(올린다) / 「꾹 눌러도
   *  안 잡힌다」(내린다) — DEFERRED 실기기 표. */
  WRITE_HOLD_MS: 450,
  /** **손이 멈추면 글씨가 끝난다 — 그 시간 ms**(web2-39 3번 · 지시문 「1초쯤」).
   *  종료 제스처를 만들지 않는 근거가 이 값이다: **숫자를 쓰는 중에 1초를 쉬는 일은
   *  드물다.** 동작점(#12 — 스윕 없음): 손 표본 0.
   *  ⚠ `HOLD_MS`(옐로 머무름 350)와 **다른 물음이라 값을 안 나눴다**: 그쪽은 「지금
   *  멈춰 있는가」(획 안)이고 여기는 「다음 획이 안 오는가」(획 사이)다. 획 사이의 쉼은
   *  획 안의 머무름보다 길다 — 두 값이 같으면 글자와 글자 사이에서 상태가 끊긴다.
   *  되돌릴 조건: 실기기에서 「다음 자리를 쓰기 전에 끊긴다」(올린다) / 「작도로 돌아왔는데
   *  아직 글씨다」(내린다). */
  WRITE_IDLE_MS: 1000,
  /** **글씨를 받는 선의 강조 굵기**(화면 px · web2-39 4번)와 **알파**.
   *  ⛔ **새 색을 안 짓는다**: 색은 `COL.snap`(「지금 짚은 것」 — 사후 수정으로 고른 치수가
   *  쓰는 바로 그 채널)이고 여기 있는 것은 «얼마나 두껍게·얼마나 옅게»뿐이다.
   *  굵기는 획 선폭(1~2px 대역)보다 눈에 띄되 덮지 않게 잡았고, 그래서 알파가 낮다 —
   *  **강조이지 덮개가 아니다**(치수선 밑에 깐다). 동작점(#12 — 스윕 없음). */
  WRITE_TARGET_PX: 6,
  WRITE_TARGET_ALPHA: 0.35,
  /** ── 치수의 대상 판정(web2-32 3번)은 **web2-39가 걷었다** ────────────────────
   *  32-3은 「숫자의 위치가 어느 선의 치수인지 정한다」였고 근접·정렬·선상 위치 셋을
   *  가중합했다(`DIM_TARGET_REACH`·`W_NEAR`·`W_ALIGN`·`W_ALONG`·`ALIGN_NEUTRAL`·
   *  `SPREAD_MIN`·`TIE` 일곱). **39-1이 그 물음 자체를 없앴다** — 꾹 눌러 고른 그 선이
   *  대상이므로 맞힐 일이 없다. 상수 일곱이 함께 걷혔다(쓰이지 않는 임계를 안 남긴다).
   *  근거 표는 `dimtarget32_web2.json`에 그대로 있다(원장은 기록이라 안 지운다). */
  /** 치수 숫자를 누를 때의 반경(화면 px) — 글자(DIM_TEXT_PX 11) 둘레의 손가락 대역 */
  DIM_LABEL_HIT_PX: 18,
  /** ── 숫자 인식의 **가로세로비 정규화**(web2-32 4번) ─────────────────────────
   *  30-8이 「자형」이 아니라 **비**를 가리켰다(비 0.65 → 6/20 · 비 1.00 → 18/20).
   *  0 = 종전(비 보존) · 1 = 두 축을 다 채움. 값은 `glyph32_web2.json`의 훑기가 정한다.
   *  ⚠ 1로 두지 않는 이유가 지시 문면이다 — 비는 «1»과 «0»을 가르는 데 쓸모가 있으므로
   *  **약한 특징으로 강등**하는 것이지 버리는 것이 아니다. */
  DIGIT_NORM_ALPHA: 0.5,
  /** 아주 좁은 글자를 무한정 늘리지 않는 바닥 — 짧은 변을 긴 변의 이 배수 아래로 안 본다 */
  DIGIT_ASPECT_FLOOR: 0.35,
  DIM_OFFSET_PX: 14,
  DIM_TICK_PX: 4,
  DIM_TEXT_PX: 11,
  /** ── 어긋남(web2-32 7번) — **적은 값과 잰 값이 다르다** ───────────────────────
   *  첫 치수가 축척을 정하고 나면(32-5) **둘째 치수부터** 「사람이 적은 값」과 「모델이
   *  가진 값」이 갈릴 수 있다. 이 문턱 밖이면 화면이 그 사실을 말한다 — **고치지는
   *  않는다**(#61 · 지시 문면: 사람이 적은 값을 프로그램이 덮어쓰지 않는다).
   *
   *  ⚠ **첫 치수에서 이 값은 구성상 0이다**(그 획이 축척의 분모였다). 29-2가 그것을
   *  「구성상 항등」으로 읽고 기능을 걷었는데(AS-C107), 30-6이 그 판정을 뒤집었다 —
   *  0이었던 것은 모형의 성질이 아니라 **표본이 하나**여서였다.
   *
   *  **동작점이다**(#12). ⚠ 초판 주석은 「2%는 2500 mm에서 50 mm이고 그것이 치수 스냅
   *  한 칸이므로 한 칸보다 작은 차이는 안 뜬다」고 적었는데 **1차 리뷰어가 이 회차의
   *  픽스처로 깼다**: 잰 값이 2000 mm면 한 칸(50)이 **2.5% > 2%라 뜬다.** 그 유도가
   *  성립하는 것은 **2500 mm 한 점**이고 그보다 짧은 선에서는 한 칸도 뜬다.
   *
   *  **왜 비인가**(진짜 이유): 절대 mm로 두면 **짧은 선이 통째로 조용해진다** —
   *  100 mm 선의 40% 어긋남이 40 mm라 한 칸 밑이다. 어긋남은 상대량이므로 자도 상대량이다.
   *  **짧은 선에서 한 칸이 뜨는 것은 결함이 아니다**: 그것은 실제로 다른 값이고 화면은
   *  「다르다」고만 말한다(**안 고친다** — #61).
   *
   *  ── **web2-34 7번 — 자를 대칭으로 바꾸고 문턱을 자연 분포 위로 올린다** ──────────
   *  ⚠⚠ 옛 값(`DIM_SKEW_RATIO` **0.02** · 자는 `|비 − 1|`)은 **소음** 기준이었는데 이 표시가
   *  말하려는 것은 소음이 아니라 **오독**이다(9000↔3000은 3배 · 자릿수 누락은 10배).
   *  DEFERRED가 적은 대로 흔들림(끝점 지터 · 소실점 각 오차)을 태워 **자연 분포**를 쟀다
   *  (`stage0/out/skew34_web2.json` — 1229칸):
   *
   *    자연 fold  중앙 1.0344 · p90 1.2568 · p95 1.6772 · p99 3.5194 · 최대 4.4218
   *    옛 문턱 0.02는 그 분포에서 **719/1009**(흔들린 칸) 뜬다 ← 사용자가 말한 병
   *
   *  **자를 대칭으로 잡은 근거**(이 자리가 가장 조용히 틀리기 쉽다): 오독은 양방향인데
   *  `|비−1|`은 위로 무한이고 **아래로 1에서 막힌다**. 같은 「100%」를 그 자에 주면
   *  3배 작게 적은 것(|0.333−1| = 0.667)도 **자릿수를 통째로 빠뜨린 것**(|0.1−1| = 0.9)도
   *  1을 못 넘어 **한 칸도 안 뜬다**(실측 0/1229 · 0/1229). 그래서 자를
   *  `fold = max(비, 1/비)`로 바꿨다 — 뒤집어도 같은 값이다.
   *
   *  **2를 고른 근거는 두 경계다**(둘 다 실측이다 — 동작점 하나로 안 말한다 #12):
   *    아래(오탐)  1.2 → 150/1229 · 1.5 → 77 · 1.75 → 60 · **2 → 59** · 2.5 → 56
   *                1.75 위로는 거의 안 준다(꼬리가 «퍼진 잡음»이 아니라 «재구성이 깨진 칸»이다)
   *    위(놓침)    3배 오독을 잡는 비율이 **2 → 1213/1229** · 2.75 → 1071 · **3.0 → 534**
   *                (3배의 fold는 정확히 3이 아니라 자연 흔들림이 곱해져 3 언저리에 퍼진다)
   *  ⚠ 이 분포는 **합성 지터**다 — 실기기 손획 표본은 여전히 0이다(원장 `what_this_does_not_say`). */
  DIM_SKEW_FOLD: 2,
  OSNAP_RADIUS_PX: 8,
  /** **선 후보의 띠 배율**(web2-26 3번) — `ext`·`perp`의 허용을 점 후보의 몇 배로 두는가.
   *  점과 선은 같은 자를 쓰면 안 된다: 점은 2차원으로 한 자리에 국소화돼 있어 반경이
   *  좁아야 모호하지 않고, 무한히 뻗은 직선은 **수직거리 한 축**만 뜻이 있어 그 축을
   *  넓혀도 다른 후보와 안 모호해진다. 실기기가 「방향은 정확한데 획득이 까다롭다」로
   *  낸 자리다(DEVICE-CHECK C6). ⚠ `ext`는 획득식이라(web2-18 2부) 이 띠는 **이미 획득한
   *  선**에만 걸린다 — 허공 끌림이 안 돌아온다. */
  OSNAP_LINE_RATIO: 2.5,
  /** **유지 띠**(이력) — 한 번 획득하면 이 배율을 벗어날 때까지 유지한다. 경계에서
   *  깜빡이는 것을 막는다. 획득 띠보다 넓어야 뜻이 있다(팔이 그 부등식을 지킨다). */
  OSNAP_LINE_HOLD_RATIO: 3.75,
  /** 탭과 드래그를 가르는 이동량 px. 이 안이면 «찍기»다(지시 4-d).
   *  별도 도구를 안 만든다 — 탭은 지금까지 잡음으로 버려지던 자리이고,
   *  클릭 = 점 찍기는 캐드 선례다(A-3: 단순한 쪽). */
  TAP_MAX_PX: 2,
  /** 「잘못 찍힌 점」 문 — 탭도 아니고(끝점 이동 > TAP_MAX_PX) 이보다 작은 raw bbox
   *  대각의 획은 **애초에 만들지 않는다**(web2-13 3-b · 개정 3 초안 §6). 화면 css px.
   *  실측 근거는 `stage0/out/stray_gate_web2.json`(실획 28706 — 원시 Quick,Draw 장치
   *  픽셀): 실획 분포는 ≈0에 뭉친 점 찍힘 무리와 수십 px부터의 의도 획으로 갈리고
   *  3~10px 골짜기가 평평하다 — 6은 탭 문(2)의 3배·의도 획 개시 대역 아래의 골짜기
   *  가운데다. ⚠ 척도는 **bbox 대각**이다 — 끝점 거리로 재면 닫힌 한 붓이 오폐기된다
   *  (원장 첫 실측이 보였다). 버린 수는 진단 패널이 센다 — 크면 이 값이 틀린 것이다.
   *  되돌릴 조건: 실기기에서 「짧은 획이 억울하게 버려진다」(DEFERRED web2-13 표). */
  STRAY_MIN_PX: 6,
  /** 대기 획이 보이는 각도 창(도) — web2-13 3-a · 초안 §5.
   *  ⚠ web2-16 3-b가 **감쇠(그라디언트)를 폐지했다** — 사람이 정했다: 「페이드 같은 거
   *  없이 즉시 화면에서 없애자. 돌아오면 보이도록」. 이제 창 안 = 원 진하기(1),
   *  창 밖 = 안 보임(0)의 **이진**이고 이 값은 그 경계다. 문서(획)는 안 지워진다 —
   *  화면에서만이다(실행취소·저장·.brnl 불변 — 팔이 지킨다).
   *  값의 근거: 시점 스냅 임계(snapAngle ≈8.25° — 그 안은 «같은 시점»으로 접힌다)보다
   *  넉넉히 크고, 90°(측면)보다 훨씬 작은 한 점 — ⚠ 동작점 하나다(#12 · AS-C44).
   *  되돌릴 조건: 실기기에서 「너무 빨리/늦게 사라진다」 관측. */
  WAIT_FADE_DEG: 30,
  /** 대기 획 파선의 화면 px 패턴(그림·빈칸) — web2-16 3-a. 제도에서 파선은 «아직/숨은»의
   *  계열이다(A-3 — 숨은선). 값은 종전 벡터 점선(setLineDash [5,4])의 규격을 그대로
   *  옮겼다 — 숫자를 새로 짓지 않는다. 이제 이 패턴을 brush 흑연(#brushc)이 긋고,
   *  classic 렌더러의 벡터 점선도 같은 상수를 읽는다(단일 출처 #54). */
  WAIT_DASH_ON_PX: 5,
  WAIT_DASH_OFF_PX: 4,
  /** **«모델에 높이가 없다»의 Y 허용**(web2-17 4부) — lifted의 모든 3D 점이 이 안이면
   *  아직 지면 작도 국면이고, 소실점 축의 선은 지면선이다(지면 규칙 확대).
   *  대역 근거(#35 — 아무것도 안 거르는 방어층이 아님을 잰다): 지면 규칙이 놓는 Y는
   *  fp 잔차 대역(≤1e-15 실측 — ground_rule_web2.json)이고, 실제 높이(수직선 하나)는
   *  사람 눈높이 스케일에서 O(1)이다 — 1e-6은 그 사이 아홉 자릿수 골짜기의 값.
   *  규칙이 실제로 꺼지는 것은 lift4.test 반증 팔이 실행으로 확인한다. */
  HEIGHTLESS_Y: 1e-6,
  /** **연장선 획득 머무름 ms**(web2-18 2-b) — 커서가 승격 선분의 끝점 오스냅 반경 안에
   *  이만큼 머물면 그 선분이 획득되고, 그때부터 그 연장이 `ext` 후보가 된다.
   *
   *  왜 획득식인가: 종전에는 모든 승격 선분의 연장이 **상시** 후보였고 길이 상한도 없어
   *  획이 늘수록 화면이 연장선으로 덮였다(사람 관측 「허공에서도 스냅이 잡혀 끌려다닌다」).
   *  AutoCAD Extension·Rhino Ext 둘 다 상시가 아니다 — 선례를 그대로 따른다(A-3).
   *
   *  값 400의 근거: **호버 의도의 관용 대역**이다. 아래는 «지나가는 중»(빠른 조준
   *  이동에서 끝점 위를 스치는 시간)이고, 위는 «멈췄다»로 읽히기 시작하는 자리다 —
   *  참조점으로 웹 툴팁의 관행 지연(300~500 ms)을 쓴다. `FOLD_DELAY_MS`(1200)와 다른
   *  물음이다: 그쪽은 «연속 조작의 손 뗌 간격»이고 이쪽은 «머물렀다의 최소»다.
   *  ⚠ **동작점 하나다 — 스윕이 없다**(#12 · AS-C55). 실기기에서 안 쟀다.
   *  되돌릴 조건: 「획득이 안 된다(너무 오래 기다린다)」면 내리고, 「지나가기만 해도
   *  획득된다」면 올린다. 둘 다 화면에 표식이 있으므로 사람이 그 자리에서 판정한다. */
  EXT_ACQUIRE_MS: 400,
  /** **획득한 끝점에서 연장이 사는 길이** — 그 선분 길이의 몇 배까지인가(web2-18 2-b).
   *  그 밖은 아무것도 아니다(종전에는 무한이었다).
   *  값 2의 근거: 제도에서 «이 선을 늘려 만나는 자리»는 그 선과 비슷한 스케일의 이웃이다 —
   *  선분 하나 몫을 더 가는 데까지가 그 어법의 대역이고, 그보다 멀면 그 선이 아니라
   *  **다른 구속**(소실점·축·교점)이 답할 자리다. ⚠ 동작점 하나다(#12 · AS-C56 — 스윕 없음).
   *  되돌릴 조건: 「연장이 닿아야 할 데까지 안 간다」가 관측되면 올린다. */
  EXT_MAX_RATIO: 2,
  /** 동시에 획득해 두는 끝점 수(web2-18 2-b) — LRU. 둘인 근거는 «두 연장의 교점»이
   *  연장선 오스냅의 표준 용법이라서다(AutoCAD Extension도 둘을 함께 든다). */
  EXT_MAX_ACQUIRED: 2,
  // ── 연장선의 **왕복 선언**(web2-30 11번) — 획득이 «머무름»에서 «왕복»으로 바뀌었다 ──
  // ⚠ `EXT_ACQUIRE_MS`는 남겨 둔다(옛 경로의 되돌리기 손잡이이자 «머무름 획득»의 기록).
  /** 왕복으로 인정할 **최소 바깥 이동**(화면 px) — 이보다 짧게 나갔다 오면 손떨림이다.
   *  값의 근거: 오스냅 반경(8px)의 대여섯 배 — 그 안의 움직임은 이미 «같은 자리»로 읽히는
   *  대역이고, 그보다 커야 «일부러 나갔다»가 된다. 동작점이다(#12 — 스윕 없음).
   *  되돌릴 조건: 「선언이 잘 안 된다」면 내리고 「그으려다 저절로 선언된다」면 올린다. */
  EXT_TRIP_MIN_PX: 48,
  /** 되돌아온 것으로 볼 **비율** — 최원거리의 이 배 아래로 돌아오면 왕복이 성립한다.
   *  0.6은 «절반 넘게 돌아왔다»의 바로 위다(0.5로 두면 시작점까지 거의 다 와야 한다). */
  EXT_TRIP_RETURN_RATIO: 0.6,
  /** 1차 판정의 자 — 바깥 이동이 그 직선 «위를 지났다»고 볼 수직거리(화면 px).
   *  오스냅 반경보다 넓다: 여기서 재는 것은 «물릴 만큼 가까운가»가 아니라 **의도**다. */
  EXT_TRIP_LINE_TOL_PX: 16,
  /** 2차 판정의 문 — 바깥 이동의 방향과 선 방향의 |cos| 하한. 0.94 ≈ **20°**.
   *  그보다 어긋나면 «그 선을 겨눈 것»으로 안 본다(조용히 아무 선이나 집지 않는다). */
  EXT_TRIP_DIR_COS: 0.94,
  /** 바깥 이동의 자취를 몇 점까지 드는가 — 1차 판정의 표본. 메모리 상한이지 임계가 아니다. */
  EXT_TRIP_PATH_MAX: 64,
  /** 끝점·중점 병합 (기하 크기 대비) */
  MERGE_RATIO: 0.002,
  /** 선분 밖 허용 (선분 길이 대비) */
  SEG_OVERSHOOT_RATIO: 0.02,
  /** 선분 **밖**(연장선) 위의 점을 리프팅이 되찾을 때의 사영 일치 한계 px.
   *  공간 여유가 아니라 **수치 동일성 검사**라서 작다 — 사람이 연장선 오스냅에 붙였다면
   *  확정 2D가 바로 그 점의 사영이므로 왕복 오차가 fp 수준이어야 한다.
   *  안쪽 점에는 MERGE_RATIO를 그대로 쓴다(그쪽은 진짜 공간 여유다). */
  LINE_MATCH_PX: 0.5,
  /** 3D 교차 허용 간격 (기하 크기 대비) */
  INTERSECT_GAP_RATIO: 0.01,
  /** **면의 평면성 허용** (기하 크기 대비). 루프의 정점들이 이 두께 안에 들어와야 면이다.
   *
   *  값을 `INTERSECT_GAP_RATIO`와 **같게** 둔 것이 근거다: 루프의 모서리는 3D 교차로
   *  이어진 것이고, 그 교차가 이미 이 간격까지 «만났다»로 받아들여진다. 면이 그보다
   *  엄하면 «만난 선들로 만든 루프인데 면은 안 된다»가 되고, 그보다 헐거우면 교차가
   *  아닌 것도 면이 된다. 두 임계는 **같은 물음의 앞뒤**라 한 값이다.
   *  ⚠ 동작점 하나다(#12) — 되돌릴 조건: 손으로 그린 정상 루프가 평면성으로 거부되는
   *  것이 관측되면 올린다. 반증은 `face.test.ts`의 「비평면은 거부한다」가 잰다. */
  PLANAR_RATIO: 0.01,

  /** 눈높이 — 지면(Y=0)에서 카메라까지. 단위는 미터로 읽는다.
   *
   *  **f와는 다른 축이다.** f는 깊이 압축률(화각)이고 이것은 **Y 스케일**이다.
   *  스케일이 무차원이어도 값 하나는 필요한데, 임의값보다 의미 있는 값이 낫다:
   *  이 도구는 사람 시점으로 공간을 조직하는 것이므로 눈높이가 사람 눈높이인 것이
   *  그 전제와 일관되고, 나중에 치수를 넣을 때 기준이 이미 서 있다(방을 그리면 대략 실척).
   *
   *  ⚙ **조절 가능하게 하지 않았다** — 지금 이 값을 바꿔야 할 사용자 요구가 없다(A-3).
   *  되돌릴 조건: 실척 치수 입력이 들어오거나, 사람이 눈높이를 바꾸려 하는 것이 관측되면
   *  그때 연다. 열 자리는 여기 하나이므로(원칙 a) 상수를 인자로 바꾸면 된다. */
  EYE_HEIGHT: 1.6,

  /** 1점 상태의 f 기본값 (f/W). 깊이 배율 게이지 — 임의값이되 화각 참고 대역(≈60°) 안.
   *  이론서 18.4: f/W ≥ 0.87이면 60° 이하. */
  DEFAULT_F_RATIO: 0.87,

  /** 지면 격자 — 한 칸 1(눈높이와 같은 단위. 눈높이가 1.6이므로 «1 m 격자»로 읽힌다)
   *  이고 원점 기준 ±GRID_HALF 칸. 화면 각도 균등 분할이 아니라 **공간의 정사각형**이다
   *  (이론서 9.5 — 등간격은 1/(V−uₙ)이 등차수열). */
  GRID_STEP: 1,
  GRID_HALF: 24,

  /** 선 굵기 px — 화면 고정, 거리 무관 */
  LINE_W_RESULT: 1.5,
  LINE_W_GUIDE: 1.3,

  /** 제도펜 니브 굵기 px — 기본값과 조절 대역. 연필과 **재료가 다르므로 도구가 다르다**
   *  (지시 4-h): 연필은 경도(2H~2B)를 고르고, 펜은 니브를 고른다. 실제 홀더펜·제도펜이
   *  그렇게 갈린다. 대역은 로트링 계열의 관용 니브(0.1~2.0 mm)를 화면 px로 읽은 것이다. */
  NIB_PX: 1.5,
  NIB_MIN: 0.4,
  NIB_MAX: 4,
  /** **제도 펜의 촉 계열 mm**(web2-30 2번) — 슬라이더를 없애고 이 다섯 중 하나를 고른다.
   *  ISO 128 / 로트링 이소그라프의 표준 계열이고 한 칸이 √2배다(0.18 · 0.25 · 0.35 ·
   *  0.5 · 0.7). **제도 펜의 굵기는 연속값이 아니다** — 실물에 없는 조작을 만들었기
   *  때문에 슬라이더에 기준이 있을 수 없었다(사람 관측: 「어떤 기준도 없이 허공에 있다」).
   *  ⛔ 계열의 나머지(0.13 · 1.0 · 1.4 · 2.0)는 안 넣는다 — 범위를 넓히지 않는다. */
  NIB_MM: [0.18, 0.25, 0.35, 0.5, 0.7] as readonly number[],
  /** mm → 화면 px 환산. **0.35 mm가 지금의 기본 굵기(`NIB_PX` 1.5 px)가 되도록** 못 박았다:
   *  0.35은 제도에서 가장 흔한 범용 촉이고, 그 자리에 기본을 두면 **이 회차가 선의 굵기를
   *  안 바꾼다**(무회귀). 그러면 다섯 촉이 0.77 · 1.07 · 1.50 · 2.14 · 3.00 px가 되어
   *  `NIB_MIN`~`NIB_MAX` 안에 든다.
   *  ⚠ 화면 px는 **줌에 안 커진다**(원칙 e) — 그래서 통 안의 견본이 그은 선과 1:1이다. */
  NIB_PX_PER_MM: 1.5 / 0.35,
  /** **펼침(flyout)과 리본 사이의 틈 px**(web2-30 3번) — 통은 누른 버튼의 왼쪽에 뜬다 */
  FLYOUT_GAP_PX: 8,
  /** 펼침이 화면 위아래로 넘칠 때 남기는 여백 px — **리본은 안 건드리고** 통만 민다 */
  FLYOUT_EDGE_PX: 6,

  /** 뷰 썸네일 폭 px(web2-12 5번) — 저장 시점에 화면 합성을 이 폭으로 줄여 굽는다.
   *  높이는 화면 비례. 동작점(#12 — AS-C39): 스윕 없음. 근거는 «펼친 목록에서 장면을
   *  알아볼 수 있는 최소 대역»의 한 점이고, 한 장의 바이트·생성 시간은 원장이 든다
   *  (views_thumb_web2.json). 상한 몫: .brnl 파서가 장당 300KB 위를 강등한다(file.ts). */
  THUMB_W: 160,

  /** 종이 탭 길게 누르기 ms(web2-19 2-c — 썸네일·이름·삭제가 열린다). 동작점(#12 —
   *  스윕 없음): 근거는 모바일 길게 누르기의 관행 대역(안드로이드 기본 ≈400~500)이고
   *  손 표본은 0이다. 이동 허용은 OSNAP_RADIUS_PX 재사용(«누른 자리에서 안 벗어났다»는
   *  같은 물음 — 새 숫자를 안 짓는다). 되돌릴 조건: 실기기에서 「탭이 자꾸 팝업이 된다」
   *  (올린다) / 「길게 눌러도 안 뜬다」(내린다) — DEFERRED 실기기 표. */
  PAPER_LONGPRESS_MS: 500,

  /** **셔터의 번쩍임 ms**(web2-25 3-a) — 「+」가 «찍는 동작»이 됐으므로 누르는 순간
   *  화면이 한 번 짧게 번쩍한다. **짧고 무채색**이다(지시 ⚠ — 색 ⛔).
   *  동작점(#12 · AS-C90): 근거는 순간 피드백의 관행 대역(100~150ms — 그보다 짧으면
   *  프레임 하나에 묻히고, 길면 «화면이 깜빡였다»가 아니라 «가려졌다»로 읽힌다).
   *  되돌릴 조건: 실기기에서 「찍힌 줄 몰랐다」(올린다) / 「눈에 거슬린다」(내린다). */
  SHUTTER_FLASH_MS: 120,

  /** **말린 앞 가장자리의 폭(CSS px)**(web2-40 2번) — 겹이 밀려 들어올 때 앞선 변에
   *  지는 그림자의 띠 폭이다. 전자책에서 **가져오는 것은 이것 하나**다(동작이 아니라
   *  앞 가장자리의 표현 — 지시 문면). 화면 고정이다: 종이의 «두께»는 문서 좌표의 길이가
   *  아니라 눈에 보이는 몇 px이므로 줌을 따라 자라면 두께가 아니라 «면»이 된다.
   *  14는 종이 한 장의 두께가 아니라 **말린 부분의 길이**다 — 이 앱의 다른 화면 고정 값
   *  대역(오스냅 반경 8 · 지우개 6)의 두 배 남짓이고, 그보다 좁으면 곡률이 안 읽히고
   *  넓으면 «그림자 띠»가 따로 보인다. 되돌릴 조건: 실기기에서 어느 쪽이든 관측되면. */
  LAY_CURL_PX: 14,
  /** 그 그림자의 **가장 짙은 자리의 알파** — 앞 가장자리에서 곱해지는 값이고 안쪽으로
   *  0까지 잦아든다. 동작 중에만 있고 제자리에서는 **정확히 0**이다(40-2 게이트 ②:
   *  동작이 끝난 화면이 동작 없이 얹은 화면과 픽셀로 같다). 0.18은 결의 진폭
   *  (`PAPER_STYLE`의 알파 0.075~0.277) 대역 안이라 «종이 위의 그늘»로 읽힌다 —
   *  그보다 짙으면 잉크로 읽히고 옅으면 두께가 안 읽힌다. */
  LAY_CURL_ALPHA: 0.18,

  /** **바탕 종이의 결은 겹보다 이 배 이상 약하다**(화면 규칙 R8 · web2-34 1번).
   *  판정식: `min(트레이싱 진폭, 옐로 진폭) ÷ 바탕 종이 진폭 ≥ PAPER_GRAIN_RATIO`.
   *
   *  **새 숫자를 안 짓는다**(#54) — 이 값은 30-9가 세운 게이트(「셋의 진폭이 서로 20%
   *  이내」 = `hi/lo ≤ 1.2`)의 **부호를 뒤집은 것**이다. 30-9가 물은 것은 「셋 다
   *  보이는가」였고 34-1이 묻는 것은 「셋 다 보이되 **바탕이 겹보다 약한가**」다
   *  (#76 ㉣의 내리는 판: 값을 바꾸면 그 값을 다른 뜻으로 읽던 팔이 깨진다 — 답은
   *  임계를 무르는 것이 아니라 팔이 무엇을 묻는지를 고치는 것이다). 두 조건은 **동시에
   *  못 선다** — 그래서 이 상수 하나가 「팔이 실제로 갈렸다」의 증거다.
   *
   *  ⚠ 하한은 이 상수가 아니다 — 지각 문턱(웨버 1% ≈ 2.1계조 · web2-26 2번)이 따로
   *  걸리고, 그 둘이 바탕 종이의 값을 (2.1, 겹최소÷1.2] 창에 가둔다. */
  PAPER_GRAIN_RATIO: 1.2,

  /** **접힌 통의 각인 글자는 연필 각인과 같은 대역이다**(화면 규칙 R6 · web2-34 2번).
   *  판정식: `펜 각인의 렌더 높이 ÷ 연필 각인의 렌더 높이 ≥ FOLD_MARK_MIN_RATIO`.
   *
   *  **왜 «높이»인가**: R6이 깨진 자리가 정확히 여기였다 — 접힌 펜의 촉은 니브 사각형의
   *  «폭»으로만 있었고(0.77~3.00 사용자단위 = 렌더 1.16~4.50 px · 이웃 칸 차 0.45~1.29 px)
   *  그 채널은 **글자가 아니라서 안 읽힌다**. 그러므로 게이트는 「채널이 있는가」가 아니라
   *  **「연필과 같은 크기의 글자인가」**여야 한다.
   *
   *  **0.9인 이유**: 이 자리의 답이 둘뿐이라 문턱이 두 답 사이에 있으면 된다 —
   *  ① 연필과 같은 `font-size`(8.5)로 두고 좁은 몸통(8.8 ↔ 연필 10)에 `textLength`로
   *     맞추면 **높이가 그대로**다(실측 비 1.000) ② `font-size`를 줄여 맞추면 높이가
   *     같이 준다(6.5로 줄인 판의 실측 비 0.733 — e2e 반증 팔이 그 수를 낸다).
   *  0.9는 그 둘 사이이고 **①만 통과시킨다**. ⚠ 자간·글꼴 폴백의 요동은 높이를 안 건드린다
   *  (같은 `font-size`면 줄 상자 높이가 같다 — 실측 15.00 px 대 15.00 px). */
  FOLD_MARK_MIN_RATIO: 0.9,

  /** **후행 확정(머무름) — 옐로 전용**(web2-22 2부 · QuickShape 어법 — Procreate 선례:
   *  끝에서 떼지 않고 잠깐 머무르면 반듯해진다. 토글이 아니라 제스처다 ⛔).
   *  600ms 초안 — Procreate 기본(중간 ≈0.8s)보다 조금 짧게 시작(동작점 #12 · AS-C79 —
   *  실기기 「머무름 시간이 손에 맞는가」 행이 판정). */
  HOLD_MS: 350,
  /** 사람이 고칠 수 있는 대역(초 → ms) — 프로크리에이트 QuickShape가 사용자에게 여는
   *  범위와 같다(A-3: 선례를 따른다). 화면 손잡이가 이 둘 사이에서만 움직인다. */
  HOLD_MS_MIN: 100,
  HOLD_MS_MAX: 1500,
  /** **표류 상자를 눈금 맞춘 시각**(web2-26 4번) — `HOLD_JITTER_PX`는 이 시간에 맞춰
   *  고른 값이다. 상자를 **시간에 비례해** 늘리고 줄이면 실효 표류 속도(`상자 ÷ 시간`)가
   *  임계 시간과 무관해진다: 4px/600ms = **6.7 px/s**가 어느 임계에서나 같은 문턱이다.
   *  ⚠ 이 비례가 없으면 「시간만 내리기」가 된다 — 600 → 350ms에서 문턱이 11.4 px/s로
   *  **느슨해져** 천천히 그린 곡선이 오히려 더 잘 직선화된다(지시가 경고한 반대 방향). */
  HOLD_DRIFT_REF_MS: 600,
  /** 아주 짧은 임계에서 상자가 손 떨림보다 작아지지 않게 하는 바닥(화면 px) */
  HOLD_JITTER_MIN_PX: 2,
  /** **머무름의 둘째 조건 — 창 속도**(web2-26 4번). 상자만으로는 「작게 빠르게 흔들다
   *  제자리로 돌아오는 손」을 못 거른다(표류가 0이다). 최근 `HOLD_SPEED_WINDOW_MS` 창의
   *  **순 이동 ÷ 창**이 이 값을 넘으면 시계가 처음으로 돌아간다.
   *  값은 **손 떨림 위 · 의도적으로 느린 획 아래**다(동작점 · AS-C97 — 분포는
   *  `hold26_web2.json`): 떨림 ±0.5px은 창 안 순 이동이 1px을 못 넘어 ≈8 px/s다. */
  HOLD_SPEED_PX_S: 25,
  /** 창 속도의 관측 창 ms — 지수평활 대신 **이동창**을 쓴다: 지수평활은 멈춘 뒤 문턱까지
   *  내려오는 데 `τ·ln(그리던 속도/문턱)`이 들어(실측 대역 600ms 남짓) 「빨라졌다」를
   *  통째로 잡아먹는다. 이동창은 창 길이만큼만 끌린다. */
  HOLD_SPEED_WINDOW_MS: 120,
  /** 머무름 판정의 이동 허용 화면 px — 손이 «가만히»일 때도 미세하게 떤다. 탭 대역
   *  (TAP_MAX_PX 2)보다 넓게 — 펜 끝 떨림은 탭보다 크다(동작점 · AS-C79). */
  HOLD_JITTER_PX: 4,
  /** 반듯해진 획이 **화면 수평·수직**에 붙는 대역(도) — 직선화가 먼저고 그다음 이 붙임
   *  (지시 2-a 순서). ⛔ 소실점 방향은 안 넣는다 — 그건 작도다(트레이싱지의 일).
   *  5°는 동작점(#12 · AS-C79) — QuickShape의 «가까우면 붙는» 감각 대역. */
  YELLOW_SNAP_DEG: 5,
  /** **옐로 raw 솎기 임계(화면 px)** — web2-24 4-b: 옐로 획은 raw(손 점렬)가 정본
   *  기하이고, 합쳐진 포인터 사건의 수백 점을 확정 시점에 RDP로 솎는다. 임계는
   *  «눈에 안 보이는 선»(0.5px 초안 — AS-C82. ⛔ 크게 잡지 마라: 눈에 보이면 그림이
   *  바뀐 것이다 — 지시 §4). 반증(㉯): 5px로 키우면 이탈이 눈에 보인다 —
   *  yellowraw_measure가 그 변이를 실제로 돌려 원장에 남긴다. */
  RAW_SIMPLIFY_PX: 0.5,
  /** **솎기 임계를 선폭에 묶는 비**(web2-27 3번) — 지시: 「허용 오차를 **화면 선폭의
   *  0.25배 아래**로 잡는다. 그 아래면 눈에 안 보인다.」 그 규칙을 값으로 옮긴 것이고,
   *  실제 임계는 `min(RAW_SIMPLIFY_PX, 0.25 × 그 획의 선폭)`이다 — 위 초안값(0.5)은
   *  **상한**으로 남는다. ⚠ HB(1.4px)에서 0.35px라 종전 0.5보다 **좁다**: 파일은 조금
   *  커지고 그림은 조금 더 정확하다. 그 맞바꿈을 원장이 값으로 낸다. */
  RAW_SIMPLIFY_WIDTH_RATIO: 0.25,

  /** **굽기(make2d)가 사람을 기다리게 해도 되는 상한 ms**(web2-23 1-b — 지시의 초안값).
   *  넘으면 진행 표시를 띄운다. ⚠ **프레임 예산이 아니다** — 옐로를 얹을 때 «한 번» 도는
   *  비용이고 기준이 「사람이 기다릴 만한가」다. 실측은 이 값의 ~1% 대역이라 진행 표시를
   *  안 만들었다(AS-C87 — 발화 조건이 없는 UI를 안 만든다). 원장 `cost23_web2.json`이
   *  이 상수를 읽어 초과 칸을 센다: 여기 두는 까닭은 임계가 원장 밖에 있으면 낡음이
   *  안 잡히기 때문이다(D-C4). */
  BAKE_BUDGET_MS: 500,

  /** **한 문서의 눈금**(값 5 MiB · 단위는 UTF-16 코드 유닛 = 문자열 length).
   *
   *  ⚠⚠ **web2-43이 이 상수의 «뜻»을 바꿨다**(값은 안 바꿨다).
   *  · 종전(web2-22 3부): 「localStorage 브라우저 상한 **가정**」. 알아낼 표준이 없어
   *    보수적으로 5MB를 가정했다(AS-C80). 그 가정을 web2-43 §0이 **실측으로 닫았다** —
   *    `store43_web2.json.localstorage_limit.value_units` = **5241856**(실제로
   *    `QuotaExceededError`에 부딪혀 쟀다). 가정은 맞았다.
   *  · 지금: 문서는 **IndexedDB**에 살고 그 예산은 GiB 대역이다(같은 원장의
   *    `storage_estimate.quota` — 실행마다 흔들리는 5~7 GiB). 그래서 「저장소가 찬다」는
   *    이 값이 재는 위험이 **아니게 됐다**. 남은 위험은 **한 문서가 커지는 것**이고
   *    답은 그대로다 — 「파일로 저장해 두라」.
   *  ⚠ 분모를 `storage.estimate().quota`로 바꾸지 **않았다**: 그러면 경고가 영영 안 뜬다
   *    (13개를 넣고도 0.23%였다 — `files43_web2.json`). 실패 전에 말하는 문이 사라지는
   *    쪽이 나쁘다(그 문을 만든 까닭이 「조용한 실패 방지」다). */
  AUTOSAVE_LIMIT_BYTES: 5 * 1024 * 1024,
  /** **자동 저장 지연 병합 ms**(web2-43 2번) — 계기는 주기가 아니라 **사건**(문서 판본
   *  변화)이고, 이 값은 「한 몸짓 안의 여러 사건을 한 번으로 묶는」 창이다.
   *  ⚠ 값 자체는 web2-05부터 코드에 박혀 있던 400을 **상수로 꺼낸 것**이고 안 바꿨다
   *  (지시 2번: 「성능이 걸리면 짧은 지연 병합은 허용 — 값은 상수로」).
   *  대가는 명시적이다: **마지막 획을 긋고 이 시간 안에 탭을 닫으면 그 획이 없다**
   *  (§0-2의 ①). 0으로 줄이면 획마다 직렬화+IndexedDB 쓰기가 돌아 그리는 동안 걸린다. */
  AUTOSAVE_DEBOUNCE_MS: 400,
  /** **최근 드로잉 목록에 뜨는 수**(web2-43 4번) — 파일 서랍 안이라 세로 자리가 유한하다.
   *  동작점(#12 · 스윕 없음): 서랍의 `max-height`(100vh − 58px)에 한 줄 34px로 들어가는
   *  수의 보수적인 한 점. 저장소에는 **다 남는다** — 이 수는 «보이는» 수다. */
  RECENT_LIMIT: 8,
  /** 사전 경고 임계(**위 눈금** 대비 비율 — 초안 70%, 동작점 AS-C80): **실패하기 전에** 말한다.
   *  autosaveWarned(실패 «후» 알림)는 그대로 남는다 — 이건 그 앞의 문이다(조용한 실패
   *  방지 — 몇 시간 그리다 어느 순간부터 저장이 안 되고 있었다는 걸 나중에 아는 형태). */
  AUTOSAVE_WARN_RATIO: 0.7,

  /** 모서리 넘김 길이 px(web2-12 8번) — **화면 고정**(원칙 e: 줌해도 안 커진다) ·
   *  표현만(기하 불변 — core/overshoot.ts 머리주석이 정본). 동작점(#12 — AS-C41):
   *  스윕 없음. 손 제도의 넘김이 선 굵기의 몇 배 대역이라는 관행에서 한 점. */
  OVERSHOOT_PX: 7,

  /** **접기 지연 ms** — 카메라 조작에서 손을 떼고 이만큼 지나면 정렬로 접힌다.
   *
   *  하한은 «연속 조작의 손 뗌 간격»이다: 여러 각도를 살필 때 한 끌기와 다음 끌기 사이가
   *  그 간격이고, 그 안에서 접히면 살펴보기가 막힌다. 참조점으로 **운영체제의 더블클릭
   *  경계 500 ms**를 쓴다 — 그것이 「한 동작으로 이어진다」의 관용 대역 상한이므로,
   *  의도적인 다시 잡기는 그보다 느리다. 여기는 그 **2.4배**라 다시 잡기가 안 걸린다.
   *  상한은 「반응이 없다」로 읽히는 대역(약 2 s) 아래다 — 접히는 것이 곧 그릴 수 있게
   *  되는 것이므로 지연이 그대로 대기시간이다.
   *
   *  ⚠ **실기기에서 안 쟀다**(마우스로 골랐다). 되돌릴 조건: 기울인 채로 살펴보는 것이
   *  막힌다는 관측이 나오면 올린다. 붙잡고 있는 동안은 무한히 안 접히므로(`held`)
   *  이 값이 짧아도 「살펴보기 자체」는 막히지 않는다 — 막히는 것은 «놓고 보기»뿐이다. */
  FOLD_DELAY_MS: 1200,
  /** 접히는 애니메이션 ms — 갑자기 바뀌면 놀란다. 시점 전환의 관용 대역(250~400 ms) 안이고,
   *  지연(1200)의 4분의 1이라 「기다렸다」와 「접힌다」가 화면에서 안 섞인다. */
  FOLD_ANIM_MS: 300,

  // ── 뷰 큐브 90° 화살표(web2-31 1번) ──────────────────────────────────────
  /** 화살표의 **안쪽 반경 · 바깥 반경 · 옆 반폭** — 전부 `cubeLayout.size`의 배수다.
   *  ⚠ px로 안 적는다(PITFALLS #88): 큐브 크기를 바꾸는 사람이 여기를 볼 이유가 없으므로
   *  절대값을 적으면 그 순간 말없이 갈린다. 배수라 결합이 코드에 보인다.
   *  바깥 반경이 1.0 미만이라 `input.ts`의 «반경 `size` 밖이면 무시» 문 안에 들어온다 —
   *  그 문이 곧 화살표의 문이기도 하다(문을 새로 안 만든다). */
  CUBE_ARROW_R0: 0.68,
  CUBE_ARROW_R1: 0.90,
  /** 옆 반폭. 기본 크기 80에서 ±10.4 px(짚는 대역 20.8 × 17.6) — 큐브 자신의 면(각 약
   *  54 px)보다 작지만 같은 급이고, 큐브 실루엣(반경 0.675·size) 바로 «밖»이라 안 먹는다. */
  CUBE_ARROW_HALF: 0.13,
  /** **가운데 「투시」의 반지름 — 크기의 배수**(web2-42 1번 · #88: px 상수 ⛔).
   *  큐브 실루엣 반경이 `0.675·size`(꼭짓점)이고 면 중심이 `0.39·size`이므로, 0.20은
   *  **면을 짚을 자리를 넉넉히 남기면서** 손가락 대역이 되는 값이다(size 80 → 지름 32 px).
   *  ⚠ 이 원이 아이소메트릭 자세의 «앞 꼭짓점»(반경 8 px)을 덮는다 — 그 순서의 근거는
   *  `viewcube.ts`의 `cubeHit` 머리주석이다. */
  CUBE_CENTER_R: 0.20,
  /** 뷰 큐브 그림쇠의 크기(px) — 종전 값 그대로. `state.ts`·`main.ts`가 각자 적던 것을
   *  `cubeLayoutFor(W)` 한 자리로 모았다(#54: 같은 규칙을 두 곳에서 계산하지 않는다). */
  CUBE_SIZE_PX: 80,
  /** **그림쇠가 비워 두는 바깥 여백(px)** — 오른쪽(1.5배 세로바까지)과 위(창 끝까지).
   *
   *  ⚠⚠ 이 둘은 «지금 큐브가 실제로 비워 두고 있던 값»이다(#88 — 남의 크기를 옮겨 적는
   *  대신 **지금 지키고 있는 경계**를 이름 붙였다): 종전 자리 cx = W−110 · cy = 60에
   *  큐브 실루엣 반경 0.675·80 = 54를 더하면 오른쪽 끝이 W−56, 위 끝이 6이다.
   *  **31-1이 바꾸는 것은 «무엇이 그 경계에 닿는가»뿐이다** — 전에는 큐브 꼭짓점이었고
   *  지금은 화살표 끝이다. 그래서 큐브가 왼쪽·아래로 물러난다(자리를 안 늘렸다).
   *  세로바를 넓히는 사람은 여기가 아니라 **`cubeLayoutFor`가 쓰는 이 여백**을 본다. */
  CUBE_MARGIN_RIGHT_PX: 56,
  CUBE_MARGIN_TOP_PX: 6,

  /** 지우개 px — 반경(화면 고정: 줌과 무관하다 · 원칙 e). `ERASER_PX`가 부팅 기본값이고
   *  `ERASER_MIN`/`MAX`는 **대역의 기록**이다(web2-34 3번에 끌개(막대)가 사라졌으므로
   *  더는 조작의 양 끝이 아니다 — 아래 계단이 그 대역을 나눈다). */
  ERASER_PX: 12,
  ERASER_MIN: 4,
  ERASER_MAX: 60,
  /** **지우개 크기 계단**(web2-34 3번 · 화면 규칙 R1 — 실물에 없는 연속 조작을 만들지
   *  마라). 30-2가 펜 굵기 슬라이더를 `NIB_MM`으로 바꾼 것과 **같은 수**이고, 값은
   *  **화면 px 반경**이다(지우개 자국은 화면 고정 원이라 통의 견본이 실제와 1:1이다).
   *
   *  **왜 이 넷인가**(#12 — 동작점 하나로 안 정한다):
   *  ① **칸비가 일정하다** — 5 → 12 → 27 → 60은 비가 2.40 · 2.25 · 2.22로 √5(2.236)
   *     대역이다. 크기의 차이는 «차»가 아니라 **«비»로 읽히므로**(웨버) 등비여야 네 칸이
   *     고르게 갈린다. 등차(예: 4·23·42·60)면 위 두 칸이 안 갈린다.
   *  ② **지금 기본값 12 px가 계단 위에 그대로 있다** — `NIB_PX_PER_MM`이 0.35 mm를 지금
   *     굵기 1.5 px에 못 박은 것과 같은 규약이다: **이 회차가 지우개의 크기를 안 바꾼다**
   *     (무회귀). 12를 계단에서 빼면 부팅 지우개가 달라지고, 그러면
   *     `passthru24_measure`의 여유 판정(`eraseClearanceMinPx > ERASER_PX / 2`, 실측 여유
   *     6.7 대 6.0)까지 같이 흔들린다.
   *  ③ **대역의 위 끝이 그대로다** — 60 = `ERASER_MAX`. 통에 담기 어렵다고 최댓값을
   *     낮추면 대역이 좁아져 **기능이 준다**(줄의 자리를 잡는 것이 답이다 — 가장 큰 줄이
   *     지름 120 px이고 `placeFlyout`이 화면 안에 넣는다).
   *  ④ 아래 끝은 5다(옛 `ERASER_MIN` 4보다 한 칸 굵다) — 등비 계단에서 12 아래 한 칸이고,
   *     4는 **끌개의 끝점**이었지 사람이 고르던 값이 아니다. 4와 5는 지름으로 2 px 차라
   *     같은 칸으로 읽힌다(칸비 1.25는 위 세 칸의 2.2배 대역 밖).
   *
   *  각인·줄의 표기는 **지름 mm의 반올림**이고 자는 `NIB_PX_PER_MM` 하나다(#54):
   *  2r/`NIB_PX_PER_MM` = 2.33 · 5.60 · 12.60 · 28.0 mm → 표기 `2 · 6 · 13 · 28`. */
  ERASER_R_PX: [5, 12, 27, 60] as readonly number[],

  /** **점별 필압 양자화 분모**(web2-11 1-c) — 저장값 = round(pressure × PRESS_Q), 0..PRESS_Q.
   *  8191인 근거: 실기기 펜(Wacom Pro Pen 3)이 **선언한 단계가 8192**(0 포함 8191 구간)라
   *  이 분모면 하드웨어가 주는 것 이상을 안 버린다(오차 ≤ 1/16382 ≈ 6.1e-5).
   *  되돌릴 조건(도달 가능한 형태 — 1차 리뷰어 [7]이 「8191을 넘으면」의 발화 불가를
   *  잡았다): 진단 패널 「단계 N · 양자화 후 M」에서 **M < N**이 관측되면 — 서로 다른
   *  원시 필압 둘이 같은 저장값으로 접힌 것이므로 — 이 분모가 브라우저 분해능보다 좁다.
   *  그때 올리고 stroke_payload_web2·input_capture_web2 원장을 재실행한다(인용 연쇄). */
  PRESS_Q: 8191,
  // ── 필압 보정(web2-26 6번 · 옵션 · 기본 꺼짐) ────────────────────────────
  /** 보정을 안 한 채 켰을 때의 `p1` 초안값 — 「센서 0~1 중 사람이 실제로 쓰는 구간은
   *  아래쪽 3분의 1 정도」(지시 1). `p0`는 0이다. */
  PRESS_CAL_P1: 0.35,
  /** `p1 − p0`의 하한 — 두 보정 획이 거의 같은 세기면 나눗셈이 폭발한다(절차가 다시 받는다) */
  PRESS_CAL_MIN_SPAN: 0.05,
  /** **바닥 농도** — 전 등급 공통(지시 4: 등급은 천장과 기울기를 정하고 바닥은 안 정한다).
   *  「거의 안 보이는 데까지」의 값. 종이 위 알파이므로 0.06은 흑연이 아주 옅게 닿은 자리다. */
  PRESS_FLOOR_ALPHA: 0.06,
  /** 최소 압력에서의 굵기 배수 — 최대에서 1이므로 굵기는 `1/이 값`배 는다(1.67배).
   *  농도는 같은 구간에서 바닥→천장(예: HB 0.06 → 0.75 = 12.5배)이라 **더 가파르다**(지시 3). */
  PRESS_WIDTH_MIN: 0.6,
  /** **«누른 자리»의 문**(web2-41 2번) — 이 획의 **최대 필압 ÷ 평균 필압**이 이 값 위면
   *  「여기서 눌렀다」로 읽는다. **절대값으로 안 잰다**(지시문): 획 안의 상대값이라
   *  보정이 켜졌든 꺼졌든, 사람마다 힘이 달라도 똑같이 작동한다 — 리프팅은 저장된
   *  원값만 보고 `PressCal`을 아예 안 읽으므로 그 무관함은 **구성상 보장**이다.
   *
   *  값의 근거는 **오발화/적중을 같이 낸 실측**이다(원장 `xint41_web2.json`의 `press_bands`
   *  — 결마다 300 시드 · 떨림 ±20%):
   *
   *      문턱   자연 결 오발화   옅은 결 오발화   의도한 누름 적중
   *      1.80      34/300           0/300          1500/1500
   *      **2.00**   **0/300**      **0/300**      **1413/1500**
   *      2.20       0/300           0/300           989/1500
   *
   *  **오발화가 0이 되는 첫 칸**에 뒀다. 대칭으로 안 고른 이유가 있다(#82의 여유와 다른
   *  물음이다): 41-1이 기본을 **대기**로 바꿨으므로 놓친 누름의 대가는 「대기로 남는다」
   *  (안전한 쪽)이고, 오발화의 대가는 **애매한 획이 서 버리는 것**(조용히 틀린 배치)이다.
   *  두 대가가 다르므로 문을 **자연 결 대역의 위 끝**(실측 1.905)에 붙인다.
   *  ⚠ **제1 단서가 아니다**(지시문): 신호가 없으면 거동이 41-1 그대로다.
   *  ⚠ 실기기 필압 결의 표본은 아직 0이다(AS-C1 계열 — `assumptions.md`). 위 결은
   *  «반사인 + 떨림»의 합성이다. 되돌릴 조건: 실기기에서 「눌렀는데 안 먹는다」면 내리고,
   *  「안 눌렀는데 서 버린다」면 올린다. */
  PRESS_PEAK_RATIO: 2,
  /** 보정 켠 획을 그릴 때 나누는 마디 수 — 마디마다 색이 갈린다(p5.brush는 획당 한 색).
   *  `PRESS_N`(필압 표본 12)과 같은 급으로 둔다 — 새 숫자를 안 짓는다(#54). */
  PRESS_SEGMENTS: 12,
  // ── 돋보기(web2-31 3번) — 대상에 맞춰 화면을 채운다 ─────────────────────────
  /** **여백 — 화면 각 변의 비**(지시 문면 「여백을 조금 둔다(10% 정도)」).
   *  대상이 채우는 것은 `1 − 2·0.10 = 80%`이고, 종횡비가 화면과 다르면 **좁은 축만**
   *  정확히 이 값이다(넓은 축은 반드시 그보다 크다 — 그것이 「채운다」의 뜻이다).
   *  ⚠ **px가 아니라 비다**(#88) — 창이 커지면 여백도 같이 큰다. px로 적으면 그 수가
   *  「그때 쓰던 화면 크기」와 말없이 묶인다. */
  ZOOM_FIT_MARGIN: 0.10,
  /** **게이트의 허용 폭(비의 절대 오차)** — 지시 문면 「여백이 10% ± 3%」 그대로다.
   *  닫힌 식이라 실측은 이보다 여섯 자릿수 아래이고(0.100000), 이 값이 가르는 상대는
   *  **반증 판**이다: 여백을 안 두는 판이 0.000000으로 넘고, 렌즈로 채우는 판은 값 축이
   *  아니라 정체 축(f·view.s)에서 갈린다(#86 — 두 축을 따로 잰다). */
  ZOOM_FIT_MARGIN_TOL: 0.03,
  // ── 화면 배율의 대역 ───────────────────────────────────────────────────────
  /** **뷰 오프셋 배율의 하·상한** — `dollyBy`가 인라인으로 들고 있던 0.2·8을 꺼낸 것이다.
   *  돋보기(작도 시점 갈래)도 같은 대역을 써야 하므로 **한 자리에서 읽는다**(#54:
   *  두 곳에 같은 수를 적고 「함께 고쳤다」로 닫으면 다음에 한쪽만 고치는 사람이 갈린다). */
  VIEW_S_MIN: 0.2,
  VIEW_S_MAX: 8,
  /** **3D 렌더의 근평면(세계 단위)** — `render3d.syncCamera`가 투영 행렬에 넣는 그 값이다.
   *  돋보기가 대상을 화면에 채울 때 **어떤 점도 이 안으로 못 들어오게** 하는 것이 그 자리의
   *  제약이므로 두 곳이 같은 값을 읽어야 한다(#54 — 갈리면 「채웠는데 앞이 잘린다」가 난다).
   *  ⚠ `camera.ts`의 `NEAR_Z`(−1e-3)와 **다른 물음**이다: 그쪽은 2D 사영의 잘라내기 경계
   *  (「이 선분의 앞부분을 그릴 수 있는가」)이고 이쪽은 **three.js가 실제로 자르는 자리**다. */
  RENDER_NEAR_UNITS: 1,
  // ── 보기 렌즈(web2-31 2번) — **해의 f가 아니라 «보는 방식»이다** ───────────────
  /** **보기 렌즈 배율의 하·상한** — 확정된 `Camera.f`의 몇 배까지 넓히고 좁히는가.
   *  0.5·2는 렌즈에서 «한 스톱»이다(초점거리 절반 = 화각이 두 배 가까이 넓어진다) —
   *  그 이상은 이 도구가 잰 적이 없으므로 대역을 넓히지 않는다.
   *  ⚠ **배율이지 mm가 아니다**(#88): 화각은 `f/W`가 정하므로 mm로 적으면 그 수가
   *  「그때 쓰던 센서 크기」와 말없이 묶인다. 여기 남는 것은 **확정된 f에 대한 비**다.
   *  ⚠⚠ `Camera.f`·`fSource`는 이 대역과 **무관하다** — 곱해지는 것은 렌더의 초점거리뿐이고
   *  리프팅은 언제나 `an.f`를 읽는다(CLAUDE.md §1 — 깊이 스케일의 출처는 하나다). */
  LENS_K_MIN: 0.5,
  LENS_K_MAX: 2,
  /** 손잡이 한 칸 — **log2(배율)** 위에서 잰다(렌즈는 곱으로 움직인다: 0.5↔1↔2가 등간격).
   *  0.05 스톱 = 배율 3.5%이고, 대역 양 끝(−1..+1) 사이에 41칸이다. */
  LENS_STEP_LOG2: 0.05,

  // ── 면 분류·채움(web2-45) ──────────────────────────────────────────────────
  /** **분류 임계(도 · 45-2)** — 법선의 기울기(수직에서 잰 각)가 이 안이면 슬라브,
   *  90°−이 값 밖이면 벽, 사이가 경사다. 10° = 손으로 세운 벽·깐 바닥의 잉크 오차가
   *  만드는 법선 흔들림(축 스냅이 지나므로 실제로는 1° 미만 — faces45 분류 스윕이 잰다)을
   *  덮고, 가장 완만한 «지붕»(통상 15°~)을 경사로 남기는 사이값이다.
   *  ⚠ 실사용 표본 없이 고른 값이다 — 자동이 틀리면 사람이 고친다(Face.cls)는 것이
   *  이 임계의 안전판이다(지시 문면 「자동 분류는 틀린다」). */
  FACE_CLASS_DEG: 10,
  /** 해칭 간격(화면 px · 45-4) — 제도 해칭의 통상 밀도(2~3mm ≈ 8~12px@96dpi) 안.
   *  화면 고정 판의 정의값이고, 면 고정 판은 생성 순간 이 px를 그 면 중심의 세계
   *  길이로 환산해 굳힌다(그 뒤로는 면에 붙는다 — 두 판의 정의 차가 곧 ⚑의 물음이다). */
  HATCH_SPACING_PX: 11,
  /** 해칭 각(도) — 제도 관행 45°. */
  HATCH_ANGLE_DEG: 45,
  /** **안 칠한 면의 «비춤» 옅기**(web2-48 48-9) — 칠·면 도구를 든 동안만 뜬다.
   *  종전 값 0.22는 «상시 표시»의 것이었다(45의 faceMat). 상시가 아니게 됐으므로
   *  「여기가 대상이다」만 말하면 되고, 그린 선(2H의 알파 0.50)보다 확실히 아래여야
   *  한다 — 도구를 든 순간 그림이 면에 묻히면 안 된다. 눈이 고른 값이다(#12 동작점 ·
   *  스윕 없음). 되돌릴 조건: 실기기에서 칠 도구를 들어도 어디가 면인지 안 보인다. */
  FACE_HINT_ALPHA: 0.14,
  /** 종이색 — `index.html`의 `--paper`와 **같은 값이어야 한다**(#54: 두 벌이지만 한쪽은
   *  CSS이고 한쪽은 GL이라 한 자리에 못 둔다 — `test/paint48.test.ts`가 둘의 일치를 잰다).
   *  칠한 면이 «서 있는 종이 한 장»으로 보이려면 이 색이다(48-9). */
  PAPER_HEX: '#f5f3ee',
  /** 해칭 선의 불투명도 — 면 채움(0.22)보다 약간 짙되 2H 잉크(0.5)보다 옅게:
   *  해칭은 «톤»이지 «선»이 아니다(지시: 선이 위에 얹힌다). */
  HATCH_ALPHA: 0.3,

  // ── 재료 칠(web2-46) ──────────────────────────────────────────────────────
  /** 마커 촉 폭(brush weight — px 대응은 brushmap 규약 그대로 배수==px로 두고 실측이
   *  잰다). 실물 코픽 브로드 촉 6~8mm를 이 앱의 선 폭 대역(잉크 1.5px = 0.35mm)의
   *  비례로 옮기면 25~34px인데 그 폭은 화면을 삼킨다 — 제도 마커 표현의 통상 획 폭
   *  (면을 몇 획으로 덮는 정도)을 취해 **10px**로 시작한다. 눈 판정은 실기기(46 ⚑). */
  MARKER_W_PX: 10,
  /** 색연필 심 폭 — 연필 대역(1.1~1.8px)보다 굵고 마커보다 훨씬 가늘게. */
  CP_W_PX: 2.5,
  /** **칠의 크기 트레이**(web2-48 48-2) — 붓·마커·색연필의 자국 폭 px.
   *  ⛔ **슬라이더가 아니다**(R1 — 실물에 없는 연속 조작을 만들지 마라): 펜 촉(30-2)·
   *  지우개 크기(34-3)와 **같은 크기 트레이**이고 각 줄에 그 굵기의 자국을 1:1로 그린다.
   *  ⚠ **새 숫자를 안 지었다**(#12 — 동작점 하나이고 스윕이 없다): 사다리는 이미 있는
   *  두 상수에 **못 박혀 있다** — `CP_W_PX`(2.5)가 첫 칸이고 `MARKER_W_PX`(10)가 가운데
   *  칸이며 사이와 밖은 **비 2의 등비**다. 그래서 46이 잰 마커·색연필 값이 그대로 이
   *  트레이의 두 칸이 되고, 종전 획의 굵기가 한 톨도 안 바뀐다(무회귀).
   *  되돌릴 조건: 실기기에서 가장 굵은 칸으로 면 하나를 몇 획에 못 덮는다. */
  PAINT_W_PX: [2.5, 5, 10, 20, 40] as readonly number[],
  // ── 숫자와 표시(web2-47) ──────────────────────────────────────────────────
  /** 스케일바 닻(화면 좌하 여백 px)·목표 폭 — 지도 앱 유동 축척바 관행(~100px 대역).
   *  자리는 좌하(우하 = 빌드 식별자 · 상단 = 뷰 큐브 대역 140px — 44의 그 규칙). */
  SCALEBAR_X_PX: 24,
  SCALEBAR_Y_PX: 34,
  SCALEBAR_TARGET_PX: 100,

  /** 마커 퇴적 간격(brush spacing) — 내장 0.03은 한 획 안 퇴적만으로 포화해 「겹치면
   *  진해진다」가 죽는다(46-0 스윕 실측: 1·2·3겹 안료 3376·3377·3377). 0.2는 1→2겹
   *  +82% · 2→3겹 +9% · 피복은 전 구간 만재(NOTES 46-0 표 · mats46_e2e가 매 실행 재확인). */
  MARKER_SPACING: 0.2,

  // ── 재료 표현(web2-49) — **벽돌은 그림이 아니라 숫자다** ──────────────────
  // 전부 실치수 mm. 생성기는 이 숫자대로 긋고 축척(lift.mmPerUnit)이 세계 좌표를 정한다.
  // 값의 근거는 시장 표준 치수다(임계가 아니라 «물건의 크기» — 스윕 대상이 아니다):
  /** 표준형 벽돌 190×57 · 줄눈 10 → 모듈 200×67(막힌줄눈 — 켜마다 반절 어긋남) */
  REP_BRICK_MODULE_W_MM: 200,
  REP_BRICK_COURSE_MM: 67,
  /** 석재(켜쌓기 러블) — 켜 높이·수직 줄눈 간격의 시드 대역 [min, max] */
  REP_STONE_COURSE_MM: [250, 400] as readonly number[],
  REP_STONE_JOINT_MM: [300, 650] as readonly number[],
  /** 목재 판재(사이딩) 폭 — 결선은 판 안에 시드로 */
  REP_WOOD_PLANK_MM: 140,
  /** 타일 300×300 격자 */
  REP_TILE_MM: 300,
  /** 기와 — 잇단(세로) 345 · 너비(가로) 300, 켜마다 반절 어긋남 */
  REP_ROOF_COURSE_MM: 345,
  REP_ROOF_TILE_W_MM: 300,
  /** 콘크리트 거푸집 판(유로폼) 600×1200 격자 */
  REP_CONC_PANEL_W_MM: 600,
  REP_CONC_PANEL_H_MM: 1200,
  /** **밀도 하한(px)** — 한 무늬 계열의 투영 간격(면 중심에서)이 이 아래면 그 계열을
   *  그 면에서 숨긴다. 실물 제도의 「작은 축척에서 재료 표현 생략」 관례의 코드판이다.
   *  4px = 선 굵기(1~1.5px)의 ~3배 — 그 아래는 선이 «간격»이 아니라 «면»으로 뭉친다.
   *  ⚠ 눈이 고른 값(#12 — 동작점 하나)이고 rep49 하네스가 경계 양쪽을 잰다.
   *  되돌릴 조건: 실기기에서 먼 벽의 벽돌이 지저분하게 뭉치거나, 반대로 너무 일찍 사라진다. */
  REP_MIN_PX: 4,
  /** **무늬의 프레임 예산(ms)** — 같은 장면 켬/끔의 프레임 중앙값 «차»(#82)가 이 안이어야
   *  한다. 8 = 60Hz 한 프레임(16.7ms)의 절반 — 무늬는 부가 겹이라 제품 몫의 절반을 넘으면
   *  그리기가 먼저 죽는다. ⚠ dpr1처럼 중앙값이 vsync 바닥(16.7ms)에 붙은 실행에서는 차의
   *  해상도가 눈금(0.1ms)뿐이다 — 뜻이 있는 판은 계산 병목 실행(dpr2 대역)이다(리뷰어 [3]). */
  REP_FRAME_BUDGET_MS: 8,
  /** 줌 실치수 유지의 허용 편차 — |정규화 비 − 1| < 0.2. 판별 대상(화면 고정)은 정규화가
   *  1/z(줌 1.45배에서 0.69 — 편차 0.31)라 문 밖이고, 측정 잡음(열 중심의 부픽셀 양자화 ·
   *  최대 간격 순서통계량)은 실측 2~4% 대역이라 문 안이다 — 그 사이의 값(2차 [11]). */
  REP_ZOOM_RETENTION_TOL: 0.2,
  /** 재료 표현 선의 불투명도 — 주선(켜·격자)과 부선(수직 줄눈·결). 해칭(0.3)과 같은
   *  «톤» 층위이되 표현은 읽혀야 하므로 주선을 약간 위로. 2H 잉크(0.5)보다는 아래. */
  REP_ALPHA_MAJOR: 0.42,
  REP_ALPHA_MINOR: 0.32,

  // ── 면 텍스처(web2-50) — 칠의 파생 표시층 ──────────────────────────────────
  /** 텍스처 긴 변의 하한/상한(px). 상한의 근거는 측정이다(⚑ — `paint50_web2.json`의
   *  `tex_budget`: 1024² RGBA ≈ 4MB/장 — 칠한 면 20·양쪽이어도 실장면 대역에서 예산 안.
   *  2048은 장당 16MB라 스무 면에서 수백 MB로 뛴다). 화면에서 차지하는 px를 2^n으로
   *  올림 양자화해 이 사이에 가둔다 — 단계가 같으면 줌이 움직여도 안 굽는다. */
  FACETEX_MIN_PX: 64,
  FACETEX_MAX_PX: 1024,
  /** 마커 칠의 한 획 알파 — canvas 'multiply'와 짝이다(겹치면 진해진다 — 46의 계약을
   *  텍스처 기제로 잇는다. 0.55: 1겹이 읽히되 3겹이 아직 포화 전 — mats46 팔이 단조를 잰다). */
  PAINT_MARKER_ALPHA: 0.55,
  /** 색연필 칠의 한 획 알파 — 완전히 덮이지 않는다(46의 관측 그대로). */
  PAINT_CP_ALPHA: 0.7,
  /** 굵기(w)가 없는 칠 획의 대체 폭 — **세계 단위**(w의 단위가 50에서 px → 세계로 바뀌었다.
   *  옛 px 값은 파서가 획째 버리므로 이 대체는 새 형식의 «w 없음»에만 닿는다). */
  PAINT_W_FALLBACK_UNITS: 0.02,
  /** 곱 게이트의 밝기 문턱(1차 리뷰어 [4] — 임계 등재): «밝아졌다»로 세는 최소 증가량
   *  (255 눈금). 3 = AA·반올림 잡음 위 · 흰 뜸의 실측 대역(수십) 아래. */
  PAINT50_LUM_TOL: 3,
  /** 원근 폭 게이트의 허용 편차 — |실측 비 ÷ 기대 비 − 1| < 이 값. 기대 비는 픽스처에서
   *  유도한다(#88 — 벽의 투영 높이 비. 손으로 옮겨 적지 않는다). 0.12: 실측 잔차
   *  대역(dpr1 +2.8% · dpr2 +5.4% — 띠 두께 자의 부픽셀 양자화·±2열 평균의 몫) 밖이고
   *  판별 대상(화면 고정 — 비 1.0 = 기대에서 −19%)은 크게 밖이다(분석 경계). */
  PAINT50_FORESHORTEN_TOL: 0.12,
  /** 곱 게이트 ②(아래가 비친다)의 문 — 칠 띠 «안» 무늬 대비 픽셀의 최소 수(2차 [3] —
   *  맨 숫자 등재). 판정 지표는 «띠 75분위 밝기보다 12 이상 어두운 픽셀 수»(대비가
   *  실제로 남는가 — over 반증에서 0 대역으로 죽는 것이 판별력이다 · 2차 [2]). */
  PAINT50_PATTERN_MIN_PX: 30,
  /** 곱 게이트 ④(선이 산다)의 문 — 위 모서리 상자의 선 잉크(#brushc) 최소 픽셀(2차 [3]). */
  PAINT50_LINE_INK_MIN_PX: 20,

  // ── 자국의 질(web2-51) — 브러시 넷의 성질 상수. 전부 «절차 생성»의 손잡이다 ──────
  /** 압력 → **농도** 직선 프로필의 기울기·바닥: density = min(1, 바닥 + 기울기 × press).
   *  지시 문면(26-6 재인용) «농도의 기울기가 굵기보다 가파르다»가 두 SLOPE의 부등식이고,
   *  단위 팔이 그 부등식을 함수 그 자체로 잰다(#54 — 팔과 제품이 같은 함수). */
  PAINT51_DENSITY_FLOOR: 0.25,
  PAINT51_DENSITY_SLOPE: 0.9,
  /** 압력 → **굵기** 배수 프로필: widthFactor = 바닥 + 기울기 × press (농도보다 완만). */
  PAINT51_WIDTH_FLOOR: 0.7,
  PAINT51_WIDTH_SLOPE: 0.45,
  /** 연필 결의 격자 크기 — **획 굵기의 배수**(세계 단위 절대 눈금이 없다 — 축척 미정에도
   *  칠하므로. 굵기 상대라 어느 축척에서도 같은 «자국 대비 결»이 난다). */
  PAINT51_PENCIL_GRAIN_K: 0.45,
  /** 연필 결의 알파 대역 — grain 해시(0..1)가 [바닥, 1]로 펴진다(불연속의 깊이). */
  PAINT51_PENCIL_GRAIN_FLOOR: 0.25,
  /** 색연필 — 결이 «굵고»(격자 배수 — 연필보다 크다) 색이 완전히 덮이지 않는다
   *  (grain이 문 아래인 칸은 피복 알파가 낮다 — 빈 알갱이). */
  PAINT51_CP_GRAIN_K: 1.1,
  PAINT51_CP_SKIP_TH: 0.42,
  PAINT51_CP_SKIP_ALPHA: 0.18,
  /** 마커 팁 — 획 양 끝의 덧찍음(곱 알파 · «획 경계가 살짝 남는다»). 길이는 굵기의 배수. */
  PAINT51_MARKER_TIP_ALPHA: 0.35,
  PAINT51_MARKER_TIP_LEN_K: 0.9,
  /** 붓 — 빗살 수·끝 갈라짐 시작(길이 비율)·발산 폭(굵기 배수)·흐름 저주파 마디 수. */
  PAINT51_BRUSH_BRISTLES: 4,
  PAINT51_BRUSH_SPLIT_T: 0.85,
  PAINT51_BRUSH_SPLIT_K: 0.8,
  PAINT51_BRUSH_FLOW_NODES: 5,
  /** 51 게이트 — dpr 1↔3 굵기 비의 허용(#26-2 계열 · 1.0 ± 이 값). */
  PAINT51_DPR_W_TOL: 0.15,
  /** 51 게이트 — 트레이 견본 굵기 ↔ 그은 자국 굵기의 허용(상대 편차). */
  PAINT51_SWATCH_W_TOL: 0.25,
  /** 52 게이트 — 축척 통과: 치수 2배 재매김에서 켜 화면 간격 비 |비−0.5|의 문.
   *  유도(#88): 간격 자(어두운 가로선 뭉치 중심)의 눈금이 켜당 ±1px 대역이고 재매김 후
   *  간격이 ~5px라 비의 눈금이 ±0.2 대역 — 그 눈금이 문이다(더 조이면 자가 못 잰다). */
  // ── web2-55 두께 — 테두리 «미터» 픽셀 게이트(등재 D-C4 — 리뷰어 1차 [9]) ──────────
  /** t 200→300에서 띠 칠 자국 «s축 대각» 성장의 상한. 유도(#88): 사영 이동·AA 요동의
   *  실측 대역(dpr1 1.032 · dpr2 1.008)의 4배 여유 아래 · 반증 신호(1.45)의 대역 밖. */
  THICK55_RATIO_MAX: 1.15,
  /** s축 1.5배 반증이 픽셀 자에 잡히는 하한 — 이론값 1.5의 α(절단·AA 손실 ~15%) 아래 */
  THICK55_FALSIFY_MIN: 1.25,
  /** «실재/변화»의 최소 픽셀 — AA 부스러기(수 px) 위 · 실측 신호(수백~수천)의 대역 아래 */
  THICK55_PRESENCE_PX: 20,
  THICK55_CHANGED_PX_MIN: 50,
  /** 위험 축(u=두께 방향)의 정본 문 둘(단위 ⑤ — 세계 단위 · 2차 [N3] 등재):
   *  평면 안 성분 ≤ EPS(부동소수 0) · 정규화 반증 이동 ≥ FALSIFY(세계 단위 가시 하한) */
  THICK55_U_PLANE_EPS: 1e-9,
  THICK55_U_FALSIFY_MIN: 1e-6,
  // ── web2-56 접합 — 제품 셋 + 게이트 문 셋(등재 D-C4) ─────────────────────────
  /** 치유 반경 mm(지시 1 — VisualARQ의 첫 숫자) — 접합 모서리가 두 중심면의 교선에서
   *  이 안이면 붙는다(밖이면 안 붙는다 #71). **임의 초판**(pri의 규약 — 사람 변경 전제):
   *  손 획 코너는 대개 획을 공유해 간격 0이고, 안 닿는 코너의 실측 대역이 아직 없어
   *  흔한 벽 두께의 반(100mm) 급으로 두었다. */
  JOIN56_CLEANUP_MM: 100,
  /** 연장 상한 mm(지시 1 — 두 번째 숫자) — 접합이 표면을 이보다 멀리 못 옮긴다.
   *  임의 초판: 두꺼운 벽(400mm)의 관통 연장(≤ t + 치유)이 넉넉히 드는 값. */
  JOIN56_MAXEXT_MM: 600,
  /** 평행 문 — 지시 허용오차 문면(dot 0.9994 ≈ 2°) 그대로. 이 이상 나란하면 접합 후보가
   *  아니다(같은 평면·이어진 벽은 코너가 아니다). */
  JOIN56_PAR_DOT: 0.9994,
  /** 중심 게이트 — 수리 후 코너 표본 사각(15% 안으로 줄여 AA 가장자리를 뺀 것) 안의
   *  종이색 픽셀 수 상한 = **0**(계단 없음). 줄였기에 0이 성립 가능한 문이다. */
  JOIN56_STAIR_PX_MAX: 0,
  JOIN56_PROBE_SHRINK: 0.15,
  /** 이동 결과가 목표 평면 «위»인가의 잔차 문(세계 단위 · 단위 팔) — 이동식이 그 평면
   *  방정식을 푸는 구성이라 부동소수 눈금(1e-12 대역)의 큰 여유 위가 0의 뜻이다. */
  JOIN56_PLANE_EPS: 1e-9,
  MATS52_SCALE_TOL: 0.2,
  /** 52 두 겹 대비의 문 — 무늬 간격이 줌(~2배)을 따라 «늘었다»의 하한. 유도(#88):
   *  간격 비 눈금 ±0.1의 3배 위 · 줌 하한(1.9) 아래(2차 [9]가 무등록 스펙 상수를 잡았다). */
  MATS52_TWO_LAYER_MIN: 1.3,

  /** 51 게이트 — 압력→농도 술어의 여유: density_ratio > 1 + 이 값(반증은 같은 술어 반전 —
   *  1차 [3]: 여유 없는 판은 평탄화에서도 참이라 반증이 못 뒤집었다).
   *  **유도**(#88 · 2차 [3]): 자는 «픽셀당 안료 비»(픽셀당이라 위치 축척이 안 실린다).
   *  0.1 = 출하 비(실측 1.66 대역)의 차 0.66의 1/6 아래 · 평탄화 잔차(실측 |1.02−1|=0.02
   *  대역)의 5배 위인 사이값. ⚠ 초판의 «농도 비 > 굵기 비» 교차-위치 게이트는 무효한
   *  자로 판명(원근 축척 ~12% 차 + 결 가시성의 농도 누설 — brush51 pressure.def 실측):
   *  기울기 부등식은 단위 팔(test/brush51.test.ts)이 재고, e2e 폭 술어는 위치 자기 기준
   *  (같은 자리의 평탄화 굵기 대조)이라 이 여유를 안 쓴다. */
  PAINT51_GATE_MARGIN: 0.1,
  /** 51 게이트 — 마커 팁/몸통 안료 비의 문 · 붓 갈라짐(끝/몸통 행 수) 문 · 붓 흐름
   *  변동 계수 하한 · 색연필 «덜 덮임» 여유 · 결 줌 비의 계수(바닥 1.4와 max). */
  PAINT51_TIP_MIN_RATIO: 1.05,
  PAINT51_SPLIT_MIN_RATIO: 1.1,
  PAINT51_FLOW_CV_MIN: 0.05,
  PAINT51_CP_COVER_MARGIN: 0.03,
  PAINT51_GRAIN_ZOOM_K: 0.45,
  PAINT51_GRAIN_ZOOM_FLOOR: 1.4,
} as const

/** **시점 스냅 임계의 상한(rad)** — 실제 임계는 `level.ts`의
 *  `snapAngle(f, W) = min(atan(f / (VP_FAR_W·W)), POSE_SNAP_RAD)` 다(web2-08 지시 3).
 *  정렬은 그 안으로 **의도적으로 가져왔을 때만** 걸리고, 밖이면 기울인 채 둔다 —
 *  머무는 상태다. 판정은 `foldTarget`/`pitchSnaps` 한 자리다. 요(2점→1점 축 붙기)도
 *  같은 임계다 — 같은 물음(「정렬과 화면에서 구별되는가」)이라 한 값이다.
 *
 *  **두 물음의 AND다**(#61의 처방 — 임계를 옮기지 않고 빠진 축을 더한다):
 *  ① `atan(f/6W)` — **그 문서의 f**로, 그 기울기의 소실점(피치면 세로 소실점, 요면
 *    화면과 거의 평행해진 가로축의 소실점)이 작도 대역(`VP_FAR_W`=6W) 밖에 있는가.
 *    밖이면 이 앱 자신이 «무한원과 구별 못 한다»로 읽는 대역이다(`classifyNext`의
 *    H 갈래와 같은 물음 — 그쪽 주석의 «기본 f일 때» 한정이 여기서는 f를 실제로
 *    넣으므로 안 걸린다). 지시 문면 「수직선이 화면상 수직에 가까워질 정도」 그대로.
 *    넓은 화각(f=0.323W — 표준 픽스처의 값)에서는 3.08°로 좁아진다 — 거기서 8°
 *    기울기의 세로 소실점은 2.2W라 작도 대역 안(화면에서 모인다)인데, 그것을 접으면
 *    보고된 증상의 재발이다(1차 리뷰어 [1]이 잡았다 — 초판은 기본 f의 고정각 하나였다).
 *    ⚠ 이 6W는 **주점 기준**이고 `classifyNext`의 6W는 **획 시작점 기준**이다 — 같은
 *    대역 «크기»의 차용이지 같은 양의 재사용이 아니다(2차 리뷰어 [N10] · AS-C21).
 *  ② 상한 `POSE_SNAP_RAD = atan(0.87/6) ≈ 8.25°` — 기본 f에서의 ①값. 망원(f≈2.74W —
 *    표준 망원 픽스처)에서 ①이 24.5°까지 벌어지는 것을 자른다: 그 대역의 기울임이
 *    접히면 「무조건 2점으로 찾아간다」가 기울일 일이 가장 많은 구도에서 재발한다.
 *    지시의 둘째 문면 「즉 내려다보거나 올려다보지 않는 정도로」가 이 자세 쪽 물음이다.
 *    ⚠ «몇 도부터 내려다보기인가»의 실측은 없다(AS-C21 — 근거 없는 각도 문구를 안 쓴다).
 *
 *  참고 환산: 궤도 1px = `ORBIT_RAD_PER_PX`(0.005 rad) = 0.286°이므로 상한 8.25°는
 *  29px, 넓은 화각의 3.08°는 11px이다. ⚠ 실기기·실사용 표본 없이 고른 값이다
 *  (assumptions AS-C21). 되돌릴 조건: 임계 바로 밖(예: 상한 근처 기울임)에 «두려는»
 *  사용이 관측되면 좁히고, 정렬로 «못 가져가겠다»는 관측이 나오면 상한 쪽을 넓힌다. */
export const POSE_SNAP_RAD = Math.atan(C.DEFAULT_F_RATIO / C.VP_FAR_W)

/** **뷰 큐브 90° 전환의 보간 시간(ms)** — 지시는 「0.3초 정도. 즉시 튀면 어디로 갔는지
 *  잃는다」이고 그것이 `FOLD_ANIM_MS`와 **같은 물음**이다(시점이 스스로 움직일 때 사람이
 *  따라갈 수 있는 시간). 그래서 **숫자를 새로 안 짓고** 그 값을 가리킨다(#54) —
 *  둘을 갈라야 할 관측이 나오면 그때 값을 나눈다. 실제로 도는 자리도 하나다:
 *  `autolevel.ts`의 `anim` 슬롯이 접기와 전환을 **같이** 들고 있어 둘이 못 겹친다. */
export const TURN_ANIM_MS = C.FOLD_ANIM_MS

/** **정착 전이의 길이(ms)** — web2-37 2번. 대기(논포토 블루)가 확정(흑연)으로 바뀌는
 *  그 순간에만 도는 짧은 전이다: 「움직임은 눈에 띄고 끝나면 사라지므로 평소에는
 *  조용하고 일어날 때만 보인다」(지시 문면).
 *
 *  **숫자를 새로 안 짓는다**(#54 · `TURN_ANIM_MS`가 낸 선례 그대로): 이것은
 *  `FOLD_ANIM_MS`와 **같은 물음**이다 — 앱이 스스로 화면을 바꿀 때 사람이 따라갈 수
 *  있는 시간(그쪽 주석의 「갑자기 바뀌면 놀란다 · 시점 전환의 관용 대역 250~400 ms」).
 *  둘을 갈라야 할 관측이 나오면 그때 값을 나눈다.
 *  ⚠ 이 값은 **표현만** 정한다 — 승격 판정·좌표·저장은 이 창과 무관하다(전이 중에도
 *  그 획은 이미 확정이다). 되돌릴 조건: 실기기에서 「바뀐 걸 못 봤다」(올린다) 또는
 *  「깜빡여서 거슬린다」(내린다). */
export const SETTLE_ANIM_MS = C.FOLD_ANIM_MS

/** **겹을 깔고 치우는 동작의 길이(ms)** — web2-40 2번. 지시는 「길이는 짧게. 0.25~0.35초」다.
 *
 *  **숫자를 새로 안 짓는다**(#54 · `TURN_ANIM_MS`·`SETTLE_ANIM_MS`가 낸 선례 그대로):
 *  이것은 `FOLD_ANIM_MS`(300)와 **같은 물음**이다 — 앱이 스스로 화면을 바꿀 때 사람이
 *  따라갈 수 있는 시간(그쪽 주석의 「갑자기 바뀌면 놀란다 · 관용 대역 250~400 ms」).
 *  그리고 300은 지시가 준 대역 0.25~0.35초의 **한가운데**다 — 새 값을 지어도 같은 자리에
 *  떨어진다. 둘을 갈라야 할 관측이 나오면 그때 값을 나눈다.
 *  ⚠ 이 값은 **표현만** 정한다 — 겹은 창이 열리는 그 순간 이미 문서에 있고(깔기) 또는
 *  이미 없다(치우기). 동작이 입력을 막지 않는 근거가 그것이다(40-2 게이트 ①). */
export const LAY_SLIDE_MS = C.FOLD_ANIM_MS

/** **축 정렬 게이트의 허용 각(도)** — web2-31 1번의 「각도 오차 < 0.01°」 그대로다.
 *  90° 회전이 정수 축 벡터의 외적으로만 이뤄지므로 실측은 이보다 다섯 자릿수 아래이고
 *  (0.000000°), 이 값은 **반증 판의 값(수십 도)과 가르는 문**으로만 쓰인다. */
export const CUBE_ALIGN_MAX_DEG = 0.01

/** **궤도 한 픽셀이 도는 각(rad)** — 데스크톱·터치가 같은 값을 쓴다.
 *  ⚠ 정의가 여기로 왔다(web2-42): `state.ts`가 그대로 다시 내보내므로 부르는 자리는
 *  안 바뀌고, **core가 그 값을 읽을 수 있게** 됐다(아래 `VIEW_NAME_ALIGN_RAD`). */
export const ORBIT_RAD_PER_PX = 0.005

/** **이름이 그 면으로 읽히는 허용 각(rad)** — web2-42 1번의 「축에서 벗어나면 축측」.
 *
 *  **숫자를 새로 안 짓는다**(#88 · #54): 궤도 **반 픽셀**이다. 그래서
 *  ① 손으로 한 픽셀이라도 돌리면 이름이 「축측」으로 바뀌고(지시 게이트)
 *  ② 뷰 큐브가 앉힌 자세는 정수 축 벡터의 외적이라 어긋남이 1e-8° 대역이므로 안 바뀐다.
 *  ⚠ `CUBE_ALIGN_MAX_DEG`(0.01°)와 **다른 물음**이다 — 그쪽은 팔이 「제품 판 ↔ 반증 판」을
 *  가르는 문이고 이것은 **화면에 무엇을 쓰는가**의 문이다(0.143° — 그 문보다 열네 배 넓다). */
export const VIEW_NAME_ALIGN_RAD = ORBIT_RAD_PER_PX / 2

/** **가상 교차 후보의 «애매함» 문**(web2-41 1번) — 후보 3D 선들이 서로 벌어진 폭을
 *  **승격 기하의 bbox 대각**으로 나눈 비. 이 위면 애매하다고 보고 그 획은 **대기한다**.
 *
 *  ⚠⚠ **후보의 «개수»가 아니라 «결과의 벌어짐»이다**(지시문이 못 박았다). 긴 선은
 *  무엇이든 여러 개를 지나가므로 개수로 재면 거의 전부가 애매해진다. 재야 할 것은
 *  후보들이 만드는 3D 선들이 서로 얼마나 떨어지는가이고, 둘이 사실상 같은 자리에
 *  놓이면 어느 쪽을 골라도 무해하다.
 *
 *  **숫자를 새로 안 짓는다**(#54 · `SETTLE_ANIM_MS`·`TURN_ANIM_MS`가 낸 선례):
 *  이것은 `INTERSECT_GAP_RATIO`와 **같은 물음**이다 — 「이 3D 간격이 «같은 자리»로
 *  읽히는가」. 그쪽은 두 선이 만났다고 볼 간격이고 여기는 두 답이 같다고 볼 간격이다.
 *  분모도 같다(`geomSizeOf` — bbox 대각). 둘을 갈라야 할 관측이 나오면 그때 값을 나눈다.
 *
 *  **여유**(#82 — 실측 원장 `xint41_web2.json`의 `spread_bands`):
 *
 *      성긴 장면(발판)                벌어짐 0.000000        ← 후보가 여럿이어도 답이 하나
 *      붐비는 장면 ①(37 픽스처)       0.000449 ~ 0.008804   ← 세로 여덟이 한 평면이라 무해
 *      붐비는 장면 ②(깊이가 갈림)     0.281600 ~ 0.579600   ← 여기가 진짜 애매함
 *
 *  문(0.01)과 ②의 아래 끝 사이가 **28배**다 — 「잘못 놓인 선 0」 게이트는 문턱을
 *  ×0.5·×2로 흔들어도 그대로 선다. 움직이는 것은 ①의 자립률뿐이고 그 방향은
 *  「대기로 남는다」라 값이 싸다(지시문이 그렇게 값을 매겼다).
 *  ⚠ 손잡이는 `lift.setXintAmbigRatio`(팔의 반증 손잡이 — 제품 경로가 아니다). */
export const XINT_AMBIG_RATIO = C.INTERSECT_GAP_RATIO

/** **꾹 누르기 시간을 사람이 고치는 대역**(web2-39 1번 — 사용자 요청).
 *  **숫자를 새로 안 짓는다**(#54 · `XINT_AMBIG_RATIO`가 낸 선례): 「사람이 손에 맞게
 *  고치는 머무름 시간의 대역」은 옐로 머무름(`HOLD_MS_MIN`~`HOLD_MS_MAX`)과 **같은
 *  물음**이고 선례도 같다(프로크리에이트 QuickShape가 사용자에게 여는 범위 — A-3).
 *  둘을 갈라야 할 관측이 나오면 그때 값을 나눈다. */
export const WRITE_HOLD_MS_MIN = C.HOLD_MS_MIN
export const WRITE_HOLD_MS_MAX = C.HOLD_MS_MAX
