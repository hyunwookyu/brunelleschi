// web2-12 e2e 임계의 단일 출처(D-C4 — 흩어지면 STALE이 안 잡힌다).
// 값의 근거는 각 스펙 주석·AS-C37·AS-C38이고, 실측은 stage0/out의 해당 원장이 정본(#47).
// draftgate가 이 객체를 원장의 thresholds 블록으로 그대로 내보낸다 — 원장과 코드가 안 갈린다.

/** 합성 화면 diff의 채널 문턱 — AA 가장자리(차 10~45)는 걸리고 압축·반올림 요동(±2)은 안 걸린다 */
export const PIXEL_DIFF_CH = 8

/** 재료 가시성(materials.spec) — 상자 20×60 CSS px · «선이 보인다»의 하한(AS-C37) */
export const VISIBLE_FLOOR = 20

/** 뗌 게이트(draftgate.spec) — 상자 20×50 CSS px (AS-C38) */
export const GATE = {
  /** 절대 상한: release_diff < stroke_px × 이 값. 새 경로 실측 대역 위 · 옛 경로 실측
   *  최저(classic dpr1 — 원장 release.classic_hb) 아래에 둔다. 실측이 움직이면 여기부터 다시 잰다. */
  ABS: 0.15,
  /** 판별 상대: release_diff < classic(옛 경로) diff × 이 값 — 옛 경로로 되돌리면 잡힌다 */
  REL: 0.6,
  /** 연장 카나리아: grown_front_diff < 상자 «전체 픽셀»(획 아님) × 이 값 — 상한 회귀 감시용.
   *  시드 판별이 아니다(그 판별은 release·강제 재그리기 팔 — AS-C38 관측 ②). */
  GROW_CANARY: 0.5,
}

/** 감쇠 동결 팔(waitfade.spec web2-14 3번)의 동작점 둘(AS-C38 선례 — 팔 임계도 단일 출처) */
export const FREEZE = {
  /** 표본 간 «변화» 데드밴드 — 기준 alphaSum 대비 비율(AA·스크롤 요동 흡수, 감쇠 계단은 걸림) */
  DEADBAND: 0.02,
  /** «자기 시점 진하기 그대로»의 하한 — 기준 alphaSum 대비 */
  HOLD_FLOOR: 0.9,
}

/** **카메라 도형 훑기의 문**(papericon31.spec · web2-31 4번) — DOM svg를 잉크 격자(16×16 ·
 *  비 보존)로 구워 Phosphor camera 계열 견본과 견준 **IoU**의 상한. 이 값 위면 「카메라
 *  도형이 화면에 있다」로 본다.
 *
 *  근거는 **실측 격차**다(원장 `papericon31_web2_dpr1.json`): 옛 아이콘을 되돌린 판의
 *  `#paper-add`가 IoU 1.0에 붙고(같은 글리프), 성한 판의 아이콘 전부가 그보다 한참 아래다.
 *  값을 둘 사이의 넓은 골에 둔다 — 좁히면 우연히 닮은 아이콘이 걸리고, 넓히면
 *  굵기만 바꾼 이식을 놓친다. 움직이면 그 원장의 `iou_top5`부터 다시 읽는다. */
export const CAMERA_IOU = 0.7

/** **카메라 도형 훑기의 둘째 문**(papericon31.spec · web2-31 4번 · 1차 리뷰어 [1][2]) —
 *  **견본 전부**(채운 판 `fill`·`duotone` 포함)에 대한 IoU 상한.
 *
 *  왜 문이 둘인가: 채운 카메라는 «가로로 긴 덩어리»라 이 앱의 **실물 도구 그림**과 가깝다
 *  (실측 최대 0.693 — 연필통 줄 ↔ duotone/camera-plus). 그러므로 이 문은 위쪽에 설 수밖에
 *  없고, 그만큼 분해능이 낮다. 낮은 쪽은 `CAMERA_IOU`(윤곽 견본 · 화면 최대 0.5787)가 든다.
 *  ⚠ **이 문을 없애면 «채운 카메라를 문자열만 바꿔 심은 칸»이 빈다**(반증 ㉠3이 그 칸이다).
 *  움직이면 원장 `papericon31_web2_dpr1.json`의 `iou_all_top5`부터 다시 읽는다. */
export const CAMERA_IOU_ALL = 0.75

/** **대기 ↔ 확정의 색상 문**(waitink37.spec · web2-37 2번) — 둘 다 «자»의 **비**다.
 *  자 = `diag.waitInk().ink`(논포토 블루)의 색상 이동(파랑−빨강). 팔이 그 자를 앱에서
 *  읽으므로 **색을 바꾸면 문이 따라 움직인다**(PITFALLS #88 — 팔이 든 상수가 결함이다).
 *
 *  근거는 **실측 격차**다(원장 `waitink37_web2.json`의 `on`·`falsification`):
 *  대기 24.6~32.2 ↔ 확정 0.1~1.0 ↔ 위약(`all`)의 확정 26.1. 사이가 **스무 배 넘게**
 *  비어 있으므로 문 둘을 그 골 안에 나란히 둔다(WAIT_MIN > CONF_MAX가 유지된다).
 *  ⚠ **자 그대로(73)가 안 나온다** — 종이색 혼합(HB 알파 0.75면 53)까지 셈해도 실측은
 *  그 절반쯤이다. 원인은 **브러시의 덮임**이고 대기·확정 양쪽에 똑같이 걸린다(평균 rgb
 *  실측: 대기 213.7/233.8/238.3 · 확정 194.7/194.9/194.5 — 종이 245 기준 각각 공칭색
 *  쪽으로 0.51·0.43만 갔다). 그러므로 이 압축은 «청색만의 손실»이 아니라 겹의 성질이다.
 *  문은 «예측한 값»이 아니라 **실측 골**에 걸어 두었다(D-4). 방향(획 각도·파선 덮임)에
 *  따라 24.6 ↔ 32.2로 흔들리므로 하한은 그 아래 끝의 절반 아래다. */
export const WAIT_HUE = {
  /** 대기 획의 평균 색상 이동 **하한** — 자의 이 배(자 73에서 10.95). 실측 대기값의
   *  절반 아래다(획 방향·파선 덮임에 따라 24.6~32.2가 나왔다 — 두 배 폭). */
  WAIT_MIN: 0.15,
  /** 확정 획의 |평균 색상 이동| **상한** — 자의 이 배(3.65). 실측 확정값은 0.1~1.0이고,
   *  위약 판 `all`(확정에 청색을 칠한 판)이 26.1이라 이 문에 걸린다. */
  CONF_MAX: 0.05,
}

// ── web2-40 1번 — 겹의 결 주기(grain40.spec) ──────────────────────────────────
/** **주기가 줄었다의 문** — 지금 상관 길이 ÷ pre-40 상관 길이가 이 값 미만이어야 한다.
 *  갈린 축은 섬유 «길이» 하나이고 그 배수가 `OVERLAY_LEN_K` = 0.5인데, 상관 길이의 비는
 *  0.5가 **아니다**: 굵기(= r의 어깨)를 안 건드렸으므로 짧은 지연 쪽이 그대로 남고
 *  줄어드는 것은 꼬리뿐이다. 실측 비가 **0.743~0.827** 대역이다(세 dpr × 겹 둘 — 원장
 *  `paper_grain40_web2.json`).
 *  문은 그 대역 **바로 위**에 둔다 — 「분명히 줄었다」를 통과시키고 「거의 그대로」(0.96 —
 *  첫 판이 반감 지연으로 쟀을 때 나온 값)를 잡는 자리다. */
export const PERIOD_DROP_MAX = 0.9

/** **바닥**(web2-26 2번이 세운 것 — 지시문 ⚠) — dpr1에서 **반감 지연**(굵기가 정하는
 *  미세 구조)이 이 기기 픽셀 수 아래로 내려가면 결이 안티에일리어싱에 먹혀 「안 보인다」로
 *  되돌아간다. **값은 재서 골랐다**(짐작 ⛔): 사람이 「안 보인다」고 한 26-2 이전 규칙이
 *  dpr1에서 **옐로 0.817 · 트레이싱 0.626**이고 지금 판이 **1.767 · 1.352**다.
 *  1.2는 그 둘 사이 — 「안 보이던 자리」의 **1.5배**(옐로)·**1.9배**(트레이싱) 위이고
 *  지금 자리의 **0.68배**(옐로)·**0.89배**(트레이싱) 아래다.
 *  ⚠ 이 값은 «충분조건»이 아니다 — 진폭 쪽 바닥은 grain26(> 2.1계조 · dpr 비)이 진다. */
export const PERIOD_FLOOR_DEVICE_PX = 1.2

/** **화면에서도 갈렸는가**(#92) — (이웃차 ÷ sd)의 지금 ÷ pre-40. 1.0이면 화면이 안
 *  갈린 것이므로 문은 1보다 위여야 하고, 잡음(스크린샷 압축·AA) 위로 조금 띄운다.
 *  실측 **1.0915**(dpr1 — 원장 `screen_dpr1.gain_ratio`: 0.5286 ÷ 0.4843). */
export const NEIGHBOR_GAIN_MIN = 1.05

// ── web2-40 2번 — 깔고 치우는 동작(slide40.spec) ──────────────────────────────
/** **동작 중 프레임의 상한 배수** — 같은 장면·같은 국면의 «정지» 프레임 대비.
 *  절대 ms는 결론이 아니다(cost18·cost20이 세운 규율 — 이 컨테이너의 시간은 흔들린다).
 *  값의 근거: 동작이 프레임에 더하는 일은 **더하기 하나(dx)와 그라디언트 띠 한 장**뿐이라
 *  원리적으로 «몇 %»여야 한다. 1.5는 그 위에 잡음 몫을 크게 얹은 자리다 —
 *  「눈에 띄게 떨어지지 않는다」(지시 문면)를 통과시키고 «한 자릿수 배»를 잡는다.
 *  움직이면 원장 `lay_slide40_web2.json`의 `gate_3.ratio`부터 다시 읽는다. */
export const SLIDE_FRAME_MAX = 1.5
