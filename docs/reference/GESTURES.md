# GESTURES — 몸짓 표 (web2-71 · 2026-09-06)

> **세 앱 중 둘 이상에 있는 몸짓만** 넣는다 — 발명하지 않는다(지시 web2-71 · UX-FLOWS §E 합의 ⑤·⑥ · §A-6 · §C-6).
> 67의 가름(펜 = 칠 · 손가락 탭 = 고르기 · 긴 누름 = 주사기)은 **불변**이고 이 표의 몸짓은 그 위에 얹힌다.
> 몸짓은 «보기»·«되돌리기»·«카드»에만 닿는다 — 형태를 만드는 원칙(획만이 형태를 만든다 · 축 스냅 기본)은 안 건드린다.
> 문턱은 `web2/src/core/constants.ts`의 `GESTURE71_*` 하나(`Math.random` ⛔) · 판정은 `web2/src/app/gestures71.ts` 하나 · 값은 원장 `stage0/out/gesture71_web2_dpr{1,2}.json`.

| 장치 | 몸짓 | 뜻 | 출처(앱) | 문턱 값 | 부르는 «그 함수»(#54) |
|---|---|---|---|---|---|
| 펜 | 그린다 · 칠한다 | 획(선 도구는 축 스냅·작도 원칙 · 칠은 톤) | 셋 다 | — | 67 가름 ① |
| 손가락 1 · 탭 | 면 고르기(칠 도구) | 67 | Feather·Procreate | 이동 ≤ 12px(PAINT67_FINGER_TAP_MAX_PX) | 67 가름 ② |
| 손가락 1 · 긴 누름 | 주사기(짚은 칠 획의 속성을 싣는다) | 67 | Procreate | WRITE_HOLD_MS 450 | 67 가름 ③ |
| 손가락 1 · 끌기 | 돌기(또는 이동 — fingerPans) | web2-26 | Feather | — | orbit/pan |
| 손가락 2 · 끌기 | 이동/확대(회전 무변) | web2-08 | 셋 다 | — | pan/zoom |
| **손가락 2 · 두드림** | **되돌리기 1회** | **71 §1** | Procreate(4:44) · Feather(↶와 같은 뜻) | 동시 80ms · 이동 ≤ 8px · ≤ 250ms | `undoOrExplain()` = btn-undo |
| **손가락 3 · 두드림** | **다시하기 1회** | **71 §1** | Procreate(5:11) | 같다 | `redo(app)` = btn-redo |
| **손가락 2 · 누르고 있기** | **되돌리기 반복**(250ms마다 · 떼면 멈춤) | **71 §1** | Procreate(「캔버스에 손가락 두 개를 대고 있으면 빠르게」) | 400ms 넘게 · 안 움직임 | `undoOrExplain()` |
| **손가락 3 · 두 번 두드림** | **격자·축 표시 토글**(첫 두드림의 다시하기를 되물린다 — 순서: 다시하기 → 되돌리기 → 토글) | **71 §2** | Feather(§A-6 「3손가락 두 번 = 격자 토글」) · Procreate 세 손가락(다시하기) | 두 번 350ms 안 | `setGrid()` = chk-grid |
| **손가락 1 · 두 번 두드림** | **정사 스냅**(여섯 정사 축 ±15° 안에서만 · 밖이면 상태줄 한 줄 · 첫 탭의 면 고르기는 그대로) | **71 §3** | Feather(§A-6 「1손가락 두 번(정사 근처) = 정사 스냅」) | 두 번 350ms 안 · ±15° | `orthoSnap()` — 「정면」(54-3)이 쓰는 그 전환(parallelPose·glide) |
| 마우스 · 더블클릭 | 정사 스냅(데스크톱 관습) | 71 §3 | — | 같다 | `orthoSnap()` |
| 손가락/펜/마우스 · **도구 재누름** | **그 도구의 통 여닫기**(연필 → 연필통 · 펜 → 촉통 · 지우개 → 크기통 · 칠 → 브러시 목록 · 면 → 면 카드) | **71 §4** | Procreate(1:26 「다시 탭하면 브러시 라이브러리」) · Feather(재누름 = 다음 모드) | — | registerBox 통 여닫기(R7 한 번에 하나) |
| 펜 · **칠 획 끝 멈춤** | **시작점→끝점 직선 띠** · 계속 끌면 끝점이 따라온다 · 떼면 확정(두 점 획 — 저장 형식 무변) · 제도 연필·펜에는 없다 | **71 §5** | Feather(직선 모드 끝점 조절) · Procreate(QuickShape) | 떼지 않고 400ms · ≤ 3px | `draft.raw = [start, end]` → `onPaint` 같은 경로(59 미리보기 == 확정본) |
| 펜 | 정사 스냅 없음 · 두드림 없음 | 장치가 뜻을 든다(67) | — | — | — |

## 없는 것(발명하지 않았다)
- 두 손가락 «회전»(Procreate 집기 회전) — 우리 캔버스는 3D 궤도라 회전의 뜻이 다르다(web2-08 무변).
- 캔버스 탭 = 패널 닫기(Feather) — R7(바깥 누름에 접힌다)이 이미 그 뜻이다(34-0).
- 선 도구의 끝점 멈춤 직선화 — 축 스냅·작도 원칙이 그 자리(지시 ⛔).

## 34-0 대조표(화면 규칙)에 더한 줄
- **몸짓은 세 앱 중 둘 이상에 있는 것만 — 발명하지 않는다**(71). 67이 더한 몸짓 줄(R10) 아래.
