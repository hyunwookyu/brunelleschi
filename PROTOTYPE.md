# PROTOTYPE.md — 이 시제품이 **되는 것과 안 되는 것**

S-A~S-10 완주 시점의 상태. 여기 적은 수치는 전부 `stage0/out/`의 원장에서 왔고
`산출물.json@상수해시`로 인용한다. **모든 수치는 합성 획이다**(V-o·AS-1) — 실획 표본이
아직 없다(`sessions/`가 비어 있다). 그것이 이 문서에서 가장 중요한 한 줄이다.

---

## 0. 한 문장 판정

**첫 시점에서 그리는 데는 선다. 돌려서 그리는 데는 서지 않는다.**

| | 자동 배치 | 획당 추가 조작 | 〃 재시도 한 번 포함 | 손으로 붙인 것의 오류 | 조용히 틀린 자동 배치 |
|---|---|---|---|---|---|
| **첫 시점** | **0.846** | **0.45** | 0.63 | 0.38 | 0.057 |
| 돌린 시점 궤도 45° | 0.419 | 1.70 | **2.56** | 0.49 | **0.655** |
| 돌린 시점 궤도 75° | 0.112 | **2.55** | **3.22** | 0.25 | 0.30 |
| 돌린 시점 궤도 110° | 0.135 | **2.48** | **3.30** | 0.32 | 0.25 |

"획당 추가 조작"은 **재시도를 0으로 잡은 하한**이다. 손으로 붙인 것의 25~49%가 크게 틀리므로,
사용자가 그것을 알아채고 한 번 더 하면 오른쪽 열이 된다. **알아채는지는 안 쟀다** —
화면에 "이 후보가 틀렸을 수 있다"는 표시가 없다(1.2).

`fix_load.json@1671e540` · 끝점 겨냥 오차 1% · 고각 18° · 구도 하나 · 시드 3 × 상자 30개.

**기준선**: CAD에서 모서리 하나를 놓으려면 최소 두 번(시작·끝)의 스냅 클릭이 필요하다.
그러므로 **획당 추가 조작이 2에 가까워지면 이 도구의 이점이 사라진다**(이 기준선은
대조 실험이 아니라 자릿수 판단이다 — 손 모델링을 실제로 재지 않았다).
첫 시점은 0.45로 여유가 있고(재시도를 넣어도 0.63), **돌린 시점 75°·110°는 2.5로 이미 넘고
45°도 재시도 한 번이면 2.56으로 넘는다.**

---

## 1. 알려진 결함

### 1.1 돌린 시점의 사선이 축으로 오배정된다 — **자동 해법이 없다**(D-S22)

화면에서 **소실점을 지나는 선은 그 축과 같은 상을 맺는다.** 어떤 시점에서는 면 위 사선이
실제로 축 소실점을 지나므로 **2D 증거만으로 두 가설이 구분되지 않는다.** 오배정된 사선은
축 소실점에 **2° 안으로** 맞는다 — 임계·각도 거부권·상대 순위 어느 것으로도 안 갈린다
(시도 넷을 다 쟀다. `view_plane.json@1671e540`).

완화로 돌린 시점만 판정을 조였다(`vp_dist_ratio` 0.06 → 0.03, `requireFarEnd`).
조용히 틀린 배치가 **29~80% 줄지만** 궤도 45°에서 **99획이 여전히 크게 틀린다**.
대가는 위 표의 배치율이다.

**시나리오 관측**(S-10): 놓인 것 중 크게 틀린 비율이 궤도 45°에서 **0.655**다.
_같은 조건에서 **모서리**(사선이 아닌 축 획)는 45°에서 크게 틀린 것이 0이다
(`view_plane.json`의 `edge_control`). 위험은 사선에 몰려 있다._

**남은 길은 2D 밖의 정보인데, 첫 후보가 안 됐다.** 가림 정합성(그린 획이 기존 기하 뒤로
가면 안 보였어야 한다)을 재 보니 **참·거짓이 안 갈린다**(`occlusion.json@1671e540`, 아홉 칸 전부).
**면을 만들지 않으므로 가릴 것이 튜브 선뿐**이고, 맞게 놓인 사선은 오히려 모서리 근처라
더 가려진다. `DEFERRED.md` 맨 아래에 남은 여지 셋을 적었다.

### 1.2 `고치기`가 정확도를 보장하지 않는다

사용자가 고른 앵커에는 검사를 걸지 않는다(D-S18 — 자동이 못 푼 것을 풀라고 만든 경로에
같은 검사를 다시 거는 것은 자기모순이다). 그 대가가 수치로 나왔다:

- **가장 가까운 후보**를 고르면 붙인 것의 **25~49%**가 참값에서 상자 대각의 0.2를 넘게 벗어난다.
- **참 끝점에 가장 가까운 후보**를 고르는 상한 모형에서도 **21~35%**가 그렇다 —
  즉 **옳은 앵커가 후보 목록에 아예 없는 경우**가 상당하다(가림·미배치 이웃 때문).

화면에는 **미확정(주황)** 표시만 있고 "이 후보가 틀렸을 수 있다"는 표시는 없다.

### 1.3 고각 18°는 미해소다 (D-S13)

D-S13의 "검사 둘로 세 각도가 모두 해소된다"는 **고각 35° 전용**이었다.
고각 18°에서는 궤도 75°의 모서리 오차 p90이 **0.591**까지 간다(35°에서는 0.051 — 11배).
**어느 검사가 왜 안 듣는지는 미측정**이다.

### 1.4 한 획에 방향이 둘 이상이면 못 놓는다 (AS-6)

축 하나에 담을 수 없다(D-S3). Quick,Draw 실획에서 **67.7%**가 여기 해당한다 —
다만 그 표본은 마우스·손가락 낙서라 대상 사용자를 대표하지 않는다(AS-13).
**숙련자 획에서 얼마인지는 미측정이고, 이 도구의 성패가 거기 걸려 있다.**

### 1.5 굽은 획은 검증 밖이다 (D-S17)

평면 경로 두 개의 검사가 모두 **끝점 둘**만 본다. 곧은 획이면 끝점 둘 + 평면이 획을
유일하게 결정하지만, 굽은 획은 **가운데가 검증되지 않는다**. 그래서 `bend > 0.18`이면
평면 경로를 쓰지 않는다. _`bend_max` 0.18은 측정이 정당화하지 못한 값이다 — 픽스처가
이봉이라 그 사이 어디에 두어도 결과가 같다._

### 1.6 경사 방향(지붕·계단)은 안 된다 (D-S17)

경사 소실점의 후보가 **점이 아니라 직선**이라 획 하나로는 못 가른다. 여러 획으로 묶으면
산포는 줄지만 수평 VP 쪽으로 **편향된 점에 수렴**한다(참 경사 VP 쪽에 서는 비율 1~4%).
원인은 AS-14(계통 왜곡이 획 간 공유). **사용자가 경사 VP를 직접 찍는 것**이 가장 단순한 길이다.

### 1.7 카메라를 다시 조정하면 손으로 붙인 배치가 사라진다 (D-S23)

`reprojectAll`이 `pts2d`에서 다시 만드는데 사람이 고른 앵커는 거기 없다. 그 앵커를 그대로
쓸 수도 없다 — 앵커는 다른 획 위의 3D 점이고 카메라가 바뀌면 **그 획도 f 10%당 상자 대각의
8%(최대 32%) 움직인다**(`persist.json@1671e540`). 지금은 **몇 개가 그렇게 됐는지 알리기만** 한다.

### 1.8 그 밖

- **저장 슬롯이 하나**다(자동 저장). 여러 작업은 내보내기로 나눈다.
- **레이어·다중 선택**이 없다(`Stroke.layer`는 필드만 있고 아무도 안 읽는다 —
  `selfcheck`가 그것을 플래그로 들고 있다).
- **면을 만들지 않는다**(D-S20). 돌려 보면 선만 보인다. 획끼리는 서로 가린다.
- **구도가 하나다.** 모든 배치 측정이 요 35°·피치 15° 3점, 직육면체 하나에서 나왔다.

---

## 2. 측정된 지표

### 2.1 축 판정 (§4.1)

| | 값 | 출처 |
|---|---|---|
| 미분류 사선 중 면 경로로 회수 | 0.84 (출하 구성 0.802) | `face_diagonal.json` |
| **사선이 축으로 오인**(첫 시점) | **0.158**, 그렇게 놓이면 오차 중앙 0.448 | `face_diagonal.json` |
| 사선이 축으로 오인(돌린 시점 45°) | 0.63 → 완화 후에도 다수 잔존 | `view_plane.json` |

### 2.2 배치 (§4.2~4.3)

| | 첫 시점 | 돌린 시점 45° / 75° / 110° |
|---|---|---|
| 배치율 | 0.846 | 0.419 / 0.112 / 0.135 |
| 후보가 아예 없는 미배치 | 0.006 | 0.006 / 0.017 / 0.013 |
| 조용히 틀린 배치(놓인 것 대비) | 0.057 | 0.655 / 0.300 / 0.250 |

`fix_load.json@1671e540`. 끝점 겨냥 오차 1%(= 이 방식이 성립한다고 주장하는 구간).

### 2.3 고치기 부하 (§1 왕복 지표)

| | 첫 시점 | 45° | 75° | 110° |
|---|---|---|---|---|
| 미배치 획당 조작 수 | 2.93 | 2.93 | 2.93 | 2.93 |
| **그린 획당 추가 조작**(재시도 0) | **0.45** | 1.70 | 2.55 | 2.48 |
| 〃 재시도 한 번 포함 | 0.63 | 2.56 | 3.22 | 3.30 |

**미배치 획당 조작 수가 세 조건에서 같은 것은 측정이 아니라 모형의 귀결이다** —
고르기 1 + 양 끝 후보 클릭 2 = 3이고, 한쪽만 후보가 있는 소수(≈7%)만 2다. 갈리는 것은
**빈도**뿐이므로 판정은 "그린 획당" 줄로 한다.
돌린 시점 획은 **"그 시점으로" 1이 더 붙는다**(실측: 1획에 4조작. `e2e.json@1671e540`) —
합성 하네스의 모형은 그 항목을 0으로 잡았으므로 위 표는 그만큼도 **하한**이다.

### 2.4 지연·성능 (S-10 실측, `e2e.json@1671e540`)

| | 값 |
|---|---|
| 획 하나 지연(`pointerup` 반환까지 — 판정·배치·튜브·다시 그리기 전부) | **중앙 2.25ms · 최대 4.7ms** |
| 궤도 회전 프레임 | 중앙 16.8ms(≈60fps, vsync) |
| 새로고침 → 복원 | 146ms · 13획 · **id·`pts2d`·`pts3d` 한 비트도 안 변함** |
| 튜브 메쉬 | 13획에 정점 1328 · 삼각형 2604 |

개발 서버(번들 없음) · Chromium · 1440×900 기준이므로 **상한**으로 읽는다.
기계에 달린 값이고 회귀 감시용이다.

### 2.5 설계 보장 (측정이 아니다 — 임계를 걸지 않는다)

- **재투영 항등**: 각 점이 자기 광선 위에 놓이므로 그린 카메라에서 되쏘면 원본으로 돌아온다(0px).
- **회전 판 불변식**: 7조합 전부 0px.
둘 다 불변식 테스트로 잠갔다. `selfcheck`의 자기참조 유형 3이 이것을 감시한다.

---

## 3. 이 수치들을 어떻게 읽어야 하는가

1. **전부 합성이다.** 실획 표본이 0이다. 두 전제(AS-6 한 획 = 한 축, AS-12 끝점 겨냥 오차)가
   모두 Quick,Draw 낙서에서 나왔고 대상 사용자를 대표하지 않는다(AS-13).
2. **끝점 겨냥 오차 1%가 성립 구간이다.** 손에 있는 유일한 실획 자릿수는 **2.8~5.1%**로
   그 밖이다. 그 구간에서는 배치율이 더 떨어지고 고치기 부하가 더 커진다
   (`persist.json`의 0.02 셀이 한 예: 사용자 지정이 9 → 34로 는다).
3. **구도가 하나다.** 획이 수십 개인 실제 그림에서는 미검증이다.
4. **비율보다 분자/분모를 본다.** 시드 하나가 결론을 흔드는 자리가 여럿 있다.

---

## 4. 다음에 손댈 것 (우선순위)

1. **실획 측정** — `sessions/`에 획이 들어오면 다른 어떤 작업보다 먼저 한다.
   위 모든 임계가 그 뒤에 재조정된다(STALE이 하류를 자동 표시한다).
2. **가림 정합성은 지금 형태로 안 된다**(`occlusion.json@1671e540`). 우선순위를 올려 타당성을
   먼저 쟀더니 **아홉 칸 전부에서 안 갈렸고 일부는 거꾸로**였다(맞은 배치가 더 가려진다).
   **원인은 면이 없다는 것**이다 — 가리는 것이 튜브 선뿐이라 화면 면적이 작고, 맞게 놓인 사선은
   면 위(모서리 근처)에 있고 틀린 것은 빈 공간에 있다. 다른 형태를 찾아야 한다(`DEFERRED.md`).
3. **`고치기`의 후보 품질** — 옳은 앵커가 목록에 없는 경우가 21~35%다. 반경·정렬·깊이
   슬라이더의 눈금을 함께 본다.
4. 고각 18° 구간의 원인 규명(어느 검사가 왜 안 듣는가).
5. 레이어·다중 선택.
