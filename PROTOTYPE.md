# PROTOTYPE.md — 이 시제품이 **되는 것과 안 되는 것**

S-A~S-10 완주 시점의 상태. 여기 적은 수치는 전부 `stage0/out/`의 원장에서 왔다.
**모든 배치 수치는 합성 획이다**(V-o·AS-1) — **대상 사용자(스타일러스·투시도 숙련자)의 획
표본이 0이다**(`sessions/`가 비어 있다). 그것이 이 문서에서 가장 중요한 한 줄이다.
_"실획"이라는 말은 아래에서 **Quick,Draw 낙서**(마우스·손가락)를 가리킬 때만 쓰고, 그때마다
그렇게 적는다 — 대상 사용자 획과 같은 것이 아니다(AS-13)._

---

## 0. 한 문장 판정

**첫 시점에서 그리는 데는 (겨우) 선다. 돌려서 그리는 데는 서지 않는다.**

`fix_load.json@1671e540` · 끝점 겨냥 오차 1% · 고각 18° · 구도 하나 · 시드 3 × 상자 30개 ·
사용자 모형 `nearest`.

| | 자동 배치 | 그린 획당 추가 조작 ① | ② +"그 시점으로" | ③ ②+조용히 틀린 것 고치기 | 조용히 틀린 자동 배치 |
|---|---|---|---|---|---|
| **첫 시점**(모서리 12획) | **0.846** | **0.45** | 0.60 | 0.75 | 0.057 |
| 돌린 시점 45°(사선 2+모서리 1) | 0.419 | 1.70 | **2.29** | **3.11** | **0.655** |
| 돌린 시점 75° | 0.112 | **2.55** | **3.44** | **3.54** | 0.30 |
| 돌린 시점 110° | 0.135 | **2.48** | **3.35** | **3.45** | 0.25 |

**①은 하한이다.** 조작 모형이 둘을 0으로 잡았다 — 돌린 시점 획을 고치려면 **그 시점으로
돌아가는 조작**이 미배치마다 하나 더 붙고(②, 실측으로 확인됐다: `e2e.json`에서 1획에 4조작),
**조용히 틀리게 놓인 획을 찾아 고치는 조작**은 아예 안 세었다(③은 그것을 미배치와 같은
3조작으로 잡은 추정이다). 그 밖에도 **찾기·확인·되돌리기**가 안 세어져 있다
(`fix_load.json`의 `ops_not_counted`).

⚠ **"그린 획당 추가 조작"은 배치율의 대수적 변환이다** — `(1 − 배치율) × 미배치당 조작 수`와
소수 넷째 자리까지 같다. **독립된 두 번째 증거가 아니다.** 미배치당 조작 수는 정의상 2~3에
갇혀 있으므로(고르기 1 + 후보 클릭 1~2), 이 지표가 담은 정보는 사실상 **배치율**이다.

### 기준선 — **판정이 여기 걸려 있고, 이것은 측정이 아니다**

초판은 "CAD에서 모서리 하나에 스냅 클릭 둘"이라 잡았다. **그것은 관대한 쪽으로 틀렸다** —
직육면체 12모서리는 꼭짓점을 공유하므로 **8클릭**이면 놓이고(모서리당 ≈0.67), 상자
프리미티브면 2~3클릭이다. 기준선을 0.67로 보면:

- 첫 시점 0.45는 **아슬아슬하게 아래**이고, 빠진 조작을 넣은 0.60·0.75는 **경계이거나 위**다.
- 돌린 시점은 어느 셀도 아래가 아니다.

**그러므로 "첫 시점은 선다"는 여유 있는 판정이 아니다.** 이 도구의 이점은 조작 수가 아니라
**그린 획이 그대로 남는다는 것**(손떨림·굵기·형태)에 있고, 조작 수로는 CAD와 비슷한 자리에 있다.
_손 모델링과의 대조 실험은 하지 않았다 — 위는 전부 자릿수 판단이다._

### ⚠ 첫 시점 셀과 돌린 시점 셀은 **획 구성이 다르다**

첫 시점은 **모서리 12획**(사선 없음), 돌린 시점은 **면 대각선 2 + 모서리 1**이다.
그러므로 위 표의 세로 비교에는 **시점 차이와 획 종류 차이가 섞여 있다.** 갈라 보면:

| 궤도 | 사선 배치율 | 사선: 놓인 것 중 크게 틀림 | 모서리 배치율 | 모서리: 놓인 것 중 크게 틀림 |
|---|---|---|---|---|
| 45° | 0.411 | **74/74 = 전부** | 0.433 | **0/39** |
| 75° | 0.039 | 7/7 = 전부 | 0.258 | 2/23 |
| 110° | 0.051 | 9/9 = 전부 | 0.303 | 0/27 |

**돌린 시점에서 자동으로 놓인 사선은 전부 크게 틀렸다**(세 각도 모두, 90건 전부).
모서리는 거의 안 틀린다. 위험은 **사선에만** 있고, 부담(조작 수)은 **둘 다** 있다
(모서리도 배치율이 0.26~0.43이라 그린 획당 1.6~2.0조작이다).

---

## 1. 알려진 결함

### 1.1 돌린 시점의 사선이 축으로 오배정된다 — **자동 해법이 없다**(D-S22)

화면에서 **소실점을 지나는 선은 그 축과 같은 상을 맺는다.** 어떤 시점에서는 면 위 사선이
실제로 축 소실점을 지나므로 **2D 증거만으로 두 가설이 구분되지 않는다.** 오배정된 사선은
축 소실점에 **2° 안으로** 맞는다 — 임계·각도 거부권·상대 순위 어느 것으로도 안 갈린다
(시도 넷을 다 쟀다. `view_plane.json@1671e540`).

완화로 돌린 시점만 판정을 조였다(`vp_dist_ratio` 0.06 → 0.03, `requireFarEnd`).
조용히 틀린 배치가 **29~80% 줄지만** 궤도 45°에서 **99획이 여전히 크게 틀린다**.
대가는 위 표의 배치율이다.

**시나리오 관측**(S-10): 놓인 것 중 크게 틀린 비율이 궤도 45°에서 **0.655**다.
_같은 조건에서 **모서리**(사선이 아닌 축 획)는 45°에서 크게 틀린 것이 0이다
(`view_plane.json`의 `edge_control`). 위험은 사선에 몰려 있다._

**남은 길은 2D 밖의 정보인데, 첫 후보가 안 됐다.** 가림 정합성(그린 획이 기존 기하 뒤로
가면 안 보였어야 한다)을 재 보니 **참·거짓이 안 갈린다**(`occlusion.json@1671e540`, 아홉 칸 전부).
**면을 만들지 않으므로 가릴 것이 튜브 선뿐**이고, 맞게 놓인 사선은 오히려 모서리 근처라
더 가려진다. `DEFERRED.md` 맨 아래에 남은 여지 셋을 적었다.
_S-8이 인용한 "뒤 모서리의 30%가 가려진다"는 **원장 밖의 값**이고 유리한 자세를 골라 잰
브라우저 픽셀 측정이다(AS-4). 위 측정이 그 수치로 기대한 것과 반대로 나온 이유이기도 하다._

### 1.2 `고치기`가 정확도를 보장하지 않는다

사용자가 고른 앵커에는 검사를 걸지 않는다(D-S18 — 자동이 못 푼 것을 풀라고 만든 경로에
같은 검사를 다시 거는 것은 자기모순이다). 그 대가가 수치로 나왔다:

- **가장 가까운 후보**를 고르면 붙인 것의 **25~49%**가 참값에서 상자 대각의 0.2를 넘게 벗어난다.
  절단값을 바꿔도 순서는 같다(첫 시점 166건에서 0.1 초과 78 · 0.2 초과 63 · 0.5 초과 36).
  **꼬리가 길다** — 45°에서 p90 0.74·최대 1.93, 75°에서 최대 **31.3**(상자 대각의 31배)이다.
- **참 끝점에 가장 가까운 후보**를 고르는 상한 모형에서도 **21~35%**가 그렇다 —
  즉 **옳은 앵커가 후보 목록에 아예 없는 경우**가 상당하다.
- **후보 반경을 넓혀도 안 낫는다**(3 → 5 → 8배: 틀린 비율 0.349 → 0.361 → 0.361,
  대신 사용자가 봐야 하는 점이 중앙 4 → 4 → 5로 는다). **원인이 반경이 아니다.**
  갈라 보면 틀린 58건 중 **앵커로 쓴 획 자체가 틀려서**가 14건이고 **44건은 앵커 획이
  맞는데도 틀렸다** — 옳은 모서리가 **아직 안 놓여서** 후보에 없는 것이 주된 원인으로 보인다.
- **손으로 붙인 결과는 하네스에서 뒤 획의 앵커로 쓰이지 않는다**(그래서 두 사용자 모형의
  배치율이 같다). 실제 앱에서는 쓰이므로 **틀린 수동 배치가 연쇄를 타는지는 미측정**이다(AS-8).
- 다만 **붙인 뒤 자동 배치를 다시 돌리면** 손 하나당 **0.14획**이 공짜로 놓인다
  (그린 획당 조작 0.451 → **0.400**). **앱은 지금 그것을 안 한다** — `applyPicks`가
  `settle`을 부르지 않는다. 코드 한 줄이고, 다음 세션의 가장 싼 개선이다.

화면에는 **미확정(주황)** 표시만 있고 "이 후보가 틀렸을 수 있다"는 표시는 없다.

### 1.3 고각 18°는 조건이 다르다 (D-S13) — **다만 초판의 숫자가 틀렸다**

D-S13의 "검사 둘로 세 각도가 모두 해소된다"는 **고각 35° 전용**이었다.

_초판은 "고각 18°에서 p90 0.591"이라 적었는데 **그것은 출하 구성이 아니라 `now` 팔
(감사 이전 동작)의 값이다**(리뷰어가 잡았다). **출하 구성**(`VIEW_OPTS`+`VIEW_AXIS_CFG`)의
같은 칸은 궤도 75°에서 **p90 0.0549 · 최대 0.5984 · 배치율 0.2458**이다
(`view_plane.json@1671e540`의 `app.edge_control`)._

그러므로 지금 남은 것은 "오차가 크다"가 아니라 **"오차가 작은 이유가 배치율이 낮아서인지
(생존자 편향) 구분되지 않는다"**이다 — 같은 칸에서 `now`는 배치율 0.6525에 p90 0.5909였다.
**어느 검사가 왜 그렇게 거르는지는 미측정**이다.

### 1.4 한 획에 방향이 둘 이상이면 못 놓는다 (AS-6)

축 하나에 담을 수 없다(D-S3). **Quick,Draw 낙서**(마우스·손가락)에서 그 비율은 두 원장이
서로 다른 표본으로 잰다 — `one_stroke_one_axis.json@1671e540`의 위반율 **0.699**(낱말 6·n=1816)와
`bend_share.json@1671e540`의 꺾인 획 비율 **0.6775**(낱말 7·n=4785)다. **같은 양의 두 측정이고
표본이 다르다**(HANDOFF S-8 잔여 ⑤). 자릿수만 읽는다.

그 표본은 대상 사용자를 대표하지 않는다(AS-13).
**숙련자 획에서 얼마인지는 미측정이고, 이 도구의 성패가 거기 걸려 있다.**

### 1.5 굽은 획은 검증 밖이다 (D-S17)

평면 경로 두 개의 검사가 모두 **끝점 둘**만 본다. 곧은 획이면 끝점 둘 + 평면이 획을
유일하게 결정하지만, 굽은 획은 **가운데가 검증되지 않는다**. 그래서 `bend > 0.18`이면
평면 경로를 쓰지 않는다. _`bend_max` 0.18은 측정이 정당화하지 못한 값이다 — 픽스처가
이봉이라 그 사이 어디에 두어도 결과가 같다._

### 1.6 경사 방향(지붕·계단)은 안 된다 (D-S17)

경사 소실점의 후보가 **점이 아니라 직선**이라 획 하나로는 못 가른다. 여러 획으로 묶으면
산포는 줄지만 수평 VP 쪽으로 **편향된 점에 수렴**한다(참 경사 VP 쪽에 서는 비율 1~4%).
원인은 AS-14(계통 왜곡이 획 간 공유). **사용자가 경사 VP를 직접 찍는 것**이 가장 단순한 길이다.

### 1.7 카메라를 다시 조정하면 손으로 붙인 배치가 사라진다 (D-S23)

`reprojectAll`이 `pts2d`에서 다시 만드는데 사람이 고른 앵커는 거기 없다. 그 앵커를 그대로
쓸 수도 없다 — 앵커는 다른 획 위의 3D 점이고 카메라가 바뀌면 **그 획도 f 10%당 상자 대각의
8%(최대 32%) 움직인다**(`persist.json@1671e540`). 지금은 **몇 개가 그렇게 됐는지 알리기만** 한다.

### 1.8 그 밖

- **저장 슬롯이 하나**다(자동 저장). 여러 작업은 내보내기로 나눈다.
- **레이어·다중 선택**이 없다(`Stroke.layer`는 필드만 있고 아무도 안 읽는다 —
  `selfcheck`가 그것을 플래그로 들고 있다).
- **면을 만들지 않는다**(D-S20). 돌려 보면 선만 보인다. 획끼리는 서로 가린다.
- **구도가 하나다.** 모든 배치 측정이 요 35°·피치 15° 3점, 직육면체 하나에서 나왔다.

---

## 2. 측정된 지표

### 2.1 축 판정 (§4.1)

| | 값 | 출처 |
|---|---|---|
| 미분류 사선 중 면 경로로 회수 | 0.84 | `face_diagonal.json@1671e540` |
| 〃 출하 구성(검사 켬) | **0.802 — 시드 1301 한 칸의 값이다** | `misassign_retry.json@1671e540` |
| **사선이 축으로 오인**(첫 시점) | **0.158**, 그렇게 놓이면 오차 중앙 0.448 | `face_diagonal.json@1671e540` |
| 사선이 축으로 오인(돌린 시점 45°) | 0.63 → 완화 후에도 다수 잔존 | `view_plane.json@1671e540` |

_초판은 0.802의 출처를 `face_diagonal.json`으로 적었는데 **거기 없는 값**이었다(리뷰어)._

### 2.2 배치 (§4.2~4.3)

| | 분모 | 첫 시점 | 돌린 시점 45° / 75° / 110° |
|---|---|---|---|
| 배치율 | 그린 획 | 0.846 | 0.419 / 0.112 / 0.135 |
| 후보가 아예 없는 미배치 | **미배치 획** | 0.006 (1/166) | 0.006 / 0.017 / 0.013 |
| 조용히 틀린 배치 | **놓인 획** | 0.057 (52/914) | 74/113 / 9/30 / 9/36 |

`fix_load.json@1671e540`. 끝점 겨냥 오차 1%(= 이 방식이 성립한다고 주장하는 구간).
**행마다 분모가 다르다**(리뷰어 지적으로 병기했다). 75°·110°의 조용히 틀린 배치는
**분자 9·분모 30~36**이므로 비율로 읽지 않는다 — 시드 셋을 합친 값이고 시드별 분해가 없다.

⚠ **`후보가 아예 없는 미배치`는 같은 시점 안에서의 값이다.** 실제 앱에서는 **시점이 교차하면
후보가 0이 된다** — `e2e.json`의 실측 한 바퀴에서는 미배치 3획 중 **1획**이 그 경우였다
(돌린 시점에서 그린 획은 첫 시점 캔버스에 후보가 없다). 합성 하네스는 그 상황을 만들지 않는다.

### 2.3 고치기 부하 (§1 왕복 지표)

| | 첫 시점 | 45° | 75° | 110° |
|---|---|---|---|---|
| 미배치 획당 조작 수 | 2.93 | 2.93 | **2.88** | **2.87** |
| **그린 획당 추가 조작**(빠진 것 0) | **0.45** | 1.70 | 2.55 | 2.48 |
| 〃 + "그 시점으로" | 0.60 | 2.29 | 3.44 | 3.35 |
| 〃 + 조용히 틀린 것 고치기 | 0.75 | 3.11 | 3.54 | 3.45 |
| 〃 + 재시도 한 번(별개 추정) | 0.63 | 2.56 | 3.22 | 3.30 |

**미배치 획당 조작 수는 정의상 2~3에 갇혀 있다** — 고르기 1 + 후보 클릭 1~2이므로
2.87~2.93의 폭은 "양 끝을 눌러야 했던 비율 87~93%"라는 뜻뿐이다(측정이 아니라 모형의 귀결).
그러므로 판정은 "그린 획당" 줄로 하고, **그 줄도 배치율의 변환**임을 §0에 적었다.
돌린 시점 획은 **"그 시점으로" 1이 더 붙는다**(실측: 1획에 4조작. `e2e.json@1671e540`) —
합성 하네스의 모형은 그 항목을 0으로 잡았으므로 위 표는 그만큼도 **하한**이다.

### 2.4 지연·성능 (S-10 실측, `e2e.json@1671e540`)

| | 값 |
|---|---|
| 획 하나 지연(`pointerup` 반환까지 — 판정·배치·튜브·다시 그리기 전부) | **중앙 2.15ms · 최대 4.0ms** |
| 궤도 회전 프레임 | 중앙 16.6ms(≈60fps, vsync) · 최대 24.2ms |
| 새로고침 → 복원 | **152ms** · 13획 |
| 튜브 메쉬 | 13획에 정점 1328 · 삼각형 2604 |

개발 서버(번들 없음) · Chromium · 1440×900 기준이므로 **상한**으로 읽는다.
기계에 달린 값이고 회귀 감시용이다.

### 2.5 설계 보장 (측정이 아니다 — 임계를 걸지 않는다)

- **재투영 항등**: 각 점이 자기 광선 위에 놓이므로 그린 카메라에서 되쏘면 원본으로 돌아온다(0px).
- **회전 판 불변식**: 7조합 전부 0px.
- **저장 왕복 항등**: 새로고침 뒤 `id`·`pts2d`·`pts3d`가 **한 비트도 안 변한다**.
  _이것은 측정이 아니라 **직렬화 왕복**이다(D-S23이 `pts3d`를 담기로 했으므로 같을 수밖에 없다).
  깨지면 저장 포맷 결함을 뜻한다 — 그래서 §2.4가 아니라 여기 둔다(리뷰어)._
셋 다 불변식 테스트로 잠갔다. `selfcheck`의 자기참조 유형 3이 이것을 감시한다.

---

## 3. 이 수치들을 어떻게 읽어야 하는가

1. **전부 합성이다.** 실획 표본이 0이다. 두 전제(AS-6 한 획 = 한 축, AS-12 끝점 겨냥 오차)가
   모두 Quick,Draw 낙서에서 나왔고 대상 사용자를 대표하지 않는다(AS-13).
2. **끝점 겨냥 오차 1%가 성립 구간이다.** 손에 있는 유일한 실획 자릿수는 **2.8~5.1%**로
   그 밖이다. 그 구간에서는 배치율이 더 떨어지고 고치기 부하가 더 커진다
   (`persist.json`의 0.02 셀이 한 예: 사용자 지정이 9 → 34로 는다).
3. **구도가 하나다.** 획이 수십 개인 실제 그림에서는 미검증이다.
4. **비율보다 분자/분모를 본다.** 시드 하나가 결론을 흔드는 자리가 여럿 있다.

---

## 4. 다음에 손댈 것 (우선순위)

1. **실획 측정** — `sessions/`에 획이 들어오면 다른 어떤 작업보다 먼저 한다.
   위 모든 임계가 그 뒤에 재조정된다(STALE이 하류를 자동 표시한다).
2. **가림 정합성은 지금 형태로 안 된다**(`occlusion.json@1671e540`). 우선순위를 올려 타당성을
   먼저 쟀더니 **아홉 칸 전부에서 안 갈렸고 일부는 거꾸로**였다(맞은 배치가 더 가려진다).
   **원인은 면이 없다는 것**이다 — 가리는 것이 튜브 선뿐이라 화면 면적이 작고, 맞게 놓인 사선은
   면 위(모서리 근처)에 있고 틀린 것은 빈 공간에 있다. 다른 형태를 찾아야 한다(`DEFERRED.md`).
3. **`고치기`의 후보 품질** — 옳은 앵커가 목록에 없는 경우가 21~35%다. 반경·정렬·깊이
   슬라이더의 눈금을 함께 본다.
4. 고각 18° 구간의 원인 규명(어느 검사가 왜 안 듣는가).
5. 레이어·다중 선택.
