# SKETCH2SPACE — 작업 지침 (CLAUDE.md)

투시 스케치 3D 도구. **현행 계획서는 [`docs/sketch3d_plan.md`](docs/sketch3d_plan.md)**(코드명 S).
이론 근거는 [`docs/perspective_theory.md`](docs/perspective_theory.md).
폐기된 계획서는 `docs/archive/`에 폐기 사유와 함께 있다.

**제품 정의**: 손으로 그린 투시도가 그대로 3차원 오브젝트가 된다.

**핵심 원칙 — 획을 버리지 않는다.** 직선을 추출해 면을 만드는 것이 아니라,
그린 곡선 그대로가 3D 공간의 곡선이 된다. 손떨림도 전부 남는다.
시스템이 판정하는 것은 **방향뿐**이다.

```
2D 획 (손떨림 포함) → 방향 판정 → 앵커 결정 → 각 점 역투영
                                   → 3D 획 (손떨림 그대로, 축을 감싸며 흔들림)
```

**사용자는 투시도 숙련자다.** 시스템이 형태에 개입하지 않는다.
- 한다: 획의 축 배정, 끝점 접합
- 안 한다: 획을 직선으로 펴기, 중간 점 이동

> 2026-08-14 두 번째 전환. 이전 접근(투시 작도로 엣지·정점을 만드는 것)은 폐기됐다.
> 왜 바꿨는지는 [`docs/archive/wireframe_plan.md`](docs/archive/wireframe_plan.md) 머리말에 있다.
> 측정 산출물은 `stage0/out/archive_W/`(작도 접근)와 `archive_pre_W/`(형태 추론 접근)에 보존.

---

## 1. 자료구조 (계획서 §5)

```
Stroke { id, pts2d[], pts3d[], axis, anchorRef, mesh, layer, color, width }
Camera { vps[], principal, f, locked }
Face   { strokes[], plane }        // 선택적, §6에서 판단
```

- **`pts2d`를 반드시 보존한다.** 카메라가 재조정되면 전체를 다시 투영해야 한다.
- `axis`: `VP1|VP2|VP3|screen|face|free`
- provenance는 **폐기**됐다(§10). **형태 추정**이 없으므로 획마다 출처를 나눌 대상이 없다.
  다만 **깊이 스케일은 카메라 설정에서 온다** — 1점 f는 설정값이고 2점 주점은 가정이다
  (이론서 16.2·16.4, assumptions AS-9). 그 출처는 `Camera.fSource` 하나에 있고 화면에 나온다.

---

## 2. 진행 규칙 — 무중단 자율 진행

S-A → S-0 → S-1 → … → S-10까지 멈추지 않고 완주한다. **중간 보고 없음. 확인 요청 없음.**

```
각 단계 완료 시:
  테스트 실행 → selfcheck 실행 → 리뷰어 호출 → 지적 대응 → 커밋 → 다음 단계
```

**각 단계는 브라우저에서 열리는 상태로 커밋한다.** S-10까지 가야 실행 가능한 것이
나오는 구조를 만들지 않는다. S-1 끝에서 소실점을 찍고 그리드가 도는 것을 볼 수 있어야 하고,
S-3 끝에서 그린 획이 3D에 놓이고 회전해서 볼 수 있어야 한다.

### A-2. 막혔을 때 — 멈추지 말고 우회한다
1. **재시도** — 같은 접근으로 최대 3회, 매번 다른 가설로
2. **우회** — 더 단순한 구현으로 대체. 계획서가 예시를 준다:
   떨림 평면이 안 정해지면 → `facing` 고정(S-3에서 결정됨, D-S8) / 축 밖 획이 어려우면 → 임시 평면 폴백만 /
   면 생성이 어려우면 → 선만 유지(§6 A안). 사실과 원래 목표를 `DEFERRED.md`에 기록
3. **격리** — 해당 기능을 끄고 나머지 진행
4. **다음 단계로** — 어떤 경우에도 다음 S로 넘어간다

**중단하는 유일한 경우**: **획 → 3D 재투영이 원리적으로 성립하지 않을 때.**
임계는 D-S6에 측정 전에 박았다(**중앙값 > 획 길이의 5%** 또는 **p90 > 15%**. 화면 대각이 아니라
획 길이 대비다 — 대각 대비로 하면 짧은 획에서 한없이 관대해진다).

**S-3에서 적용 대상을 정정했다**(D-S7). 원래 지정한 "재투영 화면 좌표차"는 **이 설계의 보장**이다 —
각 점이 자기 광선 위에 놓이므로 그린 카메라에서 되쏘면 정확히 원본으로 돌아온다(실측 0px).
보장은 불변식 테스트로 잠그고, 게이트로는 쓰지 않는다.
임계값은 그대로 두고 대상만 **① 끝점 접합 이동량 ② 축이 정해졌는데도 못 놓인 비율**로 옮겼다.

**②의 임계 20%는 사후에 정한 잠정값이며, S-10에서 실획으로 재정하기 전까지 판정 근거가 아니다.**
(그렇게 표시하지 않으면 "임계 20% < 관측 30%"만 읽고 반대로 판단하게 된다.)
지금 그 값이 하는 일은 **발동 가능한 관측 조합이 존재함을 보이는 것**뿐이다(끝점 오차 2%에서 0.234, 4%에서 0.702).

**S-3 판정은 중단 없음**이고, 근거는 임계 통과가 아니라 셋이다 — 화면 이미지가 정확히
보존되고(설계 보장, 불변식 테스트로 잠금), 평면 선택의 최적점이 참값 대조로 확인되며, 끝점 오차 1% 이하에서 깊이 오차가
연쇄를 타도 3배 안에 머문다. **성립 조건은 도구가 아니라 입력에 걸린다**(끝점 겨냥 오차
≲ 그림 대각의 1%) — 그리고 **지금 손에 있는 유일한 실획 자릿수는 그 구간 밖이다**(AS-12,
S-10 필수). 그 외에는 어떤 이유로도 멈추지 않는다.

### A-3. 판단 규칙 (사람에게 묻지 않는다)
- 측정이 가리키는 방향을 우선한다
- 사전 예상과 어긋나면 **측정을 따르고** 예상을 `assumptions.md`에 반증으로 기록한다
- 선택지가 여럿이면 **가장 단순한 것**을 고른다. 복잡한 대안은 `DEFERRED.md`
- 계획 문서와 실제가 어긋나면 **실제를 따르고 문서를 갱신한다**
- 범위를 넓히지 않는다
- **축 판정이 애매하면 미분류로 두고 사용자 지정에 맡긴다.** 조용히 틀린 축에 배정하지 않는다
- 선택 근거와 **기각 사유**를 `progress.md`에, 잠정 결정은 `DECISIONS.md`에

### A-4. 품질 하한 (타협하지 않는다)
- 기존 테스트가 깨진 채로 다음 단계에 가지 않는다
- 새 기능마다 테스트를 쓴다(반례 테스트 포함)
- selfcheck를 매 단계 돌린다 / 리뷰어를 매 단계 호출한다
- 커밋을 잘게 나눈다 / `progress.md`를 매 단계 갱신한다
- **각 단계 끝에서 브라우저로 열리는 상태여야 한다**
- **참조 저장소 코드를 복사하지 않는다**(아래 §3)

### A-1. 세션 인계
컨텍스트가 차면 `HANDOFF.md`를 갱신하고 종료한다. 다음 세션은 그 한 줄로 시작한다:
`"HANDOFF.md를 읽고 이어서 진행하라."` **이 문장이 유일한 사람 개입이다.**

---

## 3. 참조 저장소 — 코드 복사 금지

`notebysebas/sketchbook-v8` (Sketch3D). three.js r128, 단일 HTML 12,833줄, PWA.

**LICENSE 파일이 없고 소스에 저작권 표시도 없다.** 라이선스 없는 공개 저장소는
모든 권리 유보다. GitHub 약관(D.5)이 그 안에서의 열람·포크까지는 허용하지만
**수정·재배포·제품 편입은 허용하지 않는다.**

- 동작 확인과 접근 이해 용도로만 본다
- **코드를 복사하지 않는다.** 변수명·함수 구조를 그대로 옮기지도 않는다
- 필요한 것은 전부 표준 구현이므로 직접 짠다: 튜브 메쉬는 three.js `TubeGeometry`(MIT),
  속도 기반 굵기는 점 간격 계산, undo/redo·레이어·지우개는 일반적 구조,
  PWA는 manifest + service worker

이 저장소에서 얻은 것은 **"획을 원본 그대로 튜브로 쌓아도 3D 형태로 읽히고
브라우저 성능이 충분하다"**는 확인이다. 그것이 §6(면 생성) 판단의 근거가 된다.

---

## 4. 리뷰어 서브에이전트

`.claude/agents/reviewer.md`. **각 S 단계 완료 시** 호출한다. 지적이 나오면 대응 후 재호출,
2회 반복 후에도 남으면 `DEFERRED.md`에 올리고 진행한다.

리뷰어는 **코드를 보지 않는다**(문서·수치만). 이 프로젝트에서 세션 밖에서 잡힌 결함이 전부
**"테스트 통과"에 설득당하지 않는 위치**에서 나왔기 때문이다 — 마우스 등급 판정의 단위 혼동,
이론서 8.8이 결론을 뒤집은 것, 4.6%가 PCA 프레임 값이었던 것,
**게이트 값 0.692가 이전 실행의 수치였던 것**.

---

## 5. 자기검증 규칙

1. **의심 수치 자동 점검** — `python selfcheck.py`가 `stage0/out/*.json`을 스캔한다.
   1e-10 미만 오차 / 정확히 1.0·0.0 비율 / 이전 대비 완전 불변 / 0 고정 카운터 /
   단일 범주 분포 / **분포 전체가 한 값** / 비결정 시드 정적 탐지(`.py`의 `hash(str)`, `.ts`의 `Math.random`) /
   **복원↔역연산 왕복 지표**(자기참조 유형 3 — 오차류 이름의 함수가 스스로 변환을 수행하면
   그 값은 측정이 아니라 **설계 보장**일 수 있다. S-3의 `reprojectionGap`이 그 사례다.
   보장이면 원장에 그렇게 적고 **임계를 걸지 않는다**).
   의심≠오류 — 각 플래그의 원인을 확인한다.
   **측정은 반드시 `stage0/out/`에 JSON으로 남긴다.** 원장 밖 측정은 규칙이 있어도 안 걸린다.
   **STALE 탐지** — 측정 하네스는 `constantsSnapshot()`을 산출물에 넣고, selfcheck가
   `stage0/out/constants.json`(현재 값)과 대조한다. 다르면 그 산출물은 **낡았고 그것을 인용한
   문서도 낡았다**. 같은 유형이 세 번 재발해서 만든 규칙이다(비결정 시드 / 등급 단위 혼동 /
   `AXIS_TOL` 기본값이 S-3 측정을 바꾼 것). **새 임계를 만들면 `test/constants.ts`에 넣는다** —
   빠지면 안 잡힌다.
   **인용 규약**: 문서가 수치를 인용할 때 `산출물.json@해시`로 적는다. 어느 실행인지 남는다.
2. **assumptions.md** — 매 단계 관측과 대조.
3. **새 지표엔 반례 테스트** — 그 지표가 의도한 것을 재는지 확인한다.

> **수치를 문서에 쓰기 전에 원장 JSON을 그 자리에서 다시 읽는다.** 측정을 재실행했으면
> 그 값을 인용한 문서를 **전부** 찾아 고친다(`grep`으로 확인). 이 규칙은 실제로 어겨서 생겼다 —
> 게이트 값이 문서 다섯 곳에 이전 실행 값으로 남아 있었다.

**재현성**: 시드에 `hash(str)`(py)·`Math.random`(ts)을 쓰지 않는다. `stable_seed` / `rng32(seed)`.
시드 변동폭 때문에 **유효 자릿수는 2자리**로 본다.

---

## 6. 디렉토리 구조

```
SKETCH2SPACE/
  CLAUDE.md HANDOFF.md DECISIONS.md DEFERRED.md progress.md assumptions.md
  docs/
    sketch3d_plan.md       현행 계획서
    perspective_theory.md  이론서
    archive/               폐기된 계획서 3종 + 폐기 사유
  web/                     본체 (TypeScript + Vite + three.js)
    src/capture/           잉크 캡처 (Pointer Events, 마우스 1급, 팜 리젝션)
    src/s3d/               카메라·제약 누산기·투시 그리드·3D 기하·합성 잉크
    src/ui/                화면
    src/parser/            geometry/linalg/grade — 유지되는 원시 유틸
    src/data/              Quick,Draw 실획 로더
    test/                  vitest
  stage_perspective/       카메라 수학 (Python 기준 구현 — TS 이식의 정답)
  stage3/ common/ stage0/  합성 장면 · 획 원시처리 · 통계 하네스와 원장
  selfcheck.py  tests/
```

---

## 7. 이론서 참조 지도

| 절 | 내용 | 쓰는 곳 |
|---|---|---|
| 2.2 | 무한원 소실점 (c=0) | S-1 화면 밖 VP, S-2 화면평행 |
| 2.3 | 1/2/3점 투시는 하나의 이론 | S-1 누산기 |
| 5.3 | 자유도 회계 | S-1 상태 표시 |
| 6.2 | f² = \|PV₁\|\|PV₂\| | S-1 2점 해 |
| 6.3·6.4 | 주점 = 수심 | S-1 3점 해 |
| 6.5 | 예각 조건 (둔각이면 f²<0) | S-1 실시간 유효성 |
| 7.7 | 실척으로 읽히는 방향 | S-1 부분 확정 상태 작도 |
| 15.1 | 경사 소실점은 대응 수평 VP의 바로 위/아래 | S-6 축 밖 획 |
| 16.2 | 주점 = 이미지 중심 가정 | S-1 2점 경로(가정임을 명시) |
| 18.4 | 화각 임계 | S-1 구도 경고 |
