# SKETCH2SPACE — 작업 지침 (CLAUDE.md)

투시 스케치 3D 도구. **현행 계획서는 [`docs/line_plan.md`](docs/line_plan.md) 하나다**
(코드명 L, 2026-08-15 전환 / **개정 2, 2026-08-15**). 이론 근거는
[`docs/perspective_theory.md`](docs/perspective_theory.md).
폐기된 계획서는 `docs/archive/`에 사유와 함께 있다(S·G, 그리고 **개정 1 = `line_plan_r1.md`**).

**제품 정의**(개정 2 §1): **투시도와 입체를 동시에 보면서 실시간으로 입체를 생성한다.**
두 표상이 **동등**하고, 어느 쪽을 조작해도 다른 쪽이 즉시 따라온다.
3D는 부가 기능이 아니다 — 입체가 목적이고 투시도는 그 입력이자 동시에 출력이다.
구현 형태는 여전히 **입력이 손 획인 SketchUp 선 도구**이고,
처음에는 3D가 없이 **그린 선 몇 개가 카메라를 정한다.**

**공간과 투시를 이해하는 사용자를 전제한다.** 초보자용 가드레일을 두지 않는다 —
가이드를 맞추는 것도 축 각도를 트는 것도 이 사용자에게는 자연스럽다.

```
빈 화면에서 투시도를 그린다 (아직 2D)
  → 소실점이 잡히면 카메라 확정, 그때까지의 획이 3D로 올라간다
  → 이후는 실시간 — 시작점 스냅, 축 추론, 미리보기
  → 소실점이 하나 더 잡히면 차수가 승격되고 전부 다시 풀린다
```

> **2026-08-17 지시 1(D-L53)**: 남는 승격은 **P2 → P3**(기울어진 세로 모서리의 수직축 답) 하나다.
> P1(가로선 확정)은 불가역이다 — 잘못 그었으면 처음부터 다시 그린다.

사용자는 계속 **종이에 투시도를 그린 것**이다. 3D 공간을 조작한 적이 없다.
전환이 무변화이므로 그 순간을 인지하지 못한다.

**고유한 것은 둘뿐이다** — ① 초기 2D → 카메라 확정 ② 투시 차수 승격.
나머지(스냅, 축 판정, 선 도구, 뷰포트, 편집)는 **SketchUp/Rhino의 검증된 메커니즘**이므로
**새로 설계하지 말고 그대로 따른다.**

**프리핸드 보존은 미룬다**(계획서 §1.1). 획이 직선 세그먼트가 되고 손떨림은 버린다.
남는 것은 시작점과 끝점 둘뿐이다. **`pts2d`는 계속 보존하므로** 나중에 표현 계층으로 되돌아온다.

> 2026-08-15 세 번째 전환. 이전 접근(획을 사후 판정해 배치)이 실패한 원인은
> **기하 표현이 아니라 순서**였다 — 완성된 획을 나중에 판정했고, 시작점이 3D에 확정되지 않은 채
> 획 전체를 추론했고, 뷰포트를 나눠 좌표계가 둘이 됐다. 폐기 사유는
> [`docs/archive/segment_plan.md`](docs/archive/segment_plan.md) 머리말에 있다.

---

## 1. 자료구조

```
Stroke { id, pts2d[], seg3d, axis, snapStart, layer, color, width }
Camera { vps[], principal, f, fSource }
```

> **2026-08-17 지시 1(D-L53)**: `order`·`locked`은 **저장하지 않는다** — 차수는
> `perspectiveOrder(rules)`가 소실점 집합과 가로선 유무에서 매번 계산하고,
> "확정됐는가"도 계산이다(`standing()`). 상태는 넷뿐이다: NONE·P1·P2·P3.

- **`pts2d`를 반드시 보존한다.** 카메라가 재조정(차수 승격)되면 **전부 처음부터 다시 올린다**.
  부분 유지는 좌표계가 섞인 상태를 만든다(§6.1).
- `axis`: `VP1|VP2|VP3|screen|free`. **애매하면 미분류로 둔다.**
- **깊이 스케일의 출처는 `Camera.fSource` 하나다**
  (1점 f는 **임의값** — D-L53·지시 1 "깊이 배율일 뿐" · 2점 주점은 가정 — 이론서 16.2·16.4, AS-C4·AS-C5).
  ⚠ 2026-08-17 지시 3(D-L55)으로 fSource는 **화면에 안 나온다** — 자료구조와 `S2S` 진단에만 남는다.
- **소실점은 확정 후 잠긴다.** 획이 소실점 갱신에 기여하지 않는다 — **차수 승격만이 카메라를 바꾼다.**

---

## 2. 진행 규칙 — 무중단 자율 진행

L-A → L-B → L-C → L-D까지 멈추지 않고 완주한다. **중간 보고 없음. 확인 요청 없음. 시간 무관.**
컨텍스트가 소진될 때까지 최대한 많이 진행한다.

```
각 항목 **착수 시**:  tail -40 PITFALLS.md  → 그 항목에 걸리는 번호를 progress.md에 적는다
                     → **바로 아래 D(진단 규약) 다섯을 그 항목에 적용한다**
각 항목 **완료 시**:  테스트 → selfcheck → 리뷰어 → 지적 대응 → 커밋 → 다음 항목
```

### D. 진단 규약 — **증상에서 시작한다** (2026-08-21 · web2-03 지시 0)

> 같은 형태의 실수가 반복돼 사람이 매 지시에 적던 것을 여기 한 번 박는다.
> **자리를 여기로 고른 이유**: 착수 시 읽는 명령이 바로 위 줄이다. 뒤로 밀면 안 읽힌다
> (PITFALLS #55가 그 사례다 — 표를 파일 끝으로 옮겨야 읽혔다).
> `PITFALLS.md`의 「최근 다섯」 표 머리에도 이 절을 가리키는 줄이 있다.

- **D-1. 원인 후보를 받아도 그것부터 보지 않는다.** 증상이 나오는 **경로 전체에 표식을
  심고** 어디서 갈리는지 낸다. **표식이 나오기 전에 수리하지 않는다.**
  `strokeTrace`가 그렇게 해서 첫 실행에 원인을 잡았다 — 그때 사람이 준 후보 셋은 전부 틀렸다.
- **D-2. 보고된 증상을 먼저 재현한다.** 팔이 **수리 전에 실패하고 수리 후에 통과하는 것**을
  보인다. **재현 못 하면 「재현 못 했다」고 적고 고치지 않는다.**
- **D-3. 새 검사에는 반증 조건을 붙인다.** 그 검사가 **실패하는 조건**을 함께 적고
  **실제로 실패시켜 본다.** 실패 못 시키면 그 검사는 아무것도 안 잰다.
  ⚠ 「버그를 되살려 잡는지 확인한다」(A-4)는 **되살릴 버그가 있을 때만** 걸린다 —
  새로 만든 검사에는 반증 조건이 **따로** 필요하다. 불변식 k가 그 경우였다
  (작도 시점 0.000000 px는 f가 무엇이든 그렇다 — CLAUDE.md §5.1 자기참조 유형 3).
- **D-4. 사람이 준 근거는 짐작일 수 있다.** **확인 대상이지 결론이 아니다.**
  측정이 다르게 가리키면 **측정을 따르고** 그 사실을 `assumptions.md`에 반증으로 남긴다
  (⚙️ `web2/` 라인도 **`assumptions.md`가 그 자리다** — 그 표의 AS-* 행은 앱 전체에 걸린다.
   회차 서술은 `web2/NOTES.md`에 적더라도 **가정이 뒤집히면 그 행을 고친다**.
   2026-08-21에 AS-C5가 그렇게 고쳐졌다).
  지금까지 폐기된 것들이 그 형태였다 — 거리점 · 렌즈 슬라이더 · 지면 앵커 · 화각 상한.
- **D-5. 픽스처가 실제 사용 대역을 덮는지 확인한다.** 테스트가 통과하는데 실사용에서
  걸리면 **픽스처가 좁은 것**이다. 그리고 **같은 형태가 다른 곳에도 남아 있다** — 하나를
  찾으면 같은 축(거리·각도·크기·개수)으로 다른 픽스처도 훑는다.

⚠⚠ **명령을 못 박은 이유**(17차 후속 · #55): "PITFALLS.md를 읽는다"만 적혀 있어서
17차가 `tail -80`으로 읽었고 **당시 상단에 있던 「최근 다섯」 표를 지나쳤다**(그 80줄이
우연히 #49~#53이라 결과만 같았다). 표를 **파일 끝으로 옮기고** 명령을 여기 적는다 —
파일이 길어질수록 `tail`이 자연스러운 읽기이므로 **유인을 이기려 하지 말고 그 자리에 둔다**(A-3).

**`PITFALLS.md`를 읽는 시점은 착수 시다.** 완료 시에 읽으면 이미 어긴 뒤다 —
L-A.2가 그 목록의 셋(#12 동작점 하나·#14 시드 변동폭·#16 분모)을 그대로 어겼고,
그때 그 목록은 `HANDOFF.md` 안에 있어서 **읽히지 않았다.**
읽었다는 증거는 **그 항목에 걸리는 번호를 `progress.md`에 적는 것**이다. 적지 않으면 안 읽은 것으로 본다.
⚙️ **`web2/` 라인은 그 자리가 `web2/NOTES.md`의 각 절 「착수 시 PITFALLS 대조」다**
(2026-08-21 · web2-02 2차 리뷰어가 문면과 실제가 갈린 것을 잡았다). 파일이 다를 뿐 규약은 같다 —
**어느 쪽이든 번호를 적지 않으면 안 읽은 것으로 본다.**

**각 항목은 브라우저에서 열리는 상태로 커밋한다.**

### ⛳ 마감 — **배포까지가 한 항목이다**(2026-08-21 · PITFALLS #58)

```
작업 갈래에 커밋  →  검증(단위·e2e·타입·빌드)  →  main 병합·푸시
                 →  Pages 초록  →  **화면의 빌드 식별자 == 그 커밋**  →  보고
```

- **갈래 푸시는 중간 상태다.** `main` 푸시가 배포다 — 그 전까지 사람 손에는 옛 앱이 있다.
  (지금 배포되는 것은 `web2/`다. `pages.yml`의 working-directory가 정본.)
- **판정자는 화면의 빌드 식별자**다(우하단). HEAD 해시와 같아야 배포된 것이고,
  다르면 아직 안 퍼졌거나 캐시에 묶인 것이다 — `?reset`이 둘을 가른다.
- **완료 보고에 그 해시를 적는다.** 검증 수치만 적힌 보고는 미완이다
  ("검증 통과"는 저장소 안의 사실이고 배포는 저장소 밖의 사실이다 — #32의 배포판).
- ⚠ 실행 환경 지침이 «지정 갈래에만 푸시하라»고 할 때가 있다. 그때도 **마감은 병합·푸시까지**다 —
  갈래에서 멈추면 규칙을 지킨 것처럼 보이지만 결과는 «안 고친 것»과 같다(#58이 그 사례다).

**중단하는 유일한 경우: L-A 게이트 실패.**
~~상자 하나를 그리면 확정되고, 전환 시 화면이 안 튀고, 연결된 획이 전부 놓이는가.~~
**2026-08-15 그 게이트에서 실패했다**(조용히 틀린 배치 0.641). 원인은 카메라이고
대응은 계획서 §5.4다 — 검출을 초안으로 쓰고 사용자가 소실점을 맞춘다.

**새 게이트(L-A.7 재등록): 사람이 소실점을 맞춰 도달하는 실측 축 방향 오차가 0.5° 이하인가.**
(⚠ 2026-08-17 지시 2로 가이드 끌기 경로가 삭제됐다 — 판정은 종전대로 `camera_gate.json`의
각도 섭동 행이 한다: 실측 0.5°에 대응하는 `deg_0.25` 행이 통과선 안이다, PITFALLS #41의 확인 그대로.)
기준값은 **같은 하네스**(`camera_gate.json@f351839a`)의 무오차 행 —
조용히 틀림 **0.0673** · 형태 오차 중앙 **0.0296**. 통과선은 각각 1.1배·2.1배 이내이고
**5구도 전부**를 낸다(기준 구도 하나로 통과를 주장하지 않는다).
_초판은 `lift_gate`의 0.079를 대역으로 썼는데 그것은 **검출 오배정이 섞인** 값이었다(PITFALLS #27)._
통과 못 하면 멈추고 보고한다. **그 외에는 A-2대로 우회한다.**

### A-2. 막혔을 때 — 멈추지 말고 우회한다
1. **재시도** — 같은 접근으로 최대 3회, 매번 다른 가설로
2. **우회** — 더 단순한 구현으로 대체. 사실과 원래 목표를 `DEFERRED.md`에 기록
3. **격리** — 해당 기능을 끄고 나머지 진행
4. **다음 항목으로** — 어떤 경우에도 넘어간다

### A-3. 판단 규칙 (사람에게 묻지 않는다)
- **선례가 있으면 선례를 따른다.** SketchUp/Rhino가 하는 방식을 개선하려 하지 않는다.
  고유한 것은 §1.2의 둘뿐이다
- 측정이 가리키는 방향을 우선한다 — **사람이 준 근거를 포함해서다**(D-4가 정본).
  사전 예상과 어긋나면 예상을 `assumptions.md`에 반증으로 기록한다
- 선택지가 여럿이면 **가장 단순한 것**을 고른다. 복잡한 대안은 `DEFERRED.md`
- 범위를 넓히지 않는다
- **애매하면 놓지 않는다. 조용히 틀린 배치를 만들지 않는다.**
  **놓지 않되 버리지 않는다** — 미배치는 **실패가 아니라 대기 상태**다(개정 2 §9.1).
  2D 레이어에 남고 조건이 갖춰지면 승격하며, **승격은 연쇄한다**
- 선택 근거와 **기각 사유**를 `progress.md`에, 잠정 결정은 `DECISIONS.md`에

### A-4. 품질 하한 (타협하지 않는다)
- 기존 테스트가 깨진 채로 다음 항목에 가지 않는다.
  ⚙️ **항목마다 도는 것은 `npm run test:unit`이다**(2026-08-18 9차 지시 d) —
  현재 규모·시간은 **`stage0/out/test_cost.json`의 `totals.unit`이 든다**(13차에 39파일·
  382건 — 수치는 원장이 정본, 여기 수는 낡는다 #47). 전량(`npm test`)의 벽시계는
  `camera_gate.test.ts` **한 파일**이 정한다(현재값은 `by_file` 최상단이 정본. 9차
  108.7s에서 크게 늘었고 그 증가는 부하 몫이 아니었다 — progress 13차 [6]). 한 시간짜리 검증은
  항목마다 못 돌리므로 A-4가 실행 불가능해진다 — **그래서 갈랐다.**
  **측정 하네스(`npm run test:measure`)는 그 항목이 건드린 원장이 있을 때 돌린다.**
  ⚠ **갈래는 이름이 아니라 행동으로 정해진다** — `stage0/out`에 쓰면 측정이다
  (`web/tools/testSplit.mjs`). 새 하네스는 자동으로 분류되고 목록을 손으로 안 고친다.
  ⚠⚠ **느려졌다는 보고를 받으면 디스크 여유부터 본다** — 9차에 전량이 **121s → 1h52m**
  (59배)이었고 원인은 하네스가 아니라 **여유 공간 0**이었다. `stage0/out/test_cost.json`의
  `environment.disk_free_bytes`가 그 자리다
- 새 기능마다 테스트를 쓴다(**반례 테스트 포함**) — 반증 조건은 **D-3**이 정본이다
- selfcheck를 매 항목 돌린다 / 리뷰어를 매 항목 호출한다
- 커밋을 잘게 나눈다 / `progress.md`를 매 항목 갱신한다
- **각 항목 끝에서 브라우저로 열리는 상태여야 한다**
- **참조 저장소 코드를 복사하지 않는다**(아래 §3)
- **새 임계는 `web/test/constants.ts`에 넣는다**(D-C4). 빠지면 STALE이 안 잡힌다
- **팔·스윕 점을 더하면 비용을 원장에 남긴다**(9차 지시 c) — `npm run test:cost`가
  `stage0/out/test_cost.json`을 낸다(파일별 시간·갈래별 합계·하네스가 스스로 적은 실행 수).
  9차 한 세션이 팔 넷을 더하고 **비용을 아무 데도 안 적었다**
- **dpr 1에서만 확인하지 않는다**(D-C3)
- **회귀 테스트는 버그를 되살려 실제로 잡는지 확인한다.**
  ⚠ 이것은 **되살릴 버그가 있을 때만** 걸린다 — 새로 만든 검사는 **D-3**(반증 조건)을 따른다
- **폐기 코드를 지우기 전에 게이트를 통과시킨다** — 옛 계층을 남겨 둔 덕에 되돌릴 수 있었다

### A-1. 세션 인계
컨텍스트가 차면 `HANDOFF.md`를 갱신하고 종료한다. 다음 세션은 그 한 줄로 시작한다:
`"HANDOFF.md를 읽고 이어서 진행하라."` **이 문장이 유일한 사람 개입이다.**

### A-5. 완주 조건
L-D가 끝나면 `README.md`·`PROTOTYPE.md`·`DEFERRED.md`·`DECISIONS.md`·`HANDOFF.md`(완료)를
갱신하고 보고한다.

---

## 3. 참조 저장소 — 코드 복사 금지

`notebysebas/sketchbook-v8` (Sketch3D). three.js r128, 단일 HTML 12,833줄, PWA.

**LICENSE 파일이 없고 소스에 저작권 표시도 없다.** 라이선스 없는 공개 저장소는
모든 권리 유보다. GitHub 약관(D.5)이 열람·포크까지는 허용하지만
**수정·재배포·제품 편입은 허용하지 않는다.**

- 동작 확인과 접근 이해 용도로만 본다
- **코드를 복사하지 않는다.** 변수명·함수 구조를 그대로 옮기지도 않는다
- 필요한 것은 전부 표준 구현이므로 직접 짠다

SketchUp/Rhino를 **선례로 따르는 것**(A-3)은 이 금지와 다르다 —
따르는 것은 **동작 규약**(스냅 목록·우선순위·수정자 키)이지 코드가 아니다.

---

## 4. 리뷰어 서브에이전트

`.claude/agents/reviewer.md`. **각 항목 완료 시** 호출한다.
**리뷰어는 `PITFALLS.md` 대조를 반드시 낸다** — 이번 작업이 어느 항목에 해당하는지,
해당 없으면 없다고. 문서를 두는 것만으로는 안 읽혔기 때문에 **검토가 확인을 강제한다**. 지적이 나오면 대응 후 재호출,
2회 반복 후에도 남으면 `DEFERRED.md`에 올리고 진행한다.

리뷰어는 **코드를 보지 않는다**(문서·수치만). 이 프로젝트에서 세션 밖에서 잡힌 결함이 전부
**"테스트 통과"에 설득당하지 않는 위치**에서 나왔기 때문이다 — 마우스 등급 판정의 단위 혼동,
이론서 8.8이 결론을 뒤집은 것, 4.6%가 PCA 프레임 값이었던 것,
**게이트 값 0.692가 이전 실행의 수치였던 것**.

---

## 5. 자기검증 규칙

1. **의심 수치 자동 점검** — `python selfcheck.py`가 `stage0/out/*.json`을 스캔한다.
   1e-10 미만 오차 / 정확히 1.0·0.0 비율 / 이전 대비 완전 불변 / 0 고정 카운터 /
   단일 범주 분포 / **분포 전체가 한 값** / 비결정 시드 정적 탐지(`.py`의 `hash(str)`, `.ts`의 `Math.random`) /
   **복원↔역연산 왕복 지표**(자기참조 유형 3 — 오차류 이름의 함수가 스스로 변환을 수행하면
   그 값은 측정이 아니라 **설계 보장**일 수 있다. 보장이면 원장에 그렇게 적고 **임계를 걸지 않는다**).
   의심≠오류 — 각 플래그의 원인을 확인한다.
   **측정은 반드시 `stage0/out/`에 JSON으로 남긴다.** 원장 밖 측정은 규칙이 있어도 안 걸린다.
   **STALE 탐지** — 측정 하네스는 `constantsSnapshot()`을 산출물에 넣고, selfcheck가
   `stage0/out/constants.json`(현재 값)과 대조한다. 다르면 그 산출물은 **낡았고 그것을 인용한
   문서도 낡았다**. **인용 규약**: 문서가 수치를 인용할 때 `산출물.json@해시`로 적는다.
2. **assumptions.md** — 매 항목 관측과 대조.
3. **새 지표엔 반례 테스트** — 그 지표가 의도한 것을 재는지 확인한다.
   ⚙️ 반증 조건의 정본은 **§2 D-3**이다(어떻게 확인하는지까지 적혀 있다).

> **수치를 문서에 쓰기 전에 원장 JSON을 그 자리에서 다시 읽는다.** 측정을 재실행했으면
> 그 값을 인용한 문서를 **전부** 찾아 고친다(`grep`으로 확인).

**재현성**: 시드에 `hash(str)`(py)·`Math.random`(ts)을 쓰지 않는다. `stable_seed` / `rng32(seed)`.
시드 변동폭 때문에 **유효 자릿수는 2자리**로 본다. 비율보다 **분자/분모**로 적는다.

---

## 6. 디렉토리 구조

```
SKETCH2SPACE/
  CLAUDE.md HANDOFF.md DECISIONS.md DEFERRED.md progress.md assumptions.md
  docs/
    line_plan.md           현행 계획서 (코드명 L)
    perspective_theory.md  이론서
    archive/               폐기된 계획서 5종 + 폐기 사유, 구 원장(*_SG.md)
  web/                     본체 (TypeScript + Vite + three.js)
    src/capture/           잉크 캡처 (Pointer Events, 마우스 1급, 팜 리젝션)
    src/s3d/               카메라·소실점 검출·초기 해·스냅·선 도구·3D 기하
    src/ui/                화면
    src/parser/            geometry/linalg/grade — 유지되는 원시 유틸
    src/data/              Quick,Draw 실획 로더
    test/                  vitest
  stage_perspective/       카메라 수학 (Python 기준 구현 — TS 이식의 정답)
  stage3/ common/ stage0/  합성 장면 · 획 원시처리 · 통계 하네스와 원장
  selfcheck.py  tests/
```

---

## 7. 이론서 참조 지도

| 절 | 내용 | 쓰는 곳 |
|---|---|---|
| 2.2 | 무한원 소실점 (c=0) | L-A 화면 밖 VP, 화면평행 축 |
| 2.3 | 1/2/3점 투시는 하나의 이론 | L-C 차수 승격 — **분기가 아니라 개수** |
| 5.3 | 자유도 회계 | L-A 상태 표시 |
| 6.2 | f² = \|PV₁\|\|PV₂\| | L-A 2점 해 |
| 6.3·6.4 | 주점 = 수심 | L-A 3점 해 |
| 6.5 | 예각 조건 (둔각이면 f²<0) | L-A 실시간 유효성 |
| 7.7 | 실척으로 읽히는 방향 | L-A 부분 확정 상태 |
| 15.1 | 경사 소실점은 대응 수평 VP의 바로 위/아래 | L-B 축 밖 획 |
| 16.2 | 주점 = 이미지 중심 가정 | L-A 2점 경로(가정임을 명시) |
| 18.4 | 화각 임계 | L-A 구도 경고 |
