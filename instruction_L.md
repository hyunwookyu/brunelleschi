접근을 전환한다. line_plan.md를 저장소에 편입하고 docs/line_plan.md 로 두어라.
저장소는 기존 것을 유지한다. 코드 재사용이 크고 git 이력이 필요하다. 새 폴더를 만들지 않는다.
docs/sketch3d_plan.md와 docs/segment_plan.md를 대체한다. docs/perspective_theory.md는 유지한다.

## 전환 요지

만드는 것은 입력이 손 획인 SketchUp 선 도구다. 다만 처음에는 3D가 없고, 그린 선 몇 개가 카메라를 정한다.
세그먼트 게이트에서 배치율은 올랐으나(0.419 → 0.726) 조용히 틀린 배치가 4.9배 악화됐고 오류가 지지 1에 몰렸다(30.1%).
원인은 기하 표현이 아니라 순서였다.

* 완성된 획을 사후 판정 → 사용자가 결과를 나중에 본다
* 시작점이 3D에 확정되지 않은 채 획 전체를 추론 → 오차원이 하나 더
* 뷰포트를 나눠 좌표계가 둘 → 돌린 시점 사선 오배정 79%

SketchUp에 이 문제들이 없는 이유는 알고리즘이 아니다. 시작점이 스냅으로 확정되어 있고, 사용자가 그리는 동안 결과를 본다.
고유한 것은 둘뿐이다 — 초기 2D → 카메라 확정, 투시 차수 승격. 나머지(스냅, 축 판정, 선 도구, 뷰포트, 편집)는 SketchUp/Rhino의 검증된 메커니즘이므로 새로 설계하지 말고 그대로 따른다.
프리핸드 보존은 미룬다. 획이 직선 세그먼트가 되고 손떨림은 버린다. pts2d는 계속 보존하므로 나중에 표현 계층으로 되돌아온다.

## L-A. 정리 + 초기 확정 (1세션 규모)

1. 폐기 (line_plan.md §8.2) 배치 계층 전부(앵커 체인, 정합성 검사, 깊이 타당성, 고치기 후보 탐색), segment.ts, 2D/3D 분리 뷰포트, 소실점 투표 갱신, 튜브 메쉬의 곡선 보존(직선 렌더로 단순화), 돌린 시점 전용 임계. 측정 결과는 docs/archive/에 보존한다. 코드만 제거한다.
2. 문서 정리 (§8.3) progress.md, DECISIONS.md, DEFERRED.md, assumptions.md를 docs/archive/로 옮기고 새로 시작한다. 유효한 항목만 옮겨 적는다. 축적된 D-S1~D-S24, AS-1~AS-15 대부분이 무효화됐고, 새 세션이 그것을 읽으면 판단이 흐려진다.
3. 소실점 후보 검출 (§5.1) 같은 방향 클러스터의 선 3개 이상이 한 점에서 만나면 확정. 두 개는 교점이 반드시 생기므로 정보가 없다. 산포 임계는 화면 대각 비율로 넉넉히. 정밀도가 필요한 값이 아니다 — 승격 시점만 정하고 카메라는 최소제곱으로 다시 푼다.
4. 초기 3D 올리기 (§5.2) 순서대로 하나씩 올리지 않는다. 첫 획이 앵커가 되면 초기 오류가 고착되고, 그것이 세그먼트 게이트의 지지 1 문제였다.
확정 순간 → 모든 획을 축별로 분류 → 화면상 교점을 전부 찾는다 (2D에서는 확실하다) → 교점 관계를 제약으로 3D를 한 번에 푼다 → 스케일 자유도 하나만 남으므로 임의로 정한다
첫 획의 깊이를 가정할 필요가 없다. 연결되지 않은 획은 미배치로 두고 화면에 남긴다.
5. 전환 무변화 측정 (§5.3) 카메라가 맞았다면 전환 순간 화면이 똑같아야 한다. 전환 전후 픽셀 차이를 지표로 삼는다. 튀면 카메라가 틀린 것이다.

L-A 게이트: 상자 하나를 그리면 확정되고, 전환 시 화면이 안 튀고, 연결된 획이 전부 놓인다. 통과 못 하면 멈추고 보고한다.

## L-B. 실시간 선 도구 (1.5세션 규모)

1. 스냅 (§3) — Rhino Osnap / SketchUp inference를 그대로 따른다 대상: 끝점 · 중점 · 교차점 · 선 위 근처점 · 수선 발 · 정점 · 면 위 점 우선순위: 끝점 > 중점 > 교차점 > 수선 발 > 선 위 근처점 > 면 위 점 표시: 종류별 아이콘과 색, 툴팁 반경: 손으로 그으면 클릭보다 어긋나므로 넉넉히. 측정으로 정한다.
획의 첫 점이 스냅되면 3D가 확정된다. 그 뒤 커서 픽셀 하나가 끝점을 유일하게 결정하므로 추론할 것이 없다. 이 접근의 핵심이다.
2. 축 판정과 실시간 색 (§4) 카메라 확정 후에는 볼 것이 없다 — 커서 방향과 축 후보의 각도 비교. 판정된 축의 색으로 미리보기 선을 그린다. 사용자가 그으면서 보고 각도를 튼다.
3. 미리보기 선, 스냅 하이라이트 획을 놓기 전에 결과가 이미 보여야 한다.
4. 축 고정 수정자 (Shift 등)
5. 뷰포트 통합, 궤도 후 계속 그리기 (§7) 뷰포트는 하나다. 궤도 회전은 카메라 조작이고 좌표계는 계속 같다. 소실점은 잠긴다 — 차수 승격만이 카메라를 바꾼다.

L-B 게이트: 시작점 스냅 성공률, 스냅된 시작점에서의 배치 정확도, 미리보기와 확정의 일치(0이어야 함).

## L-C. 차수 승격 (0.7세션 규모)

2점 투시를 그리려면 한 소실점이 먼저 잡히고 다음이 나중에 잡힌다. 그 사이 시스템은 1점 투시로 읽는다.

1. 승격 검출과 전체 재계산 (§6.1) 전부 다시 푼다. 부분 유지는 좌표계가 섞인 상태를 만든다. 원본 pts2d가 보존되어 있으므로 새 카메라로 처음부터 올린다. 사용자 지정 스냅만 유지를 시도하고, 성립하지 않으면 알린다.
2. 재투영 어긋남 자동 검사 (§6.2) 승격은 정보가 느는 방향이므로 원본 획과 재투영의 어긋남이 줄어야 한다. 늘면 승격이 잘못된 것이다.
3. 되돌리기 (§6.3) "1점으로 되돌리기" 버튼. 임계를 정교하게 만드는 것보다 싸다.

## L-D. 마무리 (0.8세션 규모)

1. 미배치 처리 — 화면에 남기고 지정 경로
2. 저장·내보내기 갱신 (세그먼트 구조 반영)
3. 종단 검증 (Playwright)
4. 문서 — README, PROTOTYPE, DECISIONS, DEFERRED

## 진행 규칙 — 무중단 자율 진행

L-A → L-B → L-C → L-D까지 멈추지 않고 완주한다. 중간 보고 없음. 확인 요청 없음. 시간 무관. 컨텍스트가 소진될 때까지 최대한 많이 진행한다.
각 항목 완료 시: 테스트·selfcheck 실행 → 리뷰어 호출 → 지적 대응 → 커밋 → 다음
중단하는 경우는 L-A 게이트 실패 하나다. 그 외에는 A-2대로 우회한다.
A-1 세션 인계 컨텍스트가 차면 HANDOFF.md를 갱신하고 종료한다. 다음 세션이 그것만 읽고 이어갈 수 있어야 한다.
A-2 막혔을 때 재시도 3회 → 우회(더 단순한 구현) → 격리 → 다음 항목.
A-3 판단 규칙 선례가 있으면 선례를 따른다. SketchUp/Rhino가 하는 방식을 개선하려 하지 말 것. 고유한 것은 §1.2의 둘뿐이다. 측정 우선. 예상과 어긋나면 측정을 따르고 assumptions에 반증 기록. 선택지가 여럿이면 가장 단순한 것. 범위를 넓히지 않는다. 애매하면 놓지 않는다. 조용히 틀린 배치를 만들지 말 것. 놓지 않되 버리지 않는다 — 미배치는 화면에 남긴다.
A-4 품질 하한 테스트 깨진 채 다음 항목으로 가지 않는다. 새 기능마다 테스트. 반례 포함. 각 항목 끝에서 브라우저로 열리는 상태여야 한다. 참조 저장소(notebysebas/sketchbook-v8) 코드를 복사하지 않는다. 새 임계는 constants.ts에 넣는다. dpr 1에서만 확인하지 않는다. 회귀 테스트는 버그를 되살려 실제로 잡는지 확인한다. 폐기 코드를 지우기 전에 게이트를 통과시킨다 — 이전에 옛 계층을 남겨둔 덕에 되돌릴 수 있었다.
A-5 완주 조건 L-D가 끝나면 README.md, PROTOTYPE.md, DEFERRED.md, DECISIONS.md, HANDOFF.md(완료)를 갱신하고 보고한다.
