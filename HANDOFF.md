# HANDOFF.md — 세션 인계

다음 세션은 이 문서를 읽고 이어서 진행한다. 사람의 개입은 그 한 줄뿐이다.

## ⚠ 먼저 — 저장소 루트에 **`line_plan_r2.md`**(계획 개정 2)가 있다

이번 세션 중에 나타났고 **본체가 만든 것이 아니다.** "투시도와 입체를 동시에 보면서
실시간으로 입체를 생성한다"로 §1·§5·§7을 개정하고 **뷰 시스템(§9)·UI 재작성(§10)**을 더한다.
**아직 반영하지 않았다** — 사람이 지시하기 전에는 `docs/line_plan.md`가 현행이다.
사람에게 확인한 뒤 편입 여부를 정한다.

## 현재 단계 — **L-A.7 완료, L-B 착수 전**

**2026-08-15 세 번째 전환(코드명 L).** 현행 계획서는 [`docs/line_plan.md`](docs/line_plan.md) 하나.
S·G는 폐기 사유를 달아 `docs/archive/`에 있고 **아카이브를 판단 근거로 인용하지 않는다**(D-L1).

### 이번 세션이 한 것

| 항목 | 결과 |
|---|---|
| L-A.1 문서 전환 | 계획서·원장 아카이브, 유효 항목만 이관 |
| L-A.2 소실점 검출 | `vpDetect.ts`. 계획서 문장을 셋 고쳤다(D-L2·D-L3·D-L4·D-L7) |
| L-A.3 초기 3D 올리기 | `lift.ts`. **미지수가 획마다 하나인 동차 선형계** — 앵커도 씨앗도 없다 |
| L-A.4 게이트 | **실패**. 올리기는 맞고 카메라가 틀렸다(조용히 틀림 0.079 대 0.641) |
| 계획서 정정 | **§5.3·§6.2 폐기** — 입사는 사영 불변이라 f 오차를 못 잡는다 |
| L-A.5d·L-A.6 | **임계 = 축 오차 ≈1°**. 획을 늘려도 검출은 4.5~10°(AS-L10 반증) |
| L-A.5abc | 소실점 초안·조정 UI. `liftAll`을 앱에 배선 |
| L-A.7 | 동차(각도) 파라미터화 + 무한원 스냅. **리뷰어 18건 대응** |

### 지금 앱이 하는 일 (브라우저에서 확인함)

```
그린다(2D) → `소실점 추정` → 축마다 가이드 선 두 개(검출이 초안)
           → `소실점 조정`으로 끝점을 끈다 → 그리드가 실시간으로 따라온다
           → `카메라 잠금`(= 확정) → 획이 축별로 재분류되고 3D로 올라간다
```
참 소실점 `[1280,148] [−262,148] [245,2948]` 상자 12획으로 **2점 12/12 · 3점 12/12 배치**.

## ⚠ 반드시 먼저 읽는다

1. `CLAUDE.md` — 작업 지침. **§2에 게이트가 바뀌어 있다**
2. **`PITFALLS.md`** — 반복해서 걸린 것 26개.
   **읽는 시점은 항목 착수 시**이고, 걸리는 번호를 `progress.md`에 적는 것이 읽었다는 증거다
3. `docs/line_plan.md` — §5.4·§5.5가 이번에 생겼고 §5.3·§6.2가 폐기됐다
4. 이 문서
5. `progress.md` · `DECISIONS.md`(D-L1~D-L8) · `assumptions.md`(AS-L1~L10, AS-C1~C10)

## 이번 세션이 남긴 세 가지 사실

**① 올리기는 맞고 카메라가 병목이다.** 참 카메라를 주면 조용히 틀린 배치 **0.079**에
배치율 **0.995**다(S 0.057/0.846 · G 0.278/0.887). 검출 카메라로는 **0.641**.
지배항은 f가 아니라 **3D 축 방향 오차**(검출 중앙 4.18°). `lift_gate.json@1a6f104b`.

**② 계획서의 자동 판정 신호 둘이 원리적으로 성립하지 않는다.**
§5.3의 전환 픽셀 차이도 §6.2의 재투영 어긋남도 **f를 ±20% 틀리게 줘도 안 움직인다** —
입사 관계는 사영 불변이다. 대체로 시도한 교점 잔차도 같다. 둘 다 **설계 보장**이다.
→ **사람의 눈이 유일한 판정 수단**이고 그것이 §5.4의 근거다.

**③ 임계는 실측 축 오차 ≈0.5°이고 자동 검출은 거기 못 간다.**
_초판은 ≈1°였고 리뷰어 지적으로 정정했다 — 대역을 다른 하네스에서 가져왔고(PITFALLS #27)
등록 지표를 바꿨다(#28)._ 같은 하네스 무오차 행(0.0673 / 0.0296) 기준으로
실측 0.48°에서 1.03배/2.07배, 0.96°에서 1.24배/3.67배, 1.92°에서 7.5배/6.9배.
획을 축당 4 → 18 → 48 → 100으로 늘려도 검출은 **7.40 / 10.04 / 10.09 / 4.53°**다.
→ **자동 확정 경로를 두지 않는다.** 검출은 초안이고 사용자가 항상 확정한다.
`camera_gate.json@1a6f104b`.

## 다음에 할 것

### 먼저 — **§5.4 게이트를 실사용에서 확인**
새 게이트(CLAUDE.md §2, L-A.7 재등록): **사람이 끌어 도달하는 실측 축 오차가 0.5° 이하인가.**
기준값은 **같은 하네스**(`camera_gate.json@1a6f104b`) 무오차 행 — 조용히 틀림 0.0673 ·
형태 오차 중앙 0.0296. 통과선 1.1배·2.1배. **5구도 전부**를 낸다.
확인 수단은 Playwright로 핸들을 끌어 축 오차를 재는 종단 시나리오(L-D)이거나 실획 표본이다.
자동화가 어려우면 **실사용 대기**로 DEFERRED에 남긴다(AS-L9).

⚠ **핸들 예산이 좁다 — 이것이 §5.4의 실제 한계다.** 축 오차 0.5°를 다 쓰기까지 핸들이
움직여도 되는 px가 **가이드 140px에서 0.29~1.01(서브픽셀)**, **525px에서 1~4**다
(`vp_homog.json@1a6f104b`). `min_guide_ratio`를 0.12 → **0.45**로 올렸으나
**브라우저(490×672)에서는 캔버스 여백에 잘려 304~358px이 한계**다.
작은 캔버스에서는 예산을 늘릴 방법이 없다 — L-B 뷰포트 통합에서 다시 본다.

L-A.7이 더한 것: 동차 표현(`v = ℓ₁ × ℓ₂`, 나눗셈 없음)으로 **표현의 발산은 없앴다**
(각차 60→0.5°에서 위치 12,600배 대 각도 13배). **그러나 카메라는 여전히 발산한다**
(f 26배, 축 0.115 → 1.10°/px) — 재매개화는 아무것도 안 고친다(PITFALLS #24).
실제 대응은 **무한원 스냅**(각차 < 3°)이고 화면에 사유와 각차를 알린다.

### 그다음 — L-B (계획서 §9)
스냅 · 축 판정과 실시간 색 · 미리보기 · 축 고정 · 뷰포트 통합.
**L-C의 승격 자동 판정은 §6.2 폐기를 반영한다** — 사영 불변이 아닌 양(`tiltDeg` ·
소실점 삼각형 f² 편차)을 먼저 시험하고, 안 서면 사용자 확인(§6.3 되돌리기)으로 간다.

## ⛔ 아직 폐기하지 않은 코드

계획서 §8.2의 폐기는 **L-B 게이트 통과 뒤**다(A-4·사용자 지시).
- 첫 시점 배치는 이제 `liftFirstView`(→ `liftAll`)를 쓴다. **옛 `settle`/`placeInto`는 첫 시점에서
  더는 안 불린다.** 돌린 시점 경로(`VIEW_OPTS`)는 아직 쓴다 — L-B 뷰포트 통합에서 정리한다.
- `segment.ts`(G 실험) · `cameraFit.ts`(D-L6 기각) · 2D/3D 분리 뷰포트는 그대로 있다.

## ✋ 브라우저에서 보는 절차

```bash
cd web && npm run dev:5222
```
→ `http://localhost:5222`

⚠ **캔버스 크기 굳음이 세 번 재발했다**(AS-C7). `ResizeObserver`는 초기 콜백을 안 주고
`rAF`는 **숨은 탭에서 멈춘다**. 이제 `refresh()`가 `sizeStale()`로 **자가 치유**하지만,
확인은 여전히 **창을 앞에 두고** 한다.

브라우저에서 합성 상자를 넣어 보려면 **측정 하네스와 같은 픽스처**를 쓴다 —
`await import('/test/scene3d.ts')`. 손으로 "소실점 쪽으로 t만큼" 상자를 만들면
**화면에서 평행한 아핀 상자**가 나와 수렴이 0이다(이번에 실제로 걸렸다).

## 개발 명령

```bash
cd web && npx vitest run && npx tsc --noEmit && npx playwright test
```
```bash
python -m pytest tests/ -q && python selfcheck.py
```
⚠ `vitest`는 **`web/`에서** 돌린다. 저장소 루트에서 `--root web`으로 돌리면 원장 경로가
어긋나 2건이 실패한다.

검증 현황: vitest **218/218**(37파일) · tsc 0 · pytest 37/37 ·
selfcheck 플래그 1394 · **STALE 1**(`e2e.json` — Playwright 미실행) · 상수 해시 `1a6f104b`.

## 미해소

- **실획 표본이 여전히 0이다**(AS-C1). 모든 판정이 Quick,Draw 유도 합성 잉크 위에 있고
  AS-C10이 그 모델을 **양방향으로 틀렸다**고 적었다
- AS-L9: 사람이 축 오차 1°에 도달하는지 미측정
- 리뷰어 18건은 **전부 대응했다**(`progress.md`의 "L-A.7 + 리뷰어 대응"). **L-A.7 자체는
  아직 리뷰어를 안 거쳤다** — 다음 세션이 부른다
- **[13] 브라우저 확인이 형태 오차를 안 낸다** — 참값 대조를 앱에서 하려면 하네스를
  앱에 넣어야 한다. DEFERRED
- `line_plan_r2.md`(위) 처리
