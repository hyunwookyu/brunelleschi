# HANDOFF.md — 세션 인계

다음 세션은 이 문서를 읽고 이어서 진행한다. 사람의 개입은 그 한 줄뿐이다.

## 현재 단계 — **오스냅까지 섰다. 다음은 실획 표본이다**

계획서 [`docs/line_plan.md`](docs/line_plan.md) **개정 2**의 L-A ~ L-D가 전부 끝났고,
그 뒤 사람 지시가 넷 더 들어와 끝났다(2026-08-16):
**규칙 기반 소실점**(D-L37) · **축 스냅**(D-L42) · **지평선 드래그**(D-L45) · **오스냅**(D-L46).

프로그램 이름은 **Brunelleschi**, 저장 확장자는 **`.brnl`**(내용은 JSON).
배포본: <https://hyunwookyu.github.io/brunelleschi/>

## ⚡ 착수 전에 `PITFALLS.md` **상단 "최근 다섯"**부터 읽는다

목록이 **41개**다. 최근 다섯: **#41 #40 #39 #38 #37**.
읽었다는 증거는 **그 항목에 걸리는 번호를 `progress.md`에 적는 것**이다.

⚠ **이번 세션이 절차를 두 번 어겼다** — 다음 세션이 같은 자리에 안 걸리게 적는다:
1. **`web/progress.md`가 또 생겼다**(셸 작업 디렉토리가 `web/`일 때 `cat >>`를 했다).
   selfcheck는 그 파일을 **정확히 잡는다**(시험해서 확인했다) — 못 잡은 것이 아니라
   **커밋 전에 selfcheck를 다시 안 돌린 것**이다. → **`progress.md`를 적은 뒤 커밋 전에 selfcheck.**
2. **사전 등록 절을 `progress.md`에 안 뒀다**(게이트 문안이 하네스 소스에만 있었다).
   리뷰어가 L-D.2·L-D.3에서 이미 짚은 자리다(#26·#34).

## 다음에 할 일 — **실획 표본이 유일한 다음 단계다**

**모든 판정이 합성 잉크 위에 있고 AS-C10이 그 모델을 양방향으로 틀렸다고 적었다.**
`sessions/`가 비어 있는 한 아래가 전부 잠정이다:

1. **실획을 받는다** — 절차는 `README.md` 0절(앱에서 `획 내보내기` → `sessions/`).
2. `cd web && npx vitest run test/real_ink.test.ts` → AS-6 · AS-12 · 미배치율이 실획으로 나온다.
3. **그 뒤 임계를 재조정한다.** STALE 탐지가 하류 원장을 자동으로 표시한다.
4. **AS-L9를 잰다** — 사람이 **지평선을 몇 px 안에 놓는가**. ⚠ **이 물음이 바뀌었다**:
   옛 물음은 "소실점 핸들을 서브픽셀로 맞추는가"(도달 불가에 가깝다)였는데
   **D-L45가 그 자유도를 지평선 드래그로 옮겼고 창이 ±120px 이상**이다(`horizon_pitch.json`).
   **사람이 그 창에 드는지가 이 도구의 성패다.**
5. `QUESTIONS.md`의 **d·e·f**가 같은 표본으로 함께 답이 나온다
   (지평선이 수평인가 · 롤이 몇 도인가 · **축 스냅이 언제 이득으로 도는가**).

## 이번 세션(2026-08-16 이어서)이 한 것

| 항목 | 결과 |
|---|---|
| **main 병합**(사람 승인) | 규칙·축 스냅 브랜치를 fast-forward로 넣고 푸시. Actions `build`·`deploy` 초록, 사이트 갱신 확인(번들에 새 문자열) |
| **게이트 실패 해석** | 축 스냅 게이트 실패는 **기록만 한다**(사람 판정). 원인은 **카메라이지 축 스냅이 아니다**. `QUESTIONS.md` **f**에 실획 측정 항목을 등록했다 |
| **D-L45 지평선 드래그** | QUESTIONS **g**의 답. 기본 피치 상수를 바꾸는 대안을 기각했다. **3점 투시가 초기부터 선다** |
| **리뷰어 15건**(높음 4) | **넷 다 내 쪽이 틀렸다.** 도달 가능성에 판정 대상 값을 적었고(#40), 위약이 참에서 2d인 것을 "같은 크기"라 적었고, 순서 축이 등록에서 빠졌고, 오차 축이 세 점뿐이었다 → **허용 오차 곡선**을 넣었고 그것이 채택의 진짜 근거가 됐다 |
| **D-L44 `Shift` 통일** | 죽은 `"infer"`·`shiftHeld`를 지우고, 상태 줄이 광고하던 "Shift 추론 축"을 뺐다. README에 **조작 표**를 넣었다 |
| **D-L46 오스냅** | 정점 종류 · 종류별 아이콘/툴팁 · **끝점 스냅** · **양 끝이 붙으면 축 없이 두 점으로 배치**. 승격·저장·연쇄가 함께 간다 |
| **오스냅 리뷰어 15건**(높음 5) | **다섯 중 넷이 내 쪽이 틀렸다.** 게이트가 실패 불가로 등록됐고(통과선 0.19로 재등록·출처 명시), **"축 경로로는 0개 = 정의"가 실제로 틀렸으며**(판정 480회에 배정 **59**), **꺾인 획이 두 점 배치로 조용히 놓이는 문을 막았다**(굽음 가드). 남은 것은 `DEFERRED.md` |
| **종단 확인이 설계를 둘 고쳤다** | ① 끝점 스냅에서 **근처점을 뺐다**(라이노 기본값) — 안 그러면 모서리를 따라 그은 획이 전부 두 점 배치가 된다 ② 옛 단언 셋을 **새 계약으로** 갱신했다 |

## 지금 서 있는 사실 — **다음 세션이 먼저 알아야 하는 것**

### 1. 지평선이 **사람 손이 닿는 유일한 자유도**다
`horizon_pitch.json@509615a1`: 참 지평선에서 **±120px까지** 축 오차가 3.4~5.6°이고
중앙 지평선(참에서 268px)은 **21.23°**(몰아 긋기)·**12.26°**(번갈아 긋기)다.
⚠ **2026-08-17 A-4로 재측정한 값**이고 3점은 이제 지평선과 무관하다(D-L47).
소실점 핸들 예산은 **0.12~6.8px**이었다(`guide_budget.json`).
⚠ 창의 **바깥 끝은 안 쟀다**(120~268px 사이). ⚠ 참 팔은 **오라클**이다.

### 2. **같은 꼭짓점이 획마다 다른 3D 점이다**
브라우저 실측: 상자 하나(12획)에서 끝점 후보가 **24개**이고 하나도 안 합쳐진다.
같은 꼭짓점 간 거리가 **0.0305~0.1182·G**인데 병합 반경은 **0.002·G**다.
- **정점(`vertex`) 스냅이 한 번도 안 뜬다**(덮는 대상 0, #38).
- 두 앵커를 이은 방향이 **축과 6.3° 어긋난 실측**이 있다 → 두 점 배치의 축 라벨이 자주 미분류다.
- **병합 반경을 넓히는 것으로 대신하지 않는다**(이웃 꼭짓점과 같은 자릿수다). `DEFERRED.md`에 있다.

### 3. **축 경로는 대각선의 12%를 조용히 축에 붙인다**
`osnap.json@509615a1`: 같은 대각선 480개에 축 판정을 돌리면 **59(12.3%)가 축으로 배정된다**
— 그것은 회수가 아니라 **조용히 틀린 배치**다(`face_diagonal.json`). 두 점 배치의 이득은
"되게 하는 것"만이 아니라 **그 12%를 안 만드는 것**이다.
⚠ 초판이 "축 경로로는 0개(정의)"라고 적었고 **틀렸다** — 리뷰어가 잡았다.

### 4. 축 스냅은 **카메라가 부정확한 동안 손해**다(그래도 켬)
`axis_snap.json`: 조용히 틀림 **0.811 → 0.967**. 이유는 **카메라 오차 증폭**이고
이 픽스처의 카메라가 임계의 20~30배다. **부호가 뒤집히는 배수는 미측정**(QUESTIONS f).

## 검증 현황 (이 커밋 기준)

vitest **67파일 390 통과 · 1 건너뜀** · tsc·빌드 통과 · Playwright **21 통과 · 2 건너뜀** ·
pytest **73/73** · selfcheck 플래그 **1821건 · STALE(상수) 0 · STALE(정의) 0** ·
**상수 해시 `1b6175a4`** · 인용 **99건 플래그 0** · 게이트 블록 **13**(**값 대조 4 · 면제 9**) ·
훑기 4 · 분모 검사 44파일(플래그 12는 기존 `Math.max(1,n)`).

⚠ **검증 문구는 두 칸으로 적는다**(STALE 상수·정의). ⚠ **면제는 통과가 아니다.**
⚠ **건너뛴 것의 정체**: vitest는 `real_ink`(`sessions/`가 비었다), Playwright는 `coords`의
`[양성 채널] dpr 1`과 `stage`의 하나. CI에서는 `data/quickdraw`가 없어 **둘이 더 건너뛴다**
(`web/test/dataDeps.ts` — **통과로 안 센다**).
⚠ **벽시계 수치는 문서에 인용하지 않는다**(실행마다 흔들린다).

## ⛔ 남겨 둔 폐기 코드 — 측정 근거의 재현용

`segment.ts`(G 실험) · `cameraFit.ts`(D-L6) · `horizonRollZero.ts`(D-L32).
**셋 다 배선하지 않는다.** 지우면 그 판정의 근거를 다시 만들 수 없다.

## ✋ 브라우저에서 보는 절차

**배포본**: <https://hyunwookyu.github.io/brunelleschi/> · 로컬: `cd web && npm run dev:5222` → `/l.html`

앱 손잡이 `window.S2S` — `doc()` · `cam` · `stage` · `diag()` · `snap(p)` · **`endSnap(from,p)`** ·
`hoverSnap()` · **`twoPointStrokes()`** · `live()` · `axisLock()` · **`horizon()` · `setHorizon(y)` ·
`horizonGrab(p)`** · `views()` · `order()` · `promoteOrderNow()` · `chainTrace()` · `rules()` ·
`feedLine(a,b)` · `setAxisLines()` · `exportObj()` · `buildDoc2()`.

합성 상자는 **측정 하네스와 같은 픽스처**로 넣는다 — `await import('/test/scene3d.ts')`.
손으로 만들면 화면에서 평행한 아핀 상자가 나와 수렴이 0이다.
⚠ **캔버스 크기 굳음이 세 번 재발했다**(AS-C7) — 확인은 **창을 앞에 두고** 한다.

## 개발 명령

```bash
cd web && npx vitest run && npx tsc --noEmit && npm run build && npx playwright test
```
```bash
python -m pytest tests/ -q && python selfcheck.py
```
⚠ `vitest`는 **`web/`에서** 돌린다. ⚠ `playwright` 앞에 **`npm run build`**를 넣는다.
⚠ **`progress.md`는 저장소 루트의 것 하나다** — `cd web` 상태에서 `cat >>` 하지 않는다.

## 미해소 (전문은 `DEFERRED.md`)

- **실획 표본이 0이다**(AS-C1) — 위 "다음에 할 일"이 그것이다
- **사람이 지평선을 ±120px 창에 놓는지 미측정**(AS-L9의 새 형태)
- **꼭짓점 병합이 없다** — 정점 스냅 공허 · 두 점 배치의 축 라벨 미분류(위 §2)
- **`osnap.json`이 참 3D를 대상으로 쓴다**(상한) — 앱 기하로 다시 재야 한다
- **축 스냅의 손익분기 배수 미측정**(QUESTIONS f)
- **지평선 창의 바깥 끝 미측정**(120~268px 사이)
- **롤 0이 자료구조에 박혀 있다** — `RuleState.horizon`이 스칼라 y다(QUESTIONS d·e에 적었다)
- **`Math.max(1, n)`이 12개 하네스에 남아 있다**
- **v1 저장을 열 수단이 없다**(D-L29) · **사전 캐시 목록만의 몫 미측정**(D-L36)
- **`#21`의 진짜 회귀 팔이 없다** · **하네스와 앱이 같은 배치 옵션 출처를 쓰는지 아무도 안 본다**(D-L35)

## ⚠ 반드시 먼저 읽는다

1. `CLAUDE.md` — 작업 지침. §2에 게이트가 있다
2. **`PITFALLS.md`** — 반복해서 걸린 것 **41개**. **읽는 시점은 항목 착수 시**다
3. `docs/line_plan.md` — **개정 2**
4. 이 문서
5. `progress.md`(루트 하나) · `DECISIONS.md`(D-L1~**D-L46**) · `assumptions.md`(AS-L1~**AS-L19**) ·
   `DEFERRED.md` · **`QUESTIONS.md`**(a~g, **g는 D-L45로 답이 갔다**)
