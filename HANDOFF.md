# HANDOFF.md — 세션 인계

다음 세션은 이 문서를 읽고 이어서 진행한다. 사람의 개입은 그 한 줄뿐이다.

## 현재 단계 — **L-D.2 완료 · L-D.3 착수(저장 왕복만 닫음) · 다음은 L-D.3 나머지 → L-D.4**

**계획서는 [`docs/line_plan.md`](docs/line_plan.md) 개정 2**다. 개정 1은
`docs/archive/line_plan_r1.md`에 있고 **폐기가 아니라 대체**다 — 개정 2가 §2~§4·§8을
"개정 1 유지"로 참조하므로 **그 절들의 원문은 아카이브 쪽**이다.
다만 **판단 근거로 아카이브를 인용하지 않는다**(D-L1).

## ⚡ 착수 전에 `PITFALLS.md` **상단 "최근 다섯"**부터 읽는다

2026-08-16 세션에서 **#33·#32·#35가 같은 세션 안에서 재발**했다. 36개를 매 항목 대조하는
읽기 규칙이 한계에 닿아서, 이번에 구조를 바꿨다:

- **셋을 자동 검사로 옮겼다** — #33(전수 훑기 확장자 커버리지 · `sweeps.json`),
  #35(게이트 도달 가능성 필드), #36(분모 0). 반례 테스트가 `tests/test_selfcheck_new_scans.py`에 있다.
- **최근 다섯**을 목록 맨 앞에 뒀다. **자동**으로 표시된 것은 읽기 대상에서 뺀다(20개 이하 유지).
- **리뷰어가 "세션 내 재발"을 점검한다**(`.claude/agents/reviewer.md`).
- **#37 신설** — 새 지표·기전을 제안하기 전에 이론서를 확인한다(f² 편차가 6.4의 항등식이었다).

### 이번 세션이 한 것

| 항목 | 결과 |
|---|---|
| **PITFALLS 작동 문제**(사람 지시) | #33·#35·#36 자동화 · 최근 다섯 상단 · 리뷰어 세션 점검 · #37 신설 |
| **§9.5 재검토**(사람 지시) | **새로 안 쟀다.** `face_diagonal.json@34db7279`만으로 결론 — **지정 경로는 축이 아니라 면**(D-L27). L-D.1의 "0/2는 결함" 읽기를 **철회**했다 |
| **L-D.2 저장·내보내기** | `docStore.ts`(v2) — **뷰와 뷰별 2D 획**을 담는다. `doc_store.test.ts` 7건 |
| **L-D.2 내보내기** | `exportGeom`이 `ExportLine`(최소 모양)을 받는다 — 옛 UI와 L이 같은 함수(#17). OBJ 주석·glTF 노드에 **뷰 이름** |
| **L-D.2 정적 배포** | `base: "./"` · `pages.yml` · `l.html`에 manifest/PWA · `mainL`이 `./sw.js` 등록 |
| **L-D.2 배포 확인** | `e2e/static_deploy.spec.ts` — 하위 경로·SW scope·**오프라인 새로고침** 전부 통과 |
| **L-D.3 착수** | `stage.spec.ts`에 **저장 왕복** 종단 확인(뷰·2D 레이어·id·내보내기·비우기) |
| 리뷰어 | **L-D.2 검토를 걸어 둔 채 세션이 찼다** — 아래 "미해소" 참조 |

### 검증 현황

vitest **314/314**(55파일) · Playwright `stage.spec.ts` **11/11** · `static_deploy` 1/1 ·
pytest **58/58** · tsc·빌드 통과 ·
selfcheck **1552건 · STALE(상수) 0 · STALE(정의) 0** · 정의 해시 `a45078a5`.
⚠ **검증 문구는 두 칸으로 적는다** — "STALE 0"만 적었다가 **STALE(정의) 1건을 놓쳤다**(#1).

## 다음에 할 일 — **L-D.3 나머지**

계획서 L-D.3이 요구하는 것 중 **저장 왕복만 닫혔다.** 남은 것:

1. **상자 둘 이상 픽스처.** 지금 `setup(page)`는 상자 하나다(`e2e/stage.spec.ts` 33행).
   둘 이상이어야 아래 셋이 의미가 생긴다.
2. **L-B 게이트 3번** — 승격 연쇄 **여러 회** + 브라우저 확인. `promoteOrderNow`를 반복 호출하고
   각 회차의 배치·형태를 원장에 남긴다(`stage_browser.json`에 이어 붙인다).
3. **차수 표식 다중** — `orderMarks`가 2·3점을 함께 들고 있을 때의 되돌리기.
   지금 확인은 표식 하나짜리다.
4. **D-L21 재개** — DEFERRED에 있다. D-L25 목적함수로 다시 채점해야 한다(사후 재해석이므로
   **판정을 뒤집는 데 쓰지 않는다**, D-L25의 #26 범위 항목).
5. **AS-L9 시나리오** — 알려진 참 카메라의 그림을 주고 사람이 가이드를 맞춰 몇 도까지 가는지.
   **자동화가 어려우면 DEFERRED**(사람 표본이 필요하다).
6. 지연 측정 · 스크린샷 회귀.

그 다음 **L-D.4 문서**(README·PROTOTYPE·DEFERRED·DECISIONS·QUESTIONS·HANDOFF 완료).
⚠ **QUESTIONS.md가 아직 없다** — L-D.4가 만든다.
⚠ **옛 UI(`index.html`·`main.ts`)는 L-D.3 통과 후에 지운다**(A-4).

## 미해소

- **리뷰어 L-D.2 검토 17건(높음 5) 중 넷을 고쳤고 나머지는 남아 있다.**
  고친 것: "STALE 0" 정정(#1 재발) · 자동 표에 **안 잡는 것** 열(#17) · D-L27 번호 충돌과
  라벨·범위 정정(#11·#28) · README 보장 문구와 인앱 브라우저 관측(#32·#30).
  **남은 것**(다음 세션이 먼저 본다):
  1. **#25 — 이번 세션의 브라우저 확인이 원장 밖이다.** 배포 확인 ①②③·`doc_store` 7건·
     저장 왕복이 `progress.md` 문장으로만 있다. **`stage_browser.json`에 `l_d2`·`l_d3` 블록으로
     append한다**(HANDOFF가 L-B 게이트 3번에 대해 이미 지시한 절차와 같다).
  2. **#30 — 오프라인 확인에 양성 채널이 없다.** 사전 캐시 목록에서 자산 하나를 빼고
     **실제로 실패하는지** 본다. 통과가 무엇을 배제했는지 지금은 안 보인다.
  3. **#36 — `promote`·`axis_live`의 53곳**이 층화 비율(관측)인지 표시용 보호인지 안 갈렸다.
     그 둘은 **D-L21 재개의 근거 원장**이므로 재개 전에 가른다(미루기에 기한이 아니라 **트리거**를 단다).
  4. **#3 — `axis_misplaced 19 = by_reason.ok 19`가 항등인지 미확인.** 항등이면 D-L28의 20배는
     "두 경로의 비교"가 아니라 "두 정의의 비교"에 가깝다.
  5. **AS-5가 "무효"인데 그 원장을 결론에 썼다.** `placeOnFace`가 L에서 도는지 확인하고
     `assumptions.md`의 상태를 다시 적는다(D-L1: 아카이브를 근거로 인용하지 않는다).
  6. **저장 v2가 못 담는 것의 목록이 없다** — 되돌리기 이력·`orderMarks`·못 살린 스냅 표식·
     승격 손실 표시·가이드 핸들 상태. **복원 뒤 어떻게 보이는지**를 한 줄씩 적는다.
     특히 못 살린 스냅은 복원 뒤에도 표식이 남아야 한다(조용히 틀린 시작점).
  7. HANDOFF 하단의 **낡은 검증 블록**(307/307 · `3df57ac7`)과 PITFALLS 개수(34)·D-L 범위를 고친다.
     README의 "저장·내보내기가 하는 일 (S-9)" 절이 **v1 서술 그대로**다(뷰가 없다).
- **#36: `Math.max(1, n)`이 12개 하네스 70곳에 남아 있다**(`DEFERRED.md`). 검사는 켜져 있고
  파일당 한 건으로 보고한다. **다음에 그 하네스를 건드릴 때 `metrics.rate()`로 바꾼다.**
- **#35 검사가 지금 0건이다.** 반례 테스트로 **검사 자체는 동작함**을 확인했지만
  (`tests/test_selfcheck_new_scans.py`), 원장에 `gate` 키가 없어 실제로는 아무 원장도 안 덮는다.
  게이트를 그 이름으로 기록하도록 하네스를 고치는 것이 남았다.
- **배포 URL이 아직 없다** — Settings → Pages를 켜야 정해진다. 정해지면 README 1.5절에 적는다.
- 이전 세션에서 넘어온 것은 `DEFERRED.md`와 `progress.md`에 그대로 있다.

## ⚠ 반드시 먼저 읽는다

1. `CLAUDE.md` — 작업 지침. §2에 게이트가 있다
2. **`PITFALLS.md`** — 반복해서 걸린 것 **34개**(#33·#34가 이번에 추가됐다).
   **읽는 시점은 항목 착수 시**이고, 걸리는 번호를 `progress.md`에 적는 것이 읽었다는 증거다
3. `docs/line_plan.md` — **개정 2**
4. 이 문서
5. `progress.md`(**루트의 것 하나뿐이다.** ⚠ **이번 세션이 실수로 `web/progress.md`를 만들었고
   리뷰어가 잡았다** — 쉘 작업 디렉토리가 `web/`일 때 `cat >>`를 하면 그렇게 된다.
   맨 끝: **λ 결정** · **L-C.2** · **리뷰어 4회차 12건**) ·
   `DECISIONS.md`(D-L1~**D-L26**) · `assumptions.md` · `DEFERRED.md`

## L-B.3이 남긴 사실 일곱

**① 스냅은 화면 연산이고 솔버를 안 고쳐도 된다.** 올라간 기하는 되쏘면 **정확히 그 화면 점**
이므로(`segGap = 0` 보장) "3D 대상에 붙인다"와 "그 대상의 상으로 `pts2d[0]`을 옮긴다"가
같은 연산이다. ⚠ **그 항등은 그 카메라에서만 성립한다** — 차수 승격에서 스냅이 조용히 풀린다(DEFERRED).

**② 반경을 정하는 것은 (겨냥 오차 / 대상 간격)의 비다.** 격자 k3(꼭짓점 간격 중앙 21.3px)에서
**틀린 붙음이 반경과 무관하게 15076/38880(39%)에서 포화한다** — **같은 밀도에서 잰** 겨냥 오차
중앙 12.19px이 간격의 절반(10.65px)을 넘기 때문이다. **스냅으로 못 고치는 몫이 있다**(D-L15).

**③ 채택 반경 0.05는 선례(10~15px)의 5~7배다**(앱 1280×675에서 **72px**). 사전 등록한 두 규칙이
**둘 다 실패**해서(픽스처에 비용이 없었다) **비용을 측정에 넣었다** — `unintended_snap`.
0.05 → 0.10은 맞음 **+27**에 끼어듦 **+918**이다(D-L14).

**④ 스냅된 시작점에서 놓인 것의 19%가 조용히 틀린다**(474/2503, cut 0.2 · `end_jitter > 0`).
지배 사유는 스냅 실패(56)가 아니라 **축 판정 실패 681**이다.
⚠ 초판은 **못으로 박힌 `end_jitter = 0`을 섞은 분모**로 17%(569/3403)라 적었다(리뷰어 2회차 [6]).

**⑤ 참 카메라 `coarse` 0.44의 원인은 축 판정(과 그 하류인 교점 회수)이다.** 오라클 축이면
**0.44 → 0.82**이고 등급 격차가 0.19 → 0.05로 거의 사라진다. ⚠ **오라클 축 팔은 교점 집합도
함께 바꾼다**(참 교점 회수 coarse 666 → 1500) — 두 몫은 이 설계로 안 갈린다(DEFERRED).
조건수는 "못 푸는" 문제가 아니라 **"틀리게 푸는"** 문제다
(`solveNull` 실패 **0건**, 형태 오차 중앙 **0.0875~0.2692** · 조용히 틀림 **29~67%**). ⚠ **스냅은 이것을 못 고친다** —
초기 확정 순간에는 3D 대상이 0이다. **L-B.4와 L-B.7이 그 자리다.**

**⑥ 초안 확정률 0.34의 지배항은 지지선 선택이었다.** `longest`의 가이드 쌍 각차가 **최소 0.03°**다.
**출하 구성**(`len_x_sep` + 빈 축 채우기)은 **31/90 → 58/90**, 축 오차 중앙 12.35° → **8.95°**,
**f 상대오차는 0.3007 → 0.3152로 오히려 나빠졌다**(채운 축이 f에 들어가기 때문이다).
분모의 상한은 **72/90**이다(참 소실점을 넣어도 그렇다). 무한원 스냅은 9/90만 설명한다 —
**유지한다**(D-L17). ⚠ **어느 팔에서도 축 오차가 임계 0.5°의 18~28배**라 초안 카메라는
어차피 못 쓴다. **중립 시작(13.83°)보다 낫다**는 것이 초안의 가치다.

**⑦ 리뷰어가 잡은 가장 큰 것: 등록한 대조군을 안 돌렸다.** 사전 등록에 "대조군: 스냅 끔"을
적어 놓고 원장에 `control` 키가 0건이었고, 계획서 §11 L-B 게이트의 **두 번째 항목**
(스냅된 시작점에서의 배치 정확도)도 측정 대상이 아니었다. **등록을 적는 것과 돌리는 것은 다르다.**

## L-B.4·L-B.5가 남긴 사실 셋

**⑧ 확정 시점 축 판정은 임계로 못 고친다.** 45조합 전부에서 배치와 조용히 틀림이 **단조로
맞바뀐다**(`vp_dist_ratio` 0.03 → 0.22: 배치 2259 → 2632, cut 0.2 0.305 → 0.416).
등록 규칙이 고르는 이득은 **+26/4320**이고 시드 폭 **31**(387~418, 실측) 안이다.

**⑨ 답은 앵커다 — 그러나 절단 곡선이 초판도 재측정도 둘 다 깎았다.**
⚠ **초판 분모에 못박힌 `end_jitter = 0`이 25% 섞여 있었다**(리뷰어 3회차 [1], `p10 = 0`이 서명 —
L-B.3 2회차가 이미 한 번 고친 결함). `end_jitter > 0`으로 자르면 확정 배치 **1563/3240**,
앵커 **2503/3240**이고 비율 교차는 **절단 0.40**이다.
⚠ **틀린 개수는 교차한다**: cut 0.2에서 앵커 0.146 대 확정 0.1985(앵커 승),
cut 0.5에서 0.1259 대 0.0528(**확정이 2.4배 낫다**). "개수에는 교차가 없다"는 **맞은 개수**에만 참이다.
⚠ **우열은 비용비 λ가 정한다** — λ ≤ 2면 앵커가 모든 절단에서 이기고 λ ≥ 3이면 큰 절단에서 확정이 이긴다.
**A-3은 λ를 크게 보는 쪽**이므로 큰 틀림 기준에서는 앵커가 진다. **DEFERRED.**
⚠ **"등급별 분기 불필요"도 철회했다** — 앵커 팔은 오라클 대상이라 **등급 효과가 구성상 안 나온다**(#31).
⚠ 꼬리도 앵커가 나쁘다(max 13.33 대 3.35).

**⑩ 측정은 임계를 조이는 쪽을 가리킨다 — 그런데 지금은 못 따른다.**
`vp0.03_bend0.18_amb2`가 배치 −237에 **축 오배정 −94 · cut 0.5 −39%**다. §9.1의 정책
(미배치는 대기, 승격이 연쇄)에서는 그쪽이 맞지만 **회수 기전이 아직 없다**(L-B.7). D-L18.

## L-B.7이 남긴 사실 넷 — **계획서를 고쳤다**

**⑪ §9.1의 일괄 기전은 반증된 적이 없다** — 회수 0은 **미실행**이었다(PITFALLS #32).
대기 1904가 거름에서 전부 걸려 **솔버에 한 획도 안 닿았다.** 축을 주고 다시 재니
오라클 축에서 입력 1904 · **솔버가 놓은 것 1154(60.6%)**다(D-L24).
⚠ **앵커 720과 직접 견주지 않는다** — 그것은 실제 축 라벨이라 기전 차이와 축 정보 차이가 섞인다.
**축을 맞춘 앵커 대조군은 804/1904**이고 일괄이 **1.44배** 많다(804 대 720이 축 정보의 몫).
⚠ **그러나 일괄은 더 많이·더 틀리게 놓는다** — 675/1154 = **0.585**로 앵커(0.505)보다도
확정(0.362)보다도 나쁘다. **살아난 것은 개수 기전으로서다.**
⚠ **오라클은 상한**이고 실제 축 라벨로는 **24/966**이다.

**⑫ 회수하는 것은 앵커다 — 720/1904 = 37.8%.** 대기 획의 **시작점이 새로 놓인 기하에 붙으면**
§3·§7의 실시간 경로가 그 획을 놓는다. **승격은 "다시 푸는 것"이 아니라 "앵커가 생기는 것"이다**(D-L20).
계획서 §9.1을 고쳤다(#23).

**⑬ ⚠ 회수된 것이 확정된 것보다 나쁘다 — 다만 격차의 대부분은 배치가 아니라 게이지다**(#16·#8·#31).
같은 720획이 고정 게이지로 cut 0.2 **392/720(0.544)**, **자기 게이지로 286/720(0.397)**이고
확정은 **874/2416(0.362)**이다. ⚠ **자기 게이지 배수는 시드별 0.865~1.150으로 1을 가로지른다** —
**형태 격차라는 결론은 없다**(#14). 고정 게이지 배수는 시드 여섯 전부 1.20~1.57이다.
즉 **승격이 더 내는 오차는 형태가 아니라 배율이고**, 앱이 배율을 다시 안 맞추므로
**그 배율 어긋남이 사용자가 보는 오차다.** ⚠ 꼬리는 자기 게이지에서도 나쁘다(p90 ≈2배).
⚠ **구도로 가르면 1점 구도가 90/93 = 0.968이다**(같은 층의 확정은 268/422).
"미배치는 대기다"는 **개수 회수에 대해서만 참**이다.

**⑭ D-L18 기각**(D-L21). 조이기의 (배치 + 회수)가 **2976 대 기준 3136**이다 —
회수율이 두 팔에서 거의 같아(37.8% 대 37.2%) 차이를 못 메운다.
⚠ **다만 조이기는 조용히 틀림을 낮춘다**(cut 0.2 **0.4037 → 0.329**, 고정 게이지).
⚠ **"등록 규칙이 개수를 우선"은 틀린 서술이고 고쳤다**(리뷰어 [10]) — 규칙은 **두 조건의 연언**이고
**충족 가능하되 좁았다**. 규칙을 사후에 바꾸지 않았다(#28).

## L-B.M이 남긴 사실 넷 — **지표 정의 통일**(사람 지시)

**⑮ 형태 오차의 정의는 `web/test/metrics.ts` 하나다**(D-L22). 게이지(전역 배율)를 **인자로
강제**한다 — 한 번에 푸는 집합은 자기에서 적합하고, **승격 회수 획은 확정 집합의 게이지를
고정으로 받는다**. 옛 `promote.segErr`는 게이지 1이었고 그래서 같은 2416획이
`axis_live.json` 0.1222 · `promote.json` 0.1533으로 갈렸다. **지금은 일치한다.**

**⑯ 다시 재니 판정 하나가 바뀌었고, 리뷰어가 그것을 다시 깎았다.** 회수의 품질이
확정과 "같다"(옛 정의) → "1.5배 나쁘다"(고정 게이지) → "형태 1.10배 + 배율 몫"(추정기를 맞춘 뒤) →
**"배율 몫만 실재하고 형태 몫은 시드 폭 안"**(시드별로 본 뒤). **세 번 고쳤다.**
**같은 추정기로 재고 시드 폭을 보기 전까지 배수를 인용하지 않는다.**
D-L21(D-L18 기각)·D-L24는 **유지**된다.

**⑰ 정의 해시를 산출물에 넣는다**(`metric_defs`, D-L23). ⚠ **`toString()`으로 내면 안 된다** —
vitest와 Playwright의 TS 변환이 다른 문자열을 내서 **지워지지 않는 거짓 양성**이 된다.
**소스 파일 텍스트**(주석·공백 제외)로 낸다. `require`도 안 된다(ESM 러너에서 폴백).

**⑱ 갈린 지표는 형태 오차 하나였고 사본은 다섯이었다.** `axisDirErrors` 두 벌(바이트 동일) ·
`perStrokeError` 세 벌(식 동일) · **`promote.segErr`(값이 달랐다)** ·
**`axis_live`의 인라인 식**(나중에 절단 곡선에서 발견 — 전수 확인이 **함수만 찾고 인라인을 놓쳤다**).
⚠ **정의 해시가 인라인을 못 잡는다** — 하네스가 `metrics.ts`를 아예 안 부르면 해시가 그대로다.
**나머지 하네스의 인라인 식은 아직 전수로 안 봤다**(DEFERRED).

## L-B.6·L-B.8이 남긴 사실 셋

**⑲ 뷰 시스템은 배선만 남아 있었고 그것을 했다.** `stage.pose()`/`setPose()`가 자세를 나르고
규약 변환은 **거기 한 곳뿐**이다(#17). 목록 · 전환 · 삭제(그 안의 대기 획도) · 소유 표시.
사전에 적은 판정 다섯이 전부 통과했고, ③은 **지울 뷰 안에 승격 획을 만들어 넣은 뒤** 다시 봤다
(안 그러면 공허 통과다 — 리뷰어 [9]).

**⑳ 종단 확인이 버그 하나를 잡았다.** `OrbitControls.update()`가 카메라를 자기 `target`으로
다시 겨눈다 — 자세만 넣으면 방향이 덮여 **뷰로 돌아가 그릴 때마다 뷰가 하나씩 늘어난다.**
`target`을 그 자세의 시선 위에 두어 `update()`를 항등으로 만들었다.

**㉑ L-B.8은 기하를 시점 좌표로 옮겨 넣는 것으로 풀었다** — `snap.ts`·`liveLine.ts`를
**한 줄도 안 고쳤다**(A-3). `Frame { ctx, toV, fromV, dirV, pinned }`이 경계다.
`seg3d`와 `snapStart.at`은 **세계 좌표로 적는다** — 확정 시점으로 돌아가도 제자리인 것을
`l_b8`이 비트 단위로 확인한다. **§9.3이 이제 도달 가능하다**: 돌린 각도에서 그리면 뷰가 생긴다.

## L-C.1이 남긴 사실 셋

**㉒ 차수 승격은 품질을 개선하고 배치를 줄인다.** 참 3점 장면에서 카메라를 2점으로 주고
승격시키면 같은 획에서 형태 오차 중앙 **0.1259 → 0.0913** · p90 **1.1301 → 0.5395** ·
cut 0.5 **90 → 60**이고 획별로 **개선 356 대 악화 194**다. **시드 여섯 전부에서 안 나빠진다**(0.80~1.00).
⚠ **그런데 배치가 −168이다**(only_before 570 대 only_after 402) — 소실점이 셋이 되면
축 판정이 더 까다로워진다. **등록 규칙의 두 조건 중 배치가 깨졌으므로 자동으로 안 건다**(§6.2). ⚠ **그 근거는 D-L26이 대체했다**(리뷰어 2회차) — D-L25 아래에서 배치 감소는 더 이상 기각 사유가 아니고, 판정을 세우는 것은 **§6.2**(무결론이면 자동화의 근거가 없다)다.

**㉓ 가짜 승격 대조군이 움직인다**(#30). 참 2점 장면에 **없는 세 번째 소실점**을 넣으면
악화 200 대 개선 96, cut 0.2 **119 → 180**이다. 그러므로 위의 개선은 측정이다.

**㉔ "차수 승격에서 스냅이 조용히 풀린다"가 실측으로 확인됐다.** 다시 안 옮겼다면
어긋났을 거리가 중앙 **0.595px**인데 **p90 311px · max 639px**다. `promoteOrder`가
**두 번 돌아** 대상의 새 상으로 다시 옮긴다 — **1381/1676**(참 승격 216장면만. ⚠ **초판은 1908/2391이라 적었는데 그것은 가짜 승격 대조군 72장면이 섞인 값이다** — L-C.2 리뷰어 [5], #9). ⚠ **295개는 대상이 새 카메라에서
안 놓여 못 살렸고**, 끝점·중점 말고는 매개변수가 안 정해져 **`a`쪽 끝점으로 근사**한다(DEFERRED).

## ⛔ 아직 폐기하지 않은 코드

**옛 UI(`index.html` + `src/main.ts` + `src/ui/cameraPanel.ts`)는 L-B 게이트 통과 전까지
지우지 않는다**(A-4·사용자 지시). 새 UI는 **`l.html` + `src/mainL.ts`**다.
`vite.config.ts`가 둘 다 빌드한다. `segment.ts`(G 실험) · `cameraFit.ts`(D-L6 기각)도 그대로 있다.

## ✋ 브라우저에서 보는 절차

```bash
cd web && npm run dev:5222
```
→ **`http://localhost:5222/l.html`** (새 UI) · `http://localhost:5222/` (옛 UI)

⚠ **캔버스 크기 굳음이 세 번 재발했다**(AS-C7). 확인은 **창을 앞에 두고** 한다.
`SIZE_HEAL.count`가 자가 치유 발동 횟수다.

합성 상자를 넣으려면 **측정 하네스와 같은 픽스처**를 쓴다 — `await import('/test/scene3d.ts')`.
손으로 "소실점 쪽으로 t만큼" 만들면 **화면에서 평행한 아핀 상자**가 나와 수렴이 0이다.
`e2e/stage.spec.ts`의 `setup()`이 그 절차를 담고 있다. 앱 손잡이는 `window.S2S`
(`doc()` · `cam` · `stage` · `refresh` · `SIZE_HEAL` · **`snap(p)` · `snapTargets()` ·
`hoverSnap()` · `live()` · `axisLock()` · `setAxisLock()`**)다.

## 개발 명령

```bash
cd web && npx vitest run && npx tsc --noEmit && npx playwright test
```
```bash
python -m pytest tests/ -q && python selfcheck.py
```
⚠ `vitest`는 **`web/`에서** 돌린다. 저장소 루트에서 돌리면 다른 vitest가 잡히고 원장 경로가 어긋난다.
`camera_gate`가 103초 걸린다(전체 109초 중 대부분).

검증 현황: vitest **307/307**(54파일) · tsc 0 · pytest **52/52** · playwright **18/18** ·
**STALE 0 · STALE(정의) 0** · 상수 해시 **`2fd74e1c`**(⚠ `PICK_TOL` 등록으로 `46c028d1`에서 옮겼다.
값은 안 바뀌었고 인용 62곳을 고쳤다) · **정의 해시 `3df57ac7`**.

## 미해소

- **실획 표본이 여전히 0이다**(AS-C1). 모든 판정이 Quick,Draw 유도 합성 잉크 위에 있고
  AS-C10이 그 모델을 **양방향으로 틀렸다**고 적었다. `sessions/`에 들어오면 다른 작업보다 먼저 측정한다
- **AS-L9**: 사람이 축 오차 0.5°에 도달하는지 미측정. 대리값(핸들 예산)도 **핸들 하나** 가정 위에 있다
- ~~차수 승격에서 스냅이 조용히 풀린다~~ **실측했고 고쳤다**(L-C.1) — 다시 안 옮기면 p90 **311px** · max **639px**다. `promoteOrder`가 **1381/1676**(참 승격 216장면만. ⚠ **초판은 1908/2391이라 적었는데 그것은 가짜 승격 대조군 72장면이 섞인 값이다** — L-C.2 리뷰어 [5], #9)를 새 상으로 옮긴다. ⚠ **295개(17.6%)는 대상이 안 놓여 못 살렸고** 끝점·중점 말고는 매개변수가 근사다(DEFERRED)
- **앵커/확정의 우열이 비용비 λ에 달렸다** — λ ≤ 2면 앵커, λ ≥ 3이면 큰 절단에서 확정이 이긴다. **λ는 측정 밖에서 와야 하고 표본이 0이다**(DEFERRED)
- **등급별 임계 분기를 팔 *안에서* 안 쟀다** — 앵커 팔은 오라클 대상이라 등급 효과가 구성상 안 나온다(#31). 진짜 물음은 미측정이다
- **`end_jitter = 0` 못박힘이 다른 원장에도 있는지 전수 확인 안 함** — `axis_live`에서 재발했고 **selfcheck의 사각지대**다(꼬리 제외 규약)
- **인라인 지표식 전수 확인 안 함** — 정의 해시는 `metrics.ts`를 **부르는** 하네스만 덮는다. 안 부르고 인라인으로 적으면 해시가 안 움직인다
- **승격이 배치를 −168로 줄인다**(L-C.1) — 소실점이 셋이면 축 판정이 까다로워진다. 줄어든 배치가 앵커로 회수되는지 안 쟀다
- **스냅으로 못 고치는 몫**(D-L15) — L-B.4·B.5·B.7 **어느 것도 이 비를 안 바꿨다**. 남은 후보는 이어 그리기 문맥이고 계획서에 없다
- **승격이 더 내는 오차는 배율이다** — 형태만 보면 확정과 구분되지 않는다(시드 폭 안). 배율을 확정 틀에 맞추는 기전이 없다
- **1점 구도에서 승격이 무너진다** — 회수한 것의 90/93이 조용히 틀리고 연쇄가 0이다. 구도별 분기가 없다
- **연쇄의 여러 회 반복을 브라우저에서 안 봤다** — 원장은 1회분 하네스를 여러 회 돌린 것이고, 앱의 `promoteChain`은 8회 상한으로 돈다. 그 둘이 같은 것을 세는지 미확인
- ~~임계를 조이는 쪽(D-L18)~~ **닫혔다 — D-L21로 기각**. 다만 품질 우선 목적함수에서는 여전히 이긴다(DEFERRED)
- **`turn_detection` 대조군이 천장에 붙어 있다**(동적 범위 5.8pp) — 완만한 코너 픽스처가 필요하다
- `min_guide_ratio` 0.45가 **수직 가이드에 원리적으로 도달 불가**다(588.18 < 651.2) — DEFERRED
- **분포형 오차의 정확한 0을 selfcheck가 못 본다** — 꼬리 제외 규약과 충돌한다(DEFERRED)
- L-B.1 3건 · L-B.2 3건 · **L-B.3 2회차 3건** · **L-B.4 3건**을 DEFERRED로 넘겼다

⚠ **`progress.md`를 `web/`에서 만들지 않는다.** 쉘 작업 디렉토리가 `web/`일 때 생기고,
그러면 다음 세션이 그 항목을 못 본다.
## 최근 커밋
```
3362dec L-D.3 착수 — 저장·복원 종단 확인(뷰와 2D 레이어가 새로고침을 넘는다)
ad6ab80 L-D.2 저장·내보내기 + 정적 배포 — 서버 없이 아이패드에서 열린다
4ad79df §9.5 재검토 — 지정 경로는 축이 아니라 면이다(D-L27, 기존 원장으로 결론)
390cb4a PITFALLS 작동 문제 — 셋을 자동 검사로 옮기고 최근 다섯을 앞에 뒀다
d1b6ac5 L-D.1 리뷰어 대응 — "0/2는 결함"을 철회한다. 축 지정이 옳은 답이 아닐 수 있다
13f8d89 L-D.1 고치기 — 고르기·삭제는 선다. 축 지정은 안 올린다(0/2, 원인 특정)
7cb355e L-C.3 대체 자동 신호 — 후보 둘 다 안 선다. 잴 것이 없어서가 아니라 신호가 못 본다
03033a0 L-C.2 되돌리기 UI + λ 결정 — 승격이 무엇을 잃었는지 보이고, 차수로 되돌아간다
```
