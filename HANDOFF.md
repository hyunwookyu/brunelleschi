# HANDOFF.md — 세션 인계

다음 세션은 이 문서를 읽고 이어서 진행한다. 사람의 개입은 그 한 줄뿐이다.

## 현재 단계 — **L 완주(L-A ~ L-D.4). 다음은 실획 표본이다**

계획서 [`docs/line_plan.md`](docs/line_plan.md) **개정 2**의 L-A ~ L-D가 전부 끝났다.
개정 1은 `docs/archive/line_plan_r1.md`에 있고 **폐기가 아니라 대체**다 — 개정 2가
§2~§4·§8을 "개정 1 유지"로 참조하므로 **그 절들의 원문은 아카이브 쪽**이다.
다만 **판단 근거로 아카이브를 인용하지 않는다**(D-L1).

프로그램 이름은 **Brunelleschi**다(1415년경 산 조반니 세례당 실험).
자체 저장 확장자는 **`.brnl`**이고 **내용은 JSON**이다(D-L34).

## ⚡ 착수 전에 `PITFALLS.md` **상단 "최근 다섯"**부터 읽는다

목록이 **41개**다. 최근 다섯: **#41 #40 #39 #38 #37**.
**자동**으로 표시된 것은 `python selfcheck.py`가 **일부만** 잡는다 —
그 표의 "안 잡는 것" 열은 여전히 사람이 본다.

## 다음에 할 일 — **실획 표본이 유일한 다음 단계다**

**모든 판정이 합성 잉크 위에 있고 AS-C10이 그 모델을 양방향으로 틀렸다고 적었다.**
`sessions/`가 비어 있는 한 아래가 전부 잠정이다:

1. **실획을 받는다.** 절차는 `README.md` 0절에 있다(앱에서 `획 내보내기` → `sessions/`).
2. `cd web && npx vitest run test/real_ink.test.ts` → `real_ink.json`에
   AS-6(한 획 = 한 축) · AS-12(끝점 겨냥 오차) · 미배치율이 실획으로 다시 나온다.
3. **그 뒤 임계를 재조정한다.** STALE 탐지가 하류 원장을 자동으로 표시한다.
4. **AS-L9를 잰다** — 사람이 가이드를 끌어 축 오차 **0.5°**에 드는가.
   **이 도구의 성패가 거기 있다.** ⚠ 자동화가 안 되므로(사람이 끄는 것이다) 실사용 관측이어야 한다.
5. `QUESTIONS.md`의 **d·e**가 같은 표본으로 함께 답이 나온다(지평선이 수평인가 · 롤이 몇 도인가).

그 밖에 열려 있는 것은 `QUESTIONS.md`(사람 판단 다섯) · `DEFERRED.md` · `PROTOTYPE.md` §5.

## 완주 시점의 상태

### 무엇이 서는가

**빈 화면에 그리면 소실점이 잡히는 순간 그때까지의 획이 3D로 올라가고, 그 뒤로는
시작점 스냅 · 축 추론 · 미리보기가 실시간으로 돈다.** 궤도로 돌려 이어 그리면 그 각도가
새 뷰가 되고, 소실점이 하나 더 잡히면 차수가 승격되며 전부 다시 풀린다. 되돌릴 수 있다.
저장·복원이 뷰와 2D 레이어까지 넘기고, 서버 없이 정적 배포로 아이패드에서 열린다.

### 무엇이 안 서는가 — **가장 큰 것 하나**

**카메라 자동 확정이 없다.** 검출의 축 방향 오차가 중앙 **4~10°**이고 필요한 것은 **0.5°**다.
획을 100개까지 늘려도 단조로 안 준다(AS-L10 반증). **롤 0 제약도 실패했다**(D-L32).
그래서 **사용자가 가이드 여섯을 끈다.** 검출은 시작 위치만 준다(D-L16).
⚠ **사람이 0.5°에 도달하는지는 표본이 0이다**(AS-L9).

### 검증 현황

vitest **332 통과 · 1 건너뜀**(58파일) · Playwright **22 통과 · 1 건너뜀 / 23**
(coords 9 · stage 12 · static_deploy 2) · pytest **71/71** · tsc·빌드 통과 ·
**GitHub Actions 세 job 전부 초록**(`build` · `deploy` · `measure`) ·
selfcheck **1757건 · STALE(상수) 0 · STALE(정의) 0** ·
**상수 해시 `34db7279`** · **정의 해시 `a45078a5`** ·
자동 검사 커버리지 훑기 **4** / 인용 91 / 게이트 **9**(**값 대조 3 · 면제 6**) / 하네스 40.

⚠ **검증 문구는 두 칸으로 적는다** — "STALE 0"만 적었다가 STALE(정의) 1건을 놓친 적이 있다(#1).
⚠ **면제는 통과가 아니다** — `reachability_absent` 게이트 여섯은 도달 가능성이 **미상**이고
근거로 쓰지 않는다.
⚠ **건너뛴 것의 정체를 적는다**(#32) — Playwright 쪽은 `coords`의 `[양성 채널] dpr 1`
(dpr 1에서는 ×dpr 자리가 제자리라 **가를 것이 없다**), vitest 쪽은 `real_ink`
(`sessions/`가 비었다). **둘 다 의도된 것이고 빌드본 없음이 아니다.**
⚠⚠ **CI에서는 vitest 건너뜀이 셋이다** — `data/quickdraw`가 없어 `one_stroke_one_axis`·
`bend_share`가 더 건너뛴다. **통과로 세지 않는다**(`web/test/dataDeps.ts`).
⚠⚠ **벽시계 수치(지연·복원 ms)를 문서에 인용하지 않는다** — 같은 기계에서도
2.1↔2.2ms · 1152↔1175ms로 흔들려 **문서가 곧 낡는다**(실제로 낡았다). 판정은 자릿수이고
값은 `stage_browser.json`의 `l_d3`에 있다.

### 문서 여섯 (A-5)

| 문서 | 무엇 |
|---|---|
| [`README.md`](README.md) | 이름·유래 · 실행 · 배포·원격 접속 · **되는 것/안 되는 것** · 라이선스(MIT, D-L33) |
| [`PROTOTYPE.md`](PROTOTYPE.md) | 알려진 결함 · 측정된 지표 · **§5 실사용 취약점**(⚠ 예상이고 측정이 아니다) |
| [`DEFERRED.md`](DEFERRED.md) | 우회·단순화·미룬 항목 — **트리거를 함께 적는다** |
| [`DECISIONS.md`](DECISIONS.md) | 잠정 결정 D-L1~**D-L36**과 기각 사유 |
| [`QUESTIONS.md`](QUESTIONS.md) | **사람 판단 다섯** — 각각 "지금 하는 것"(잠정값)을 함께 |
| [`HANDOFF.md`](HANDOFF.md) | 이 문서 |

## 이번 세션이 한 것

| 항목 | 결과 |
|---|---|
| **#40 신설**(사람 지시) | #35를 만든 세션이 그 필드를 **항등으로 채웠다**. `gate()`가 **수치 + 출처**를 강제하고 selfcheck가 **값 대조**한다. `scan_citation_hashes`도 신설 — 옛 해시 23곳을 잡았다 |
| **L-D.3 종단 검증** | **사전 등록 다섯 전부 통과** — 상자 둘 24획 · **승격 연쇄 3회** · 표식 [2,3] · 지연 10ms 미만 · 스크린샷 동일 |
| **#40 리뷰어 11건**(높음 4) | **넷 다 내 쪽이 틀렸다.** `deg_N`이 **준** 각도인 것을 실측으로 읽어 "게이트 실패"라 적었다 — **철회** |
| **옛 UI 삭제** | 잠금 둘(`coords.spec`·`wiring.test`)을 **먼저 옮긴 뒤** 지웠다 |
| **#41 신설**(사람 지시) | 중단 조건에 걸렸다고 적었으면 실제로 멈춘다 |
| **삭제 리뷰어 16건**(높음 4) | **셋이 내 쪽이 틀렸다** — `[회귀]` 팔이 #21이 아니었고(#30이다), "보호가 `stage.spec`으로 옮겨졌다"가 틀렸고, **#40의 규칙을 바로 다음 항목이 어겼다** |
| **L-D.4 문서** | 이름 반영 · `QUESTIONS.md` 신설 · `PROTOTYPE.md` §5 · README를 L 기준으로 다시 씀 · D-L33~D-L36 |
| **CI 실패 고침**(사람 지시) | 외부 데이터 의존을 **`test/dataDeps.ts` 등록처**로 묶고(건너뛰되 **통과로 안 센다**), `pages.yml`에서 **배포와 측정을 갈랐다**. **Pages가 배포됐다** |

## L이 남긴 사실 — **판정의 단일 출처는 `assumptions.md`다**

이 문서는 그것을 **요약하지 않는다** — 요약이 갈리면 다음 세션이 낡은 쪽을 읽는다.
AS-L1~AS-L12에 반증과 조건이 전부 있고, 각 항목의 과정은 `progress.md`에 있다.

**결론을 가장 크게 바꾼 넷만 적는다**(전문은 `assumptions.md`):

- **AS-L1 반증** — "선 3개가 한 점에서 만나면 소실점"은 **상자의 꼭짓점이 완벽히 만족한다**.
  조건 셋을 더해야 성립한다(도달거리 · 지지 가중합 · 국소화 가능성).
- **AS-L3·AS-L6 반증** — 계획서가 자동 판정 신호로 지정한 둘이 **입사 관계에서 유도한 양**이라
  **사영 불변**이고 f에 반응하지 않는다. 대체 후보 둘도 안 섰다(L-C.3).
- **AS-L10 반증** — 획이 쌓여도 축 오차가 임계로 안 내려간다. **자동 확정 경로가 없는 이유**다.
- **AS-L11 절반 반증** — "미배치는 대기다"는 **개수 회수에 대해서만** 참이다.
  회수된 것의 품질은 확정보다 나쁘고 **1점 구도에서는 90/93이 조용히 틀린다.**

## ⛔ 남겨 둔 폐기 코드 — **측정 근거의 재현용**

`segment.ts`(G 실험) · `cameraFit.ts`(D-L6 기각) · `horizon.ts`(D-L32 기각).
**셋 다 배선하지 않는다.** 지우면 그 판정의 근거를 다시 만들 수 없다.
`stage0/out/archive_W/e2e.json`도 같은 이유로 남겼다 — 만드는 스펙이 없어져 **다시 못 돌리므로**
`@해시` 인용을 뗐고 **현재형 근거로 쓰지 않는다**(`PROTOTYPE.md` §2.4에 그렇게 박았다).

## ✋ 브라우저에서 보는 절차

**배포본**: <https://hyunwookyu.github.io/brunelleschi/> (설치 없이 바로 열린다)

로컬:
```bash
cd web && npm run dev:5222
```
→ **`http://localhost:5222/l.html`**(루트로 들어가도 그리로 보낸다)

⚠ **캔버스 크기 굳음이 세 번 재발했다**(AS-C7). 확인은 **창을 앞에 두고** 한다.
`S2S.diag()`의 `size_heal`이 자가 치유 발동 횟수다.

합성 상자를 넣으려면 **측정 하네스와 같은 픽스처**를 쓴다 — `await import('/test/scene3d.ts')`.
손으로 "소실점 쪽으로 t만큼" 만들면 **화면에서 평행한 아핀 상자**가 나와 수렴이 0이다.
`e2e/stage.spec.ts`의 `setup(page, { boxes: 2 })`가 그 절차를 담고 있다.
앱 손잡이는 `window.S2S` — `doc()` · `cam` · `stage` · `refresh` · `SIZE_HEAL` · **`diag()`** ·
`snap(p)` · `snapTargets()` · `hoverSnap()` · `live()` · `axisLock()` · `views()` ·
`order()` · `promoteOrderNow()` · **`chainTrace()`** · `orderMarks()` · `revertToOrder()` ·
`exportObj()` · `exportGltf()` · `buildDoc2()`.

## 개발 명령

```bash
cd web && npx vitest run && npx tsc --noEmit && npm run build && npx playwright test
```
```bash
python -m pytest tests/ -q && python selfcheck.py
```
⚠ `vitest`는 **`web/`에서** 돌린다. 저장소 루트에서 돌리면 다른 vitest가 잡히고 원장 경로가 어긋난다.
⚠ **`playwright` 앞에 `npm run build`를 넣는다** — `dist/`가 낡으면 `static_deploy`가 실패한다.
`camera_gate`가 103초 · `horizon`이 160초 걸린다.

**외부 데이터가 있어야 하는 자리에서 조용히 건너뛰지 않게** 하려면:
```bash
cd web && S2S_REQUIRE_DATA=1 npx vitest run
```
그 모드는 **건너뛰기를 실패로 바꾼다**(`test/dataDeps.ts`). CI는 그 플래그 없이 돈다.

## 미해소 (전문은 `DEFERRED.md`)

- **실획 표본이 0이다**(AS-C1). 위 "다음에 할 일"이 그것이다
- **AS-L9**: 사람이 축 오차 0.5°에 도달하는지 미측정. 대리값도 **핸들 하나** 가정 위에 있다
- **#21의 진짜 회귀 팔이 없다** — `coords`의 양성 채널은 **탐침의 눈**만 확인한다.
  "앱이 dpr을 두 번 적용해도 그 스펙이 실패한다"는 **미확인**이다
- **하네스와 앱이 같은 배치 옵션 출처를 쓰는지 아무도 안 본다** — 옛 `wiring` 검사의 대상이
  사라졌고 "`stage.spec`으로 옮겨졌다"는 **철회했다**(D-L35)
- **`Math.max(1, n)`이 12개 하네스 70곳에 남아 있다** — **D-L21 재개 전에**
  `promote`·`axis_live`의 53곳을 먼저 가른다
- ~~배포 URL이 없다~~ **배포됐다**: <https://hyunwookyu.github.io/brunelleschi/>
  (2026-08-16 첫 실행, 세 job 전부 초록). ⚠ **실제 Pages의 동작 확인은 원장 밖 관측이다** —
  `static_deploy.spec.ts`는 여전히 **로컬 정적 서버**를 잰다
- **저장소 디렉토리 이름이 `SKETCH2SPACE`다** — `brunelleschi`로 바꾸는 것은 사람이 한다
- **v1 저장을 열 수단이 없다** — 옛 UI를 지웠다. 데이터는 `current` 키에 그대로 있으므로
  읽는 스크립트를 한 번 짜면 된다(D-L29)
- **사전 캐시 목록만의 몫이 미측정이다** — 앱이 자기 서비스 워커를 매번 재등록해 못 가른다(D-L36)
- **인라인 지표식 전수 확인 안 함** · **`end_jitter = 0` 못박힘 전수 확인 안 함**
- **승격이 배치를 −168로 줄인다** · **1점 구도에서 승격이 무너진다**
- **스냅으로 못 고치는 몫**(D-L15) — L-B.4·B.5·B.7 어느 것도 이 비를 안 바꿨다
- `min_guide_ratio` 0.45가 **수직 가이드에 원리적으로 도달 불가**다(588.18 < 651.2)
- L-B.1 3건 · L-B.2 3건 · L-B.3 3건 · L-B.4 3건 · L-D.0 3건 · L-D.4 4건 · 삭제 4건이
  `DEFERRED.md`에 있다

## ⚠ 반드시 먼저 읽는다

1. `CLAUDE.md` — 작업 지침. §2에 게이트가 있다
2. **`PITFALLS.md`** — 반복해서 걸린 것 **41개**. **읽는 시점은 항목 착수 시**이고,
   걸리는 번호를 `progress.md`에 적는 것이 읽었다는 증거다
3. `docs/line_plan.md` — **개정 2**
4. 이 문서
5. `progress.md`(**루트의 것 하나뿐이다** — 쉘 작업 디렉토리가 `web/`일 때 `cat >>`를 하면
   `web/progress.md`가 생긴다) · `DECISIONS.md`(D-L1~**D-L36**) ·
   `assumptions.md`(AS-L1~**AS-L12**) · `DEFERRED.md` · **`QUESTIONS.md`**

## 최근 커밋
```
8ba355c 옛 UI 삭제 리뷰어 대응 — 높음 넷 중 셋이 내 쪽이 틀렸다
832d76b L-D.4 문서 — 이름 Brunelleschi 반영 · QUESTIONS.md 신설 · 실사용 취약점 기록
0e1f2ea 옛 UI 삭제 — 잠금 둘을 새 UI로 옮긴 뒤 지웠다. #41 신설
f4f9383 #40 리뷰어 대응 — 내 판정 하나가 프레임 혼동이었다. 철회한다
9f2b778 L-D.3 종단 검증 — 상자 둘, 사전 등록 다섯 전부 통과
```
