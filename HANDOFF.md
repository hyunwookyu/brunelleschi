# HANDOFF.md — 세션 인계

다음 세션은 이 문서를 읽고 이어서 진행한다. 사람의 개입은 그 한 줄뿐이다.

## 현재 단계 — **L-B.1 완료, L-B.2 착수 전**

**계획서는 [`docs/line_plan.md`](docs/line_plan.md) 개정 2**다(2026-08-15, 사람 지시로 편입).
개정 1은 `docs/archive/line_plan_r1.md`에 있고 **폐기가 아니라 대체**다 —
개정 2가 §2~§4·§8을 "개정 1 유지"로 참조하므로 **그 절들의 원문은 아카이브 쪽**이다.
다만 **판단 근거로 아카이브를 인용하지 않는다**(D-L1).

### 이번 세션이 한 것

| 항목 | 결과 |
|---|---|
| 계획 개정 2 편입 | 제품 정의(투시도와 입체가 **동등·동시**) · 미배치를 **대기 상태**로 · **뷰 시스템** · **UI 재작성** |
| 계획서 정정 | 개정 2가 L-A.7 **이전에** 쓰여 정정 전 수치를 담고 있었다 — §5.2·§5.4·§12·§13을 고쳤다 |
| **L-B.1 단일 뷰포트** | `sceneCam.ts`·`doc.ts`·`camState.ts`·`stage.ts`·`mainL.ts`·`l.html`. 리뷰어 **2회 29건 대응** |

### 지금 앱이 하는 일 (`l.html`, 브라우저·Playwright에서 확인함)

```
빈 화면에 그린다(2D 레이어) → `소실점 추정`(검출은 초안)
  → `가이드 조정`으로 끝점을 끈다 → 그리드·미리보기가 실시간으로 따라온다
  → `확정` → 획이 축별로 재분류되고 3D로 올라간다. **3D가 잉크가 있던 그 픽셀에 그려진다**
  → `궤도`로 돌린다(확정 뷰의 2D 층은 숨는다) → `확정 시점으로` 되돌아온다
```

## ⚠ 반드시 먼저 읽는다

1. `CLAUDE.md` — 작업 지침. §2에 게이트가 있고 제품 정의가 개정 2로 갱신됐다
2. **`PITFALLS.md`** — 반복해서 걸린 것 **29개**.
   **읽는 시점은 항목 착수 시**이고, 걸리는 번호를 `progress.md`에 적는 것이 읽었다는 증거다
3. `docs/line_plan.md` — **개정 2**. §9(뷰 시스템)·§10(UI 재작성)이 새로 생겼다
4. 이 문서
5. `progress.md`(맨 끝 두 절) · `DECISIONS.md`(D-L1~D-L11) · `assumptions.md` · `DEFERRED.md`

## L-B.1이 남긴 사실 셋

**① 이 항목에는 측정이 하나도 없다 — 전부 설계 보장이다**(PITFALLS #5).
`seg_gap = 0`, `three_vs_project_gap = 5.0e-13`, 그리고 **`chord_dev`도 보장이다** —
초판이 "사용자가 실제로 보는 변화"라는 실측으로 적었는데 리뷰어가 뒤집었다.
**대조군에 양성 채널을 붙여 확인했다**: f×0.8→1.2에서 3D 세그먼트 길이는 1.04 → 0.74로
움직이는데 `chord_dev`는 **소수 넷째 자리까지 불변**이다. `stage_cam.json@1a6f104b`.
→ **셋 다 임계를 걸지 않는다**(D-L11). `test/scene_cam.test.ts`가 불변식으로 잠근다.

**② 참 카메라에서도 `coarse` 등급 배치가 0.44다**(precise 0.63 · medium 0.60, 분모 1440).
`coarse`는 `lift_gate`·`camera_gate`가 **한 번도 안 잰 등급**이다. 카메라가 완벽해도
잉크가 거칠면 무너진다. **L-B.3 스냅이 시작점을 3D에 못박은 뒤 다시 잰다**(DEFERRED).

**③ 핸들 예산은 늘었지만 "해소"가 아니다.** 단일 뷰포트의 가이드는
**607.6 / 618.5 / 1233.3 / 1254.6 / 1256.8 / 1327.7px**인데 `min_guide_ratio` 0.45가
요구하는 **651px에 둘이 미달**이다. **가장 짧은 가이드가 정확도를 정하므로** 개선은
4배가 아니라 **약 2배**(600px 행 1.28~4.33px 대 옛 300px 행 0.63~2.16px)다.
그나마 **핸들 하나만 움직인다는 가정** 위의 값이고 사람은 핸들 12개를 만진다.

## 다음에 할 것 — **L-B.2**(계획서 §11의 L-B 목록 2번)

**가이드 선 조정 UI와 실시간 피드백**(§5.2). 지금도 끌 수는 있으나:
- 초안 가이드가 **검출 지지선에서 나오므로** 길이를 그림이 정한다 → 짧은 둘이 요구치 미달(위 ③)
- 무한원 스냅(3°) 표시는 있으나 **각 축이 지금 얼마나 민감한지**를 화면이 안 알린다
- 확정 전 미리보기는 있으나 **어느 획이 왜 안 올라가는지**가 안 보인다

그다음 L-B.3(스냅) · L-B.4(축 판정·실시간 색·미리보기) · L-B.5(축 고정) ·
L-B.6(뷰 시스템) · L-B.7(승격 연쇄) · L-B.8(궤도 후 계속 그리기).

**L-B 게이트**(§11): 시작점 스냅 성공률 / 스냅된 시작점에서의 배치 정확도 /
미리보기와 확정의 일치(0) / 승격 연쇄가 실제로 일어나는가.
**통과 못 해도 멈추지 않는다** — A-2대로 우회하고 DEFERRED에 남긴다.

## ⛔ 아직 폐기하지 않은 코드

**옛 UI(`index.html` + `src/main.ts` + `src/ui/cameraPanel.ts`)는 L-B 게이트 통과 전까지
지우지 않는다**(A-4·사용자 지시). 새 UI는 **`l.html` + `src/mainL.ts`**다.
`vite.config.ts`가 둘 다 빌드한다. 게이트를 지나면 `l.html`이 `index.html`이 된다.
`segment.ts`(G 실험) · `cameraFit.ts`(D-L6 기각) · 2D/3D 분리 뷰포트도 그대로 있다.

## ✋ 브라우저에서 보는 절차

```bash
cd web && npm run dev:5222
```
→ **`http://localhost:5222/l.html`** (새 UI) · `http://localhost:5222/` (옛 UI)

⚠ **캔버스 크기 굳음이 세 번 재발했다**(AS-C7). 확인은 **창을 앞에 두고** 한다.
`SIZE_HEAL.count`가 자가 치유 발동 횟수다(L-B.1 종단에서 3).

합성 상자를 넣으려면 **측정 하네스와 같은 픽스처**를 쓴다 — `await import('/test/scene3d.ts')`.
손으로 "소실점 쪽으로 t만큼" 만들면 **화면에서 평행한 아핀 상자**가 나와 수렴이 0이다.
`e2e/stage.spec.ts`의 `setup()`이 그 절차를 그대로 담고 있으니 복사해 쓰면 된다.
앱 손잡이는 `window.S2S`(`doc()` · `cam` · `stage` · `refresh` · `SIZE_HEAL`)다.

## 개발 명령

```bash
cd web && npx vitest run && npx tsc --noEmit && npx playwright test
```
```bash
python -m pytest tests/ -q && python selfcheck.py
```
⚠ `vitest`는 **`web/`에서** 돌린다. 저장소 루트에서 `--root web`으로 돌리면 원장 경로가
어긋나 2건이 실패한다. `camera_gate`가 105초 걸린다(전체 107초 중 대부분).

검증 현황: vitest **233/233**(39파일) · tsc 0 · pytest **44/44** · playwright **9/9** ·
selfcheck 플래그 1451 · **STALE 0** · 상수 해시 `1a6f104b`.

## 미해소

- **실획 표본이 여전히 0이다**(AS-C1). 모든 판정이 Quick,Draw 유도 합성 잉크 위에 있고
  AS-C10이 그 모델을 **양방향으로 틀렸다**고 적었다. `sessions/`에 들어오면 다른 작업보다 먼저 측정한다
- **AS-L9**: 사람이 축 오차 0.5°에 도달하는지 미측정. 대리값(핸들 예산 1.3~4.3px)도
  **핸들 하나** 가정 위에 있다
- **분포형 오차의 정확한 0을 selfcheck가 못 본다** — 꼬리 제외 규약과 충돌한다(DEFERRED)
- **참 카메라·coarse 배치 0.44** — `lift.ts`의 문제이고 L-B.3 뒤에 다시 잰다(DEFERRED)
- `min_guide_ratio` 0.45가 새 뷰포트에서도 가이드 둘을 자른다 — L-B.2에서 다시 본다
- 리뷰어 2회차 14건 중 **3건을 DEFERRED로 넘겼다**(위 셋). 나머지 11건은 대응했다
