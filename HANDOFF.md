# HANDOFF.md — 세션 인계

다음 세션은 이 문서를 읽고 이어서 진행한다. 사람의 개입은 그 한 줄뿐이다.

## 읽어야 할 문서 순서
1. `CLAUDE.md` — 작업 지침(A-1~A-4, 자료구조, 참조 저장소 금지 규정, 이론서 지도)
2. `docs/sketch3d_plan.md` — **현행 계획서**(코드명 S). 폐기된 3종은 `docs/archive/`
3. 이 문서 (아래 현재 단계)
4. `DECISIONS.md` 맨 아래 "S 전환" 절 — D-S1~D-S4
5. `DEFERRED.md`, 필요할 때 `docs/perspective_theory.md`, `assumptions.md`(맨 위 S 절)

## ⚠ 이 프로젝트에서 반복해서 걸린 것 두 가지

1. **문서의 수치가 원장(JSON)보다 뒤처진다.** 수치를 문서에 쓰기 전에 `stage0/out/`의
   JSON을 그 자리에서 다시 읽는다. 재측정했으면 그 값을 인용한 문서를 `grep`으로 전부 찾아 고친다.
2. **측정이 자기확인이 된다.** S-2에서 실제로 걸렸다 — 합성 활이 대칭이라 최소제곱 주축이
   참 방향과 정확히 나란해져 판정이 공짜로 맞았다(조용히 틀림 0.0007 vs 치우친 조건 0.0361).
   **수치가 이상하게 좋으면 노이즈 모델의 성질을 재고 있는 것은 아닌지 먼저 의심한다.**

## 현재 단계
**S-A · S-0 · S-1 · S-2 완료 → S-3(획 → 3D) 진행**

| 단계 | 상태 |
|---|---|
| S-A 전환 정리 | 완료 — 폐기 제거, `wire/`→`s3d/`, assumptions 이관 |
| S-0 기반 | 완료 — three.js 뷰포트(온디맨드 렌더), PWA 골격, geom3d 테스트 10건 |
| S-1 카메라 확정 | 완료 — §3 전체. 지면 격자(진짜 투시 투영), 화면 밖 VP 화살표 |
| S-2 방향 판정 | 완료 — 미결 2항목 결정(D-S2·D-S3), 자기확인 발견·정정 |

## 다음에 할 일 — S-3 획 → 3D (계획서 §4.2 · §4.4 · §4.5)

**이 단계가 이 방식의 전제를 시험한다. 유일한 중단 조건이 여기 있다**(CLAUDE.md A-2):
미리보기와 확정 결과가 사용 불가능할 만큼 다르거나, 회전 후 형태를 알아볼 수 없으면 멈추고 보고한다.

만들 것은 `web/src/s3d/place.ts`(신규)와 `Stroke` 자료구조(계획서 §5).
이미 있는 것: `geom3d.ts`의 `closestPoints`·`rayThrough`·`axisDirection`·`rayPlane`·`project`,
`axis.ts`의 `classifyStroke`.

**이미 만든 것**: `s3d/stroke.ts` — `Stroke` 자료구조, `wobbleNormal`(떨림 평면 두 방식),
`placeStroke`, `previewStroke`, `reprojectionGap`, `findAnchor`, `attachStroke`, `reprojectAll`.
**테스트가 아직 없다** — 다음 세션이 먼저 쓴다(A-4).

1. ~~**`Stroke` 자료구조**~~ 완료. — `{ id, pts2d, pts3d, axis, anchorRef, mesh, layer, color, width }`.
   **`pts2d`를 반드시 보존한다**(카메라 재조정 시 전체 재투영).
2. **§4.2 앵커 체인** — 씨앗(첫 획 시작점, 지면 Z=0 가정) → 축 방향 광선-직선 최근접점 →
   기존 획과의 교차가 새 앵커 → 반복. **연결된 것만 배치한다.** 떠 있는 획은 미룬다.
3. **§4.4 떨림 평면 — 두 방식을 모두 구현해 비교한다**(assumptions S-2).
   (가) 시선을 포함하는 평면: 그린 순간의 화면과 완전히 일치
   (나) 화면 평행 평면: 회전 후 형태가 다르게 보일 수 있다
   **판단 기준은 회전 후 자연스러움**이다. AS-8(앵커 체인 오차 전파)도 같은 산출물에 넣는다 —
   연쇄 길이 대비 깊이 오차. W-2가 "지배항은 위치"를 측정했고 W의 대응(정점 병합)은 폐기됐다. 측정을 `stage0/out/wobble_plane.json`에 남긴다.
   안 정해지면 A-2 단계 2 — 시선 포함 평면으로 고정하고 DEFERRED에 기록.
4. **§4.5 미리보기 ↔ 확정 차이 측정**(AS-3, **중단 조건**).
   **임계는 이미 숫자로 박혀 있다**(DECISIONS D-S6, 측정 전에 정했다):
   재투영 화면 좌표차의 **중앙값 > 획 길이의 5%** 또는 **p90 > 15%**면 중단하고 보고한다.
   화면 대각이 아니라 **획 길이** 대비다.
   그리는 중에는 임시 평면(시선 수직)에 놓고, `finStroke`에서 축 판정 후 전체 재투영한다.
   **화면상 차이**(재투영 결과를 다시 투영한 것 vs 원본 `pts2d`)를 화면 대각 대비로 잰다.
   `stage0/out/preview_gap.json`에 남긴다. 이 값이 크면 미리보기 방식을 바꾼다(계획서 §9).
5. **이 단계 끝에서 그린 획이 3D에 놓이고 회전해서 볼 수 있어야 한다**(A-4).
   S-4(튜브 메쉬)는 그 다음이므로 지금은 `THREE.Line`으로 충분하다.

**측정 설계 주의**: S-2에서 걸린 자기확인이 여기서도 가능하다. 합성 획으로 재투영 차이를
재면 **노이즈 모델이 대칭인 만큼 차이가 작게 나온다.** `renderInk`의 `skew`를 0.5가 아닌
값으로도 재고, 대표값은 치우친 조건으로 읽는다.

## 잠정 결정
`DECISIONS.md` — D-S1(three.js 0.185), D-S2(크게 휜 획 미분류), D-S3(여러 축 미분류·분절 안 함),
D-S4(합성 활 마루를 인자로 — 자기확인 차단). 그 위는 폐기된 접근의 것이다.

## ⚠ 가장 큰 위험 — AS-6 (한 획 = 한 축)

리뷰어가 짚었고 **바로 재서 확인됐다**. 실획(n=1816)에서
**한 축에 담기는 획은 29.6%뿐이고 69.9%가 한 획에 방향을 둘 이상 담는다.**
임계에 둔감하다(25°→80°에서 0.70→0.58) — 검출기 설정의 산물이 아니다.

계획서 §4.1의 기본 동작("획 전체가 하나의 축에 배정된다")이 여기 걸려 있다.
표본이 낙서(Quick,Draw)라 **상한**으로 읽지만, 숙련자가 낮을 것이라는 예상은 **미측정**이다.
**이 값이 그대로면 S-2가 대부분을 미분류로 보내 도구가 쓸 수 없다.**

지금 할 수 있는 것이 없다(숙련자 획을 잴 수단이 없다). **S-10에서 실제 도구 획으로
재측정하는 것이 필수**이고, 높으면 설계를 다시 봐야 한다 — 사용자가 명시적으로
"여기서 축이 바뀐다"를 지정하는 방식 등(조용한 분절은 §1을 깨므로 여전히 금지).

## 미해소 지적
- 리뷰어 S-A·S-0 17건 중 **높음 6건 전부 대응**(progress.md의 대응 표).
  남은 중간·낮음 중 처리 안 된 것: DEFERRED의 단계 배정이 아직 W-3/W-6/W-8을 가리킨다
  (소멸 항목과 존속 항목을 S 번호로 재배정해야 한다),
  selfcheck 산출물에 정적 탐지가 훑은 파일 수 필드가 없다,
  잉크 캡처 회귀 테스트 0(S에서도 유지 항목인데 무검증).
- **S-1·S-2 리뷰어 호출은 아직 안 했다.** S-3 완료 시 함께 호출한다.
- selfcheck 1건: `square.too_bent_rate = 0` — square 획은 전부 `multi_axis`로 먼저
  걸려서 곡률 판정까지 가지 않는다(순서상 정상). 원인 확인됨.
- `bend_max` 0.18은 측정이 정당화하지 못한다(표본이 임계를 시험하지 못함) — S-10 이후 재결정.
- AS-1("숙련자 획에서 정확한가")은 **표본이 없다**. S-2가 잰 것은 합성이다.

## 개발 환경
```bash
cd web && npx vite --port 5222 --strictPort
```
`S2S_HTTP=1`을 주면 HTTP(기본은 HTTPS). 5173은 다른 프로세스가 잡고 있어 5222를 쓴다.
브라우저 확인은 마우스 `PointerEvent`를 직접 디스패치하면 된다.

테스트: `cd web && npx vitest run` · `python -m pytest tests/ -q` · `python selfcheck.py`.
카메라 기준값 재생성: `python web/test/gen_camera_ref.py`.

## 최근 커밋
```
0727bf8  S-A·S-0 리뷰어 대응 — 새 접근의 단위 가정이 실획에 반박된다
84c0649  HANDOFF 갱신
c6cea24  S-2: 방향 판정 — 미결 두 항목 결정, 자기확인을 스스로 잡았다
44a4d76  S-1: 지면 격자와 화면 밖 소실점
f58fec1  S-0: three.js 뷰포트 + PWA 골격
778046f  S-A: 접근 전환 정리 — 획을 버리지 않는다
```
