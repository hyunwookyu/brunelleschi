# HANDOFF.md — 세션 인계

다음 세션은 이 문서를 읽고 이어서 진행한다. 사람의 개입은 그 한 줄뿐이다.

## ⛔ 현재 단계 — **L-A 게이트 실패. 멈추고 보고했다** (사람의 결정을 기다린다)

계획서가 정한 **유일한 중단 조건**에 걸렸다(`docs/line_plan.md` §9 · CLAUDE.md §2).
사전 등록한 통과 기준과 관측은 `progress.md`의 "L-A.4 — 게이트 측정과 판정: 실패" 절에 있다.
원장: `stage0/out/lift_gate.json@034fc089` · `stage0/out/vp_detect.json@034fc089`.

**한 줄 요약**: **올리기(§5.2)는 맞고 카메라가 틀렸다.**
참 카메라를 주면 조용히 틀린 배치가 **34/430 = 0.079**(S 0.057 · G 0.278 대역)이고
배치율은 **0.995**(S 0.846 · G 0.887)다. 검출 카메라로는 조용히 틀림이 **230/359 = 0.641**로
무너진다. 지배항은 f가 아니라 **3D 축 방향 오차 중앙 4.18° · p90 11.75°**다
(f만 10% 틀리게 줘도 형태 오차 중앙은 0.098 → 0.115뿐이다).

**그리고 계획서가 제안한 자동 검사 둘이 원리적으로 성립하지 않는다.**
§5.3의 전환 픽셀 차이도 §6.2의 재투영 어긋남도 **f를 ±20% 틀리게 줘도 안 움직인다** —
입사 관계는 사영 불변이므로 카메라를 검증할 수 없다. 둘 다 **측정이 아니라 설계 보장**이다.

### 사람이 고를 것 — 넷

1. **소실점 정밀도를 올린다(미시도, 가장 싼 다음 측정).** 상자 하나는 축당 4획이라 지렛대가
   짧다. **획 수를 늘려 축 방향 오차 곡선을 재는 것**이 첫 후속이다.
2. **카메라를 사용자가 확정한다.** 옛 S 접근의 소실점 찍기 UI가 **그대로 살아 있다.**
   고유 항목 하나(§1.2의 "초기 2D → 카메라 확정")를 잃는 대신 형태가 참 카메라 대역으로 간다.
3. **L-B로 넘어가고 초기 부정확을 받아들인다.** 실시간 스냅은 시작점이 확정된 뒤의 이야기이므로
   그 뒤 획은 구조에 정확히 붙는다. 대가는 첫 구조가 돌려 보면 기울어 있는 것이다.
4. **실획 표본을 받고 다시 잰다.** 지금 판정은 Quick,Draw 유도 합성 잉크 위에 있고
   AS-C10이 그 모델을 **양방향으로 틀렸다**고 이미 적었다. 대상 사용자는 숙련자다.

## 지금까지 (접근 전환, 코드명 L)

**2026-08-15 세 번째 전환.** 현행 계획서는 [`docs/line_plan.md`](docs/line_plan.md) 하나다.
S(`sketch3d_plan`)와 G(`segment_plan`)는 폐기 사유를 달아 `docs/archive/`에 있다.
**아카이브를 판단 근거로 인용하지 않는다**(D-L1).

만드는 것은 **입력이 손 획인 SketchUp 선 도구**다. 프리핸드 보존은 미뤘다(획이 직선
세그먼트가 되고 손떨림은 버린다. `pts2d`는 계속 보존한다).

### 끝난 것

| 항목 | 무엇 | 파일 |
|---|---|---|
| L-A.1 | 문서 전환 — 계획서·원장 아카이브, 유효 항목 이관 | `docs/archive/*_SG.md`, D-L1 |
| L-A.2 | 소실점 후보 검출 | `web/src/s3d/vpDetect.ts`, `web/test/vp_detect.test.ts`(11) |
| L-A.3 | 초기 3D 올리기 — 2D 교점 제약 **동차 선형계 일괄 해** | `web/src/s3d/lift.ts`, `web/test/lift.test.ts`(9) |
| L-A.4 | 게이트 측정 — **실패 판정** | `web/test/lift_gate.test.ts` → 원장 둘 |

**핵심 기전**(L-A.3): 획 하나의 3D 직선은 해석 평면(눈+화면 직선) 안에 있고 축이 방향을
정하므로 **미지수가 획마다 하나**(`s_i`)다. 화면 교점 하나가 `s_i·(r·e_j) − s_j·(r·e_i) = 0`이라는
**선형** 제약이 되고, 전체가 `A s = 0`이 되어 **남는 자유도는 전역 배율 하나**다.
앵커도 씨앗도 없다. 정확한 투영에서 참 상자가 배율까지 복원된다(형태 오차 < 1e-6).

**리뷰어 지적 20건(높음 14)에 전부 대응했다.** 아래가 그 목록이다.

## ⚠ 반드시 먼저 읽는다

1. `CLAUDE.md` — 작업 지침(A-1~A-5)
2. **`PITFALLS.md`** — 반복해서 걸린 것 26개. **L-A.2가 이 중 셋을 그대로 어겼다.**
   HANDOFF 안에 두면 전환 때 지워지므로 따로 뺐다
3. `docs/line_plan.md` — 현행 계획서
4. 이 문서
5. `progress.md`(L-A 기록·게이트 사전 등록·**L-A.4 판정**) ·
   `DECISIONS.md`(D-L1~D-L8, D-C1~C6) · `assumptions.md`(AS-L1~L8, AS-C1~C10)

## 리뷰어 대응 (L-A.1/L-A.2에 20건, 높음 14 — **전부 대응했다**)

| 지적 | 어떻게 대응했나 |
|---|---|
| 수치가 **원장 밖**에 있다 | `vp_detect.json` · `lift_gate.json`을 냈다. 모든 수치를 `@034fc089`로 인용한다 |
| **D-L2가 자기를 반박한다** — `vpMisfit`은 이미 각도량 | 맞다. D-L2를 다시 썼다: **단위 변경은 아무것도 고치지 않는다**(PITFALLS #24 신설). `misfit 0.06 = 6.892°`이므로 2.5°는 오히려 3배 엄격하다. 실제로 고친 것은 D-L3·D-L4였고 첫 실패의 소실점 셋은 전부 **꼭짓점**이었다 |
| **오배정 분모**가 행마다 다르다 | 분모를 "소실점에 배정된 획"으로 정의하고 `unassigned`·`population`을 함께 낸다 |
| "2.5°가 낫다"가 표에서 안 나온다 | 구도 5개로 다시 재니 **1.5~6°에서 확정률 0.82~0.85, 오배정 0.110~0.122로 거의 평탄**하다. 값은 유지하되 "둘 다 낫다"는 주장을 뺐다 |
| **끝점 오차 0~0.01만** 쟀다 | **0.03·0.05를 넣었다**(실획 자릿수). 그 구간에서 참 카메라로도 조용히 틀림이 0.52·0.63이다 |
| AS-L1이 반증됐는데 ⏳ | ❌로 바꾸고 관측을 적었다 |
| **계획서 §5.1이 안 고쳐졌다** | §5.1·§5.2·§5.3에 개정 블록을 넣었다(A-3). "정밀도가 필요한 값이 아니다"를 철회했다 |
| **D-L5의 대조가 결론을 지지하지 않는다** | 맞다. 대안 설명이 옳았다 — 구도를 늘리니 f 중앙이 0.0921 / 0.0860 / **0.0793**으로 움직인다. **결론을 뒤집어 `len²` 가중을 채택했다.** 길이비도 함께 낸다(2.03~4.44) |
| "직교 조건은 카메라가 검증한다"가 틀렸다 | 맞다. f²<0은 둔각만 거른다. 그 문장을 뺐다 |
| AS-L3·AS-L6이 자기참조 유형 3 | **반례 검사를 사전 등록하고 돌렸다.** f 0.8~1.2배에서 `segGap`이 전부 0.0 — 지표를 폐기했다. 대체로 제안한 교점 잔차도 안 움직인다 |
| AS-L2에 AS-20 수치가 없다 | 적었다. 그리고 **가짜 교점률을 게이트 지표로 세었다**(0.076~0.174) |
| `archive_SG/` 이동이 살아 있는 근거를 가져간다 | **이동이 불필요해졌다** — 전체 테스트를 돌리면 각 원장이 현재 스냅샷으로 다시 써진다. STALE은 지금 **1건**(`e2e.json`, Playwright 미실행) |
| "반복해서 걸린 것" 목록 이관 | `PITFALLS.md` 신설(26개) |
| HANDOFF가 폐기된 G를 가리킨다 | 재작성 |
| 밴드 1.5~6°가 표에 없다 | 여섯 행을 전부 원장에 넣었다 |
| `min_reach`가 한쪽뿐 | **1점 구도(소실점이 그림 안)를 넣어 거짓 음성 방향을 쟀다** — `reach 4`에서 참 소실점이 죽는다(3점 검출 0/24). D-L8 |
| 게이트에 사전 등록 수치가 없다 | `progress.md`에 **측정 전에** 박았다(조건·분모·기준) |
| AS-C8이 금지된 비교를 한다 | 두 지표를 갈라 적고 `bend_max` 0.18이 잠정값임을 표시했다 |
| f 오차 9%는 다른 행의 값 | 채택 동작점 값으로 고쳤고, **f가 지배항이 아니라는 것**을 L-A.4가 새로 밝혔다 |
| STALE 21 → 20 | 고쳤다(지금은 1) |

## ⛔ 아직 폐기하지 않은 코드 (L-A.5)

계획서 §8.2는 배치 계층·`segment.ts`·분리 뷰포트 등을 폐기하라고 했다.
**게이트 통과 뒤에 지운다**(A-4) — 먼저 지웠다면 게이트가 실패한 G-A에서 앱이 안 도는 상태로
멈췄을 것이다. 지금 앱(`main.ts`)은 **여전히 옛 배치 계층**을 쓰고 브라우저에서 그대로 열린다.
`vpDetect.ts`·`lift.ts`는 **나란히** 있고 앱에 배선되지 않았다.

## ✋ 브라우저에서 보는 절차

```bash
cd web && npm run dev:5222
```
→ `http://localhost:5222` (**새 탭에서 연다** — 레이아웃 타이밍, AS-C7)

## 개발 명령

```bash
cd web && npx vitest run && npx tsc --noEmit && npx playwright test
```
```bash
python -m pytest tests/ -q && python selfcheck.py
```

검증 현황(2026-08-15 L-A.4 시점): vitest **200/200**(33파일) · tsc 0 · pytest 37/37 ·
selfcheck 플래그 1340 · **STALE 1**(`e2e.json` — Playwright를 이번에 안 돌렸다) ·
상수 해시 `034fc089`.

## 최근 커밋
```
04d73d2 L-A.3 초기 3D 올리기 — 미지수가 획마다 하나인 동차 선형계
e2cda3e L-A.2 소실점 후보 검출 — 계획서 문장 그대로는 작동하지 않았다
99b0d8d 접근 전환 — line_plan.md를 docs/로 편입(코드명 L), 원장을 새로 시작
```
