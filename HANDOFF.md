# HANDOFF.md — 세션 인계

다음 세션은 이 문서를 읽고 이어서 진행한다. 사람의 개입은 그 한 줄뿐이다.

## 읽어야 할 문서 순서
1. `CLAUDE.md` — 작업 지침(A-1~A-4, 자료구조, 참조 저장소 금지 규정, 이론서 지도)
2. `docs/sketch3d_plan.md` — **현행 계획서**(코드명 S). 폐기된 3종은 `docs/archive/`
3. 이 문서 (아래 현재 단계)
4. `DECISIONS.md` 맨 아래 "S 전환" 절 — **D-S1~D-S18**
5. `DEFERRED.md`(맨 아래 S-3 두 절), `assumptions.md`(맨 위 S 절, **AS-1~AS-16**), 필요할 때 `docs/perspective_theory.md`

## ⚠ 이 프로젝트에서 반복해서 걸린 것 (열넷)

1. **문서의 수치가 원장(JSON)보다 뒤처진다.** 수치를 문서에 쓰기 전에 `stage0/out/`의
   JSON을 그 자리에서 다시 읽는다. 재측정했으면 그 값을 인용한 문서를 `grep`으로 전부 찾아 고친다.
   **S-3에서 또 걸렸다** — 이전 실행의 "268점"이 세 문서에 퍼져 있었고 원장에는 없었다(리뷰어가 잡았다).
2. **측정이 자기확인이 된다.** S-2는 합성 활의 대칭성, **S-3은 합성 잉크가 끝점을 참 모서리에
   못으로 박고 있던 것**이었다(`sin(πu)`가 양 끝에서 0). 그 상태로 앵커 체인을 재니
   "오차가 누적되지 않는다"가 나왔다. **수치가 이상하게 좋으면 노이즈 모델의 성질을 재고 있는
   것은 아닌지 먼저 의심한다.**
3. **지표가 정의의 귀결일 수 있다.** S-3의 떨림비가 그랬다 — 채택한 평면에서 극값인 것이
   그 평면의 정의였다. **지표 하나가 이기면 그 지표가 왜 이기는지 먼저 묻는다.**
   (족 전체를 스윕해 보는 것이 답을 준다.)
4. **모델이 문제를 만들지 못할 수 있다.** S-2b의 첫 왜곡 모델은 **정상 카메라**였다 —
   왜곡을 넣었다고 생각했는데 다른 카메라를 넣은 것이라 파이프라인이 알아서 흡수한다.
   **반례 테스트를 모델 자체에도 건다**("이 왜곡이 다른 카메라로 흡수되지 않는가").
6. **원장을 재실행하는 변경을 했으면 그 값을 인용한 문서를 전부 `grep`한다.**
   → **이제 기계가 잡는다**(STALE). 다만 잡는 것은 **상수뿐**이고 알고리즘 변경은 여전히 사람이 본다.
   S-2b의 b는 `AXIS_TOL` 기본값이라 **다른 단계의 측정까지 바꿨다** — 그것을 모르고
   S-3 문서를 그대로 뒀다. 판정 규칙을 건드리면 판정을 쓰는 **모든** 원장이 바뀐다.
8. **분포의 꼬리를 본다.** S-5에서 중앙값(0.03)만 출력해 p90(0.87)을 놓칠 뻔했다.
   중앙값이 좋아도 p90·최댓값을 함께 찍는다.
9. **"왜 안 되는지" 추측하지 말고 카운터를 넣는다.** S-6에서 45°만 안 고쳐진 이유를
   추측할 뻔했는데, 세어 보니 **38.5%가 자유단**이라 검사가 아예 안 걸린 것이었다.
10. **성공률을 셀 때 무엇이 섞였는지 먼저 가른다.** S-6 (2)에서 회수율을 0.867로 셌다가
   고쳤다 — **축으로 잘못 배정된 사선이 놓인 것**까지 세고 있었다. 그건 회수가 아니라
   조용히 틀린 배치다. 갈라 세니 84%(회수)와 15.8%(오배정)로 따로 보였다.
7. **"이래서 필요하다"는 주장에는 대조군을 붙인다.** S-4에서 두 번 걸렸다 —
   선분 보간(차이 없음)과 평행 이송(특이점에서만 갈림). 둘 다 대조군을 넣자 주장이 좁혀졌다.
11. **한계 집합을 뺄셈으로 만들지 않는다.** S-6 (2b)에서 "A가 B보다 더 놓는 158획"으로
   기각 근거를 세웠는데 **그런 집합은 없었다**(두 팔의 배치 집합이 포함관계가 아니다).
   S-6 (3)에서 **같은 형태로 재발**했다 — 폴백의 성적을 면 경로와 섞어 읽어 결론이 반대로
   나왔다. **직접 센 집합으로만 비율을 낸다.**
12. **동작점을 하나 고르지 않는다**(D-S7). S-6 (2b) 초판이 끝점 오차 0.01 하나로 결론을
   냈는데, 0.02(실획에 더 가까운 자리)에서는 이득이 3.2배 → 1.13배로 사라졌다.
13. **절단값이 결론을 정하면 그 결론은 없다.** 0.1·0.2·0.5를 다 낸다.
14. **시드 변동폭이 결론의 여유보다 크면 그 결론은 없다.** 31% 대 28%로 기각했다가
   시드 범위가 0.20~0.34인 것을 보고 근거를 바꿨다.
5. selfcheck가 자기참조 **유형 3**을 잡는다 — "복원한 값을 그 복원에 쓴 연산으로 되검사하는가".
   걸리면 그 값은 측정이 아니라 **설계 보장**일 수 있다. 보장이면 원장에 적고 임계를 걸지 않는다.

## 현재 단계
**S-A~S-6 완료 · S-7 (1) 미배치 처리 완료 → S-7 나머지 편집 기능**

> **핵심 기능 + 회수 경로가 다 섰다.** 그리기 → 회전 → 이어 그리기 → **못 놓은 획을 손으로 붙이기**.
> **지금 사용자 획을 받을 수 있다** — 아래 "실획을 주시려면" 절이 그 절차다.

| 단계 | 상태 |
|---|---|
| S-A~S-5 | 완료 (앞선 표는 progress.md에 있다) |
| S-6 (1) 가림 접합 | 완료 — 정합성 + 깊이 타당성 두 검사(D-S13) |
| S-6 (2) 면 위 사선 | 완료 — 면은 축 두 개가 편다(D-S14) |
| S-6 (2b) 축 오배정 | 완료 — **검사를 첫 시점에도 켠다. 되돌리기는 기각**(D-S16) |
| S-6 (3) 경사·자유곡선 | 완료 — **경사 VP 안 만든다. 폴백은 곧은 획에만. 굽음은 검증 밖**(D-S17) |
| S-7 (1) 미배치 처리 | 완료 — **고치기 도구**(D-S18). D-S15의 (b)~(d) |

**브라우저에서 지금 되는 것**: 소실점 셋 → 그리기 → 축 색 판정 → 3D 배치 → 회전 →
`여기서 그리기`로 이어 그리기 → **`고치기`로 못 놓은 획을 직접 붙이기**(후보 점 클릭 또는
깊이 슬라이더). 미확정 배치는 **주황색**으로 구분된다. 하단 `획 내보내기`로 실획 세션을 받는다.

## ⚠ 이번 세션이 바꾼 것 — 다음 세션이 **먼저 알아야 할 것**

1. **첫 시점에도 정합성 검사가 켜졌다**(D-S16). 배치율이 0.95 → 0.82로 떨어졌다.
   **이것은 의도된 것이다** — 검사가 떨어뜨리는 집단은 남기는 집단보다 2.8~13배 자주 틀려
   있었다. 남은 것은 미배치로 남고 **S-7의 `고치기`가 받는다.**
2. **굽은 획은 평면 경로 둘 다에서 빠졌다**(D-S17). `bend > 0.18`이면 면 위 사선도 자유 곡선
   폴백도 안 쓴다 — 두 검사가 다 **끝점 둘**만 보므로 굽음이 검증되지 않는다.
   **AS-7이 실획의 26.5%가 곡선이라 하므로 미배치의 주된 원인이 될 수 있다**(단 그 값은
   다른 임계에서 나왔다 — `bend > 0.18`인 실획 비율은 **아직 잰 적이 없다**).
3. **상수 이름이 둘 바뀌었다**: `view_far_end_check` → `far_end_check`,
   새 상수 `retry_as_face`(0) · `span_far_end`(1) · 옵션 `freeBendMax`.
   현재 상수 해시 **`0803f3fe`**, selfcheck **700건 · STALE 0**.
4. **앱에 §4.3이 아예 배선돼 있지 않았다**(S-6 2b에서 발견). 측정만 되고 `settle`이 옵션 없이
   불리고 있었다. **측정했다고 앱이 쓰는 것은 아니다** — 다음에도 배선을 확인한다.

## 다음에 할 일 — S-7의 나머지

1. **돌린 시점의 미배치 획을 그 시점에 겹쳐 그린다.** 지금은 목록에 `(돌린 시점)`으로 뜨고
   "그 시점에서만 보입니다"라고만 안내한다. `#ink3d` 캔버스에 `pts2d`를 파선으로 그리면 된다
   (그 좌표계가 곧 그 캔버스다). `main.ts`의 `drawBelowInk`에 대응하는 3D 쪽 훅이 필요하다.
2. **돌린 시점에서도 `고치기`가 되게 한다.** 지금 `editClick`은 첫 카메라 좌표만 다룬다.
   `viewPlaceCtx`로 후보를 뷰 좌표에 투영하면 같은 흐름이 선다.
3. **undo/redo · 레이어 · 지우개 · 다중 선택.** 참조 저장소 코드를 복사하지 않는다(§3).
   undo는 `drawn` 배열과 `rawPoints`·`viewOrigin`·`provisional` 셋을 함께 되돌려야 한다.
4. **돌린 시점의 평면 경로**(DEFERRED S-6 3). `addStrokeFromView`에는 `facePlanes`·`freeSpan`이
   없다 — 돌린 뒤 그은 면 위 사선·자유 곡선은 안 놓인다. **측정 없이 켜지 않는다**
   (`view_measure.test.ts`에 팔을 붙여 재고 판단한다).

그 다음은 **S-8 면 판단**(A안 = 선만 유지로 먼저 확인) → S-9 저장·배포 → S-10 종단 검증.

## ✋ 브라우저에서 보는 절차

```bash
cd web && npm run dev:5222
```
→ `http://localhost:5222` (**새 탭에서 연다** — 레이아웃 타이밍, AS-11)

1. 왼쪽 위 `축1`/`축2`/`축3`으로 소실점 셋을 찍는다(짧게 탭).
2. `그리기`로 상자를 그린다. 축 색으로 칠해지고 오른쪽 3D에 쌓인다.
3. **`고치기`로 못 놓은 획을 붙인다** — 획을 누르면 후보 점이 뜨고, 점을 누르면 붙는다.
   양쪽을 다 고르면 확정, 한쪽만 고르면 **미확정(주황)**이다.
4. 오른쪽 3D를 드래그해 돌리고, `여기서 그리기`로 그 시점에서 이어 그린다.

`window.s2s`에 `{strokes, panel, viewport, strokeView, addStroke, addStrokeFromView,
worldAxes, setViewMode, placeCtx, refresh, exportSession, editClick, select, candidates,
provisional}`이 있다. 마우스 `PointerEvent`를 `#ink`·`#ink3d`에 디스패치하면 된다.

## ✍ 실획을 주시려면 (지금 가능하다)

이 프로젝트의 **가장 큰 위험 둘(AS-6·AS-12)이 표본 문제**이고, 지금 그것을 잴 수 있다.

1. 위 절차대로 **평소대로** 그린다. 고쳐 그리지 말고 평소 손대로 긋는다 —
   재는 것이 손의 성질이므로 잘 그리려 하면 측정이 망가진다.
2. 하단 **`획 내보내기`** → JSON을 저장소의 **`sessions/`** 에 넣는다.
3. `cd web && npx vitest run test/real_ink.test.ts`
   → `stage0/out/real_ink.json`에 AS-6 위반율 · **AS-12 끝점 겨냥 오차** · 미배치율 ·
   판정 분포 · 화각별 편차가 기록된다. 지금은 `awaiting_samples`다.

담기는 것: 원본 포인터 표본 `[x, y, t, 필압, 기울기X, 기울기Y]` + 판정 결과 + 배치 결과 + 카메라.
담기지 않는 것: 사용자 식별 정보. 자세한 이유는 `README.md` 0절.

## ⚠ 실획이 `sessions/`에 들어오면 — **다른 작업보다 먼저 측정한다**(사용자 지시)

```bash
cd web && npx vitest run test/real_ink.test.ts
```
잴 것: AS-6(현재 0.699) · AS-12(현재 대각의 3~5%) · AS-13 · AS-15 ·
미배치율(현재 합성 0.234) · 획별 편차의 위치 의존성(S-2b 근거) · 체계적 왜곡의 실제 크기
(현재 warp ≲ 0.3 가정).

**Quick,Draw와 크게 다르면 재조정한다**: `AXIS_TOL` · 접합 반경(D-S10) · 미배치 임계(D-S7) ·
S-2b의 a·b 파라미터 · 왜곡 모델 유효 범위 · S-6의 검사 둘(D-S13).
**재조정하면 STALE이 하류 측정을 자동으로 표시한다** — 표시된 것을 전부 재측정하고 문서를 갱신한다.

실획이 없으면 **기다리지 말고** S-6을 계속한다.

## S-3이 남긴 것 — 다음 단계가 알아야 할 것

### 채택·정정
- **떨림 평면 = `facing`**(D-S8). L을 담되 시선에 가장 정면으로 서는 평면.
  계획서의 두 후보 어느 쪽도 아니다 — 눈을 지나는 평면은 **원리적으로 불가능**하고
  (화면에 직선 하나로 맺힌다), 화면 평행 평면은 축을 잃는다.
  **주장은 좁혀져 있다**: 족 안 ±20°는 구분되지 않고, 채택은 극단의 기각으로 지지된다.
- **AS-3의 임계 대상 정정**(D-S7). 원래 지정한 "재투영 화면 좌표차"는 **항등**이라 발동할 수
  없었다. 임계값은 두고 대상을 접합 이동량과 미배치율로 옮겼다.
  **미배치율 임계 20%는 사후에 정한 잠정값이며 S-10까지 판정 근거가 아니다.**
- **지면 평면은 수직축이 정한다**(`geom3d.groundFrame`). 눈높이 게이지는 `EYE_HEIGHT` 한 군데.
- **몸통 접합은 필수다**(§4.2 3번). 끝점끼리만 이으면 끝점 오차 2%에서 47%가 앵커를 못 찾는다.

### S-7 (1)이 남긴 것
- **`고치기` 도구가 미배치의 최종 경로다**(D-S18). 후보 점 클릭 또는 깊이 슬라이더.
  **사용자가 고른 앵커에는 검사를 걸지 않는다** — 자동 경로가 못 푼 것을 풀라고 만든
  경로에 같은 검사를 다시 거는 것은 자기모순이다.
- **미확정을 표시한다**: 축+한쪽 또는 양쪽이면 확정, 나머지는 **주황색**. 확인:
  bend 0.306 획이 후보 하나로는 끝 오차 0.804(미확정), 양쪽을 고르면 0.0055(확정).
- **자료구조를 안 늘렸다** — `Stroke.color`로 표시하고 미확정 여부는 앱의 `Set`에 둔다.

### S-6 (3)이 남긴 것
- **경사 소실점을 안 만든다**(D-S17). 후보가 **점이 아니라 직선**이라 획 하나로는 못 가르고,
  획 2·3·4·6으로 묶으면 산포는 줄지만 **수평 VP 쪽으로 편향된 점에 수렴**한다
  (참 경사 VP 쪽에 서는 비율이 획 6에서 1~4%). 원인은 AS-14 — 계통 왜곡이 획 간 공유다.
  **다시 볼 길은 사용자가 경사 VP를 직접 찍는 것**이 가장 단순하다(DEFERRED).
- **자유 곡선 폴백은 양 끝 앵커가 정하는 `facing` 평면**이고 **곧은 획에만** 켠다.
- **⚠ 굽음은 검증 밖이다.** 평면 경로 둘의 검사가 다 **끝점 둘**만 본다 — 곧은 획이면
  끝점 둘 + 평면이 획을 유일하게 결정하지만 굽은 획은 가운데가 검증되지 않는다.
  굽은 획은 면 경로 62% · 폴백 51%가 크게 틀렸고 **최선의 평면은 0.078**이었다
  (답이 없는 게 아니라 둘 다 못 고른다). `bend > 0.18`이면 두 경로 다 안 쓴다.

### S-6 (2b)가 남긴 것
- **정합성 검사가 첫 시점에도 켜졌다**(D-S16). 근거는 **떨어뜨리는 집단의 농축비**
  (27조합 최솟값 1.72). **이득은 끝점 오차에 단조 감소한다** — 0.005에서 3.2배,
  0.02에서 1.13배다. **채택은 방법이 성립한다고 주장하는 구간(≲1%)에 걸린다.**
- **되돌리기는 기각**(같은 결정). 되돌린 사선은 정확했지만(3~6%) 같은 신호가 정상 모서리를
  3~4배 더 끌어들이고 그쪽이 27~63% 틀린다. **겨냥할 수단이 없다.**
- **판정의 여유(margin)를 신호로 쓰면 갈릴 수 있다** — 새 기전이라 DEFERRED에 있다.

### S-6 (2)가 남긴 것
- **면을 만들지 않는다**(D-S14). 면은 축 두 개가 펴는 평면이고 앵커가 위치를 정한다 —
  루프 검출도 S-8도 필요 없다. 어느 면인지는 **S-6 (1)의 정합성 검사**로 고른다.
- **회수 경로는 선다**: 미분류 사선의 84%가 놓이고 p90 0.057. _출하 구성(검사 켬)에서는 같은 시드에서 0.802다._
- **⚠ 병목은 판정이다**: 사선의 **15.8%가 축으로 오인**되고 그렇게 놓이면 오차 중앙 0.448.
  §4.1의 문제이지 §4.3의 문제가 아니다.
- **표시 결함을 고쳤다**: 돌린 시점 획이 왼쪽 캔버스에 엉뚱한 자리로 그려지고 있었다.
- **미배치 처리 결정은 D-S15에 있고 구현은 S-7이다** — 놓지 않되 버리지 않는다.

### S-6 (1)이 남긴 것
- **돌린 시점의 앵커는 검사 둘로 고른다**(D-S13). 정합성(양 끝 후보가 있을 때) +
  깊이 타당성(자유단). **하나로는 각도에 따라 구멍이 남는다** — 45°에서 38.5%가 자유단이었다.
- **남는 한계**: 두 검사는 **내부 정합성**을 강제할 뿐 참값 일치를 보장하지 않는다.
  엉뚱한 두 모서리에 **일관되게** 붙으면 통과한다(최댓값 0.88~1.06 잔존).
  자동으로 더 풀 근거가 없다 — **사용자에게 보이고 고르게 하는 것**이 남은 길이다(S-7).

### S-5가 남긴 것
- **돌린 뒤에는 소실점을 다시 찍지 않는다.** 축 방향은 그대로이고 소실점만 다시 계산된다
  (`s3d/viewCamera.ts`). `Viewport.pose()`가 three 자세를 우리 규약으로 낸다 — **뒤집기는 거기 한 군데**.
- **접합률 = 배치율**(0.849~0.994). 새 시점에서 놓인 획은 전부 기존 기하에 붙었다.
- **회전 판 불변식 0px**(7조합 전부). 설계 보장이라 임계를 걸지 않는다.
- **가림 접합** — 화면에서 접합을 판정하므로 돌린 시점에서 앞뒤가 겹치면 깊이가 전혀 다른
  획에 붙었다(p90 0.39~0.92). **S-6 (1)에서 해소**(위). 깊이 가드는 대체됐다.
- **고각 0에서 수직 소실점이 무한원이 된다** — 없는 소실점을 지어내지 않는 처리가 작동한다.
  _고각을 안 넣었을 때는 그 경우가 한 번도 안 나왔다. 지시가 명시해서 알아챘다._

### S-4가 남긴 것
- **튜브는 직접 짰다** — three.js `TubeGeometry`는 반지름이 상수라 못 쓴다.
  프레임은 **평행 이송**이고, 그것이 필요한 것은 **접선이 기준축을 정확히 통과하는 획**이다
  (곧은 획·나선에서는 대조군과 차이가 없다. 특이점 반례 테스트가 근거다).
- **굵기는 `Stroke`에 없다.** 앱의 6튜플 맵(`rawPoints`)을 `StrokeView`가 조회한다.
  세계 반지름은 `(w_px/2)·z/f` — 그린 굵기에서 온다.
- **획당 삼각형 336(중앙), 생성 0.320ms** — 온디맨드 렌더로 충분하다.
- **재지 않은 것 둘이 자기참조였다**(화면 폭 = 그은 굵기 / 회전 후 1/깊이).
  둘 다 구성의 귀결이라 불변식 테스트로만 잠갔다.

### S-2b가 남긴 것
- **사용자 획의 오차는 무작위가 아니다**(AS-14). 손떨림 + **체계적 왜곡**(위치 의존·획 간 일관).
  이론서 18.4 — 화각 60°를 넘으면 사람이 수렴을 완화해 그린다. **고치지 않고 받아들인다.**
- **a(위치 의존 임계)·b(상대 순위) 켜져 있다.** `classifyStroke`에 카메라를 넘기면 켜진다.
  앱은 넘긴다. **c(획 간 일관성)는 꺼져 있다** — 구제 29건 중 8건이 틀렸다(D-S9).
  `s3d/consensus.ts`에 남아 있고 측정에서만 켠다.
- **왜곡은 구도에 달려 있다.** 화각 60° 안쪽에서는 아예 생기지 않는다 —
  S-3이 쓴 구도(60.7°)가 그 경계라 S-3 수치는 이 효과에 거의 영향받지 않는다.
- 예상이 반증됐다(지시는 c가 가장 클 것으로 예상). **b가 가장 컸다.**

### 실획 수집 — **수단은 섰다. 측정은 사용자 획이 들어온 뒤다**

`stage0/out/real_ink.json`이 지금 `awaiting_samples`다. 획이 들어오면:
```bash
cd web && npx vitest run test/real_ink.test.ts
```
절차는 `README.md` 0절. 받은 JSON을 `sessions/`에 넣으면 된다.
**사용자 획을 기다리지 않는다** — S-4를 계속한다.

### ⚠ 가장 큰 위험 둘

**AS-12 (신규, S-3이 발견) — 끝점 겨냥 오차가 성립 구간 밖일 수 있다**
이 방식은 끝점 겨냥 오차가 **그림 대각의 1% 근처**여야 성립한다(그 이상이면 축이 정해진 획의
30~70%가 안 놓이고 깊은 연쇄가 무너진다). 손에 있는 유일한 실획 자릿수는 Quick,Draw
`closure_gap` 환산 **0.028~0.051**로 그 **3~5배**다. **그대로면 도구를 쓸 수 없다.**
완화 근거 둘(표본이 낙서다 / closure_gap은 다른 양이다) 다 미측정.
**S-10에서 실제 도구 획으로 재는 것이 필수.**

**AS-6 (기존) — 한 획 = 한 축**
실획(n=1816)에서 한 축에 담기는 획은 **29.6%**뿐이고 69.9%가 한 획에 방향을 둘 이상 담는다.
임계에 둔감하다(25°→80°에서 0.70→0.58). 표본이 낙서라 **상한**으로 읽지만 숙련자가 낮을
것이라는 예상은 **미측정**이다. **S-10 재측정 필수.**

두 위험이 같은 곳을 가리킨다 — **실획 수집이 이 방식의 실제 시험대**다.
그때까지의 모든 수치는 합성이다(V-o·AS-1).

**AS-13 — 이 두 수치로 설계 결정을 하지 않는다.** 둘 다 Quick,Draw(마우스·손가락 낙서)에서
나왔고 대상 사용자를 대표하지 않는다. 구 합성 노이즈가 실제보다 가혹했던 것과 같은 유형이고
방향만 반대다. **tolerance를 넉넉히 잡거나 축 판정을 보수적으로 만들거나 미배치 폴백을 과하게
설계하지 않는다.** 현재 값은 잠정이고 실획 측정 후 조정한다. (D-S10이 그 적용 사례다 —
접합 반경을 키우면 미배치가 0이 되지만 키우지 않았다.)

### 그 밖에 알아둘 것
- **깊이 오차의 지배항은 앵커 체인이 아니라 카메라다**(AS-9). f 오차 10%가 체인 전체보다 크고
  연쇄를 타지 않는 전역 배율 편향이다(이론서 16.4). 평면 비교는 참 카메라 조건에서 한 것이다.
- **측정 조건 표기를 반드시 함께 읽는다.** 원장의 `conditions` 블록에 (축 판정/oracle,
  끝점 오차, skew, 등급, 접합 방식, 시드)가 있다. **참 축 + 끝점 오차 0은 가장 유리한 조건**이다.
- **구도가 하나뿐이다**(요 35°·피치 15° 3점, 직육면체 하나). 원장 `single_composition_warning`.

## 미해소 지적 — **이번 세션에서 새로 생긴 것 먼저**

- **S-6 (3) 리뷰어 2차 잔여**: ① `bend_max` 0.18을 이 표본이 시험하지 못한다(굽은/곧은
  픽스처가 이봉이라 그 사이 어디에 두어도 같다. D-S2가 같은 상수에 대해 이미 같은 말을 적었다).
  **이제 이 값이 판정만이 아니라 배치 여부를 정한다** — S-10에서 두 역할을 함께 재정한다.
  ② 등급 스윕이 없다(medium 하나. `coarse`의 저주파 0.0937은 임계의 절반이라 곧은 획이 더
  잘려 나갈 수 있다). ③ `free_curve`의 62%는 픽스처 비율 1:1에 얹혀 있다(성분은 25%와 98%다) —
  **결정의 근거는 62%가 아니라 "굽음은 검증 밖"이라는 구분**이라고 문서에 적어 두었다.
- **`preview_gap.json`의 `unanchored_of_classified`는 검사를 끈 구성의 값이다**(D-S16 이후).
  출하 구성의 값은 `misassign_retry.json`에 있다(0.139/0.153/0.313). 하네스를 고쳐 두 구성을
  다 내게 할지는 DEFERRED에 있다.
- **AS-2·D-S8의 평면 비교가 중앙값만으로 돼 있다**. 꼬리를 병기했고 순서는 유지된다
  (`facing` p90 0.7284 < `screen` 0.8221 < `sight` 1.2301). 다만 중앙값의 5~6배 격차가
  p90에서는 1.1~1.7배로 좁혀진다 — **채택은 유지되나 주장은 좁혀졌다**.
- **원장 조건 블록에 판정 설정이 없다**(S-2b·S-4 잔여). S-6 (3)에서 **같은 형태로 재발**했다
  (팔을 가르는 `freeBendMax`가 원장 `opts`에 없었다 — 고쳤다). `wobble_plane`·`preview_gap`은
  아직 그대로다.

## 미해소 지적 (이전 세션)
- 리뷰어 S-3 2차 잔여 6건 중 **S-4 배정 2건은 해소**(선분 보간 대조군 — 추정이 반증됐다,
  등급 순서 역전 — 끝점 오차가 원인). 나머지는 S-5 배정.
- 리뷰어 **S-2b·S-4 21건 대응 완료**, 잔여 6건은 DEFERRED(기록·재현성, S-5 배정).
- **가장 중요했던 지적**: S-2b의 b가 켜지면서 **S-3 측정이 바뀌었는데 문서가 뒤처졌다**.
  세 번째 재발이다. 원장을 재실행하는 변경을 했으면 **그 값을 인용한 문서를 전부 grep**한다.
- (d) 왜곡 모델 추정은 하지 않았다(지시가 시간을 쓰지 말라 했고, 그 국소 형태인 c가 28% 틀렸다).
- S-A·S-0 리뷰어 잔여 중 아직 처리 안 된 것: DEFERRED의 단계 배정이 W-3/W-6/W-8을 가리킨다
  (S 번호로 재배정 필요), selfcheck 산출물에 정적 탐지가 훑은 파일 수 필드가 없다,
  **잉크 캡처 회귀 테스트 0**(S에서도 유지 항목인데 무검증 — S-10 Playwright).
- **STALE 자동 탐지가 섰다** — 공유 상수가 바뀌면 그것으로 잰 산출물이 자동으로 잡힌다
  (`test/constants.ts` · `selfcheck.scan_stale_constants`). 세 번 재발한 유형이다.
  **새 임계를 만들면 `test/constants.ts`에 넣는다** — 빠지면 안 잡힌다.
  현재 STALE **0건**, 상수 해시 `0803f3fe`. 문서는 `산출물.json@해시`로 인용한다.
- selfcheck **700건** — 대부분 oracle 조건의 정의상 0이다. **분류 문장이 부정확한 것이
  DEFERRED에 올라 있다**(S-5). `tube_render.json`은 0건인데 `build_failed=0`이 키 이름 규칙을
  비껴간 것이라 그것도 DEFERRED다.
- `bend_max` 0.18은 측정이 정당화하지 못한다 — S-10 이후 재결정.
- AS-1("숙련자 획에서 정확한가")은 **표본이 없다**. 조용히 틀리는 비율이 조건에 따라
  0.036~0.13으로 흔들린다.

## 개발 환경
```bash
cd web && npx vite --port 5222 --strictPort
```
`S2S_HTTP=1`을 주면 HTTP(기본은 HTTPS). 5173은 다른 프로세스가 잡고 있어 5222를 쓴다.
`npm run dev:5222 --prefix web`이 HTTP+5222 한 줄이고 `.claude/launch.json`이 그것을 쓴다.
브라우저 확인은 마우스 `PointerEvent`를 직접 디스패치하면 된다.
`window.s2s`에 `{strokes, panel, viewport, strokeView, addStroke, addStrokeFromView, worldAxes,
setViewMode, placeCtx, refresh, exportSession, editClick, select, candidates, provisional}`이 노출돼 있다.

테스트: `cd web && npx vitest run` · `python -m pytest tests/ -q` · `python selfcheck.py`.
카메라 기준값 재생성: `python web/test/gen_camera_ref.py`.

## 최근 커밋
```
fea33ce  S-7 (1): 미배치 획을 사용자가 붙인다 — 검사하지 않고, 미확정을 표시한다
f9fcd9a  S-6 (3): 경사 VP는 안 만든다. 폴백은 곧은 획에만. 굽음은 검증 밖이었다
b89b256  S-6 (2b): 정합성 검사를 첫 시점에도 켠다. 되돌리기는 기각 — 겨냥할 수단이 없다
4cfc663  S-6 (2): 면 위 사선 — 면은 축 두 개가 편다. 병목은 판정이었다
44e1223  S-6 (1): 가림 접합의 근본 해법 — 검사 하나로는 안 되고 둘이 필요했다
057da4f  S-5: 회전 후 이어 그리기 — 핵심 기능. 가림 접합 결함을 찾았다
```
