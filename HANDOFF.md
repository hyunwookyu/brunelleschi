# HANDOFF.md — 세션 인계

다음 세션은 이 문서를 읽고 이어서 진행한다. 사람의 개입은 그 한 줄뿐이다.

## 읽어야 할 문서 순서
1. `CLAUDE.md` — 작업 지침(A-1~A-4, 자료구조, 이론서 참조 지도). **A-2의 중단 조건이 바뀌었다.**
2. `docs/wireframe_plan.md` — 현행 계획서
3. 이 문서 (아래 현재 단계)
4. `DECISIONS.md` — 이미 내린 판단. **D-01이 가장 중요하다**(중단 조건 교체)
5. `DEFERRED.md` — 의도적으로 줄인 것
6. 필요할 때 `docs/perspective_theory.md`, `assumptions.md`(맨 위 W 절부터)

## 현재 단계
**W-A · W-0 · W-1 · W-2 완료 → W-3(기하 구축) 진행 중**

W-3에서 **자료구조(`wire/model.ts`)와 배치(`wire/place.ts`)는 만들었고 테스트가 돈다**
(vitest 55/55). 남은 것은 아래 "W-3에서 반드시 할 것".

## 지금까지의 결론 네 줄
- 중단 조건은 **`optimal_rate` ≥ 0.5**로 바뀌었다(측정 **0.7125**, 통과). 다만 **이 게이트는 아직 약하다** — DECISIONS D-01 끝의 미해소 결함 넷을 볼 것. 원래 지시의
  "1획=1엣지"는 검출기가 **둔감할수록 오르므로** 게이트로 쓸 수 없다 — 격자 전체에서
  단조 증가함을 확인했다(0.217→0.345). 근거 전문은 DECISIONS D-01.
- 카메라 수학은 TS로 이식돼 Python 기준값과 13/13 일치한다. 1점 f는 **설정값**이고
  8.7/8.8 형태가정 역산은 Python·TS 양쪽에서 제거됐다.
- 제약 누산기가 §3.1대로 동작한다 — 점·선·지평선·렌즈가 같은 곳에 닿는다.
- **W-2에서 제약 재적합이 반증됐다.** 코너 오차의 지배항은 방향이 아니라 위치이고,
  선을 따로 고치면 못 준다. 분류만 쓰고 재적합은 껐다(A-2 단계 2 우회).
  **이것이 W-3의 설계를 좁힌다** — 아래를 볼 것.

## ⚠ 먼저 읽을 것 — 이번 세션에서 **가장 크게 틀린 것**

**이전 실행의 수치를 문서에 남긴 채 원장(JSON)만 갱신했다.** 리뷰어가 잡았고,
D-1b-3에서 한 번 겪은 유형이 그대로 재발했다. 게이트 값이 0.692로 문서 다섯 곳에
퍼져 있었는데 원장은 0.7356이었고, 코드 결함을 고친 뒤에는 다시 0.7125가 됐다.

**규칙**: 수치를 문서에 쓰기 전에 **원장 JSON을 그 자리에서 다시 읽는다.**
측정을 재실행했으면 그 값을 인용한 문서를 **전부** 찾아 고친다(`grep`으로 확인할 것).

## W-3에서 반드시 할 것 — 리뷰어 미해소 지적 (DEFERRED 표)

게이트(`optimal_rate`)에 **해소되지 않은 결함이 넷** 있다. W-3에서 잰다.
1. **곡선 제외분** — 획 분류로는 곡선이 28%인데 `out_of_scope`는 6~7%다. 차이만큼이
   분모에 in-scope로 들어가 값을 부풀릴 수 있다. optimal 판정에서 곡선 획을 뺀 값을 낸다.
2. **위치 오차 지표** — 지금은 엣지 **개수**만 비교해 과분할 1 + 미분할 1이 상쇄된다.
   합성 정답에는 코너 위치가 있으므로 거리 분포를 병기한다.
3. **`straight_tol` 스윕** — 게이트 값에 직접 곱해지는 인자인데 한 점(0.10)뿐이다.
   0.05 / 0.10 / 0.15에서 optimal_rate를 낸다.
4. **임계 재설정** — 격자 12개 중 0.5 미만이 양 극단 2개뿐이라 게이트가 사실상 발동하지
   않는다. 위 셋의 결과로 임계를 **측정 기반**으로 다시 정한다.

그 밖에 W-3에서 함께: **카메라 측정을 원장에 남긴다**(W-1이 하나도 안 남겼다),
**2·3점에서 렌즈를 움직였을 때 폭이 움직이는지 확인**(§3.2 게이지가 1점 전용임이 드러났다).

## 다음에 할 일 — W-3 기하 구축 (계획서 §5.1, §4.2)

**W-2가 이 단계의 설계를 좁혔다.** 코너 오차는 방향이 아니라 위치에서 온다. 선을 따로
고쳐서는 줄지 않는다. 그러므로 **코너를 명시적 정점(Vertex)으로 만들고 선들이 그것을
공유하게** 하는 것이 핵심이며, 그 순간 재적합이 하려던 일이 자연히 이뤄진다.

**만든 것**: `wire/model.ts`(Vertex/Edge/Face, 루트 3필드, 광선-직선 최근접점,
소실점↔3D 방향, 정점 병합), `wire/place.ts`(씨앗·축 배치·앵커 체인), `test/place.test.ts`(12건).
**커서 한 점 → 3D 한 점이 오차 1e-6으로 성립함을 확인했다**(정답 상자 왕복).

**남은 것**: 아래 4~6 + 화면 배선(그리기 도구가 지금은 엣지를 담아 두기만 한다).

1. ~~`wire/model.ts` — 자료구조~~ **완료**. CLAUDE.md §1에 형태가 있다.
   ```
   Vertex { id, position: Vec3, provenance }
   Edge   { id, v0, v1, direction: 0|1|2|"screen"|"free" }
   Face   { id, edges[], normal, plane }     // W-5
   Camera { vps[], principal, f, locked }    // wire/camera.ts의 CameraSolution을 감싼다
   ```
   provenance는 `measured | setting | default` 셋뿐이다. **루트 필드 제한은 유지**하되
   자료구조 변경은 사람에게 올린다(A-3 예외) — 지금 형태 그대로 가면 올릴 일이 없다.
2. ~~**광선-직선 최근접점.**~~ **완료**. 카메라가 확정되면 커서 픽셀 하나가 3D 점 하나를 정한다 —
   눈에서 커서로 나가는 광선과 (시작점 + t·축방향) 직선의 최근접점. 이것이 배치의 전부다.
   `wire/camera.ts`의 게이지(`frontalWorld`, `depthFromForeshortening`)와 맞춰야 한다.
3. ~~**앵커 체인** (§4.2)~~ **골격 완료**(연결된 것만 배치, 떠 있는 획은 보류): 씨앗(지면 접촉점, Z=0) → 알려진 방향으로 광선-직선 최근접점 →
   기존 엣지와의 교차가 새 앵커 → 반복. **연결된 것만 배치된다.**
   떠 있는 획은 깊이 미결정으로 남긴다(추정하지 않는다).
4. **정점 병합** — 가까운 두 끝점을 하나의 Vertex로 접는다. 이것이 W-2가 못 준
   위치 오차를 줄이는 자리다. **재적합 전후 코너 오차를 다시 재서 비교할 것**
   (`refit_gain.json`과 같은 형식으로 `stage0/out/anchor_gain.json`). W-2의 수치가 기준선이다.
5. **비직교 회수율(assumptions W-5) 측정.** 면 위 사선은 방향이 `free`지만 양 끝점이
   기존 기하에 닿으면 배치된다. 회수 여부는 배치로 정해지므로 수치를 여기서 낸다.
   이전 접근에서 비직교 52.6%가 범위 밖이었다(V-z)는 것이 비교 대상이다.
6. **스냅** (§4.3): 끝점/중점/엣지 위/축 방향/평행·수직. tolerance는 넉넉히 두고 W-8에서 조정.

**주의**: 눈금 있는 지면 격자(§3.8)를 W-1에서 미뤘다 — 지면 씨앗점이 생기는 여기서 깐다.

## 개발 환경
```bash
cd web && npm run dev:http -- --port 5222
```
`.claude/launch.json`에 `web-dev`가 있으나 5173이 다른 프로세스에 잡혀 있어 포트를 바꿔 쓴다.
브라우저 확인은 마우스 `PointerEvent`를 직접 디스패치하면 된다(W-1에서 그렇게 검증했다).

테스트: `cd web && npx vitest run` (edge_fidelity는 ~100초 걸린다), `python -m pytest tests/ -q`,
`python selfcheck.py`. 카메라 기준값 재생성: `python web/test/gen_camera_ref.py`.

## 잠정 결정
`DECISIONS.md` — W-A 5건(D-A1~D-A5), W-0 3건(D-01~D-03).

## 미해소 지적
- 리뷰어 W-A 14건 · W-0 17건은 전부 대응했다(progress.md의 대응 표 두 개).
- 리뷰어 W-1 17건 중 **13건 대응, 4건 미해소**(게이트 결함 넷). DEFERRED 표에 전부 있고
  위 "W-3에서 반드시 할 것"이 그 처리 계획이다. **W-2·W-3 리뷰어 호출은 아직 안 했다.**
- DEFERRED에 옮긴 잔여: `straight_tol` 근거 산출물(W-2에서 함께 낸다),
  V-A 검증 경로 소실(W-8에서 최소 수집기 재작성 + optimal_rate 재측정),
  최소분할 DP 근사, 잉크 캡처 회귀 테스트 0(W-8 Playwright).

## 최근 커밋
```
1f89dcc  W-1 리뷰어 대응 + W-3 착수: 원장과 문서 불일치 전면 정정
e0b0a34  W-2: 방향 분류 성립, 제약 재적합은 반증
bb4e446  HANDOFF 갱신
e90ee28  W-1: 카메라 확정 UI + 제약 누산기 — 화면에서 동작 확인
29187ed  W-0 2차 + W-1 카메라: 중단 지표를 양방향으로 교체, 카메라 수학 이식
c02d631  W-0: 획→엣지 매핑 측정
482adb0  W-A: 접근 전환 정리
```
