# HANDOFF.md — 세션 인계

다음 세션은 이 문서를 읽고 이어서 진행한다. 사람의 개입은 그 한 줄뿐이다.

## 읽어야 할 문서 순서
1. `CLAUDE.md` — 작업 지침(A-1~A-4, 자료구조, 참조 저장소 금지 규정, 이론서 지도)
2. **`PROTOTYPE.md`** — **되는 것과 안 되는 것 + 측정된 수치 전부**(S-10 완주 산물)
3. `docs/sketch3d_plan.md` — 계획서(코드명 S). 폐기된 3종은 `docs/archive/`
4. 이 문서 (아래 현재 단계)
5. `DECISIONS.md` 맨 아래 "S 전환" 절 — **D-S1~D-S25**
6. `DEFERRED.md`(맨 아래 S-9·S-10 두 절과 **⬆ 우선순위가 올라간 항목**),
   `assumptions.md`(**AS-1~AS-17**), 필요할 때 `docs/perspective_theory.md`

## 현재 단계 — **S-A ~ S-10 완주**

| 단계 | 상태 |
|---|---|
| S-A~S-8 · S-8b · S-8c | 완료 (표는 `progress.md`) |
| **S-9 저장·배포** | 완료 — IndexedDB 자동 저장 · OBJ/glTF · PWA · MIT(D-S23·D-S24) |
| **S-10 종단 검증** | 완료 — Playwright 한 바퀴 · 고치기 부하 판정(D-S25) |

**계획서의 단계는 끝났다.** 다음에 할 일은 아래 두 갈래뿐이고, **첫째가 절대 우선**이다.

## 다음에 할 일

### 1. ⚠ 실획 측정 — **`sessions/`에 획이 들어오면 다른 무엇보다 먼저**

```bash
cd web && npx vitest run test/real_ink.test.ts
```

지금 `stage0/out/real_ink.json`은 `awaiting_samples`다. 받는 절차는 `README.md` 0절.

잴 것: AS-6(현재 0.699) · **AS-12 끝점 겨냥 오차**(현재 대각의 2.8~5.1%, 성립 구간은 ~1%) ·
AS-13 · AS-15 · 미배치율 · 획별 편차의 위치 의존성 · 체계적 왜곡의 실제 크기.

Quick,Draw와 크게 다르면 재조정한다: `AXIS_TOL` · 접합 반경(D-S10) · 미배치 임계(D-S7) ·
S-2b의 a·b · 왜곡 모델 유효 범위 · S-6의 검사 둘(D-S13) · **S-8b의 부적합도 임계**(D-S22) ·
`bend_max`(D-S17). **재조정하면 STALE이 하류 산출물을 자동 표시한다** — 표시된 것을 전부
재측정하고 그것을 인용한 문서를 `grep`으로 전부 고친다.

### 2. ⚠ 가림 정합성은 **재 봤더니 지금 형태로 안 된다** — 다른 형태를 찾아야 한다

우선순위는 올라갔다(고치기 부하가 기준선을 넘었다). **그래서 타당성부터 쟀고 결과가
음성이다**(`occlusion.json@1671e540`): 놓인 획의 "가려진 점 비율"이 참·거짓 배치를
**아홉 칸(반경 3 × 궤도 3) 전부에서 안 가른다**. 8px에서는 **거꾸로** 간다 —
맞게 놓인 사선이 더 가려진다(45°: 0.12 대 0.045).

**원인: 가릴 것이 없다.** 면을 안 만들므로(D-S20) 가리는 것이 튜브 선뿐이고,
맞게 놓인 사선은 면 위(모서리 근처)에, 틀린 것은 빈 공간에 있다.

**다음 세션이 할 일은 "구현"이 아니라 "다른 형태 찾기"다.** 후보 셋을 `DEFERRED.md`
맨 아래에 적어 뒀다(내부 통과 = 면이 필요해 §1과 충돌 / 진짜 깊이 버퍼 읽기 /
가림이 아닌 다른 2D 밖 정보 — 시점을 하나 더 요구하기). **아무것도 안 하는 것도 답이다** —
그때는 README가 안내하는 대로 "사선은 첫 시점에서 긋는다"로 남는다.

### 3. 실획 재조정과 **함께** 해야 하는 것(따로 하면 원장이 두 번 낡는다)

- **`screen` 후보의 선택 전 거름**(반복 유형 19의 새 사례. `test/prefilter.test.ts`).
- **`sm`과 `vpMisfit`의 단위가 다른데 같은 목록에서 정렬된다** — 순위 판정 전체가 그 위에 있다.
- `bend_max` 0.18 재결정(D-S17·D-S2) · S-2b의 a·b 파라미터 · D-S22의 부적합도 임계.

그 밖의 남은 것: 레이어·다중 선택(S-7 잔여) · 고각 18° 구간의 원인 규명(D-S13) ·
`고치기`의 후보 품질(옳은 앵커가 목록에 없는 경우 21~35%. **반경 확대는 기각됐다** —
원인이 반경이 아니라 "옳은 모서리가 아직 안 놓였다"였다. `DEFERRED.md` 맨 아래).

**⚠ 값싸 보이는 개선 둘이 측정으로 기각됐다**(같은 절에 사유가 있다) — 고치기 뒤 자동 배치
재실행(공짜로 놓인 것의 47.6%가 크게 틀렸다)과 후보 반경 확대. **개수만 세지 말 것.**

## ⚠ 이 프로젝트에서 반복해서 걸린 것 (열아홉)

1. **문서의 수치가 원장(JSON)보다 뒤처진다.** 수치를 쓰기 전에 `stage0/out/`의 JSON을 그
   자리에서 다시 읽는다. 재측정했으면 인용 문서를 `grep`으로 전부 고친다.
2. **측정이 자기확인이 된다.** S-2는 합성 활의 대칭성, S-3은 합성 잉크가 끝점을 참 모서리에
   못으로 박고 있던 것. **수치가 이상하게 좋으면 노이즈 모델의 성질을 재는 것은 아닌지 먼저 의심한다.**
3. **지표가 정의의 귀결일 수 있다.** S-3의 떨림비. **하나가 이기면 왜 이기는지 먼저 묻는다.**
4. **모델이 문제를 만들지 못할 수 있다.** S-2b의 첫 왜곡 모델은 정상 카메라였다.
   **반례 테스트를 모델 자체에도 건다.**
5. selfcheck가 자기참조 **유형 3**을 잡는다 — 복원한 값을 그 복원에 쓴 연산으로 되검사하는가.
   보장이면 원장에 그렇게 적고 **임계를 걸지 않는다**.
6. **원장을 재실행하는 변경을 했으면 인용 문서를 전부 `grep`한다.** → 상수는 기계가 잡는다(STALE).
   알고리즘 변경은 여전히 사람이 본다.
7. **"이래서 필요하다"는 주장에는 대조군을 붙인다.** S-4에서 둘, **S-9에서 또 하나**
   ("`pts3d`는 재생성된다" → 지우개 스윕이 그 말을 갈랐다).
8. **분포의 꼬리를 본다.** 중앙값이 좋아도 p90·최댓값을 함께 찍는다.
9. **"왜 안 되는지" 추측하지 말고 카운터를 넣는다.** S-6의 자유단 38.5%.
10. **성공률을 셀 때 무엇이 섞였는지 먼저 가른다.** 회수와 조용히 틀린 배치를 갈랐다.
11. **한계 집합을 뺄셈으로 만들지 않는다.** 직접 센 집합으로만 비율을 낸다.
12. **동작점을 하나 고르지 않는다**(D-S7). **S-9에서 또 걸릴 뻔했다** — 끝점 오차 0.01만
    쟀다가 0.02를 넣으니 사용자 지정 배치가 9 → 34로 늘었다.
13. **절단값이 결론을 정하면 그 결론은 없다.** 0.1·0.2·0.5를 다 낸다.
14. **시드 변동폭이 결론의 여유보다 크면 그 결론은 없다.** **비율보다 분자/분모로 적는다** —
    S-9의 "22%"가 시드 하나에 0.11~0.33으로 흔들렸다(리뷰어가 잡았다).
15. **측정 경로와 앱 경로가 갈라진다.** → `appPlace.ts` 단일 출처 + `wiring.test.ts`가 잡는다.
    **덮는 범위는 `PlaceOpts`뿐**이고 `PlaceCtx`·`AxisCfg`·`TUBE_TOL`은 밖이다.
16. **성공률을 잴 때 분모가 전부인지 본다.** S-8의 "미분류로 남은 사선만 재고 있었다".
    **S-9에서 또 나왔다** — 자동 배치 분모를 생존자 43으로 적었다가 모집단 46으로 고쳤다.
17. **써 놓고 안 읽는다.** `Stroke.color`. → `selfcheck.scan_unread_fields`가 잡는다.
18. **⚠ 설명이 검사를 끈다**(S-9 신규). 저장 코드 주석에 "`Stroke.layer`는 담지 않는다"고
    적었더니 **그 문장이 읽기 1건으로 잡혀 플래그가 사라졌다**. 주석을 지우고 세게 고쳤고
    `tests/test_selfcheck_scan.py`가 블록 주석·JSDoc·URL·문자열 한계를 잠근다.
19. **거름을 선택 전에 걸면 경쟁자가 죽어 상대 순위가 거저 통과한다**(S-8b). 각도 거부권을
    `Infinity`로 걸었더니 오배정이 79% → 90%로 **늘었다**. 거름은 **선택 뒤에** 건다.
    **점검했고 하나 더 찾았다**(S-10): `classifyStroke`가 `screen` 후보를 선택 전에 거른다 —
    유한 후보가 하나뿐인 구도에서 `runnerUp`이 사라져 **애매성 판정이 무조건 통과**하고,
    미분류가 됐어야 할 획이 축으로 배정된다. 반례를 `test/prefilter.test.ts`에 박았다.
    **고치지 않았다** — 판정 규칙 변경이라 모든 원장이 낡는다(STALE). 실획 재조정과 함께 한다.

## ✋ 브라우저에서 보는 절차

```bash
cd web && npm run dev:5222
```
→ `http://localhost:5222` (**새 탭에서 연다** — 레이아웃 타이밍, AS-11)

1. `축1`/`축2`/`축3`으로 소실점 셋을 찍는다(짧게 탭 — **두 점 이상 움직여야 획으로 잡힌다**).
2. `그리기`로 상자를 그린다. 축 색으로 칠해지고 오른쪽 3D에 쌓인다.
3. `고치기`로 못 놓은 획을 붙인다(후보 점 클릭 또는 깊이 슬라이더).
4. 3D를 드래그해 돌리고 `여기서 그리기`로 이어 그린다. 돌린 시점의 미배치는 3D의 `고치기`로.
5. `지우개`·`Ctrl+Z`. 하단 `OBJ`/`glTF`로 내보내고, **작업은 자동 저장된다**(하단에 시각).

종단 검증: `cd web && npx playwright test`(전용 서버 5223을 띄운다 — 5222와 섞으면 504가 난다).

## 개발 명령

```bash
cd web && npx vitest run && npx tsc --noEmit && npx playwright test
```
```bash
python -m pytest tests/ -q && python selfcheck.py
```
빌드·아이콘: `cd web && npm run build` · `npm run icons`.
카메라 기준값 재생성: `python web/test/gen_camera_ref.py`.

`window.s2s`에 앱 손잡이가 있다: `{strokes, panel, viewport, strokeView, addStroke,
addStrokeFromView, worldAxes, setViewMode, setView3dMode, placeCtx, refresh, exportSession,
editClick, editClick3d, select, candidates, candidates3d, provisional, userPlaced, undo, redo,
eraseAt, undoDepth, redoDepth, sameView, viewCamHas, projectToView, buildDoc, applyDoc,
saveNow, exportObj, exportGltf}`.

## 미해소 지적

- **S-10이 남긴 것**: ① **가림 30%가 여전히 원장 밖이다**(D-S20의 근거. Playwright에서도
  세지 않았다 — 뒤 모서리를 알고 픽셀을 대조하는 코드가 필요하다) ② 지연은 데스크톱·개발
  서버 값이고 **iPad 실측이 없다** ③ 손 모델링 대비는 **자릿수 판단**이지 대조 실험이 아니다
  ④ 고치기 부하를 고각별로 가르지 않았다(18°만).
- **S-9가 남긴 것**: 카메라 재조정 때 사용자 지정 배치를 복원하지 못한다(앵커를 "획 id + t"로
  저장하면 가능하다 — 자료구조를 늘리는 일이라 미뤘다) · 저장 슬롯 하나 ·
  `scan_unread_fields`가 **문자열 리터럴 안의 필드명은 읽기로 센다**(한계를 테스트로 못 박음).
- **S-8 리뷰어 잔여**(D-S21): ④ B안 기각의 (4)는 "같은 문제일 수 있다"까지가 근거다
  ⑤ `bend_share`와 `one_stroke_one_axis`의 표본이 다르다(낱말 7 대 6, 4785 대 1816).
- **AS-4는 좁은 근거로 종결했다** — 구도 하나·물체 하나·관찰자 하나, 사전 기준 없음.
- **`bend_max` 0.18을 이 표본이 시험하지 못한다**(픽스처가 이봉이다). 실획 이후 재결정.
- **원장 조건 블록에 판정 설정이 없다**(`wobble_plane`·`preview_gap`).
- `preview_gap.json`의 `unanchored_of_classified`는 검사를 끈 구성의 값이다(출하 구성은
  `misassign_retry.json`의 0.139/0.153/0.313).
- **구도가 하나뿐이다**(요 35°·피치 15° 3점, 직육면체 하나). 모든 배치 측정이 그 위에 있다.

## 최근 커밋
```
e0588df  S-10 (1) 종단 검증 — 실제 앱·실제 포인터 이벤트로 시나리오 한 바퀴
e1eec04  S-9 리뷰어 대응 — 재생성은 사용자 지정 배치를 복원하지 않는다(9/9)
eed7c05  S-9: 저장은 pts3d까지 담는다 — 재생성이 화면과 갈리는 자리가 셋이었다
ff7c809  S-8c: 써 놓고 안 읽는 필드를 selfcheck가 잡는다
a1e0b4f  S-8b: 돌린 시점 사선 오배정은 투영의 애매성이었다 — 완화만 된다
b1a7d08  S-8 리뷰어 대응 — 돌린 시점 사선의 진짜 위험은 §4.1이었다
08c54dd  S-8: 면은 만들지 않는다 — 가림은 이미 있고, 면은 그리지 않은 기하다
1826049  S-7 (4): 돌린 시점의 고치기 — 미배치 처리가 완결된다
4c5193e  S-7 (0): 측정-앱 배선을 한 곳으로 모으고 기계로 잠갔다
```
