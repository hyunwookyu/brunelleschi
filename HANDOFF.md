# HANDOFF.md — 세션 인계

다음 세션은 이 문서를 읽고 이어서 진행한다. 사람의 개입은 그 한 줄뿐이다.

## 현재 단계 — **2026-08-18 6차 지시: 항목 1·2·7·11·12 완료. 3·4·5·6·8·9·10이 남았다**

6차 지시는 **실획 첫 표본의 보고**에 대한 12항목이었다. 사용자가 정한 순서는
**1·2·7·11 → 병합 → 4 → 병합 → 나머지**다. 이 세션은 **1·2·7·11**(커밋 `7569115`)과
**12**(등재만 — 지시가 그렇게 명시했다)를 끝냈다. **다음 세션은 항목 4부터다.**

## ⚡⚡ 먼저 알아야 할 것 — **실획 파일이 저장소에 없다**

지시문은 `brunelleschi-2026-08-17T11-41-36_brnl.json`(5획 · 1180×668)을 `sessions/`에 넣고
`real_ink`를 먼저 돌리라 했다. **그 파일이 이 기계 어디에도 없다** — 사용자 폴더 아래에
`brunelleschi*`·`*.brnl`이 하나도 없고 `sessions/`에는 `README.md`뿐이다.
`npx vitest run test/real_ink.test.ts`는 **`awaiting_samples`로 건너뛴다**.

그래서 지시문이 **보고한 수**(VP0 902,157 / VP1 −231,157 / W 1180 / Δ0.0~1.7° / askStats 6 /
pathStats direct 16·lift 18·twoPoint 3 등)를 입력으로 쓰되 **측정이라고 적지 않았다** —
`confirm_rules.json`의 `fov_gate.real_ink_reported.source`와 `what_this_does_not_say`가 그
사실을 든다. **파일이 들어오면 그것이 다른 무엇보다 먼저다**(AS-L26 · `concurrent_deg` ·
`AIM_JITTER_DEG` · 항목 2의 세 사유가 전부 거기 달려 있다).

## ⚡ 착수 전에 `PITFALLS.md` 상단 "최근 다섯"부터 읽는다

읽은 증거는 걸리는 번호를 `progress.md`에 적는 것. ⚠ **6차 실증: #42가 또 났다** —
착수 표가 "원장이 인용하는 번호"를 열 개로 적었는데 원장은 다섯이었고, 6-R에서 고친 표가
이번엔 **#24를 놓쳤다**(원장 안에 있다). **완료 대조 grep은 원장 전문을 대상으로 하고,
고친 뒤에도 다시 센다.**

## 핵심 설계 변경 (6차, D-L66 ~ D-L69)

```
D-L66  `pose === null`은 결함이 아니라 **확정 뷰의 정의**다 — 지시 1-a·1-c를 따르지 않았다.
       채우면 오히려 확정 뷰를 못 찾는다(뷰 전환·일괄 풀이·저장 복원이 그 술어로 찾는다).
       술어를 `doc.ts`의 `confirmViewOf`·`isConfirmView` 하나로 모았다(#17)
D-L67  `SStroke.snapEndDistPx` — 조리개 밖 40px 프로브. `snapEnd null` 하나로는
       "반경이 좁은가 / 후보가 없는가"가 안 갈린다. `on_face`는 화면 거리가 정의상 0이라 뺀다
D-L68  `fovGate` — f² ≤ 0이면 **거부**, `f/W < 0.87`이면 경고하되 확정. 화면 밖 소실점은 정상이다.
       ⚠ 대역은 **시거리 f/W**가 가른다(18.4) · f에 **−h²**가 들어간다(6.2는 주점도 지평선 위를 전제)
D-L69  `resolvePool` — **소실점은 세 번째 선이 정한다.** 둘까지는 대기, 셋이 모이면 그 점,
       갈리면 2점이 한 번에. 모임은 **각**(`concurrent_deg` 3°) + **교점이 두 선의 연장 위**
```

## ⚠⚠ 지금 서 있는 사실 — 다음 세션이 먼저 알아야 할 것

1. **항목 11의 대가가 측정됐고 숨기지 않았다**: 같은 축 둘이 먼저 오는 대조 팔에서 **새 규칙이
   24/120 틀린다**(옛 규칙 0/120). 주 팔에서는 옛 120/120 · 새 7/120이다. 바꾼 이유는 옛 규칙의
   실패가 **불가역**(2점 → 1점 고착)이고 새 규칙의 실패는 **다음 획이 고칠 수 있는** 종류라서다.
2. **`rule_camera`가 갈라졌다**: `camera_ok_rate` 0.637 → **0.9115** · `asks_per_run` 3.287 →
   **2.667**이 좋아지고, `deg_median` 17.58 → **31.61** · `order_median` 2 → **1**이 나빠졌다.
   기전은 대기가 길어져 **화면 가로선이 먼저 선언되어 P1로 굳는** 것이고, 지시 11-1 다섯째 줄이
   그 동작을 명시한다. ⚠ **그 지표가 포화했을 수 있다**(`deg_p90`이 여섯 팔 전부 40.66) — DEFERRED.
3. ⚠⚠ **중단 조건 게이트(`camera_gate.json`)는 이 변경을 안 지난다** — 그 하네스는 소실점을
   직접 주고 `recoverCamera`만 부른다. 그 원장이 안 움직인 것은 **안전의 증거가 아니라 사정거리
   밖**이라는 뜻이다(6-R 재검 #41). 이 변경을 실제로 지나는 하네스는 `rule_camera` 하나다.
4. **`on_face`가 언제나 이긴다** — 지면 스냅의 화면 거리가 **정의상 0**이라 다른 후보가 조리개
   밖이면 반드시 붙는다. 그래서 ① `snapDistPx`가 겨냥 거리 대신 0을 적고 ② 4차 지시 1-b의
   2D 폴백이 카메라 확정 뒤 **한 번도 안 돈다**. **항목 4-5가 그것을 지운다.**
5. 상수 해시 **54346ad1**(6차에 두 번 움직였다: `FOV_GATE` 신설 → 키 이름 정정).
   `n_stale = 3`(`draft_recover`·`guide_budget`·`horizon` — 하네스가 은퇴해 다시 안 돈다).
6. 수는 적지 않는다 — **`selfcheck.json`의 `coverage`를 그 자리에서 읽는다.**

## 다음에 할 일 — **항목 4부터. 지시문 전문은 6차 사용자 메시지에 있다**

| 항목 | 내용 | 상태 |
|---|---|---|
| **4** | **깊이선은 방향만 확정하고 좌표는 연결이 정한다**(핵심·구조 변경) — 세 상태(방향 미정 / **방향 확정** / 좌표 확정) · 무한직선 표현 · 연결과 확정의 연쇄 · 첫 앵커 · **on_face 임의 스냅 제거**(4-5) · 수직선도 같은 취급인가(4-6) · 회귀 팔(4-7) | **다음** |
| **3** | 3점은 **시점의 성질**이지 그리는 선의 종류가 아니다 — 차수를 카메라 상태로만 정한다 · "수직축입니까" 물음 제거 · **승격 UI 제거**(요약·되돌리기·잃은 것 표시 · D-L26·§6.2 관련 코드) · asked를 다시 잰다 | 남음 |
| 5 | 남는 물음의 문구("바닥에 놓인 선입니까, 위로 뻗는 선입니까") | 3에 딸림 |
| 6 | **real_ink 측정** — 파일이 오면 K의 지표 전부 + AS-L24·L25 확인 + Track 2의 합성 기반 결론 유효 범위 재기술 | **파일 대기** |
| 8 | 손가락 배정(2 팬+줌 / 1 궤도)이 이미 그런지 확인 · 큐브 화살표를 회전의 주 수단으로 · 1점 회전 신중 | 남음 |
| 9 | 팬·줌이 화면 조작인가 공간 조작인가 — 후보 셋 중 단순한 것(A-3) · `setViewOffset` | 남음 |
| 10 | 큐브의 **기준 좌표계를 그린 공간의 축으로** — 자세가 도는 것과 기준계가 도는 것을 구분한다 | 남음 |
| 12 | 보조 소실점 — **등재 완료**(`DEFERRED.md` 맨 끝, 지시문 설계 전부) | ✅ |

**이월**: 6-R 재검의 남은 지적(잡음·시드 스윕 #12·#14 · `beyondSegment` 위약 팔 #39 ·
화각 비대칭 동작점 · 그리드 방향 각차 · 위치를 구속하는 등록 기준 부재 · D-L42 표제 쌍)은
전부 `DEFERRED.md` "6-R 재검에서 새로 등재한 것"에 있다.

## 검증 현황 (`7569115` 기준)

vitest **450 통과 · 1 건너뜀**(71파일 — `confirm_rules.test.ts` 신규) · tsc · 빌드 통과 ·
Playwright **42 통과 · 2 건너뜀** · pytest **73** · selfcheck 인용 **0** · 게이트 **0** ·
`n_stale` **3** · `scan_zero_denominator` 11건은 **6차 이전부터 있던 것** · 상수 해시 **54346ad1**.

⚠ **아직 푸시하지 않았다** — 커밋만 했다.

## ✋ 브라우저

`cd web && npm run dev:5222` → `/l.html`(S2S_HTTP=1). `window.S2S` 6차 추가분:
`endProbe(from, p)` `aimDist()`. 기존: `doc()` `cam` `order()` `standing()` `snap2d(p)`
`pickVp(p)` `switchView(id)` `askStats()` `cubeSpin(rad,ms)` `viewCube()` 등.

## 개발 명령

```bash
cd web && npx vitest run && npx tsc --noEmit && npm run build && npx playwright test
```
```bash
python -m pytest tests/ -q && python selfcheck.py
```
⚠ `playwright` 앞에 `npm run build`. ⚠ `progress.md`는 루트 하나.
⚠ **selfcheck는 저장소 루트에서 돌린다**(`web/`에서 돌리면 아무것도 안 나온다).

## 반드시 읽는 것

1. `CLAUDE.md` 2. **`PITFALLS.md`** 3. `docs/line_plan.md` 4. 이 문서
5. `progress.md` 맨 끝(6차 절 전부 + **6-R 대응 표**) · `assumptions.md`(**AS-L28**까지) ·
`DECISIONS.md`(~**D-L69**) · `DEFERRED.md`(2026-08-18 6차 신규 + 6-R 재검 + **지시 12 등재**) ·
`QUESTIONS.md`
