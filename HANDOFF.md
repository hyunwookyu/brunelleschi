# HANDOFF.md — 세션 인계

다음 세션은 이 문서를 읽고 이어서 진행한다. 사람의 개입은 그 한 줄뿐이다.

## 현재 단계 — **L-A 진행 중** (접근 전환, 코드명 L)

**2026-08-15 세 번째 전환.** 현행 계획서는 [`docs/line_plan.md`](docs/line_plan.md) 하나다.
S(`sketch3d_plan`)와 G(`segment_plan`)는 폐기 사유를 달아 `docs/archive/`에 있다.
**아카이브를 판단 근거로 인용하지 않는다**(D-L1).

만드는 것은 **입력이 손 획인 SketchUp 선 도구**다. 프리핸드 보존은 미뤘다(획이 직선
세그먼트가 되고 손떨림은 버린다. `pts2d`는 계속 보존한다).

### 끝난 것

| 항목 | 무엇 | 파일 |
|---|---|---|
| L-A.1 | 문서 전환 — 계획서·원장 아카이브, 유효 항목 이관 | `docs/archive/*_SG.md`, D-L1 |
| L-A.2 | 소실점 후보 검출 | `web/src/s3d/vpDetect.ts`, `web/test/vp_detect.test.ts`(11) |
| L-A.3 | 초기 3D 올리기 — 2D 교점 제약 **동차 선형계 일괄 해** | `web/src/s3d/lift.ts`, `web/test/lift.test.ts`(9) |

**핵심 기전**(L-A.3): 획 하나의 3D 직선은 해석 평면(눈+화면 직선) 안에 있고 축이 방향을
정하므로 **미지수가 획마다 하나**(`s_i`)다. 화면 교점 하나가 `s_i·(r·e_j) − s_j·(r·e_i) = 0`이라는
**선형** 제약이 되고, 전체가 `A s = 0`이 되어 **남는 자유도는 전역 배율 하나**다.
앵커도 씨앗도 없다. 정확한 투영에서 참 상자가 배율까지 복원된다(형태 오차 < 1e-6).

### 다음에 할 것 — **L-A.4 게이트 + 리뷰어 지적 대응**

**리뷰어가 L-A.1/L-A.2에 20건(높음 14)을 냈고 대응이 진행 중이다.** 아래 "미해소 지적"이
그 목록이다. 대응 없이 L-B로 가지 않는다.

L-A 게이트(계획서 §9, **유일한 중단 조건**): 상자 하나를 그리면 확정되고, 전환 시 화면이
안 튀고, 연결된 획이 전부 놓인다. **사전 등록 수치는 `progress.md`의 "L-A 게이트 사전 등록" 절.**

## ⚠ 반드시 먼저 읽는다

1. `CLAUDE.md` — 작업 지침(A-1~A-5)
2. **`PITFALLS.md`** — 반복해서 걸린 것 26개. **L-A.2가 이 중 셋을 그대로 어겼다.**
   HANDOFF 안에 두면 전환 때 지워지므로 따로 뺐다
3. `docs/line_plan.md` — 현행 계획서
4. 이 문서
5. `progress.md`(L-A 기록·게이트 사전 등록) · `DECISIONS.md`(D-L1~D-L5, D-C1~C6) ·
   `assumptions.md`(AS-L1~L7, AS-C1~C10)

## 미해소 지적 (리뷰어, L-A.1/L-A.2 — 대응 중)

**높음**
- L-A.2·D-L5의 수치가 **원장 밖에 있다** — `stage0/out/vp_detect.json`을 내야 한다(PITFALLS #25)
- **D-L2가 자기를 반박한다** — `vpMisfit`이 이미 각도량이면(sin φ/2) "먼 소실점에서 발산"이라는
  기각 근거가 성립하지 않고, 실패값 0.061은 7.0°로 **새 임계 2.5°보다 3배 느슨하다**.
  즉 단위 변경이 검출 실패를 고친 기전이 아니다(PITFALLS #24)
- **오배정 분모가 행마다 다르고(399~456) 정의가 없다.** 모집단 480 중 빠진 것이 무엇인가(#11)
- **"2.5°가 낫다"가 표에서 나오지 않는다** — 오배정률 최소는 2.0°, f 오차 최소는 3.0°/4.0°,
  검출 최대는 4.0°. 차이가 시드 변동폭보다 작다(#14)
- **끝점 오차 0~0.01만 쟀다.** 실획 자릿수 0.028~0.051이 그 구간 전부 바깥(#12)
- **AS-L1이 반증됐는데 상태가 ⏳이고 관측 칸이 비어 있다**
- **계획서 §5.1이 갱신되지 않았다** — 기각된 문장("산포 임계는 화면 대각 비율")이 원문에 남아
  있고, "정밀도가 필요한 값이 아니다"는 표가 반증한다(2.0→3.0에서 f 오차 29% 이동)(#23)
- **D-L5의 대조가 결론을 지지하지 않는다** — 상자 12모서리는 화면 길이가 비슷해 길이 가중이
  균등 가중과 같아진다(#6). p90은 12% 움직였다
- **"검출 직교 조건은 카메라가 검증한다"가 틀렸다** — f²<0은 **둔각만** 거른다. 예각인 아무 세 점도
  통과한다(이론서 6.5). 꼭짓점 셋이 예각이었으면 카메라 단계는 통과시켰다
- **AS-L3·AS-L6이 자기참조 유형 3인데 표시가 없다.** f 오차는 전역 배율이라 같은 카메라
  재투영에서 상쇄된다 — §5.3 지표는 **틀린 카메라를 못 잡는다**(PITFALLS #5)
- **AS-L2에 AS-20의 반증 수치가 없다.** §5.2의 "2D 교점은 손실 없는 정보다"는 가림 교차를
  포함한다(상자 하나에서 27 중 3). **초기 확정은 여전히 사후 일괄 해**이므로 "실시간이라
  괜찮다"는 논거가 여기엔 안 걸린다
- **`archive_SG/` 이동이 살아 있는 전제의 근거를 함께 가져간다** — `real_ink.json` ·
  `bend_share.json` · `one_stroke_one_axis.json`은 AS-C2·C3·C8의 유일한 자릿수다.
  **stage0/out/에 남기고 스냅샷만 재생성한다**
- **"반복해서 걸린 것" 목록이 이관되지 않았다** → `PITFALLS.md`로 옮겼다(대응 완료)
- **HANDOFF가 폐기된 G 접근을 가리켰다** → 이 문서로 대체(대응 완료)

**중간·낮음**
- 밴드 1.5~6°가 표에 없는 행을 근거로 하고, 표의 3.0°(3.69%)가 밴드 밖
- `min_reach = 1.0`이 스윕도 없고 반례가 한쪽뿐(거짓 양성만). **근 소실점 구도가 표본에 없다** —
  소실점 근처의 긴 획이 배제되는데 그 획이 그 소실점을 가장 잘 정한다
- L-A 게이트에 사전 등록 수치가 없었다 → `progress.md`에 박았다(대응 완료).
  L-A.5(폐기 코드 제거)가 계획서 §9에 없다
- AS-C8이 `bend_share.json`의 caveat이 금지한 비교를 한다(26.5%와 0.6025는 다른 지표)
- 채택 동작점의 f 오차는 **0.105**이고 9%는 다른 행의 값. AS-C4에 따르면 그 구간에서
  모서리 오차가 5.2배로 커진다는 귀결이 안 적혔다
- STALE은 21건이 아니라 **20건**(`selfcheck.json` `n_stale`)

## ⛔ 아직 폐기하지 않은 코드 (L-A.5)

계획서 §8.2는 배치 계층·`segment.ts`·분리 뷰포트 등을 폐기하라고 했다.
**게이트 통과 뒤에 지운다**(A-4) — 먼저 지웠다면 게이트가 실패한 G-A에서 앱이 안 도는 상태로
멈췄을 것이다. 지금 앱(`main.ts`)은 **여전히 옛 배치 계층**을 쓰고 브라우저에서 그대로 열린다.
`vpDetect.ts`·`lift.ts`는 **나란히** 있고 앱에 배선되지 않았다.

## ✋ 브라우저에서 보는 절차

```bash
cd web && npm run dev:5222
```
→ `http://localhost:5222` (**새 탭에서 연다** — 레이아웃 타이밍, AS-C7)

## 개발 명령

```bash
cd web && npx vitest run && npx tsc --noEmit && npx playwright test
```
```bash
python -m pytest tests/ -q && python selfcheck.py
```

검증 현황(2026-08-15 L-A.3 시점): vitest **198/198**(32파일) · tsc 0 · pytest 37/37 ·
selfcheck 플래그 861 · **STALE 20**(전부 `VP_TOL`/`LIFT_TOL` 신규 등록 때문) ·
상수 해시 `6c7f9be8`.

## 최근 커밋
```
e2cda3e L-A.2 소실점 후보 검출 — 계획서 문장 그대로는 작동하지 않았다
99b0d8d 접근 전환 — line_plan.md를 docs/로 편입(코드명 L), 원장을 새로 시작
bf021f5 G-A 세그먼트 기하 — G-A.1 게이트에서 멈춘다
```
