# HANDOFF.md — 세션 인계

다음 세션은 이 문서를 읽고 이어서 진행한다. 사람의 개입은 그 한 줄뿐이다.

## 현재 단계 — **L-D.0 완료(게이트 실패) · 다음은 L-D.3 나머지 → L-D.4**

**계획서는 [`docs/line_plan.md`](docs/line_plan.md) 개정 2**다. 개정 1은
`docs/archive/line_plan_r1.md`에 있고 **폐기가 아니라 대체**다 — 개정 2가 §2~§4·§8을
"개정 1 유지"로 참조하므로 **그 절들의 원문은 아카이브 쪽**이다.
다만 **판단 근거로 아카이브를 인용하지 않는다**(D-L1).

## ⚡ 착수 전에 `PITFALLS.md` **상단 "최근 다섯"**부터 읽는다

목록이 **39개**다. 최근 다섯: **#39 #38 #37 #36 #35**.
**자동**으로 표시된 것은 `python selfcheck.py`가 **일부만** 잡는다 —
그 표의 "안 잡는 것" 열은 여전히 사람이 본다.

## 프로그램 이름은 **Brunelleschi**로 정해졌다 (사람 지시)

유래: 필리포 브루넬레스키, 1415년경 산 조반니 세례당 실험으로 선 투시를 실증했다.
**L-D.4에서 반영한다** — README 제목·유래 한 줄 / 저장소명 `brunelleschi` /
`manifest.json`의 `name`·`short_name` / 페이지 `title` / `package.json` / 배포 URL.
`short_name`은 아이콘 아래 표시되므로 축약한다(**Brunel** 또는 **Bru**).
자체 저장 확장자가 필요하면 **`.brnl`**, 내용은 JSON 유지(텍스트 편집기·`git diff`·디버깅).

## 이번 세션이 한 것

| 항목 | 결과 |
|---|---|
| **#35 검사가 0건이던 것**(사람 지시) | 원장 37개를 훑고도 `gate` 이름 블록이 0이었다. **기록하는 쪽을 고쳤다** — `web/test/gate.ts`의 `gate()`가 `registered`·`reachability` 둘을 강제하고 일곱 하네스가 그 이름으로 적는다. **0 → 7** |
| **덮는 대상 수 출력**(사람 지시) | `selfcheck._cover()`가 검사마다 `{scan, unit, targets, hits}`를 `coverage`에 남기고 찍는다. **0이면 그 자체가 플래그**. #33 훑기 3 · #36 하네스 38 |
| **`scan_citation_hashes` 신설**(#33 값 대조) | 인용 해시를 원장의 **현재 해시**와 대조한다. 첫 실행에서 **23곳**이 걸렸다(옛 `@1671e540` 등) |
| **리뷰어 L-D.2 검토 19건** | 높음 1(D-L28 항등) 중간 12 낮음 6 — **전부 대응했다**(아래 표) |
| **L-D.0 롤 0 제약**(사람 지시) | **게이트 실패.** 축 오차 7.4048 → 6.1460°(임계 ≈1°의 6배)이고 **배치가 붕괴한다**. D-L32 |
| **L-D.3 사전 등록** | 판정 다섯을 측정 전에 박았다(리뷰어 [11]) |

## 검증 현황

vitest **321/321**(57파일) · Playwright **21/21**(네 스펙: `coords`·`flow`·`stage`·`static_deploy`) ·
pytest **63/63** · tsc·빌드 통과 ·
selfcheck **1601건 · STALE(상수) 0 · STALE(정의) 0** ·
**상수 해시 `34db7279`**(⚠ `HORIZON_TOL` 등록으로 `2fd74e1c`에서 옮겼다. **값이 바뀐 원장은 없다** —
새 상수를 읽는 기존 하네스가 없기 때문이고 D-L10이 같은 상황을 이미 받아들였다. 인용 92곳을 고쳤다) ·
**정의 해시 `a45078a5`** · 자동 검사 커버리지 훑기 3 / 인용 92 / 게이트 7 / 하네스 38.
⚠ **검증 문구는 두 칸으로 적는다** — "STALE 0"만 적었다가 STALE(정의) 1건을 놓친 적이 있다(#1).

## L-D.0이 남긴 사실 넷 — **게이트 실패**

**⓵ 이론은 맞다.** 이론서 3.1(수평 방향의 소실점은 지평선 위) + 롤 0이면 그 직선이 화면 수평이고
§5.3의 자세 자유도가 3 → 2다. 수심 조건(6.3)으로 세 번째 소실점이 **직선이 아니라 점 하나**로
정해진다: `V₃ = (Pₓ, h − (Pₓ−x₁)(Pₓ−x₂)/(P_y−h))`. **참 카메라에서 오차 0으로 성립한다**
(`horizon.json@34db7279`의 `orthocenter_identity`). ⚠ **항등이므로 정보량은 0이다**(#5).

**⓶ 검출 입력에서는 안 선다.** L-A.6과 같은 조건에서 축 방향 오차 중앙(도):

| 축당 획 | 기준 | 롤0 사영 | 오라클 축 라벨 | 제약 적합 |
|---|---|---|---|---|
| 4 | 7.4048 | **6.1460** | **4.4599** | 5.7339 |
| 18 | 10.0437 | 8.0473 | 4.9690 | 9.2961 |
| 48 | 10.0922 | 9.4668 | 9.2513 | 8.7764 |
| 100 | 4.5290 | 4.6094 | 8.4495 | 8.5438 |

**임계 ≈1°에 어느 팔도 못 간다.** 가장 좋은 칸이 4.46°로 **4.5배**다. 꼬리는 거의 안 움직인다
(p90 35 → 36 → 12.5 → 37.7). ⚠ k_3는 n=93, k_4는 n=18이다 — **뒤의 두 행은 표본이 작다**(#14).

**⓷ 그리고 배치가 붕괴한다** — 놓인 획 2049 → 792 / 6035 → 522 / 2045 → 9 / 692 → **0**.
**사영의 성질인지 제약의 성질인지 갈랐다**(#32·#39): 제약을 **적합 안에** 넣은 팔
(`fitHorizonPair` — 미지수 `(x₁, x₂, h)`를 길이² 가중 최소제곱 한 번으로)도 **안 살아난다**
(848 / 730 / 3 / 0). 소실점 이동량 중앙이 **78~98px**이고, 즉 검출이 낸 두 "수평" 소실점의
y가 실제로 그만큼 어긋나 있다. **붕괴는 제약의 성질이다.**

**⓸ 양성 채널: 손익분기는 롤 ≈5°다.** 참 축도 같은 각으로 돌려 기준계를 맞추면
롤 0/2/5/10°에서 4.4693 / 5.0614 / 5.9275 / **10.0103**°이고 기준선은 **6.0933°로 고정**된다
(기준계가 맞았다는 표시). 10°에서는 제약이 해를 끼친다.
⚠⚠ **초판은 참 축을 안 돌려 이 대조군이 통째로 무효였다** — `roll_zero` 팔이 롤을 되감아
"기울여도 좋아진다"로 읽혔고, 재던 것이 품질이 아니라 **기준계 불일치**였다(#35의 ⚠⚠ · #37).

**판정(D-L32)**: 자동 확정 경로를 **안 되살린다.** 사용자 확정을 유지한다(§5.4).
`horizon.ts`는 D-L6과 같은 처리 — 코드는 남기고 **앱에 배선하지 않는다.**
⚠ **"초안 품질이 올랐다"고 적지 않았다** — 축 방향 오차는 초안의 지표가 아니다(#2).
초안의 지표는 `draft_recover`의 확정률·축 오차이고 **그 하네스로 안 쟀다**. `DEFERRED.md`에 트리거.

## 다음에 할 일 — **L-D.3 나머지**

**판정은 `progress.md`의 "L-D.3 — 사전 등록: 판정" 절에 박혀 있다**(다섯 항목, 각각 도달 가능성 포함).

1. **상자 둘 이상 픽스처.** 지금 `setup(page)`는 상자 하나다(`e2e/stage.spec.ts`).
2. **L-B 게이트 3번** — 승격 연쇄 **여러 회** + 브라우저 확인. `stage_browser.json`에 이어 붙인다.
3. **차수 표식 다중** — `orderMarks`가 2·3점을 함께 들고 있을 때의 되돌리기.
4. **지연 측정 · 스크린샷 회귀.**
5. **AS-L9 시나리오** — 자동화가 어려우면 **DEFERRED**(사람 표본이 필요하다).
6. ~~D-L21 재개~~ — **여기서 안 한다.** `DEFERRED.md`가 트리거를 달았다: `promote`·`axis_live`의
   `Math.max(1, n)` 53곳이 층화 비율인지 표시용 보호인지 **먼저 갈라야** 한다(그 둘이 D-L21의 근거 원장).

그 다음 **L-D.4 문서**(README·PROTOTYPE·DEFERRED·DECISIONS·QUESTIONS·HANDOFF 완료).
⚠ **QUESTIONS.md가 아직 없다** — L-D.4가 만든다. 올릴 넷은 아래 절에 있다.
⚠ **옛 UI(`index.html`·`main.ts`)는 L-D.3 통과 후에 지운다**(A-4).
⚠ **지우기 전에 절차를 README에 적는다**(D-L29) — v1 저장은 새 UI에서 안 열리므로
옛 UI로 `획 내보내기`를 해 두어야 한다.

## L-D.4의 `QUESTIONS.md`에 올릴 넷 (사람 지시)

**a. 면 위 임의 점 스냅** — 벽에 창을 그릴 때 모서리에서 시작하지 않는다. 면이 스냅 대상이면
임의 점의 3D가 결정된다. D-S20(면 안 만듦)은 **표시용 판단**이었고 스냅용은 별개다.
같은 구조가 벽면 패턴·바닥 타일·천장 조명·가구 윤곽에 적용된다.
이론서 9.5절 등간격 판별로 규칙적 배치 자동 채움도 가능해진다.

**b. 승격 대상이 아닌 획** — 해칭·지시선·치수선·메모는 애초에 3D가 되면 안 된다.
지금 2D 레이어가 "승격 대기"뿐이라 이것들이 **미배치로 집계된다**. 네 상태가 필요할 수 있다:
`pending` / `placed` / `surface`(면에 붙은 주석) / `view`(뷰에 붙은 주석). 구분은 **펜 채널**이
유력하다(색으로 갈리고 사후 변경 가능). **자동 판정은 하지 않는다** — 틀린 분류가 조용히 남으면
A-3 위반. **미배치율의 분모가 `pending`만이 되므로 지표 정의가 바뀐다.**
⚠ **`Stroke.layer`가 그 자리다**(D-L31) — 지금 쓰기 0 · 읽기 0으로 남겨 두었고
selfcheck 플래그가 "아직 안 정해졌다"의 표시다.

**c. 근경 자동완성** — 2점 이상에서 근경은 왜곡 때문에 생략되는 것이 관행이다. 3D 입장에서는
형태가 미완결이라 돌리면 잘려 보인다. 다만 생략이 **구도 선택**이기도 하므로 조용히 채우면 안 된다.
**채우되 다르게 표시하는 쪽**(파선·흐리게)이 A-3과 맞다. 기하적으로 **유일하게 결정되는 것만**
채운다 — 열린 루프의 닫힘, 축 대칭, 이미 정해진 면의 경계. 유일하지 않으면 안 채운다.

**d. 카메라 롤** — L-D.0에서 롤 0을 전제로 쟀고 **손익분기가 5°**로 나왔다(위 ⓸).
종이를 기울여 그리는 사용자가 있으면 그 위에서 깨진다. 실사용에서 확인할 항목이며,
문제가 되면 초기 확정 시 한 자유도로 두고 이후 잠그는 방식으로 되돌린다.
⚠ **지금은 제약 자체를 안 걸었으므로**(D-L32) 이 물음은 **다시 걸 때** 생긴다.

## 리뷰어 L-D.2 검토 19건 — **전부 대응했다**

| # | 지적 | 대응 |
|---|---|---|
| [4] **높음** | `axis_misplaced 19 = by_reason.ok 19`가 항등인가 | **항등으로 확인**(합 120 = `n_diagonals`). D-L28에서 **"20배"를 뺐다** — 짝지은 비교가 아니라 분류기가 가른 disjoint 두 집단이다. 남는 말은 "축 경로는 자기가 가장 축답다고 고른 사선에서도 크게 틀린다" |
| [1][15][16] | 브라우저 확인이 원장 밖 | `static_deploy.json`·`doc_store.json` 신설. `stage_browser.persist` → **`l_d3_save_roundtrip`**. `identical`이 덮는/안 덮는 필드 명시. `reload_ms` 1156과 `e2e.json` 146이 **다른 구간**임을 적었다 |
| [2] | 오프라인 양성 채널 없음 | 팔 둘. **A(사전 캐시에서 `.js` 제거)는 안 깨졌다** — 런타임 캐시가 메운다. **즉 ③의 통과는 사전 캐시 목록의 효력이 아니다.** B(캐시의 `.js` 삭제)는 깨진다 |
| [3] | #36 미루기에 트리거 없음 | **D-L21 재개 전에 가른다**로 트리거를 달았다 |
| [5] | AS-5 무효 원장을 결론에 씀 | D-L28·`assumptions.md`에 그 사실을 옮겨 적었다(상수 해시가 현재값이라 STALE로 안 잡힌다) |
| [6] | 저장 v2가 못 담는 것의 목록 없음 | 일곱을 원장·README에 적었다. 가장 나쁜 것은 **못 살린 스냅 표식**(조용히 틀린 시작점이 정상으로 보인다) |
| [8][9] | `e2e.json` 재실행 뒤 인용 안 고침 · 옛 해시 17곳 | **`scan_citation_hashes` 신설**로 23곳을 찾아 고쳤다. `e2e`는 값도 바뀌어(2.15 → 1.8ms · 152 → 146ms · 메쉬 1328/2604 → 310/576) 문서를 고쳤다 |
| [10][19] | `corner_err_ratio` 명칭 정정이 DEFERRED에 안 옮겨짐 · 0.8417과 0.8416 | 옮겼다. **101/120 · 85/101**로 분자/분모로 적는다 |
| [11] | L-D.2·L-D.3에 사전 등록 판정 없음 | **L-D.3 판정 다섯을 박았다**(각각 도달 가능성 포함) |
| [12] | `progress.md`에 L-D.3 절 없음 | 소급해서 적었다(**소급이라는 사실을 함께**) |
| [13] | DECISIONS에 L-D.2 결정이 하나도 없음 | **D-L29**(v1 거절) · **D-L30**(내보내기 3D만) · **D-L31**(`layer` 유지) |
| [14] | Playwright 네 스펙 중 둘만 적음 | 전체 돌렸다 — **21/21** |
| [17] | 계획서 §11 L-D 미갱신 | 완료 표시. **L-D에 게이트가 없다**는 사실도 적었다 |
| [18] | 저장 슬롯 트리거 발화했는데 판단 없음 | 판단했다 — **하나로 둔다. 파일이 슬롯이다** |
| [7] | HANDOFF 낡은 블록 · PITFALLS 개수 · D-L 범위 · README v1 서술 | 이 문서를 다시 썼다. README "저장·내보내기가 하는 일"을 v2로 |

## ⛔ 아직 폐기하지 않은 코드

**옛 UI(`index.html` + `src/main.ts` + `src/ui/cameraPanel.ts`)는 L-D.3 통과 전까지
지우지 않는다**(A-4·사용자 지시). 새 UI는 **`l.html` + `src/mainL.ts`**다.
`vite.config.ts`가 둘 다 빌드한다. `segment.ts`(G 실험) · `cameraFit.ts`(D-L6 기각) ·
**`horizon.ts`(D-L32 기각)** 도 그대로 있다 — 셋 다 **측정 근거의 재현용**이다.

## ✋ 브라우저에서 보는 절차

```bash
cd web && npm run dev:5222
```
→ **`http://localhost:5222/l.html`** (새 UI) · `http://localhost:5222/` (옛 UI)

⚠ **캔버스 크기 굳음이 세 번 재발했다**(AS-C7). 확인은 **창을 앞에 두고** 한다.
`SIZE_HEAL.count`가 자가 치유 발동 횟수다.

합성 상자를 넣으려면 **측정 하네스와 같은 픽스처**를 쓴다 — `await import('/test/scene3d.ts')`.
손으로 "소실점 쪽으로 t만큼" 만들면 **화면에서 평행한 아핀 상자**가 나와 수렴이 0이다.
`e2e/stage.spec.ts`의 `setup()`이 그 절차를 담고 있다. 앱 손잡이는 `window.S2S`
(`doc()` · `cam` · `stage` · `refresh` · `SIZE_HEAL` · `snap(p)` · `snapTargets()` ·
`hoverSnap()` · `live()` · `axisLock()` · `setAxisLock()` · `views()` · `exportObj()` · `exportGltf()`)다.

## 개발 명령

```bash
cd web && npx vitest run && npx tsc --noEmit && npx playwright test
```
```bash
python -m pytest tests/ -q && python selfcheck.py
```
⚠ `vitest`는 **`web/`에서** 돌린다. 저장소 루트에서 돌리면 다른 vitest가 잡히고 원장 경로가 어긋난다.
`camera_gate`가 103초 · `horizon`이 113초 걸린다(전체 116초 중 대부분, 병렬로 돈다).

## 미해소

- **실획 표본이 여전히 0이다**(AS-C1). 모든 판정이 Quick,Draw 유도 합성 잉크 위에 있고
  AS-C10이 그 모델을 **양방향으로 틀렸다**고 적었다. `sessions/`에 들어오면 다른 작업보다 먼저 측정한다
- **AS-L9**: 사람이 축 오차 0.5°에 도달하는지 미측정. 대리값(핸들 예산)도 **핸들 하나** 가정 위에 있다
- **롤 0의 초안 효과를 안 쟀다**(L-D.0) — `draft_recover`에 `roll_zero` 팔을 넣을 때 잰다(DEFERRED에 트리거)
- **배치 붕괴의 자리를 특정 안 했다**(L-D.0) — 소실점을 78~98px 옮기면 `liftAll`의 무엇이 거절하는가.
  미배치 사유별 카운터가 이 하네스에 없다(`lift_grade`가 같은 분해를 이미 한다)
- **`Math.max(1, n)`이 12개 하네스 70곳에 남아 있다** — 검사는 켜져 있고 파일당 한 건으로 보고한다.
  **D-L21 재개 전에 `promote`·`axis_live`의 53곳을 먼저 가른다**
- **배포 URL이 아직 없다** — Settings → Pages를 켜야 정해진다. 정해지면 README 1.5절에 적는다.
  `pages.yml`은 **아직 한 번도 실행되지 않았다**(README가 그렇게 적고 있고 그것이 맞다, #32의 거울상)
- **앵커/확정의 우열이 비용비 λ에 달렸다** — λ ≤ 2면 앵커, λ ≥ 3이면 큰 절단에서 확정. **표본이 0이다**
- **인라인 지표식 전수 확인 안 함** — 정의 해시는 `metrics.ts`를 **부르는** 하네스만 덮는다
- **`end_jitter = 0` 못박힘이 다른 원장에도 있는지 전수 확인 안 함** — selfcheck의 사각지대(꼬리 제외 규약)
- **분포형 오차의 정확한 0을 selfcheck가 못 본다** — 꼬리 제외 규약과 충돌한다(DEFERRED)
- **승격이 배치를 −168로 줄인다**(L-C.1) · **1점 구도에서 승격이 무너진다**(회수한 것의 90/93이 조용히 틀림)
- **스냅으로 못 고치는 몫**(D-L15) — L-B.4·B.5·B.7 **어느 것도 이 비를 안 바꿨다**
- **승격이 더 내는 오차는 배율이다** — 배율을 확정 틀에 맞추는 기전이 없다
- **`turn_detection` 대조군이 천장에 붙어 있다**(동적 범위 5.8pp) — 완만한 코너 픽스처가 필요하다
- `min_guide_ratio` 0.45가 **수직 가이드에 원리적으로 도달 불가**다(588.18 < 651.2) — DEFERRED
- L-B.1 3건 · L-B.2 3건 · L-B.3 2회차 3건 · L-B.4 3건 · **L-D.0 3건**을 DEFERRED로 넘겼다

## ⚠ 반드시 먼저 읽는다

1. `CLAUDE.md` — 작업 지침. §2에 게이트가 있다
2. **`PITFALLS.md`** — 반복해서 걸린 것 **39개**(#38·#39가 이번에 추가됐다).
   **읽는 시점은 항목 착수 시**이고, 걸리는 번호를 `progress.md`에 적는 것이 읽었다는 증거다
3. `docs/line_plan.md` — **개정 2**
4. 이 문서
5. `progress.md`(**루트의 것 하나뿐이다.** ⚠ 쉘 작업 디렉토리가 `web/`일 때 `cat >>`를 하면
   `web/progress.md`가 생긴다 — 이전 세션이 실제로 그랬다. 맨 끝: **L-D.0** · **L-D.3 사전 등록**) ·
   `DECISIONS.md`(D-L1~**D-L32**) · `assumptions.md`(**AS-L12** 신설) · `DEFERRED.md`

## 이전 세션들이 남긴 사실

L-B.3 일곱 · L-B.4·B.5 셋 · L-B.7 넷 · L-B.M 넷 · L-B.6·B.8 셋 · L-C.1 셋은
`progress.md`와 `assumptions.md`(AS-L1~AS-L12)에 있다. **판정의 단일 출처는 `assumptions.md`**이고
이 문서는 그것을 요약하지 않는다 — 요약이 갈리면 다음 세션이 낡은 쪽을 읽는다.

## 최근 커밋
```
1f2db16 L-D.3 사전 등록 — 판정 다섯을 측정 전에 박는다(리뷰어 [11])
7192c2b L-D.0 롤 0 제약 — 게이트 실패. 이론은 맞고 검출 입력에서 안 선다
256e24c 리뷰어 L-D.2 대응 (2/2) — 판단을 원장 아닌 문서에도 적는다
87d1a88 리뷰어 L-D.2 대응 (1/2) — 인용 해시 값 대조 · 오프라인 양성 채널 · 원장 밖 확인
4bb9936 #35 검사가 0건이던 것을 고친다 — 검사가 아니라 기록하는 쪽을
```
