# HANDOFF.md — 세션 인계

다음 세션은 이 문서를 읽고 이어서 진행한다. 사람의 개입은 그 한 줄뿐이다.

## 읽어야 할 문서 순서
1. `CLAUDE.md` — 작업 지침(A-1~A-4, 자료구조, 참조 저장소 금지 규정, 이론서 지도)
2. `docs/sketch3d_plan.md` — **현행 계획서**(코드명 S). 폐기된 3종은 `docs/archive/`
3. 이 문서 (아래 현재 단계)
4. `DECISIONS.md` 맨 아래 "S 전환" 절 — **D-S1~D-S8**
5. `DEFERRED.md`(맨 아래 S-3 두 절), `assumptions.md`(맨 위 S 절, AS-1~AS-12), 필요할 때 `docs/perspective_theory.md`

## ⚠ 이 프로젝트에서 반복해서 걸린 것 세 가지

1. **문서의 수치가 원장(JSON)보다 뒤처진다.** 수치를 문서에 쓰기 전에 `stage0/out/`의
   JSON을 그 자리에서 다시 읽는다. 재측정했으면 그 값을 인용한 문서를 `grep`으로 전부 찾아 고친다.
   **S-3에서 또 걸렸다** — 이전 실행의 "268점"이 세 문서에 퍼져 있었고 원장에는 없었다(리뷰어가 잡았다).
2. **측정이 자기확인이 된다.** S-2는 합성 활의 대칭성, **S-3은 합성 잉크가 끝점을 참 모서리에
   못으로 박고 있던 것**이었다(`sin(πu)`가 양 끝에서 0). 그 상태로 앵커 체인을 재니
   "오차가 누적되지 않는다"가 나왔다. **수치가 이상하게 좋으면 노이즈 모델의 성질을 재고 있는
   것은 아닌지 먼저 의심한다.**
3. **지표가 정의의 귀결일 수 있다.** S-3의 떨림비가 그랬다 — 채택한 평면에서 극값인 것이
   그 평면의 정의였다. **지표 하나가 이기면 그 지표가 왜 이기는지 먼저 묻는다.**
   (족 전체를 스윕해 보는 것이 답을 준다.)

## 현재 단계
**S-A · S-0 · S-1 · S-2 · S-3 완료 → S-4(렌더링) 진행**

| 단계 | 상태 |
|---|---|
| S-A 전환 정리 | 완료 |
| S-0 기반 | 완료 — three.js 뷰포트(온디맨드), PWA 골격 |
| S-1 카메라 확정 | 완료 — §3 전체. **단 S-3이 결함 둘을 찾았다**(AS-10 지면 평면, AS-11 캔버스 1×24). 고쳤고, S-1 절의 브라우저 수치는 근거로 쓰지 않는다 |
| S-2 방향 판정 | 완료 — 미결 2항목 결정(D-S2·D-S3), 자기확인 발견·정정 |
| S-3 획 → 3D | 완료 — **중단 없음**. 떨림 평면 `facing` 채택(D-S8), AS-3 대상 정정(D-S7), AS-8 정량화 |

**브라우저에서 지금 되는 것**: 소실점 셋을 찍고 → 그리면 → 획이 축 색으로 판정되고 →
3D에 놓이고 → 돌려서 볼 수 있다. 미분류 획은 회색 파선으로 남고 축 선택 버튼이 뜬다.

## 다음에 할 일 — S-4 렌더링 (계획서 §7)

1. **튜브 메쉬** — three.js `TubeGeometry`(MIT). 지금은 `THREE.Line`이다.
   `s3d/strokeView.ts`가 그 자리다. **참조 저장소 코드를 복사하지 않는다**(CLAUDE.md §3).
2. **속도 기반 굵기** — 점 간격에서 계산한다. `pts2d`에 시간이 들어 있다
   (`capture/inkCanvas.ts`가 `[x,y,t,pressure,tiltX,tiltY]`를 담지만 `Stroke.pts2d`는
   `[x,y]`만 보관한다 — 굵기를 쓰려면 여기를 먼저 정해야 한다).
3. **캡**, 온디맨드 렌더 유지(`Viewport.invalidate`).
4. DEFERRED의 S-3 잔여 중 **S-4로 배정된 둘**을 함께 처리한다:
   선분 보간 대조군, 등급 순서 역전의 원인 분리.

## S-3이 남긴 것 — 다음 단계가 알아야 할 것

### 채택·정정
- **떨림 평면 = `facing`**(D-S8). L을 담되 시선에 가장 정면으로 서는 평면.
  계획서의 두 후보 어느 쪽도 아니다 — 눈을 지나는 평면은 **원리적으로 불가능**하고
  (화면에 직선 하나로 맺힌다), 화면 평행 평면은 축을 잃는다.
  **주장은 좁혀져 있다**: 족 안 ±20°는 구분되지 않고, 채택은 극단의 기각으로 지지된다.
- **AS-3의 임계 대상 정정**(D-S7). 원래 지정한 "재투영 화면 좌표차"는 **항등**이라 발동할 수
  없었다. 임계값은 두고 대상을 접합 이동량과 미배치율로 옮겼다.
  **미배치율 임계 20%는 사후에 정한 잠정값이며 S-10까지 판정 근거가 아니다.**
- **지면 평면은 수직축이 정한다**(`geom3d.groundFrame`). 눈높이 게이지는 `EYE_HEIGHT` 한 군데.
- **몸통 접합은 필수다**(§4.2 3번). 끝점끼리만 이으면 끝점 오차 2%에서 47%가 앵커를 못 찾는다.

### ⚠ 가장 큰 위험 둘

**AS-12 (신규, S-3이 발견) — 끝점 겨냥 오차가 성립 구간 밖일 수 있다**
이 방식은 끝점 겨냥 오차가 **그림 대각의 1% 근처**여야 성립한다(그 이상이면 축이 정해진 획의
30~70%가 안 놓이고 깊은 연쇄가 무너진다). 손에 있는 유일한 실획 자릿수는 Quick,Draw
`closure_gap` 환산 **0.028~0.051**로 그 **3~5배**다. **그대로면 도구를 쓸 수 없다.**
완화 근거 둘(표본이 낙서다 / closure_gap은 다른 양이다) 다 미측정.
**S-10에서 실제 도구 획으로 재는 것이 필수.**

**AS-6 (기존) — 한 획 = 한 축**
실획(n=1816)에서 한 축에 담기는 획은 **29.6%**뿐이고 69.9%가 한 획에 방향을 둘 이상 담는다.
임계에 둔감하다(25°→80°에서 0.70→0.58). 표본이 낙서라 **상한**으로 읽지만 숙련자가 낮을
것이라는 예상은 **미측정**이다. **S-10 재측정 필수.**

두 위험이 같은 곳을 가리킨다 — **S-10의 실획 수집이 이 방식의 실제 시험대**다.
그때까지의 모든 수치는 합성이다(V-o·AS-1).

### 그 밖에 알아둘 것
- **깊이 오차의 지배항은 앵커 체인이 아니라 카메라다**(AS-9). f 오차 10%가 체인 전체보다 크고
  연쇄를 타지 않는 전역 배율 편향이다(이론서 16.4). 평면 비교는 참 카메라 조건에서 한 것이다.
- **측정 조건 표기를 반드시 함께 읽는다.** 원장의 `conditions` 블록에 (축 판정/oracle,
  끝점 오차, skew, 등급, 접합 방식, 시드)가 있다. **참 축 + 끝점 오차 0은 가장 유리한 조건**이다.
- **구도가 하나뿐이다**(요 35°·피치 15° 3점, 직육면체 하나). 원장 `single_composition_warning`.

## 미해소 지적
- 리뷰어 S-3 **2차 잔여 6건**을 `DEFERRED.md` 맨 아래에 올렸다(2회 대응 규칙).
  전부 유효한 지적이며 S-4·S-5에 배정돼 있다.
- S-A·S-0 리뷰어 잔여 중 아직 처리 안 된 것: DEFERRED의 단계 배정이 W-3/W-6/W-8을 가리킨다
  (S 번호로 재배정 필요), selfcheck 산출물에 정적 탐지가 훑은 파일 수 필드가 없다,
  **잉크 캡처 회귀 테스트 0**(S에서도 유지 항목인데 무검증 — S-10 Playwright).
- selfcheck 117건 — 전부 원인 확인(분류표는 progress.md S-3 절 맨 아래).
- `bend_max` 0.18은 측정이 정당화하지 못한다 — S-10 이후 재결정.
- AS-1("숙련자 획에서 정확한가")은 **표본이 없다**. 조용히 틀리는 비율이 조건에 따라
  0.036~0.13으로 흔들린다.

## 개발 환경
```bash
cd web && npx vite --port 5222 --strictPort
```
`S2S_HTTP=1`을 주면 HTTP(기본은 HTTPS). 5173은 다른 프로세스가 잡고 있어 5222를 쓴다.
`npm run dev:5222 --prefix web`이 HTTP+5222 한 줄이고 `.claude/launch.json`이 그것을 쓴다.
브라우저 확인은 마우스 `PointerEvent`를 직접 디스패치하면 된다.
`window.s2s`에 `{strokes, panel, viewport, strokeView, addStroke, placeCtx, refresh}`가 노출돼 있다.

테스트: `cd web && npx vitest run` · `python -m pytest tests/ -q` · `python selfcheck.py`.
카메라 기준값 재생성: `python web/test/gen_camera_ref.py`.

## 최근 커밋
```
acce36c  HANDOFF 갱신 — AS-6 위험 명시, S-3 착수 지점
0727bf8  S-A·S-0 리뷰어 대응 — 새 접근의 단위 가정이 실획에 반박된다
c6cea24  S-2: 방향 판정 — 미결 두 항목 결정, 자기확인을 스스로 잡았다
44a4d76  S-1: 지면 격자와 화면 밖 소실점
```
