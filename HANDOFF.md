# HANDOFF.md — 세션 인계

다음 세션은 이 문서를 읽고 이어서 진행한다. 사람의 개입은 그 한 줄뿐이다.

## 현재 단계 — **2026-08-17 지시 A~K 중 A·B·C·D·E·F·G가 끝났다. H·I·J·K가 남았다**

아이패드 배포본 시험 뒤 사람 지시가 **A~K 열한 묶음**으로 들어왔다(원문은 그 세션의 첫 사용자 메시지).
그중 **A**(축 스냅) · **B**(차수 판정) · **C**(f 자동) · **D**(펜 채널) · **E**(선 표시) ·
**F**(확정 버튼 제거) · **G**(입력 라우팅)가 끝났다.
**묶음마다 main에 병합한다**(사람 지시) — G까지 푸시했고(`f48c504`) **Actions `Pages` 초록을 확인했다**
(run 31971459870). 배포본이 최신이다.

⚠ **사람이 "G까지만 하고 멈춰라. 이후 지시가 있다"고 했다** — H를 착수하지 않았다.
다음 지시가 오기 전까지 H·I·J·K는 아래 표 그대로 대기다.

프로그램 이름은 **Brunelleschi**, 저장 확장자는 **`.brnl`**(내용은 JSON).
배포본: <https://hyunwookyu.github.io/brunelleschi/> **main 병합·배포가 최신이다**(Actions `Pages` 초록 확인).

## ⚡ 착수 전에 `PITFALLS.md` **상단 "최근 다섯"**부터 읽는다

목록이 **41개**다. 최근 다섯: **#41 #40 #39 #38 #37**.
읽었다는 증거는 **그 항목에 걸리는 번호를 `progress.md`에 적는 것**이다.

⚠ **이번 세션이 그것을 한 번 어겼다** — A는 착수 표를 적었는데 **B가 안 적었고 리뷰어가 잡았다**.
"바로 앞 항목이 만든 규칙을 다음 항목이 어긴다"는 #41·#40이 등재된 그 형태다. **항목마다 적는다.**

## 다음에 할 일 — **① 낡은 인용 정리(두 세션 연속 이월) · 그다음 남은 지시 H·I·J·K**

| 묶음 | 요지 | 메모 |
|---|---|---|
| ⚠ **0. 낡은 인용 정리 — 두 세션 연속 이월** | 재측정 뒤 안 고친 인용들. **A·B 리뷰 [15]가 지목했고 G에서도 안 했다**(리뷰어가 두 번 다 짚었다). 대상: **AS-L16**("팔 넷에서 9.9569로 완전히 같다") · **AS-L17** · **AS-L18** · **D-L42**("0.811 → 0.967" → **지금 0.8287 → 0.9638**, 그 정정값은 아래 §5에 이미 있다) · **DEFERRED 269·276·277·279** | `@해시`가 없어 **자동 검사가 못 잡는다** — 사람이 원장을 열어 대조해야 한다. **다른 묶음보다 먼저 한다**(세 번째로 미루면 그 값들이 근거로 재인용된다) |
| ~~**G** 입력 장치로 도구가 갈린다~~ | **끝났다**(2026-08-17). 세 후보를 실제로 재고 골랐다 — `stage0/out/touch_route.json@509615a1`. ⚠ **적어 뒀던 함정 셋 중 하나가 사실이 아니었다**: three r185의 `OrbitControls`에는 `rotateLeft`·`rotateUp`·`pan`·`dollyIn`·`dollyOut`이 **공개 메서드로 있다**(AS-L21에 반증으로 등재). ①(재발행)은 **합성 조건에서** 던지고, ②(잉크 캔버스에 컨트롤)는 **펜이 궤도를 돈다**(1.117 rad). 채택: **라우팅만 직접**(`InkCanvas`가 라우터) + **카메라는 공개 API**(D-L51·D-L52) |
| **H** 오스냅 | 반경이 너무 넓다 → 기본을 줄이고 설정 가능하게(화면 픽셀 기준, 확대·축소 무관) · **종류별 켜고 끄기**(라이노) · **수직점(perp) 동작 확인** · **H-3 거리점 스냅 편입**(C-5에서 표시만 하고 못 한 것 — 45° 선을 정확히 그으려면 필요하다) | `SNAP_TOL.radius_ratio`와 `snap.ts`의 종류 목록. 거리점은 `vpRules.distancePoints()`가 이미 낸다 |
| **I** 자동 분할 + 지우개 | 교차·접촉에서 세그먼트 분할(SketchUp) · 세그먼트 지우개 · **부분 지우개는 분할의 특수 경우로 구현**(I-1 코드를 그대로) · **종속성 정의**(지운 조각이 다른 세그먼트의 앵커였으면) | `DEFERRED`의 "세그먼트 삭제 시 매달린 것들의 처리"가 여기서 처리된다. **지우개가 생기면 삭제가 흔해지므로 미정의면 지울 때마다 구조가 무너진다** |
| **J** 화면 | 그리드 생성이 불균일하다(`grid.ts`) → 간격·범위를 카메라와 무관하게 균일하게 + 토글(기본 켬) · 정보 패널이 화면을 가린다 → **기본 접힘 + 한 줄 요약**(현재 차수, 다음에 할 것) | |
| **K** 실획 측정 준비 | `real_ink.test.ts`가 AS-L9 · QUESTIONS f · AS-6 · 스냅 반경 · 모호 발동 빈도 · **최소 길이 임계** · 정점 산포 · 채널 사용 비율을 전부 내게 한다 | `sessions/`가 여전히 **비어 있다**. 획이 들어오면 **다른 작업보다 먼저 돌린다** |

**그리고 완료 후**: main 병합 → Actions 초록 → 사이트 갱신 확인(지시문 마지막 줄).

## 지난 세션(2026-08-17)이 한 것

| 항목 | 결과 |
|---|---|
| **A** 축 스냅 | ① `screenOrthoSnap` 신설 — **카메라 없이 첫 획부터** 화면 가로·세로로 스냅된다(임계는 `RULE_TOL.screen_axis_deg` 그대로, #17) ② `newRuleState`의 슬롯 2가 **화면 수직**이다(수직축이 아예 없던 자리) ③ **수심 유도를 뺐다** — 지평선 높이가 3점을 만들던 경로 ④ 2점에서 지평선은 **주점 y**다(피치가 아니다) ⑤ `min_vp_len_ratio` 신설 ⑥ `snapAxisTable`로 스냅 표를 코드 하나에 두고 5개 상태를 잠갔다 |
| **B** 차수 후보 | `orderCandidate`(초기·1점 후보·2점 후보) + **전수 전이표 48셀**. `promoted`(1점→2점 승격)를 지웠다 — "되돌리기를 만들지 않는다". **1점 선언은 `initial`에서만** 열린다 |
| **C** f 자동 | 렌즈 슬라이더를 뺐다(**D-L39 뒤집기**). 1점의 f는 **거리점**에서 나온다(7.4). f가 없으면 3D를 안 세우고 "바닥이나 벽을 하나 그려주세요"라 안내한다. 거리점을 지평선 위에 표시한다 |
| **F** 확정 버튼 | 카메라가 서는 순간 **자동으로 잠근다**. 버튼·처리기를 지웠다(`S2S.confirmNow()`가 종단 확인 경로) |
| **D** 펜 채널 | 보조선(기본)·결과선·주석. 주석만 3D로 안 올라가고 판정은 `doc.liftable` 하나가 한다. 스냅 강도는 채널로 안 가른다. 내보내기는 결과선만(보조선은 옵션). 옛 저장본은 보조선으로 열린다 |
| **E** 선 표시 | **미리보기가 가장 진하고 확정선이 흐리던 것을 뒤집었다.** 보조선 표시 토글과 "돌리면 흐려짐"을 다른 손잡이로 뒀다. ⚠ 종단 확인이 둘을 잡았다 — 처음 고른 흐림이 너무 옅어 **돌리면 화면이 통째로 비었고**, `paler()`가 축 미분류 획에서 `undefined`를 받아 **던져서 그리는 중 스냅이 통째로 안 걸렸다** |
| **측정이 설계를 정정했다** | 초판의 3점 입구가 **손 오차(4~8°)로 발화**했고, 한 선을 x=W/2까지 외삽하니 **각 오차가 증폭**됐고, 화면 수평선 하나가 **그림 전체를 1점으로 되돌렸다**. 셋 다 고쳤다. ⚠ **그 근거 수치는 중간 실행이라 원장에 없다**(`DEFERRED.md`) |
| **리뷰어 20건**(높음 8) | **일곱이 내 쪽이 틀렸다** — 개선 수치가 다른 원장의 값 · 표제가 아닌 표에서 읽음 · 순서 격차를 축 스냅 성과로 적었는데 껐다 켜도 같음 · AS-L20이 다른 기전의 팔 · 새 임계에 대체할 값이 이미 있었음. 전부 고치거나 철회했다 |

## 지금 서 있는 사실 — **다음 세션이 먼저 알아야 하는 것**

### 1. **3점 투시의 입구가 바뀌었고 그 정확도는 미측정이다**
지평선은 더 이상 3점을 만들지 않는다. 3점은 **사용자가 기울어진 세로 모서리를 "수직축"이라
답할 때만** 선다(선 둘 이상이면 최소제곱 교점, 하나면 `∩ x = W/2` + 기울기 ≥ 8°).
**그 입구를 가르는 팔이 원장에 없다** — `horizon_pitch.json`의 팔은 전부 **지평선** 축이다.

### 2. **지평선의 이득은 살아 있으나 중앙값 위의 결론이다**
`horizon_pitch.json@509615a1`: 참 **3.39°**(몰아) · **4.88°**(번갈아) / 중앙 **21.23°** · **12.26°** /
위약 **31.70°** · **33.39°**. **±120px까지 3.39 → 5.63°**.
⚠ p90은 참 30.79 대 중앙 35.47(1.15배)이고 **max는 참이 더 나쁘다**(53.92 대 47.10).
⚠ 위약 팔은 카메라가 선 실행이 **32/144**뿐이라 참(96/144)과 **모집단이 다르다**.

### 3. **순서 가정(AS-L13)의 방향이 뒤집혔다**
`rule_camera.json@509615a1`의 **표제 표**(1pt 항등 구도를 뺀 `headline_no_identity`, #5):
몰아 긋기 **14.42°** · 번갈아 **9.82°** · 검출 **9.17°**. **몰아 긋기가 이제 더 나쁘고**
**게이트(규칙 ≤ 검출)는 여전히 실패다**. ⚠ 뒤집힌 원인은 **안 갈랐다**(A·B·A-4가 같은 실행에 섞였다).

### 4. **`horizon_pitch.json`의 `passed`는 이제 영원히 false다**
등록 조건 ①("중앙 지평선 팔은 차수 3이 0")이 잰 기전(수심 유도)이 없어졌다.
**하네스의 등록문 자체는 아직 안 고쳤다** — 다음 세션이 원장만 읽으면 틀린 문장을 읽는다.

### 5. **재측정 뒤 안 고친 인용이 남아 있다**(리뷰어 [15])
`@해시`가 없어 자동 검사가 못 잡는 자리다: **AS-L16**("팔 넷에서 9.9569로 완전히 같다") ·
**AS-L17 · AS-L18 · D-L42**("0.811 → 0.967" → 지금 **0.8287 → 0.9638**) ·
**DEFERRED 269·276·277·279**. **다음 세션이 그것부터 고친다.**

## 검증 현황 (이 커밋 기준)

vitest **71파일 441 통과 · 1 건너뜀** · tsc·빌드 통과 · Playwright **26 통과 · 2 건너뜀** ·
pytest **73 통과** · selfcheck 플래그 **1833건 · STALE(상수) 0 · STALE(정의) 0** ·
**상수 해시 `509615a1`** · 인용 **110건 플래그 0** · 게이트 블록 13 · 훑기 4 ·
분모 검사 44파일(플래그 12는 기존 `Math.max(1,n)`).
⚠ **`GESTURE_TOL`(제스처 감도)은 `SHARED_CONSTANTS`에 없다** — 그래서 상수 해시가 안 움직였다.
사유와 대가(STALE 사각지대)는 `DEFERRED.md`에 있다.

⚠ **면제는 통과가 아니다.** ⚠ **벽시계 수치는 문서에 인용하지 않는다.**
⚠ 건너뛴 것: vitest `real_ink`(`sessions/`가 비었다) · Playwright `coords`의 `[양성 채널] dpr 1`과
`stage`의 하나. CI에서는 `data/quickdraw`가 없어 둘이 더 건너뛴다(**통과로 안 센다**).

## ⛔ 남겨 둔 폐기 코드 — 측정 근거의 재현용

`segment.ts`(G 실험) · `cameraFit.ts`(D-L6) · `horizonRollZero.ts`(D-L32) ·
**`horizon.ts`의 `vpVerticalFromOrthocenter`**(D-L38 → A-4로 배선 해제).
**넷 다 배선하지 않는다.** 지우면 그 판정의 근거를 다시 만들 수 없다.

## ✋ 브라우저에서 보는 절차

로컬: `cd web && npm run dev:5222` → `/l.html`

앱 손잡이 `window.S2S` — `doc()` · `cam` · `stage` · `diag()` · `snap(p)` · `endSnap(from,p)` ·
`hoverSnap()` · `twoPointStrokes()` · `live()` · **`live2d()` · `orthoSnap(a,b)` · `snapAxes()`** ·
`axisLock()` · `horizon()` · `setHorizon(y)` · `horizonGrab(p)` · `views()` · `order()` ·
**`orderCandidate()` · `distance()` · `distancePoints()` · `confirmNow()` ·
`channel()` · `setChannel(c)` · `setPickedChannel(c)` · `channels()` ·
`exportGuides()` · `setExportGuides(on)` · `showGuides()` · `setShowGuides(on)`** ·
`promoteOrderNow()` · `chainTrace()` · `rules()` · `feedLine(a,b)` · `setAxisLines()` · `exportObj()`.

⚠ **`확정`·`렌즈` 버튼이 없어졌다** — 종단 확인은 `S2S.confirmNow()`를 부른다.
합성 상자는 **측정 하네스와 같은 픽스처**로 넣는다 — `await import('/test/scene3d.ts')`.
⚠ **캔버스 크기 굳음이 세 번 재발했다**(AS-C7) — 확인은 **창을 앞에 두고** 한다.

## 개발 명령

```bash
cd web && npx vitest run && npx tsc --noEmit && npm run build && npx playwright test
```
```bash
python -m pytest tests/ -q && python selfcheck.py
```
⚠ `vitest`는 **`web/`에서** 돌린다. ⚠ `playwright` 앞에 **`npm run build`**를 넣는다.
⚠ **`progress.md`는 저장소 루트의 것 하나다** — `cd web` 상태에서 `cat >>` 하지 않는다.

## 미해소 (전문은 `DEFERRED.md`의 **2026-08-17 절**)

- **실획 표본이 0이다**(AS-C1) — K가 그것이다
- **3점 입구의 정확도 미측정** · **`horizon_pitch`의 등록문이 낡았다** · **A·B가 같은 실행에 섞였다**
- **`min_vp_len_ratio`의 덮는 대상 미상 · 스윕 미실행** · **`first_pair_sep_deg_median` 전 층 null**
- **`axis_snap`의 `order_gap_off = 0`이 미실행** · **위약 팔이 1.0으로 포화**
- **C-5의 거리점 스냅 미구현**(표시만 했다 → **H-3**) · **H·I·J·K 전부**(G는 끝났다)
- 이전 세션에서 이어지는 것: 꼭짓점 병합 없음(정점 스냅 공허) · `osnap.json`이 참 3D를 대상으로 씀 ·
  축 스냅 손익분기 배수 미측정(QUESTIONS f) · 지평선 창의 바깥 끝 미측정 ·
  롤 0이 자료구조에 박힘(QUESTIONS d·e) · v1 저장을 열 수단 없음(D-L29)

## ⚠ 반드시 먼저 읽는다

1. `CLAUDE.md` — 작업 지침. §2에 게이트가 있다
2. **`PITFALLS.md`** — 반복해서 걸린 것 **41개**. **읽는 시점은 항목 착수 시**다
3. `docs/line_plan.md` — **개정 2**
4. 이 문서
5. `progress.md`(루트 하나, 맨 끝 2026-08-17 절) ·    `assumptions.md`(AS-L1~**AS-L21**) · `DECISIONS.md`(~**D-L52**) · `DEFERRED.md`(**2026-08-17 절**) · `QUESTIONS.md`(a~g)
