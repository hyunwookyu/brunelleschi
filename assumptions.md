# assumptions.md

계획서(docs/sketch3d_plan.md)의 **전제**를 항목화한다(CLAUDE.md §5 자기검증 2).
각 단계 완료 시 관측과 부합하는지 재확인하고, 어긋나면 그 전제에 의존하는 판정 기준을
함께 갱신한다. 상태: ✅부합 · ⚠️갱신됨 · ❌위반(판정 함께 수정) · ⏳미검증 · 🗄무효(접근 전환)

---

## S 전환 (2026-08-14, 두 번째) — 신규 전제

접근이 다시 바뀌었다: **투시 작도로 엣지·정점을 만들던 것**에서 **획을 그대로 3D 곡선으로
올리는 것**으로. 판정하는 것은 방향뿐이고 손떨림은 보존한다.

> **번호 규칙**: 전제는 `AS-n`, 단계는 `S-n`이다. 둘을 같은 이름으로 쓰다가
> 표 안에서 "S-3 … ⏳ S-3"처럼 읽혀 리뷰어가 지적했다.

| # | 전제 | 출처 | 관측 | 상태 |
|---|---|---|---|---|
| AS-1 | 축 판정이 **숙련자 획**에서 충분히 정확하다 | §4.1 | **표본이 없다**(리뷰어). 숙련자 획을 잴 수단이 지금 없고, V-o는 합성으로 실제를 주장하는 것을 금지하며 V-A 수집 경로는 삭제됐다. S-2가 잰 것은 **합성**이며 대표값(치우친 활)은 판정률 0.926 / 정확도 0.961 / **조용히 틀림 0.036**이다(`axis_judgement.json`). **S-3이 같은 지표를 다른 조건에서 다시 냈다** — 끝점 겨냥 오차 2%·3점 구도에서 `wrong_axis_rate`(같은 정의)가 **0.088~0.131**, 미분류 0.107~0.332다(`wobble_plane.json` `by_grade`). 즉 **조용히 틀리는 비율은 구도와 끝점 정확도에 3배 넘게 흔들린다** — 0.036을 단일 대표값으로 읽으면 안 된다. "숙련자 획에서"는 S-10 이후 실획 수집 뒤 | ⏳ 표본 없음, 범위 0.036~0.13 |
| AS-2 | **떨림 평면** 선택이 회전 후 자연스럽다 | §4.4 | **성립하되 계획서의 두 후보 어느 쪽도 아니다**(D-S8). 눈을 지나는 평면은 **원리적으로 불가능**하고(화면에 직선 하나로 맺혀 떨림 점이 한 개도 안 놓인다), 화면 평행 평면은 축을 잃는다(각도 34°). 채택한 `facing`은 **대표 조건**(판정 통과·끝점 오차 2%·skew 0.12·medium)에서 축 각도 **2.14°** / 모서리 오차 **0.0597** / 깊이 오차 **0.0277**이고 `sight` 13.31°/0.2412/0.1385, `screen` 34.06°/0.3644/0.2021이다. **떨림비는 근거에서 뺐다** — 평면 족 스윕에서 `facing`이 극값이라 정의의 귀결이었다(리뷰어). 모서리 오차도 같은 자리에서 극값이고 **그쪽은 참값 대조**라 채택은 유지된다. `stage0/out/wobble_plane.json` | ⚠️ 갱신됨(셋째 평면) |
| AS-3 | **미리보기와 확정 결과의 차이가 작다** | §4.5 | **성립한다. 단 D-S6이 지정한 양은 항등이었다** — 각 점이 자기 광선 위에 놓이므로 재투영 좌표차는 실측 415획에서 **정확히 0px**(중앙·p90·최대). 임계값은 그대로 두고 **적용 대상만** 접합 이동량과 미배치율로 정정했다(D-S7): **판정을 한 조건에서 읽지 않는다** — 끝점 겨냥 오차 1% 이하면 성립(접합 이동 p90 0.048, 미배치 0.002), 2%면 축이 정해진 획의 **30%**가 안 놓이고, 4%면 **70%**가 안 놓인다. 즉 **중단 조건은 도구가 아니라 입력에 걸린다**: 이 방식은 끝점 겨냥 오차가 그림 대각의 1% 근처일 때 성립한다. 그 값이 숙련자에게 얼마인지는 미측정(AS-1) — **S-10 첫 측정 대상** | ⚠️ 통과, 대상 정정 |
| AS-4 | 면 없이 형태가 읽힌다 | §6 | 참조 구현이 선만으로 동작 | ⏳ S-8 |
| AS-5 | 축 밖 획의 상당 부분이 **면 위 사선**이다 | §4.3 | 분모를 **오탐 보정 후 비직교 직선 획**으로 정의한다. 비교 기준은 52.6%가 아니라 보정값 **0.29~0.36**(V-z). 곡선 26.5%는 분모에서 뺀다 | ⏳ S-6 |
| **AS-6** | **한 획 = 한 축** | §4.1 기본 동작 | **실획 위반율 0.699**(n=1816, Quick,Draw). 한 축에 담기는 획은 **29.6%**뿐이다. 임계에 둔감하다(25°→80°에서 0.70→0.58). 표본이 **낙서**이므로 위반율의 **상한**으로 읽으나, 숙련자가 낮을 것이라는 예상은 **미측정**이다. 이 값이 그대로면 S-2가 대부분을 미분류로 보내 도구가 쓸 수 없게 된다 — **S-10에서 실제 도구 획으로 재측정하는 것이 필수**다. `stage0/out/one_stroke_one_axis.json` | ⚠️ 위반 큼, 재측정 필수 |
| **AS-7** | 곡선 획이 소수다 | 리뷰어 재배치 | **아니다**. 같은 표본에서 곡선 **26.5%**(archive_W/stroke_edge.json). W에서는 "범위 밖"이라 넘겼지만 이번 접근은 곡선을 1급으로 다루므로 **더 중요해졌다** — §4.1 "크게 휜 획" 처리가 실획의 1/4에 걸린다 | ⚠️ AS-1·AS-5의 규모 지표 |
| **AS-8** | 앵커 체인의 위치 오차가 누적되지 않는다 | 리뷰어 지적 | **틀렸다 — 누적한다. 그리고 조건에 따라 무너진다.** 끝점 오차 **1% 이하**면 연쇄 6까지 3.3배 안(√n 꼴). **2%부터는 깊은 연쇄가 무너진다** — 연쇄 6~8이 0.29~0.38로 연쇄 0의 **14~18배**이고 5→6 한 칸에서 4.6배가 뛴다. 같은 조건에서 배치도 실패하기 시작한다(0.89). _1차 보고가 연쇄 5까지만 싣고 "폭주하지 않는다"고 적었던 것을 리뷰어 지적으로 정정했다._ 깊은 연쇄의 표본은 2~42로 얇아 자릿수만 읽는다. **떨림 평면이 이 값을 정한다** — `sight`에서는 끝점 오차 1%에 0.019→0.140(**7배**)이다. **그리고 이 결론은 "참 카메라가 주어졌을 때"다** — f를 10% 틀리게 주면 깊이 오차가 0.013→0.024~0.032로 체인 전체보다 커지고, 연쇄를 타지 않는 **전역 배율 편향**이다(이론서 16.4·AS-9 그대로). **1차 측정은 자기확인이었다**: `renderInk`의 활이 `sin(πu)`라 끝점이 참 모서리에 못으로 박혀 있어 "누적 0"이 나왔다(실획 모델의 `closure_gap`을 생성기가 구현하지 않았다). 끝점 오차를 인자로 빼고 스윕한다. **대표값은 고르지 않는다** — 숙련자 끝점 오차 표본이 없다(AS-1). `stage0/out/wobble_plane.json` | ⚠️ 반증·정량화됨 |
| **AS-12** | 사용자의 **끝점 겨냥 오차**가 이 방식이 성립하는 범위에 있다 | S-3이 발견 | **지금 손에 있는 유일한 실획 자릿수는 성립 구간 밖이다.** S-3 측정: 끝점 겨냥 오차가 그림 대각의 **1% 근처**여야 성립한다(그 이상이면 축이 정해진 획의 30~70%가 안 놓이고 깊은 연쇄가 무너진다). 실획에서 끌어올 수 있는 값은 Quick,Draw `closure_gap` 환산 **0.028~0.051**로 그 **3~5배**다. **이 값이 그대로면 도구를 쓸 수 없다** — AS-6과 같은 형태의 위험. 완화 근거 둘 다 미측정: ① 표본이 낙서다 ② `closure_gap`은 *자기 시작점*으로 돌아오는 오차이지 *다른 획의 모서리*를 겨냥하는 오차가 아니다(상한 보장 없음, 하한일 수도). **S-10에서 실제 도구 획으로 재는 것이 필수** | ⚠️ 최대 위험, 재측정 필수 |
| **AS-10** | 지면은 카메라 좌표계의 `y = const`다 | S-1 구현(암묵) | **틀렸다.** 카메라가 수평일 때만 맞고 **3점 투시에서는 기울어 있다**. 지면 법선은 **수직 소실점 방향**이다(`geom3d.groundFrame`). S-1의 격자와 S-3의 씨앗이 서로 다른 평면을 쓰고 있었다 — 격자 위에 그은 획이 격자 밖에 놓인다. 격자선이 자기 축의 소실점을 지나는지로 잠갔다(구 가정에서는 그 검사가 깨진다) | ❌ 위반, 고침 |
| **AS-11** | 캔버스 크기가 생성 시점에 잡혀 있다 | S-1 구현(암묵) | **틀렸다.** 잉크 캔버스가 **1×24로 굳어** 있었다(S-0에서 3D 캔버스에 겪고 고친 것과 같은 자리인데 적용 안 함). 그 크기가 화각·무한원 판정(`isFiniteVp`)의 기준이라 **소실점 셋이 둘로 읽혔다** — 브라우저에서 `case: 2pt`, f 307, 화각 0.19°. `ResizeObserver`로 고친 뒤 f·주점이 정확히 복원된다 | ❌ 위반, 고침 |
| **AS-9** | 깊이 좌표의 출처를 구분하지 않아도 된다 | CLAUDE.md §1 | **아니다 — S-3이 실측했다.** f를 −20~+20% 틀리게 주면 깊이 오차 중앙이 0.0132 → 0.0335·0.0604, 모서리 오차가 0.0269 → **0.1408·0.1453**(5.2배)이다. **f 오차 10%가 앵커 체인 전체보다 크고**, 연쇄를 타지 않는 **전역 배율 편향**이다(이론서 16.4 그대로). f 20% 오차의 모서리 오차 0.1408은 §4.4 평면 채택 근거(`facing` 0.0597 ↔ `sight` 0.2412) **사이에 든다** — 카메라 설정 오차가 평면 선택의 차이와 같은 대역이다. 1점에서 f는 설정값, 2점에서 주점은 가정(W-y 미검증). provenance를 되살리지 않는 판단은 D-S5에 적었고, **출처는 `Camera.fSource` 하나가 들고 화면에 나온다** | ❌ 위반(설정 오차가 지배항), D-S5 유지 |

## W 접근(투시 작도)에서 무효가 된 항목

W 전환에서 세웠던 전제 중 **획을 직선으로 대체하는 것**에 딸린 것들이 무효다.
지우지 않는다 — 두 번째로 접근을 바꾼 이유가 여기 있다.

| 항목 | 무효 사유 |
|---|---|
| W-1 / W-1′ (획→엣지 매핑) | **획에서 엣지를 뽑지 않는다.** 게이트였던 `optimal_rate`도 함께 무효. 그 지표에 남아 있던 미해소 결함 넷(발동 조건 부재·전제와 구현 품질 혼재·개수만 비교해 상쇄·곡선 계수 불일치)도 대상이 사라져 종결된다 |
| W-2 (제약 재적합) | 이미 반증됐고(코너 오차 중앙 0.4~2% 감소, p90 악화), 새 접근에는 재적합이라는 연산 자체가 없다. **이 반증이 접근을 바꾼 근거 중 하나다** |
| W-3 (카메라 확정 부담) | S-1에서 같은 UI를 쓰므로 **유지**한다 → 아래로 옮김 |
| W-4 (면 생성 경계 조건) | 엣지 그래프의 최소 순환이 아니라 획 루프의 근사 평면 문제로 바뀐다 → S-4로 대체 |
| W-5 (면 위 사선 회수) | 질문은 살아 있고 S-5로 이어진다 |
| W-y (2점 주점 = 이미지 중심) | **유지**한다. S-1이 같은 2점 경로를 쓴다 → 아래로 옮김 |

**W에서 유지되는 것**: W-3(카메라 확정이 부담이 아닌가 — 미검증), W-y(주점 가정 — 미측정,
S-1이 직접 쓴다), 그리고 아래 "유지되는 것" 절 전체.

## 첫 전환(형태 추론 → 투시 작도)에서 무효가 된 항목

삭제하지 않는다 — **왜 접근을 바꿨는지가 이 항목들에 적혀 있다**. 다만 이후 판단의
근거로 쓰지 않는다. 해당 코드는 W-A a에서 제거됐고 측정 산출물은
`stage0/out/archive_pre_W/`에 보존돼 있다.

| 무효 항목 | 무효 사유 |
|---|---|
| V-d, V-f, V-g, V-k, V-x, V-B, V-D, V-I, V-G, V-M, V-N, V-O, V-P, V-F, V-J, V-K, V-t, V-i, V-s | **형태 추론 자체가 폐기**됐다. 종횡비 미확정·축 뒤바뀜·꼬리 실패·지표 맹점은 전부 "미지수가 관측보다 많다"는 하나의 원인에서 나왔고, 카메라를 먼저 확정하면 그 원인이 사라진다 |
| V-C, V-E | 불확실성 전파·시거리 상한 게이트가 함께 폐기(추론하지 않으므로 불확실성이 없다). 단 **화각 경고(18.4)는 유지** |
| V-L | 다중 뷰 정합이 폐기됐다(돌려서 계속 그리면 같은 세계) |
| V-v, V-w | 이미 해소됐고, 새 접근에서는 질문 자체가 성립하지 않는다(면을 직접 만드니 매스/보이드가 자동) |
| V-a, V-b, V-c, P2, P3, A1~A4, S1~S3, D1~D3, M1~M3 | IR·평면 파서·footprint 압출에 딸린 전제. 자료구조가 Vertex/Edge/Face로 대체됨 |

**유지되는 것 (13건)**: V-A(Quick,Draw가 애플펜슬을 대표하는가 — 미검증),
V-z(직교 전용의 한계 — 면 위 사선으로 완화되나 곡선은 여전히 범위 밖), V-q(잉크 노이즈 모델),
V-H(재현성 규칙), V-p(등급 판정 단위), V-m(마우스 전용 tolerance 불요 — W-2 판단의 근거),
V-o(합성이 실제를 대표하지 않는다 — W-0이 실획·합성을 **분리해 보고**하는 이유),
M4(무노이즈 1.5e-16 자기참조 — W-0/W-1 재발 유형), P1/P4/V-n(지연 예산), C1(캡처 포맷).
V-y는 위 **W-y**로 승격했다.

**추가 무효 (4건, 표에 누락됐던 것)**: V-e·V-h(합성→실영상 전이, 트랙2 2G 대상 소멸),
V-j(치수 발화율 — 발화 입력이 범위 밖), V-l(앵커 유입 경로 — IR 앵커 폐기),
V-r·V-u(구 노이즈 모델 의존 결과의 유효성 — 그 결과들이 폐기됨).

> 위 표의 무효 항목은 아래 개별 행에서 상태를 **🗄**로 바꾸지 않았다. 행마다 고치면
> 반증 경위(❌ 표시와 근거)가 지워진다 — 그 경위가 접근을 바꾼 이유이기 때문이다.
> **이 절이 무효 여부의 단일 판정처다.** 개별 행의 ✅/❌는 "폐기 당시의 상태"로 읽는다.

---

## 파서·아키텍처

| # | 전제 | 출처 | 관측 | 상태 |
|---|---|---|---|---|
| A1 | 파서는 결정론적·소규모 → TS 이식 타당 | spec §1.3 | `parse_strokes` 결정론적, tol 스칼라뿐 | ✅ |
| A2 | SolveSpace 바인딩이 유일한 난점, 솔버는 폐합만 | spec §1.3/§1.4 | **SolveSpace 미사용**. 폐합은 `recognize_rectilinear`가 자체 수행 | ❌→이식 난도↓, 표 "폴리곤 폐합" 자체구현 확인 |
| A3 | 기본 분할 = DP + 축클러스터 + 스냅 (분리 단계) | spec §1.4 표 | 기본=PaleoSketch `recognize_rectilinear`(프레임+스냅 융합). DP는 폴백 | ⚠️ 포팅 우선 paleo, 표는 DP경로 |
| A4 | tolerance는 10 스칼라 | plan §6.4 | `DEFAULT_TOL` 10 + `PALEO` 6 + stage1 상수(CANON_DIAG/FIT_MAX/LINE_CAP) | ⚠️ TS 이식은 PALEO+stage1 상수도 포함 |

## 스키마

| # | 전제 | 출처 | 관측 | 상태 |
|---|---|---|---|---|
| S1 | IR 루트 6필드 | spec 전제 | **7필드**(relations+openings, 지난 세션) | ❌→판정 G "27테스트"→**32**, §10 동일, TS 스키마 7필드 |
| S2 | 앵커 하나로 전체 절대치수 확정 | plan §3.3 | `apply_ops` 스케일 전파 동작 | ✅ |
| S3 | 관계 4종 | plan | 코퍼스 실측 **5종**(+aligned) | ⚠️(반영됨) |

## 표시·confidence (판정 D 핵심)

| # | 전제 | 출처 | 관측 | 상태 |
|---|---|---|---|---|
| D1 | 30% 완성에 저confidence **볼륨이 존재**해 파선 표시로 D 만족 | spec §0.2 D/§3.5 | **30%→parseable 0.017**(볼륨 없음), 50%→0.23. 그릴 볼륨 자체가 없음 | ❌→**D 교체**: 표시층 tentative hint(획뭉치 bbox/hull 파선상자, IR밖) |
| D2 | 중간 confidence 볼륨이 흔함 | plan §3.5 | 파서 결정적 → tentative 볼륨 희소(완전 아니면 미형성) | ❌→D1과 동일 해법 |
| D3 | confidence 임계 0.8/0.5가 실 값과 정합 | spec §3.5 | 완성 precise 0.83~0.89(≥0.8), coarse 0.63(0.5~0.8), 부분 <0.5 | ✅ |

## 측정 정합 (자기참조·자명 점검)

| # | 전제 | 출처 | 관측 | 상태 |
|---|---|---|---|---|
| M1 | multiplex 명명 결합률이 실제 성능 | score m_multiplex | **named_rate=1.0은 자기참조**: make_multi가 deixis 위치를 볼륨 중심에 둠→항상 최근접 자명 | ❌→비자기참조 테스트(중심에서 벗어난 deixis) 추가 필요 |
| M2 | query scale 최상위율 1.0이 유의 | score m_query_loop | scale 무앵커 시 산출물 미결정→항상 최고. 1.0은 정의상. 측정 대상=랭킹 정합(정상) | ⚠️ 정상이나 "1.0" 확인함 |
| M3 | opening_pos impact 0.0 | stage6 | pos가 산출물(부피)에 무영향→정확히 0. 이 경우 정상(조용히 채움) | ✅(확인함) |
| M4 | 카메라 fit_error | stage3 | 무노이즈 1.5e-16은 자기참조 → **폐기**, 노이즈 조건 재측정(p95 0.013~0.105) | ⚠️(해결됨) |

## 캡처 (V-5 대비)

| # | 전제 | 출처 | 관측 | 상태 |
|---|---|---|---|---|
| C1 | capture가 Pointer Events + pressure/tilt 저장 | spec §5.1/§5.3 | index.html: pressure·tilt 획득하나 **저장 포맷에 tiltX/Y·seq 없음** | ⚠️→V-5에서 추가 |

## 성능 (V-3/V-8 대비)

| # | 전제 | 출처 | 관측 | 상태 |
|---|---|---|---|---|
| P1 | 획→3D p95 <150ms 달성 가능 | spec §0.2 A/§8 | **V-4 측정: compute 전구간 p95 <2ms(1/3/8볼륨), 스케일 평탄. rAF(≤16ms) 더해도 «150ms** | ✅ V-4(compute), rAF포함 전경측정 V-8 |
| P4 | 렌더 비용이 지연 예산 안에 든다(판정 A 150ms 중 렌더 몫) | 지시서 V(추가) | **V-4 확인**: GPU 렌더+지오메트리 생성 몫 미미(웜 <2ms, 픽셀리드백 345k px 실렌더). 소수 압출 박스라 렌더 저비용. rAF 대기(1프레임)가 렌더 몫의 지배항 | ✅ V-4(compute+GPU), rAF V-8 |
| P2 | TS·Python 파서 출력 일치 | spec §9 V-1 | **달성**: footprint ±3%, confidence ±0.05, 카메라 fit_error <1e-6 (TS 14/14) | ✅ V-1 |
| P3 | relate 기하가 Python(shapely)와 일치 | spec §1.4 | bbox 근사는 L/凹/계단서 37.5% 불일치(거짓 penetrate) → **폴리곤 기반(엣지구간·점포함)으로 교체 → 0% 불일치**(8/8) | ✅ 해소(shapely 불필요) |

## 지시서 V 트랙 (2026-08-13 추가)

| # | 전제 | 출처 | 관측 | 상태 |
|---|---|---|---|---|
| V-a | 브라우저 파서가 Python 참조와 동등 | 지시서 §1.3 | 파리티 22/22. **V-2 씬빌더/렌더가 IR 소비 검증**(MeshSpec·Three 어댑터 30/30, 픽셀리드백). 파서 IR→씬 경로 무손실 | ✅ V-2 확인 |
| V-b | 증분 갱신이 전체 재파싱과 같은 결과 | 지시서 §2.1 | **검증됨**: 증분 최종 IR==multiplex(footprint·confidence·relations 일치), 브리징병합·순서무관 포함. 획순서 보존이 핵심(merge_polyline 순서의존) | ✅ V-3 하드게이트 |
| V-c | tentative hint가 정식 볼륨으로 매끄럽게 전환 | 지시서 §3.5/D | **씬 레벨 검증됨**: 30% hint스펙→100% 볼륨스펙, diff가 hint제거+볼륨추가(scene.test). 실 Three.js 렌더는 V-8 | ✅ 씬레벨(렌더 V-8) |
| V-d | 투시 단독 복원은 불안정(평면 병용 필수) | plan §3.8 | **트랙2가 직접 시험**. 결과 따라 §3.8 개정 가능 | ⏳ 트랙2 |
| V-e | 작도 영상 정답이 프리핸드 조건으로 전이 | 트랙2 2A→2B | 이상적 선에서 얻은 대응구조가 노이즈서 유효한지 2B로 확인 | ⏳ 트랙2 2G |
| V-f | 소실점만으로 세계 종횡비 결정 안 됨 | 트랙2 2B | **확인**(원리적 제약). 2F가 우회: 직각가정으로 직사각은 결정 가능, 비직교는 불가 | ✅ 확인, 2F 우회 |
| V-g | 건축 평면 코너는 직각 | 트랙2 2F-a | 직사각 truth서 종횡비 복원 IoU 0.85-0.96(성립). **비직교 건축서 붕괴**(IoU 0.62) | ⚠️ 직사각 한정 |
| V-h | 합성 노이즈 스윕이 실영상 조건 대표 | 트랙2 2A/2F | L1~L3 IoU가 합성 기반. **2G 실영상으로 검증 필요**(합성-실 차이=V-e 답) | ⏳ 2G |
| V-i | 직교폴리곤은 직사각 분해로 종횡비 결정 | 트랙2 2H | **폐기(V-5c 재측정)**. 구 값(L자 L1 IoU 0.92)은 도달 불가 코너정밀도(0.48% vs 파서 한계 1.68%) 산물이었다. 실측 노이즈에서 **precise L 0.469 / U 0.454 / 계단 0.290, medium L 0.444** — 전부 0.6 미달 → **달성 가능한 어떤 등급에서도 불성립** | ❌ 폐기(V-t) |
| V-j | 치수 발화가 실제 세션에서 충분 (§3.3 앵커 핵심) | plan §3.3 | **부정(재측정)**: 설계자막 치수율 0.73%(Wilson95 0.50~1.08%), 4/22만 치수. '20건 예시'는 수집기 하드캡(~25세그와 정합, 모순아님). 작도영상(2A) 미수집→분리대조 2G후 | ❌→§3.3 명시세션 한정, 비례전파 기본. 표본 얇음(CI넓음) |
| V-l | 앵커 유입이 발화에 국한되지 않음 | 지시서 V-4 | **V-4 구현·검증**: 볼륨 픽→치수 패널→숫자→`applyOps`(발화 앵커와 동일). 실 DOM: 폭12m→footprint 12.00 비례 전파. 마우스 세션서 무차원 고착 방지. id 이동은 centroid 재매칭으로 방어 | ✅ V-4 |
| V-k | 종횡비 복원 꼬리 실패가 사전판정 가능 | 트랙2 2I | **부정**: fit_error 미예측(recall~0), 기하게이트 recall 0.51. 종횡비 모호는 내재적 | ❌→신뢰 요구시 평면/앵커 |
| V-m | 마우스 획이 pen 획과 같은 tolerance로 처리 가능 | 지시서(마우스 우선) | **V-5b 실획 재측정으로 해소**(합성 아님). Quick,Draw raw 실마우스 잉크: 등급 **precise/medium/coarse 혼재**(square 47/26/47, 중앙≈medium — V-5 1차 "전부 precise"는 오판). 그럼에도 **현행 단일 tolerance로 square 자기정합 IoU 0.928·parseable 0.99**(외부 게이트 TU-Berlin 0.897 상회). **12종 tolerance 스윕서 개선 변형 0** → 평탄 최적. → **마우스 전용 세트 불필요**(근거가 등급 라벨이 아니라 실획 파싱 성능) | ✅ V-5b |
| V-o | 합성 마우스 노이즈가 실제 마우스 드로잉을 대표한다 | V-5 측정 설계 | **반증**(V-5b). 스펙트럼 분해: 실획 저주파(변 휘어짐) 0.075 / 고주파(방향급변) 3.4° vs 합성 0.007 / 14.5° — **양방향으로 틀림**(고주파 과다, 저주파 결여). 합성은 화이트 jitter라 paleo 가우시안 평활이 전량 제거 → parseable 1.0·IoU 0.98의 낙관 편향. 실획의 저주파 휘어짐은 평활로 안 지워져 IoU 0.93로 하락 | ❌ → 합성 수치로 마우스 현실 주장 금지. 튜닝 판단은 실획으로만 |
| V-v | 사용자가 평면과 투시를 함께 그린다 | plan §3.3/§3.8 | **반증 → 해소(지시 2)**: 평면을 **파생 출력**으로 재배치(`to_ir.py`), 카메라 복원을 소실점 개수로 통합(`camera_unified.py`), §3.8 전면 개정, 안내 배너에서 "평면을 그리거나" 제거(재발 방지 테스트) | ✅ 해소 |
| V-w | 그려진 형태는 솔리드 매스다 | IR Volume | **반증 → 해소(지시 3)**: `Volume.kind`(mass/room) 추가, 투시 기하로 판별(실내 1.000 / 외부매스 0.905 / 정면근접 회피 0.950). 발화 보조는 애매하면 추정 금지. 렌더는 room=BackSide. penetrate 해석도 kind 조합별로 분리 | ✅ 해소 |
| V-x | 꼬리 실패는 사전 판정 불가 | 트랙2 2I | **원인 규명 완료·결론은 부분 유지**(지시 1 측정). 진단(8.8: 실패에 기하적 퇴화 없음 → fit_error recall≈0)은 **맞았다**(2:1 직사각→함의 시거리 정확히 2배로 검증). 그러나 **시거리 게이트는 2I에 적용 불가** — 2I의 2점 직각복원에서 f는 두 VP+주점만으로 결정되어(6.2) 종횡비와 독립(무노이즈 f/W=1.000 불변, 테스트로 잠금). 게이트가 정보를 갖는 곳은 **1점 형태가정 경로**: 거기선 precision **≈0.998**(n=3000 확대표본; n=600의 1.000은 경계효과)/recall 0.477(하한+상한). **18.4는 단측**이라 상한(d>3W) 추가 필요(깊음은 하한이 51.7%, 얕음은 상한이 39.8%, 서로 못 잡음) | ⚠️ 2I 사전판정은 여전히 불가, 게이트는 1점 경로에 채택 |
| V-F | plane_iou가 복원 품질을 대표한다 | 트랙2 전 지표 | **부분 반증(1-ter A) + 전수 확대(B-2)**: 같은 유형 맹점이 6개 지표에 더 있었다(부피서명·polygon_aspect·aspect_rel_err·aspect_equiv·relations 유효율·명명 결합률·confidence). 반례 테스트 8종으로 고정, `score.py`에 병기 규정 명시. **부분 반증(1-ter A)**: 종횡비 오차에 **둔감**하다. IoU 0.85가 오차 10~20%를, IoU 0.655가 **35~60%** 를 숨긴다(2I 실패기준 <0.6을 통과). 상관 spearman −0.626(느슨). → 종횡비 상대오차를 **1급 지표로 병기**(2A/2C·2F·2H 전 모듈). IoU 단독 판정 금지 | ❌→병기로 대체 |
| V-M | L자 외곽 비례는 measured로 쓸 수 있다(4.6%) | D-1b-2 전제 | **반증**: 4.6%는 **PCA 정렬 상자**의 값이고, L은 질량분포가 대각이라 그 상자가 45° 돌아 있다(참 aspect 1.333). 건축적으로 의미 있는 **자연 외곽**(변 방향 정렬, 참 1.0)의 오차는 **16.7%**(precise)/33%(medium)다. 같은 표본·프레임만 교체로 확인(0.056 vs 0.167). 부호 오차 중앙 0.852로 접기 artifact도 아니다 | ❌ 전제 무너짐 |
| V-N | 노치는 외곽보다 부정확하다 | D-1b-2 전제 | **반증(뒤집힘)**: 노치 크기 오차 **0.042/0.034**(L precise)로 외곽 종횡비 0.148보다 정확하다. **정정**: 모서리(u,v)를 '게이지 0.500'이라 한 것은 구 코드가 모서리 라벨로 매칭한 산물이었고, 크기 기준 매칭 시 0.042/0.034로 **모서리도 measured**다(게이지 잔차 0.083). U·계단 노치도 같은 수준(du 0.065~0.135) | ❌ 순서 반대, 모서리도 measured |
| V-O | 노치 정확도는 국소성에서 온다 | D-1b-3 초안 | **반증(리뷰어 지적, 이론 귀속 교체)**: 이론서 **16.4**의 잔여 자유도는 **깊이 방향 아핀변환**이므로 훼손되는 것은 **교차 방향 비**(외곽 종횡비)이고 **같은 방향 안의 비**(노치폭÷전체폭)는 불변이다. du·dv를 따로 재어 확인. 이 설명이 검증 가능하고 U·계단까지 포괄한다 | ❌ 국소성 아님, 방향 문제 |
| V-P | U·계단은 노치가 서로 간섭해 분할이 불가하다 | D-1b-2 d | **측정 근거 없었음(리뷰어 지적 [7])**. 구 판은 U·계단 노치를 아예 재지 않고(null) 단정했다. 다중 노치를 각각 재니 **크기는 L과 비슷하게 정확**(du 0.065~0.135). 단 외곽 종횡비는 0.247~1.207로 크다 | ❌ 근거 없음 → 범위 재검토 대상 |
| V-J | IoU가 회전을 정규화한다 | 트랙2 평가 | **반증(D-1b)**: `_norm`은 스케일·이동만 정규화한다. 90° 회전 게이지 해가 0.53으로 찍혀 없는 오차를 셌다. B-2 맹점과 같은 계열이되 **이번엔 과민**. → `similarity_iou`(회전·대응 동치)를 표준으로, `oriented_iou`는 다중뷰·대지방위 용도로 분리 병기 | ❌→용도 분리 |
| V-K | 재투영 오차가 독립 검증이다 | D-1 1차 설계 | **반증**: 복원 평면이 `H⁻¹(관측점)`이라 같은 `H`로 되쏘면 항등이다. 0.0은 자기참조. selfcheck '카운터 0' 플래그로 발견해 판정 근거에서 제외 | ❌ 자기참조 |
| V-L | 게이지는 다중 뷰에서도 무해하다 | D-1 파급 | **미검증**. 단일 뷰에서는 무해하나 뷰마다 독립이면 조합이 2^n이 된다. 다중 뷰 정합 재개 시 전제이며, 대지 방위가 있는 경우에도 문제가 된다 → 그때는 `oriented_iou`가 맞는 지표 | ⏳ 다중뷰 재개 시 |
| V-I | 축 뒤바뀜은 실제 형태 오류다 | 1-ter B 주장 | **반증(D-1)**: 게이지 자유도다. 두 해를 각자의 복원 카메라로 재투영하면 오차 **0.0**(단일 뷰 구분 불가), 정점 대응까지 탐색한 상사 IoU **중앙 0.9998**(96%가 0.95 초과), 최적 대응이 198/200에서 **90° 순환 이동**. IoU 0.527은 `_norm`이 회전을 정규화하지 않아 생긴 **지표의 산물**. → 축 토글 불필요, Track 2 IoU는 과소평가(회전 동치로 재산출 필요) | ❌ 반증 |
| V-G | 종횡비 라벨(폭/깊이)이 기하로 결정된다 | 2점 복원 | **반증**: 8way 대응 탐색이 두 해 중 하나를 임의로 고르며 종횡비는 **역수까지만** 결정된다. 실측 뒤바뀜 **28.75%**. 기존 2F 오차 지표가 이를 오차로 세어 55% 과대했다 | ❌→역수 동치 계산, 축 라벨은 unresolved(1-ter B) |
| V-H | 측정이 재현 가능하다 | 전 측정 | **반증 → 수정(1-ter A, B-0)**: `hash(str)` 시드가 PYTHONHASHSEED 무작위화로 실행마다 달랐다(2F IoU 0.877↔0.885 표류). `stable_seed`(crc32)로 교체. **B-0 전수 확인에서 `stage3/noise_eval.py` 1건 추가 발견·교체**(CP-0.1 근거를 낸 스크립트). selfcheck에 정적 탐지 규칙 추가 → 잔여 0. **시드 변동폭 ±0.010~0.019 → 두 자리까지만 유의.** 재검토 결과 이전 판정 5건 전부 유지(범위 비겹침 기준). 단 개별 수치는 시드 뽑기였다(부분획 0.108→0.008은 실제 0.10±0.04→0.002±0.004) | ⚠️ 수정·소급 완료, 유효 자릿수 2 |
| V-B | 2점 종횡비 실패를 이진 게이트로 사전 판정할 수 있다 | 지시 1-bis 전 | **반증·종결**: 기하적 서명이 없어(무노이즈 f/W=1.000이 종횡비 무관) 원리적으로 불가. 불확실성 구간 폭도 실패를 예측 못 함(**AUC 0.496**, pearson −0.036). 게이트 시도 종결 → **구간 표현으로 대체** | ❌ 종결 |
| V-C | 불확실성 구간이 보정돼 있다(참값을 정직하게 담는다) | 지시 1-bis | **성립**: 커버리지 **0.94 @ CI 0.90**(약간 보수적). 단 **역수 동치**를 인정해야 성립 — 종횡비는 width/depth 라벨링 때문에 역수까지만 결정된다(참 0.6 → 점추정 1.667). 최초 측정이 이를 빠뜨려 0.732로 과소 산출 | ✅ (역수 동치 조건부) |
| V-D | 2점 투시 단독으로 종횡비가 확정된다 | plan §3.8 완화안 | **부정**: 90% 구간 상대폭 중앙 **0.894**(종횡비 1.5면 ~[1.0,2.4]). 2F plane_iou 0.846이 관대해 보인 것은 평면 IoU가 종횡비 오차에 둔감하기 때문. → 앵커·다중뷰 필요(지시 2.5·5의 근거) | ❌ 확정 안 됨 |
| V-E | d>3W 상한이 구도 타당성 기준이다 | 지시 1 상한 채택 | 이론서 18.4는 **광각 단측만** 다룬다. 상한은 프로젝트 추가 휴리스틱(화각 19° 미만)이며 이론적 근거가 아니라 **실측 recall로만 정당화**됨(얕은 오류 39.8% 포착, 하한은 0%). 실획 검증 시 재확인 대상 | ⚠️ 휴리스틱(실측 근거) |
| V-A | Quick,Draw 노이즈가 애플펜슬 획을 대표한다 | V-5c 노이즈 모델 | **미검증**. 현 잉크 모델·코너오차(precise 1.68%/medium 2.69%/coarse 4.21%)는 전부 **Quick,Draw = 마우스·손가락 입력**에서 나왔다. 애플펜슬은 필압·기울기·샘플링률이 다르므로 저주파 휘어짐 분포가 다를 수 있다. 다르면 **노이즈 모델을 펜/마우스 두 벌로 분리하고 Track 2 임계를 재산출**해야 한다 → 재작업 위험 등록. 검증 경로였던 세션 수집기(지시 4.4)는 **W-A에서 삭제됐다** — W-8에서 최소 수집기를 다시 만들어 재측정한다(DEFERRED). 이것이 D-01의 유일한 되돌림 장치다. **1-bis 불확실성 전파의 입력도 같은 분포**이므로 바뀌면 구간 폭·임계가 함께 바뀐다 | ⏳ 실획 수집 후 |
| V-y | 2점 투시에서 주점은 이미지 중심이다 | 이론서 16.2 | 2점은 자유도 1 → 주점 x 또는 f 중 하나를 가정해야 f 결정. **구현·명시 완료**(`camera_unified`가 결과에 assumption 문자열로 기록, IR View에 전달). **실제 손 스케치에서 성립 여부는 여전히 미검증** — 틀리면 f가 틀린다 | ⚠️ 구현·명시됨, 검증 미완 |
| V-z | 직교 전용 파서가 건축 스케치를 충분히 포괄한다 | plan §2 | **부정·정량화(지시 7)**: Quick,Draw 건축 계열 실획 n=1330에서 **파서 범위 밖 0.586**(비직교 0.526 / 오목 0.308). 검출기 오탐(square 0.317 = 의도상 직교인데 걸림, 코너 중앙 3)을 바닥으로 보정해도 house 0.29 / castle 0.36 비직교, **castle 오목 0.84**. §2의 범위 제한은 **의식적 축소로서 유효하나 대가가 절반 이상**임을 확인 | ❌ 포괄 못함(기록된 한계) |
| V-q | 합성 잉크 노이즈가 실획 스펙트럼을 재현한다 | V-5c 지시 0 | **재구축으로 성립**(`common/inknoise.py`): 저주파 활 먼저 → 고주파 → 코너 오버슈트 → 폐합오차. 스펙트럼 겹침(저주파 0.035~0.078 vs 실획 0.051~0.076, 고주파 2.5~2.9° vs 2.5~3.6°). 구 모델은 고주파 58~117°로 **실획의 20~40배**. **잔여**: 신 합성 parseable 1.0 vs 실획 0.91~0.95 — 실획 실패는 떨림이 아니라 구조요인(다획·비직교·부가획)에서 옴(실획 precise가 parseable 최저 0.913로 역상관) → 노이즈가 아니라 픽스처 형태 분포 문제 | ⚠️ 스펙트럼 성립, parseable 격차는 형태분포로 이관 |
| V-r | 구 합성이 실제보다 쉬웠다(→Track2 IoU 과대평가) | 지시 0 가설 | **잉크 모델에 대해서는 반증**: 구 합성 selfIoU 0.56~0.77 < 실획 0.85~0.93 → 오히려 **더 어려웠다**. 단 Track 2 과대평가 자체는 사실이며 **원인이 다르다**(코너 노이즈 임의값, V-s) | ❌ 가설 반증, 결론은 다른 경로로 성립 |
| V-s | Track 2 노이즈가 실측에 근거한다 | 트랙2 GRADE_NOISE | **위반**: `{L1:2, L2:8, L3:20}`px는 임의 절대픽셀. 실측 코너오차(precise 1.68%/medium 2.69%/coarse 4.21% of 대각) 대비 **L1 2px=0.48%는 파서 달성한계의 1/3.5로 도달 불가능**. → 표본별 대각 비율로 교체(`stage_perspective/noise.py`), 등급명 실측 등급으로. **2H 결론 뒤집힘**(L자 0.92→0.469) | ❌→수정, 2H 폐기 |
| V-t | Track 2 결과가 유효하다 | 트랙2 전반 | 재측정 완료(지시 0.4): 2A/2C·2F·2I **유지**(값 하향), **2H 폐기**(복합 직교폴리곤은 등급 불문 IoU<0.6). 2I는 노이즈 교체에도 불변 → 실패가 내재적임을 강화 | ⚠️ 부분 유효(2H 제외) |
| V-u | 구 노이즈 모델에 의존한 다른 측정이 유효하다 | 지시 0.5 | 의존 파일 9개 확인. 재측정 3건: 부분획 단조성 위반 0.108→**0.008**, 카메라 fit_error coarse p95 0.105→**0.024**(근사모드 전 등급 0%). **CP-0.1 임계 0.10은 유지되나 근거가 바뀜**(구 "coarse p95≈임계"는 폐기 모델 산물). **미재측정**: score.py 기준선·05·06·make_fixture·make_multi → 재측정 시 §13 멈춤조건2와 충돌하므로 '회귀 아닌 기준선 재수립'으로 별도 처리 필요 | ⏳ 3건 완료, 나머지 보류 |
| V-p | register 등급 판정이 유의미한 양을 잰다 | stage1 detect_grade | **위반 발견·수정**(V-5b): GRADE_CENTROID{0.049,0.197,0.215}는 **전체획** 직진성 적합값인데 detect_grade는 **국소창(12)**으로 비교 → 단위 불일치(같은 실획 0.210 vs 0.012) → **무엇을 넣어도 precise**. stage0/01에 국소창 통계 추가 후 centroid 교체{0.0090,0.0138,0.0164}. 등급은 정보용(tolerance 미선택)이라 파싱 거동 불변 | ⚠️ 수정됨 |
| V-n | 브라우저 pane 비표시 시 rAF·워커메시지 스로틀 | V-4 측정 환경 | **확인**: hidden 탭 rAF 완전정지(600ms 0프레임), 첫 워커메시지 배치지연 ~14ms. → 절대지연은 전경/Playwright(V-8) 필요, compute는 Node 측정으로 우회 | ✅ V-4(측정법 함의) |
