# assumptions.md

계획서(`docs/line_plan.md`)의 **전제**를 항목화한다(CLAUDE.md §5 자기검증 2).
각 단계 완료 시 관측과 대조하고, 어긋나면 **측정을 따르고** 여기에 반증을 적는다.

상태: ✅부합 · ⚠️갱신됨 · ❌위반(판정 함께 수정) · ⏳미검증

> **2026-08-15 L 전환에서 새로 시작했다.** 이전 원장은 `docs/archive/assumptions_SG.md`에 있다.
> S·G 접근의 AS-1~AS-20 중 **배치 계층·프리핸드 보존에 딸린 것은 전부 무효**다
> (앵커 체인, 떨림 평면, 지지 수, 정합성 검사, 미리보기-확정 차이).
> 아래 "이월" 절이 살아남은 것의 **단일 판정처**다 — 아카이브의 개별 행은 폐기 당시 상태로 읽는다.

> **⚠ 계획서의 자동 판정 신호 둘이 반증됐다**(L-A.4, AS-L3·AS-L6). §5.3의 전환 픽셀 차이와
> §6.2의 재투영 어긋남은 **입사 관계에서 유도한 양**이고 **입사는 사영 불변**이므로
> f 오차에 반응하지 않는다. 실측: f를 0.8~1.2배로 틀리게 줘도 둘 다 0.0이다.
> **하나의 오류이고 계획서를 고쳤다.** 판별법은 PITFALLS #5에 있다 —
> **틀린 카메라를 넣어 그 지표가 움직이는지 본다.**

---

## L 접근 신규 전제

| # | 전제 | 출처 | 관측 | 상태 |
|---|---|---|---|---|
| AS-L1 | **선 3개 교점이 소실점 확정 근거로 충분하다** | §5.1 | **아니다.** 그 조건은 **상자의 꼭짓점**이 완벽하게 만족하고, 노이즈가 있으면 참 소실점보다 더 잘 맞는다 — 첫 실행에서 검출된 소실점 셋이 전부 꼭짓점이었다. 조건 셋을 더해야 성립한다: **도달거리**(D-L3) · **지지 가중합 + 재배정**(D-L4) · **국소화 가능성**(D-L7). 그 뒤에도 확정률은 5구도 합산 **0.84~0.85**이고 오배정 **0.110~0.122**다(`vp_detect.json@f351839a`) | ❌ 반증, 조건 셋 추가 |
| AS-L2 | **2D 교점 제약으로 한 번에 풀면 지지 1 문제가 사라진다** | §5.2 | **부분 성립.** ⚠ **아래 0.079/0.995는 폐기된 대역이다**(L-B.1 정정, PITFALLS #27): 0.079는 `lift_gate` 값으로 **검출 오배정이 섞였고**(같은 하네스 무오차 행은 **0.0673**), 0.995는 **최대 연결 성분 분모**다(모집단 기준 **0.885**). 판정("부분 성립")은 유지되지만 **근거 수치를 0.0673/0.885로 읽는다**. 원문: 참 카메라를 주면 조용히 틀린 배치가 **34/430 = 0.079**로 G의 0.278보다 낫고 배치율은 0.887 → 0.995다. 그런데 **§5.2의 "2D 교점은 손실 없는 정보다"는 틀렸다** — 구 AS-20이 이미 반증한 것이 그대로 남아 있다("화면에서 가로지르는 것과 3D에서 만나는 것이 다르다", 조용히 틀림 0.057 → 0.278). 정확한 투영을 넣은 상자 하나에서도 27개 교점 중 **3개가 가림 교차**이고, 그 3개만으로 동차계의 영공간이 1차원이 아니게 되어(최소 고유값 6.95e-3) 참 상자가 복원되지 않았다(형태 오차 0.79). **끝점 근접 조건**을 더해 24/24로 만들었고 그래도 노이즈에서 가짜 교점이 **0.076~0.174** 남는다. 그리고 **초기 확정은 여전히 사후 일괄 해**이므로 "실시간이라 괜찮다"는 논거가 여기엔 걸리지 않는다 | ⚠️ 조건부 성립, 가짜 교점 잔존 |
| AS-L3 | **전환이 무변화로 보인다**(카메라가 맞으면 화면이 안 튄다) | §5.3 | **성립하지만 그것은 측정이 아니라 설계 보장이다**(selfcheck 자기참조 유형 3, PITFALLS #5). 각 점이 자기 광선 위에 있으므로 되쏘면 정확히 원본으로 돌아온다 — `segGap` 최대 **0.0**. 결정적으로 **f를 0.8·0.9·1.1·1.2배로 틀리게 줘도 0.0이다**(사전 등록한 반례 검사). 그러므로 §5.3의 "튀면 카메라가 틀린 것"은 **성립하지 않는다.** 대체로 제안한 교점 잔차도 같은 구간에서 0.0285→0.0262로 **안 움직인다** — 입사 관계는 사영 불변이므로 **원리적으로** 카메라를 못 검증한다. 사용자에게 실제로 보이는 변화는 **획이 직선으로 펴지는 것**이고(`chord_dev`, 옛 `inkGap`) 그것은 프리핸드를 미룬 대가다(§1.1). **⚠ L-B.1이 그 값도 측정이 아님을 확인했다** — f를 ±20% 틀리게 줘도 **소수 넷째 자리까지 그대로**다(`stage_cam.json@f351839a`의 `control_wrong_camera`, 5구도 전부). 값을 정하는 것은 **잉크 모델의 활**과 **획 길이**뿐이고, 분포는 잉크 등급을 따라 커진다 — 중앙 precise **0.52** · medium **0.78** · coarse **1.05**px(등록 시드 6 × 잡음 4). ⚠ **"정비례"는 아니다**: `lf_bow` 비 1:1.77:2.97에 대해 중앙 비는 1:1.49:2.00이고, **등급마다 놓인 획 수가 달라 구도 구성이 다르다**(#11). **실획 현실을 주장하지 않는다**(AS-C10) ⚠ **[5차 항목 1 병기 — 재검 [1-5]]** 이 보장은 **좌표 재투영에 한한다** — 화면 무변화는 뷰 소속 배선이 따로 정하고, 그 배선이 실제로 깨져 있었다(확정 전 획이 가짜 뷰로 들어가 화면에서 통째로 사라짐 — 5차 항목 1, D-L64가 수리). 보장과 배선을 같은 말로 부르지 않는다 | ❌ 지표 폐기, 보장으로 재분류. **`chord_dev`도 잉크 모델의 성질이다** |
| AS-L4 ⚠**하네스 동작점(0.05)의 값**(2026-08-17 D-L56 — 앱 조리개는 15px로 줄었고 그 동작점의 맞음/미스는 실획 전까지 미측정) | **시작점 스냅이 손 획에서 충분히 붙는다** | §3 | **L-B.3이 쟀다**(`snap.json@f351839a`). **붙기는 붙는다** — 반경 0.05에서 `end_jitter > 0`인 결론 집합의 맞음이 **2826/3240**(0.87)이고 미스가 56/3240이다. **그러나 "충분히"는 대상 밀도가 정한다**: 격자 k3(획 144 · 참 꼭짓점 간격 중앙 21.3px)에서는 **틀린 붙음이 반경과 무관하게 15076/38880에서 포화한다** — 겨냥 오차 중앙 12.19px(**같은 밀도에서 잰 값**)이 간격의 절반(10.65px)을 넘기 때문이다. **한계를 정하는 것은 반경이 아니라 (겨냥 오차 / 대상 간격)의 비**이고 **스냅으로 못 고치는 몫이 있다.** 배치까지 가면 반경 0.05에서 **2503/3240**이 놓이고 **놓인 것의 19%(474/2503)가 조용히 틀린다**(cut 0.2, `end_jitter > 0`). ⚠ 대상을 **참 3D**로 준 상한이고, 잉크는 합성이다(AS-C1) | ⚠️ **붙는다 — 다만 밀도가 상한을 정한다** |
| AS-L8 | **한 시점의 손 획 상자 하나로 카메라가 충분히 정해진다** | L-A.4가 발견 | **아니다 — 이것이 게이트 실패의 원인이다.** 참 카메라를 주면 조용히 틀린 배치 0.079(⚠ `lift_gate` 값. **같은 하네스 무오차 행은 0.0673**이고 대역은 그쪽이다 — PITFALLS #27), 검출 카메라로는 **0.641**이다. 지배항은 f가 아니라 **축 방향**이다: f만 10% 틀리게 줘도 형태 오차 중앙은 0.098 → 0.115뿐인데, 검출 축 방향은 참 축에서 중앙 **4.18°** 벗어나 있다. `axisDirection = unit([vp − P, f])`이므로 먼 소실점의 100px 오차가 곧 2~3°다 | ❌ **게이트 실패의 원인** |
| AS-L9 | **사용자가 소실점을 눈으로 맞출 수 있다** — 그리고 그 정확도가 참 카메라 대역에 든다 | §5.2 (L-A.5, 개정 2 번호) | **임계는 실측 축 오차 ≈0.5°다**(리뷰어 지적으로 ≈1°에서 정정). 같은 하네스의 무오차 행(조용히 틀림 **0.0673** · 형태 오차 중앙 **0.0296**)을 대역으로 쓰면 실측 0.48°에서 1.03배/2.07배, 0.96°에서 1.24배/3.67배, 1.92°에서 7.5배/6.9배다. **§5.5가 등록한 지표는 형태 오차 0.026**이고 그 기준으로는 0.48°에서 이미 2배다(`camera_gate.json@f351839a`). **핸들 예산으로 환산하면**(각차 3~20°, `vp_homog.json@f351839a`) 가이드 140px에서 **0.29~1.01px(서브픽셀)**, 300px에서 0.63~2.16, **600px에서 1.28~4.33**, 1250px에서 2.68~9.03이다. L-B.1의 단일 뷰포트가 내는 가이드는 **607.6 / 618.5 / 1233.3 / 1254.6 / 1256.8 / 1327.7px**인데(`stage_browser.json`), **가장 짧은 것이 정확도를 정하므로** 옛 좌우 분할(304~358px)과 견주면 **약 2배**다 — 가장 긴 것으로 읽으면 4배가 나온다. ⚠ `min_guide_ratio` 0.45가 요구하는 651px에 **여섯 중 둘이 미달**이라 잘림이 없어진 것은 아니다. ⚠ **핸들 하나만 움직인다는 가정** 위의 값이다 — 사람은 핸들 12개를 만진다. **⚠ L-B.2가 검출 초안으로 실제로 쟀다**(`guide_budget.json@509615a1`): 늘린 뒤 구도별 최소 예산이 3pt_yaw20_pitch25 **3.32** · 3pt_yaw35_pitch15 **0.69** · 3pt_yaw50_pitch8 **0.73** · 2pt **0.23** · **1pt 0.12**px다. **1점 구도는 세 시드 전부가 `unusable_px`(1px) 미만**이고 2pt·3pt에도 서브픽셀 셀이 있다(31 중 9). 참 소실점 가이드의 1.3~4.3px은 **낙관값**이었다. ⚠ **대리값이다**(PITFALLS #2): **"사람이 그 안에 놓는다"는 여전히 표본 0이다** | ⚠️ 임계 0.5°, **검출 초안 실측 0.12~6.8px(1점 구도는 서브픽셀)**, 도달 여부 미측정 |
| AS-L10 | **획이 쌓이면 축 방향 오차가 임계 아래로 내려간다** | §5.5 (L-A.6) | **반증 — 단, 조건이 붙는다: "내부 격자선을 늘린 경우".** 축당 4 → 18 → 48 → 100획에서 검출 축 오차 중앙 **7.4 / 10 / 10 / 4.5°**(n=500 / 501 / 93 / **18**, 시드 6 / 6 / 3 / **1**)로 **임계(≈0.5°)에서 한 자릿수 멀다**. _비단조 주장은 뺀다 — 뒤의 두 점은 표본이 1/5~1/28이다(리뷰어 지적)._ ⚠ **늘린 획이 `1/k` 길이의 내부 격자선이라 소실점까지의 지렛대가 오히려 짧아진다** — 지렛대가 같거나 긴 획을 늘리는 경로(상자 여러 개·긴 모서리)는 **막히지 않았다**. 배치율 급락(0.569 → 0.128)의 큰 몫은 `min_len_ratio`가 아니라 **카메라 실패**다(`camera_ok` 258/300 → 6/18). 오라클 조용히 틀림도 k와 함께 나빠진다(0.376 → 0.478 / 0.413 / 0.406). **→ 자동 확정 경로를 두지 않는다**(이 결론은 조건과 무관하다) | ❌ 반증(조건부) |
| AS-L11 | **미배치는 실패가 아니라 대기 상태다 — 조건이 갖춰지면 승격하고, 승격은 연쇄한다**(개정 2 §9.1) | §9.1 (L-B.7·L-B.M) | **개수 회수에 대해서만 성립하고, 품질에 대해서는 반증됐다**(`promote.json@f351839a`, **참 카메라**). ① **연쇄는 일어나고 이 픽스처에서 수렴한다**(⚠ **3점 구도의 현상이다** — 구도별 추가 59·68·75·16·**0**) — 1회차 720/1904 뒤 2회차 **192** · 3회차 **25** · 4회차 **1**로 추가 **218**(연쇄 포함 938/1904 = 0.493), 상한 8회 도달 **0/338**. ⚠ **장면당 획이 12개**라 연쇄 깊이 4는 **픽스처가 정하는 상한에 가깝다** — 상자 둘 이상은 안 쟀다. ② **승격이 더 내는 오차는 형태가 아니라 배율이다.** 같은 720획이 고정 게이지로 **392/720(0.544)**, **자기 게이지로 286/720(0.397)**이고 확정은 **874/2416(0.362)**이다. 자기 게이지 배수는 **시드별 0.865~1.150으로 1을 가로지르므로 그 격차는 결론이 아니고**(#14), 고정 게이지 배수는 시드 여섯 전부 1.20~1.57이다. 앱이 배율을 다시 안 맞추므로 **그 배율 어긋남이 사용자가 보는 오차다.** ⚠ 꼬리는 자기 게이지에서도 나쁘다(p90 ≈2배). ③ **회수율도 품질도 한 값이 아니다** — 잡음으로 0.71 → 0.21(연쇄 포함 0.75 → 0.30), **구도로는 조용히 틀림이 3pt 52/163(0.32)에서 1점 구도 90/93(0.968)까지 간다**(같은 층의 확정은 82/412과 268/422이다). **1점 구도는 연쇄도 0이다.** **1점 구도는 회수한 것의 거의 전부가 조용히 틀린다.** 그리고 37.8%는 `axis_deg` 12 · 반경 0.05라는 **동작점 하나의 통과율**이다(스윕 0.25~0.49, 개수와 틀림이 단조로 맞바뀐다). ⚠ **참 카메라 조건이다.** 앱의 `promoteChain`은 초안·조정 카메라 위에서 돈다. ⚠ 앵커 팔의 거부 사유가 `no_snap`·`angle_over` 둘뿐이라 **'축이 정해지면 반드시 놓인다'가 구성상 항등**이다. [6차 항목 2 대조 — 재검 [B-8]] 이 행의 1점 반증은 **검출·승격 합성 층**(참 카메라 대비 조용히 틀림)의 것이고, 6차의 직접 좌표 경로는 **확정 후 배치**라 대상이 다르다 — 직접 경로가 이 반증을 해소하지도 악화하지도 않는다(같은 f 배율 위 — AS-C4 주석). 실획(K)의 path_use·조용히 틀림이 판정자다 | ⚠️ **개수는 성립·연쇄는 수렴(픽스처 한정). 품질은 반증. 1점 구도에서 거의 전부 틀린다** |
| AS-L12 | **롤 0을 고정하면(지평선 수평) 미지수가 하나 줄어 자동 검출이 임계 아래로 간다** | L-D.0 (사람 지시, 2026-08-16) | **반증.** 이론은 맞다 — 이론서 3.1 + 롤 0이면 지평선이 화면 수평이고 §5.3의 자세 자유도가 3 → 2다. 수심 조건(6.3)으로 세 번째 소실점이 점 하나로 정해지며 **참 카메라에서 오차 0으로 성립한다**(항등이므로 정보량 0, #5). **그러나 검출 입력에서는 안 된다**: 축 방향 오차 중앙 7.4048 → **6.1460**(축당 4획)으로 17% 줄 뿐이고 임계 ≈1°의 **6배**, 오라클 축 라벨로도 **4.4599°**다. **그리고 배치가 붕괴한다**(2049 → 792 / 6035 → 522 / 2045 → 9 / 692 → **0**). 사영이 아니라 **제약 적합**으로 넣어도 안 살아난다(848 / 730 / 3 / 0). ⚠⚠ **다만 붕괴가 제약의 성질이라는 초판 서술은 철회했다** — **위약 대조군**(같은 크기 **임의 방향** 이동)이 똑같이 붕괴한다(832 / 802 / 14 / 0). 붕괴는 **검출 해를 그 지지선에서 떼어 놓는 것 자체**의 대가이고 방향과 무관하다. 검출 지평선의 실제 어긋남은 Δy **24~53px**이다. **AS-L10의 반증을 뒤집지 못한다.** ⚠ 양성 채널: 손익분기는 롤 **5°** 근처이고 10°에서는 해를 끼친다(`horizon.json@509615a1`) | ❌ 반증 — **AS-L10을 못 되살린다** |
| AS-L5 | **실시간 색이 오배정을 줄인다** | §4 | 사용자 행동에 달렸다 — **직접 측정 불가**, 간접 지표만 | ⏳ 간접 |
| AS-L6 | **승격 시 재투영 어긋남이 준다** | §6.2 | **반증. 계획서 §6.2를 폐기했다.** AS-L3과 같은 하나의 오류다 — 재투영 어긋남은 카메라와 무관하게 0이므로(각 점이 자기 광선 위) 승격 전후에 **둘 다 0**이다. L-C가 그 신호를 쓰려면 다른 양을 찾아야 한다 — 후보는 획별 `tiltDeg`(축 방향이 해석 평면을 벗어난 각. 끝점 오차 0.03에서 중앙 2.06°, 0.05에서 3.49°로 실제로 움직인다)와 소실점 삼각형의 f² 편차(6.3의 자유도 0 조건)다. **L-C.3이 후보 둘을 시험했고 둘 다 안 섰다**(`order_signal.json@f351839a`): **f² 편차는 자기참조 유형 3**이다 — 주점을 수심으로 잡는 순간 세 f²가 같아지는 것이 항등식이라 **틀린 소실점·둔각 삼각형·가짜 승격 아홉 전부에서 편차가 2.6e-16 이하**다(정보량 0). **`tiltDeg`는 사영 불변은 아니지만**(f×0.8/1.0/1.2에서 **장면 중앙**이 1.414/1.316/1.228 — ⚠ 이 셀 앞쪽의 2.06°/3.49°는 **획별·잡음별** 값이라 **단위가 다르다**) **오승격을 못 가른다.** 근거는 기준과 무관한 것부터 셋이다: **① 구도 교락이 없는 `wrong_vp3` 팔에서 구도별 판별력이 0.015·0.074·0** · **② 시드 부호가 갈린다**(여섯 중 둘) · **③ 합산 0.1979는 구도 간 비교다**(가짜 팔이 2pt 한 구도뿐이라 3pt 층에서는 못 잰다. 시드 3을 빼면 0.1393). ⚠⚠ **그것은 신호의 실패이지 픽스처의 실패가 아니다** — **참값을 고정 게이지로 재면 판별력이 가짜 승격 0.9694 · 틀린 VP3 0.5446으로 둘 다 등록 기준 0.5를 넘는다.** 자기 게이지 값(0.4979 · 0.1265)은 **최소제곱이 전역 배율을 없앤 뒤의 수**이고 **AS-L11이 '사용자가 보는 오차'라 적은 배율 성분을 지운 것**이다(가짜 승격이 0.2537 → 3.1196으로 12배 벌어진다). 틀린 VP3(+400,+900px)의 **축 각오차는 중앙 5.59°로 AS-L9 임계의 11배**다. 구도 효과도 갈랐다 — 같은 2pt 장면을 옳게 2점으로 푼 대조군이 0.1435다. **그러므로 잴 것은 많고 관측 가능한 신호를 아직 못 찾은 것이다.** `tiltDeg`가 **f 오차와 축 오차를 섞는다.** → **사용자 확인으로 갔다**(§6.2의 두 갈래 중 하나). 그 되돌리기가 **L-C.2**다 | ❌ 반증, §6.2 폐기. **대체 후보 둘도 반증**(L-C.3) |
| AS-L7 | **초기 획이 교점을 충분히 만든다** | §10 | **교점 수는 충분하다** — 상자 하나에서 24개(꼭짓점 8 × 짝 3)이고 미지수는 12개라 영공간이 1차원이다(최소 고유값 −3.6e-17). 부족한 것은 **개수가 아니라 정밀도**다: 축당 4획으로는 소실점 지렛대가 짧아 3D 축 방향 오차 중앙 4.18°가 남고 그것이 형태 오차 0.271을 만든다. **획이 더 쌓이면 나아지는지는 미측정** — L-A.4가 남긴 첫 후속 측정이다 | ⚠️ 개수는 충분, 정밀도가 부족 |

## 이월 (S·G에서 살아남은 것)

| # | 전제 | 출처 | 관측 | 상태 |
|---|---|---|---|---|
| AS-C1 | **실획 표본이 대상 사용자를 대표한다** | 구 AS-13 / V-A | **의심.** 손에 있는 것은 Quick,Draw(마우스·손가락 낙서)뿐이고 대상은 스타일러스·투시도 숙련자다. **실획 표본은 여전히 0이다.** → 이 수치로 설계 결정을 하지 않는다 ⚠ **[web2-13 3-b 사용 기록]** STRAY_MIN_PX가 Quick,Draw 대역을 근거로 골라졌다(`stray_gate_web2.json`) — **이 행의 유보 아래의 사용**이다: 실측이 판별한 것은 「후보들이 폐기 수를 거의 안 가른다」까지이고 값 선택은 관용(탭 문 3배), 최종 판정은 실기기 + 진단 패널 카운터가 진다(AS-C45) ⚠ **[7차 대조]** 첫 실획 표본(.brnl)이 **도착했으나 저장소 밖이다** — `sessions/`에 파일이 없고(작업 트리·전 브랜치·이슈 확인) 지시문의 **사람 보고 수치**만 있다(지평선 y=157 · 소실점 방향 Δ0.0~1.7°(화면 2D 현↔소실점 각 — ⚠ 합성의 축 오차 31.6°(rule_camera deg_median)는 **3D 축 방향 오차라 다른 양·다른 프레임**이다. 비교 불가 — 3·4-R [1]) · 획 5 · 끝점 스냅 0). 원장 표본 수는 여전히 0이고 real_ink는 awaiting — **파일이 오면 재현·재측정한다.** 사람 보고 중 "겨냥이 정확하다"는 관찰은 7차 결함 수정(캐시·측정)의 착수 근거로만 썼다 | ⚠️ 설계 결정 보류(원장 표본 0 — 보고만 도착) |
| AS-C2 ⚠**하네스 동작점(0.05)의 서술**(D-L56 — 앱 조리개는 15px) | **끝점 겨냥 오차**가 도구가 성립하는 범위에 있다 | 구 AS-12 | **영향이 크게 줄었다.** L 접근은 시작점을 **스냅으로 확정**하므로 겨냥 오차가 배치의 오차원이 아니다 — 남은 의존은 **스냅 반경 하나**다. Quick,Draw `closure_gap` 환산 0.028~0.051이 반경 결정의 유일한 자릿수. **⚠ L-B.3이 그 축소를 뒤집었다**: 반경을 정하는 것은 겨냥 오차 **단독이 아니라 (겨냥 오차 / 대상 간격)의 비**이고, 그 비가 1/2를 넘으면 **어떤 반경으로도 못 고친다**(`snap.json@f351839a`의 `by_density`). 합성 잉크의 겨냥 오차 중앙은 `endJitter` 0.01/0.03/0.05에서 **4.7 / 14.7 / 23.8px**이고 등급은 거의 안 바꾼다 — **활이 양끝에서 0이라 `lf_bow`가 시작점에 안 닿고**, 닿는 유일한 성분인 `hf_sigma`가 등급 간 8%밖에 안 다르기 때문이다(**모델의 성질이다**, #2·#3). 채택 반경 **0.05**(앱 1280×675에서 72px)는 선례(10~15px)의 5~7배이고 근거는 `unintended_snap`이다 — 0.10으로 키우면 맞음이 +27인데 **의도하지 않은 붙음이 +918**이다 | ⚠️ **비(比)의 문제로 재정의됨. 실획 자릿수는 여전히 `closure_gap` 하나** |
| AS-C3 | **한 획 = 한 축** | 구 AS-6 | 실획 위반율 0.699(n=1816, Quick,Draw 낙서 — 상한으로 읽는다). L에서는 획이 **직선 세그먼트**가 되므로 위반하면 방향 하나로 접힌다. 다만 **실시간 미리보기가 그 자리에서 보이므로** 사용자가 끊어 긋는다(AS-L5와 같은 논리). **⚠ L-B.4가 쟀다 — 접히지 않는다.** 인접한 두 모서리를 한 획으로 이은 대조군에서 **92.4~98.2%가 거부되어 2D로 대기**한다(`axis_live.json`의 `control_true_corner_rejected`). 지배 규칙은 **`multi_axis` 1991/2054**다. 즉 위반 획은 **접히는 것이 아니라 미배치(대기)로 간다** — A-3의 "애매하면 놓지 않는다"가 그대로 작동한다. ⚠ 대조군의 동적 범위가 5.8pp로 천장에 붙어 있어 **완만한 코너는 안 쟀다**(DEFERRED) | ⚠️ **접히지 않고 대기로 간다**(직각 코너 한정) |
| AS-C4 | **깊이 좌표의 출처를 구분해야 한다** | 구 AS-9 | **성립.** f를 10~20% 틀리게 주면 모서리 오차가 0.027 → 0.141(5.2배)이고 연쇄를 타지 않는 **전역 배율 편향**이다(이론서 16.4). 1점 f는 설정값, 2점 주점은 가정 → `Camera.fSource`가 출처를 든다(⚠ "화면에 나온다"는 D-L55(5차 지시 3)로 뒤집혔다 — 자료구조·S2S 진단에만 남는다. 6차 재검 [B-8]이 낡음을 잡음). [6차 항목 2 대조] **1점 직접 좌표 경로도 같은 f 배율 위다** — 직접·lift가 같은 f를 쓰므로 동등성에는 안 닿지만 입체의 깊이:폭 비가 그 상수에 달렸다(`one_point_direct.json@f351839a` 원장 명시) | ✅ 유지 |
| AS-C5 | **2점 투시에서 주점 = 이미지 중심** | 구 W-y / 이론서 16.2 | **미검증.** 자유도 1을 이 가정으로 소진하므로 틀리면 f가 틀린다. L도 같은 경로를 쓴다. ⛔ **제자리 정정(2026-08-19 14차 항목 2 · D-L95)**: 가정의 사거리가 **x 하나로 줄었다** — y는 지평선이 주는 값이라(피치 0 강제, 이론서 3.1) 가정이 아니고, 앱 카메라·real_ink·fovGate가 전부 [W/2, 지평선 y] 한 규약이다. 남는 미검증은 **x = W/2**뿐이다(16.2 — 두 소실점만으로는 방정식 하나에 미지수 둘(x·f)이라 x가 결정 불가). ⛔ **제자리 정정(2026-08-21 · web2-03 지시 1)**: 「앱 카메라가 [W/2, 지평선 y] 한 규약」이 **더 이상 참이 아니다** — 배포되는 앱은 `web2`이고 그 **1점 주점은 [깊이 소실점 x, 지평선 y]**다. 1점에서는 f²=|PV₁||PV₂|를 안 쓰므로 주점이 자유롭고, 그 자유를 **세 축의 직교**에 쓴다(이론서 5.2·5.3: 1점의 소실점 = 주점). **가정이 남는 곳은 2점뿐**이고 이 행의 제목 그대로다. ⛔ **제자리 정정(2026-08-27 · web2-17 1-a)**: 「y는 지평선이 주는 값이라 가정이 아니다」가 **더 이상 참이 아니다** — 지평선이 상시(프레임 상수 H/2)가 되면서 y도 **가정**이 됐다(사람이 긋던 자유도가 «팬으로 종이를 옮기는» 쪽으로 넘어갔다 — 이론서 5.3 자유도 회계에서 카메라 몫 하나가 줄고 문서 배치 몫이 그만큼 늘었다). 이제 이 행은 **x = W/2(2점) · y = H/2(전 차수)** 둘 다 프레임 상수 가정이다. 0-b의 «화면 수평 획 = 무조건 1점, 애매함 없음»도 부분 반증됐다 — **지평선 위의 수평 획은 애매함이 되살아난다**(모든 수평 소실점이 그 선 위에 있다 — web2-17이 퇴화로 처리, entry17_web2.json band_boundary) | ⏳ **2점의 x·전 차수의 y 미측정**(1점 x는 가정이 아니라 유도) |
| AS-C6 | **지면 법선은 수직 소실점 방향이다** | 구 AS-10 | 카메라 좌표계 `y = const`는 **카메라가 수평일 때만** 맞다. `geom3d.groundFrame`이 단일 출처 | ❌ 위반 후 고침 |
| AS-C7 | **캔버스 크기가 생성 시점에 잡혀 있다** | 구 AS-11 | **틀렸다. 세 번 걸렸다.** ① `ResizeObserver`가 초기 콜백을 안 준다(S-4) → `requestAnimationFrame`을 함께 걸었다. ② **rAF는 숨은 탭에서 완전히 멈춘다**(구 V-n: 600ms에 0프레임) — 브라우저 창이 뒤에서 열리면 둘 다 발화하지 않고 `acc.imgSize`가 `[1,24]`로 굳는다. 그 크기가 무한원 판정의 기준이라 **소실점이 하나로 읽혔다**(L-A.5 브라우저 확인). ③ 대응: 관찰자를 더 걸지 않고 **쓰는 자리에서 자가 치유**한다 — `refresh()`가 `sizeStale()`을 보고 고친다. `setTimeout(0)`·`visibilitychange`도 함께. ⚠ **자가 치유는 재발을 관측 불가로 만든다**(PITFALLS #7·#19) → 발동 횟수를 진단에 낸다. **L-B.1 관측**: 단일 뷰포트에서 `SIZE_HEAL.count = 3`(창 1280 → 820 변경 포함, `stage_browser.json`). 창을 줄이면 `setViewOffset`의 `offsetX`가 124.7 → 0으로 **다시 계산된다** — 투영이 굳지 않는다. ⚠ **web2-14 4번의 스냅샷 겹 결함은 이 계열이 아니다** — backing이 굳은 것이 아니라 **CSS 표시 크기의 부재**(canvas는 대체 요소라 `inset:0`이 못 늘린다 — 명세 동작)였고, 답도 관찰자·치유가 아니라 **명시**(fitSnap이 style 크기를 준다)다. 같은 «캔버스 크기 규약» 가족의 다른 갈래로 여기 기록한다(snap_ghost_web2*.json). ⚙️ **네 번째 사례(2026-08-27 web2-17)**: 뷰 썸네일 팝업이 **이미지 디코드 전의 offsetHeight**로 top을 굳혀 20뷰에서 화면 밖으로 넘쳤다(top 112 + 로드 후 792 = 904 > 800 — data URL <img>도 비동기 디코드다). 대응은 같은 형태의 자가 치유(load마다 재클램프 — main.ts openViewsPop)이고, «재발 관측 불가» 단서는 e2e views.spec의 최종 상태 bbox 팔이 진다(발동 횟수 계수는 안 달았다 — 재클램프가 수렴 계산이라 실패가 위치로 남고 그 위치를 팔이 직접 잰다) | ❌ 위반, **네 번 고침** |
| AS-C8 | **곡선 획이 소수다** | 구 AS-7 | **아니다.** ⚠ **두 수를 한 문장에 놓지 않는다** — `bend_share.json`의 caveat이 금지한다: 곡선 **26.5%**는 `straight_tol` 0.10 기준의 **다른 지표**이고, `bend > 0.18`인 비율 **0.6025**는 또 다른 집단이다(꺾인 획 88% · 매끄러운 획 1.2%). 그리고 **`bend_max` 0.18 자체가 측정이 정당화하지 못한 잠정값**이다(같은 원장의 `threshold_note`). **L에서는 지금 범위 밖이다**(프리핸드를 미뤘다). 획이 직선 세그먼트가 되므로 굽은 획은 그 현(弦)으로 접힌다. **표현 계층 복귀 시 되살아난다** | ⏳ 미룸(§1.1) |
| AS-C9 | **측정이 재현 가능하다** | 구 V-H | `hash(str)`·`Math.random` 금지, `stable_seed`/`rng32`. **시드 변동폭 때문에 유효 자릿수는 2자리**이고 비율보다 분자/분모로 적는다 | ✅ 규칙 |
| AS-C10 | **합성이 실제를 대표한다** | 구 V-o | **반증됐다**(양방향으로 틀렸다 — 고주파 과다, 저주파 결여). 합성 수치로 실획 현실을 주장하지 않는다 | ❌ 규칙으로 굳음 |
| **AS-C11** | **접기 지연 1200 ms가 손에 맞는다**(놓고 그만큼 지나면 정렬로 접힌다 — 2026-08-21 web2-04) | ⚙️ web2 | ⚠ **미검증. 마우스로 골랐다.** 참조점 둘도 **이 저장소의 측정이 아니라 관용값**이다: 하한은 「운영체제 더블클릭 경계 500 ms」(의도적인 다시 잡기는 그보다 느리다 → 1200은 2.4배), 상한은 「반응이 없다로 읽히는 대역 약 2 s」. **실기기(펜·손가락)의 다시 잡기 간격 표본이 0이다**(AS-C1과 같은 자리). 붙잡고 있는 동안은 무한히 안 접히므로 살펴보기 «자체»는 안 막히고, 막히는 것은 «놓고 보기»뿐이다. ⛔ **그 되돌릴 조건이 2026-08-25 web2-08 지시 3으로 발화했다** — 사람 문면 「내려다보거나 올려다보기도 해야 하는데 무조건 2점 투시 시점을 찾아가니 불편하다」가 정확히 「기울인 채로 놓고 살펴보는 것이 막힌다」는 관측이다. 대응은 지연을 올리는 것이 아니라 **접기 자체에 임계를 둔 것**(AS-C21) — 임계 밖 자세는 이제 영영 안 접히므로 이 값의 적용 범위가 «임계 안 자세»로 좁아졌다. 그 안에서는 여전히 미검증이다. **되돌릴 조건(남은 몫)**: 임계 안에서 놓고 보다가 접혀 방해된다는 관측이 나오면 올린다 | ⏳ 실기기 미측정 · **web2-08에 범위 축소** |
| **AS-C12** | **접기 애니메이션 300 ms가 「갑자기 바뀌지 않는다」를 만든다** (2026-08-21 web2-04) | ⚙️ web2 | ⚠ **미검증. 관용값이다** — 「시점 전환의 관용 대역 250~400 ms」는 이 저장소의 측정이 아니다. 근거로 든 것은 그 대역 안이라는 것과 **지연(1200)의 ¼이라 「기다렸다」와 「접힌다」가 화면에서 안 섞인다**는 것 둘이고, 둘 다 관측이 아니다. 스윕이 없다(#12). ⚠ **이 세션은 브라우저 창이 숨겨져 애니메이션을 눈으로 못 봤다**(#22 — rAF 정지). 팔이 재는 것은 「여러 프레임에 걸치고 단조로 줄어든다」이지 「놀라지 않는다」가 아니다. **되돌릴 조건**: 접히는 것이 급하다·느리다는 관측이 나오면 그때 움직인다 | ⏳ 실기기 미측정 · 픽셀 미확인 |
| **AS-C13** | **소실점 오스냅 표식의 반지름 7 px가 ✕와 겹쳐도 읽힌다** (2026-08-21 web2-05) | ⚙️ web2 | ⚠ **미검증. 좌표와 픽셀 수만 쟀다.** 실측(dpr 1, 30×30 상자): 커서를 올리면 **80 → 182(+102)**, 수리 전에는 80 → 80(차 0)이었다. 그러나 «픽셀이 늘었다»가 «사람이 그것을 붙은 신호로 읽는다»는 아니다 — ✕(반지름 6의 대각선)가 이미 그 자리에 있고 원이 그 둘레를 두른다. 근처점 표식(반지름 4의 원)과는 크기·색으로 갈린다고 **판단**했고 실기기·눈으로 안 봤다(#22). **되돌릴 조건**: ✕와 원이 뭉쳐 보이거나 근처점과 헷갈린다는 관측이 나오면 형태를 바꾼다(같은 자리에 다른 도형을 쓴다). ⛔ **「크기·색으로 갈린다」의 «색» 몫이 web2-08 지시 2로 사라졌다** — 표식이 무채색(**현재 2H `#9c9c9c`·알파 0.50** — web2-10 지시 6. ⛔ «#555»는 web2-08~10 사이의 값이고 #555는 이제 **축 안내**의 색이다)이 되어 근처점(같은 색 원)과는 이제 **크기(7 vs 4)로만** 갈린다. 판단의 나머지 절반이 없어졌으므로 이 행의 되돌릴 조건이 그만큼 가까워졌다(관측이 오면 형태를 가른다) | ⏳ 실기기·눈 미확인 · **web2-08에 색 단서 소멸** |
| **AS-C14** | **정렬을 떠나기 직전의 포즈가 곧 «사용자가 정한 카메라 상태»다** (2026-08-21 web2-05) | ⚙️ web2 | ⛔ **web2-04 판이 반증됐다** — 그때는 「접기가 **눈높이를 유지한다**」였고, 그 눈높이가 **궤도가 만든 부수 효과**라는 것을 사람이 지시로 뒤집었다(실측 1.600 → 6.590 → 접은 뒤 6.590). ⚠ **전제 열을 한 번 고쳤다** — 초판은 「접기가 되돌릴 것은 «정렬을 떠나기 직전의 포즈»다」였는데
그것은 **설계 규칙(당위)이라 관측으로 뒤집힐 수 없다**(2차 리뷰어 [8]). 반증 가능한 명제는
지금 제목의 것이고, 규칙 자체는 `NOTES`의 web2-05 절이 자리다. 그 근거는 **궤도 중에 눈높이를 바꿀 길이 없다**는 것(지시 d)이다. ⚠ **그 근거가 깨지는 길**: 궤도 중에 팬·줌이 돈다(`input.ts`는 궤도 포즈에서 팬·줌을 받는다) — 그러면 «떠나기 직전»과 «지금 사용자가 원하는 것»이 갈린다. ~~지금은 **궤도 중 팬·줌도 접기가 되돌린다**~~ ⛔ **줌은 이제 지킨다**(아래). **되돌릴 조건**: 「돌려보다 줌해서 맞춘 거리가 접으면 사라진다」는 관측이 나오면, 궤도 중의 팬·줌만 앵커에 반영하는 길을 연다 ⛔ **2026-08-21 web2-06에 그 관측이 왔다**(지시 5: 「궤도 반경을 조절할 수 있어야 한다」). **줌 쪽만 열었다** — 궤도가 반경을 구성상 보존하므로 반경 변화는 «떠나기 직전»에 없던 사용자의 값이라는 **근거가 있고**, 팬에는 그 근거가 없다(NOTES web2-06 지시 5 · `DEFERRED.md`). 열되 **앵커를 안 건드렸다**: 접기가 앵커를 «지금 반경»으로 맞춘다 | ⏳ 팬은 여전히 미검증 |
| **AS-C15** | **획의 처짐이 `PARALLEL_PX`(1.5px)를 넘으면 사람이 소실점을 겨눈 것이다** (2026-08-21 web2-03) | ⚙️ web2 | ⛔ **반증됐다**(2026-08-21 web2-06 지시 2). 그 값은 **포인터 잡음** 대역에서 골랐는데, 실제로 그 대역을 정하는 것은 **손의 겨냥 오차**였다 — 지평선을 긋고 수평선을 그으면 처짐 2px에서 이미 **33W 소실점**이 섰다(그 획은 작도 획이라 실행취소도 안 된다). web2-03이 그 되돌릴 조건을 미리 적어 뒀다(「실기기에서 수평 의도가 소실점으로 읽히면」). 고친 방식은 값을 올리는 것이 **아니라** 물음의 나머지 절반을 더한 것이다 — `VP_FAR_W` | ✅ 반증 |
| **AS-C16** | **사람이 «그어서» 만드는 소실점은 시작점에서 6W 안에 있다**(`VP_FAR_W`) (2026-08-21 web2-06) | ⚙️ web2 | ⚠ **미검증이고, 근거가 한 번 깎였다**(web2-06 1차 리뷰어 [2]): 「사람이 든 대역의 밖 첫 눈금」은 **틀렸다** — 지시 원문의 「완만하면 5000px을 **넘는다**」에서 4.2W는 하한이다. 6W는 그 대역 **안을 자르는 값**이고, 남는 근거는 **대비표**(4·6·8·10을 갈아 끼운 실측)와 **두 기준의 출처**다: 하한 「1~5W는 산다」는 **앞 회차(web2-03)의 게이트**이지 사람 문면이 아니고(문면에는 상한이 없다), **상한 「6 vs 8」에는 측정이 없다** — 손 겨냥 오차를 잰 적이 없다(AS-C1: 실획 표본 0). ⛔ 초판이 든 「K=8은 흡수를 4px 아래로 떨어뜨린다」는 **철회한다**(표에서 K=8은 정확히 4.0px이고 4px의 출처가 없다 · 2차 리뷰어 [N2]). 6은 **보고된 증상 쪽에 여유를 둔 선택**이다. 각도(7.1°~9.9°)는 **f가 기본값일 때**의 참고이고, 이 갈래는 소실점 0~1개일 때만 돌아 그때 f가 언제나 기본값이다. **대가를 안다**: 6W 밖은 그어서 못 만들고 지평선 위를 찍어야 한다(NOTES의 대비표). 되돌릴 조건: 6W 밖 소실점을 그어서 만들려는 사용이 관측되면 올린다 | ⏳ 미검증 |
| **AS-C17** | **돌려볼 대상은 «펜으로 딴 선»이다** — 궤도 중심이 그 경계 상자다 (2026-08-21 web2-06 지시 4) | ⚙️ web2 | ⚠ **사람이 준 전제다**(「결과물은 펜으로 딴 선이다」). 측정이 뒷받침하는 것은 **효과**뿐이다 — 연필 구축선이 중심을 끌어가 반경이 17.335였고 펜만 보면 7.274다. **한 자리에서 어긋났고 고쳤다**(1차 리뷰어 [9]): 펜이 없을 때 경계 상자는 무게중심보다 **먼** 중심을 낸다(52.087 = 17.335의 **3배**)이고 그 방향은 사람이 보고한 증상과 같다. 그래서 「펜 선이 없으면 연필로 **대신한다**」를 **«종전 규칙(무게중심)으로 돌아간다»**로 읽었다 — 펜 이전 구간은 이 회차가 안 건드린다(D-4: 측정이 사람이 준 근거를 포함해 우선한다). 팔이 두 수를 다 낸다 | ⏳ 미검증(펜이 있는 구간) |
| **AS-C18** | **면의 평면 허용이 3D 교차 허용과 같은 값이어야 한다**(`PLANAR_RATIO` = `INTERSECT_GAP_RATIO` = 0.01) (2026-08-21 web2-07) | ⚙️ web2 | ⚠ **미검증. 값을 지어내지 않았을 뿐 «맞다»는 측정이 없다.** 근거는 **같은 물음의 앞뒤**라는 것이다: 루프의 모서리는 그 간격까지 «만났다»로 받아들여진 3D 교차이므로, 면이 더 엄하면 «만난 선들로 만든 루프인데 면은 안 된다»가 되고 더 헐거우면 교차 아닌 것도 면이 된다. **분모가 같은 양임은 팔이 잰다**(둘 다 `geomSize3` — `face.test.ts`의 「같은 분모」). ⚠ **동작점 하나이고 스윕이 없다**(#12) — 반증 팔의 두 점(이탈 0.0025 통과 · 0.0124 거부)은 임계를 감쌀 뿐 대역을 안 훑는다. ⚠ **정상 경로에서는 한 번도 안 걸린다** — 탭으로 만든 면은 구성상 한 평면이고(찾기가 평면 안에서 돈다), 승격을 태워도 이탈이 2.0e−17로 남았다(#35: 발화 경로가 아직 없다). ⛔ **되돌릴 조건이 구성상 발화 불가다**(2차 리뷰어 [3]) — 「정상 루프가 거부되면」으로 적었는데 정상 경로에서는 거부가 안 난다. **다시 쓴다**: ① `PLANAR_RATIO`를 0.0001이나 1.0으로 갈아 끼웠을 때 **합성 반증 팔 말고** 깨지는 팔이 하나라도 생기면 그때 이 값이 동작점이 되고 스윕이 필요하다 ② 그때까지 이 값은 **아무것도 안 가르는 상수**이고, 이 행은 「방어층을 남긴 판단」의 기록이다. 그 판단의 근거: 찾기가 늘 평면 안에서 돈다는 것은 «지금 구현»의 성질이지 자료구조의 성질이 아니다(경계가 곡선을 받으면 깨진다) | ⏳ 미검증 · **발화 경로 없음(아무것도 안 가른다)** |
| **AS-C19** | **겹쳐 보이는 면 중 사람이 가리키는 것은 «가까운 쪽»이다** (2026-08-21 web2-07) | ⚙️ web2 | ⚠ **미검증. A-3(3D 도구의 집기 관행)을 따른 것이고 이 저장소의 관측이 아니다.** 측정이 뒷받침하는 것은 **반대편의 기각**뿐이다 — 화면 넓이로 고르면 벽 뒤의 바닥(순환 29503)이 벽(43130)보다 작아 **더 먼 평면**이 잡혔다. 반례가 대안을 세우지는 않는다. ⚠⚠ **이 앱의 렌더링에서 「보이는 것 = 앞엣것」이 성립하지 않는다** — 면이 `depthTest:false`라 서로를 안 가리므로 뒤엣면도 그대로 비친다. 즉 **집기와 그리기가 어긋나 있다**(`DEFERRED.md`). ⛔ **되돌릴 조건 초판이 정의되지 않은 양을 썼다**(2차 리뷰어 [6]) — 「픽셀로 위에 보이는 면」은 서로 안 가리는 알파 합성에서 읽히는 양이 아니다(겹친 자리는 두 색이 섞인다). **다시 쓴다**: **탭으로 도달할 수 없는 면의 수**를 센다 — 어떤 화면 점에서도 `faceAt`이 안 내는 면이 있으면 그 면은 **만들 수는 있어도 탭으로 못 없앤다**(토글이 유일한 몸짓이므로 되돌릴 길이 사라진다). 그 수가 0보다 크면 집기 규칙이나 렌더링을 고친다. ⚠ 지금 벽·바닥 장면에서는 바닥이 일부 드러나 있어 그 수가 0이다 — 완전히 가려지는 배치를 만들면 1이 된다 | ⏳ 미검증 |
| **AS-C20** | **면의 옅은 채색(`#8d8880` · 불투명도 0.22)이 그림보다 눈에 안 띈다** (2026-08-21 web2-07) | ⚙️ web2 | ⚠ **미검증. 눈으로 골랐다.** 근거는 규칙 둘 — 상시 표시라 무채색이고(4-c), 종이(`#f5f3ee`)보다 어둡되 가장 옅은 심(2H, alpha 0.50)보다 옅다. 측정은 **선이 안 가려진다**는 것뿐이다(e2e: 경계선 위 창의 픽셀이 칠한 뒤에도 안 줄어든다). 그것은 「안 가린다」이지 「안 띄어 보인다」가 아니다. ⚠ **겹치면 두 배로 진해진다** — 깊이를 안 쓰므로 벽과 바닥이 겹친 자리가 어둡다(스크린샷에서 보인다). 값 하나이고 스윕이 없다(#12). ⛔ **초판에 되돌릴 조건이 없었다**(2차 리뷰어 [5]) — 등재가 판정자 없는 기록이 된다. **되돌릴 조건**: ① 겹친 자리(면 둘이 포개진 화면 영역)의 채색이 **가장 옅은 심(2H, alpha 0.50)보다 진하게** 읽히면 겹침 합성을 고친다 — 픽셀 밝기로 재는 양이다 ② 실기기에서 면이 선보다 먼저 눈에 든다는 관측이 오면 불투명도를 내린다 | ⏳ 눈 확인만 · 실기기 미측정 |
| **AS-C21** | **시점 스냅 임계 `snapAngle = min(atan(f/6W), 8.25°)`가 «의도적 정렬 복귀»와 «기울여 두기»를 가른다** (2026-08-25 web2-08 지시 3) | ⚙️ web2 | ⚠ **미검증 — 실기기·실사용 표본 0으로 고른 값이다**(AS-C1과 같은 자리). 두 갈래의 근거가 다르다: ① 대역 갈래 atan(f/6W)는 `VP_FAR_W`의 **대역 «크기» 차용**이다 — 같은 양의 재사용이 아니다(그쪽 6W는 획 시작점 기준, 이쪽은 주점 기준 — 2차 리뷰어 [N10]이 초판의 「재사용」을 잡았다). 물음의 형태(«그 소실점이 작도 대역 안인가»)가 같아 값을 빌린 것이고, AS-C16이 그 6W 자체의 미검증을 들고 있으므로 그 미검증이 여기로 상속된다. ⚠ ①이 이기는 대역(f < 0.87W)의 실사용 f 관측은 0이다 — 근거는 «앱이 그 문서를 받는다»(원칙 f)뿐이다(2차 [N9]) ② 상한 갈래 8.25°(기본 f의 ①값)는 **자세 읽기**인데 「내려다보기」와 갈리는 경계의 실측이 없다 — 「내려다보기 ≥ 10° 대역」 같은 문구를 근거로 쓰지 않는다(측정도 관용값도 아니다 · 1차 리뷰어 [11]). 환산 참조점 0.286°/px는 `ORBIT_RAD_PER_PX`(0.005 rad)에서 나온 이 저장소의 상수다. 값 둘 다 동작점이고 스윕이 없다(#12) — 경계 팔(1.05/0.95배)은 임계를 감쌀 뿐 대역을 안 훑는다. 요 스냅도 같은 임계인데 «축 근처 2점 구도를 유지하려는 사용»의 표본이 0이다. **되돌릴 조건**: ① 임계 바로 밖에 «두려는» 사용(상한 근처 기울임 유지)이 관측되면 상한을 좁힌다 ② 정렬로 «못 가져가겠다»(광각에서 스냅이 안 걸린다)는 관측이 나오면 대역 갈래에 하한을 둔다 ③ 2점 구도가 축 근처에서 도는 사용이 관측되면 요 임계만 가른다 | ⏳ 실기기 미측정 |
| **AS-C22** | **⛔ web2-12 7번이 뒤집었다(#65 — 이 행이 그 «현재 사실» 정정이다)**: 지평선 진하기의 출처는 이제 경도표(MAT)가 아니라 **COL.horizon(작도 대역)**이고 토글이 하한 논리를 풀었다 — 「H(0.60)로 올린다」는 폐기(옅음 관측이 오면 COL.horizon 알파를 올린다 — 재료로 안 돌아간다). 실측은 `horizon_web2*.json`이 정본. 아래는 web2-08 당시의 기록이다: **지평선의 진하기 2H 급(색·알파 0.50·1.1px)이 「h 내지 2h」 문면에 맞고 안 사라진다** (2026-08-26 web2-08 지시 1) | ⚙️ web2 | ⚠ **미검증 — 눈·실기기 표본 0.** 값은 경도표(MAT) 재사용이라 새 상수는 없지만 **선택(2H, 옅은 쪽)은 동작점**이다(#12): 문면 「너무 진하다」의 방향으로 골랐고, h(0.60)와의 비교 관측이 없다. 팔의 임계(열당 잉크량 상한 250 · 하한 60)는 실측 넷(수리 전 382/319 · 알파만 192/160 · H 154/191 · 2H 128/128, dpr1/dpr2)의 사이에 놓은 값이고 **h~2h 문면 전체가 통과한다**(초판 임계 150은 H를 거부했다 — 1차 리뷰어 [1], 등록 기준과 판정 지표가 갈렸던 자리). ⚠ 반대 방향의 틈은 남는다(2차 [N6] — 2회차 잔여로 기록): 팔은 «무게가 상한 안인가»만 가르므로 지평선이 2H에서 H·«알파만 0.50» 쪽으로 굳어도 초록이다 — h~2h가 전부 문면 안이라 **급의 정체는 팔이 아니라 코드(MAT['2H'] 단일 참조)가 지킨다**. 급 판정 팔을 더 두는 것은 임계만 늘린다(#12)고 판단해 안 했다. ⚠ 커버리지 위상은 지평선 y=400 한 점에서 dpr 둘로만 훑었다. **대가**: ① 사용자가 2H로 그은 획과 지평선의 색·알파·굵기가 같다 — 갈리는 단서는 전폭(화면 끝까지)·«지우개가 안 먹는다»·**입자 없음**(지평선은 작도선이라 흑연 입자를 안 얹는다 — 2차 [N7]: 그래서 잉크량이 «정확히» 같지는 않다)이고 실사용 미판정 ② 상시 작도 표식 둘(지평선 ↔ 소실점 ✕ `#8a7f6a`)의 색이 갈렸다(web2-02 4-c의 통일이 깨짐 — 제자리 정정함). **되돌릴 조건**: 「지평선이 안 보인다」 관측 → H(0.60)로 올린다(팔이 그대로 통과한다 — 상한 250 안) · 「2H 획과 헷갈린다」 관측 → 색을 ✕와 같은 웜 그레이로 되돌리고 알파만 낮춘다 | ⏳ 실기기·눈 미확인 |
| **AS-C23** | **오스냅 기호의 무채색(현재 2H `#9c9c9c`·알파 0.50 — 처음 등재 시 `#555`)이 형태만으로 종류·존재를 가른다** (2026-08-26 web2-08 지시 2) | ⚙️ web2 | ⚠ **미검증 — 형태 구분(□◆△✕⊥▫○)은 Rhino 관행(A-3)이지 이 저장소의 관측이 아니다.** 팔이 재는 것은 «그려졌다»(painted +20)와 «무채색이다»(채널 차 ≤ 12 — 수리 전 초록은 131로 걸린다. 131 = 순색 130 + 합성 반올림 ±1)뿐이고, 자리는 **✕가 없는 끝점 한 점**이다(#12): 소실점 오스냅 자리는 밑에 ✕(`#8a7f6a`, 채널 폭 32)가 상시로 있어 24px 상자가 12를 넘는다 — **기호 자신**은 거기서도 `#555`다(«오스냅 기호 무채색»의 범위는 기호이지 자리가 아니다). ⚠ **HB 획(#5a5a5a) 위에서의 가시성은 안 쟀다** — 색 차 5/255뿐이라 형태가 가른다고 «판단»만 했다(render2d 주석도 그렇게 고침). painted +20의 실측(끝점 표식, 24px 상자): dpr1 **+62** · dpr2 **+253** — 임계 +20은 그 아래 3.1×/12.6× 여유다(2차 [N16]이 «관측 없음»을 잡아 재실측). 채도 12는 AA 잡음 위에 놓은 값이고 스윕이 없다. **되돌릴 조건**: 획 위에서 표식이 안 보인다는 관측 → 굵기(1.5px)를 올리거나 흰 테를 두른다 — **색으로 돌아가지 않는다**(지시 문면). ⚠ **web2-10 지시 6이 진하기를 «HB 대역» → 2H 급으로 내렸다**(실기기 「진하다」 관측 — MAT['2H'] 색·알파 0.50을 그대로 읽고 굵기 1.5는 유지. 표식 평균 알파 실측 0.775 → 0.388/0.381 dpr1/2) — 이 행의 «HB 대역» 서술은 그 전의 사실이다. **획 위 가시성의 눈 관측 1회**(헤드리스 스크린샷 — 2H 획 끝점의 □가 형태로 읽힌다): «형태가 가른다»가 관측으로 바뀌었다. 다만 표본은 □ 하나·HB 획 하나·화면 하나다(#12) | ⏳ 실기기 미확인(헤드리스 눈 1회) |
| **AS-C24** | **필기 인식($P·REJECT 0.10)과 음성 파서가 실사용 입력을 읽는다** (2026-08-26 web2-08 지시 4) | ⚙️ web2 | ⚠ **실필기·실음성 표본 0.** 인식 실측은 전부 **합성 표본**(템플릿 흔들기 — 이동·배율·±6°·잡음 3px 급, 시드 고정)이다: 열 숫자 × 변형 넷 **40/40**(⚠ 인식률이 아니다 — 자기 템플릿의 변형이라 «그 흔들기가 임계 안»이라는 말이다) · 잡음 **8/8 거부**(가로선·W·N·X·ㄷ·체크·대각선·삼각형 — 2차 [11]로 2종에서 넓혔다). REJECT 0.10은 그 표본의 실측 사이(옳음 최악 0.061 · 잡음 최선 0.1252(X→'8' 후보) — 여유는 임계 분모로 아래 39%·위 25%)이고 **흘린 실필기는 «?»가 나는 쪽으로 기운다** — 그 방향을 고른 근거는 «조용히 틀린 치수보다 다시 쓰기가 싸다»(비용 비대칭 · #61 ⚠⚠의 형태). 음성은 **배선까지** 쟀다(모의 인식기 e2e — 인식 결과가 창 규칙을 타고 적용된다) — 파서는 지시의 예 셋을 문면 그대로 읽지만(팔) 실제 인식기가 낼 변형(띄어쓰기·조사·「쩜」 등)의 표본이 없다 — 「쩜」은 안 받는다(«점»만). 글리프 묶음(가로 겹침)·소수점(크기 0.18배 미만)도 동작점이다(#12). **되돌릴 조건**: 실기기에서 «?»가 잦으면 REJECT를 올리기 전에 **오독을 먼저 잰다**(임계를 올리면 오독이 같이 늘어난다 — 틀린 치수는 실행취소도 안 된다) · 음성이 못 읽는 실제 발화가 오면 그 문형을 파서 팔에 추가하고 넓힌다. ⚠⚠ **실기기 1회 반증됨(필기 쪽)** — web2-10 지시 8 관측: 「필기 인식이 전혀 안 된다. 대부분 물음표이거나 숫자를 잘못 읽는다」. 합성 40/40이 «인식률이 아니다»라던 이 행의 유보가 맞았다 — $P는 제스처 인식기라 숫자 필기에 원리적으로 안 맞는다(템플릿=필체 하나·획 수 민감·분절 불가 — 지시 문면). 대응: web2-10 8-a가 **확정 경로(키패드)**를 세우고 확률 입력을 전부 스테이징으로 돌렸다(자동 적용 폐지). 인식기 교체는 8-b | ⛔ **필기 반증됨**(음성은 여전히 표본 0) |
| **AS-C27** | **지시 1의 메타 선택이 맞다 — `theme-color = 매니페스트 theme_color`(#f5f3ee) · apple 메타 셋 · status-bar-style `default`** (2026-08-26 web2-09 지시 1 · 2차 리뷰어 [16]) | ⚙️ web2 | ⚠ **전부 미검증 — iOS는 장치가 없고(확인 불가로 명기) 안드로이드 표시줄도 실기기 표본 0.** status-bar-style `default`의 근거는 「black-translucent는 콘텐츠가 상태줄 아래로 들어가 종이색(#f5f3ee) 배경에서 시계가 안 읽힌다」는 **눈 판단이 아니라 추론**이고 그 표시 자체를 본 적이 없다(#12 — 동작점 하나). theme-color 동일값의 근거는 「갈리면 설치 전후 표시줄이 다르다」(규격 추론). **되돌릴 조건**: ① 실기기(안드로이드)에서 설치 표시줄 색이 이상하면 theme-color부터 본다 ② iOS 장치가 생겨 상태줄이 콘텐츠를 가리면 status-bar-style을 바꾼다 — 그 전에는 이 행이 «iOS 경로는 검증 없는 선언»의 기록이다 | ⏳ 실기기·iOS 미측정 |
| **AS-C25** | **선언 두 겹(뷰포트 메타 · 전 지점 유효 touch-action) + 이벤트 보험(두 손가락 touchmove·gesture*·Ctrl+휠 preventDefault)이면 실기기(안드로이드 크롬)의 핀치·이중 탭이 브라우저 줌을 안 낸다** (2026-08-26 web2-09 지시 2) | ⚙️ web2 | ⚠ **미검증 — 실기기 표본 0.** 헤드리스는 이 가정을 **못 잰다**: 메타·CSS를 다 벗겨도 CDP 합성 핀치가 visualViewport.scale을 못 움직였다(계측 불능 — 반증 못 하는 계측은 판정에서 폐기, D-3). 팔이 재는 것은 원인 값(유효 사슬·preventDefault 도달)뿐이다. ⚠ **지시가 준 후보 「#app 바깥에서 시작한 핀치가 샌다」는 반증됐다**(D-4) — 전 요소가 #app의 DOM 자손이라(#app이 inset:0) 「바깥」이 없고, 25점 격자 누수 0이었다. 남은 채널 후보: ① 안드로이드 접근성 「강제 확대」(선언을 **설계상** 이긴다 — 켜져 있으면 web 쪽 답이 없다) ② 비-크롬 브라우저 ③ 크롬 「데스크톱 사이트」 모드(뷰포트 선언 취급이 다르다 — 14인치 대화면이라 기본일 가능성. 이 저장소에서 잰 적 없음, 2차 [17]). **이 열거의 완전성 근거는 없다** — 실기기에서 셋 다 아니면 채널을 다시 가른다. **되돌릴 조건**: 실기기에서 여전히 브라우저 줌이 나면 강제 확대 설정부터 본다(DEFERRED의 web2-09 표) — 꺼져 있는데도 나면 이 행이 반증이고 채널을 다시 갈라야 한다 | ✅ **실기기 1회 관측됨**(2026-08-26 · web2-10 지시 머리 — 「PWA 판정 전부 통과: 설치·전체화면·핀치·아이콘」. 핀치가 브라우저 줌을 안 냈다). ⚠ **요약 회신 한 문장**이라 채널 후보 ①②③의 개별 가름·이중 탭·데스크톱 모드 여부는 그 문면에 없다 — 되돌릴 조건은 그대로 산다 |
| **AS-C26** | **패널(설정·파일)의 터치 스크롤을 막힌 채 둬도 내용에 닿는다** (2026-08-26 web2-09 지시 2 · 2차 리뷰어 [15]) | ⚙️ web2 | ⚠ 초판은 패널에 `pan-y`를 열었는데 **「패널을 스크롤 못 한다」는 관측이 없다** — 유효 사슬은 수리 전에도 none이었으므로(전 요소가 #app 자손) pan-y는 이 회차가 사슬을 실제로 «푼» 유일한 자리였고, 관측 없는 넓힘이라(A-3) 되돌렸다. 지금 상태: 전 지점 none, 패널은 터치로 스크롤이 안 된다(휠은 touch-action과 무관해 데스크톱은 돈다). 스크롤 요소가 `details.pane > div` 규칙 하나뿐임은 전수 grep(overflow-y:auto 한 곳). 내용(설정 열 줄·파일 다섯 줄)이 max-height(100vh−20px) 안에 드는 화면에서는 스크롤 자체가 필요 없다 — 그 전제가 이 행의 가정이다. **열 조건**: 실기기에서 패널 내용이 잘려 손가락으로 못 닿는다는 관측 — 그때 pan-y를 열되 그 지점의 확대 차단은 규격 근거뿐임을 함께 등재한다. ⚠ **web2-16 1부 갱신**: 진단 패널(#diagpanel)은 이 행의 범위 밖이 됐다 — `pointer-events:none`(포인터 삼킴 수리)이 깔려 **pan-y 경로 자체가 봉쇄**됐다. 그 패널이 화면을 넘게 자라면(지금 412px/800) 답은 스크롤이 아니라 줄 수 줄이기·두 열·글자 축소다(DEFERRED 「패널 터치 스크롤」 행) | ⏳ 실기기 미측정 |
| **AS-C28** | **short_name 12자가 안드로이드 홈 화면 라벨 잘림선 안이다** (2026-08-26 web2-10 지시 1) | ⚙️ web2 | ⚠ **미검증 — 사람이 준 근거다**(지시 문면 「12자 — 안드로이드 라벨 잘림선 안」, D-4: 확인 대상이지 결론이 아니다). 이 저장소에 그 잘림선의 측정은 없고, 실기기 확인은 **재설치 전에는 구조적으로 불가**하다 — 이미 설치된 앱의 라벨은 설치 시점에 굳어 안 바뀐다(같은 지시 문면). 팔은 값 등식만 잰다(길이 단언은 등식 아래 항등이라 안 뒀다 — 1차 리뷰어 [3]). **되돌릴 조건**: 재설치한 실기기 홈 화면에서 「Brunelleschi」가 잘려 보이면 이 행이 반증이고 더 짧은 이름을 정한다 | ⏳ 실기기 재설치 후 확인 |
| **AS-C29** | **축 스냅 파선 안내(#555·1px·양끝 너머 34px·AXIS_DASH 패턴 넷)가 「축에 붙었다」를 읽히게 한다** (2026-08-26 web2-10 지시 7) | ⚙️ web2 | ⚠ **미검증 — 실기기 눈 표본 0.** 헤드리스가 잰 것은 존재(너머 painted 32/109)·무채색(채도 0)·대역 분리(어두움 0.297/0.384 vs 오스냅 0.153/0.149)·붙음/안 붙음 가름뿐이다. 연장 34px·1px·패턴 넷은 동작점(#12). ⚠ 축 스냅이 기본인 앱이라 안내가 뜨는 획의 비율이 사실상 높다 — «붙었다»의 정보는 「이 순간 어느 방향으로 물려 있는가」이고, 소음으로 읽히면 안내를 끝 쪽 하나로 줄인다(2차 [15]). ⚠ #555의 진하기 자체는 「진하다」 관측이 오스냅(1.5px 채움형 기호)에 대한 것이라 1px 파선에는 안 옮겼다 — 실기기에서 「안내가 진하다」 관측이 오면 내린다. **되돌릴 조건**: 「축에 붙었는지 모르겠다」 관측 → 채널을 더한다(굵기·끝 표식) — **색으로 돌아가지 않는다** | ⏳ 실기기 미측정 |
| **AS-C30** | **세로바 1.5배(아이콘 27~93px·gap 9px·시각 선 굵기 1.95px)가 펜에 맞다** (2026-08-26 web2-10 지시 5) | ⚙️ web2 | ⚠⚠ **잘림 경계 서술이 web2-12 2부로 낡았다(#65 — 그 행의 «현재 사실» 정정)**: 배치가 **상단 고정(top 12px)**·시점 묶음 편입·간격 축소로 바뀌어 「top 50% 가운데 고정이라 양끝이 나간다」「754px·766px 경계·dpr≤2.25」는 전부 옛 배치의 수다 — 현행 잘림 경계는 `sidebar_layout_web2*.json`의 **clip_boundary_css_h** 필드가 정본이다(상단 고정이라 **아래만** 잘린다). 1.5배 배수 자체는 AS-C40이 잇는다. ⚠ **미검증 — 실기기 손 표본 0.** 1.5배는 지시가 «정도»로 준 값이고 스윕이 없다(#12). 시각 굵기 1.95px(viewBox 스케일 1.3×1.5)는 헤드리스 눈 1회로 골랐다 — 「뭉툭한가」는 실기기 축(2차 [11] · DEFERRED). 뷰포트 800 실측 754px — **CSS 높이 766px 아래면 세로바가 위아래로 잘린다**(top 50% 가운데 고정이라 양끝이 나간다. 2880×1800이 dpr≤2.25면 든다 — dpr은 미상, 진단 패널이 읽는다). **되돌릴 조건**: 실기기에서 잘리면 그때 정한다(지시 문면 — max-height·스크롤은 touch-action none과 상충해 안 미리 깔았다) | ⏳ 실기기 미측정 |
| **AS-C31** | **숫자 키패드(키 34px·gap 5px·자릿수 상한 12·적용 뒤 값 유지·스테이징 후 명시 적용)가 치수 입력의 확정 경로로 손에 맞다** (2026-08-26 web2-10 지시 8-a) | ⚙️ web2 | ⚠ **미검증 — 실기기 손 표본 0.** 키 34px는 지시 5 대역(하한 27px)의 위에 눈으로 골랐고 스윕 없음(#12). «적용 뒤 값 유지»는 「같은 치수를 잇달아 적용하는 손에 맞다」는 추정이고, 남은 값이 창 규칙을 타고 **지금 창의 대상**에 실린다(팔이 그 경로를 잰다 — id 5에 75). «스테이징 후 명시 적용»은 web2-08의 「읽히면 즉시 적용」을 뒤집은 것(지시 8-a ② 문면) — 손이 «한 번 더 누르기»를 성가셔하면 그 관측이 이 행의 되돌릴 조건이다(그때도 **첫 치수(scaleRef)만은 명시 적용을 유지한다** — 조용히 틀리면 실행취소가 없다) | ⏳ 실기기 미측정 |
| **AS-C32** | **digitnet(784-64-11 MLP·int8·MNIST 학습)이 실기기 펜 숫자 필기를 읽는다** (2026-08-26 web2-10 지시 8-b) | ⚙️ web2 | ⚠ **실필기 표본 0 — MNIST가 MovinkPad 펜 필기(EMR·또박또박 쓰는 전용 칸)를 대표한다는 근거는 「둘 다 손글씨 숫자」뿐이다.** 실측: MNIST test 10k에서 int8 9628/10000(숫자별 분자/분모는 stage0/out/digitnet_mnist.json) · 합성 획 표본 39/40(digit_accuracy_web2.json — ⚠ 그 표본은 $P 템플릿이라 $P에 유리하고 digitnet에는 분포 밖이다) · 합성 잡음 클래스 거부 93.4%. **NET_REJECT 0.52의 «분리»는 시드에 따라 뒤집힌다**(#12·#59 — seed 7에선 옳은 최악 0.543 > 잡음 최선 0.497이지만 **seed 101에선 0.452 < 0.497로 역전** — 시드 스윕 실측). 그래서 임계는 잡음 차단이 아니라 **불확신 기권** 손잡이다 — 잡음 차단은 잡음 클래스(933/1000)가 지고, 지키는 불변은 **오독 0 · 잡음 수용 0**(4시드 × 40+8 전부에서 성립 — 팔이 잰다. 기권 방향이 비용 비대칭의 안전한 쪽이라 임계에 비대칭을 더 안 줬다 — 올리면 기권만 늘고, 확정은 어차피 키패드가 진다). MNIST 10k에 임계를 적용한 배포 구성의 수: **맞음 9483 · 기권 325 · 오독 192**(digitnet_mnist.json int8_with_reject — 96.3%는 임계 미적용 분류기의 수다). 내장 API(①)는 헤드리스에 없어 모의 배선만 쟀다 — 실기기(안드로이드 크롬)에 있는지·정확한지는 진단 패널 「필기 인식 API」 줄과 실사용이 가른다. **되돌릴 조건**: 실기기에서 digitnet 오독(«?»가 아니라 틀린 숫자)이 관측되면 NET_REJECT를 올리기 전에 **오독부터 잰다**(AS-C24의 규칙 그대로 — 확정은 어차피 키패드가 진다) | ⏳ 실기기 미측정 |
| **AS-C33** | **점별 입력(rawIn)은 펜에만 실으면 된다 — 마우스·손가락의 필압·기울기는 정보가 없다** (2026-08-26 web2-11 1-c) | ⚙️ web2 | ⚠ **관측 1종(헤드리스 크로뮴)뿐이다.** 실측: 크로뮴 마우스는 pressure 0.5(상수)·tiltX/Y 0·twist 0으로 온다(`stage0/out/input_capture_web2.json` — undefined가 아니라 **0**이다. ⚠ 이 관측의 결론(2부 폴백의 가름): **값이 아니라 rawIn 유무로 가른다** — 마우스도 tilt를 «0»으로 주므로 «tilt 0 = 기울기 정보 있음»으로 읽으면 마우스와 수직 펜이 안 갈린다. rawIn이 없는 획(마우스·손가락·옛 파일)은 기울기 변조 없이 그린다). 점마다 상수를 실으면 정보 없이 파일만 큰다(점당 +12.31B — stroke_payload_web2.json, 해시 없음(웹2 원장은 상수 스냅샷 등록부 밖 — 구조적)). ⚠ 손가락(터치)의 필압은 실기기에서 안 봤다 — 안드로이드 터치는 pressure를 실을 수 있다. **되돌릴 조건**: 실기기에서 손가락 필압이 상수가 아니라는 관측이 오고 그것을 쓰는 표현이 서면 터치에도 싣는다(`input.ts` `quantIn`의 `drawingType` 조건 한 줄) | ⏳ 실기기 미측정 |
| **AS-C34** | **H·F·B는 이웃 질감(2H/HB/2B) + MAT 진하기·굵기로 경도 차가 손에 읽힌다** (2026-08-26 web2-11 2-c) | ⚙️ web2 | ⚠ **실기기 손 표본 0.** 질감 급이 3계단으로 동률이 된 대신 진하기가 잇는다 — 세션이 잰 것: brush에 실제로 넘어가는 색의 광도가 2H→2B 강한 단조(단위 팔) + **렌더 어두움**이 2H<H<HB<2B 단조(e2e — 인접 급 2H↔H도 갈렸다). brush.add 보간을 기각한 근거: 내장 파라미터가 라이브러리 내부값이라 보간은 반증 못 하는 수를 짓는다(D-3). **되돌릴 조건**: 실기기 「경도 차가 안 읽힌다」 → brush.add 보간을 연다 | ⏳ 실기기 미측정 |
| **AS-C35** | **alpha의 종이색 혼합 근사(불투명 픽셀)가 화면에서 종이답게 읽힌다** (2026-08-26 web2-11 2-c) | ⚙️ web2 | ⚠ **실측이 초판 기대를 두 번 반증했다**: «완전 불투명 없음» → 있음 → «반투명 몫 실존» → **없음**(2H 획에서 «알파>0인 픽셀» 중 «알파==255»의 몫이 1 — 분자/분모 정의·값은 `stage0/out/brush_visual_web2.json`의 opacity_2h가 정본. e2e가 0.9 하한으로 고정: 라이브러리가 바뀌면 깨져서 알린다. «뒤를 가린다»는 이 실측 + 위 겹 알파 255의 브라우저 합성 규칙의 귀결이다). 귀결 둘: ① 획끼리 겹쳐도 진짜 알파처럼 안 진해진다 ② **옅은 획이 뒤의 3D(Line2·면)를 픽셀 단위로 가린다**(classic grain은 반투명이라 살짝 비쳤다 — 이 갈림이 화면에서 거슬리는지는 실기기 몫). ⚠ **web2-12 10번(종이 마스크)이 «합성» 층을 바꿨다**(3차 [7] — #67의 자기 대조): CSS 알파 마스크가 합성 알파를 최대 ~22% 깎으므로 «옅은 획이 뒤의 3D를 픽셀 단위로 가린다»는 마스크 몫만큼 약해졌다(캔버스 버퍼·이 행의 실측(opaque 몫)은 불변 — 마스크는 합성 단계다. materials 팔은 변화 기반 diff라 기준·현재에 같이 걸려 상쇄되고 마스크 켠 채 전 팔 통과 — AS-C37 문턱 8의 여유도 그 실행이 보였다). **표본 폭**(web2-12 1번이 넓혔다 — 1차 리뷰어 [14]): 2H 한 급이던 실측을 **일곱 급 × dpr 둘**로 — `materials_visible_web2*.json`의 brush.*.brushc_painted/brushc_opaque 필드가 정본이고 전부 몫 1.0(INK는 질감 없음 0/0 — 구조적). straight alpha 선언(web2-12 수리)이 색을 바꿀 수 있는 픽셀은 0<α<255뿐인데 그 몫이 0이라 획 색 불변도 이 필드가 진다. **되돌릴 조건**: 실기기 「겹침이 밋밋하다/3D가 지워져 보인다」 → 색 혼합 대신 픽셀 알파를 실을 길(캔버스 후처리·라이브러리 opacity 손잡이)을 그때 판다 | ⏳ 실기기 미측정 |
| **AS-C36** | **기본 렌더러를 brush로 둬도 된다** (2026-08-26 web2-11 2-b) | ⚙️ web2 | 근거는 사람의 결정 문면(「먼저 무겁더라도 좋은 걸 해봐야, 불편했을 때 고칠 수 있다」)이다 — 성능·느낌의 실기기 표본 0. 되돌리기는 세로바 토글 한 번(localStorage `b2-renderer`)이고 classic 경로는 전부 산다(2-b·grain 분기). **되돌릴 조건**: 실기기에서 brush가 못 쓸 정도면 사람이 토글로 내리고, 그 관측이 오면 기본값을 classic으로 뒤집는다(코드 한 줄 — state.ts) | ⚠ **되돌리기 몫이 배포 b70b655에서 실효였음이 반증됐다**(2026-08-26 web2-12 1번 · #65의 형태): classic 전환이 흰 장막을 못 걷었다 — `brush.clear()`가 남긴 규약 위반 버퍼(1,1,1,0)는 렌더러와 무관하게 합성을 덮는다(표식 실측: classic 전환 후에도 화면 0). «classic 경로가 산다»(코드 경로)와 «토글이 화면을 되돌린다»(합성)가 다른 명제였다. straight alpha 수리로 되살아났고 materials.spec classic 팔이 상시로 잰다. 실기기 성능·느낌 몫은 여전히 ⏳ |
| **AS-C37** | **재료 가시성 팔의 동작점(상자 20×60px · 채널 차 문턱 8 · 하한 20픽셀)이 「획이 화면에 보인다」를 잰다** (2026-08-26 web2-12 1번) | ⚙️ web2 | ⚠ 동작점 셋 다 스윕 없음(#12) — 문턱 8은 AA 가장자리(차 10~45)와 압축 요동(±2) 사이, 하한 20은 **실측 최저와 「몇 픽셀 얼룩」 사이**(값은 원장을 그 자리에서 읽는다 — `materials_visible_web2.json`(dpr1)·`materials_visible_web2_dpr2.json`의 renderer.grade.during/after 필드. 여유는 5배 이상). ⚠ 헤드리스가 보인 것은 **전면 소실(0)을 가른다**는 것뿐이다 — 부분 소실·옅어짐에 대한 판별력은 스윕이 없어 안 쟀고, 「보인다」의 눈 판정은 실기기 몫. ⚠⚠ **이 팔이 세워진 계기의 반증 둘(D-4)**: web2-12 지시가 준 잉크 소실 후보 ㉮(`grain:0`이 그리기 관문) ㉯(`mat.w`가 0/undefined로 굵기 0)는 **둘 다 틀렸다** — 표식 실측: 획은 문서·승격·#gl(Line2)까지 전부 정상이고, 사라진 원인은 **#brushc의 규약 위반 알파(1,1,1,0)+premultiplied 선언이 만든 흰 장막**이 #gl을 덮은 것(brushlayer.ts `withStraightAlpha` 머리주석·materials.spec) | ⏳ 실기기 미측정(헤드리스: 수리 전 실패 4/4·되돌리면 실패 — 전면 소실 판별만 확인) |
| **AS-C38** | **뗌 게이트의 동작점(상자 20×50px · `e2e/thresholds.ts`의 GATE 셋 — 절대 ABS·상대 REL·연장 카나리아 GROW_CANARY)이 「떼는 순간 아무 일도 없다」를 잰다** (2026-08-26 web2-12 2번) | ⚙️ web2 | ⚠ 동작점 넷 다 스윕 없음(#12) — 값·정의의 단일 출처는 `web2/e2e/thresholds.ts`이고 실측은 `draft_gate_web2*.json`(dpr별 파일)의 release·flicker·thresholds 필드가 정본(#47 — 여기 수를 안 적는다). 미리보기가 확정과 같은 두 겹(질감 #brushc·몸체 **Line2 그 자체**)으로 그려져 잔차는 양 dpr 전 재료에서 0 대역이다(2D 캔버스로 몸체를 흉내 낸 중간판 둘은 반증됨 — 잉크 겹은 겹 순서 역전, 아래 겹 벡터는 반투명 합성 파이프라인 차. state.ts draftBrushed 머리주석). ⚠ 절대 ABS 하나로는 옛 경로의 일부 조합(원장 classic_hb의 낮은 dpr2 값)을 못 가른다 — 판별은 상대 REL과 반증 실행(양 dpr에서 옛 경로 차단·시드 프레임 변동 모두 게이트·강제 재그리기 팔이 실패)이 함께 진다. **관측**: 획을 늘리면 앞부분 입자가 **시드와 무관하게** 재배치된다(브러시가 전장을 파라미터로 삼는다 — 시드 축의 판별자는 release·forced_redraw_diff다). 이것이 실기기에서 거슬리면 이 행이 되돌릴 조건이다(그때는 «앞부분 고정» 점진 확정 렌더를 판다) | ⏳ 실기기 미측정(헤드리스: 반증 둘 × 양 dpr 실행) |
| **AS-C39** | **뷰 썸네일의 동작점(폭 `C.THUMB_W`=160px · JPEG 품질 0.72 · 장당 300KB 파서 상한)이 「보고 고른다」에 충분하다** (2026-08-26 web2-12 5번) | ⚙️ web2 | ⚠ 셋 다 스윕 없음(#12). 실측(한 장 바이트·저장 탭 ms·뷰 1/5/20의 .brnl 크기)은 `views_thumb_web2.json`이 정본 — ㉮(저장 시점 굽기)를 고른 근거가 그 값들이다(장이 KB 대역이라 파일 부담이 작고, ㉯(열 때 재렌더)는 뷰마다 장면 재사영이 필요해 «펼치기»가 무거워진다). 300KB 초과분 강등(파서 상한 — file.ts 300000이 정본)은 조작된 파일 방어이지 정상 경로에선 안 닿는다. **개수 상한은 안 둔다(결정 — 지시가 물은 동작점의 답)**: 장당 바이트가 KB 대역이라 수십 뷰까지 파일 부담이 완만하고(1/5/20 실측 — 증분 선형), 상한이 자를 «정상 사용»의 근거가 없다. 대가: **썸네일이 자동 저장(localStorage)에도 실린다** — 뷰 수가 quota를 밀면 기존 방어선(autosaveWarned 알림)이 그 자리다. ㉮/㉯ 대비도 실측이다(capture_ms ↔ regen_view_ms 필드 — ㉯는 펼칠 때 뷰마다 시점 이동+전량 재그리기 몫이 든다). **알아볼 수 있는가는 실기기 눈 몫**(DEFERRED). 되돌릴 조건: 「썸네일이 작아서 못 알아본다」 → THUMB_W를 올리고 원장을 다시 잰다 | ⏳ 실기기 미측정 |
| **AS-C40** | **세로바 동작점(--ui-scale 1.5 · --hit-pad 3px · --btn-gap 1px · 그룹 여백 10px · 상단 고정 · 연필통 행 15px 기준)이 펜에 맞다** (2026-08-26 web2-12 3·6번) | ⚙️ web2 | ⚠ 전부 스윕 없음(#12 — AS-C30의 후속: 1.5배는 그 행 그대로이고 **배치·잘림 경계는 이 회차가 뒤집었다**(AS-C30의 정정 참조)). 헤드리스 실측·판정은 sidebar.spec가 정본이고 값은 `sidebar_layout_web2*.json`(dpr별)의 overlap(두 도구 상태·굵기 막대 포함)·sidebar_pencil/pen·right_edge_x·hit_pad_css·clip_boundary_css_h 필드다. **누르기 쉬워졌는가·연필통이 그림을 가리는가는 실기기 손 몫**(DEFERRED). 되돌릴 조건: 「아직 누르기 어렵다」 → hit-pad를 올리되 화면 안 수용 팔이 지키는 높이 예산과 함께 움직인다 | ⏳ 실기기 미측정 |
| **AS-C41** | **모서리 넘김의 동작점(C.OVERSHOOT_PX=7 · 만나는 끝만 · 승격 획만 · 3D 일치 tol 상대 1e-6)이 「손 제도로 읽힌다」에 맞다** (2026-08-26 web2-12 8번) | ⚙️ web2 | ⚠ 넷 다 스윕 없음(#12). 7px 근거는 «선 굵기의 몇 배» 제도 관행의 한 점 · «만나는 끝만»은 제도 관행(넘김은 모서리에서 생긴다 — 지시의 «후자») · 대기 획 제외는 3D 만남 정본 부재(화면 일치는 시점 따라 생멸하는 가짜 만남 — #64의 형태. ⚠ 2026-08-27 개정 3 초안 §3이 대기선과의 화면 교차를 **정의 사건**으로 쓰기로 했다 — 이 문장을 뒤집는 것이 아니라 갈라 선다: 이 행이 막는 것은 화면 일치를 **관계**로 쓰는 것이고, 초안 §3.0이 사건은 **B의 시점에서만** 세도록 제한해 시점 생멸 문제를 피한다. `line_plan_r3_draft.md` §3.0이 정본) · tol은 오스냅으로 붙은 좌표의 일치 확인이라 좁다(#63 규율: 좌표에는 허용이 필요 없다). **기하 불변**: 판정은 core/overshoot.ts(읽기 전용·캐시), 표현은 render2d 꼬리 — a·b·lift·오스냅이 안 움직이는 것을 단위 팔(불변)·e2e(꼬리 끝 오스냅이 실제 끝점으로)·반증(전 끝 넘김 → 자유 끝 팔 실패)이 잰다. **제도처럼 보이는가는 실기기 눈 몫**(DEFERRED). 되돌릴 조건: 「넘김이 어색하다」 → 길이·대상을 그 관측으로 다시 정한다 | ⏳ 실기기 미측정 |
| **AS-C42** | **(지시 전제의 반증 — D-4) 「치수선 끝 화살표가 있다」는 web2-12 지시 11번의 전제는 사실이 아니다** (2026-08-26) | ⚙️ web2 | 이 앱의 치수(web2-08)는 리본 패널(값 입력·표시)이고 캔버스에 치수선·보조선·화살표를 그리는 코드가 없다(전수 grep — render2d·render3d·dimpanel). 그래서 「표현만 바꾼다」의 대상이 없고, 치수선 그리기는 새 기능이라 안 만들었다(A-3 — 사실과 원래 목표는 DEFERRED web2-12 3부 표). 치수선을 그리게 되는 회차의 관행 정본은 지시 11번 문면(45° 틱·보조선 이격·지나침) | ✅ 전수 grep로 확인 |
| **AS-C43** | **제도 표현의 동작점 — 번짐(밀집 ≥5점·끝 원 0.75/0.7배·가장자리 9px당 1점·알파 0.45)·종이(타일 128px·이빨 깊이 ~22%·고정 시드 rng32(7)·위상 문서 고정·결 크기 화면 고정)** (2026-08-26 web2-12 9·10번) | ⚙️ web2 | ⚠ 전부 스윕 없음(#12). 밀집 5점 근거: 120Hz 스트림에서 ≈40ms 체류 — 그보다 짧은 멈춤은 손떨림 대역. 끝 원 배율·가장자리 밀도·이빨 깊이는 눈 1회의 한 점. 시드 고정은 §5 재현성(실행마다 같은 종이·같은 자국). 실측: 번짐·궤도 비용은 `drafting_web2*.json`·게이트 불변은 `draft_gate_web2*.json`. **자연스러운가는 실기기 눈 몫**(DEFERRED 3부 표) | ⏳ 실기기 미측정 |
| **AS-C44** | **대기 획 시점 감쇠의 동작점(C.WAIT_FADE_DEG=30° · 선형 감쇠 · 창 밖 0 · 회전 없는 이동은 종전 흐림 0.3 유지)이 「자기 시점에서만 보인다」의 손 감각에 맞다** (2026-08-27 web2-13 3-a) | ⚙️ web2 | ⚠⚠ **web2-16 3-b가 이 행의 둘을 폐지했다**(사람 결정 「페이드 같은 거 없이 즉시」): 선형 감쇠 → **이진**(창 30° 안 1·밖 0·경계 fp 부스러기는 밖으로) · 회전 없는 이동의 0.3 흐림 → **1**(창 안 원 진하기). 창 30° 값 자체는 유지 — ⚠ 이진화로 «창 안 실효 가시 대역»이 넓어졌다(선형이면 15°에서 절반이던 것이 전 구간 원 진하기): 그 대가의 판정은 실기기 「즉시 끊김 체감」 행이 든다(DEFERRED web2-16 표). 이하 원문은 web2-13 시점 서술이다: ⚠ 동작점 하나·스윕 없음(#12). 30°의 근거: 시점 스냅 임계(snapAngle ≈8.25° — 그 안은 «같은 시점»으로 접힌다)보다 넉넉히 크고 측면(90°)보다 훨씬 작은 한 점 — 실측 근거는 없다. 팬·달리(회전 0) 갈래는 감쇠가 아니라 종전 0.3(각도 척도 부재 — waitfade.ts 주석). 검증: 단위(waitfade.test — 0 도달·경계 fp·0.3 갈래) · e2e(waitfade.spec — 궤도 후 두 겹(#ink·#brushc) 0 + 합성 감도 증인 + 되돌림 반증, dpr 둘). ⚠ **갈래 ㉮(딱 끊기)와의 실질 차이는 창 «안»의 연속성뿐이다** — 창 밖은 ㉮와 같은 상태(정확히 0)다(3차 [35]). 실기기 관측이 「획이 없어졌다」로 오면 손잡이는 감쇠 곡선이 아니라 **창 크기(WAIT_FADE_DEG)**다. 되돌릴 길: 설정 「대기 획은 그린 시점에서만」 끄기(옛 동작 그대로 — A-4). ⚠ **실기기 관측이 왔다(2026-08-27 web2-14 3번)**: 「돌리면 서서히 사라지고 돌아오면 다시 생겨 성가시다」 — 틀린 것은 창·곡선이 아니라 **실시간 반응**이었다(부분 반증: «연속 감쇠가 손 감각에 맞다»의 «제스처 중» 절반). 수리: 제스처·접기 애니 동안 판정 포즈 동결(state.fadePose · beginNavHold/endNavHold). 실측은 e2e(wait_freeze_web2*.json — 실제 드래그 왕복 변화 0회·창 밖 68.8° 왕복)가 지고, 단위 navhold.test는 배선·항등 확인이다(동결값에 같은 입력 — 측정이 아니다. §5.1 유형 3). ⚠ 대가: 드래그 중 대기 획이 동결값(대개 1)로 남아 옛 0.3 흐림보다 **진하게** 미끄러진다(한시 — DEFERRED 표 ②가 판정) · 제스처를 나눈 왕복은 뗄 때마다 1회씩 불연속이다(표 ③). 창 크기·곡선·창 밖 0의 «손 감각» 몫은 이번 관측이 안 건드렸다 — **미검증으로 남는다**(이번 판정이 가리킨 것은 실시간 반응이었다는 것이 이 회차의 읽기다) | ⏳ 실기기 재판정(동결 후) |
| **AS-C45** | **「잘못 찍힌 점」 문(C.STRAY_MIN_PX=6 css px · 척도는 raw bbox 대각 · 탭 대역은 불변)이 사고 획만 거른다** (2026-08-27 web2-13 3-b) | ⚙️ web2 | 실측(`stray_gate_web2.json` — 원시 Quick,Draw 28706획)이 **판별한 것은 「후보 3~10 어디든 폐기 수가 좁은 띠 안에 있다(값은 원장 below 행)」까지다** — 후보들 사이는 데이터가 거의 안 가른다. **그 안에서 6을 고른 것은 관용이다**(탭 문 2의 3배 — 3차 [29]). 분자는 갈려 있다: below에는 점 찍힘(below_zero 필드)과 비영 짧은 획이 섞여 있고 후보가 자르는 비영 획의 상한은 below−below_zero다([27]). ⚠ 한계: ① 좌표 단위·dpr 미기록 — 1:1 읽기의 **방향을 모른다**(장치 px·dpr>1이면 실 폐기율은 더 크다 — [32]) ② 폐기율 최고 행은 skyscraper(보수 행) ③ **AS-C1의 유보 아래 있다**(Quick,Draw는 대상 사용자 대표성이 의심 — 대역 근거로만 쓰고 판정은 실기기·진단 패널 카운터) ④ 줌 프레임: raw는 문서 좌표라 ×view.s로 화면 px 환산(탭 문과 같은 규약 — e2e는 s=1만, #12). ⚠ 끝점 거리 척도는 기각됐다(닫힌 한 붓 오폐기 — 원장 superseded_scale·stray.test 반례). 버린 수는 진단 패널 「버린 짧은 획」이 센다(팔이 패널 문자열까지 잰다) — 크면 이 값이 틀린 것이다 | ⏳ 실기기 미측정(억울한 폐기 관측 대기) |
| **AS-C46** | **「카메라 닫힘 = `constructionDone`」이 «더 올 승격이 없다»의 판정으로 충분하다** (2026-08-27 web2-13 4부 — 굳힘의 문 · 초안 §9.2) | ⚙️ web2 | 근거: 소실점 둘이면 f²=\|u₁\|\|u₂\|·주점 W/2가 확정이고 셋째 «찍기»는 축만 더한다(camera.ts — f는 앞 둘에서만) · 1점 잠금이면 두 번째 소실점이 안 생긴다. ⚠ 알려진 열림 경로 하나: 잠금은 계산이라 **잠금을 만든 가로 획을 지우면** 승격이 다시 가능해진다 — 그때는 승격 «사건» 규칙(camSig 변화 → own3 전부 버리고 재굳힘)이 상위로 덮는다(own3d.test 4-c 팔이 그 경로를 통째로 잰다). ⚠ L 라인(P3 유한)으로 옮기면 닫힘 판정이 「P3 도달」로 바뀐다(초안 §9.2 — 이 행은 web2의 것) | ✅ 팔로 확인(잠금→해제→승격 경로) · ⏳ 실기기 판정은 기본값 전환과 함께(DEFERRED) |
| **AS-C47** | **잉크 심판의 허용 오차(OWN3_TOL_PX=0.01px — 수치 동일성)가 정상 굳힘을 절대 안 걸고 오류를 절대 통과 안 시킨다** (2026-08-27 web2-13 4-a) | ⚙️ web2 | ⚠ 「절대」는 경험적 주장이다 — **알려진 예외가 이미 하나 있다**(dim 획의 b 끝: 치수가 길이를 대체 — 검사에서 뺀다). 같은 형태의 예외 후보(연장선 스냅·화면 줌 s≠1·면 경계·지우개 절단)를 전수로 훑지는 않았다 — 지우개 절단은 4차 [46]으로 승계 규칙+팔이 섰고, 나머지는 미실측. 실측 여유는 `own3d_invariant_web2.json`(국면 넷 — 신선·왕복·삭제·승격 후. margin 필드가 정본 — 수치를 여기 안 적는다 #47). 반증은 +0.1 세계단위(문 위 서너 자릿수 — 문 바로 위 대역은 안 쟀다 #12). 거짓 경보 한 번이 관측되면 그 국면을 원장에 더하고 값을 다시 본다 | ⚠ **둘째 예외 발견(2026-08-27 web2-14 2번 반증 픽스처의 첫 판 — 폐기된 픽스처의 1회 관측, b 끝 6.9px. 살아 있는 재현 팔·원장 없음 — 3차 [B])**: 축 허용각(3.4°) 안에서 어긋나게 그어진 잉크가 **사슬**로 올라가 굳으면 b 끝 사영이 잉크에서 벗어난 채 굳는다 — 축-풀이 의미론(끝이 축 위로 미끄러진다)이 굳힘에 그대로 실린다. 도달 경로(3차 [A] 판정): **지우개 조각은 아니다** — 조각 끝은 어버이 3D 직선 위 점의 사영이라 구성상 잉크가 그 직선 위다(own3d.test 4차 [46] 팔이 조각의 잉크 심판 통과를 실측). .brnl 왕복도 좌표 보존이라 아니다(4-b 팔). 남는 경로는 ① 손 편집·외부 생성 파일 ② **대기 획에 오스냅(끝점·중점·near)으로 끝을 붙였는데 그 대기 획이 안 올라간 경우**(끝점·중점은 web2-13부터, near는 web2-14부터 — ⚠ **끝점·중점 항은 web2-16에서 좁아졌다**: 축 걸린(조준선 있는) 획에서는 끝점·중점도 축선에 사영되어 그 점이 구성상 축선 위다(끝이 축선 밖으로 밀려 대기 획이 안 올라가는 경로가 그만큼 닫혔다 — 남는 것은 조준선 없는 자유 획의 점 스냅뿐). ⚠ **near 항은 web2-15에서 좁아졌다**: 조준선이 있는 획(축 스냅이 붙은 획)에서는 near를 안 내고 겉보기 교차가 답이며 그 점은 구성상 축선 위이자 잉크 위다. 조준선 없는 자유 획에서는 종전대로 남는다) — ②의 굳힘 값은 사슬이 매번 내던 값의 사본이라 화면이 더 틀려지는 것은 없고 차이는 근거 삭제 후 유지(의도된 기능)뿐이다. 4-g 교점 정의는 왕복 문이 같은 획을 거부한다(vptouch 반증 ③) — 사슬과 교점의 비대칭으로 기록 | ⏳ 예외 전수 미실시(거짓 경보 관측 대기) |
| **AS-C48** | **안드로이드 15 크롬 + Wacom Pro Pen 3E는 지우개 끝을 `buttons & 32`로 보내고, 그리는 내내 유효하다** (2026-08-27 web2-15 2-a) | ⚙️ web2 | ✅ **실기기 관측으로 확인됐다 — web2-11 1-d가 「확인 대상」으로 남긴 자리다**(DEFERRED 「지우개 끝 신호」 행을 이 관측이 닫는다). 사람이 진단 패널에서 읽었다: 「지우개로 그리는 동안 `buttons`가 `32`로 뜬다」. 일반 펜 접촉은 `buttons 1`이라 둘이 깨끗이 갈린다 — 추측할 것이 없다. ⚠ `button === 5`는 **누름·뗌 순간에만** 오므로 판정에 못 쓴다(진단 패널은 기록만 남긴다). 이 비트가 없으면 **절대 전환하지 않는다**(잘못 감지하면 그리다가 지워진다 — 「조용히 틀린 배치」의 지우개판). 팔: `e2e/tiperase.spec.ts` 넷 + 반증(코드에서 비트 판정을 실제로 빼 팔 둘이 죽는 것을 확인) | ✅ 실기기 확인 |
| **AS-C49** | **지우개 끝은 호버에 아무 신호도 안 보낸다 — 전환도 커서도 «닿아야» 뜬다** (2026-08-27 web2-15 2-a) | ⚙️ web2 | ✅ **실기기 관측**: 「지우개가 호버링만 할 땐 뜨지 않는다」. **다시 재지 않는다**(지시 문면). 두 귀결: ① **호버 미리보기가 불가능하다** — 「지금 지우개를 들고 있다」를 닿기 전에 못 보인다. 다음 사람이 「호버 커서가 안 뜬다」를 결함으로 오독하지 않게 HANDOFF에 적었다. ② **도구 상태를 바꾸는 설계가 불가능하다** — 전환이 접촉 순간에만 일어나므로 뗌에서 되돌려야 하고 그 복원이 어긋나는 경우(앱 정지·이벤트 유실·포인터 겹침)가 생긴다. 그래서 `pointerdown`에서 **그 획 하나만** 지우개 경로로 보낸다(`App.tipErase` — 매 접촉에서 다시 정해지므로 되돌릴 것이 없다). ⚠ 다른 기기·브라우저는 미측정 | ✅ 실기기 확인 |
| **AS-C50** | **축스냅된 획이 대기선을 만나는 자리는 «하나»이고, 그것이 사람이 겨눈 답이다**(겉보기 교차 — AutoCAD apparent intersection 선례) (2026-08-27 web2-15 1번) | ⚙️ web2 | 근거는 기하다(시작점+방향이 정해진 직선과 화면 위 선분은 많아야 한 점에서 만난다) + 선례(A-3 — AutoCAD·Rhino의 같은 오스냅). ⚠ **web2-14의 near 답이 이 행에 반증됐다**: 「몸통에 수직으로 붙인다」는 축스냅된 획의 끝을 축선에서 최대 오스냅 반경만큼 밀어내 `axisOfStroke`가 축을 잃게 하고 정의를 **조용히** 무산시킨다(원장 `xint_web2.json` — 죽은 38칸 중 37칸이 그 형태). 실측 폭: 구도 3 × 획 길이 4 × 손 오차 25 — 전 격자 300 중 **성립하는 275칸**(건너뛴 쌍 하나 = A_가까운VP×L140. 분모는 원장 `denominator`가 든다)에서 정의 237 → **275**, 반경 밖은 8 → 22/55. ⚠ **몸통 대역 한정**이다. ⚠⚠ **2차 리뷰가 결론 둘을 뒤집었다**: ㉠ 끝점·중점 대역에서 **기전 ①은 살아 있다** — 길이를 흔들고 세 대역을 다 훑은 `edge_band` **297칸 중 52칸이 축을 잃고 죽는다**(L40 28·L70 19·L110 5 / 반대끝 30·중점 18·시작끝 4. 죽은 칸의 종류는 end 34·mid 18이고 xint는 0). **이 가정은 `near` 통로에 한해서만 선다.** ㉡ **여유의 대가는 0이 아니다** — 대기선 둘의 교차가 구멍(8px)보다 가까우면 «안 겨눈» 선이 이기고 정의된다(`distractor` 1/20, gap 10). ⚠ 새로 닫힌 것: 문값이 조준선 방향으로 **정확히 8에서 갈린다**(`boundary_along_aim`) — 다만 «왜 8인가»는 여전히 미측정(#12). ⚠ **시점 축이 격자에 없다**(리뷰 [11]): 정의는 `atOwnPose`가 막지만(missed.pose) 스냅은 화면 좌표라 시점 문이 없어 «붙는데 정의는 안 서는» 조합이 가능하고 그것을 재는 칸이 없다. ⚠ **가정으로 남는 부분**: 구멍(오스냅 반경 8px)을 **축스냅된 끝**에서 재는 것이 실제 손에 맞는가 — 헤드리스는 「그 값으로 재면 275칸이 선다」까지만 말한다. 조준선을 따라 얼마나 못 미치는지의 실기기 대역은 미측정. ⚠ **web2-16 2부 갱신**: 위 ㉠(끝점·중점 대역의 기전 ① 생존·52칸)은 **해소됐다** — 축 걸린 획의 2D 특징점 오스냅이 축선에 사영되므로(수선의 발) 이제 그 대역에서도 축이 산다. 재실행 `xint_web2.json` edge_band: 현행 죽음 3(중점 off=20 — 의도가 갈린 자리·계수에 잡힘) · **w15 열이 그 52를 재현해 원장에 들었다**(L40 28·L70 19·L110 5 / end 34·mid 18). 「near 통로에 한해서만 선다」는 문장도 넓어졌다 — 이제 겉보기 교차(몸통)와 사영(특징점)이 같은 원리(축이 방향·오스냅이 위치)의 두 사례다. 3D 특징점은 #63 그대로(점이 이긴다 — 양 끝 3D라 축 없이 승격) | ⏳ 실기기 미측정(DEFERRED web2-15 표) |

## 규칙 기반 전환이 세운 가정 (2026-08-16)

| # | 가정 | 상태 |
|---|---|---|
| **AS-L13** | **사람은 축별로 몰아 긋는다**(한 방향의 선들을 이어서 긋는다) | ⚠⚠ **2026-08-17 재측정으로 방향이 뒤집혔다.** 옛 값은 몰아 6.93° · 번갈아 **32.57°**였고 '몰아 긋는다'가 **유리한** 가정이었다. `rule_camera.json@f351839a`(표제 표 = 1pt 항등 구도를 뺀 `headline_no_identity`, #5)에서는 **몰아 14.42° · 번갈아 9.82°** — **몰아 긋기가 더 나쁘다.** 즉 이 가정은 이제 **불리한** 가정이고, 사람이 실제로 몰아 긋는다면 축 오차가 그만큼 크다. ⚠ 뒤집힌 원인은 안 갈랐다(A의 화면 직교 스냅 · B의 '1점 선언은 첫 획에서만' · A-4의 3점 입구가 **같은 실행에 섞여 있다**, 리뷰어 [14]). ⚠ 설명 기전으로 쓰던 `first_pair_sep_deg_median`은 **원장 40여 곳 전부 `null`**이다(#38 덮는 대상 0) — 순서 설명 전체가 그 미기록 위에 있다. **실획 표본이 0이므로**(AS-C1) 사람이 어느 쪽으로 긋는지는 여전히 모른다. ⚠⚠ **2026-08-17 4차 D-L59(첫 소실점 = 두 선 교점)가 원장을 다시 움직였다** — 현행: 몰아 **31.61°** · 번갈아 **27.85°**(방향은 유지되나 격차가 좁아졌고 둘 다 커졌다 — 대가·사유는 D-L59). `first_pair_sep_deg_median`도 이제 기록된다(몰아 6.58° · 번갈아 39.85°) — 위의 '#38 덮는 대상 0' 문장은 그 시점의 것이다 | ⚠ **[5차 병기 — 재검 [28], [3-1] 정정]** 앱 동작점(스냅 팔)에서도 같은 방향이다: 물음 오라클 판 몰아 35.00°·번갈아 29.66° → **무물음 선언 판(지시 3, 현행) 둘 다 35.00°**(위약 31.61°/26.76°, `rule_camera.json@f351839a`). ⛔⛔ **제자리 정정(2026-08-18 8차 2차 지시 항목 0 · 8-2R [4] · #42 ⑥): 그 "현행 둘 다 35.00°"는 **8차 항목 1 직전(`d85dca8`)의 값**이다. 항목 1이 P1 잠김을 열면서 원장이 크게 움직였다 — 현행 스냅 팔은 **몰아 7.4653° · 번갈아 7.7742°**(위약 몰아 7.116 · 번갈아 8.0827)이고 **팔마다 값이 다르다.** ⚠ **방향(몰아가 더 나쁘다)도 이제 성립하지 않는다** — 스냅 팔에서는 몰아 7.4653 < 번갈아 7.7742로 **뒤집혔고**, 원시 팔에서는 몰아 7.9567 > 번갈아 7.3722로 유지된다. **AS-L13의 방향 판정은 팔에 따라 갈리므로 이 원장으로는 못 세운다.** ⚠ **[6차 주석]** 이 줄이 인용한 수치는 **6차 지시 11**(소실점은 **세 번째 선**이 정한다) **이전 실행**의 것이다 — 원장이 다시 움직였다(현행 값은 원장을 그 자리에서 읽는다, #1).
| **AS-L14** | **묻는 횟수가 사람이 견딜 만하다** | ⚠ **[7차 항목 2 대조]** 첫 실획의 사람 보고는 **6회/획 5**(원장 밖 — 파일 미도착, AS-C1 7차 주석)로 '너무 많다'였다. 끝점 오스냅 현의 vpdir 판정(D-L79 ② — 지지로만)이 모호 물음 일부를 대체하되, **P1 가드의 물음은 그대로 둔다**(ortho 강제는 리뷰어 2-R″ [1]로 기각 — 그 물음이 이 행 아래 가드의 존재 이유다). 합성 15px 팔 asks 불변·r40 팔만 감소 — 실사용 효과는 다음 표본 askStats가 판정한다. ⚠ **미검증.** 상자 하나(12획)에 물음이 **2.625(번갈아, rule_drawn)·2.589(몰아, rule_grouped)** 난다(`rule_camera.json@f351839a`의 `headline_no_identity` — ⛔⛔ **제자리 정정(2026-08-18 8차 2차 지시 항목 0 · 8-2R [4] · #42 ⑥): 옛 값 **3.755·2.964**는 8차 항목 1 직전의 것이다. 스냅 팔은 **2.182 / 2.255**다 — ⚠ 3·4-R [10]이 낡은 값(3.18/4.02, 대소도 반대)을 잡아 현행 원장으로 정정. 옛 값 이후 D-L59·6차 재실행들이 원장을 움직였다 — 오라클 팔 `rule_wide_pair`는 4.22이고 뺐다. 2026-08-17 3차의 "소실점 있는 상태의 가로선은 묻는다" 가드가 올린 값이다. 전 값 2.5~2.6은 직전 커밋 원장 `git show 47da6c4:stage0/out/rule_camera.json`에서 재현된다. 조용히 틀리는 문을 닫은 대가다). 물음은 조용히 틀리는 것을 막는 장치이지만(A-3) 너무 잦으면 도구가 안 쓰인다. **실사용 관측이어야 한다** ⚠⚠ **[D-L59 주석]** 이 항목이 인용한 수치는 4차 D-L59 이전 실행의 것이다 — 원장이 다시 움직였다(현행 값은 원장을 그 자리에서 읽는다, #1). |
| **AS-L16** | **축 스냅이 축 오차를 줄인다** | ⛔ **반증**(2026-08-16. ⚠ **수치는 2026-08-18 8차 재실행분이다** — 리뷰어 [1]의 확대 감사가 옛 값 8.8363/9.9543을 잡았다. **결론은 안 바뀌었고 수치만 움직였다**). 축 오차 중앙이 **스냅 켬/끔에서 완전히 같다** — 층별 값은 다르지만(번갈아 **6.4634°** · 몰아 **7.4792°**(지평선 켬) · **12.9107°**(지평선 끔) — 12차 D-L89 재실행 값. 항목 1 시점 값은 6.9189/7.8135였다) **각 층 안에서 켬 = 끔**이다(켬 6.4634 = 끔 6.4634 · 켬 7.4792 = 끔 7.4792). 소실점은 확정 후 잠기므로(CLAUDE.md §1) **뒤에 그은 선이 카메라에 기여하지 않는다.** 축 스냅이 바꾸는 것은 **배치의 형태**이지 카메라가 아니다. 사람 지시 4의 예상이 여기서 갈렸다. _초판의 "팔 넷에서 9.9569로 완전히 같다"는 재실행 전 값이었다_ ⛔ **제자리 정정(14차 항목 3 재실행 · 2차 [C] · #42 ⑨)**: 인용 수치가 다시 낡았다 — 현행 `axis_snap.json@f351839a`은 번갈아 **6.9537** · 몰아 **8.1649**이고 **등식(켬 = 끔)은 층마다 그대로 성립한다**(6.9537 = 6.9537 · 8.1649 = 8.1649). AS-L17 행이 든 위약 동일성 트리오도 현행은 **6.9537 / 1314 / 1121**이다(동일성 유지 — 값만 이동) |
| **AS-L17** | **축 스냅이 배치 품질을 올린다** | ⛔ **반증**(같은 원장 `axis_snap.json@f351839a`. ⚠ **수치는 2026-08-18 8차 재실행분** — 옛 1094/1322 · 1270/1322은 리뷰어 [1]이 잡았다. **방향은 그대로다**). 조용히 틀림(절단 0.2, 번갈아 긋기)이 **1040/1381(0.7531) → 1184/1381(0.8573)**로 **나빠진다**(몰아도 같은 방향: 1098/1405 → 1278/1405 — 12차 D-L89 재실행 값·방향 불변. 항목 1 시점 값 943/1086·분모 1273). ⛔⛔⛔ **철회한다 — 위 정정 시도가 틀렸다**(2026-08-18 8차 2차 지시 8-2R′ [1]). 초판은 "켬/끔이 `silent_0_2` 1091로 완전히 동일해져 이 반증이 무너졌다"고 적었다. **원장을 잘못 읽었다**: `snap_off__horizon_on__drawn.silent_0_2`는 **948**(rate 0.7435)이고 1091은 **`placebo_wrong_horizon__drawn`의 값**이다 — 위약 행을 끔 행으로 읽었다. **격차는 그대로 있고 AS-L17의 반증은 성립한다**(켬 1184/1381 = 0.8573 대 끔 1040/1381 = 0.7531 · 몰아 1278 대 1098 — 12차 D-L89 재실행 값). ⚠⚠ **왜 틀렸는지가 이 정정의 내용이다**: `placebo_wrong_horizon__drawn`이 `snap_on__horizon_on__drawn`과 **`deg_median`·`placed`·`silent_0_2` 셋 다 글자 그대로 같다**(12차 D-L89 재실행 값 6.4634 / 1381 / 1184 — 동일성은 그대로다) — **위약이 본 팔과 완전히 동일한 값을 낸다**는 것이고 그 자리는 **#40 ②·#5**다(0·1이 아니면서 자명한 값). 그 동일성이 오독을 만들었고, **그 위약 팔이 이 원장에서 정보를 안 나른다는 것 자체가 기록할 결함이다** — `DEFERRED` 등재. ⚠ 항목 1이 이 원장을 다시 돌린 것은 사실이다(`camera_ok_rate` 0.9792 → **0.8792** · `axes_measured` 301 → 384) — **모집단은 바뀌었고 결론은 안 바뀌었다.** 축 스냅은 획을 축에 정확히 붙이므로 **카메라 오차를 고스란히 물려받는다** — 스냅이 없으면 획마다 다른 방향이 들어와 `liftAll`의 전역 해에서 일부 상쇄된다. ⚠ **조건부다**: 이 픽스처의 카메라 축 오차가 중앙 6.4634~12.9107°(12차 D-L89 값)로 임계(0.5°)의 **13~26배**다. **카메라가 정확해지면 부호가 뒤집힐 수 있고 그것은 안 쟀다** ⚠ **8차 항목 1이 카메라를 실제로 고쳤다**(P1 잠김) — 이 조건부를 다시 재는 것이 다음 자리다. ⛔⛔ **제자리 정정(2026-08-18 8차 2차 지시 항목 0 · #42 ⑨ — `git show d233ec4 --stat -- stage0/out/`으로 항목 1이 건드린 원장 열여섯을 훑었다)**: 항목 1은 이 원장도 다시 돌렸고 **`axes_measured` 301 → 384 · `camera_ok_rate` 0.9792 → 0.8792**로 움직였다. **AS-L16의 결론(켬 = 끔)은 그대로다** — 층 안에서 여전히 완전히 같다(12차 D-L89 재실행 값 6.4634 = 6.4634 · 7.4792 = 7.4792 · 12.9107 = 12.9107). **바뀐 것은 AS-L17 쪽이다**(아래 행) ⛔ **제자리 정정(14차 항목 3 재실행 · 리뷰어 2차 [6] · #42 ⑥)**: 인용 수치가 다시 낡았다 — 현행 `axis_snap.json@f351839a`은 켬 **1121/1314** 대 끔 **978/1314**(절단 0.2, 번갈아) · 몰아 켬 **1247/1369** 대 끔 **1067/1369**다. **방향(켬이 나쁘다)은 그대로고 반증은 성립한다** |
| **AS-L18** | **모호 판정이 결과를 바꾼다** | ⚠⚠ **이 하네스로는 못 잰다 — 배치 불변은 측정이 아니라 구성의 귀결이다**(#32·#5, 2026-08-17 리뷰어 [6]). 하네스가 발동을 **세기만 하고** 배치는 발동 여부와 무관하게 `pick`으로 놓는다(`axis_snap_measure.test.ts` — 발동이 배치 경로에 **안 닿는다**). 그래서 스윕 전 행에서 배치·조용히 틀림이 글자 그대로 불변인 것은(⚠ 옛 인용 1322·1270은 8차 재실행으로 낡았다 — 현행은 원장을 그 자리에서 읽는다, 리뷰어 [1]) **당연하고 정보량이 0이다.** 앱에서는 발동이 **물음**이 되므로 결과를 바꿀 수 있다 — 그 효과는 이 하네스 밖이다. **남는 측정은 발동률과 정확도뿐**(`axis_snap.json@f351839a`): 임계 0~24°에서 발동과 정확도를 낸다 — ⚠ **수치는 `ambiguous_sweep`의 `deg_0`~`deg_24`를 그 자리에서 읽는다**(옛 인용 "0 → 534/1100 · 최대 0.1037"은 8차 재실행으로 낡았다, 리뷰어 [1]) — 낮고, 그것이 신호의 성질인지 카메라 축이 이미 9~13° 틀려 "참 축" 대응이 흔들리는 탓인지 안 갈랐다 _(초판의 "0 → 1161 · 정확도 0.047"은 재실행 전 값. "이 픽스처에서는 안 바꾼다"라던 첫 결론은 #32로 철회 — 미실행을 반증으로 읽은 것이다)_ ⚠ **[6차 주석]** 이 줄이 인용한 수치는 **6차 지시 11**(소실점은 **세 번째 선**이 정한다) **이전 실행**의 것이다 — 원장이 다시 움직였다(현행 값은 원장을 그 자리에서 읽는다, #1). |
| **AS-L19** | **지평선이 맞으면 축 오차가 준다**(D-L45의 전제) | ♻️ **되살아났다**(2026-08-20 18차 · D-L109): 새 절차가 지평선을 **다시 소실점 확정의 입력**으로 만들었으므로 이 가정의 기전이 돌아왔고, `horizon_pitch.json`의 세 팔이 다시 갈린다 — 참 **3.4499°** · 중앙 **21.1871°** · 위약 **37.4237°**(몰아 긋기). **그러므로 가정은 이 픽스처에서 지지된다.** ⚠ 다만 그 값들은 **참 지평선**(손 오차 없음)에서 나온 것이라 «사람이 그은 지평선»에 대한 판정이 아니다(`DEFERRED.md` 18차). ⚠ 그 원장의 조항 ①(중앙 팔의 차수 3이 0)은 **여전히 도달 불가**다 — 수심 유도는 D-L47에서 제거됐고 이 항목이 안 되돌렸다. ⛔ **아래는 폐기됐던 시기의 기록이다**: ⛔ 폐기 — 대상 소멸(2026-08-17 5차 이월-3): D-L59로 지평선이 소실점 확정에 안 쓰여(결과다) 이 가정의 기전 자체가 없다. 세 팔 동일값(horizon_pitch 은퇴 — 역사 기록). 찍기 경로가 잇고 그 측정은 **AS-L23·`pick_vp.json@f351839a`**이다. 아래는 폐기 전 마지막 관측: 성립하나 전제의 절반이 폐기됐다(`horizon_pitch.json@f351839a`, 2026-08-17 A-4로 재측정). **살아 있는 절반**: 참 지평선 팔의 축 오차 중앙이 **3.39°**(몰아 긋기) · **4.88°**(번갈아)이고 중앙 지평선은 **21.60°** · **12.26°**, 위약(중앙에서 참과 같은 크기·반대 방향)은 **31.61°** · **33.39°**다 — 참이 중앙보다·위약보다 낫다(등록 조건 ②·③, **두 순서 전부 통과**). **⭐ 허용 오차 곡선**도 살아 있다: 참에서 **±120px까지** 3.39 → **5.52°**로 완만하고 전 구간이 중앙 팔보다 낫다. ⚠⚠ **폐기된 절반**: '3점 투시가 초기부터 선다'는 **지평선과 무관해졌다** — 차수 3인 실행이 참·중앙·위약 **세 팔 전부 80~85/144**다(옛 판은 중앙 0/144 · 참 141/144였다). A-4가 수심 유도를 뺐고 3점은 이제 **사용자가 기울어진 세로 모서리를 '수직축'이라 답할 때** 선다. 그러므로 **등록 조건 ①은 성립하지 않고 성립해서도 안 된다**(그 조건이 잰 기전이 없어졌다). ⚠ 세 팔 전부 **물음에 참 축으로 답한 오라클** 위의 값이라 어느 팔도 실사용 성능이 아니다. ⚠⚠ **중앙값 하나 위의 결론이다**(리뷰어 [12]): p90은 참 30.79 대 중앙 36.12(1.17배)이고 **max는 참이 더 나쁘다**(53.92 대 47.10). 꼬리에서는 이득이 사라진다. ⚠⚠ **조건 ③의 두 팔은 같은 모집단이 아니다**(리뷰어 [13]): 위약은 카메라가 선 실행이 **30/144**(축 66개)이고 참은 **96/144**(축 228개)다 — 위약의 중앙값은 살아남은 21%만의 값이다(#11 — 2026-08-17 3차 재실행 값). ⚠ **사람이 그 창에 드는지는 여전히 표본 0**이다(AS-L9). ⛔⛔ **제자리 정정(2026-08-18 8차 2차 지시 항목 0 · #42 ⑨ — `git show d233ec4 --stat -- stage0/out/`으로 항목 1이 건드린 원장 열여섯을 훑었다)**: 이 항목이 인용한 수치는 **전부 8차 항목 1 이전**이다(`horizon_pitch.json`도 그 커밋이 다시 돌렸다 — `deg_median` **31.6083 → 7.068**). 항목이 이미 **폐기(대상 소멸)**라 값을 되읽지 않았고, **되읽어도 결론이 안 바뀐다**(기전 자체가 없다). ⛔⛔ **2026-08-17 4차 D-L59로 '살아 있는 절반'도 폐기됐다** — 지평선이 소실점 확정에 안 쓰이므로(첫 소실점 = 두 선 교점, 지평선 = 그 y) 참·중앙·위약 세 팔이 **동일한 값**(31.61°)을 낸다. 이 항목이 재던 기전 자체가 제거됐고 허용 오차 곡선도 함께 무의미해졌다(`tolerance_px: null`). 위 수치는 전부 D-L59 이전 실행의 기록이다 ⚠ **[6차 주석]** 이 줄이 인용한 수치는 **6차 지시 11**(소실점은 **세 번째 선**이 정한다) **이전 실행**의 것이다 — 원장이 다시 움직였다(현행 값은 원장을 그 자리에서 읽는다, #1). |
| **AS-L20** | **3점 투시의 입구는 '기울어진 세로 모서리를 수직축이라 답하는 것'이고, 그 선(들)에서 읽은 V₃가 쓸 만하다**(2026-08-17 A-4의 전제) | ⚠ **부분 검증**. 선이 **둘 이상**이면 최소제곱 교점이고 (`lsIntersection`), 하나뿐이면 **주점 x = 이미지 중심**(16.2)을 써서 `∩ (x = W/2)`로 푼다. ⚠⚠ **초판이 언제나 후자였고 측정이 기각했다**: 수직 소실점은 화면에서 아주 멀어 한 선을 x = W/2까지 **외삽**하면 각 오차가 크게 증폭된다 — 몰아 긋기 축 오차 중앙 **37.7°**. 그리고 손으로 그은 세로선은 **4~8°(애매 구간)**로 흔히 떨어지므로 **손 오차가 3점을 만들었다**. 지금은 ① 선 하나로 선언하려면 기울기가 **`depth`(≥8°)**여야 하고 ② 둘 이상이면 교점으로 푼다. 그 뒤 축 오차가 3.39~4.88°다. ⛔ **제자리 정정(2026-08-18 8차 2차 지시 8-2R′ [6] · #42 ⑨)**: 그 **3.39~4.88°는 현행 원장에 없다** — 8차 항목 1이 `horizon_pitch.json`도 다시 돌렸고 현행은 **전 팔 7.068(몰아) / 6.5846(번갈아)**다(12차 재실행 값). ⛔ 제자리 정정(14차 항목 3 재실행 · 리뷰어 2차 [6]): 현행 `horizon_pitch.json@f351839a`은 **6.6685(몰아) / 6.221(번갈아)**다 — 결론 불변. ⚠ 이 인용에 `@해시`가 없어 `scan_cited_values`의 **모집단 밖**이었다(②형) — **자동이 아니라 사람이 잡았다.** ⚠⚠ **초판이 여기에 3.39~4.88°를 적었고 그것은 다른 기전의 값이다**(리뷰어 [10]) — 그 수치는 `horizon_pitch.json`의 **참 지평선 팔**이고 '지평선을 참값에 놓았을 때'의 축 오차이지 '기울어진 세로 모서리로 V₃를 세웠을 때'의 정확도가 아니다. **그것을 가르는 팔이 원장에 없다.** ⚠ 있는 값도 전부 **오라클**(물음에 참 축으로 답함) 위이고, **사람이 그 물음에 옳게 답하는지는 표본 0**이다 |
| **AS-L15** | **화면 축 임계 4°가 손으로 그은 수평선을 담는다** | ⚠ **부분 반증.** 잡음 0.03~0.05에서 수평 모서리가 8°를 넘어 **깊이선으로 조용히 읽히는 실행이 있다**(1점 구도에서 가짜 소실점이 생겼다). 8° 밖은 애매가 아니라 깊이이므로 **묻지 않는다** — 그 자리가 이 규칙의 조용히 틀리는 문이(⚙️ **12차 D-L89 갱신 — 3차 리뷰어 [11]**: 그 무물음 구간이 **4°부터로 넓어졌다** — 애매 대역(4~8°)도 이제 묻지 않고 깊이선이다. 조용히 틀리는 문이 그만큼 넓어진 것이고, 합성에서 사라진 물음의 크기는 progress 12차 항목 4 재정정 절(fixed 팔 1121건)이 든다)다. 실획의 자릿수를 모른다(AS-C1). [6차 항목 2 대조 — 재검 [B-8]] 1점 직접 경로도 이 문을 공유한다 — 축 판정(스냅 선언·화면 축 임계)이 앞단이고 직접 좌표는 그 뒤의 배치일 뿐이다. 판정이 틀리면 직접 경로는 그 틀린 축의 좌표를 정확하게 낸다 |
| **AS-L21** | **터치 제스처를 직접 처리하면 카메라 수학을 다시 짜야 한다**(2026-08-17 G의 사전 예상 — **반증됐다**) | HANDOFF가 후보 ③의 함정으로 "three 최신판은 `rotateLeft` 같은 공개 API가 없다"를 적어 뒀고, 그것이 사실이면 궤도·팬·줌을 `Spherical`로 새로 구현해야 했다(A-3의 "선례를 따른다"와 어긋나는 자리). **측정이 기각했다**: three(판 번호는 `package.json`이 든다 — 여기 안 박는다, #47)의 `OrbitControls`에 `rotateLeft`·`rotateUp`·`pan`·`dollyIn`·`dollyOut`이 **공개 메서드로 다섯 다 있다**(`touch_route.json@f351839a`의 `candidates.c3_direct.public_api`). 그래서 **라우팅만 직접 하고 카메라 수학은 그대로 쓴다** — 궤도 중심·감쇠·각도 한계가 한 군데에 남는다. ⚠ 그 팔이 확인한 **작동**은 회전 하나다(줌은 확정 카메라에서 궤도 반지름이 0이라 못 쟀고 팬은 값이 없다) — 줌·팬은 앱 팔이 낸다 |
| **AS-L22** | **첫 소실점을 정한 깊이선(짝의 두 선, 또는 점 찍기의 호스트 선)은 수평 방향의 상이다**(D-L59 ②·D-L60의 전제 — 그래야 "지평선 = 그 y"가 이론서 3.1이다) | ❓ **미검증**(2026-08-17 4차, 항목 3 리뷰어 [6] 등재 · 항목 4 리뷰어 [2]가 찍기 경로까지 넓혔다 — 찍기는 선 **하나**라 전제가 더 약하다: 그 선이 경사선이면 어긋남을 잡을 짝조차 없다). 규칙의 분류는 깊이/화면축뿐이라(8° 임계) **경사 모서리(지붕·계단·램프)도 깊이선**이고, 그 교점·투영점은 지평선이 아니라 경사 소실선 위다(이론서 3.3) — 그런 선으로 첫 소실점이 서면 지평선이 틀린 높이에 굳는다. 사용자가 첫 대각선을 수평 방향으로 긋는지는 실획 표본이 가른다(K) |
| **AS-L23** | **찍기 확정(pickVpAt)의 축 오차는 찍기 위치 오차에 둔감하고, 교점 경로(D-L59)보다 낫다**(4차 지시 4-b의 전제) | ⚠ **합성에서 성립·실사용 미검증**(`pick_vp.json@f351839a`, 5차 이월-3). 찍기 0 팔 중앙 **2.67°** 대 two_lines **31.61°**(같은 픽스처·같은 물음 오라클), 보조선 방향 ±120px 스윕에서 중앙 2.54~3.99°로 평탄(tolerance_px 120). ⛔⛔ **제자리 정정(2026-08-18 8차 2차 지시 항목 0 · #42 ⑨ — `git show d233ec4 --stat -- stage0/out/`으로 항목 1이 건드린 원장 열여섯을 훑었다)**: 앞 두 값이 낡았다 — 현행은 **찍기 0 팔 1.985° 대 two_lines 7.4804°**다. ⚠⚠ **격차가 11.8배에서 3.8배로 줄었다** — 항목 1이 `two_lines` 경로를 크게 고쳤기 때문이다(31.61 → 7.48). **결론(찍기가 낫다)의 방향은 유지되나 크기는 3분의 1이다.** 찍기 실패는 **44/240 그대로**이고(⛔ 10차 제자리 정정: D-L85(반경 8)로 **70/240** — 찍기 조리개가 오스냅 반경과 한 값이다. 원장을 읽는다) `camera_ok_rate`는 two_lines 0.9917 → **0.8667**로 나빠졌다. ⛔ **제자리 정정(10차 리뷰어 4차 [3] · #42 ⑥)**: 앞의 정정이 `pick_failed`만 갈고 같은 문장의 세 수를 남겼다. D-L85 재실행 현행(원장을 그 자리에서 읽었다): 찍기 0 팔 중앙 **2.1556°** 대 two_lines **7.4804°**(= **약 3.5배** — "3.8배"도 낡은 값이다) · `pick_0`의 `axes_measured` **425 → 411**(⛔ 12차 D-L89 재실행 현행 **410** — 3차 [10]). ⛔ **제자리 정정(12차 화각 상한 재실행 · #42 ⑥)**: 현행(12차 D-L89 재실행)은 찍기 0 팔 **2.1252°** 대 two_lines **7.1224°**(약 3.4배) · two_lines `axes_measured` **396**이다(`pick_vp.json@f351839a` — 결론 불변). ⛔ 제자리 정정(14차 항목 3 재실행 · 리뷰어 2차 [6]): 현행은 찍기 0 팔 **2.2252°** 대 two_lines **7.8653°**(약 3.5배) · two_lines `axes_measured` **404** · pick_0 **410**이다 — 결론 불변. 결론(찍기가 낫다·분모 불일치 규약 위반)은 그대로다. ⚠⚠ **두 중앙은 분모가 다르다**(8-2R′ [10] — 이 항목이 `factor_split`에 세운 규약을 여기서 안 지켰다): `pick_0`은 `camera_ok` **240** · `axes_measured` **425**(⛔ 5차 [3]: 현행 **411** — 같은 행 앞의 정정이 이 자리를 빼먹었다, #34), `two_lines`는 **208** · **377**이고, 게다가 `pick_0`의 중앙은 **`pick_failed` 44(현행 70)를 뺀 것이 아니다.** **그러므로 "3.8배"는 층 내 개선과 층 구성 변화를 안 가른다**(#9) — 분모가 같은 지표가 이 원장에 없다. `DEFERRED` 등재. 원인 후보(안 갈랐다): 선상 이동은 |V−P|만 바꿔 축 기울기 민감도가 f/(f²+D²)≈0.7~3.4°/120px. ⚠ 찍기 실패 44/240(⛔ 5차 [3] 현행: **70/240** · 호스트 선 **8px** 밖 — `pick_tol_px`가 오스냅 반경과 한 값이라 D-L85로 함께 줄었다. 잉크 오차가 크면 참 자리가 선 밖) · pick_0은 오라클(#35, 사람 찍기 정확도 표본 0 = AS-L9 자리) · fwd 120의 실패 증가는 보조선 길이(참 너머 10%)의 픽스처 산물이다 ⚠ **[6차 주석]** 이 줄이 인용한 수치는 **6차 지시 11**(소실점은 **세 번째 선**이 정한다) **이전 실행**의 것이다 — 원장이 다시 움직였다(현행 값은 원장을 그 자리에서 읽는다, #1). |
| **AS-L24** | **확정 전 2D 판정(오스냅·직교·vp_dir·정렬)이 규칙 카메라의 축 오차를 개선한다**(이월-2 지시의 암묵 전제 — "하네스가 낡았다"의 기대 방향) | ⛔ **반증**(합성, `rule_camera.json@f351839a`, 5차): 스냅 팔이 원시보다 같거나 나쁘고(물음 오라클 판: 몰아 31.61→35.00·번갈아 27.85→29.66 / **무물음 선언 판(지시 3, 현행): 둘 다 →35.00, 번갈아 격차 7.15°** — 재검 [3-1] 정정), **위약(같은 크기 임의 방향)은 원시와 같다** — 나빠짐은 스냅 방향의 체계 효과다. 짝지은 부분집합에서도 방향 유지. ⚠ 시드 폭 미확인(#14)·합성 잉크는 짝 미의도(AS-L13) — 실획(K)이 최종 판정자. 지시 3(스냅 수평선 무물음 확정)이 물음 오라클 자리를 바꾸므로 3-c에서 재측정한다 ⚠⚠ **[7차 지시 1-a 재측정]** 강제 선언을 지운 현행 판(D-L70)에서는 **원시↔스냅 격차가 거의 사라졌다**(`rule_camera.json`의 `headline_no_identity`: 번갈아 31.6083 → 31.6083 · 몰아 35 → 35) — 즉 **나빠짐의 출처는 스냅 좌표가 아니라 강제 선언이었다.** 이 가정('스냅이 개선한다')은 여전히 반증이지만 '스냅이 악화시킨다'도 이제 못 적는다. ⛔⛔ **제자리 정정(2026-08-18 8차 2차 지시 항목 0 · 8-2R [4] · #42 ⑥): 위 "현행 판"의 두 값은 **8차 항목 1 직전(`d85dca8`)의 것**이고, 항목 1 이후 **짝지은 층에서 부호가 세 팔 중 둘에서 뒤집혔다**(`gate.result.paired`): 번갈아(drawn) **7.3342 → 7.7742**(스냅이 **나쁘다**) · 몰아(grouped) **7.8135 → 7.4653**(스냅이 **좋다**) · wide_pair **8.7129 → 8.0003**(스냅이 **좋다**). ⚠⚠ **그러므로 '격차가 사라졌다'는 더 이상 참이 아니고, 반증도 지지도 이 원장으로는 못 세운다** — 팔에 따라 부호가 갈린다(#12 동작점 하나). **다시 열어 둔다.** ⛔⛔ **그리고 위약이 그 '좋아짐'을 통째로 삼킨다**(8-2R′ [3] · **#39의 나머지 절반**): 같은 크기·임의 방향으로 옮긴 위약 팔이 **`rule_grouped_placebo` 7.116** · `rule_drawn_placebo` 8.0827 · `rule_wide_pair_placebo` 9.724다. **몰아 위약 7.116은 스냅 팔 7.4653보다도 좋다** — 즉 "몰아에서 스냅이 개선한다"로 읽은 것은 **같은 크기의 무의미한 이동으로도(더 잘) 재현된다.** 재고 있는 것은 **스냅의 방향**이 아니라 **그 크기의 이동 일반**일 수 있고, 그것이 #39가 규정한 자리다. **AS-L24는 지지도 반증도 없이 열려 있다.** ⚠ **[6차 주석]** 이 줄이 인용한 수치는 **6차 지시 11**(소실점은 **세 번째 선**이 정한다) **이전 실행**의 것이다 — 원장이 다시 움직였다(현행 값은 원장을 그 자리에서 읽는다, #1). | ⛔⛔ **제자리 정정(2026-08-18 9차 항목 0 · D-L80 · #42 ⑥)**: `resolve2dCore`에 **판정 뒤집기 가드**가 들어가 이 원장을 다시 돌렸다. **반증이 부분적으로 뒤집혔다** — 현행 `headline_no_identity`(그 자리에서 읽었다): 몰아 원시 **7.9567**(불변) → 스냅 **6.9314**(**좋아진다**) · 번갈아 원시 **7.3722**(불변) → 스냅 **8.33**(**나빠진다**) · 넓은짝 원시 **9.0578**(불변) → 스냅 **7.5135**(좋아진다). 즉 **셋 중 둘에서 스냅이 이긴다.** ⚠ 위약(같은 크기 임의 방향)은 몰아 **7.1615**이므로 **스냅(6.9314)이 위약도 이긴다** — 이것이 8차까지 못 하던 것이다. ⚠⚠ **AS-L24를 참으로 뒤집지 않는다**: 번갈아 팔은 여전히 나빠지고 시드 폭은 미측정이며(#14) **합성이다**(AS-L25 계열). 남는 판정은 **"스냅의 손해는 가드가 만든 것이었다"** 쪽이다. ⛔⛔ **제자리 정정(10차 항목 4 · D-L85 반경 8 · #42 ⑥)**: 다시 뒤집혔다 — 현행 `headline_no_identity`(그 자리에서 읽었다): 몰아 스냅 **7.3165** · **위약 6.7125가 스냅을 다시 이긴다**(#39 복귀 — ⚠ **몰아 팔 하나에서다**(리뷰어 4차 [10]): 번갈아는 위약 **8.4515 > 스냅 7.5752**, 넓은짝은 위약 **10.3657 > 스냅 8.3491**로 **두 팔에서는 스냅이 위약을 이긴다**. "위약 재역전"을 전 팔의 사실처럼 적지 않는다. ⚠ **스냅↔위약 비교는 짝지은 층이 아니다**(리뷰어 5차 [15] · #11): 두 팔의 `axes_measured`가 최대 14축 다르다(341↔327 등) — `paired_headline`은 raw↔snap에만 있었다. ⚠ **절반 해소(11차 항목 5 · 리뷰어 [2])**: `paired3_headline`(셋 다 카메라가 선 픽스처)이 생겼고 **판정이 같다**(번갈아·넓은짝은 스냅이 이기고 몰아만 진다). ⛔ **그러나 '짝지음 부재의 산물이 아니다'로는 못 읽는다** — 짝짓기가 걷어낸 픽스처가 팔당 **1~3개**뿐이고 **몰아 팔은 두 중앙값이 비짝지음 표와 글자 그대로 같다**(원장의 `dropped_by_pairing`·`medians_moved_by_pairing`). 말할 수 있는 것은 **'이 짝짓기에 판정이 둔감하다'**까지다. ⚠ 축 수는 여전히 팔마다 다르다(무한원 축 제외)) · 번갈아 스냅 7.5752 · 넓은짝 스냅 **8.3491(원시 9.0578보다는 낫지만 9차보다 나빠졌다)**. 그리고 같은 원장의 r4 **6.9304**·r40 **6.9637**이 r8보다 낫다(시드 폭 미측정 #14). **"가드가 만든 것"이라는 9차의 판정도 이제 못 세운다 — 반경과 가드가 얽혀 있다. AS-L24는 계속 열려 있다**(D-L85 대가 ⑤·⑥). ⛔ **제자리 정정(2026-08-18 12차 항목 1 — 화각 상한 `FOV_GATE.reject_fov_deg` 등록 재실행 · #42 ⑥⑨, 12차 리뷰어 [3])**: 현행 `headline_no_identity`(12차 D-L89 재실행 — 그 자리에서 읽었다): 몰아 스냅 **7.068** · 위약 **6.6539**(위약이 이긴다 — D-L85 재역전 유지) · 번갈아 스냅 **7.3134** · 위약 **7.5944**(스냅이 이긴다) · 넓은짝 스냅 **8.0867** · 위약 **10.1448**(스냅이 이긴다) · 원시 몰아 **8.0056**/번갈아 **7.3226**/넓은짝 **9.1867** · r4 **6.7929** · r40 **6.9124**(둘 다 r8 = 몰아 스냅 7.068보다 낫다 — 방향 유지). **판정 방향은 전부 그대로이고 수만 움직였다.** ⚙️ **2026-08-19 표본 도착 주석(지시 0-f · 13차 리뷰어 [1][2]로 정정)**: 실획에서 이 가정의 대응량(3D 축 방향 오차)은 여전히 없다(#24 — `real_ink.pending_judgements.as_l24_l25_synthetic_bias`는 그대로 막힘). ⛔ 초판 주석의 "스냅이 잡힌 획만 Δ≈0"은 **철회한다** — 유일한 Δ=0(s78)은 정확히 0 = **보장**으로 갈리는 값이라(#5, 원장의 guaranteed 표식 — 자기 소실점의 항등이든 되쓰기의 항등이든 구조 산물) 증거가 아니고, 08-17 파일의 on_face 스냅 획들은 스냅인데 Δ가 0이 아니다 — ⚠ 다만 그 기록은 옛 on_face 자동 부착(legacy 오염)의 것이라 반례의 사거리는 '자동 on_face는 방향을 정착시키지 않는다'까지다(13차 리뷰어 2차 [7]). **철회의 주 근거는 #5(보장) 하나로 선다.** 획별 스냅 표식은 이제 `per_doc[*].vp_dir_err_strokes` 행에 있다 — 상관 주장은 그 필드로만 한다. ⛔ **제자리 정정(2026-08-19 14차 항목 1 — 화각 상한 제거 D-L93 재실행 · #42 ⑥)**: 원장이 다시 움직였고 **번갈아(drawn) 팔의 부호가 또 뒤집혔다**(그 시점 값: drawn 스냅 7.3232 ↔ 위약 7.2127 — 여유 0.11°는 시드 폭 미측정 #14. camera_ok 226 → 204 — D-L93 대가 절). ⛔⛔ **제자리 정정(같은 날 14차 항목 3 · D-L96 재실행)**: **스냅 팔이 통째로 무너졌다** — 현행(그 자리에서 읽음): `rule_drawn_snap` **31.6083** · `rule_grouped_snap` **33.2719**(원시 몰아 7.6889는 불변). 기전은 스냅 품질이 아니라 **선언 게이트**다: 스냅 모사가 얕은 참-축 획을 직교 스냅으로 눕혀 가로축을 **선언**하고, 깊이선 둘 확정(D-L96)이 그 아래서 한 축의 이중 예약을 굳힌다. **이 값들로 AS-L24(스냅이 축 오차를 개선/악화)를 판정하지 않는다** — 재는 것이 스냅의 방향 효과가 아니라 D-L96 선언 규칙이 됐다(#24 — 같은 지표가 다른 것을 재게 됨). 실획이 판정자. **AS-L24는 계속 열려 있다** — 어느 부호도 근거로 인용하지 않는다. |
| **AS-L25** | **직교 스냅이 걸린 선의 무물음 화면 축 선언(5차 지시 3)이 실사용에서 이득이다**(전제: 스냅으로 수평이 된 선은 수평 의도다) | ⚠ **합성에서는 불리하다**(`rule_camera.json@f351839a`, 5차): 합성 잉크는 수평 의도가 없어 4° 안 깊이 모서리가 조용히 화면 축이 되고, 스냅 팔 축 오차가 전부 35°(물음 오라클이 정정하던 것이 사라짐 — ⛔⛔ **제자리 정정(2026-08-18 8차 2차 지시 항목 0 · 8-2R [4] · #42 ⑥): 그 "전부 35°"는 8차 항목 1 직전의 값이다. **현행은 팔마다 다르다**: 7.7742 / 7.4653 / 8.0003 / 6.8811 / 10.557)·camera_ok 몰아 167→177·**번갈아 109→157** ⛔ **제자리 정정(8-2R′ [8])**: 그 네 수는 **현행 원장에 없다** — 괄호 안 각도만 고치고 이 쌍은 그대로 뒀던 자리다(#42 ⑤). 현행 `camera_ok`는 `headline` 211·211·211·197·194·205 · `headline_no_identity` 192·192·192·182·182·184이고 **167/177/109/157에 대응하는 팔·모집단을 못 찾았다**(어느 실행의 값인지 미상 — #25). **그러므로 이 문장의 "더 서고 더 틀린다"는 근거를 잃었다**(더 서고 **축 오차로** 더 틀린다, #16 — 8-R′: 최대 팔 병기·용어 정정). **합성은 이 설계의 판정자가 아니다** — 의도가 없는 입력이기 때문(AS-L13과 같은 뿌리). 실획(K)이 판정자다. 설계는 사람 지시로 유지 ⚙️ **12차 항목 4(D-L89) 주석**: 무물음 선언이 **가드된 형태로 되살아났다** — 5차 판(D-L56)과 달리 kindFlipGuard(D-L80)가 원 방향 depth 획에는 직교 스냅을 안 걸므로, 무물음 통과(hint "screen")는 원 방향 ≤4° 획에만 붙는다. 이 전제("스냅으로 수평이 된 선은 수평 의도다")는 다시 현행 설계의 전제이고 **판정자는 여전히 실획이다**(12차 보고 asked 3/13·3/11이 물음 잔존의 실측이었고 D-L89가 그 물음을 지웠다 — 다음 표본이 이 전제의 첫 실측이 된다) ⚠ **[6차 주석]** 이 줄이 인용한 수치는 **6차 지시 11**(소실점은 **세 번째 선**이 정한다) **이전 실행**의 것이다 — 원장이 다시 움직였다(현행 값은 원장을 그 자리에서 읽는다, #1). | ⛔⛔ **제자리 정정(2026-08-18 9차 항목 0 · D-L80)**: 이 항목이 지목한 기전(**4° 안 깊이 모서리가 조용히 화면 축이 된다**)의 **주된 경로가 `classifyLine`이 아니었다**. `classify_leak.json`이 갈랐다 — 원시 각으로만 판정하면 누수가 **1035/2880**이고, 앱처럼 `resolve2dCore`를 지나면 **1284/2880**으로 는다. 그 차이 **353**획 중 **108**획은 원본이 **8° 밖**(= `depth`)인데 스냅이 화면 축으로 옮긴 **모순**이고, 그 71획이 **1점 구도**에 있다. 가드를 넣어 그 모순을 **108 → 72**(1점 71 → 55)로 줄였다. **남은 72는 끝점 오스냅이 만든다** — 그것은 항목 2(반경)의 대상이다(`ablation`의 `r8_rel_on` 35/2880이 그 지렛대를 보인다). ⚙️ **2026-08-19 표본 도착 주석**: 표본 셋은 전부 **D-L89 이전 저장본**이다 — 파일별 `ask`(`real_ink.json@f351839a`의 `k_metrics.per_doc[*].ask`)가 12차 보고(asked 3/13·3/11)를 파일로 확인했다(물음 잔존의 실측 — D-L89의 동기). D-L89 이후 물음 체제의 첫 실측은 여전히 다음 표본이다.

## 무효 (L 전환)

접근이 바뀌어 **대상이 사라진** 항목. 지우지 않고 여기 적는다 —
왜 세 번째로 접근을 바꿨는지가 그 항목들에 있다.

| 무효 항목 | 무효 사유 |
|---|---|
| AS-2(떨림 평면) · AS-3(미리보기-확정 차이) · AS-8(앵커 체인 누적) · AS-16(화면 접합) · AS-17(`pts3d` 파생) | **배치 계층이 폐기됐다.** 시작점이 스냅으로 확정되므로 평면 선택도 앵커 체인도 없다. `pts2d`는 계속 보존하므로 AS-17의 결론(재생성으로는 "그 3D"가 안 나온다)은 **저장 형식 규칙으로만** 남는다 |
| AS-5(면 위 사선 비율) | 축 밖 획을 사후에 추론하지 않는다. 사용자가 그리는 중에 축을 본다. ⚠ **2026-08-16: 그 원장(`face_diagonal.json`)이 D-L28의 근거로 다시 쓰였다** — 무효 표시된 가정 위의 측정을 판단에 쓴 것이고, D-L1(아카이브를 근거로 인용하지 않는다)의 경계에 있다. **결론을 그만큼 약하게 적었고**(짝지은 비교가 아니다) §6(면)을 열 때 그 조건에서 다시 잰다. 상수 해시가 현재값이라 **STALE로는 안 잡힌다** |
| AS-18~AS-20(세그먼트) | 세그먼트 실험이 폐기됐다. **AS-20의 반증이 L 전환의 근거**이므로 아카이브에 남는다 |
| AS-14·AS-15(체계적 왜곡) | 판정을 사후에 하지 않으므로 왜곡 보정의 대상이 없다. 다만 **소실점 검출(§5.1)의 산포 임계**가 왜곡을 흡수해야 한다 → AS-L1이 그 자리다 |
| AS-4(면 없이 형태가 읽힌다) | 여전히 유효하나 **판단 근거가 좁다**(구도 하나·물체 하나·관찰자 하나). 면 생성은 L 범위 밖이다 |

## 2026-08-18 6차 지시 (AS-L26 ~ AS-L28)

| # | 전제 | 관측 |
|---|---|---|
| **AS-L26** | **손 오차가 작다** — 사용자는 소실점을 향해 정확히 긋는다(지시문 보고: 획의 2D 방향과 소실점 방향의 차 **Δ0.0~1.7°**). ⚠⚠ **초판은 여기에 "합성 31.6°와 자릿수가 다르다"를 붙였고 리뷰어가 잡았다** — 31.6083은 `rule_camera`의 **3D 축 방향 오차**이고 Δ1.7°는 **화면 겨냥 각차**다. 프레임도 정의도 다르므로 그 비교는 손 오차의 크기를 말해 주지 않는다(#24). **같은 양의 합성 값은 원장에 없다** — `concurrent_deg`의 근거는 실획 보고 값 하나이고 그래서 동작점 하나다(#12) | ⚠ **표본을 이 저장소에서 확인하지 못했다** — `sessions/`가 비었고 `real_ink`는 `awaiting_samples`다. 그럼에도 이 전제 위에 `RULE_TOL.concurrent_deg = 3°`를 놓았다(그 상한의 약 두 배). **표본이 들어오면 가장 먼저 확인할 값이다** — 실제 분포가 더 넓으면 3°가 진짜 지지선을 버린다. AS-L24·L25(합성이 이 설계에 구조적으로 불리하다)를 지지하는 방향이다 ⛔⛔ **2026-08-19 표본 도착 — 보고 범위가 실측으로 반증됐다**: 세 파일 합산 겨냥 각차(`real_ink.json@f351839a`의 `k_metrics.vp_dir_err_deg` — 보장 0 제외)는 **중앙값부터 3°를 넘는다**(값은 그 필드를 그 자리에서 읽는다, #47. ⚠ 2차 리뷰어 [3] 병기: 이 분포 12행 중 **10행이 `near_vp_degenerate` 표식**이라 이 문장 단독으로는 겨냥 증거 규칙과 충돌한다 — 다만 그 표식은 **과잉 표식이 실측됐다**(`per_doc[*].near_vp_lever` — s44 Δ10.32·s45 Δ7.11은 지렛대 0.2° 안팎으로 퇴화가 아니라 실제 방향 이탈): 순수 비표식 n=2(4.94·9.15)에 이 두 행을 더해 읽어도 3° 위가 유지된다). "Δ0.0~1.7"은 선택 인용이었다 — 11차가 이미 첫 표본 실측 최대 9.15를 냈고 새 두 파일은 더 퍼진다. ⚠ **임계(`concurrent_deg` 3°)는 이 관측으로 움직이지 않는다**(#26 스윕 없이 안 움직인다 · #5 선택 절단: 이 분포는 축 배정을 **통과한** 획만 담는다). ⚠ 표본들은 12차 수리(무앵커 방향 스냅) **이전**에 그려졌다 — 스냅 보조가 없던 상태의 손 겨냥이다. 함의는 둘로 갈린다: 소실점 **확정**(두 선)은 이 오차 위에서도 섰고(세 파일 전부 두 슬롯 확정 — `per_doc[*].camera_assumed_principal_center`), 지지 판정(concurrent_deg 3°)과의 대조는 **프레임이 다르다**(#24 · 13차 리뷰어 [5]): 이 분포는 원시 첫·끝점의 현 방향이고 규칙이 재는 것은 **대표 직선**(representative)이다 — 9차가 같은 지표에서 이미 가른 자리(축 0 획이 대표 직선 기준 0인데 원시 기준 1.74·5.61). 대표 직선 프레임의 겨냥 각차와 3°가 버리는 참 지지선 비율은 미측정이다(DEFERRED 등재 — 6차 사전 트리거의 후속). ⚠ 극단 화각(163° — ⛔ "거부 대역"은 12차 상한 시절의 말이다: 14차 D-L93으로 상한이 제거됐고 이 절단은 **분석 층화**로만 남는다, `vp_dir_err_deg_fov_ok_cutoff`) 파일을 빼도 분포 중앙이 3°를 넘는 것은 그대로다 — 그 분포는 `k_metrics.vp_dir_err_deg_fov_ok`가 필드로 든다(13차 리뷰어 2차 [14]로 신설 — 원장 밖 계산을 없앴다. ⚠ 이 분포도 9행 중 7행이 표식 행이다 — 위 병기와 같은 유보·같은 과잉 표식 참조). ⚙️ **13차 항목 4 주석(4-R 리뷰어 대응으로 재정정)**: 최상위 꼬리는 겨냥 오차가 아니라 **지표의 근접 퇴화 — 지렛대 증폭**이었다(원시 시작점의 손떨림 이탈 ÷ 소실점 거리 — `delta_conflict.json@f351839a`의 `per_stroke.jitter_lever_deg` ≈ `vp_dir_err_deg` 실측. `near_vp_degenerate` 표식 — ⚠ 극단 카메라 전용이 **아니고**(98° 파일 행에도 붙는다) 표식 행의 Δ는 겨냥 증거로 안 쓴다). **비퇴화 부분집합이 아주 얇다**: `vp_dir_err_deg_stable` n=2 · 무스냅(무앵커 겨냥 논의의 모집단 — `vp_dir_err_deg_stable_no_snap`)은 **n=1**, 절단 감도(`vp_dir_err_stable_cutoff` — 0.5×/1×/2×에서 n=4/2/1)도 크다(#13·#14). 값들은 어느 절단에서도 3° 위지만, **판정을 "반증 유지"에서 "반증 방향 — 표본 부족으로 단정 불가"로 내린다**(4-R [B-2]·[B-5]. 2차 [5]로 한 겹 더: 표식이 **대리 조건이라 과잉 배제가 실측됐다** — s44·s45의 큰 Δ는 퇴화가 아니어서 반증 방향을 지지하는 행이 배제돼 있었다. 배제 판정은 `near_vp_lever`와 함께 읽고, 표식의 기전량 단위 재정의는 DEFERRED). 프레임 문제(원시 현 ↔ 대표 직선)의 온전한 해소는 DEFERRED 그대로다. |
| **AS-L27** | **다른 축의 대각선 둘은 소실점을 만들지 않는다**(6차 지시 11) — 그 교점은 공간의 한 점이다 | **합성 120구도(겨냥 잡음 1.7°)에서 옛 규칙이 묶음을 120/120 틀렸다**(`confirm_rules.json@f351839a`의 `vp_rule_three.old_rule.wrong`). 새 규칙은 두 선에서 **120/120 대기**하고 셋째까지 넣어 **7/120** 틀린다(잡음 0에서는 3/120). ⚠⚠ **대조 팔**(같은 축 둘이 먼저 오는 120구도)에서는 옛 규칙이 **0/120**이고 새 규칙이 **24/120**이다 — 즉 이 전제가 말하는 것은 "옛 규칙이 나쁘다"가 아니라 **"순서가 가른다"**이고, 새 규칙은 그 대가를 치른다. ⚠ 옛 규칙의 0/120은 **구성의 항등에 가깝다**(그 규칙의 묶음 정오는 "첫 두 선이 같은 축인가"와 동치다) — 그 팔이 보이는 것은 **지표가 0과 120을 모두 낸다**는 양방향 도달 가능성이다(#30, 6-R 재검). ⚠ 겨냥 잡음도 시드도 **동작점 하나**다(#12·#14) |
| **AS-L28** | **소실점은 그린 구간 **밖**에 있다** — 종이 위 작도가 그렇다(선을 연장해서 찾는다) | 이 전제를 **판정에 넣었다**(`beyondSegment`). 넣기 전 합성 틀림 15/120 → 넣은 뒤 **3/120**. 반증되면(그린 구간 안에 소실점이 오는 작도가 흔하면) 그 15가 되돌아온다 ⚠ 각(`convergeDeg`)만으로는 안 되는 이유가 이것이다: 점이 선분 **가까이** 있으면 어느 선에 대해서도 각이 작다 ⚙️ **2026-08-19 13차 4-R [B-10] 실획 대조**: `near_vp_degenerate` 표식(시작점→소실점 거리 < 현 길이)이 붙은 실획 10행이 반증 후보로 보였으나, **같은 현에 `beyondSegment`를 직접 물으니 10행 전부 참**이다(`real_ink.json@f351839a`의 `per_doc[*].near_vp_beyond_segment`) — dVp < 길이와 "구간 안"은 다른 판정이고(투영 매개변수 기준 ±extend_ratio), **전제는 실획에서도 유지된다.** 검출 규칙이 이 획들을 거르고 있지 않다 |

## 2026-08-18 7차 지시 (AS-L29 ~ )

| # | 전제 | 관측 |
|---|---|---|
| **AS-L29** | **차수가 P1에 갇히는 원인은 직교 스냅의 우회다**(7차 지시 1-a — 보고된 값은 P1 243→181 · P3 33→75) | ⚠⚠ **[7차 항목 3 주석] 아래 수치는 항목 1 시점(커밋 `a62d10b`)의 것이다** — 항목 2·3이 원장을 재실행했다(현행: P1 521 → 497 · `first_declaration.screen_h` 495/600 · 요인 분해 1.3329 / 1.3511 / 0.2452). 결론(우회 제거만으로는 안 풀린다)은 그대로다. ⚠⚠ **이 저장소의 합성 하네스에서는 반증에 가깝다.** `order_lock.json`(600실행)에서 우회를 지운 팔은 P1 519 → **499** · P3 42 → **50**으로 거의 안 움직인다. 카운터가 원인을 짚는다 — `first_declaration.screen_h`가 **497/600**(잡음 0 층 **120/120**)이고 **우회를 지워도 그 수가 안 변한다**. 즉 주 기전은 `classifyLine`이 화면 수평 4° 안 깊이 모서리를 `screen_h`로 읽고 소실점이 없을 때 **묻지 않고 받는 것**이다(`stepRule` 739~745행). ⚠ **지시문의 수와 다른 이유는 미상이다** — 보고된 243/33은 300실행 규모이고 이 하네스는 600(순서 2)이며 하네스 구성이 같은지 확인할 길이 없다(그 커밋이 저장소에 없다). **합성이다**(AS-L24·L25). ⚠ **요인 분해**(`order_lock.factor_split`)가 그 대신 가리키는 것: 축 라벨만 참으로 바꾸면 형태 오차 1.3876 → 1.4091(안 움직인다) · 소실점만 참으로 바꾸면 → **0.2466**. **틀린 것은 축 배정이 아니라 카메라다** ⛔⛔ **제자리 정정(2026-08-18 8차 2차 지시 항목 0 · 8-2R [3] · #42 ⑥)**: 위 세 수(1.3876 · 1.4091 · 0.2466)는 **현행 원장에 없다** — 8차 항목 1이 원장을 다시 돌렸다. **그리고 중앙값으로는 방향이 뒤집혔다**: `fixed` **0.5731** · 축 라벨만 참 **0.8148**(안 움직이는 것이 아니라 **나빠진다**) · 소실점만 참 **0.4543**(21%만 좋아진다). ⚠⚠ **결론은 유지되지만 근거 지표가 바뀐다** — 중앙은 팔마다 **놓인 수가 달라** 비교할 수 없고(#9·#15: 축 라벨 참 팔은 4743/7200을 놓고 소실점 참 팔은 2524/7200을 놓는다), **분모가 모든 팔에서 같은** `within_cut_0_2`(분모 7200)로 읽으면 **fixed 46 · 축 라벨만 참 13 · 소실점만 참 908 · 오라클 4004**로 **소실점 쪽이 20배**다. **"틀린 것은 카메라다"는 그 지표에서 오히려 더 강하다.** |
| **AS-L30** | **시작점 스냅과 방향 스냅이 충돌해 방향이 죽는다**(7차 지시 1-d) | **반증.** 코드에 그 구조가 없다 — `placeLive`가 스냅된 앵커를 위치로 쓰고 `resolveLive`가 거기서 축 방향으로 세그먼트를 낸다. `confirm_dir.json`이 한 팔에서 셋을 잠근다(`snap_start` 있음 · `axis`가 수 · `to_vp_deg < 0.01°`). 사용자가 본 "한 점에 모인다"는 **부챗살의 중심이 앵커**인 것이고, 그 선들이 소실점을 안 지난 것은 **AS-L31(원시 궤적)**의 귀결이다 |
| **AS-L31** | **확정 경로가 미리보기와 다른 방향을 쓴다**(7차 지시 1-c) | **지지.** 카메라가 선 뒤 `pts2d`는 `raw`였다(`frame() ? raw : resolve2d(raw)`). ⚠⚠ **지지의 근거는 원장의 0이 아니다**(7-R 리뷰어 [12]) — `confirm_dir.json`의 두 각차는 **정확히 0**이고 `preview.screen`과 `confirmed.pts2d`가 같은 배열이다. 그것은 측정이 아니라 `lift.ts`의 `segGap = 0` **보장**이다(#5·#40 ②). 정보를 가진 값은 **되살림 판의 `to_vp_deg = 4.5246°`**(겨냥한 5°가 그대로 남는다)이고 그 판은 커밋되지 않으므로 **원장 밖이다**(#25). ⚠ **2026-08-19 14차 항목 0이 같은 전제의 새 자리 둘을 실측했다**(D-L92): ① 미리보기의 앵커 자격이 확정과 달랐다(on_edge·on_face — `anchorQualified`로 통일) ② 승격 연쇄·교차 앵커의 축 되쓰기는 각도 무제한이었다 — 짝 대조가 원장에 있다(`dir_state.json@f351839a`의 `chain_dir_guard`: 가드 끔에서 회전 배치 — 켬에서 대기·좌표 불변. 크기는 픽스처가 정하므로 #46 범주만 인용). 전제는 그 두 자리에서 계속 지지였고, 수리로 **유계가 됐다** — 임계(`LIFT_TOL.parallel_deg`) **안**의 되쓰기는 여전히 미리보기 없이 일어나고(2차 리뷰어 [4] — "닫힘"이 아니다), 그 대역의 발생 빈도는 실획이 판정자다(D-L92) |
| **AS-L32** | **"수직축입니까" 물음을 없애면 물음 수가 0에 가까워진다**(7차 지시 3-f) | ⚠ **부분 반증.** 물음 합이 1381 → **1319**(−4.5%)로 줄 뿐이다(`order_lock.json`, 600실행). 남는 물음이 전부 `screen_or_depth`(4~8° 애매 구간)이고 그것은 3점과 무관하다 — 합성 잉크가 그 구간에 자주 떨어진다(AS-L25). **대신 다른 것이 크게 움직였다**: `camera_ok_rate`가 0.9167·0.9115 → **1.000**이 됐다(`rule_camera.json`) — 옛 판에서 카메라가 안 서던 실행의 상당수가 수직축 답 뒤 3점 카메라가 **예각 조건**(이론서 6.5)을 못 넘어 막힌 것이었다. ⚠ 실획 표본이 판정자다(지시 3-f는 "asked 6 / 5획"을 근거로 들었고 그 표본이 저장소에 없다 — AS-L26과 같은 자리) |
| **AS-L33** | **3점 구도는 사용자가 그려서 선언한다**(A-4 이래의 전제) | ⛔ **7차 지시 3이 이 전제를 폐기했다.** "3점 투시는 2점이 서 있는 상태에서 카메라를 기울여야 나온다. 3점은 시점의 성질이지 그리는 선의 종류가 아니다." 그 귀결로 **빈 캔버스에서 3점 스케치를 직접 시작할 수 없다** — 2점으로 시작해 기울인다. ⚠ **그 제약이 실사용에서 견딜 만한지는 표본이 없다**(AS-C1·AS-L9와 같은 자리). 건축 투시도에서 3점(올려다봄·내려다봄)으로 **처음부터** 그리는 관행이 흔하면 이 결정이 되돌아온다 |

## 2026-08-18 8차 2차 지시 (AS-L34 ~ AS-L37)

| # | 전제 | 관측 |
|---|---|---|
| **AS-L34** | **P1의 배치가 나쁜 것은 임의 f의 배율 오차 때문이다**(8차 1차 항목 1의 귀속 · 지시 2-b의 가설) | ⛔ **반증.** 소실점만 참으로 주면 P1 중앙이 **1.6914 → 0.4782**(3.5배)이고 절단 0.2 안이 **0 → 39**인데, **f를 ×2로 흔들어도 4/708**에 그친다(`order_lock.json@f351839a`의 `by_order.p1`). **원인은 f가 아니라 소실점이다.** ⚠⚠ **첫 판정 시도는 무효였다**(8-2R″ [2]): `fixed_true_f` 팔의 개입이 **4%뿐**이라(참 f 1000 · 임의 f 960 · `f_intervention.ratio_median` 1.0021) 그 팔의 무효과는 아무것도 안 말한다 — **양성 채널(×0.5·×2)을 넣고서야 지표가 f에 반응한다는 것이 확인됐다**(×0.5에서 P1 중앙 2.0866). ⚠ **"임의 f 자체의 대가"는 여전히 미측정이다** — 이 픽스처에서 임의 f와 참 f가 4%밖에 안 다르다 ⛔ **제자리 정정(10차 리뷰어 4차 [3] · #42 ⑥ — D-L85(반경 8) 재실행)**: 인용 수치가 전부 움직였다(원장을 그 자리에서 읽었다): P1 중앙 `fixed` **1.681** · 소실점만 참 **0.492** · 절단 0.2 안 **0 → 40**(true_vp_rule_axis) · f ×2는 **3/330** · ×0.5 중앙 **2.1685** · ×2 중앙 **1.2505** · 참 f **1.6475**. P1 배치 분모도 708 → **330**(P1 실행 59 → 65). ⛔ **제자리 정정(리뷰어 5차 [5] · #9·#15)**: 4차 정정의 "(3.4배)"는 **놓인 수가 다른 두 팔의 중앙값 비교였다**(fixed 330/780 ↔ true_vp 119/780 — AS-L29가 이 원장에 세운 규약 위반). 분모가 같은 지표로 다시 적는다(분모 780 = P1 실행 65 × 12획): 절단 0.2 안 `fixed` **0/780** · f ×2 **3/780** · **소실점만 참 40/780** — 배수 대신 이 셋을 결론의 근거로 쓴다. **반증의 방향은 그대로다 — 소실점이 원인이고 f가 아니다**(오히려 이 지표에서 더 선명하다: 40 대 3 대 0) |
| **AS-L35** | **P1의 배치 0은 구조적이다**(8차 1차 항목 1의 "P1은 0이다") | ⛔ **반증.** `stroke_budget.two_box`(획 24)에서 P1의 절단 0.2 안이 **15/2869**다 — **0이 아니다.** 상자 하나의 P1 배치가 **708**뿐이라 0.5% 사건이 안 나온 것이다(#38 — 표본이 작다). ⚠ **여섯 시드 전부 0인 것도 강건성이 아니다**(8-2R″ [3]): 시드당 P1 배치가 52~156이고 기대 건수가 **0.27**이라 분산 0은 희소성의 귀결이다(#14 — 분자/분모로 읽는다). **P1은 P2보다 7배 나쁜 것이지 불가능한 것이 아니다** ⛔ **제자리 정정(10차 리뷰어 4차 [3] · #42 ⑥ — D-L85 재실행)**: 현행(원장을 그 자리에서 읽었다) `two_box` P1 절단 안 **33/2642**(1.25%) · `one_box` P1 배치 **330**(708이 아니다) · P2 대비 **5.0배**(214/3454). ⛔⛔ **11차가 이 자리를 세 번 고쳤다 — 최종 판정은 "이 자료로는 못 세운다"다.**
① 먼저 **철회**했다(기대 ≈4.1 · 관측 0).
② 리뷰어가 **검정의 단위**를 짚어 되돌렸다(#48): 한 실행의 획 12~24개는 **같은 카메라**를 공유하므로 배치는 독립 시행이 아니다 — 실제로 `two_box`의 성공은 **다섯 실행**에 몰려 있다. 실행 단위로 다시 내니 안 기각됐다.
③ ⛔⛔ **그러나 그 검정은 이 표본에서 기각을 낼 수 없다**(11차 리뷰어 2차 [3] · #35·#40 ②): 기각 방향의 **유일한 관측은 0**이고 그때의 p가 이미 알파보다 크다 — 즉 `not_rejected`는 자료의 값이 아니라 **표본 크기의 귀결**이다. 원장이 그 사실을 스스로 든다(`test_can_reject` · `min_attainable_p` · `runs_needed_for_alpha` · `verdict: underpowered`).
👉 **그러므로 희소성 논증은 이 자료로 세울 수도 무너뜨릴 수도 없다.** ①의 철회도 ②의 복원도 근거가 없다 — 필요한 것은 **P1 실행을 그 필드가 든 수만큼 늘린 하네스**다. ⚠ 두 단위의 p가 알파를 사이에 두고 갈리는 것도 그래서 결론이 못 된다(#13). ⚠ 분모가 **배치**라 AS-L34가 쓰는 **시도**(P1 실행 × 획)와 **다른 양**이다(리뷰어 2차 [12] — 원장 산문이 그 구분을 든다). **AS-L35의 반증 자체**(`two_box`의 절단 안이 0이 아니다)는 이 논증과 **무관하게** 선다. ⚠ 수를 여기 안 박는다(**#47**) — 값은 그 필드를 그 자리에서 읽는다 |
| **AS-L36** | **P0(카메라 미확정)는 "방향 확정 · 좌표 미정"으로 흡수된다**(`HANDOFF`가 항목 5-1에 걸어 둔 기대) | ⛔ **거의 반증.** P0 1680획 중 소실점이 선 것은 **84획(5%)**뿐이고(`p0_reasons.vp_but_no_screen_h` — 7실행), **나머지 1596획(95%)에는 소실점이 없어 3D 방향 자체가 미정**이다. **미배치율의 분모는 거의 안 바뀐다.** ⚠ 지시 1-c가 실제로 가리킨 "카메라가 서면 일괄로 올라간다"는 기전은 **이미 있다**(`pts2d` 보존 + 재풀이, §6.1) — P0는 "일괄이 안 도는 실행"이 아니라 **"12획 안에 끝내 안 선 실행"**이다 |
| **AS-L37** | **합성 하네스가 P0를 만드는 것은 획 예산 때문이다**(지시 1-d의 가설) | ⚠ **부분 지지 · 귀속은 미확정.** 상자를 하나 더 놓아 획을 12 → 24로 늘리면 **P0가 140 → 63**(−55%)이고 `stood_at.max`가 **12 → 23**으로 늘어난다(예산이 꼬리를 자르고 있었다 — #32). ⛔ **그러나 "획만" 늘어난 것이 아니다**(8-2R″ [5]): 대기선 간 각차가 **중앙 19.8833 → 43.9619 · 최소 6.367 → 24.2011**로 통째로 달라졌다 — 둘째 상자가 **지지선의 배치(정보량)도** 늘린다. **획 수와 정보량을 가르는 위약**(같은 상자에 덧그리기)이 **없다**(#39). ⚠ 그리고 **좋아지기만 하지 않는다**: P1이 59 → 240으로 늘어 형태 오차 중앙이 **0.5731 → 0.8429**로 나빠진다(#15·#16) ⛔ **제자리 정정(10차 리뷰어 4차 [3] · #42 ⑥ — D-L85(반경 8) 재실행)**: 인용 수치가 전부 그 시점 값이다. 현행(원장을 그 자리에서 읽었다): P0 **88 → 69**(−22% — 옛 −55%보다 약하다) · `stood_at.max` **12 → 19** · P1 **65 → 227** · 형태 중앙 **0.5869 → 0.75** · 대기선 각차(pending_2_awaiting_third)는 중앙 **23.8702 → 22.6295**(거의 불변) · 최소 **4.0033 → 15.3914**. ⚠ **"정보량도 늘린다"의 증거가 옮겨 갔다** — 옛 판은 중앙이 통째로 움직였고(19.88 → 43.96) 현행은 **최소만** 오른다. 결론(부분 지지 · 획 수와 정보량을 가르는 위약 부재)은 그대로다 |
| **AS-L38** | **오스냅 반경이 "안 붙어야 할 곳에 붙는다"의 원인이다**(2026-08-18 8차 3차 지시 항목 2의 전제) | ⚠ **이 저장소의 합성으로는 확인도 반증도 못 했다**(9차 항목 2). `osnap_two_sided.json`에서 잘못된 연결은 **3점 구도에서만** 났고(r15 57/1152 · 66/1152) **1점·2점은 반경 40에서도 0/1152**다 — 그런데 **그 0은 반증이 아니라 도달 불가**다(#35: 픽스처가 그 층에서 대상을 못 만든다, 9-R″ [3]). ⚠⚠ **반대 방향 측정은 있다**: `classify_leak.ablation`에서 반경 **15 → 8**이 D-L80 모순을 **72 → 35** (1점 **55 → 31**)로 줄인다 — 즉 **반경이 판정을 뒤집는 쪽으로는 실제로 기여한다.** ⛔⛔ **제자리 정정(2026-08-18 11차 항목 3 · D-L88)**: 그 **0/1152가 픽스처의 성질임이 실측으로 확인됐다** — 분리 체제 팔(`by_composition_calibrated` — `min_cand_px` > 40px)에서 **다섯 구도 전부 잘못된 연결이 난다.** ⚠ **다만 앱 동작점(r8)에서는 1점·2점이 여전히 0**이고 도달은 r25~r40의 지터 꼬리다(11차 리뷰어 [2]). **그러므로 이 가정은 '합성으로 반증됐다'가 아니라 '합성이 그 층을 못 만들고 있었다'였다.** 다만 **지지로 뒤집지도 않는다**: 두 보정 팔의 값 크기는 **픽스처가 고른 인접도의 귀결**이다(#46 — 인접 팔은 후보를 조리개 안까지 데려오고, 분리 팔은 40px 밖으로 민다). 그리고 **원인이 갈렸다**: 인접 체제 r8의 잘못된 연결은 **중점 후보가 지배**하고, 중점만 끈 개입 팔(`midpoint_off_adjacent`)에서 크게 준다 — **반경과 나란한 지렛대가 후보 종류다**. **AS-L38은 열려 있고 판정자는 그대로 실획이다.** **남는 후보 둘**(관계 스냅 · `on_face`)도 미측정이고 `on_face`는 표본이 가리키는 쪽이다 ⚙️ **2026-08-19 13차 항목 0·5 제자리 갱신**(5·6·7-R [2] — 앞 문장들의 수치가 낡았다): `real_ink.json@f351839a` 실측으로 `snap_dist_px.n = 4**(0 아님)** · `start_kinds`는 **on_face 5 / endpoint 2 / vertex 1**(= 5/8 — 옛 "5/5"는 첫 표본만의 값). 사전등록 조건(p10 > 조리개)은 **reopen = false**(`radius8_revert_condition`)이나 n=4 · 구조 0 의심 미해소라 **정보량이 0에 가깝다** — 판정은 여전히 다음 표본이다(조리개·토글 스냅샷이 저장 형식에 없어 그 표본도 동작점 미상일 수 있다 — DEFERRED 그 행) |

## 2026-08-18 10차 (AS-L39 ~ AS-L40 — 리뷰어 4차 [18] 등재)

| 번호 | 가정 | 상태 |
|---|---|---|
| **AS-L39** | **그리는 중(핀 상태·카메라 전)의 두 손가락은 "종이 밀기" 의도다**(10차 항목 5 · D-L86의 전제 — 지시 문면 "그리는 중에는 화면 팬"이 이 해석을 지정했다) | ❓ **실기 미검증.** `view_pan.json`이 확인한 것은 **배선**(두 손가락 → viewPan, 핀·문서·주점 불변)이지 의도 일치가 아니다. 합성 터치(dpr 1, AS-C1)뿐이고, 실기(아이패드)에서 사용자가 그리는 중 두 손가락으로 **기대하는 것**(종이 밀기인가, 확대인가, 궤도인가)의 표본이 0이다. ⚠ 핀치(확대) 의도는 현재 **무시된다**(DEFERRED 10차 절) — 실기에서 확대 기대가 흔하면 이 가정의 절반이 꺾인다 ⛔ **제자리 정정(14차 항목 5 · D-L94 · 리뷰어 [8])**: 그 절반이 실제로 꺾였다 — 사용자 보고("화면을 옮기고 **확대**하려는데")가 확대 기대의 첫 표본(1건)이고, 핀치는 이제 **화면 줌으로 받는다**(무시 폐기 — `view_pan.json@f351839a` ③″). 남은 절반(두 손가락 이동 = 종이 밀기)은 여전히 실기 표본 0이다 |
| **AS-L40** | **3점 구도에서 큐브 정면 탭의 의도는 "그린 입체의 입면"이다**(10차 항목 6 · D-L87의 전제 — 큐브 기준계를 그린 축으로 돌린 이유) | ❓ **실기 미검증.** `cube_frame.json`이 확인한 것은 두 기준계의 **판별**(새: 시선→그린 깊이축 · 옛: 세계 −z, cos35°·cos15° ≈ 0.7912 간격)이지 의도가 아니다. 사용자가 3점 구도에서 "정면"을 탭할 때 기대하는 것이 그린 상자의 입면인지(우리 선택), 화면 기준 정면(옛 거동)인지 표본 0이다. ⚠ SketchUp의 뷰 큐브는 **모델 축** 기준이 선례다(A-3 — 그 선례가 이 선택의 근거) |
| **AS-L41** | **`liftAll`의 3D 세그먼트는 배정된 축 방향으로 정확히 놓인다**(12차 항목 2 초안의 전제 — solveInto에 D-L71 되쓰기를 넣으려던 근거) | ⛔ **반증**(12차 항목 2 실측 · `vp_dir_consistency.test.ts` 항등 팔): liftAll의 끝점은 그 화면점의 **광선 위**에 있으므로(λ·ray) 재투영이 그린 현과 정확히 같다 — 겨냥이 벗어난 획의 3D 방향은 축이 아니라 **그린 현을 따른다**(해석 평면 안의 절충). 축 방향이 정확해지는 것은 **겨냥이 정확할 때뿐**이다(스냅 → 풀이 사슬 팔: 재투영 Δ < 0.01). 그래서 되쓰기는 항등이라 뺐다 — 잉크·3D는 그 경로에서 자기일관이다 |
| **AS-L42** | **확정 시점의 수평 평면 축 대기 획은 바닥 격자다**(13차 항목 2 · D-L90 — 지면 첫 앵커의 전제. 지시 2-a의 근거 "사용자가 바닥 격자를 그은 것"의 일반화) | ⚠ **전제 위반의 비용은 실측돼 있다**(`first_anchor.json@f351839a`의 참값 팔): 지면 위 높이 h의 수평선(상자 윗모서리·허공)이 확정 시점 대기 집합에 섞이면 깊이가 **1/(1−h/눈높이)배**로 조용히 늘고 **방향은 보존된다**(등고선의 지면 그림자는 순수 배율) — 즉 방향 지표로는 안 잡힌다. 발동이 첫 앵커 순간 하나뿐이라 노출은 확정 전에 그린 획에 한정된다. ⚠ **이 표본으로는 판정 불가다**(13차 1·2 리뷰어 2차 [3] — 초판의 '반례 미관측'은 정보량 0이었다: 방향이 보존되므로 이 저장소의 어떤 방향 지표도 위반을 낼 수 없고(#32 — 관측 불가를 반증으로 안 쓴다), 근거도 15-16-18 한 파일의 재구성뿐이었다). **관측 채널이 없다는 것 자체가 이 가정의 상태다** — 지면 배치 획이 나중에 연결(교차·오스냅)로 다시 좌표를 받을 때의 깊이 불일치가 후보 판정자이고 DEFERRED에 있다. 위반 비용의 크기는 `first_anchor.json@f351839a`의 `ground_truth.offplane`이 든다(깊이 1/(1−h)배·방향 내적 1) | 13차 신설 |
| **AS-L43** | **연장 의도 가정**(13차 항목 3 · D-L91): 3D 획의 끝점에 스냅해 그 선의 상 방향 5°(`LIFT_TOL.parallel_deg`) 안으로 그으면 사용자는 **그 3D 직선을 이어 그릴 의도**다 | ⚠ 실기 미검증(AS-L39·L40 계열 — 의도 가정). 합성·재구성 근거: 발동은 좁고(임계 5°·끝점 조리개 안 시작·바깥 방향만) 어긋난 원 선에서는 연장이 원 선을 따른다(`extension_snap.json@f351839a`의 divergence_arm — 그것이 정의다. '옳음'은 원 선의 옳음에 상속). 반증 조건: 실사용에서 끝점 근처 5° 안 손짓이 연장이 **아니었던** 사례(토글 끔·Shift 자유 획이 회피 수단) | 13차 신설 |
| **AS-L44** | **단축 상의 방향은 잡음이다**(13차 항목 3 — 연장선 가드): 원 선의 상이 조리개(`OSNAP_RADIUS_PX`)보다 짧으면 그 상의 방향으로 연장을 정하면 안 된다 | ⚠ 임계는 재사용이지 측정이 아니다(#17 — 값 자체의 스윕은 `extension_snap.json@f351839a`의 guard_sweep이 동작점 4/8/40을 든다: 6px 상은 가드 0·4에서 발동하고 8·40에서 차단). 실측 사례: 입면에서 깊이축 선의 상이 몇 px가 되어 ⑥ 팔의 발동이 좌우 반전 방향으로 났던 것(basic_flow 네 입면 — 가드 도입의 동기) | 13차 신설 |

## 2026-08-19 15차 (AS-L45 ~ AS-L47 — 항목 1)

| 번호 | 가정 | 상태 |
|---|---|---|
| **AS-L45** | **확정 순간이 스냅 기록을 버려서 연결이 끊어진다**(15차 지시 1-b의 후보 ①) | ⛔ **반증.** 종단 팔 전부 `snap2dStart`/`snap2dEnd`와 `ofId`가 확정 전후로 **그대로** 남는다(`confirm_link_e2e.json@f351839a` — 삼각형 팔들이 `snap_records` 2 · `rows[].ofId` 보존). 같은 실행에서 **기하는 실제로 벌어져 있다**: 되맞춤만 끈 팔의 연결 간격이 상대 **0.00192**(`summary.declared_only_gap_rel_max`)이고 그 연결점이 화면에서 **9.27px** 떨어져 있다(`arms[1].rows[1].break_px`) — 기록은 있고 기하가 없다. 사라진 적이 없다. 사용자가 본 "스냅이 풀린다"의 실체는 기록이 아니라 **기하**다 — 일괄 풀이가 3D 끝점을 `rep`(PCA 극단)의 광선에 놓는데 오스냅이 붙여 준 점은 `pts2d`의 양 끝이다. ⚠ 반대 방향으로 **없던 것이 하나 확인됐다**: 일괄 풀이 경로는 3D 참조(`snapStart`/`snapEnd`)를 **아예 안 남겼다**(수리 전 팔 `ref3d` 전부 false) — 기록은 있는데 3D 참조가 없는 상태였다 |
| **AS-L46** | **D-L92의 5° 방향 가드(`CHAIN_DIR_GUARD`)가 스냅된 획을 2D로 돌려보낸다**(15차 지시 1-b의 후보 ②) | ⚠ **반증이 아니라 갈렸다**(1차 리뷰어 [6]④ 지적으로 정정 — 초판은 "분모가 0"이라 적었고 그것은 팔 하나의 값이었다). 종단 여섯 팔의 분모는 `confirm_link_e2e.json@f351839a`의 `summary.chain_dir_guard_attempts` = **[1, 0, 0, 3, 0, 0]**이다. ① **증상 ②의 기하(열린 삼각형)에서 가드는 아무것도 안 돌려보냈다** — 수리 전 구성(`arms[0]`)은 발화 기회가 **1**이고 `rejected`는 **0**이다(`summary.chain_dir_guard_rejected` = [0,0,0,3,0,0]). 즉 **발화 기회가 없어서가 아니라 통과시켰기 때문에** 증상 ②의 원인이 아니다. ⚠ 2차 리뷰어 [1]로 재정정: 1차 대응의 "발화 기회가 0"은 수리 **후** 세 팔의 값이었다. ② **닫힌 삼각형에서는 3/3 발화한다**(`arms[3].chainDirGuard` — attempts 3 · rejected 3). 거기서 막히는 획은 **자기 소실점까지 그은 획**이라 축 투영 배치가 그린 현과 크게 어긋난다 — 막는 것이 옳다(A-3). ⚠ 그래서 "가드가 무해하다"도 아니고 "가드가 원인이다"도 아니다: **기하가 정한다.** 실사용 층의 판정자는 여전히 실획이다 |
| **AS-L47** | **`representative`(PCA 대표 직선)의 끝은 그린 끝점을 대신할 수 있다**(`lift.ts`가 확정 배치의 끝점을 `rep.a`·`rep.b`의 광선에 놓는 전제 · `findJoints`가 접합을 대표 직선 교점에서 찾는 전제) | ⛔ **반증(실획).** 실획 표본 셋 22획에서 `rep` 끝과 그린 끝점이 **중앙 6.5px · p90 16.5px · 최대 79.4px** 어긋난다(`confirm_link.json@f351839a`의 `real_ink_rep_offset` — 시작 끝만 보면 중앙 7.0px, 조리개 8px를 넘는 획이 **8/22**. ⚠ **모집단은 원시 점열이 남은 획 22개**이고 전체는 29다 — 두 점으로 접힌 7획은 `rep`가 그 두 점을 정확히 지나 **정의상 0**이라 뺐다(`n_strokes_all`·`n_excluded_two_point`, 1차 리뷰어 [8]). 두 대표 직선의 **교점**은 그보다 더 벌어진다(`rep_intersect_offset` — 중앙 13.1px · 최대 34.4px이고 접합 반경 `tol_px` 21.71px을 넘는다). 그래서 ① 붙여 그은 획이 "구조에 안 이어졌다"로 떨어지고 ② 놓여도 두 획의 그 끝이 떨어졌다. ⚠⚠ **이 반증이 14차까지 안 보인 이유**: 방향·끝점 스냅이 걸린 획은 `resolve2dCore`가 `pts = [a, b]`를 내므로 `rep`가 그 두 점을 **정확히** 지난다 — 떨림 0으로 그은 종단 팔(`declared_only_straight`)이 같은 구성에서 간격 **0**을 낸다(`summary.straight_gap_rel_max`). **프리핸드가 판정자다**(#12의 동작점 함정이 등급 축에서 난 것) | 15차 신설 |

## 2026-08-19 15차 (AS-L48 ~ AS-L49 — 항목 3·8)

| 번호 | 가정 | 상태 |
|---|---|---|
| **AS-L48** | **모델링 시점에 그리면 표시가 화면에 붙어 따라다니는 유령이 된다**(14차 항목 6-e · D-L97이 그리기를 막은 이유) | ⛔ **반증(구조).** 그 이유는 **이미 해소돼 있었다** — 소실점·지평선·격자가 전부 `viewOverlayCtx`·`drawGrid`로 **그 시점의 값**을 다시 계산한다(14차 항목 6-c가 그렇게 고쳤다). 즉 차단의 근거가 그 항목 안에서 사라졌는데 차단만 남았다. 실측: `view_axes.json@f351839a`의 **자세 다섯 전부**(핀 포함 — 배열이 5원소다, 2차 리뷰어 [9])에서 `blocked_now` 거짓 · `finite_axes` 3. ⛔ `horizon_gap_px` 0은 실측이 아니라 **설계 보장**이라 이 목록에서 뺐다(2차 리뷰어 [5] · #5 — 지평선을 그 두 소실점으로 만들었으므로 0이다). ⚠ **새로 생긴 미검증 전제**가 있다: "어느 자세에서 그은 획이든 쓸 만하게 놓인다" — 이 원장은 **획이 문서에 남는가**까지만 재고 3D 배치의 옳음은 안 잰다. ⛔ 초판은 그 몫을 `dir_state`·`confirm_link`로 넘겼는데 **그 두 원장에 돌린 자세 행이 0건**이라 위임할 자리가 없었다(2차 리뷰어 [2]) — 그 대역을 재는 원장은 **없다** | 15차 신설 |
| **AS-L49** | **소실점이 화면 밖 극단이면 스냅이 무의미하다**(D-L97의 6-e · `docs/line_plan.md` §9.3 — 모델링을 좁힐 근거로 지시 3-c가 다시 부른 전제) | ❓ **미검증 — 그리고 반대 방향 관측이 있다.** 그 대역은 실재한다(`view_axes.json@f351839a`의 `poses[*].axis_vps[*].dist`가 화면 대각의 몇 배까지 간다). 그런데 그 자세들에서 **획은 문서에 남는다**(`stroke_added` 참 · `blocked_now` 거짓). ⛔ 초판은 여기에 "그리기와 **스냅**이 정상"이라 적고 근거를 "축 방향은 3D에서 정확하므로"로 댔는데 **둘 다 이 원장이 안 잰 양이다**(2차 리뷰어 [3] — `view_axes.json`에 스냅 필드가 없고, 뒷말은 AS-L49 자신이 ❓로 놓은 명제를 근거로 되쓴 순환이다). 그래서 뺐다. **그러므로 "무의미하다"의 실측이 없다** — 무엇이 망가지는지 재는 팔이 먼저다(`DEFERRED`). ⚠ 15차 항목 3의 초판이 이 말을 **가까운 소실점**으로 잘못 읽었다(2차 리뷰어 [1]) — 그 오독은 progress에 제자리 정정으로 남는다 | 15차 신설 |
| **AS-L50** | **호버 표식과 착지 스냅은 같은 후보를 낸다**(옛 코드의 암묵 전제 — 표시는 호버 좌표로, 확정은 착지 좌표로 물었다) | ⛔ **반증(실측).** 스타일러스에서는 두 좌표가 갈린다: `hover_lock.json@f351839a`의 `summary.off_start_kind`가 착지 12.5px 어긋남에서 **null**을 낸다(호버는 `vertex`를 보이고 있었다). 마우스에서 안 보인 이유는 누르는 동안 커서가 안 움직여 두 좌표가 **정확히 같기** 때문이다 — 전제가 입력 기기에 딸려 있었다. ⚙️ 그 뒤 절반도 **실측이 됐다**(1차 리뷰어 [10]): `summary.mouse_land_gap_px` **0**(마우스 팔 · 같은 원장). 수리는 D-L103 | 15차 신설 |
| **AS-L51** | **호버 자리 기준 잠금 반경(`hover_lock.json@f351839a`의 `setup.lock_radius_px`)이 조용히 틀린 배치를 만들지 않는다**(D-L103의 대가 판단 — "사용자가 보고 받아들인 자리이므로") | ❓ **미검증.** 논증은 구조적이다(잠금의 재료가 표식 자신이라 표식 없이 붙는 일이 없다). 그런데 **표식을 보고도 다른 곳에 대려 한 손짓**이 있으면 그것을 가로챈다 — `stale` 갈래(잠금 반경 밖)가 그 몫을 받지만 문턱의 **자리**가 실측이 아니라 **기존 상수 재사용**(#17)이다(문턱이 그 상수 자리에 실제로 있다는 것은 잰다 — `summary.threshold_band_px`). 판정자는 실획: 잠금이 붙인 획 중 사용자가 무른 비율. 트리거: 이번 세션 이후에 앱으로 그린 표본 | 이번 세션 신설 |
| **AS-L52** | ⛔ ~~**지평선 끌기는 «확정 후 잠금» 때문에 안 열린다**는 것이 14차까지의 읽기였다~~ **이 전제 자체가 틀렸다**(1차 리뷰어 [3]) — 저장소는 두 층을 **둘 다** 적고 있었다 | ⚙️ **반증이 아니라 정정.** D-L45 말미와 D-L60이 *"확정 전엔 지평선이 없고 확정 후엔 잠긴다 — **도달 불가**"*를 4차에 이미 명시했다. 그러므로 이 항목은 **아무것도 반증하지 않았다** — 한 것은 등재된 **휴면을 깨기로 한 결정**(D-L104)이다. 실측은 그 휴면이 지금도 그대로임을 확인한다(`horizon_drag.json@f351839a`의 `off_grab_empty` 전부 거짓). ⚠ 초판이 이것을 "짐작이 틀렸다"로 적은 것은 **기록을 안 읽고 반증을 주장한 것**이고, 그 자체가 이 세션이 조심해야 할 형태다 | 이번 세션 신설 |
| **AS-L53** | **토글을 켜면 빈 종이 감각(4차 지시 4-a)이 다시 해쳐진다** | ❓ **미검증. 우려 둘을 갈라 적는다**(1차 리뷰어 [10] — 초판은 앞엣것을 "진짜 우려가 아니다"로 지웠는데 D-L60은 둘을 **병렬로** 적는다). ① **소실점이 (선 × 미리 깔린 지평선)으로 굳는 것** — ⛔ **2026-08-20 18차에 이 구조가 뒤집혔다**(D-L109): 소실점은 이제 **정의상** «깊이선 × 지평선»이고 지평선은 **풀이의 전제**다. 그러므로 «구조로 막힌다»는 근거가 사라졌다 — 그런데 **우려의 내용은 그대로 실현된 것이 아니다**: 미리 깔린 지평선이 아니라 **사용자가 그은 지평선**이고, 그 선이 정확히 사용자가 겨냥한 시선 높이다(D-L59가 되돌리려 했던 것은 «기본 위치가 깔려 있는 지평선»이었다 — D-L109는 기본 위치를 두지 않는다). ⛔⛔ **제자리 정정(2026-08-20 19차 항목 3)**: «기본 위치를 두지 않는다»가 **새 문서에서만 참이었다**. **옛 저장본을 열면 18차 이전의 `defaultHorizon`(= 화면 중앙)이 그대로 복원돼 «미리 깔린 지평선»이 실제로 있었고**, 사용자가 상단에 그은 획이 그 위에서 **가로축 선언**으로 먹혔다 — 즉 이 항목 ①의 우려가 **복원 경로에서는 실현됐다**(사람이 보고했다: «상단에 그어도 화면 중간 높이에 지평선이 생긴다»). D-L115가 그 경로를 막았다(소실점이 얹혀 있지 않은 지평선은 문서에서 안 읽는다). 실측은 `procedure_repair.json@f351839a`의 `horizon.restored_old_save_no_vp`(수리 후 `null`)와 `gate.revert_check.B_horizon_y`(수리를 되돌리면 337 = H/2)다. ⚠ **대가는 실측으로 미확정이다**: 지평선의 손 오차가 축 오차를 얼마나 키우는지 **안 쟀다**(`DEFERRED.md` 18차 «사람이 그은 지평선의 오차»). ② **빈 종이 감각** — **기본 끔으로만** 막힌다(`off_visible`의 빈 화면 값 거짓). 켠 사용자에게는 그대로 해당하고 **실사용 미판정**이다 | 이번 세션 신설 · **18차에 ①이 반증됨**(구조가 바뀌었다) · **19차에 ①이 복원 경로에서 실현됐음이 실측됨**(제자리 정정) |
| **AS-L54** | **이론서 15.1의 「경사 소실점은 대응 수평 소실점의 바로 위/아래에 있다」가 일반적으로 성립한다** | ⛔ **반증(실측).** **2점 전용이다.** 3점 픽스처에서 화면 세로 어긋남이 `aux_vp.json@f351839a`의 `summary.slope_dx_screen_px`이고 0이 아니다. **일반형**은 「경사 소실점이 «대응 수평 소실점 ↔ 수직축 소실점» 직선 위에 있다」이고 그 잔차가 `slope_line_off_px`(0)다 — 수직축이 무한원(2점)일 때 그 직선이 화면 세로가 되어 15.1의 문면이 된다. 이론서가 틀린 것이 아니라 **문면이 2점 문맥에 있다** — 15.1의 *"수평 방향 (a, 0, c)"*에서 y 성분 0이 «세계 수평면 = 카메라 y=0 평면»(피치 0)을 뜻한다. 15.2가 *"작도로는 측점을 쓴다"*라고 적은 것이 같은 자리다. 판정: `test/aux_vp.test.ts`의 3점 팔. ⚙️ **16차 추가 실측**: 15.2의 «측점에서 pitch로 쏘는» 경사 작도 자체도 2점 전용임을 반례 팔이 쟀다(`slopeByMeasuringPoint` — 2점 1e-6px 안 · **3점 >1px**). 일반형(직선 위) 잔차는 세 차수 전부 1e-6px 안 | 이번 세션 신설 · 16차 보강 |
| **AS-L55** | **이론서 6.2의 탈레스 작도 「|PE| = f」가 일반적으로 성립한다** | ⛔ **반증(실측).** 역시 **2점 전용**이다(6.2의 전제: 주점이 지평선 위에 있다). 초판 구현이 그대로 썼고 앱 픽스처(3점)에서 상대오차 0.23이 났다. 일반형은 `f² = edgeDist² − principalOffset²`이고 그것이 6.3의 수심 정리·7.6의 「E의 위치도 변마다 다르다」와 같은 말이다. 고친 뒤 상대오차는 `aux_vp.json@f351839a`의 `summary.f_rel_gap`이다. ⚠ **단위 팔이 2점만 돌아서 안 잡혔다** — 종단이 3점 픽스처라 거기서 났다(#12: 동작점 하나가 위험한 자리 — 16차가 그 형태를 **#51**로 등재했다). ⚙️ **16차 추가 실측**: 탈레스의 E(rabatment된 평면상 카메라 위치)에서 쏜 광선의 각 = 세계 각이 **전 차수에서** 선다(2점·3점·롤 3점 — 광선 논증이 |PE|=f를 안 쓰기 때문. `test/aux_vp.test.ts` «만드는 경로 셋») | 이번 세션 신설 · 16차 보강 |
| **AS-L56** | **보조 소실점을 축 경쟁에 넣으면 조용히 틀린 배치가 는다** | ❓ **미검증 — 구조로만 막았다.** 규약은 ① 비기면 축이 이긴다 ② 강제 축이 있으면 경쟁 자체를 안 한다 ③ 보조로 놓인 획은 `axis: free`라 카메라에 되먹임되지 않는다. 그런데 **틀린 보조 각도를 넣었을 때의 대가는 안 쟀다** — 그 팔이 없다. 판정자는 실사용이다: 보조를 쓴 획 중 사용자가 무른 비율 | 이번 세션 신설 |
| **AS-L57** | **큐브 정렬이 방향만 맞추면 롤은 따라온다** | ⛔ **반증(실측).** `OrbitControls`가 `camera.up`을 +Y로 고정하므로 **그린 세로축이 세계 Y와 어긋난 만큼 화면이 기운다** — 면 탭 뒤 축 셋의 화면 방향이 `lens_roll_osnap.json@f351839a`의 `s_roll.reproduced_before_fix`(−8.7°·0°·−98.7°)였다. `setPose`가 `cam.up`을 **그린 기준계의 Y**로 세워야 한다(D-L112). 수리 뒤 0°·0°·−90°이고, 모서리·꼭짓점·「작도 시점으로」에서도 세로축 이탈 0이다(`more_vertical_off_ortho_deg`) | 18차 신설 |
| **AS-L58** | **f를 바꾸면 투영만 바뀌므로 «3D 불변»을 확인하면 충분하다** | ❓ **불충분했다 — 팔을 보강했다.** 「3D 불변」만 보면 **f 숫자만 바뀌고 렌더가 그것을 안 읽는 상태**와 갈리지 않는다(#30). 양성 채널을 넣었다: 같은 획의 끝점이 화면에서 **20.8px 움직인다**(`lens_roll_osnap.json@f351839a`의 `r_lens.marked_end.moved_px` — ⚠ 초판 등재 값 12.59는 낡은 인용이었다: 20차 전수 훑기에서 발견, 20차 세션 시작 커밋(37a4bf2)의 원장도 이미 20.8이다. 언제 갈렸는지는 안 파고들었다 — 판정(0이 아니다·양성 채널이 산다)은 어느 값이든 같다). ⚠ **렌즈를 바꾼 화면이 «옳은가»는 여전히 안 잰다** — 카메라 조작이라 옳고 그름이 없다 | 18차 신설 |
| **AS-L59** | **하단바가 한 줄 가로 스크롤이어도 조작에 문제가 없다** | ⛔ **반증(실측).** 폭 500px에서 오스냅 종류 여덟 중 **넷만** 보였고 가로 넘침이 948px였다(`lens_roll_osnap.json@f351839a`의 `t_panel.reproduced_before_fix`). 1280px에서는 여덟이 다 보여 **안 드러났다** — 좁은 폭이 아니면 안 잡히는 형태다. 줄바꿈으로 고쳤고 500·360px 둘에서 8/8이다(D-L113) | 18차 신설 |

## 2026-08-20 20차 (AS-L60 ~ AS-L61 — D-L118 국면 분리)

| 번호 | 가정 | 상태 |
|---|---|---|
| **AS-L60** | **그리기 도중의 사선으로 차수를 올리는 것(조용한 1점→2점 승격)은 사용자 의도가 아니다**(D-L118이 그리기 국면에서 vp_fixed를 뺀 근거) | ❓ **미검증 — 사람 보고의 방향과는 일치한다**(«확정 후 그은 획이 사라진다»가 승격 의도였다면 «사라진다»로 보고되지 않았을 것). 실측 근거는 승격이 **암묵**이었다는 것뿐이다(`post_confirm_invariant.json@f351839a`의 `revived_bug` — 알림 없이 order 1→2). 반대 방향 표본(사선을 긋고 2점을 기대한 손짓)이 오면 D-L118의 되돌릴 조건 ①이 열린다 — 그때 답은 게이트 삭제가 아니라 명시적 작도 재진입이다 | 20차 신설 |
| **AS-L61** | **그리기 국면의 지지선 세기 부재는 아무 표시·판정도 굶기지 않는다**(D-L118의 대가 표 2행) | ❓ **미검증 — 전량 검증이 반례를 안 냈다는 것까지만**(전건 통과 — 규모·시간은 `test_cost.json@f351839a`의 `totals`가 정본이다 #47, 죽은 원장 확인은 progress 20차 «전수 훑기» 절). 지지선 수(`support`)는 차수 계산(`perspectiveOrder`)에 안 들어가지만, 그것을 읽는 표시가 **앞으로** 생기면 이 가정이 그 자리의 결함이 된다 — DEFERRED 20차 2행이 트리거를 든다 | 20차 신설 |

## 2026-08-27 web2-17 (AS-C51 ~ AS-C52 — 지평선 상시)

| 번호 | 가정 | 상태 |
|---|---|---|
| **AS-C51** | **지평선의 문서 y = H/2 상수가 사람의 눈높이 선언을 대신할 수 있다**(팬이 종이를 옮기므로 결과가 같다 — web2-17 1-a) | ⏳ **논증+오라클 실측, 실기기 미검증.** 평행이동 동치는 값으로 섰다(`horizon_equiv_web2.json` — 옛 코드 오라클 대비 3D 오차, 정수 격자는 구성상 0·반증 dy=0은 0.5 이상 벌어짐). 남는 가정은 **사용감**이다: «팬으로 지평선을 옮긴다»가 «지평선을 긋는다»만큼 자연스러운가 — 실기기 몫(DEFERRED web2-17 표). 이 가정은 AS-C5의 y 상수화와 한 몸이다. 부속 동작점: 지평선 «따라긋기» 퇴화 대역 = OSNAP_RADIUS_PX(8px — #12: 스윕 없음, 경계 플립은 `entry17_web2.json` band_boundary가 8/9px에서 실측). 실기기에서 손이 지평선을 따라 그을 때 8px 안에 드는가가 되돌릴 조건이다 | web2-17 신설 |
| **AS-C52** | **선언(첫 획) 전에는 줌을 막아도 된다**(줌은 화각의 두 번째 선언이 되므로 — f = 0.87·W가 문서 단위라 줌 뒤 같은 손짓이 다른 화각으로 앉는다) | ⏳ **근거는 기하, 사용감 미검증.** 막힌 줌이 무반응으로 읽히는 몫은 첫 안내 문구(「줌은 첫 획 뒤에 열린다」)가 진다 — 실기기에서 그 문구로 충분한가가 되돌릴 조건(DEFERRED web2-17 표) | web2-17 신설 |

## 2026-08-27 web2-17 5부 (AS-C53)

| 번호 | 가정 | 상태 |
|---|---|---|
| **AS-C53** | **지평선 자동 숨김의 «첫 소실점이 화면 안» 조건이 사람의 읽기와 맞는다** — 규칙은 문면(「소실점이 **하나** 생기는 순간」) 그대로 **첫 소실점(vp0)** 하나를 본다. ⚠ 알려진 좁힘: 2점 상태에서 vp0만 밖으로 나가고 vp1이 보이면 — 눈높이는 vp1로 여전히 읽히는데 — 지평선이 다시 뜬다(문면 준수의 대가 — 성가시면 «보이는 소실점이 있으면»으로 넓힌다) | ⏳ **규칙은 팔로 섰고(horizon_hide_web2.json — 온·오프·제스처 동결 0회) 사용감 미검증.** 여백 0(뷰포트 사각형)이 동작점이다(#12 — ✕ 컬링과 같은 함수·여백만 다름). 되돌릴 조건: 실기기에서 「소실점이 가장자리에 걸쳐 있을 때 지평선이 성가시게 드나든다」 관측(그때 여백/이력을 단다) | web2-17 신설 |

## 2026-08-27 web2-18 (AS-C54 ~ AS-C58 — 겹·연장선 획득·제스처 타일)

| 번호 | 가정 | 상태 |
|---|---|---|
| **AS-C54** | **「잉크가 보인다」의 하한 0.7**(연필 위 잉크가 맨 종이 위 잉크의 이 비율 이상 달라져야 한다 — `e2e/inklayer.spec.ts`의 `VISIBLE_RATIO`) | ⏳ **동작점 하나 — 스윕 없음**(#12). 1.0이 아닌 이유는 기전이 있다: 흑연 위에서는 잉크 AA 가장자리의 대비가 낮아 문턱(`PIXEL_DIFF_CH`) 아래로 떨어지는 칸이 남는다. 실측이 그 선에서 멀다(수리 전 **0.149** ↔ 수리 후 **1.052** — `inklayer_web2{,_before}.json`)이므로 지금은 어느 쪽으로도 여유가 크다. 되돌릴 조건: 수리 후 값이 이 선 근처에서 흔들리면 대비 자체가 지표로 못 쓰는 것이므로 지표를 바꾼다 | web2-18 신설 |
| **AS-C55** | **연장선 획득의 머무름 400 ms**(`C.EXT_ACQUIRE_MS`) — 「지나가는 중」과 「멈췄다」를 가르는 자리 | ⏳ **동작점 하나 — 스윕 없음·실기기 미측정**(#12). 근거는 호버 의도의 관용 대역(웹 툴팁의 관행 지연 300~500 ms)이고 **손 표본은 0이다**. `FOLD_DELAY_MS`(1200)와 다른 물음이다(그쪽은 «연속 조작의 손 뗌 간격»). 되돌릴 조건: 「획득이 안 된다(오래 기다린다)」면 내리고 「스치기만 해도 획득된다」면 올린다 — **화면에 표식이 있어 사람이 그 자리에서 판정한다**(DEFERRED 실기기 표) | web2-18 신설 |
| **AS-C56** | **획득한 끝점에서 연장이 사는 길이 = 그 선분 길이의 2배**(`C.EXT_MAX_RATIO`) | ⏳ **동작점 하나 — 스윕 없음**(#12). 근거는 제도의 어법(「이 선을 늘려 만나는 자리」는 그 선과 비슷한 스케일의 이웃이고, 그보다 멀면 다른 구속이 답할 자리다). **상한의 몫은 값으로 잡혔다**: 허공 855칸에서 끝점을 전부 획득해도 `ext`가 75가 아니라 **55**다(`extacq_web2.json` — 20칸을 상한이 잘랐다). 되돌릴 조건: 「연장이 닿아야 할 데까지 안 간다」 관측 | web2-18 신설 |
| **AS-C57** | **화면 밖 잘라내기 여유 24 px**(`brushlayer.CLIP_MARGIN_PX`) — 획 중심이 화면 밖이어도 입자가 안으로 들어오는 몫 | ⏳ **실측 + 여유.** 입자가 획 중심에서 옆으로 번지는 거리를 **매 실행 잰다**(`gesture_tiles_web2.json`의 `clip_margin.particle_spread_css_px` — 기본 재료에서 2 CSS px). 굵기 대역 상한(니브 4 px)과 더해도 6 px이므로 24는 그 네 배다. ⚠ **반증을 실제로 돌렸다**: 여유 0에서 팔 ②-b가 빨개진다. ⚠ 번짐은 **재료·굵기에 따라 다를 수 있다** — 잰 것은 기본 재료 하나이므로 그 축의 스윕은 없다 | web2-18 신설 |
| **AS-C59** | **동시에 획득해 두는 끝점은 둘이다**(`C.EXT_MAX_ACQUIRED` — LRU) | ⏳ **동작점 하나 — 스윕 없음**(#12 · 1차 리뷰어 [16]이 빠진 것을 잡았다). 근거는 «두 연장의 교점»이 연장선 오스냅의 표준 용법이라는 것(AutoCAD Extension도 둘을 함께 든다). 셋 이상이 필요한 사용은 관측된 적이 없다. 되돌릴 조건: 「셋째를 획득하면 첫째가 사라져 불편하다」 관측 | web2-18 신설 |
| **AS-C60** | **화면보다 긴 획은 «줄여 굽고 늘려 붙여도» 된다**(3-c ㉢ — 아틀라스가 `#brushc`(화면 크기)이므로 화면 폭을 넘는 획은 그 안에 안 들어간다) | ⏳ **발화를 확인했고 대가는 미측정.** 안 그리는 길은 사람의 구속(「돌리는 중에도 질감」)이 막으므로 **줄여 굽고 늘려 붙인다** — 그 획만 결이 더 늘어난다. 실제로 발화한다: `gesture_tiles_web2*.json`의 `tiles_after_release.bakeClamped = 1`(팬 국면에서 한 획 — 지평선 대역의 긴 획이다. 궤도 국면은 0). ⚠ **그 획 하나의 결이 얼마나 늘어나는지는 따로 안 쟀다** — 전체 상자 차(`release_diff`)에 섞여 있다. 되돌릴 조건: 실기기에서 「긴 선만 돌릴 때 흐려진다」 관측 → 아틀라스를 여러 판으로 쪼개 온전한 길이로 굽는다(`bakePasses`가 이미 다판을 돈다) | web2-18 신설 |
| **AS-C58** | **제스처 중 «구운 타일의 아핀 변환»이 사람이 받아들일 만한 흑연이다**(3-c ㉢ — 길이 방향으로만 배율이 실리므로 결이 늘거나 눌린다) | ⏳ **대가를 값으로 재 뒀고 사용감은 미검증.** 놓은 뒤 화면과의 차: 상자(520×320)에서 dpr1 **7486칸(4.5%)** · dpr2 **20373칸(12.2%)**, 어두운 칸은 dpr1 5477→6185(+13%) · dpr2 13481→19181(+42%) — **제스처 중이 놓은 뒤보다 옅다**(늘어난 결이 문턱 아래로 내려간다). 정본은 언제나 **놓은 뒤 화면**이다. 되돌릴 조건: 실기기에서 「돌릴 때 결이 이상하다/옅어진다」 관측 — 그때는 굽기를 제스처 중 다시(배율이 크게 달라진 획만) 하는 길이 있다(DEFERRED) | web2-18 신설 |

## 2026-08-28 web2-19 (AS-C61 ~ — 제도판)

| 번호 | 가정 | 상태 |
|---|---|---|
| **AS-C61** | **파선 ✕ 예고의 규격 = 확정 ✕와 같은 형태(6px 팔·1px 선·COL.vpMark)에 파선만 WAIT_DASH(5·4)** | ⏳ **눈으로만 골랐다**(값 자체는 기존 상수 재사용 — 새 숫자 없음). 6px 팔에 5·4 파선이면 팔 하나에 눈금이 1~2개라 «파선임»이 실기기에서 읽히는가는 미검증. 되돌릴 조건: 실기기에서 「확정 ✕와 구별이 안 된다」 관측 — 그때 파선 간격이 아니라 **크기·회전**을 가른다(파선 밀도는 이미 최소다) | web2-19 신설 |
| **AS-C62** | **종이 띠의 형태 — 높이 ≈28px(패딩이 정한다)·탭 폭 = 이름 길이·활성 표시 = 아래 테두리 소거(바탕과 이어짐)** | ⏳ **눈으로만 골랐다.** 값은 CSS(패딩 8/7px + 12px 글자)가 내고 하드코딩 높이는 없다(⑦의 조건). 활성이 색이 아니라 «종이와 한 장으로 붙음»인 것은 대응표 규칙 둘(형태가 가른다)을 따른 것. 되돌릴 조건: 실기기에서 「어느 장인지 안 보인다」 | web2-19 신설 |
| **AS-C63** | **길게 누르기 500 ms**(C.PAPER_LONGPRESS_MS — 썸네일·이름·삭제 팝업) | ⏳ **동작점 하나 — 스윕 없음**(#12). 근거는 모바일 관행 대역(≈400~500ms)이고 손 표본 0. 이동 허용은 OSNAP_RADIUS_PX 재사용(새 숫자 없음). 되돌릴 조건: 「탭이 자꾸 팝업이 된다」(올린다) / 「안 뜬다」(내린다) — DEFERRED 실기기 표 | web2-19 신설 |
| **AS-C64** | **전체 화면 나가는 손잡이 = 우하단 22px 아이콘(빌드 식별자 위)** | ⏳ **눈으로만 골랐다.** 크기·자리 근거는 「보이되 그림을 안 가린다」의 한 점(빌드 식별자와 같은 구석·같은 급의 크롬). 되돌릴 조건: 실기기에서 「전체 화면에서 못 나오겠다」 — 그때 크기를 키우지 자리를 옮기지 않는다(우하단이 유일한 상시 구석이다) | web2-19 신설 |
| **AS-C65** | **눈 띠의 자리 right:170px — 뷰 큐브(cx W−110·size 80)와 안 겹친다** | ⏳ **산술 + 눈.** 큐브 왼끝 W−150, 눈 띠 오른끝 W−170 — 20px 여유(동작점 — 스윕 없음 #12). 되돌릴 조건: 화면이 좁은 기기에서 종이 띠(#topleft max-width 100vw−140)와 겹치는 관측 | web2-19 신설 |
| **AS-C66** | **선 아이콘 굵기 = Phosphor light** | ⏳ **값이 근거지만 표본이 좁다**(1차 리뷰 [5]로 ✅에서 내렸다): 잉크량(알파 합·64px 합성 래스터 — `icon_weight_web2.json`) 최근접이 light(ref 568 · thin 357 · light 524 · regular 710). 선 굵기 환산(stroke_frac × 30px 렌더 = ref 1.95px·light 1.41·regular 1.875 — 환산 조건은 원장 def)은 regular가 근소하게 가깝다. ⚠ 잰 것은 **arrows-out 한 글리프 대 ref 한 글리프**(글리프 축 스윕 없음 — #12)이고, 합성 래스터라 **dpr 무관**(dpr1·dpr2 원장이 같은 값인 것은 설계다 — 화면 렌더가 아니다). 최종 판정은 실기기 눈이다(DEFERRED 「아이콘 판독」 행). 되돌릴 조건: 「새 아이콘이 옛것과 무게가 다르다」 | web2-19 신설 |
| **AS-C67** | **「무채색」의 팔 판정 = 채널 스프레드 ≤ 40**(icons.spec ② — 속성·계산색 둘 다) | ⏳ **동작점 하나 — 스윕 없음**(#12 · 2차 리뷰 [9]로 등재). 근거 대역: 이 앱의 웜 그레이는 스프레드 13(--ui #8d8880)~32(표식 #8a7f6a)이고 강조·정체색은 95(옐로 롤)~168(안내 파랑) — 안쪽 여유 8 · 바깥쪽 여유 55. 되돌릴 조건: 웜 그레이 파레트가 더 짙어져 32가 40에 다가오면(그때 임계가 아니라 **파레트 목록 대조**로 바꾼다 — 스프레드는 근사다) | web2-19 신설 |

## 2026-08-28 web2-20 (AS-C68 ~ — 종이를 얹는다)

| 가정 | 내용 | 상태 | 비고 |
|---|---|---|---|
| **AS-C68** | **종이 둘의 «불투명도» = 막 색조 그 자체**(옐로 [242,227,179] · 트레이싱지 [230,233,237]) — 곱 합성에서는 밝기가 곧 비침이라(흰색=투명) 별도 불투명도 값이 없다 | ⏳ **스윕 없음 — 동작점 하나씩**(#12). 옐로는 이름이 색이라 대역이 좁고, 트레이싱지의 **한색(살짝 파랑)은 ④ 곡선 실측이 정했다** — 난색 회색이면 옐로와 섞일 때 곱이 채도를 «올려» 곡선(섞으면 채도 하강)이 안 선다. 하한은 ⑧ 팔(세 장 겹침에도 아래 획 상대 유지 ≥0.35×)이 지킨다. 되돌릴 조건: 실기기 「옐로가 옅다/짙다」 — 색조만 움직이고 곱·구조는 안 건드린다 | web2-20 신설 |
| **AS-C69** | **결의 진하기 = 섬유색 tint×0.82(B만 0.8)·알파 0.02~0.07 · 개수 옐로 420/트레이싱지 170** | ⏳ **눈으로 골랐다**(스윕 없음). 곱이라 결이 진할수록 아래가 어두워진다 — ⑧이 하한을 지킨다. 되돌릴 조건: 실기기 「결이 그물 같다/안 보인다」 | web2-20 신설 |
| **AS-C70** | **타일 256 device px · 패턴 배율 k=0.5·view.s·dpr**(타일 1px = 0.5 doc 단위 — dpr2에서 원해상도·dpr1에서 절반의 고운 결) | ⏳ 256은 관행 대역(2의 거듭제곱·재굽기 비용과 반복 주기의 절충 — 스윕 없음). ⑤'(이음매 없음)·⑦(rect 성장 불변)이 값 무관의 성질을 지킨다. dpr2 재굽기는 ⑤'' 실측 | web2-20 신설 |
| **AS-C71** | **줌에서 결이 함께 커진다**(k에 view.s가 실린다 — 결은 종이의 성질이지 화면의 성질이 아니다) | ⏳ **큰 배율의 뭉개짐 상한은 안 걸었다** — 상한을 걸 근거 관측이 없다(줌 스윕 없음). 되돌릴 조건: 실기기 「크게 확대하면 결이 뭉개진다」 — 그때 k에 상한(또는 고배율 재굽기) | web2-20 신설 |
| **AS-C72** | **가장자리 선 = COL.vpMark · 비활성 0.4/활성 0.8 알파 · 0.8/1.2px** — 「여기까지가 이 종이다」를 옅은 무채색 둘레로 | ⏳ **눈으로 골랐다**(작도 표식과 같은 색 — 새 색을 안 지었다 #54). 막이 그려질 때만(같은 filmSplit). 되돌릴 조건: 실기기 「경계가 안 보인다/시끄럽다」 | web2-20 신설 |
| **AS-C73** | ~~rect 기본값 = 얹는 순간 보이는 화면~~ ⚠ **web2-21 3-b로 대체 — AS-C76(짧은 변 5% 인셋 + 층별 흔들림)이 정본이다**(제자리 정정 · 3·4부 리뷰 [5]). 자람·끌기 세부는 그대로 유효 · 이후 획이 밖으로 나가면 **커밋 시 bbox 합집합으로 자란다** · 가장자리 끌기로 손 조절(오스냅 반경 허용) | ⏳ 「보이는 화면」은 지시 문면이고, **자람과 끌기의 세부(최소 8 doc px·rect가 줄 때 획이 밖에 남는 것 허용)는 동작점**이다. 되돌릴 조건: 실기기 「종이가 멋대로 커진다/줄일 수 없다」 | web2-20 신설 |

## 2026-08-28 web2-21 (AS-C74 ~ — 겹의 규율 · 면 일괄)

| 가정 | 내용 | 상태 | 비고 |
|---|---|---|---|
| **AS-C74** | (반증 기록 — D-4) 지시 1-b의 원인 서술 「스케일 기준 획의 겹을 끄면 스케일이 **다음 치수 획으로 물러난다**」 | ❌ **반증됨** — 실측은 **물러가지도 못하고 null**(scaleOf가 scaleRef 획을 doc에서 찾고 lifted 부재로 그대로 null 반환 — 바탕에 치수 획이 있어도 폴백이 안 탔다). 원장 `scale_layer_web2_before.json` A·B행. 수리(후보를 바탕 획으로 좁힘) 후에는 상태 자체가 앱 경로에서 안 생긴다 | web2-21 1-b |
| **AS-C75** | 1-a 하네스 동작점: 지터 폭 **±3px**(x·y 독립) · 창-벽 명목 간격 mid 15.6/low 13.7px · 위치 2 · 대조 시드 2×2 | ⏳ **스윕 없음 — 동작점**(#12). 「안 닿음」 전제는 명목이 아니라 **칸마다 실측**(`minGapPx > 8` 단언 — 전 칸 최소 11.7)이 지킨다. 지터 ±3의 근거는 오스냅 반경(8) 안의 손 대역이라는 것뿐 — 실기기 손 분포 실측 아님 | web2-21 1-a |
| **AS-C76** | 겹 기본 rect = 짧은 변 **5%** 인셋 · 흔들림 평행이동 **±6px**·크기 **±4px**(화면 px · rng32(layer.id)) | ⏳ **눈으로 골랐다**(지시의 5%는 초안 — 스크린샷 대조로 «가장자리가 읽힌다» 확인, 스윕 없음). 그림자·회전 없음은 지시 ⛔. 되돌릴 조건: 실기기 「종이가 안 올라가 보인다/어긋남이 산만하다」 — 값만 움직인다(구조 불변). ⚠ 인셋은 **비례**(짧은 변 5%)·흔들림은 **고정 px**라 작은 화면(짧은 변 400px → 인셋 20px)에서는 비가 뒤집힌다(3·4부 리뷰 [10]) — 실기기 화면 크기에서 어긋나면 흔들림도 비례로 바꾼다 | web2-21 3-b |
| **AS-C77** | 면 일괄 후보 테두리 규격 = **COL.snap · 파선 6/4 · 1.6px**(화면 고정 — render2d #ink) | ⏳ **눈으로 골랐다**(스윕 없음 — 순간 피드백 채널의 기존 색을 재사용해 새 색은 안 지었다 #54). 되돌릴 조건: 실기기 「후보 테두리가 시끄럽다/안 보인다」 | web2-21 4-d |

## 2026-08-28 web2-22 (AS-C78 ~ — 옐로 2D · 후행 확정 · 자동 저장)

| 가정 | 내용 | 상태 | 비고 |
|---|---|---|---|
| **AS-C78** | cost22 격자의 동작점: 옐로 비율 **0.5 고정**(지시 문면 「절반」) · rawIn 비율 **0.5 고정**(cost18의 가운데 칸) | ⏳ **비율 축 스윕 없음**(#12 — 0·1부 리뷰 [7]). 옐로 «자체»의 osnap 기여 0은 설계 보장(원장 flags_explained)이고, 비율이 다른 장면의 절대값은 미측정. 되돌릴 조건: 실기기 자유 스케치의 실제 옐로 비율 관측이 0.5와 크게 다르면 그 비율로 재측정 | web2-22 0부 |
| **AS-C79** | 후행 확정 동작점 셋: `HOLD_MS` **600ms**(Procreate 중간보다 짧게 시작) · `HOLD_JITTER_PX` **4** · `YELLOW_SNAP_DEG` **5°** | ⏳ **셋 다 동작점**(#12 — 지시 문면 「재서 정한다」의 «재기»는 실기기 몫: DEFERRED 「머무름 시간이 손에 맞는가」 행이 판정자다). 팔은 양끝(0·1e9)과 대역 경계(±0.1°)만 잰다 | web2-22 2부 |
| **AS-C80** | 자동 저장 상한 **5MB 가정**(localStorage — 알아낼 표준 없음·지시 문면) · 사전 경고 임계 **70%** | ⏳ **가정이다**(지시가 가정이라 적으라 명령). 실제 실패가 나면 그때의 바이트를 원장에 적어 가정을 고친다(3부 문면). 70%는 동작점 | web2-22 3부 |
| **AS-C81** | 후행 확정 표식 = **무채색 고리**(COL.construction · 반지름 7px · 1.2px 선 — 끝점에·머무름 성립 동안) | ⏳ **눈으로 골랐다**(순간 피드백 대역 — 새 색 없음 #54). 되돌릴 조건: 실기기 「반듯해진 순간을 모르겠다/시끄럽다」(DEFERRED 표식 행) | web2-22 2부 |


## 2026-08-28 web2-24 (AS-C82 ~ — 관통선 하네스 · 옐로 프리핸드)

| 가정 | 내용 | 상태 | 비고 |
|---|---|---|---|
| **AS-C82** | 옐로 raw 솎기 임계 `C.RAW_SIMPLIFY_PX` **0.5px(화면)** — «눈에 안 보이는 선» | ⏳ **동작점**(#12 — 지시 초안값 그대로. ⛔ 크게 잡지 마라 — 지시 §4). 반증 실행: 5px 변이는 이탈 2.0~4.3px로 눈 대역(1px)을 넘는다(`yellowraw_web2.json` simplify.refute_5px — 임계가 실제로 그림을 가른다). 되돌릴 조건: 실기기 「솎은 선이 원본과 달라 보인다」 → 값만 내린다 | web2-24 4-b |
| **AS-C83** | 1부 관통선 하네스 동작점: 지우기 **확대 view.s=2**(문서 실효 반경 6px) · 지우기 점 = 바깥 토막 중앙(실제 선 위) · 시드 7종 × 위치 2(21 격자) | ⏳ **동작점**(#12·#14 — 2차 [5][6]). 실측 여유 min **6.7px**(실효 6 대비 0.7px — 시드가 늘면 더 좁은 칸 가능). s=1(반경 12)이면 14칸 중 **4칸**이 부수 삭제 대역(`passthru24_web2.json` aggregate.cells_clearance_below_12) — 실기기 표 「확대 없이 딸 때」 행이 그 판을 잇는다 | web2-24 1부 |
| **AS-C84** | 겹 가장자리 손잡이가 뜨는 거리 = **오스냅 반경 재사용**(`C.OSNAP_RADIUS_PX` 8px / view.s — rect 끌기 판정 `rectEdgesAt`와 같은 값·같은 함수) | ⏳ 새 숫자를 안 지었다(#54). 표시 규격(COL.snap · 알파 0.5 · 1.2px)은 **눈으로 골랐다**. 되돌릴 조건: 실기기 「손잡이가 안 잡힌다/너무 일찍 뜬다」 | web2-24 4-d |

## 2026-08-28 web2-23 (AS-C85 ~ — 굽기·은선)

| 이름 | 가정 | 상태 · 근거 | 회차 |
|---|---|---|---|
| **AS-C85** | 밑그림의 경도: **보이는 선 = `F` · 가린 선 = `H`**(둘 다 `MAT` 그대로 — 새 색·굵기 ⛔) | ⏳ **사람이 정했다**(지시 2-a 원문 「가린 선은 h 계열, 보이는 선은 f 나 b 계열」). F·H를 고른 근거: 두 계열 안에서 **가장 가까운 이웃**이라 종이 위에서 한 그림으로 읽힌다(B 쪽으로 벌리면 결과선처럼 무거워진다). 되돌릴 조건: 실기기 「은선이 안 읽힌다/너무 진하다」 → 계열 안에서 한 칸 옮긴다(DEFERRED web2-23) | web2-23 2-a |
| **AS-C86** | 굽기의 «같은 평면» 판정(점–평면 거리)과 «깊이» 여유(광선 위 거리)가 **둘 다 `C.PLANAR_RATIO × 기하 크기`를 쓴다** ⚠ **같은 수이지 같은 양이 아니다** — 평면 거리는 법선 방향, 깊이는 시선 방향이고 둘의 환산 계수가 `1/(n·rd)`다(#74 ㉠의 실측: `n·rd≈0.108`에서 어긋남 0.026~0.064가 깊이 0.28로 커진다) | ⏳ **동작점 하나**(#12 — 리뷰 [10]로 ✅에서 내렸다). 논증된 것은 **하한**뿐이다(더 엄해질 수 없다: 그 두께 안에서 만난 선들로 만든 면이다). **상한은 안 쟀다** — 느슨하면 실제로 가려야 할 얕은 겹침을 놓친다. 되돌릴 조건 둘: ㉠ 실기기 「가려야 할 선이 안 가려진다」(→ 깊이 여유만 좁힌다: 그때 두 값이 갈리고 상수도 갈라야 한다) ㉡ 「자기 면 위의 선이 사라진다」(→ 평면 판정을 넓힌다). **새 숫자를 안 지었다**(#54) — 반증 실행: 조항을 빼면 «거의 평면 + 비스듬한 시선»에서 벽 위의 선이 자기 벽에 가려 사라진다(`make2d.test.ts` ㉮ — 두 부등식을 값으로 못 박았다) | web2-23 1-c |
| **AS-C87** | 굽기 상한 **500ms**(넘으면 진행 표시) — 「사람이 기다릴 만한가」의 초안값 | ⏳ **지시의 초안값**. ⚠ **D-4 — 지시 1-b의 「수십 ms로 예상된다」는 짐작이었고 측정이 다르게 가리켰다**: 격자 최악 칸(획 400×면 40 = 대상 선분 560·조각 5024)이 **한 자릿수 ms**다(`cost23_web2.json` — 여러 실행의 관측 대역 **4.8~6.2ms** · 상한의 ~1%. ⚠ **시간 값은 실행마다 뜬다** — 문서는 대역만 적고 칸별 수는 원장 `rows`가 정본이다: 2차 리뷰 [1]이 그 자리를 잡았다). 상한 자체는 원장 `run.budget_ms`(= `C.BAKE_BUDGET_MS`)가 정본이다. 그래서 **진행 표시를 안 만들었다**(발화 조건 없음 — 범위를 안 넓힌다). 되돌릴 조건: 실기기 멈춤 관측 또는 이 원장의 최악 칸이 500ms에 근접. ⚙️ **web2-25가 같은 원장에 `indoor_near_clip`을 더했다**(근평면 잘라내기가 붙어 실내 시점에서 면이 처음으로 계산에 든다 — 조각 520→3,253). 「이 원장의 최악 칸」에는 **그 블록도 든다**(둘은 같은 실행의 값이다). 지금은 둘 다 한 자릿수 ms이고 `over_budget`이 둘 다 빈 목록이다 | web2-23 1-b |
| **AS-C88** | 밑그림은 **자기가 대체하는 선 자리만** 도려낸다(그 선을 종이색으로 지우고 F·H로 다시 긋는다) — 지우는 굵기는 `C.NIB_MAX`(그 자리에 있을 수 있는 가장 굵은 선 — 새 숫자 ⛔ #54) · **단위는 화면 CSS px**(문서 좌표로 그리며 `1/view.s`를 곱하고 캔버스 변환이 `dpr×view.s`라 device px에서 `4×dpr` — 줌·dpr 무관하게 화면에서 늘 4px. 2차 리뷰 [12]: 이 회차가 dpr에서 데었으므로 단위를 적는다) | ⏳ **구조가 요구했고, 전량 e2e가 초판을 뒤집었다**: 「치환」이 서려면 원래 획을 지워야 하지만(안 지우면 「가린 선 빼기」가 아무 일도 안 한다), **rect 전체를 덮은 초판**은 web2-20 3부 게이트 ⑧(「세 장을 겹쳐도 아래 획이 읽힌다」)을 깼다(paper.spec ⑦⑧⑨ 대비 0.0002 — #71 ㉤의 형태). 선 자리만 도려내면 둘 다 선다. 대가(알려짐): 아래 겹의 획이 3D 선과 **겹치는 자리**에서 `NIB_MAX` 폭만큼 지워진다 — DEFERRED web2-23. 되돌릴 조건: 실기기 「밑그림 선 둘레가 허옇다」 → 폭을 재료 굵기 기반으로 좁힌다 | web2-23 2부 |

## 2026-08-28 web2-25 (AS-C89 ~ — 종이와 겹의 손)

| 이름 | 가정 | 상태 · 근거 | 회차 |
|---|---|---|---|
| **AS-C89** | 썸네일 스트립의 띠 높이 **48px**(썸네일 26+테두리 2 · 이름 11 · 패딩 4/3 · 사이 2 · 아래 테두리 1). 썸네일 칸은 **26×39px**(화면 비 1.5) | ⏳ **동작점**(#12 — 지시 3-b가 준 대역 「45~50px」 안. 실측 48px을 e2e `strip.spec` ①이 그 대역으로 단언한다). **사람이 늘 표시로 정했으므로 접지 않는다.** 활성은 **형태**가 가른다(바탕과 이어짐 + 썸네일 테두리가 진해진다 — 색 ⛔, 대응표 규칙 둘). 되돌릴 조건: 실기기 「썸네일이 너무 작아 시점을 못 알아본다」(올린다) / 「띠가 화면을 먹는다」(내린다) | web2-25 3-b |
| **AS-C90** | 셔터 번쩍임 **120ms**(`C.SHUTTER_FLASH_MS` — 흰색 알파 .55에서 0으로) | ⏳ **동작점**(#12). 근거는 순간 피드백의 관행 대역(100~150ms — 그보다 짧으면 프레임 하나에 묻히고, 길면 「깜빡였다」가 아니라 「가려졌다」로 읽힌다). ⚠⚠ **화면 잔존은 그 상수보다 짧다**(`shutter25_web2{,_dpr2}.json`이 정본 — 관측 대역 dpr1 **115~135ms** · dpr2 **80~100ms**. ⚠ 시간 값이라 실행마다 뜬다: 문서는 대역만 적는다): rAF 폴링의 해상도와 프레임 부하가 그 차의 몫이고 **dpr2에서 관행 대역 아래로 내려간다**. 즉 100~150은 **상수의 대역**이지 사람이 보는 시간의 대역이 아니다(리뷰 2차 [5]) — 실기기 D10이 「찍힌 줄 몰랐다」를 내면 그 값이 상수를 올리는 근거다. 팔의 문은 **48~400ms**이고 그 아래 문은 반증이 정했다(한 프레임 16ms은 지표의 분해능이라 상수 0에서도 통과했다 — #71 ㉢). **무채색**이다(지시 3-a ⚠ — 색 ⛔. e2e가 r==g==b를 단언한다). 되돌릴 조건: 실기기 「찍힌 줄 몰랐다」(올린다) / 「눈에 거슬린다」(내린다) | web2-25 3-a |
| **AS-C91** | 솔로는 **`setLayerOn`으로 나머지를 끈 것**이다(표시 전용 필드 ⛔) — 되돌릴 켬/끔을 런타임에 기억하고, 손으로 눈을 건드리면 **기억만 버리며**, 종이를 바꾸면 되돌린다 | ⏳ **판단이다**(지시 4-a ⚠가 「정하고 적으라」 한 자리 · 근거 D-W12 [6]). 그래서 「꺼진 겹은 3D에서도 빠진다」(web2-20 4-b)에 **자동으로** 같이 걸린다 — `solo.test.ts` ③이 「손으로 둘을 끈 것과 **같은 lifted 수**」로 값을 낸다. **대가**: 솔로인 채로 저장하면 «나머지를 끈 문서»가 저장된다(`on`은 문서 상태다). 되돌릴 조건: 실기기 「솔로로 보다가 저장했더니 겹이 꺼져 있었다」 → 저장 직전에 되돌리거나 표시 전용으로 옮긴다(그때는 게이트가 둘이 되므로 «둘이 갈리지 않음»을 팔로 못 박아야 한다) | web2-25 4-a |
| **AS-C92** | 저장 좌표 반올림 = **소수 1자리**, 그리고 **`raw`(표현용 점렬) 하나에만** 건다 | ⏳ **동작점**(#12 — 지시 5-b의 초안값 「소수 1자리」 그대로). 근거: 0.1px는 눈에 안 보이고 **솎기 임계(0.5px · AS-C82)보다도 촘촘하다** — ⚠ 그 비교는 **`view.s = 1` 프레임의 진술**이다(반올림은 문서 px · 솎기 임계는 화면 px). `view.s > 5`면 문서 솎기 임계가 반올림 칸보다 작아지고 그 대역은 **안 쟀다**(DEFERRED web2-25). 범위가 `raw` 하나인 것은 **팔이 정했다**(회차 중 두 번 좁혔다): 확정 끝점 `a`·`b`는 **잉크 심판**(own3d §7 · `OWN3_TOL_PX` 0.01px)이 걸려 있어 깎으면 왕복에서 불변식이 깨지고(own3d.test 넷), 밑그림 마디는 web2-23의 **왕복 동일성**을 깬다(underlay.test ④ — **되돌린 사유는 이 팔 하나**다). ⚠ 「밑그림은 문서의 0.1%라 얻을 것도 없다」는 **이 픽스처(마디 8개)의 관측**이고 실사용 대역이 아니다 — 최악 밑그림은 마디 609·약 90KB이고 그런 문서에서는 20% 대역이다(#75 ㉡의 「표의 몫은 픽스처의 성질이다」 · 그 대역 미측정은 DEFERRED web2-25). 그림이 안 바뀐다는 것은 e2e가 **두 차의 비교**로 낸다(반올림 몫 **0.02~0.05%** ↔ 재그리기 몫 0.18~0.32% ↔ 잡음 0.000% — 정본은 `roundsave25_web2{,_dpr2}.json`). 되돌릴 조건: 실기기 「선이 달라 보인다」(자릿수를 올린다) / 파일이 여전히 크다(자릿수를 내리는 것은 ⛔ — 그때는 다른 축을 본다) | web2-25 5-b |
| **AS-C93** | 옐로 획의 `rawIn`은 **press만** 싣는다(tilt·twist ⛔). 트레이싱지·바탕은 **종전 그대로**(전부 싣는다) | ⏳ **표가 지목했다**(`filesize25_web2.json`: rawIn 23.7%이고 그 안 tilt·twist가 **90,678B · press가 61,827B = 1.47배**. ⚠ 필드 수는 셋 대 하나인데 바이트 비가 1.47인 것은 **좌표 문자열의 길이가 다르기 때문**이다 — press는 소수 셋(0.123)이고 tilt·twist는 정수다(input.ts가 Math.round 한다). 초판이 「세 배」라 적은 것은 필드 수를 바이트로 읽은 것이다). ⚠⚠ **D-4 반증**: 지시 5-b의 근거문 「트레이싱지·바탕에서는 기울기가 **표현에 쓰인다**」는 **지금은 참이 아니다** — `tiltX`/`tiltY`/`twist`를 읽는 렌더 코드가 없다(grep: `brushmap.ts`의 `rawIn.press` 하나. 나머지는 캡처와 진단 패널의 살아 있는 사건뿐). 그래도 그쪽은 **안 건드렸다**: 지시의 ⛔이고 「솎지 않는 원본을 남겨 둔다」가 web2-24 4-b의 결정이다. 되돌릴 조건: 기울기를 실제로 쓰는 표현이 서면 **옐로에도 다시 싣는다**(그때는 이 행이 뒤집힌다) / 파일이 여전히 크면 트레이싱지 몫도 사람과 이야기하고 뺀다 | web2-25 5-b |

## 2026-08-29 web2-26 (AS-C94 ~ — 실기기 확인에서 나온 여섯)

| 이름 | 가정 | 상태 · 근거 | 회차 |
|---|---|---|---|
| **AS-C94** | 종이 결의 **주기·길이·굵기는 CSS px**에 못 박고(타일이 `TILE_CSS` 128 CSS px를 덮고 device 크기가 dpr을 따른다 — 타일 1px = 기기 1px), 진폭은 **실제 dpr에서 잰다**. 옐로 섬유 = 길이 8~22 · 굵기 **1.5~2.8** · 알파 **0.075~0.18** / 트레이싱 = 3.5~10 · **1.0~2.0** · **0.06~0.145** | ⏳ **실기기가 뒤집었다**(「종이·옐로·트레이싱지 **모두** 결이 안 보인다 — 스크린샷 확대에서도」 · DEVICE-CHECK G3). 종전 값은 **dpr에 두 번 실려** 있었다(타일이 늘 256 device px + 패턴 배율 `0.5·s·dpr`) — CSS 굵기가 `기본값×dpr/4`라 dpr1에서 **0.175 CSS px**였다. 실측(`paper_grain26_web2.json`): 결의 진폭이 dpr1 **0.134** · dpr3 **1.361**(255계조 중 — 웨버 1% ≈ 2.1계조 **아래**)이고 dpr 비가 **10.2**였다. 지금은 **3.910 / 4.179 / 4.334**(dpr 1·2·3)이고 비가 **1.108**이다. **굵기의 하한은 팔이 정했다** — 1.1~2.3에서는 dpr 비가 1.186이라 게이트(1.0±0.15) 밖이었다: 기기 픽셀 하나보다 가는 섬유는 dpr마다 다르게 뭉갠다. 반증 손잡이 `diag.fiberLegacyForTest`가 옛 규칙을 되살려 **매 실행 게이트를 깨뜨린다**. 되돌릴 조건: 실기기 「결이 너무 세다/지저분하다」(알파를 내린다) · 「아직 안 보인다」(올린다 — 그때 웨버 문턱 2.1의 가정을 다시 본다) | web2-26 2번 |
| **AS-C95** | **선 후보(`ext`)의 띠 = 점 반경의 2.5배**(20px) · **유지 띠 3.75배**(30px · 이력) | ⏳ **동작점**(#12 — 지시가 「넓은 띠」만 주고 수를 안 줬다). 근거: 점 후보는 2차원으로 국소화돼 있어 반경이 좁아야 모호하지 않고, 무한히 뻗은 직선은 **수직거리 한 축**만 뜻이 있다. `ext`는 **획득식**이라(web2-18 2부) 이 띠가 사람이 집은 선에만 걸린다 — 허공 끌림이 안 돌아온다. 실측(`extband26_web2.json`): 연장 대역 격자에서 획득이 **40/110 → 100/110**(8~18px 오프셋이 0/10 → 10/10) · 점 물림은 격자 1,815칸에서 **뺏긴 칸 0** · 갈린 칸은 전부 `ext`. 원칙 d는 넓힌 대역 전수 110표본에서 미리보기 == 커밋(그중 **70표본을 `ext`가 정했다** — 「넓힌 띠가 실제로 발화한다」의 분해능 단언). 되돌릴 조건: 실기기 「이제 너무 잘 붙어서 딴 데로 끌린다」(내린다) / 「여전히 까다롭다」(올린다 — 그때 20px 오프셋이 아직 0인 것이 다음 대역이다) | web2-26 3번 |
| **AS-C96** | **`perp`는 선 후보가 아니라 점 후보다** — 띠를 안 넓힌다(기본 `perpLine: false`) | ⏳ ⚠⚠ **D-4 반증 — 지시와 갈렸다.** 지시 26-3은 「선 후보(`ext` `perp`)는 수직거리 기준의 넓은 띠」라고 했고, 그 근거는 「무한히 뻗은 직선은 수직거리 한 축만 의미가 있다」였다. **수선의 발은 무한 직선 위의 점이 아니다** — `closestOnSeg3`가 선분 **안으로 클램프**하고(끝점에 닿으면 끝점 스냅의 몫이다) 그래서 점 후보와 똑같이 2차원으로 국소화돼 있다. 그리고 `ext`와 달리 **획득 절차가 없어** 승격 선분 전부에 늘 켜져 있다. 측정: 띠를 2.5배로 넓히자 깨끗한 세로획 (640,416.25)→(640,450)의 끝이 **(640,465)로 15px 밀렸고 축(V)을 잃었다** — 「조용히 틀린 배치」이고 지시 자신이 경고한 자리다(web2-17 회귀 위험). 기존 팔 둘(`own3d.test` 4-d와 그 D-3 반증)이 같은 이유로 빨개졌다. 반증 손잡이 `OsnapSettings.perpLine`을 남겨 **팔이 매 실행 그 밀림을 실증한다**(`extband26.test.ts` ⑤). 되돌릴 조건: 실기기에서 「수선이 안 걸린다」가 나오면 — 그때는 띠가 아니라 **`perp`의 발동 조건**(획득식으로 바꾸기)을 먼저 본다 | web2-26 3번 |
| **AS-C97** | 옐로 머무름 직선화의 문은 **둘**이다 — ① 표류: ref에서의 거리 ≤ `HOLD_JITTER_PX × holdMs / HOLD_DRIFT_REF_MS`(바닥 `HOLD_JITTER_MIN_PX` 2px) ② 창 속도: 최근 `HOLD_SPEED_WINDOW_MS`(120ms) 창의 순 이동 ÷ 창 ≤ `HOLD_SPEED_PX_S`(25 px/s). 기본 임계 시간 **350ms**(종전 600) · 사람이 **100~1500ms**에서 고친다(기기 설정 — localStorage) | ⏳ **실기기가 「조금 길다」로 냈다**(DEVICE-CHECK D6). ⚠⚠ **시간만 내리면 반대편으로 넘어진다**: 고정 상자+시간의 실효 표류 문턱이 `상자 ÷ 시간`이라 600 → 350ms가 6.7 → **11.4 px/s로 넓힌다** — 천천히 그린 곡선이 **더** 잘 직선화된다. 그래서 상자를 시간에 **비례**시켜 그 문턱을 **어느 임계에서나 6.7 px/s로 고정**했다(실측 스윕 `hold26_web2.json`: 거의 직선 획에서 5 px/s는 서고 10 px/s는 안 선다 — 경계가 그 값이다). 창 속도(②)는 표류만으로 못 거르는 「작게 빠르게 흔들다 제자리로 돌아오는 손」의 몫이고, **지수평활이 아니라 이동창**이다(평활은 멈춘 뒤 문턱까지 `τ·ln(그리던 속도/문턱)`이 들어 「빨라졌다」를 통째로 먹는다 — 실측 600ms 대역). 결과: 멈춘 뒤 성립까지 **704 → 456ms**(같은 픽스처). 대역 100~1500ms는 프로크리에이트 QuickShape가 여는 범위 그대로다(A-3). 반증(D-3): `tickHold`의 `driftPx` override로 **옛 게이트를 그대로 재현**하면 8·10 px/s 곡선이 350ms에서 직선화되고 600ms에서는 안 된다 — 팔이 매 실행 그것을 낸다. 되돌릴 조건: 실기기 「아직 길다」(사람이 슬라이더로 내린다 — 기본값을 옮기는 것은 그다음) / 「가만히 뒀는데 안 반듯해진다」(창 속도 25를 올린다) | web2-26 4번 |
| **AS-C98** | **한 손가락의 뜻**은 `fingerPans(app)` 하나가 정한다 — ① 이 세션에서 **펜이 쓰였으면** 이동 ② 안 썼어도 **돌 것이 없으면**(`lifted` 비어 있음) 이동 ③ 그 외에는 **종전대로 궤도**. 손가락은 여전히 **안 그린다** | ⏳ **실기기가 냈다**(「눈높이 선언 단계에서 화면 이동에 두 손가락을 요구한다」 · DEVICE-CHECK G9). ⚠⚠ **D-4 — 지시가 적은 「현행(한 손가락 그리기, 두 손가락 이동)」은 이 코드가 아니다**: `pointerdown`의 touch 갈래가 `beginDraft` **앞에서 반환**하므로 손가락은 한 번도 그린 적이 없고, 현행은 「한 손가락 궤도 · 두 손가락 팬+줌」이다. 지시의 명령문은 「펜을 안 썼으면 **현행 유지**」이므로 그대로 따랐다 — 손가락 그리기를 새로 만들면 터치만 쓰는 사람에게서 **궤도를 통째로 뺏는다**(두 손가락은 팬+줌이라 대체 경로가 없다). ②는 지시에 없던 조항인데 **증상이 정확히 그 자리다**: 선언 단계는 `lifted.size === 0`이라 `orbitBy`가 첫 줄에서 반환해 한 손가락이 **아무 일도 안 했다**. 궤도를 뺏는 것이 아니라 **비어 있던 자리를 채우는 것**이라 잃는 동작이 없다. 실측(e2e `gesture.spec` 26-5): 선언 단계 한 손가락 드래그 → `ox 0 → 100` · 펜 세션에서 `ox 0 → 100` · **penUsed를 내리면 `ox 100 → 100`**(반증). `penUsed`는 **세션 한정 런타임**이고 한 번 참이면 안 내려간다(펜을 내려놓았다고 손가락의 뜻이 바뀌면 그게 더 헷갈린다). 되돌릴 조건: 실기기 「펜을 쓴 뒤 손가락으로 돌리고 싶은데 안 된다」 → 두 손가락에 궤도를 주는 것이 다음 후보다(지금 두 손가락은 팬+줌이라 자리가 없다 — 그때는 사람과 이야기한다) | web2-26 5번 |
| **AS-C99** | **필압 보정**(옵션 · 기본 꺼짐)의 모형: ① 재매핑 `p' = clamp((p−p0)/(p1−p0),0,1)^γ` ② 농도 `alpha = 바닥 + (천장_g − 바닥)·p'^k_g`(바닥 `PRESS_FLOOR_ALPHA` **0.06 · 전 등급 공통** · 천장 = `MAT[g].alpha` 그대로 · 기울기 `k` 2H 1.8 → 2B 0.7) ③ 굵기 `width = 기본_g · (0.6 + 0.4·p')`. 초안 `p1` **0.35**(보정 전) · 보정은 **두 획의 중앙값** · 결과는 **문서**(`doc.press`)에 | ⏳ **사람의 증상 진술이 정본이다**(DEVICE-CHECK B1 · 원문은 web2-26 지시 6). ⚠⚠ **D-4 — 지시 3의 원인 후보가 틀렸다**: 「농도와 굵기가 한 계수를 공유할 가능성이 크다」 → **공유가 아니라 부재**였다. 필압은 `pressureProfile`을 타고 **굵기에만** 실리고(p5.brush의 점별 pressure) 농도는 `strokeColor(grade)`의 등급 상수다 — 즉 「약하게 그으면 옅게」가 **아예 없었다**. 그것이 「하한선이 너무 높다」의 정체이고, 「진한 연필일수록 크게 느껴진다」는 열린 폭(천장−바닥)이 2H 0.44 → 2B 0.86으로 두 배라는 사실이다(둘 다 팔이 값으로 낸다). 실측: 켬에서 굵기 ×1.67 동안 농도 ×12.5(HB) — 지시 3의 「굵기 1.5~2배 · 농도는 거의 검정까지」 대역. 화면(dpr1): 같은 p=0.20 획이 꺼짐 162.3 → 켬 180.8(옅다) · 2B 최대 76.9 ↔ 2H 최대 213.0(천장이 등급을 가른다). **꺼짐은 «없음»이지 «on:false»가 아니다** — 파일 바이트가 옛 문서와 같고 렌더가 보정 갈래에 한 번도 안 들어간다(e2e ①: 껐다 켰다 다시 끈 화면이 **픽셀 동일**, 그리고 켠 화면은 **다르다**는 분해능 단언이 짝). **문서에 붙는 이유**(지시 5): 압력을 원값으로 저장하고 그릴 때 매핑하므로(원칙 b) 기기 설정이면 옵션을 켜는 순간 예전 그림들의 농도까지 바뀐다. ⚠ 알려진 대가: p5.brush가 **획당 한 색**이라 켠 획은 `C.PRESS_SEGMENTS`(12) 마디로 나눠 긋는다 — 꺼짐에서는 종전대로 한 번의 spline이다. 되돌릴 조건: 실기기 「보정을 켰는데도 하한이 높다」(바닥을 더 내린다) / 「너무 옅어서 안 보인다」(바닥을 올린다) / 「마디 이음매가 보인다」(마디 수를 올리거나 색 보간을 브러시 안으로 옮긴다) | web2-26 6번 |

## 2026-08-29 web2-27 (AS-C100 ~ — 지평선 위 · 눈 토글 · 파일 크기)

| 이름 | 가정 | 상태 · 근거 | 회차 |
|---|---|---|---|
| **AS-C100** | 지평선 **위**에 그은 첫 선은 **천장 평면 `y = 2·EYE_HEIGHT`**(3.2m)에 놓인다(`pointOnCeiling` — `pointOnGround`는 안 고친다). ⚠ **세로선은 예외다**: 걸침 거부가 안 걸리고 종전대로 아래 끝이 바닥이다. **걸치는 수평·깊이 선은 거부**하고 사유 `straddle`을 남긴다 | ⏳ **사람이 규칙을 냈다**(「지평선 기준으로 mirror해서 그 반대점을 원점으로」). 그 규칙의 기하학적 정체가 이 평면이고 **근사가 아니라 정확히 그 점**이다(눈높이 면에 대한 광선 뒤집기 → 바닥 교점 → 되뒤집기). 팔이 그것을 좌표로 확인한다(`ceiling27.test.ts` ① — 대칭점의 `x`·`z`가 9자리 일치, `y`가 눈높이에 대해 12자리 대칭). 값 3.2m은 임의가 아니라 **눈높이를 바닥에 대해 되접은 결과**이고 실제 층고에 가깝다. ⚠⚠ **세로선 예외는 팔이 강제했다**: 초판이 「걸치면 거부」를 종류 구분 없이 걸어 **눈앞의 기둥**(바닥에서 눈높이를 지나 올라가는 가장 흔한 획)이 통째로 대기로 떨어졌고 `fold_measure` 둘이 빨개졌다. 방향의 y가 0인 축(수평·깊이)만 「선 전체가 한 수평면」이라 걸침이 무한대를 뜻한다. ⚠ **거부는 되살림 패스에도 걸어야 했다**: 첫 갈래에서 거부해도 `while(extended)` 패스가 `s.a`만 보고 지면에 앉혀 **눈 뒤까지 뻗는 선분**을 만들었다(실측 z −16.704 → +16.704 — 조용히 틀린 배치). 다만 **천장 갈래는 그 패스에 안 넣었다**(지시가 「첫 선 처리에서」로 자리를 못 박았고, 넣었더니 지평선 위에 그린 평범한 깊이선들이 3.2m로 올라가 기존 팔 열둘이 깨졌다). 되돌릴 조건: 실기기 「천장에 그린 적 없는데 3.2m로 올라간다」 → 첫 선 갈래를 «두 끝이 다 위 + 축이 수평/깊이»로 더 좁힌다 | web2-27 1번 |
| **AS-C101** | **겹의 표시는 `on` 하나가 정한다** — `filmSplit`이 「활성 겹이 없으면 갈림도 없다」에서 「**겹이 없으면** 갈림도 없다」로 바뀌었다. 종이가 활성이면 **켜진 겹 전부가 막**이고 위/아래 갈림이 없다. 그리고 활성 겹을 끄면 활성이 **한 단계 아래**(켜져 있고 안 잠긴 겹)로 내려간다 — 없으면 종이로 | ⏳ **재현이 지시와 갈렸다**(D-2). 지시가 적은 증상은 「눈을 누르면 거기서 그린 선만 사라지고 종이는 남는다」였는데, 표식이 낸 것은 **그 반쪽의 다른 얼굴**이었다: ㉠ 활성 겹을 끄면 활성이 `null`이 되고 `filmSplit`이 막을 통째로 접어 **남아 있는 아래 겹의 종이면까지** 사라졌다(실측 204,192,151 → 244.7,242.7,237.8 = 맨 종이) ㉡ 그 뒤 눈을 **다시 켜면** `on`은 참으로 돌아오는데 활성이 `null`이라 **막이 접힌 채 남았다**(획은 `yellowVisible`가 `on`만 보므로 돌아온다) — 즉 사람이 본 「선만 돌아오고 종이는 안 온다」가 그 자리다. 원인은 하나다(#75 ㉠): **획과 종이면이 다른 술어를 읽었다**. 수리도 하나다 — 표시의 술어를 `on`으로 합친다. ⚠ 「겹이 없으면 갈림 없음」은 남긴다(겹을 안 쓰는 문서의 비용이 web2-18과 같다는 3-b 회계의 전제). ⚠ 「활성 겹 **위**의 막은 안 그려진다」(web2-20 3-b)는 **활성 겹이 있을 때만**의 규칙으로 그대로다 — paper.spec ③이 지킨다. 실측(e2e `eyelayer27`): 끄면 rgb 차 **0.000** · sd **0.000**(맨 종이와 동일) / 껐다 켜면 rgb 차 **0.000** · sd 3.914 → **3.914** · 획 3 → 3 / 활성 겹을 꺼도 아래 겹의 노란 색조가 남고 활성이 아래로 내려간다. 되돌릴 조건: 실기기 「종이에 그리려고 활성을 내렸는데 위 겹의 색조가 화면을 덮는다」 → 그때는 활성 null에서 막을 다시 접고, 대신 **눈으로 끈 것과 활성 내림을 다른 상태로 가른다** | web2-27 2번 |

