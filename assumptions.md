# assumptions.md

계획서(`docs/line_plan.md`)의 **전제**를 항목화한다(CLAUDE.md §5 자기검증 2).
각 단계 완료 시 관측과 대조하고, 어긋나면 **측정을 따르고** 여기에 반증을 적는다.

상태: ✅부합 · ⚠️갱신됨 · ❌위반(판정 함께 수정) · ⏳미검증

> **2026-08-15 L 전환에서 새로 시작했다.** 이전 원장은 `docs/archive/assumptions_SG.md`에 있다.
> S·G 접근의 AS-1~AS-20 중 **배치 계층·프리핸드 보존에 딸린 것은 전부 무효**다
> (앵커 체인, 떨림 평면, 지지 수, 정합성 검사, 미리보기-확정 차이).
> 아래 "이월" 절이 살아남은 것의 **단일 판정처**다 — 아카이브의 개별 행은 폐기 당시 상태로 읽는다.

> **⚠ 계획서의 자동 판정 신호 둘이 반증됐다**(L-A.4, AS-L3·AS-L6). §5.3의 전환 픽셀 차이와
> §6.2의 재투영 어긋남은 **입사 관계에서 유도한 양**이고 **입사는 사영 불변**이므로
> f 오차에 반응하지 않는다. 실측: f를 0.8~1.2배로 틀리게 줘도 둘 다 0.0이다.
> **하나의 오류이고 계획서를 고쳤다.** 판별법은 PITFALLS #5에 있다 —
> **틀린 카메라를 넣어 그 지표가 움직이는지 본다.**

---

## L 접근 신규 전제

| # | 전제 | 출처 | 관측 | 상태 |
|---|---|---|---|---|
| AS-L1 | **선 3개 교점이 소실점 확정 근거로 충분하다** | §5.1 | **아니다.** 그 조건은 **상자의 꼭짓점**이 완벽하게 만족하고, 노이즈가 있으면 참 소실점보다 더 잘 맞는다 — 첫 실행에서 검출된 소실점 셋이 전부 꼭짓점이었다. 조건 셋을 더해야 성립한다: **도달거리**(D-L3) · **지지 가중합 + 재배정**(D-L4) · **국소화 가능성**(D-L7). 그 뒤에도 확정률은 5구도 합산 **0.84~0.85**이고 오배정 **0.110~0.122**다(`vp_detect.json@509615a1`) | ❌ 반증, 조건 셋 추가 |
| AS-L2 | **2D 교점 제약으로 한 번에 풀면 지지 1 문제가 사라진다** | §5.2 | **부분 성립.** ⚠ **아래 0.079/0.995는 폐기된 대역이다**(L-B.1 정정, PITFALLS #27): 0.079는 `lift_gate` 값으로 **검출 오배정이 섞였고**(같은 하네스 무오차 행은 **0.0673**), 0.995는 **최대 연결 성분 분모**다(모집단 기준 **0.885**). 판정("부분 성립")은 유지되지만 **근거 수치를 0.0673/0.885로 읽는다**. 원문: 참 카메라를 주면 조용히 틀린 배치가 **34/430 = 0.079**로 G의 0.278보다 낫고 배치율은 0.887 → 0.995다. 그런데 **§5.2의 "2D 교점은 손실 없는 정보다"는 틀렸다** — 구 AS-20이 이미 반증한 것이 그대로 남아 있다("화면에서 가로지르는 것과 3D에서 만나는 것이 다르다", 조용히 틀림 0.057 → 0.278). 정확한 투영을 넣은 상자 하나에서도 27개 교점 중 **3개가 가림 교차**이고, 그 3개만으로 동차계의 영공간이 1차원이 아니게 되어(최소 고유값 6.95e-3) 참 상자가 복원되지 않았다(형태 오차 0.79). **끝점 근접 조건**을 더해 24/24로 만들었고 그래도 노이즈에서 가짜 교점이 **0.076~0.174** 남는다. 그리고 **초기 확정은 여전히 사후 일괄 해**이므로 "실시간이라 괜찮다"는 논거가 여기엔 걸리지 않는다 | ⚠️ 조건부 성립, 가짜 교점 잔존 |
| AS-L3 | **전환이 무변화로 보인다**(카메라가 맞으면 화면이 안 튄다) | §5.3 | **성립하지만 그것은 측정이 아니라 설계 보장이다**(selfcheck 자기참조 유형 3, PITFALLS #5). 각 점이 자기 광선 위에 있으므로 되쏘면 정확히 원본으로 돌아온다 — `segGap` 최대 **0.0**. 결정적으로 **f를 0.8·0.9·1.1·1.2배로 틀리게 줘도 0.0이다**(사전 등록한 반례 검사). 그러므로 §5.3의 "튀면 카메라가 틀린 것"은 **성립하지 않는다.** 대체로 제안한 교점 잔차도 같은 구간에서 0.0285→0.0262로 **안 움직인다** — 입사 관계는 사영 불변이므로 **원리적으로** 카메라를 못 검증한다. 사용자에게 실제로 보이는 변화는 **획이 직선으로 펴지는 것**이고(`chord_dev`, 옛 `inkGap`) 그것은 프리핸드를 미룬 대가다(§1.1). **⚠ L-B.1이 그 값도 측정이 아님을 확인했다** — f를 ±20% 틀리게 줘도 **소수 넷째 자리까지 그대로**다(`stage_cam.json@509615a1`의 `control_wrong_camera`, 5구도 전부). 값을 정하는 것은 **잉크 모델의 활**과 **획 길이**뿐이고, 분포는 잉크 등급을 따라 커진다 — 중앙 precise **0.52** · medium **0.78** · coarse **1.05**px(등록 시드 6 × 잡음 4). ⚠ **"정비례"는 아니다**: `lf_bow` 비 1:1.77:2.97에 대해 중앙 비는 1:1.49:2.00이고, **등급마다 놓인 획 수가 달라 구도 구성이 다르다**(#11). **실획 현실을 주장하지 않는다**(AS-C10) | ❌ 지표 폐기, 보장으로 재분류. **`chord_dev`도 잉크 모델의 성질이다** |
| AS-L4 | **시작점 스냅이 손 획에서 충분히 붙는다** | §3 | **L-B.3이 쟀다**(`snap.json@509615a1`). **붙기는 붙는다** — 반경 0.05에서 `end_jitter > 0`인 결론 집합의 맞음이 **2826/3240**(0.87)이고 미스가 56/3240이다. **그러나 "충분히"는 대상 밀도가 정한다**: 격자 k3(획 144 · 참 꼭짓점 간격 중앙 21.3px)에서는 **틀린 붙음이 반경과 무관하게 15076/38880에서 포화한다** — 겨냥 오차 중앙 12.19px(**같은 밀도에서 잰 값**)이 간격의 절반(10.65px)을 넘기 때문이다. **한계를 정하는 것은 반경이 아니라 (겨냥 오차 / 대상 간격)의 비**이고 **스냅으로 못 고치는 몫이 있다.** 배치까지 가면 반경 0.05에서 **2503/3240**이 놓이고 **놓인 것의 19%(474/2503)가 조용히 틀린다**(cut 0.2, `end_jitter > 0`). ⚠ 대상을 **참 3D**로 준 상한이고, 잉크는 합성이다(AS-C1) | ⚠️ **붙는다 — 다만 밀도가 상한을 정한다** |
| AS-L8 | **한 시점의 손 획 상자 하나로 카메라가 충분히 정해진다** | L-A.4가 발견 | **아니다 — 이것이 게이트 실패의 원인이다.** 참 카메라를 주면 조용히 틀린 배치 0.079(⚠ `lift_gate` 값. **같은 하네스 무오차 행은 0.0673**이고 대역은 그쪽이다 — PITFALLS #27), 검출 카메라로는 **0.641**이다. 지배항은 f가 아니라 **축 방향**이다: f만 10% 틀리게 줘도 형태 오차 중앙은 0.098 → 0.115뿐인데, 검출 축 방향은 참 축에서 중앙 **4.18°** 벗어나 있다. `axisDirection = unit([vp − P, f])`이므로 먼 소실점의 100px 오차가 곧 2~3°다 | ❌ **게이트 실패의 원인** |
| AS-L9 | **사용자가 소실점을 눈으로 맞출 수 있다** — 그리고 그 정확도가 참 카메라 대역에 든다 | §5.2 (L-A.5, 개정 2 번호) | **임계는 실측 축 오차 ≈0.5°다**(리뷰어 지적으로 ≈1°에서 정정). 같은 하네스의 무오차 행(조용히 틀림 **0.0673** · 형태 오차 중앙 **0.0296**)을 대역으로 쓰면 실측 0.48°에서 1.03배/2.07배, 0.96°에서 1.24배/3.67배, 1.92°에서 7.5배/6.9배다. **§5.5가 등록한 지표는 형태 오차 0.026**이고 그 기준으로는 0.48°에서 이미 2배다(`camera_gate.json@509615a1`). **핸들 예산으로 환산하면**(각차 3~20°, `vp_homog.json@509615a1`) 가이드 140px에서 **0.29~1.01px(서브픽셀)**, 300px에서 0.63~2.16, **600px에서 1.28~4.33**, 1250px에서 2.68~9.03이다. L-B.1의 단일 뷰포트가 내는 가이드는 **607.6 / 618.5 / 1233.3 / 1254.6 / 1256.8 / 1327.7px**인데(`stage_browser.json`), **가장 짧은 것이 정확도를 정하므로** 옛 좌우 분할(304~358px)과 견주면 **약 2배**다 — 가장 긴 것으로 읽으면 4배가 나온다. ⚠ `min_guide_ratio` 0.45가 요구하는 651px에 **여섯 중 둘이 미달**이라 잘림이 없어진 것은 아니다. ⚠ **핸들 하나만 움직인다는 가정** 위의 값이다 — 사람은 핸들 12개를 만진다. **⚠ L-B.2가 검출 초안으로 실제로 쟀다**(`guide_budget.json@509615a1`): 늘린 뒤 구도별 최소 예산이 3pt_yaw20_pitch25 **3.32** · 3pt_yaw35_pitch15 **0.69** · 3pt_yaw50_pitch8 **0.73** · 2pt **0.23** · **1pt 0.12**px다. **1점 구도는 세 시드 전부가 `unusable_px`(1px) 미만**이고 2pt·3pt에도 서브픽셀 셀이 있다(31 중 9). 참 소실점 가이드의 1.3~4.3px은 **낙관값**이었다. ⚠ **대리값이다**(PITFALLS #2): **"사람이 그 안에 놓는다"는 여전히 표본 0이다** | ⚠️ 임계 0.5°, **검출 초안 실측 0.12~6.8px(1점 구도는 서브픽셀)**, 도달 여부 미측정 |
| AS-L10 | **획이 쌓이면 축 방향 오차가 임계 아래로 내려간다** | §5.5 (L-A.6) | **반증 — 단, 조건이 붙는다: "내부 격자선을 늘린 경우".** 축당 4 → 18 → 48 → 100획에서 검출 축 오차 중앙 **7.4 / 10 / 10 / 4.5°**(n=500 / 501 / 93 / **18**, 시드 6 / 6 / 3 / **1**)로 **임계(≈0.5°)에서 한 자릿수 멀다**. _비단조 주장은 뺀다 — 뒤의 두 점은 표본이 1/5~1/28이다(리뷰어 지적)._ ⚠ **늘린 획이 `1/k` 길이의 내부 격자선이라 소실점까지의 지렛대가 오히려 짧아진다** — 지렛대가 같거나 긴 획을 늘리는 경로(상자 여러 개·긴 모서리)는 **막히지 않았다**. 배치율 급락(0.569 → 0.128)의 큰 몫은 `min_len_ratio`가 아니라 **카메라 실패**다(`camera_ok` 258/300 → 6/18). 오라클 조용히 틀림도 k와 함께 나빠진다(0.376 → 0.478 / 0.413 / 0.406). **→ 자동 확정 경로를 두지 않는다**(이 결론은 조건과 무관하다) | ❌ 반증(조건부) |
| AS-L11 | **미배치는 실패가 아니라 대기 상태다 — 조건이 갖춰지면 승격하고, 승격은 연쇄한다**(개정 2 §9.1) | §9.1 (L-B.7·L-B.M) | **개수 회수에 대해서만 성립하고, 품질에 대해서는 반증됐다**(`promote.json@509615a1`, **참 카메라**). ① **연쇄는 일어나고 이 픽스처에서 수렴한다**(⚠ **3점 구도의 현상이다** — 구도별 추가 59·68·75·16·**0**) — 1회차 720/1904 뒤 2회차 **192** · 3회차 **25** · 4회차 **1**로 추가 **218**(연쇄 포함 938/1904 = 0.493), 상한 8회 도달 **0/338**. ⚠ **장면당 획이 12개**라 연쇄 깊이 4는 **픽스처가 정하는 상한에 가깝다** — 상자 둘 이상은 안 쟀다. ② **승격이 더 내는 오차는 형태가 아니라 배율이다.** 같은 720획이 고정 게이지로 **392/720(0.544)**, **자기 게이지로 286/720(0.397)**이고 확정은 **874/2416(0.362)**이다. 자기 게이지 배수는 **시드별 0.865~1.150으로 1을 가로지르므로 그 격차는 결론이 아니고**(#14), 고정 게이지 배수는 시드 여섯 전부 1.20~1.57이다. 앱이 배율을 다시 안 맞추므로 **그 배율 어긋남이 사용자가 보는 오차다.** ⚠ 꼬리는 자기 게이지에서도 나쁘다(p90 ≈2배). ③ **회수율도 품질도 한 값이 아니다** — 잡음으로 0.71 → 0.21(연쇄 포함 0.75 → 0.30), **구도로는 조용히 틀림이 3pt 52/163(0.32)에서 1점 구도 90/93(0.968)까지 간다**(같은 층의 확정은 82/412과 268/422이다). **1점 구도는 연쇄도 0이다.** **1점 구도는 회수한 것의 거의 전부가 조용히 틀린다.** 그리고 37.8%는 `axis_deg` 12 · 반경 0.05라는 **동작점 하나의 통과율**이다(스윕 0.25~0.49, 개수와 틀림이 단조로 맞바뀐다). ⚠ **참 카메라 조건이다.** 앱의 `promoteChain`은 초안·조정 카메라 위에서 돈다. ⚠ 앵커 팔의 거부 사유가 `no_snap`·`angle_over` 둘뿐이라 **'축이 정해지면 반드시 놓인다'가 구성상 항등**이다 | ⚠️ **개수는 성립·연쇄는 수렴(픽스처 한정). 품질은 반증. 1점 구도에서 거의 전부 틀린다** |
| AS-L12 | **롤 0을 고정하면(지평선 수평) 미지수가 하나 줄어 자동 검출이 임계 아래로 간다** | L-D.0 (사람 지시, 2026-08-16) | **반증.** 이론은 맞다 — 이론서 3.1 + 롤 0이면 지평선이 화면 수평이고 §5.3의 자세 자유도가 3 → 2다. 수심 조건(6.3)으로 세 번째 소실점이 점 하나로 정해지며 **참 카메라에서 오차 0으로 성립한다**(항등이므로 정보량 0, #5). **그러나 검출 입력에서는 안 된다**: 축 방향 오차 중앙 7.4048 → **6.1460**(축당 4획)으로 17% 줄 뿐이고 임계 ≈1°의 **6배**, 오라클 축 라벨로도 **4.4599°**다. **그리고 배치가 붕괴한다**(2049 → 792 / 6035 → 522 / 2045 → 9 / 692 → **0**). 사영이 아니라 **제약 적합**으로 넣어도 안 살아난다(848 / 730 / 3 / 0). ⚠⚠ **다만 붕괴가 제약의 성질이라는 초판 서술은 철회했다** — **위약 대조군**(같은 크기 **임의 방향** 이동)이 똑같이 붕괴한다(832 / 802 / 14 / 0). 붕괴는 **검출 해를 그 지지선에서 떼어 놓는 것 자체**의 대가이고 방향과 무관하다. 검출 지평선의 실제 어긋남은 Δy **24~53px**이다. **AS-L10의 반증을 뒤집지 못한다.** ⚠ 양성 채널: 손익분기는 롤 **5°** 근처이고 10°에서는 해를 끼친다(`horizon.json@509615a1`) | ❌ 반증 — **AS-L10을 못 되살린다** |
| AS-L5 | **실시간 색이 오배정을 줄인다** | §4 | 사용자 행동에 달렸다 — **직접 측정 불가**, 간접 지표만 | ⏳ 간접 |
| AS-L6 | **승격 시 재투영 어긋남이 준다** | §6.2 | **반증. 계획서 §6.2를 폐기했다.** AS-L3과 같은 하나의 오류다 — 재투영 어긋남은 카메라와 무관하게 0이므로(각 점이 자기 광선 위) 승격 전후에 **둘 다 0**이다. L-C가 그 신호를 쓰려면 다른 양을 찾아야 한다 — 후보는 획별 `tiltDeg`(축 방향이 해석 평면을 벗어난 각. 끝점 오차 0.03에서 중앙 2.06°, 0.05에서 3.49°로 실제로 움직인다)와 소실점 삼각형의 f² 편차(6.3의 자유도 0 조건)다. **L-C.3이 후보 둘을 시험했고 둘 다 안 섰다**(`order_signal.json@509615a1`): **f² 편차는 자기참조 유형 3**이다 — 주점을 수심으로 잡는 순간 세 f²가 같아지는 것이 항등식이라 **틀린 소실점·둔각 삼각형·가짜 승격 아홉 전부에서 편차가 2.6e-16 이하**다(정보량 0). **`tiltDeg`는 사영 불변은 아니지만**(f×0.8/1.0/1.2에서 **장면 중앙**이 1.414/1.316/1.228 — ⚠ 이 셀 앞쪽의 2.06°/3.49°는 **획별·잡음별** 값이라 **단위가 다르다**) **오승격을 못 가른다.** 근거는 기준과 무관한 것부터 셋이다: **① 구도 교락이 없는 `wrong_vp3` 팔에서 구도별 판별력이 0.015·0.074·0** · **② 시드 부호가 갈린다**(여섯 중 둘) · **③ 합산 0.1979는 구도 간 비교다**(가짜 팔이 2pt 한 구도뿐이라 3pt 층에서는 못 잰다. 시드 3을 빼면 0.1393). ⚠⚠ **그것은 신호의 실패이지 픽스처의 실패가 아니다** — **참값을 고정 게이지로 재면 판별력이 가짜 승격 0.9694 · 틀린 VP3 0.5446으로 둘 다 등록 기준 0.5를 넘는다.** 자기 게이지 값(0.4979 · 0.1265)은 **최소제곱이 전역 배율을 없앤 뒤의 수**이고 **AS-L11이 '사용자가 보는 오차'라 적은 배율 성분을 지운 것**이다(가짜 승격이 0.2537 → 3.1196으로 12배 벌어진다). 틀린 VP3(+400,+900px)의 **축 각오차는 중앙 5.59°로 AS-L9 임계의 11배**다. 구도 효과도 갈랐다 — 같은 2pt 장면을 옳게 2점으로 푼 대조군이 0.1435다. **그러므로 잴 것은 많고 관측 가능한 신호를 아직 못 찾은 것이다.** `tiltDeg`가 **f 오차와 축 오차를 섞는다.** → **사용자 확인으로 갔다**(§6.2의 두 갈래 중 하나). 그 되돌리기가 **L-C.2**다 | ❌ 반증, §6.2 폐기. **대체 후보 둘도 반증**(L-C.3) |
| AS-L7 | **초기 획이 교점을 충분히 만든다** | §10 | **교점 수는 충분하다** — 상자 하나에서 24개(꼭짓점 8 × 짝 3)이고 미지수는 12개라 영공간이 1차원이다(최소 고유값 −3.6e-17). 부족한 것은 **개수가 아니라 정밀도**다: 축당 4획으로는 소실점 지렛대가 짧아 3D 축 방향 오차 중앙 4.18°가 남고 그것이 형태 오차 0.271을 만든다. **획이 더 쌓이면 나아지는지는 미측정** — L-A.4가 남긴 첫 후속 측정이다 | ⚠️ 개수는 충분, 정밀도가 부족 |

## 이월 (S·G에서 살아남은 것)

| # | 전제 | 출처 | 관측 | 상태 |
|---|---|---|---|---|
| AS-C1 | **실획 표본이 대상 사용자를 대표한다** | 구 AS-13 / V-A | **의심.** 손에 있는 것은 Quick,Draw(마우스·손가락 낙서)뿐이고 대상은 스타일러스·투시도 숙련자다. **실획 표본은 여전히 0이다.** → 이 수치로 설계 결정을 하지 않는다 | ⚠️ 설계 결정 보류 |
| AS-C2 | **끝점 겨냥 오차**가 도구가 성립하는 범위에 있다 | 구 AS-12 | **영향이 크게 줄었다.** L 접근은 시작점을 **스냅으로 확정**하므로 겨냥 오차가 배치의 오차원이 아니다 — 남은 의존은 **스냅 반경 하나**다. Quick,Draw `closure_gap` 환산 0.028~0.051이 반경 결정의 유일한 자릿수. **⚠ L-B.3이 그 축소를 뒤집었다**: 반경을 정하는 것은 겨냥 오차 **단독이 아니라 (겨냥 오차 / 대상 간격)의 비**이고, 그 비가 1/2를 넘으면 **어떤 반경으로도 못 고친다**(`snap.json@509615a1`의 `by_density`). 합성 잉크의 겨냥 오차 중앙은 `endJitter` 0.01/0.03/0.05에서 **4.7 / 14.7 / 23.8px**이고 등급은 거의 안 바꾼다 — **활이 양끝에서 0이라 `lf_bow`가 시작점에 안 닿고**, 닿는 유일한 성분인 `hf_sigma`가 등급 간 8%밖에 안 다르기 때문이다(**모델의 성질이다**, #2·#3). 채택 반경 **0.05**(앱 1280×675에서 72px)는 선례(10~15px)의 5~7배이고 근거는 `unintended_snap`이다 — 0.10으로 키우면 맞음이 +27인데 **의도하지 않은 붙음이 +918**이다 | ⚠️ **비(比)의 문제로 재정의됨. 실획 자릿수는 여전히 `closure_gap` 하나** |
| AS-C3 | **한 획 = 한 축** | 구 AS-6 | 실획 위반율 0.699(n=1816, Quick,Draw 낙서 — 상한으로 읽는다). L에서는 획이 **직선 세그먼트**가 되므로 위반하면 방향 하나로 접힌다. 다만 **실시간 미리보기가 그 자리에서 보이므로** 사용자가 끊어 긋는다(AS-L5와 같은 논리). **⚠ L-B.4가 쟀다 — 접히지 않는다.** 인접한 두 모서리를 한 획으로 이은 대조군에서 **92.4~98.2%가 거부되어 2D로 대기**한다(`axis_live.json`의 `control_true_corner_rejected`). 지배 규칙은 **`multi_axis` 1991/2054**다. 즉 위반 획은 **접히는 것이 아니라 미배치(대기)로 간다** — A-3의 "애매하면 놓지 않는다"가 그대로 작동한다. ⚠ 대조군의 동적 범위가 5.8pp로 천장에 붙어 있어 **완만한 코너는 안 쟀다**(DEFERRED) | ⚠️ **접히지 않고 대기로 간다**(직각 코너 한정) |
| AS-C4 | **깊이 좌표의 출처를 구분해야 한다** | 구 AS-9 | **성립.** f를 10~20% 틀리게 주면 모서리 오차가 0.027 → 0.141(5.2배)이고 연쇄를 타지 않는 **전역 배율 편향**이다(이론서 16.4). 1점 f는 설정값, 2점 주점은 가정 → `Camera.fSource`가 출처를 들고 화면에 나온다 | ✅ 유지 |
| AS-C5 | **2점 투시에서 주점 = 이미지 중심** | 구 W-y / 이론서 16.2 | **미검증.** 자유도 1을 이 가정으로 소진하므로 틀리면 f가 틀린다. L도 같은 경로를 쓴다 | ⏳ 미측정 |
| AS-C6 | **지면 법선은 수직 소실점 방향이다** | 구 AS-10 | 카메라 좌표계 `y = const`는 **카메라가 수평일 때만** 맞다. `geom3d.groundFrame`이 단일 출처 | ❌ 위반 후 고침 |
| AS-C7 | **캔버스 크기가 생성 시점에 잡혀 있다** | 구 AS-11 | **틀렸다. 세 번 걸렸다.** ① `ResizeObserver`가 초기 콜백을 안 준다(S-4) → `requestAnimationFrame`을 함께 걸었다. ② **rAF는 숨은 탭에서 완전히 멈춘다**(구 V-n: 600ms에 0프레임) — 브라우저 창이 뒤에서 열리면 둘 다 발화하지 않고 `acc.imgSize`가 `[1,24]`로 굳는다. 그 크기가 무한원 판정의 기준이라 **소실점이 하나로 읽혔다**(L-A.5 브라우저 확인). ③ 대응: 관찰자를 더 걸지 않고 **쓰는 자리에서 자가 치유**한다 — `refresh()`가 `sizeStale()`을 보고 고친다. `setTimeout(0)`·`visibilitychange`도 함께. ⚠ **자가 치유는 재발을 관측 불가로 만든다**(PITFALLS #7·#19) → 발동 횟수를 진단에 낸다. **L-B.1 관측**: 단일 뷰포트에서 `SIZE_HEAL.count = 3`(창 1280 → 820 변경 포함, `stage_browser.json`). 창을 줄이면 `setViewOffset`의 `offsetX`가 124.7 → 0으로 **다시 계산된다** — 투영이 굳지 않는다 | ❌ 위반, **세 번 고침** |
| AS-C8 | **곡선 획이 소수다** | 구 AS-7 | **아니다.** ⚠ **두 수를 한 문장에 놓지 않는다** — `bend_share.json`의 caveat이 금지한다: 곡선 **26.5%**는 `straight_tol` 0.10 기준의 **다른 지표**이고, `bend > 0.18`인 비율 **0.6025**는 또 다른 집단이다(꺾인 획 88% · 매끄러운 획 1.2%). 그리고 **`bend_max` 0.18 자체가 측정이 정당화하지 못한 잠정값**이다(같은 원장의 `threshold_note`). **L에서는 지금 범위 밖이다**(프리핸드를 미뤘다). 획이 직선 세그먼트가 되므로 굽은 획은 그 현(弦)으로 접힌다. **표현 계층 복귀 시 되살아난다** | ⏳ 미룸(§1.1) |
| AS-C9 | **측정이 재현 가능하다** | 구 V-H | `hash(str)`·`Math.random` 금지, `stable_seed`/`rng32`. **시드 변동폭 때문에 유효 자릿수는 2자리**이고 비율보다 분자/분모로 적는다 | ✅ 규칙 |
| AS-C10 | **합성이 실제를 대표한다** | 구 V-o | **반증됐다**(양방향으로 틀렸다 — 고주파 과다, 저주파 결여). 합성 수치로 실획 현실을 주장하지 않는다 | ❌ 규칙으로 굳음 |

## 규칙 기반 전환이 세운 가정 (2026-08-16)

| # | 가정 | 상태 |
|---|---|---|
| **AS-L13** | **사람은 축별로 몰아 긋는다**(한 방향의 선들을 이어서 긋는다) | ⚠⚠ **2026-08-17 재측정으로 방향이 뒤집혔다.** 옛 값은 몰아 6.93° · 번갈아 **32.57°**였고 '몰아 긋는다'가 **유리한** 가정이었다. `rule_camera.json@509615a1`(표제 표 = 1pt 항등 구도를 뺀 `headline_no_identity`, #5)에서는 **몰아 14.42° · 번갈아 9.82°** — **몰아 긋기가 더 나쁘다.** 즉 이 가정은 이제 **불리한** 가정이고, 사람이 실제로 몰아 긋는다면 축 오차가 그만큼 크다. ⚠ 뒤집힌 원인은 안 갈랐다(A의 화면 직교 스냅 · B의 '1점 선언은 첫 획에서만' · A-4의 3점 입구가 **같은 실행에 섞여 있다**, 리뷰어 [14]). ⚠ 설명 기전으로 쓰던 `first_pair_sep_deg_median`은 **원장 40여 곳 전부 `null`**이다(#38 덮는 대상 0) — 순서 설명 전체가 그 미기록 위에 있다. **실획 표본이 0이므로**(AS-C1) 사람이 어느 쪽으로 긋는지는 여전히 모른다 |
| **AS-L14** | **묻는 횟수가 사람이 견딜 만하다** | ⚠ **미검증.** 상자 하나(12획)에 물음이 **2.5~2.6회** 난다(`rule_camera.json`의 `asks_per_run`). 물음은 조용히 틀리는 것을 막는 장치이지만(A-3) 너무 잦으면 도구가 안 쓰인다. **실사용 관측이어야 한다** |
| **AS-L16** | **축 스냅이 축 오차를 줄인다** | ⛔ **반증**(2026-08-16, `axis_snap.json`). 팔 넷에서 축 오차 중앙이 **9.9569로 완전히 같다** — 소실점은 확정 후 잠기므로(CLAUDE.md §1) **뒤에 그은 선이 카메라에 기여하지 않는다.** 축 스냅이 바꾸는 것은 **배치의 형태**이지 카메라가 아니다. 사람 지시 4의 예상이 여기서 갈렸다 |
| **AS-L17** | **축 스냅이 배치 품질을 올린다** | ⛔ **반증**(같은 원장). 조용히 틀림이 **0.811 → 0.967**로 **나빠진다**. 축 스냅은 획을 축에 정확히 붙이므로 **카메라 오차를 고스란히 물려받는다** — 스냅이 없으면 획마다 다른 방향이 들어와 `liftAll`의 전역 해에서 일부 상쇄된다. ⚠ **조건부다**: 이 픽스처의 카메라 축 오차가 중앙 9.96~15.47°로 임계(0.5°)의 20~30배다. **카메라가 정확해지면 부호가 뒤집힐 수 있고 그것은 안 쟀다** |
| **AS-L18** | **모호 판정이 결과를 바꾼다** | ⚠ **이 픽스처에서는 안 바꾼다**. 임계 0~24° 스윕에서 발동이 0 → 1161로 느는데 **배치율과 조용히 틀림이 거의 불변**이다. 정확도는 0.047이고, 그것이 신호의 성질인지 **카메라 축이 이미 10~15° 틀려 "참 축" 대응이 흔들리는 탓인지 안 갈랐다** |
| **AS-L19** | **지평선이 맞으면 축 오차가 준다**(D-L45의 전제) | ✅ **성립하나 전제의 절반이 폐기됐다**(`horizon_pitch.json@509615a1`, 2026-08-17 A-4로 재측정). **살아 있는 절반**: 참 지평선 팔의 축 오차 중앙이 **3.39°**(몰아 긋기) · **4.88°**(번갈아)이고 중앙 지평선은 **21.23°** · **12.26°**, 위약(중앙에서 참과 같은 크기·반대 방향)은 **31.70°** · **33.39°**다 — 참이 중앙보다·위약보다 낫다(등록 조건 ②·③, **두 순서 전부 통과**). **⭐ 허용 오차 곡선**도 살아 있다: 참에서 **±120px까지** 3.39 → **5.63°**로 완만하고 전 구간이 중앙 팔보다 낫다. ⚠⚠ **폐기된 절반**: '3점 투시가 초기부터 선다'는 **지평선과 무관해졌다** — 차수 3인 실행이 참·중앙·위약 **세 팔 전부 80~85/144**다(옛 판은 중앙 0/144 · 참 141/144였다). A-4가 수심 유도를 뺐고 3점은 이제 **사용자가 기울어진 세로 모서리를 '수직축'이라 답할 때** 선다. 그러므로 **등록 조건 ①은 성립하지 않고 성립해서도 안 된다**(그 조건이 잰 기전이 없어졌다). ⚠ 세 팔 전부 **물음에 참 축으로 답한 오라클** 위의 값이라 어느 팔도 실사용 성능이 아니다. ⚠⚠ **중앙값 하나 위의 결론이다**(리뷰어 [12]): p90은 참 30.79 대 중앙 35.47(1.15배)이고 **max는 참이 더 나쁘다**(53.92 대 47.10). 꼬리에서는 이득이 사라진다. ⚠⚠ **조건 ③의 두 팔은 같은 모집단이 아니다**(리뷰어 [13]): 위약은 카메라가 선 실행이 **32/144**(축 71개)이고 참은 **96/144**(축 228개)다 — 위약의 중앙값은 살아남은 22%만의 값이다(#11). ⚠ **사람이 그 창에 드는지는 여전히 표본 0**이다(AS-L9). |
| **AS-L20** | **3점 투시의 입구는 '기울어진 세로 모서리를 수직축이라 답하는 것'이고, 그 선(들)에서 읽은 V₃가 쓸 만하다**(2026-08-17 A-4의 전제) | ⚠ **부분 검증**. 선이 **둘 이상**이면 최소제곱 교점이고 (`lsIntersection`), 하나뿐이면 **주점 x = 이미지 중심**(16.2)을 써서 `∩ (x = W/2)`로 푼다. ⚠⚠ **초판이 언제나 후자였고 측정이 기각했다**: 수직 소실점은 화면에서 아주 멀어 한 선을 x = W/2까지 **외삽**하면 각 오차가 크게 증폭된다 — 몰아 긋기 축 오차 중앙 **37.7°**. 그리고 손으로 그은 세로선은 **4~8°(애매 구간)**로 흔히 떨어지므로 **손 오차가 3점을 만들었다**. 지금은 ① 선 하나로 선언하려면 기울기가 **`depth`(≥8°)**여야 하고 ② 둘 이상이면 교점으로 푼다. 그 뒤 축 오차가 3.39~4.88°다. ⚠⚠ **초판이 여기에 3.39~4.88°를 적었고 그것은 다른 기전의 값이다**(리뷰어 [10]) — 그 수치는 `horizon_pitch.json`의 **참 지평선 팔**이고 '지평선을 참값에 놓았을 때'의 축 오차이지 '기울어진 세로 모서리로 V₃를 세웠을 때'의 정확도가 아니다. **그것을 가르는 팔이 원장에 없다.** ⚠ 있는 값도 전부 **오라클**(물음에 참 축으로 답함) 위이고, **사람이 그 물음에 옳게 답하는지는 표본 0**이다 |
| **AS-L15** | **화면 축 임계 4°가 손으로 그은 수평선을 담는다** | ⚠ **부분 반증.** 잡음 0.03~0.05에서 수평 모서리가 8°를 넘어 **깊이선으로 조용히 읽히는 실행이 있다**(1점 구도에서 가짜 소실점이 생겼다). 8° 밖은 애매가 아니라 깊이이므로 **묻지 않는다** — 그 자리가 이 규칙의 조용히 틀리는 문이다. 실획의 자릿수를 모른다(AS-C1) |

## 무효 (L 전환)

접근이 바뀌어 **대상이 사라진** 항목. 지우지 않고 여기 적는다 —
왜 세 번째로 접근을 바꿨는지가 그 항목들에 있다.

| 무효 항목 | 무효 사유 |
|---|---|
| AS-2(떨림 평면) · AS-3(미리보기-확정 차이) · AS-8(앵커 체인 누적) · AS-16(화면 접합) · AS-17(`pts3d` 파생) | **배치 계층이 폐기됐다.** 시작점이 스냅으로 확정되므로 평면 선택도 앵커 체인도 없다. `pts2d`는 계속 보존하므로 AS-17의 결론(재생성으로는 "그 3D"가 안 나온다)은 **저장 형식 규칙으로만** 남는다 |
| AS-5(면 위 사선 비율) | 축 밖 획을 사후에 추론하지 않는다. 사용자가 그리는 중에 축을 본다. ⚠ **2026-08-16: 그 원장(`face_diagonal.json`)이 D-L28의 근거로 다시 쓰였다** — 무효 표시된 가정 위의 측정을 판단에 쓴 것이고, D-L1(아카이브를 근거로 인용하지 않는다)의 경계에 있다. **결론을 그만큼 약하게 적었고**(짝지은 비교가 아니다) §6(면)을 열 때 그 조건에서 다시 잰다. 상수 해시가 현재값이라 **STALE로는 안 잡힌다** |
| AS-18~AS-20(세그먼트) | 세그먼트 실험이 폐기됐다. **AS-20의 반증이 L 전환의 근거**이므로 아카이브에 남는다 |
| AS-14·AS-15(체계적 왜곡) | 판정을 사후에 하지 않으므로 왜곡 보정의 대상이 없다. 다만 **소실점 검출(§5.1)의 산포 임계**가 왜곡을 흡수해야 한다 → AS-L1이 그 자리다 |
| AS-4(면 없이 형태가 읽힌다) | 여전히 유효하나 **판단 근거가 좁다**(구도 하나·물체 하나·관찰자 하나). 면 생성은 L 범위 밖이다 |

| **AS-L21** | **터치 제스처를 직접 처리하면 카메라 수학을 다시 짜야 한다**(2026-08-17 G의 사전 예상 — **반증됐다**) | HANDOFF가 후보 ③의 함정으로 "three 최신판은 `rotateLeft` 같은 공개 API가 없다"를 적어 뒀고, 그것이 사실이면 궤도·팬·줌을 `Spherical`로 새로 구현해야 했다(A-3의 "선례를 따른다"와 어긋나는 자리). **측정이 기각했다**: three r185의 `OrbitControls`에 `rotateLeft`·`rotateUp`·`pan`·`dollyIn`·`dollyOut`이 **공개 메서드로 다섯 다 있다**(`touch_route.json@509615a1`의 `candidates.c3_direct.public_api`). 그래서 **라우팅만 직접 하고 카메라 수학은 그대로 쓴다** — 궤도 중심·감쇠·각도 한계가 한 군데에 남는다. ⚠ 그 팔이 확인한 **작동**은 회전 하나다(줌은 확정 카메라에서 궤도 반지름이 0이라 못 쟀고 팬은 값이 없다) — 줌·팬은 앱 팔이 낸다 |
