# DECISIONS.md — 잠정 결정

계획서(`docs/line_plan.md`)를 따르되 **계획서에 없거나 계획서와 어긋나는** 판단을 여기 적는다.
근거와 **기각 사유**를 함께 적는다(CLAUDE.md A-3).

> **2026-08-15 L 전환에서 새로 시작했다.** D-S1~D-S24 · D-G1~D-G4는
> `docs/archive/DECISIONS_SG.md`에 있고 **대부분 무효**다(배치 계층이 폐기됐다).
> 살아남은 것은 아래 "이월"에 다시 적었다 — 아카이브를 판단 근거로 인용하지 않는다.

---

## L-A

### D-L1 — 문서 원장을 아카이브하고 새로 시작했다

`progress.md`·`DECISIONS.md`·`DEFERRED.md`·`assumptions.md`를 `docs/archive/*_SG.md`로 옮기고
유효 항목만 옮겨 적었다(계획서 §8.3).

**왜**: 축적된 D-S1~D-S24 · AS-1~AS-20의 대부분이 **배치 계층 전제 위에 있었고** 그 계층이
폐기됐다. 새 세션이 그것을 읽으면 이미 없는 기전에 대한 판단을 근거로 삼는다.
실제로 그 위험이 관측됐다 — G-A 세션이 "지지 1"을 D-S22("2D 증거로 구분 불가")로 설명하며
같은 완화를 네 번 다시 시도했다.

**기각**: 원장을 그대로 두고 무효 표시만 다는 것. 3286줄(progress) + 889줄(DECISIONS)이
컨텍스트에 들어오면 표시가 있어도 읽힌다. 파일을 옮기는 것이 유일하게 확실한 격리다.

**보존**: 삭제하지 않는다. 접근을 세 번 바꾼 이유가 그 안에 있고, 측정 산출물
(`stage0/out/*.json`)은 손대지 않는다.

### D-L2 — 소실점 지지 임계를 **각도로 적는다**(단위 변경이고, 고친 것은 아니다)

계획서 §5.1은 "산포 임계는 화면 대각 비율"이라 적었다. **그대로 하지 않았다.**
화면 대각 대비 픽셀 거리는 **먼 소실점에서 발산한다** — 각도 오차 θ에서 소실점까지의 수직거리는
`D·sin θ`이고 2점 투시의 소실점은 화면 몇 배 밖에 있는 것이 정상이다. 그 단위로는
어떤 임계도 전부 기각하거나 전부 통과한다. 그래서 **각도**(유계, 0~90°)로 적는다. 값은 2.5°.

**⚠ 이 항목의 초판은 틀렸다**(리뷰어가 짚었다). 초판은 "획 길이 대비(`vp_dist_ratio` 0.06)도
같은 이유로 발산하고, 잉크 노이즈에서 참 소실점의 부적합도 0.061이 임계 0.06을 넘어
검출이 통째로 실패했다 → 각도로 바꿔 고쳤다"고 적었다. 두 군데가 틀렸다.

1. **`vpMisfit`은 이미 각도량이다.** 코드가 재는 것은 중점에서 「먼 끝점→소실점」 직선까지의
   수직거리 ÷ 획 길이이고, 중점은 끝점에서 길이의 절반이므로
   `vpMisfit = sin φ / 2`이며 `φ = asin(2·misfit)`이다(수치로 확인:
   획 (0,0)–(100,0)·소실점 (1000,50)에서 0.02497 = sin φ/2). 상한이 0.5라 발산하지 않는다.
   그러므로 **부적합도 → 각도는 단조 재매개화이고 아무것도 고치지 않는다**(PITFALLS #24).
2. **자릿수도 반대다.** `misfit 0.06`은 **6.892°**이므로 채택한 2.5°는 실패한 임계보다
   **약 3배 엄격하다**(`vp_detect.json@f351839a`의 `unit_equivalence`).

**검출 실패를 실제로 고친 것은 D-L3(도달거리)과 D-L4(가중합·재배정)다.**
첫 실패에서 검출된 소실점 셋은 전부 **상자의 꼭짓점**이었다 — 임계의 문제가 아니었다.

_`axis.ts`의 `angle_max_deg` 주석은 `misfit = (D/L)·sin θ`라 적어 두었는데 그 유도도 같은
오해다. 기본값이 0(꺼짐)이고 그 경로는 L에서 폐기되므로 고치지 않고 적어 둔다._

### D-L3 — "선 3개가 한 점에서 만난다"에 **도달거리 조건**을 더했다

**상자의 꼭짓점이 소실점으로 잡힌다.** 꼭짓점에는 모서리 셋이 **정확히** 한 점에서 만나므로
§5.1의 문장을 완벽하게 만족하고, **노이즈가 있으면 참 소실점보다 더 잘 맞는다** —
꼭짓점의 산포는 끝점 오차 몇 px이고 소실점의 산포는 지렛대로 증폭된다.
실제로 잉크 노이즈를 넣은 첫 실행에서 검출된 "소실점" 셋이 전부 상자의 꼭짓점이었다.

계획서가 "**같은 방향 클러스터**의 선 3개"라 쓴 것이 빠진 조건이다. 방향을 직접 군집화하는
대신 **소실점이 획의 가까운 끝에서 획 길이의 1배 이상 밖에 있어야 한다**로 뒀다(`min_reach`) —
같은 방향으로 수렴하는 획들만 그 성질을 가지며 근·원 소실점에서 똑같이 작동한다(A-3).
반례를 `test/vp_detect.test.ts`에 박았다(`min_reach: 0`으로 내리면 꼭짓점이 실제로 잡힌다).

### D-L4 — 지지를 **개수가 아니라 가중합**으로 세고, 마지막에 다시 배정한다

개수로만 세면 **지평선 근처 모서리가 두 수평 소실점을 거의 다 지나므로**(둘 다 지평선 위에
있다) 엉뚱한 중간 지점이 양쪽 축의 획을 긁어모아 지지 6을 만든다. 참 소실점의 지지 4보다 많고,
그때 검출은 소실점을 **둘밖에** 못 찾았다. `Σ(1 − φ/tol)`로 세면 0°짜리 4개가 이긴다.

**탐욕적 제거의 순서 의존도 남는다** — 먼저 뽑힌 소실점이 경계선을 데려가면 두 번째 군집이
지지 3을 못 채운다. 그래서 전부 찾은 뒤 **최소 각도로 다시 가르고** 군집마다 최소제곱을
한 번 더 돌린다. 그것이 §5.1의 "카메라는 최소제곱으로 다시 푼다"의 자리다.

_"개수만 세면 반대로 간다"는 이 프로젝트에서 세 번째다(D-C6의 구제 경로 둘)._

### D-L5 — 최소제곱에 **길이² 가중을 넣는다** (초판을 뒤집었다)

**초판은 "가중을 넣지 않는다"였고 틀렸다.** 근거로 "가중 없음/`len`/`len²`에서 f 상대오차
중앙이 0.094로 전혀 안 움직이므로 f 오차의 출처는 추정기가 아니라 입력"이라 적었다.

리뷰어가 대안 설명을 짚었다 — **구도가 하나(직육면체 하나)면 12모서리의 화면 길이가
서로 비슷해 길이 가중이 균등 가중과 같아진다.** 그것을 직접 재고 구도를 다섯으로 늘려
다시 돌렸다(`vp_detect.json@f351839a`):

| 가중 | f 상대오차 중앙 | p90 |
|---|---|---|
| 균등 | 0.0921 | 0.4055 |
| `len` | 0.0860 | 0.4044 |
| **`len²`** | **0.0793** | **0.3942** |

화면 길이 최대/최소 비는 구도마다 **2.03 / 4.37 / 2.07 / 2.03 / 4.44**다 — 1.5 미만이 아니므로
가중은 항등이 아니고, 그래서 중앙값이 실제로 움직인다. **`len²`를 채택한다**(D-L8과 같은 근거:
짧은 획의 방향 분산이 `1/len²`에 비례하므로 그 역수가 가중이다).

**그리고 "f 오차는 알고리즘의 몫이 아니다"라는 결론은 철회한다.** 그것은 근거가 없었다.
_다만 L-A.4가 더 중요한 것을 밝혔다 — **형태를 무너뜨리는 지배항은 f 크기가 아니라 축 방향
오차(중앙 4.18°)다.** f를 10% 틀리게 줘도 형태 오차 중앙은 0.098 → 0.115뿐이다._

### D-L6 — **카메라 일괄 적합을 기각했다** (회전 3 + f 1)

L-A.4가 "카메라가 형태를 무너뜨린다"를 확인한 뒤, 소실점을 하나씩 맞추는 대신
**회전 3 + f 1을 획 전부로 한 번에** 맞춰 봤다(`web/src/s3d/cameraFit.ts`).
잔차는 소실점을 거치지 않는다 — 해석 평면 법선 `n`과 축 방향 `d`의 내적이며,
유한·무한원 소실점을 같은 식으로 다룬다. 최소화는 Nelder-Mead(결정론적).

**f가 나빠졌다**(`lift_gate.json@f351839a`의 `fitted_camera`):

| 구도 | 검출 f 오차 | 적합 후 |
|---|---|---|
| 3점 요35°·피치15° | 0.0762 | **0.0827** |
| 3점 요50°·피치8° | 0.1141 | **0.3829** |
| 2점 | 0.1864 | **0.3943** |
| 1점 | (설정값) | **2.0** (상한까지 달아났다) |

형태도 나빠졌다(기준 구도 조용히 틀림 0.641 → 0.655).
**기각한다.** 코드는 남기고 앱에 배선하지 않는다(측정 근거를 재현할 수 있어야 한다).
_추정: 목적함수가 f에 대해 거의 평탄한데(입사 관계의 사영 불변성과 같은 뿌리) 축 배정 오차
11%가 최소화를 끌고 간다. 확인하지 않았다._

### D-L7 — **국소화 가능성 조건**을 더했다 (`min_spread_mult`)

**화면 평행 축의 획들은 서로 나란하므로 노이즈만으로도 아주 먼 데서 만난다.** 그것을 유한
소실점으로 받으면 2점 투시가 3점으로 읽히고 `fFromThreeVps`가 엉뚱한 f를 낸다 —
실제로 2점 구도에서 수직 4획이 `[245, −9252]`에 모여 지지 4를 만들었다.

교점의 위치 불확실도는 `노이즈 / 방향 산포`에 비례하므로 **지지선 방향의 최대 각차**를
임계로 쓴다. `support_deg`의 1.5배(=3.75°).

| 배수 | 참 소실점이 유한인 다발을 살림 | 참 무한원 다발을 잘못 받음 |
|---|---|---|
| 0 | 221/221 = 1.00 | 67/67 = 1.00 |
| 1.0 | 208/221 = 0.94 | 45/67 = 0.67 |
| **1.5** | **199/221 = 0.90** | **33/67 = 0.49** |
| 2.0 | 190/221 = 0.86 | 22/67 = 0.33 |
| 3.0 | 171/221 = 0.77 | 15/67 = 0.22 |

⚠ **깨끗한 분리가 아니다.** 산포 분포가 겹친다(참 유한 최소 0.43° · 참 무한원 최대 17.1°).
_초판 주석은 "1.3~3.4° 대 4.2~54.5°로 갈린다"고 적었는데 그것은 **손으로 고른 4장면**의
값이었다. 전 모집단에서는 겹친다 — 고쳤다._
걸린 다발은 버리지 않고 `infinite`로 표시해 그 획들을 **`screen` 축**으로 보낸다(이론서 2.2).

### D-L8 — **`min_reach = 1.0`은 양방향으로 재고 정했다**

초판은 값의 근거가 한쪽뿐이었다(꼭짓점을 막는가 = 거짓 양성만). 리뷰어가 짚었고,
**소실점이 그림 안에 있는 구도(1점 투시)를 넣어 거짓 음성 방향도 쟀다**:

| `min_reach` | 먼 구도 3점 검출 | 먼 구도 오배정 | **1점 구도** 3점 검출 | 1점 오배정 |
|---|---|---|---|---|
| 0 | 46/96 | 245/852 | 11/24 | 29/199 |
| 0.5 | 49/96 | 114/828 | 9/24 | 9/183 |
| **1.0** | **50/96** | **113/826** | 8/24 | **8/180** |
| 2.0 | 39/96 | 113/782 | 8/24 | 8/180 |
| 4.0 | 19/96 | 85/648 | **0/24** | 6/146 |

0으로 내리면 꼭짓점 오검출로 오배정이 2배가 되고, 4로 올리면 **1점 구도의 참 소실점이
아예 죽는다** — 거짓 음성이 실재함을 확인했다. 1.0이 양쪽에서 최적이다.

## 이월 (S·G에서 살아남은 결정)

| # | 결정 | 근거 | 원래 번호 |
|---|---|---|---|
| D-C1 | **깊이 출처를 `Camera.fSource` 하나가 들고 화면에 나온다.** provenance를 획마다 나누지 않는다 | f 오차 10%가 앵커 체인 전체보다 크고 **전역 배율 편향**이다(이론서 16.4). 출처가 여럿이 아니라 하나이므로 자료구조를 늘릴 이유가 없다 | D-S5 |
| D-C2 | **애매하면 놓지 않는다. 다만 버리지 않는다** — 미배치는 화면에 남기고 지정 경로를 준다 | 조용히 틀린 배치가 미배치보다 나쁘다(A-3). "놓지 않는다"를 "버린다"로 읽으면 사용자가 그린 것이 사라진다 | D-S15 |
| D-C3 | **좌표 규약의 단일 출처는 `canvasFrame.ts`다.** dpr은 `setTransform`에서 한 번만 적용한다 | 같은 캔버스를 둘이 다른 규약으로 쓰다 iPad 좌표 버그가 났고 **dpr 1인 데스크톱·Playwright 어디서도 안 보였다**. `test/canvas_frame.test.ts`·`e2e/coords.spec.ts`가 dpr 1·2·3에서 잠근다 | S-10 후속 |
| D-C4 | **새 임계는 `web/test/constants.ts`에 넣는다** | 빠지면 STALE 탐지가 안 걸리고, 상수가 바뀌어도 그것에 의존한 측정이 낡은 줄 모른다. 같은 유형이 세 번 재발해서 만든 규칙 | CLAUDE.md §5 |
| D-C5 | **거름은 선택 뒤에 건다** | 거부권을 선택 **전에** 걸었더니 경쟁자가 죽어 상대 순위가 거저 통과했고 오배정이 79% → 90%로 **늘었다**. 반례가 `test/prefilter.test.ts`에 있다 | D-S21 / S-8b |
| D-C6 | **`retry_as_face` 같은 "구제 경로"는 구제의 질을 재고 켠다** | 되돌려진 사선 자체는 정확했으나 **제대로 배정된 것 143획을 함께 끌어들여 32%를 틀렸다**(기저 6%의 5배). 회수율만 보면 반대로 간다 | D-S9 / D-S19 |


## L-B에서 정한 것

| # | 결정 | 근거 |
|---|---|---|
| D-L9 | **three 카메라를 확정 투시 카메라와 같게 만드는 것은 `sceneCam.ts` 하나가 한다.** 주점이 화면 중심이 아닌 경우는 `setViewOffset`으로 푼다 | 단일 뷰포트(§10.3)의 전제다. 출처가 둘이면 창 크기 변경에서 갈라진다 — `viewport.resize()`가 `aspect = w/h`로 덮어쓰던 자리가 실제로 그것이었다. `projectionHook`으로 한 곳에 묶었다 |
| D-L10 | **`FREE_FOV_DEG`(자유 궤도 화각 45°)는 `test/constants.ts`에 넣지 않는다** | D-C4의 목적은 "**상수가 바뀌었는데 그것에 의존한 측정이 낡은 줄 모르는 것**"을 막는 것이다. 자유 궤도 화각은 **어느 하네스도 읽지 않으므로** 그 실패 방식이 성립하지 않는다. ⚠ **"해시가 바뀌어 기존 산출물이 STALE이 된다"는 근거로 쓰지 않는다** — L-A.2가 `VP_TOL`을 등록하며 20건을 STALE로 만들고 받아들였다(리뷰어 지적). 그 근거를 인정하면 어떤 상수도 등록할 수 없다. **하네스가 이 값을 읽게 되는 순간 넣는다** |
| D-L12 ⚠**대상 소멸**(2026-08-17 지시 2 — 검출 초안·민감도 경로 삭제, D-L54) | **핸들 예산을 화면에 낸다**(축별 °/px와 px). 1px 미만이면 "손으로 맞출 수 없다"고 알린다 | 자동 판정이 원리적으로 불가능해 사용자의 눈이 유일한 판정 수단인데(§5.3) **눈은 자기 손이 얼마나 정밀해야 하는지를 못 본다.** L-A.7이 잰 값이 원장에만 있었다. 계산은 `vpSensitivity.ts` 하나가 하고 **앱과 하네스가 같이 쓴다**(#17) |
| D-L13 ⚠**대상 소멸**(2026-08-17 지시 2 — 검출 초안·민감도 경로 삭제, D-L54) | **가이드는 선 자체를 잡아 옮기고, `소실점 추정`이 캔버스 끝까지 늘린다** | 라이노·SketchUp에서 선을 끄는 것과 같다(A-3). 늘리기는 **방향과 소실점을 안 바꾸고 지렛대만** 늘린다 — 예산이 길이에 거의 반비례하기 때문이다. ⚠ **이득은 중앙 1.33배뿐이다**(`guide_budget.json`): 늘린 뒤 길이는 그 방향의 캔버스 현이 상한이라 세로 가이드는 588px에서 막힌다 |
| D-L11 | **`chord_dev`(옛 `ink_gap`)에 임계를 걸지 않는다** — 측정이 아니다 | f를 ±20% 틀리게 줘도 **소수 넷째 자리까지 그대로**다(`stage_cam.json`의 `control_wrong_camera`, 5구도 전부). `seg_gap = 0`이므로 재투영 세그먼트는 언제나 대표 직선이고, 값을 정하는 것은 **잉크 모델의 활(`lf_bow`)과 획 길이**뿐이다. AS-L3이 "사용자에게 실제로 보이는 변화"라 적어 둔 것을 **파이프라인의 측정으로 읽으면 안 된다** |
| D-L14 | **스냅 반경은 `SNAP_TOL.radius_ratio = 0.05`**(하네스 960×672에서 58.6px · 앱 1280×675에서 **72px**). 선례(SketchUp/Rhino 10~15px)의 5~7배다 | 손 획의 겨냥 오차 자체가 크다 — 시작 잉크 점이 참 모서리의 상에서 어긋나는 거리가 중앙 **4.7 / 14.7 / 23.8px**(`endJitter` 0.01/0.03/0.05, `snap.json@f351839a`). ⚠ **사전 등록한 두 규칙이 둘 다 실패했다**(순이득 최대는 픽스처에 비용이 없어 계속 커지고, 틀림 ≤ 10%는 대상이 촘촘하면 해가 없다). 그래서 **비용을 측정에 넣었다** — `unintended_snap`(무관한 자리에서 시작하려 할 때 끼어드는 비율). 0.05 → 0.10은 맞음 **+27**에 끼어듦 **+918**이다. 규칙 C(포화점)도 절단 0.5%·1%에서 k3에 대해 0.05를 고른다(**2%면 0.035** — 그 사실도 적는다) |
| D-L15 | **한계를 정하는 것은 반경이 아니라 (겨냥 오차 / 대상 간격)의 비다** — 스냅으로 못 고치는 몫이 있다 | 격자 k3(꼭짓점 간격 중앙 21.3px)에서 **틀린 붙음이 반경과 무관하게 15076/38880에서 포화한다.** 전부 `endpoint`이고 **이웃 꼭짓점을 문 것**이다. 겨냥 오차 중앙 12.2px이 간격의 절반을 넘기 때문이다. 반경을 줄여도 늘려도 안 없어진다. AS-C2를 "겨냥 오차의 절대 크기" 문제에서 **비의 문제로** 다시 적었다 |
| D-L16 ⚠**대상 소멸**(2026-08-17 지시 2 — 검출 초안·민감도 경로 삭제, D-L54) | **초안 지지선은 `len_x_sep`**(`len_i · len_j · sin(각차)` 최대)이고, **빈 축만 중립 위치로 채운다** | 소실점은 **두 선의 교점**이라 조건을 정하는 것이 길이만이 아니다 — `longest`의 가이드 쌍 각차는 **최소 0.03°**였고 그런 축은 무한원 스냅이 비운다. 지지선만 바꾸면 카메라 성립 **31/90 → 43/90**, 축 오차 중앙 12.35° → **9.05°**, f 0.3007 → **0.2432**(`draft_recover.json@509615a1`). `widest`가 확정률 1건 높지만 축 오차·f는 `len_x_sep`이 낫다. **채우기**는 `guides_lt_4` 21/90 때문이다 — 그 축은 손잡이가 아예 없어 **사용자가 끌 것이 없고** 조정 UI의 전제가 깨진다. 중립은 축 오차 **13.83°**로 초안보다 나쁘므로 **있는 축은 안 건드린다**. 합쳐서 `guide_budget`의 카메라 성립이 **31/90 → 58/90**이 됐다(상한은 72/90이다). ⚠ **출하 구성**(`len_x_sep_filled`)의 f 상대오차는 **0.3152로 기준선 0.3007보다 나쁘다** — 채운 축이 f 산출에 들어가기 때문이다(리뷰어 2회차 [9]). **'f가 나아졌다'는 지지선만 바꾼 팔의 성질이고 출하 구성에는 해당하지 않는다.** 채택 근거는 확정률과 축 오차이지 f가 아니다 |
| D-L17 ⚠**대상 소멸**(2026-08-17 지시 2 — 검출 초안·민감도 경로 삭제, D-L54) | **무한원 스냅(`HOMOG_TOL.snap_deg` 3°)은 유지한다** — 초안 실패의 지배항이 아니었다 | 끄면 31/90 → 40/90이다(**9건**). 지지선만 바꾼 팔에서는 스냅된 축이 42 → **0**이 된다. ⚠ **출하 구성에서는 다시 58이다**(리뷰어 2회차 [10]) — 채우기가 넣는 중립 수직 둘이 **정확히 나란해서**(각차 0°) 무한원으로 판정되기 때문이고, **그것이 옳은 동작이다**(화면 평행 축에는 소실점이 없다, 이론서 2.2). 즉 스냅이 비우는 것은 검출 실패가 아니라 **채운 기본값**이다. 유지 근거는 원래대로다: 스냅은 **좁은 각차에서 조작이 불가능해지는 것**을 막는 장치다(각차 3°에서 0.39°/px = 0.5° 예산의 78%) |
| D-L18 | **`AXIS_TOL`과 `LIVE_TOL.axis_deg`를 이번에 바꾸지 않는다** — 다만 **조이는 방향이 유력하고 L-B.7 뒤에 다시 본다** | 45조합 전부에서 배치와 조용히 틀림이 **단조로 맞바뀐다**(`axis_live.json@f351839a`). 등록 규칙("조용히 틀림 ≤ 기준이면서 배치 최대")이 고르는 것은 기준보다 **+26/4320**이고 시드 폭 **31**(387~418, 실측) 안이다. ⚠ **그 규칙은 완화 방향만 볼 수 있다** — 반대로 보면 `vp0.03_bend0.18_amb2`가 배치 −237에 **축 오배정 −94 · cut 0.2 −8.3pp · cut 0.5 −39%**다. 이 프로젝트의 정책(A-3·§9.1: 미배치는 대기, 승격이 연쇄)에서는 **그쪽이 맞다.** 그러나 **회수 기전이 아직 없으므로**(L-B.7) 지금 바꾸면 검증되지 않은 전제 위에 서게 된다 |
| D-L19 | **축 고정은 `resolveLive` 안에 둔다.** 바깥에서 축을 덮지 않는다 | 미리보기와 확정이 **같은 함수**를 부르는 것이 §11 게이트의 "일치 0"을 **항등**으로 만드는 근거다(그래서 그 항목을 게이트에서 뺐다). 바깥에서 덮으면 그 항등이 깨지고, **깨진 것을 아무도 못 본다** — 둘이 다른 코드가 되는 순간 배선 테스트가 잡을 대상이 사라진다 |
| D-L20 | **승격은 "다시 푸는 것"이 아니라 "앵커가 생기는 것"이다** — 일괄 재풀이 경로를 쓰지 않는다 | 계획서 §9.1의 "§5.4의 일괄 풀이를 재사용한다"를 그대로 만들어 돌렸더니 **회수 0/1904**이다(`promote.json@f351839a`, 세 팔 전부). 대기 사유가 `축이 미분류다`(1116)와 `구조에 이어지지 않았다`(620)인데 **둘 다 획 하나의 성질**이라 문맥을 더 준다고 안 바뀐다. 앵커 경로는 **720/1904 = 37.8%**를 회수한다. ⚠ **회수된 것이 더 정확하지 않다**(cut 0.2에서 **392/720 = 0.544** 대 확정 **874/2416 = 0.362**. ⚠ 그 격차는 **전부 게이지 추정기**다 — 형태만 보면 시드 폭 안이라 결론이 없다, D-L22) — 개수의 기전이다. _초판은 게이지를 안 없앤 `segErr`로 43.6% 대 42.3%라 '같다'고 적었다 — **D-L22가 고쳤다.**_ ⚠ **D-L24가 이 결정의 범위를 좁혔다**: 일괄 기전은 반증된 적이 없고 막고 있던 것은 축 판정이다. "일괄 재풀이 경로를 쓰지 않는다"는 **지금 구현에서 실제로 회수하는 경로가 앵커라는 뜻**이다. `promote()`(일괄)는 **대조군으로 남긴다** — 지우면 다음 세션이 계획서 원문을 다시 시도한다 |
| D-L21 | **D-L18 기각.** 축 판정 임계를 안 바꾼다 | 등록 규칙(#26: (배치+회수)가 기준 이상 **이면서** 조용히 틀림이 낮을 것)에서 조이기의 (배치+회수)가 **2976 대 기준 3136**으로 작다 — 회수율이 두 팔에서 37.8% 대 37.2%로 거의 같아 차이를 못 메운다. ⚠ 조이기는 조용히 틀림을 **cut 0.2 0.4037 → 0.329**로 낮춘다(고정 게이지, D-L22. _옛 지표로는 0.426 → 0.385였다_). ⚠ **"규칙이 개수를 우선하도록 짜여 있어서"는 틀린 서술이고 고친다**(리뷰어 [10]) — 규칙은 **두 조건의 연언**이었다. 단조 맞바꿈 아래서 통과하려면 회수가 그 단조성을 깨야 했고 안 깼다. **충족 가능하되 좁은 규칙**이다. 품질 우선에서는 여전히 조이기가 이긴다. **규칙을 사후에 바꾸지 않는다**(#28) — 목적함수를 바꾸려면 사유가 측정 밖에서 와야 하고 그 표본이 없다 |
| D-L22 | **형태 오차의 정의와 게이지는 `web/test/metrics.ts` 하나가 정한다.** 한 번에 푸는 집합은 그 집합에서 배율을 적합하고, **승격 회수 획은 확정 집합의 배율을 고정으로 받는다** | `liftAll`이 깊이를 `gauge_height`로 정규화하므로 **절대 배율은 자유도**다(이론서 16.2·16.4). 그것을 안 없애고 참값과 빼면 그 차이는 형태 오차가 아니라 **게이지 차이**다 — 옛 `promote.segErr`가 게이지 1이었고, 같은 2416획이 `axis_live.json` **0.1222** 대 `promote.json` **0.1533**으로 갈렸다(리뷰어 L-B.7 [1]). ⚠ **회수 획만 따로 적합하면 안 된다** — 그러면 "이미 확정된 틀에 붙는다"는 성질이 지워진다. 고정 게이지로 다시 재니 회수의 조용히 틀림이 cut 0.2 **392/720 = 0.544** 대 확정 **874/2416 = 0.362**다. ⚠ **그 1.5배를 배치의 성질로 읽으면 안 된다**(리뷰어 [1]): 확정은 `fit`이고 회수는 `고정`이라 **추정기가 다르고**, 최소제곱 적합은 정의상 오차를 낮추므로 **편향의 방향이 정해져 있다**(#31). 같은 720획을 **자기 게이지**로 재면 **286/720 = 0.397**이고 그 배수는 **시드별로 0.865~1.150으로 1을 가로지른다** — **시드 폭 안이므로 형태 격차라는 결론은 없다**(#14). 고정 게이지 배수는 시드 여섯 전부에서 1.20~1.57이다. 반대 방향(확정을 회수의 게이지로)도 929/1979로 나빠지고, 같은 부분집합의 자기 게이지는 743/1979이라 **추정기 9.4pp · 부분집합 1.3pp**로 갈린다. 그러므로 정확한 문장은 **"승격이 더 내는 오차는 형태가 아니라 배율이다"**이다 — ⚠ 다만 **꼬리는 자기 게이지에서도 나쁘다**(p90 0.9144 대 0.4468 ≈ 2배). **cut 0.2 비율 · 중앙 · p90의 배수가 서로 달라 한 수로 적지 않는다**(#8) — 앱이 회수 획에 배율을 다시 안 맞추므로 **사용자가 보는 것은 고정 게이지 쪽**이다. ⚠ **구도로 가르면 1점 구도가 90/93 = 0.968이다**(리뷰어 [2]). 확정이 0이라 게이지가 안 서는 장면(기준 팔 14개)은 **형태 오차 분모에서 뺀다**(#11) |
| D-L23 | **정의 해시를 산출물에 넣는다**(`metric_defs: metricsSnapshot()`). 상수 스냅샷과 같은 자리, 다른 대상 | 상수 스냅샷은 **임계값**만 본다. L-B.7에서 상수는 같은 채 **식**이 갈렸고 STALE은 아무것도 안 짚었다. 해시는 **`metrics.ts`의 소스 텍스트**(주석·공백 제외)에서 낸다 — ⚠ 첫 판은 `Function.prototype.toString()`을 썼는데 **vitest(esbuild)와 Playwright의 TS 변환이 같은 코드에 다른 문자열을 내서** 두 러너의 산출물이 서로 STALE로 떴다(`0ef2a685` 대 `ead43fb8`). 그것은 **다시 돌려도 안 지워지는 거짓 양성**이다. 소스 파일은 러너와 무관하다. 반례는 `tests/test_selfcheck_stale_metrics.py` 8건 |
| D-L24 | **§9.1의 일괄 기전을 폐기하지 않는다** — 반증된 적이 없다 | 초판의 "회수 0"은 **미실행이었다**(PITFALLS #32). 대기 1904가 거름에서 전부 걸려 **솔버에 한 획도 안 닿았다**. 축 라벨을 주고 다시 돌리니 오라클 축에서 입력 1904 · 솔버가 놓은 것 **1154** · 채택 **1154(60.6%)**다. ⚠ **앵커 720과 직접 견주지 않는다**(리뷰어 [4]) — 그것은 실제 축 라벨이라 기전 차이와 축 정보 차이가 섞인다. **축을 맞춘 앵커 대조군은 804/1904**이고 일괄이 그보다 **1.44배** 많다(나머지 804 대 720이 축 정보의 몫이다). 동작점을 헐겁게 해도 **축을 맞춘 채로** 앵커는 **968**이라 1154에 못 미친다(2회차 [2]가 실 라벨 934를 대체했다 — 오라클 축에서는 `axis_deg`가 무효라 흔드는 것은 스냅 반경 하나다). ⚠ **그러나 일괄은 더 많이, 더 틀리게 놓는다**(#15·#16). **추정기를 맞춰**(셋 다 자기 게이지) 보면 일괄 **558/1154 = 0.484** · 축을 맞춘 앵커 **269/804 = 0.335** · 확정 **0.362**다 — 일괄만 나쁘다(1.34배는 시드 밴드 밖이다). ⚠ **축을 맞춘 앵커 0.335는 확정 0.362와 구분되지 않는다** — 비가 0.925이고 그것은 같은 세션이 "결론 없음"으로 판정한 시드 밴드 0.865~1.150 **한가운데**다(3회차 [5]). 초판의 '앵커도 확정보다 나쁘다'(고정 게이지 대 fit)도, 1회차 교정의 '앵커가 확정보다 낫다'도 **둘 다 못 적는다.** **§9.1이 살아난 것은 개수 기전으로서다.** 즉 막고 있던 것은 기전이 아니라 **축 판정**이고, 그것이 L-B.3(a)·L-B.4가 지배항으로 지목한 바로 그것이다. ⚠ **오라클은 상한이다** — 연쇄가 실제로 붙인 축 라벨로는 **24/966**뿐이다. D-L20의 "앵커가 생기는 것"은 **지금 구현에서 실제로 회수하는 경로**라는 뜻으로 좁혀 읽는다 |

## L-C에서 정한 것

| # | 결정 | 근거 |
|---|---|---|
| D-L25 | **비용비 λ = 3이고 판정 절단은 0.5다.** 두 값을 **사전 등록 목적함수**로 박는다: `점수 = (놓였고 맞은 것 − 3 × 조용히 틀린 것) / 전체`, 조용히 틀림은 **형태 오차 > 0.5**. 앵커와 확정이 겨루는 자리에서는 **확정(보수적) 쪽이 기본값**이다. ⚠ **근거는 정책 하나뿐이고 측정이 아니다** — 아래를 반드시 함께 읽는다 | **λ는 측정 밖에서 와야 하고**(DEFERRED, AS-C1: 표본 0) **사람이 정했다**(2026-08-16 지시). 근거: 이 도구에서 **미배치는 2D 레이어로 가고 승격 대기 상태가 된다**(§9.1) — 비용이 **유예**된다. **틀린 배치는 조용히 남고 그 위에 구조가 쌓인다** — 비용이 **영구**이고 **복리**다. 비용이 대칭이 아니다. A-3의 "애매하면 놓지 않는다 / 놓지 않되 버리지 않는다"가 같은 판단이다. ⚠⚠ **초판은 "절단 곡선이 그것을 수치로 확인했다"고 적었다 — 철회한다**(L-C.2 리뷰어 [2][3]). 그 문장이 인용한 칸(cut 0.5 · λ=3: 앵커 0.2688 대 확정 0.2713)의 **여유는 0.0025**이고 n = 3240에서 **8획어치**다. **시드별로 다시 재니 부호가 3 대 3으로 갈린다** (`axis_live.json@f351839a`의 `seed_spread_at_dl25_point`: 시드 1·3·4는 확정, 2·5·6은 앵커. 여유 폭 **0.2166**). **그 칸은 근거가 못 된다**(#14). ⚠ **절단 0.5도 측정이 고른 것이 아니다**(#13) — λ=3에서 확정이 이기는 것은 **cut 0.5~0.6뿐**이고 cut 0.4와 0.7·1.0에서는 앵커가 이긴다. 0.5는 `metrics.ts`의 **등록 절단 셋 {0.1, 0.2, 0.5} 중 큰 것**이고 그 이상의 근거는 없다. ⚠ 두 팔의 **추정기도 다르다**(확정=적합 게이지, 앵커=게이지 1)라 그 하네스의 `estimator_warning`이 "'확정이 낫다'는 결론은 할인해서 읽는다"고 미리 적어 두었다. **그러므로 D-L25는 정책 결정이고 측정 지지가 없다.** 그것이 이 결정의 성격이고, 그렇게 적는 것이 이 항목이 하는 일이다 — λ는 원래 측정 밖에서 오기로 되어 있었다. ⚠⚠ **그렇다면 그 정책의 사실 전제도 대조해야 한다**(L-C.2 리뷰어 2회차): 전제는 "미배치는 대기이므로 비용이 **유예**된다"이고 그것이 **AS-L11**인데, 그 항목의 현재 상태는 ⚠️ **"개수는 성립·연쇄는 수렴(픽스처 한정). 품질은 반증. 1점 구도에서 거의 전부 틀린다"**다. 즉 **유예는 절반만 참이다** — 회수율이 동작점 하나에서 37.8%(연쇄 포함 49.3%)이고 **1점 구도는 연쇄가 0**이며, 회수된 것의 **배율 어긋남이 사용자가 보는 오차로 남는다**. **그래도 λ ≥ 3을 유지한다**: 회수가 절반이어도 미배치의 기대 비용은 **틀린 배치보다 작다**(틀린 배치는 회수 확률이 0이고 그 위에 구조가 쌓인다). 다만 **1점 구도에서는 그 논증이 안 선다** — 연쇄가 0이면 유예가 아니라 **영구 미배치**다. **구도별 λ는 미측정이고 DEFERRED다.** ⚠ **#26 범위**: "사전 등록"은 **앞으로의 측정**에 대해서다. D-L25로 다시 채점한 것(D-L21·D-L26)은 전부 **이미 수집된 데이터**이므로 그 재채점은 사전 등록이 아니라 **사후 재해석**이고, 그래서 판정을 뒤집는 데 쓰지 않았다(둘 다 기존 판정을 유지했다). ⚠ **옛 등록 규칙**("조용히 틀림 ≤ 기준이면서 배치 최대")은 **완화 방향만 볼 수 있었다**(#31). D-L25가 그것을 **대체한다** — 새 규칙은 한 수라 양방향이다. **사후 변경이 아니다**(#28): 근거가 측정 밖(사람의 값 판단)에서 왔고 그 사실을 여기 적는다. **대체 범위는 둘이고 둘 다 다시 채점했다** — D-L18/D-L21(축 판정 임계, `DEFERRED`)과 L-C.1의 승격 규칙(D-L26) |
| D-L26 | **L-C.1의 판정("차수 승격을 자동으로 걸지 않는다")은 유지한다** — 다만 근거를 바꾼다. 옛 근거는 "배치가 −168로 줄었다"였고, 새 근거는 **D-L25 목적함수에서 결론이 안 선다**는 것이다 | D-L25가 옛 등록 규칙을 대체했으므로 **L-C.1의 규칙도 다시 채점해야 한다**(L-C.2 리뷰어 [4] — 초판은 D-L18만 다시 채점하고 이것을 빠뜨렸다). 다시 채점하려면 **한쪽만 놓인 획의 형태 오차**가 필요한데 초판 하네스가 `both_placed`에서만 쟀다. 넣어서 다시 재니(`order_promote.json@f351839a`의 `dl25_rescore`) 합산으로는 **승격이 이긴다** — 절단 0.5·λ=3에서 승격 전 **0.1443** 대 후 **0.1705**(여유 +0.0262, n = 2592에서 68획어치)이고 λ ≥ 2의 모든 절단에서 그렇다(λ=1에서만 진다). ⚠ **그런데 시드별로 보면 부호가 3 대 3으로 갈린다**(#14, D-L25가 같은 자리에서 걸린 것과 **같은 형태**다): 시드 2·4·6은 +0.0787/+0.1134/+0.0694, 시드 1·3·5는 −0.0463/−0.0254/−0.0324이고 **폭이 0.1597로 합산 여유의 6배**다. **그러므로 D-L25 목적함수는 승격에 대해 아무 말도 안 한다.** ⚠⚠ **초판은 여기서 "그러면 A-3이 남고 그것은 자동으로 안 거는 쪽"이라 적었는데 그것은 틀렸다** (L-C.2 리뷰어 2회차). 같은 원장에서 **승격은 모든 절단에서 조용히 틀림을 줄인다** (cut 0.1 628 → 485 · cut 0.2 379 → 308 · cut 0.5 187 → 128, 분모 2592 공통)이므로 A-3의 두 조항("애매하면 놓지 않는다" · "조용히 틀린 배치를 만들지 않는다")은 **둘 다 승격 쪽**을 가리킨다. **A-3은 이 판정을 지지하지 않는다.** 판정을 세우는 것은 **계획서 §6.2**다 — "사용자가 즉시 보고 되돌린다. 임계를 정교하게 만드는 것보다 싸다"이고, **무결론이면 자동화의 근거가 없으므로 현상유지(= 수동)**다. 그리고 승격은 **화면이 실제로 움직이는** 조작이라(§6.2) 근거 없이 자동으로 거는 것은 사용자가 안 시킨 큰 변화를 일으키는 것이다. ⚠ **양성 채널은 별개로 산다**(#30, 2회차 리뷰어): 같은 산식이 **가짜 승격**에는 절단 여섯 전부에서 벌을 매기고 등록 동작점에서 **−295획**이다(`dl25_rescore.spurious_control_rescore`) — 즉 **목적함수는 눈이 떠 있고**, 결론이 없는 것은 참 승격의 효과가 **시드 폭 안**이기 때문이다. ⚠ **판정은 같지만 근거가 달라졌다는 것을 적는다**(#28) — 옛 근거("배치가 준다")는 D-L25 아래에서 더 이상 기각 사유가 아니다. 배치와 틀림이 한 수로 합쳐졌기 때문이다. ⚠ **`only_*` 획은 짝이 없다**(#11) — 승격 전후가 다른 획 집합이라 이 점수는 짝지은 비교가 아니다. 분모(전체 2592획)만 같다 |
| D-L27 | **`PICK_TOL.radius_ratio = 0.0145`는 측정이 정하지 않았다 — 선례에서 온 잠정값이다** | SketchUp/Rhino의 픽 조리개가 10~15px이고 앱 1280×675에서 이 비가 **약 21px**이다. ⚠ **`SNAP_TOL`과 다르다**: 그쪽은 `unintended_snap`(무관한 자리에서 시작할 때 끼어드는 비율)으로 **측정이 정했고**(D-L14, 0.05 → 0.10이 맞음 +27에 끼어듦 +918) 근거가 원장에 있다. **고르기에는 그런 측정이 없다** — 커서로 짚는 것이라 손 획의 겨냥 오차(중앙 4.7~23.8px)가 안 들어오고, 대신 **촘촘한 기하에서 엉뚱한 획을 지우는 비용**이 있는데 그것을 안 쟀다. **측정 전에 박은 값이 아니라 선례를 따른 값이라고 적는다**(#26의 취지: 임계가 사후에 움직이지 않게 한다). 재려면 `unintended_pick`(고르려던 것과 다른 것이 잡히는 비율)이 필요하고 그것은 L-D.3의 자리다 |
| D-L28 | **§9.5에서 "면 대각선"의 지정 경로는 축이 아니다** — L-D.1이 축 지정을 안 올린 것은 결과적으로 맞았다. 다만 **면 지정은 아직 없다**(면 생성이 범위 밖이므로 §6과 함께 다시 본다). 지금 상태: 지우는 경로만 서 있고 지정 경로는 **없다** | **새로 재지 않고 기존 원장으로 결론했다**(2026-08-16 지시). `face_diagonal.json@f351839a`가 같은 픽스처에서 둘을 갈라 놨다 — **축으로 배정된 사선**은 15.83%이고 그렇게 놓인 것의 `corner_err_ratio`(끝점 거리 ÷ 상자 대각, **게이지 없음** — D-L22의 형태 오차와 다른 양이다) 중앙이 **0.4483**인데, **면 경로로 회수된 85획(끝점 170)의 중앙**은 **0.0216**이다(**20배**). 그 원장이 직접 `**축으로 배정된 사선이 놓인 것은 회수가 아니라 조용히 틀린 배치다**`라고 적었다. 같은 원장의 면 경로(**축 둘이 펴는 평면 + 앵커가 위치**)는 미분류 사선의 **84.16%**를 놓고 오차 중앙이 0.0216이다.
⚠ **결론을 사선으로 좁힌다**(리뷰어): 그 원장의 모집단은 **면 대각선 120개**뿐이고,
§9.5가 다루는 대기 획에는 **사선이 아니면서 축이 미분류라 대기 중인 것**이 더 많다
(`promote` 계열: 축 미분류 1116 · 구조 미연결 620). 그 집단에 대해 이 원장은 **아무 말도 안 한다** —
"축 지정 자체가 불필요한가"는 **미측정**이다.
⚠ **동작점 하나다**: 시드 9700 · `medium` · `end_jitter 0.01` 하나이고 **시드 폭을 안 쟀다**(#12·#14).
**두 수는 그 조건의 값이다.**
⚠⚠ **항등으로 확인됐다**(리뷰어 2회차, #3). `by_reason`은 `{no_axis_matches 96, ok 19,
multi_axis 4, ambiguous 1}`이고 합이 **120 = `n_diagonals`**다. `axis_assigned_rate 0.1583 ×
120 = 19 = axis_misplaced.n`. 즉 **`axis_misplaced`는 `by_reason.ok`의 다른 이름**이고,
"축으로 배정된 사선은 전부 오배정"은 측정이 아니라 **정의다**(사선에 축이 붙으면 그것이 곧 오배정).
**그러므로 20배의 성격이 바뀐다** — 두 집단(19획과 85획)은 **판정 대상인 그 분류기가 갈라 놓은
disjoint 부분집합**이고, 같은 획에 두 경로를 걸어 본 것이 **아니다**. 짝지은 비교가 아니므로
"면이 축보다 20배 낫다"고 읽으면 안 된다(#11: 분모가 다른 두 양).
⚠ **다만 선택 효과의 방향은 적어 둔다** — 19획은 분류기가 **가장 축답다고 고른** 사선이므로
축 경로의 **자기 최선 사례**다. 그 최선에서도 오차 중앙이 0.4483이다. 이것은
"면이 20배 낫다"보다 약하지만 **"축 경로는 자기가 고른 것에서도 크게 틀린다"**는 말은 된다.
**D-L28의 결론은 그 약한 형태로만 선다.** 짝지은 비교(같은 획에 두 경로)는 **미측정**이다.
그리고 그 19획은
**자동 판정이 틀리게 배정한 것**이지 사용자가 지정한 것이 아니다 — **사용자 지정의 대리값**이다(#2).
⚠ **#35의 오라클이 없다**: 이 원장은 §9.5 조건이 아니라 **자동 경로**의 것이다.
⚠⚠ **원장을 그대로 인용할 수 없다는 단서가 `DEFERRED.md`에만 있었다 — 여기 옮긴다**(리뷰어 [5]).
그 측정은 **폐기된 배치 계층**(D-S14·S-6·S-8)의 것이고 **AS-5는 지금 "무효"로 분류돼 있다**
(`assumptions.md`). D-L1이 "아카이브를 판단 근거로 인용하지 않는다"고 적었으므로,
이 결정은 **무효 표시된 가정 위의 원장을 근거로 쓴 것**이고 그 사실이 결론의 등급을 정한다 —
§6(면)을 열 때 **그 조건에서 다시 재야** 한다. ⚠ 상수 해시가 현재값(`1b6175a4`)이라 **STALE로는 안 잡힌다** — 알고리즘·가정 변경은 기계가 못 본다. ⚠ 이 문장이 **맨 해시**라 `scan_citation_hashes`도 못 봤다(리뷰어 [11], #38이 경고한 이름 기반 사각지대). 상수 해시를 문서에 적을 때는 **원장 인용 형식**(`파일.json@해시`)을 쓰거나 여기처럼 사람이 갱신한다. — PITFALLS 표의 "알고리즘 변경은 사람이 본다"가 이 사각지대다.
⚠ **"0/2는 결함"이라는 L-D.1의 읽기를 철회한다** — 축 지정이 0/2로 안 올라간 것은 결함이 아니라 **옳은 거절**일 수 있다. **근거는 위의 약한 형태다**: 축 지정은 방향만 주고 위치를 안 주며, 축 경로는 **자기가 가장 축답다고 고른 사선에서도** 오차 중앙 0.4483을 낸다. (초판이 쓴 "20배"는 짝지은 비교가 아니므로 이 자리에서 빠진다.) ⚠ **면 지정을 지금 넣지 않는다**: 면 경로는 **평면(축 둘) + 앵커**를 요구하는데 L에는 면 개념이 없고(§6 범위 밖) 스냅이 앵커를 대신하는 구조다. 넣으려면 §6과 함께 봐야 하므로 `DEFERRED.md`로 간다. ⚠ **개수만 세지 않는다**(#9·#15): 면 경로를 붙이면 회수가 늘 것처럼 보이지만 그 원장은 **회수와 조용히 틀림을 갈라 세야** 한다고 적었다 |
| D-L29 | **저장 v2는 v1 문서를 열지 않는다 — 이전(migration) 경로를 안 짠다** | L-D.2. v1에는 **뷰가 없다**. `viewRef`를 만들어 내려면 "이 획이 어느 시점의 것인가"를 지어내야 하는데 그 정보가 문서에 없다 — **없는 소유 관계를 지어내는 것**이고 A-3("애매하면 놓지 않는다")에 어긋난다. 대신 **키를 갈랐다**(`current` 대 `current2`) — 같은 DB에서 서로 덮지 않으므로 v1 문서는 **남아 있고 옛 UI로 열린다**(#34: 같은 조건을 쓰는 다른 호출부를 함께 봤다). ⚠ **귀결을 적는다**: **옛 UI(`index.html`)로 그린 저장은 새 UI(`l.html`)에서 안 뜬다.** 옛 UI를 지울 때(L-D.3 통과 후) 그 문서들이 **열 수단을 잃는다** — 지우기 전에 `JSON` 내보내기로 옮기는 절차를 README에 적어야 한다. 원장: `doc_store.json@f351839a`의 `v1_rejected` |
| D-L30 | **내보내기는 3D만 낸다 — 2D 레이어는 안 나간다** | L-D.2. 대기 획(§9.1)은 **좌표가 없다** — 화면 좌표만 있고 3D가 없다. OBJ·glTF에 넣으려면 임의의 깊이를 정해야 하고 그것이 조용히 틀린 배치를 파일로 내보내는 것이다. **대신 뷰 이름을 주석·노드 이름에 남긴다**(`view=확정 뷰` · `s1@확정 뷰`) — 어느 시점에서 그린 것인지가 후속 작업에 필요하다. ⚠ **`skippedUnplaced`를 세어 낸다**(#7: 추측 대신 카운터) — 몇 획이 빠졌는지 사용자가 안다 |
| D-L31 | **`Stroke.layer`는 지우지 않고 그대로 둔다 — 아직 아무도 안 쓴다** | selfcheck가 **쓰기 0 · 읽기 0**으로 잡고 있고(#18) 저장 v2도 안 담는다. 지우는 것이 #18의 방향처럼 보이지만, 이 필드는 **`QUESTIONS.md` (b)의 자리**다 — "승격 대상이 아닌 획"(해칭·지시선·치수선·메모)을 가르는 상태가 필요하고, 그때 `layer`가 `pending`/`placed`/`surface`/`view`를 담는다. **지금 지우면 그 결정이 왔을 때 CLAUDE.md §1의 자료구조를 다시 고쳐야 한다.** ⚠ **그러므로 selfcheck의 플래그는 유지한다** — 끄지 않는다(#19: 설명이 검사를 끄면 안 된다). 플래그가 남아 있는 것이 "아직 안 정해졌다"의 표시다 |
| D-L32 | **롤 0 제약(지평선 수평 고정)을 앱에 배선하지 않는다** — 코드는 남긴다 | L-D.0(사람 지시). 이론은 맞다: 이론서 3.1(수평 방향의 소실점은 지평선 위) + 롤 0이면 그 직선이 화면 수평이고, §5.3의 자세 자유도가 3 → 2가 된다. 수심 조건(6.3)으로 세 번째 소실점이 `V₃ = (Pₓ, h − (Pₓ−x₁)(Pₓ−x₂)/(P_y−h))`로 **직선 하나가 아니라 점 하나로** 정해지고, 참 카메라에서 **오차 0으로 성립한다**(`horizon.json@509615a1`의 `orthocenter_identity`). ⚠ **그러나 그것은 항등이고 정보량이 0이다**(#5). **게이트는 실패했다**(⚠ 등록 임계 ≈1°는 **정정 전 값**이고 현행은 0.5°다 — 사후에 안 고친다(#26·#28). 방향이 한쪽이라 결론은 안 바뀐다): L-A.6과 같은 조건에서 축 방향 오차 중앙이 7.4048 → **6.1460**(축당 4획)으로 **17% 줄 뿐**이고 임계 ≈1°의 **6배**다. 오라클 축 라벨을 줘도 **4.4599°**로 4.5배다. 꼬리는 거의 안 움직인다(p90 35 → 36, 오라클 12.5, #8). ⚠⚠ **그리고 배치가 붕괴한다** — 놓인 획이 2049 → 792(축당 4획), 6035 → 522(18획), 2045 → 9(48획), 692 → **0**(100획)이고 조용히 틀림도 오른다. **붕괴가 사영의 성질인지 제약의 성질인지 갈랐다**(#32): 사후 사영은 소실점을 자기 지지선에서 떼어 놓으므로, 제약을 **적합 안에** 넣은 팔(`fitHorizonPair` — 미지수 `(x₁,x₂,h)`를 길이² 가중 최소제곱 한 번으로 푼다)을 함께 냈다. **안 살아난다**(848 / 730 / 3 / 0). 소실점 이동량 중앙이 **78~98px**이고, 즉 검출이 낸 두 "수평" 소실점의 y가 실제로 그만큼 어긋나 있다 — ⚠⚠ **초판은 여기서 "붕괴는 제약의 성질이다"라고 적었고 철회한다**(리뷰어 [4][5]). **위약 대조군**(소실점을 같은 크기로 **임의 방향** 이동)이 똑같이 붕괴한다 — 832 / 802 / 14 / 0. 그러므로 붕괴는 **롤 0의 성질이 아니라 검출 소실점을 그 지지선에서 떼어 놓는 것 자체**의 성질이고 **방향과 무관하다.** 옳은 대조군은 적합 팔이 아니라 **위약 팔**이었다. ⚠ 그래도 함의는 남는다 — 롤 0을 걸려면 **반드시 그만큼 옮겨야** 하므로 **검출 해를 사후 교정하는 어떤 방법도 이 대가를 낸다.** ⚠ Δy는 **24~53px**이다(78~98은 적합 팔의 **2D** 이동량이고 Δx를 포함한다 — 성분 혼동을 고쳤다). ⚠ **무한원 축 구도가 원인이 아니다**: 수직축 합성을 끈 팔(`vertical_synthesized` 0)의 배치가 사영 팔과 **완전히 같고**(792/522/9/0), 구도별로는 **3점 구도가 가장 심하다**(424 → 56). ⚠ **`HORIZON_TOL` 스윕은 공허하다**(#31): 아홉 격자점이 같은 수를 낸다 — `applyRollZero`가 `min_slope_deg`를 아예 안 부른다. **그 두 임계는 결론에 안 닿는다.** ⚠ **양성 채널**(#30): 참 축도 같은 각으로 돌려 기준계를 맞추면 손익분기가 **롤 5° 근처**이고 10°에서는 제약이 해를 끼친다(4.47 / 5.06 / 5.93 / **10.01**° 대 기준 6.0933° 고정). **초판은 참 축을 안 돌려 이 대조군이 무효였다** — `roll_zero` 팔이 롤을 되감아 "기울여도 좋아진다"로 읽혔고, 재던 것이 품질이 아니라 **기준계 불일치**였다(#35의 ⚠⚠ · #37). **그러므로 자동 확정 경로를 되살리지 않고 사용자 확정을 유지한다**(§5.4). `horizon.ts`는 D-L6(카메라 일괄 적합)과 같은 처리다 — 측정 근거를 재현할 수 있어야 하므로 코드는 남기고 경로는 안 연다. ⚠ **"초안 품질이 올랐다"고 적지 않는다**: 축 방향 오차는 초안의 지표가 아니다(초안이 하는 일은 **끌기 시작 위치**이고 그 지표는 `draft_recover`의 확정률·축 오차다, D-L16·D-L17). 그 하네스로 안 쟀으므로 적으면 #2다. `DEFERRED.md`에 트리거를 달았다 |
| D-L33 | **MIT로 낸다**(`LICENSE`). D-S24를 **다시 확인해 옮겨 적는다** | ⚠ 옛 근거는 아카이브(D-S24)에 있고 **D-L1이 아카이브를 판단 근거로 인용하지 말라고 했다** — 그래서 여기 다시 세운다. **① 참조 저장소 코드를 한 줄도 안 옮겼다**: `notebysebas/sketchbook-v8`은 **LICENSE가 없고 소스에 저작권 표시도 없다**(= 모든 권리 유보). GitHub 약관(D.5)이 열람·포크까지는 허용하지만 수정·재배포·제품 편입은 아니다(CLAUDE.md §3). 필요한 것(튜브 메쉬·속도 기반 굵기·실행 취소·PWA)은 **전부 표준 구현이라 직접 짰다.** **② 의존성이 전부 허용적이다**: three.js(MIT) · Vite(MIT) · TypeScript(Apache-2.0) · vitest(MIT) · Playwright(Apache-2.0). **③ 이 도구의 가치는 코드가 아니라 측정 원장에 있다** — `stage0/out/`의 40개 원장과 그 판정 과정이 재현 가능한 것이 이 저장소의 내용이고, 그것을 닫아 둘 이유가 없다. ⚠ **Quick,Draw 데이터는 별개다** — Google의 CC BY 4.0이고 `sessions/`에 들어올 실획도 **제공자의 것**이다. 코드 라이선스가 데이터에 미치지 않는다는 것을 `README` 0절에 적었다 |
| D-L34 | **자체 저장 확장자는 `.brnl`이고 내용은 JSON 그대로다** | 이름이 정해지며 함께 정했다(사람 지시). **확장자를 따로 두는 이유**: OBJ·glTF는 **3D만** 내보내므로(D-L30) 뷰와 2D 레이어를 담는 유일한 형식이 이것이고, `.json`으로 두면 아무 JSON이나 열리는 것처럼 보인다(`isDoc2`가 거절한다, D-L29). **내용을 JSON으로 두는 이유**: 텍스트 편집기로 열리고 `git diff`가 되고 디버깅이 쉽다 — **이 저장소의 원장이 전부 그런 이유로 JSON이다.** MIME도 `application/json`으로 낸다 |
| D-L35 | **`wiring.test.ts`를 지우지 않고 검사 목적을 뒤집었다** — "앱이 옵션을 직접 안 적는다" → "**새 UI가 폐기된 배치 계층으로 안 돌아간다**" | 원 검사의 대상이 사라졌다(새 UI는 `settle` 계층을 안 쓴다). PITFALLS가 "대상이 사라진 항목은 지우지 말고 그렇게 표시한다"이므로 방향을 뒤집었다. **공허해지지 않게 짝을 달았다**(#32): `settle(` 이 없다 + `liftAll`·`placeLive`·`solveInto`가 **실제로 있다**. ⚠⚠ **초판은 "원래 보호가 `stage.spec`으로 옮겨졌다"고 적었고 철회했다**(리뷰어) — `stage.spec`은 앱 함수를 부르지만 **참값 대조를 안 한다**(그 원장이 스스로 "측정 경로와 앱 경로를 섞는 일"이라 적는다). **그러므로 지금 그 확인은 없다**: L의 배치 옵션을 하네스와 앱이 같은 출처에서 받는지 아무도 안 본다. `DEFERRED.md`에 트리거를 달았다 |
| D-L36 | **오프라인 양성 채널을 팔 둘에서 하나로 줄이고 기존 읽기를 철회했다** | 팔 A(사전 캐시에서 `.js`를 뺀 **별도 워커**)는 교락됐다 — **앱이 페이지마다 자기 서비스 워커를 다시 등록**하므로 같은 scope에 워커 둘이 얹힌다(캐시 이름 둘의 공존으로 확인. ⚠ 어느 쪽이 제어했는지는 **안 봤다** — "경쟁"은 추론이다). 빌드가 바뀌자 결과가 뒤집혔고, **교락 가능성만으로 그 팔의 결론은 못 쓴다.** 거기서 읽은 "③의 통과는 사전 캐시 목록의 효력이 아니다"를 **철회한다.** 남은 팔 하나(앱의 워커를 두고 **캐시의 `.js`만 지운다**)는 결정론적이고 **문서는 살고 앱은 죽는다**(`doc` true · `app` false) — 그것이 ③이 배제하는 것이다. ⚠ **사전 캐시 목록만의 몫은 이 설계로 안 갈린다**(앱의 등록을 못 끈다) — **미측정**이고 `what_this_does_not_say`에 올렸다 |
| D-L37 | **소실점 확정을 규칙 기반으로 전면 교체한다** — 검출 초안·가이드·민감도 경로를 뺀다 | 2026-08-16 사람 지시. **막고 있던 것은 정확도가 아니라 입구였다**: 검출이 초안을 못 내면 가이드가 안 생기고, 가이드가 없으면 카메라가 없고, 카메라가 없으면 **무엇을 그려도 대기**다. 새 규칙은 추정을 안 한다 — ⓐ 화면 가로·세로 선은 **축 자체**(무한원, 이론서 2.2) ⓑ 깊이선 둘의 교점이 첫 소실점이고 **그것이 지평선을 정의한다** ⓒ 다른 방향 깊이선 **하나**와 지평선의 교점이 두 번째다(셋이 아니다 — 지평선이 자유도 하나를 이미 먹었다, 이론서 3.1) ⓓ 세 번째는 수심 조건으로 **유도**한다(6.3). **유일한 판단은 깊이인가 화면 가로세로인가**이고 임계 둘(4°·8°) 사이는 **애매**로 두어 **묻는다**. 지운 것: 방향 클러스터링·3선 교점 산포 임계·검출 초안 생성(전부 추정 경로). ⚠ **측정이 지시문을 하나 정정했다**(#23): "오차의 출처가 그은 선의 정확도뿐"이 아니라 **두 선의 각차**가 함께 정한다 — 나란한 두 선의 교점은 조건수가 죽으므로 `min_sep_deg` 3° 아래에서는 소실점을 안 만들고 기다린다. ⚠⚠ **축 오차는 4~10° 대역 안에서 자리를 바꾼 것뿐이다**(`rule_camera.json`: 규칙 6.93° 대 검출 9.17°, 같은 픽스처). **임계 0.5°에는 어느 팔도 못 간다** — 자동 확정이 없다는 사실(AS-L10)은 안 바뀌었다. ⚠⚠⚠ **순서가 지배한다**: 축별로 몰아 그으면 6.93°, 번갈아 그으면 **32.57°**다. 규칙 ⓑ가 "**처음** 두 깊이선"이라 그 둘이 다른 축이면 엉뚱한 점이 나온다 |
| D-L38 ⚠**재현 수단 소멸**(2026-08-17 지시 2 — `horizonRollZero.ts`·`horizon.test.ts` 삭제, D-L54. 판정 원장 `horizon.json`은 파일로만 남는다) | **`horizon.ts`를 앞 순서로 다시 쓰고 배선한다.** 폐기된 사후 사영은 `horizonRollZero.ts`로 옮긴다 | 2026-08-16 사람 지시. D-L32가 게이트에 실패한 이유는 제약이 틀려서가 아니라 **순서가 반대**였기 때문이다 — 검출된 두 소실점에 수평을 **사후로 강제**하면 소실점이 자기 지지선에서 떨어지고 `liftAll`이 전부 거절한다(배치 2049 → 792). 바른 순서는 **첫 소실점이 지평선을 정의하고 두 번째가 거기 놓이는 것**이고, 그러면 사영이 아예 없다(두 번째 소실점도 자기 지지선 위에 있다 — 그 선과 지평선의 교점이므로). **`applyRollZero`·`fitHorizonPair`는 지우지 않았다** — `horizon.test.ts`가 D-L32의 판정 근거를 계속 재현한다(CLAUDE.md의 폐기 코드 규칙). ⚠ D-L32의 결론은 **철회하지 않는다**: 그것은 "사후 사영을 배선하지 않는다"이고 지금 배선한 것은 **다른 경로**다 |
| D-L39 | **1점 투시의 f를 기본 렌즈 35mm 환산으로 채운다**(설정이지 측정이 아니다) | 소실점 하나면 자유도 1(f)이 남는다(이론서 5.3). 지금까지 `lensF`를 **아무도 안 세웠고** 그래서 `recoverCamera`가 `ok:false`를 내고 `ctx()`가 `null`이 되어 **1점 투시에서는 무엇을 그려도 대기**였다 — 규칙 ⓑ가 "1점 투시 확정"이라고 하려면 이 자리가 채워져 있어야 한다. **출처를 화면에 낸다**(`fSource = "setting(렌즈)"`, CLAUDE.md §1). 슬라이더는 **소실점이 하나일 때만** 뜬다 — 2·3점에서는 측정이 f를 정하고 설정이 이기면 안 된다 |
| D-L40 | **무한원 축의 3D 방향을 `PlaceCtx.axisDirs`로 넘긴다** | 화면 평행 축은 소실점이 없어 `vps[i] = null`인데 **방향은 정해져 있다**(가로 `(1,0,0)` · 세로 `(0,1,0)`, 이론서 2.2). 그것을 넘길 자리가 없어서 **1점 투시의 가로·세로 획이 못 올라가고 있었다**. `axis: "screen"`으로 두는 길도 있었지만 그러면 방향이 **그 획의 대표 방향**이 되어 같은 축의 두 획이 서로 안 나란해진다. 규칙을 안 쓰는 옛 하네스에서는 `undefined`라 **동작이 같다** |
| D-L41 | **획을 렌더하는 자리 셋을 직선으로 바꾼다.** `pts2d`·`pts3d`는 보존한다 | 2026-08-16 사람 지시. 계획서 §1.1이 "획이 직선 세그먼트가 되고 손떨림은 버린다"인데 **렌더 층이 그 결정을 안 따르고 있었다** — 화면에 손 획이 그대로 보였다. 고친 자리: `inkCanvas`(진행 중 · 확정) · `mainL.drawPending`·승격 손실 표시 · `strokeView` 튜브 중심선(양 끝 둘). ⚠ **3D 레이어는 이미 직선이었다**(`stage.ts`가 `LineSegments`를 쓴다) — 보이던 것은 **2D 잉크 층**이다. ⚠ 진행 중 미리보기는 **증분 그리기를 못 쓴다**(커서가 움직이면 앞 직선을 지워야 한다) → 전체 재그리기로 바꿨다. `pts2d`는 계속 보존하므로 프리핸드로 돌아갈 때 이 네 자리만 되돌리면 된다 |
| D-L42 | **축 스냅(라이노 직교 모드)을 넣고 기본을 켬으로 둔다** — **측정이 반대 방향을 가리키는데도** | 2026-08-16 사람 지시 1. 그리는 동안 방향을 축으로 강제하고 커서는 **직선 위로 수직 투영**된다. 토글(`F8`)과 수정자(`Shift`, 그 획만 자유) 둘을 둔다 — 상황이 다르다. ⛔ **사전 등록 게이트가 실패했다**(`axis_snap.json@f351839a`): 조용히 틀린 배치(절단 0.2, 번갈아 긋기)가 **1094/1322(0.8275) → 1270/1322(0.9607)**로 **나빠진다**. 이유는 기전의 성질이다 — 축 스냅은 획을 축에 **정확히** 붙이므로 **카메라 오차를 고스란히 물려받고**, 스냅이 없으면 획마다 다른 방향이 들어와 전역 해에서 일부 상쇄된다. **이 픽스처의 카메라 축 오차가 임계의 18~26배**(8.84~12.91° 대 0.5°)라 그 증폭이 지배한다. _(초판의 0.811 → 0.967 · 20~30배는 재실행 전 값 — 2026-08-17 원장 재독으로 정정)_ **그래도 켬으로 둔다**: ① 사람 지시가 그리기 **감각**에 대한 것이고(라이노 선례) 측정은 **합성 잉크의 배치 정확도**다 — 같은 것을 재지 않는다 ② 카메라가 임계에 가까워지면 부호가 뒤집힐 수 있고 **그 조건은 안 쟀다** ③ 끄는 길이 둘 다 있다. **측정된 대가를 `PROTOTYPE.md`와 `QUESTIONS.md`에 적었다** — 숨기지 않는다 ⚠⚠[D-L59 주석] 이 항목이 인용한 rule_camera·axis_snap·horizon_pitch 수치는 **4차 D-L59(첫 소실점 = 두 선 교점) 이전 실행의 것**이다 — 그 뒤 원장이 다시 움직였다(**5차 시점**: rule_camera headline 몰아 31.61°·번갈아 27.85°, axis_snap 배치 820, horizon_pitch 세 팔 동일 — ⚠ **6차로 또 움직였다**: headline 몰아(rule_grouped) **35** · 번갈아(rule_drawn) **31.6083**, axis_snap `placed` **1486/1403/1617**(820은 `placed`가 아니라 `snapped_strokes`였다 — 6차 리뷰어가 잡았다)). 현행 값은 원장을 그 자리에서 읽는다(#1). |
| D-L43 | **지평선을 처음부터 두고 모든 수평 소실점을 그 위에 놓는다.** 옵션으로 끄지 않는다 | 2026-08-16 사람 지시 2. 옛 판은 "첫 소실점이 지평선을 정의한다"라 **첫 소실점만 두 선의 교점**이었고 그 둘이 나란해지면 소실점이 폭주했다. 지금은 교점의 한 쪽(지평선)이 **오차 없이 정확**하므로 그 실패 모드가 사라지고 **깊이선 하나로 소실점이 선다**. 높이는 **카메라 피치가 정한다**(궤도로 바뀐다). ⚠ **"사용자가 직접 조절하지 않는다"는 D-L45가 대체했다** — 소실점이 서기 전까지는 사용자가 끈다(그 조작이 곧 피치를 주는 것이다). **위약 팔이 이득의 출처를 갈랐다**(#39): 지평선을 **틀린 높이**에 고정하면 축 오차 **22.95°(번갈아)·28.14°(몰아)**·배치 **775/1322·754/1188**(0.59·0.63)로 무너진다(`axis_snap.json@f351839a` — 초판의 "32~35°·1/3"은 재실행 전 값, 2026-08-17 정정) — 즉 **"있다"가 아니라 "맞다"가 이득이다**. ⚠ **곁가지**: 기본 지평선이 화면 중앙 = **피치 0**이므로 수심 유도가 `null`이고(이론서 2.2) **초기 스케치는 1점·2점뿐이다.** 3점은 궤도 뒤다 ⚠⚠ **2026-08-17 4차 D-L59로 이 결정의 본체가 폐기됐다**: 지평선은 더 이상 소실점 확정의 입력이 아니고(첫 소실점 = 그린 두 선의 교점, 지평선 = 그 y), 이 항목의 위약·오라클 대조가 재던 기전이 없어졌다(`horizon_pitch`·`axis_snap`의 위약 팔이 참 팔과 동일값 — 항목 3 리뷰어 [4]). 아래 수치는 그 시점 기전의 기록이다 ⚠ **[6차 주석]** 이 줄이 인용한 수치는 **6차 지시 11**(소실점은 **세 번째 선**이 정한다) **이전 실행**의 것이다 — 원장이 다시 움직였다(현행 값은 원장을 그 자리에서 읽는다, #1). |
| D-L44 | **`Shift`의 뜻을 "추론 축 잠금"에서 "그 획만 자유"로 바꾼다** | 축 스냅이 **언제나** 돌므로 옛 잠금이 하는 일이 없어졌다(추론이 곧 스냅이다). 라이노 직교 모드의 수정자와 같은 자리다(A-3). 화살표의 **명시적 축 고정**은 그대로 둔다(SketchUp) — 그것은 "추론을 덮는 것"이라 여전히 뜻이 있다 |
| D-L45 | **지평선을 사용자가 끈다**(소실점이 서기 전까지). 기본 피치 상수는 안 바꾼다 | 2026-08-16 사람 지시(QUESTIONS **g**). 문제는 D-L43의 곁가지였다 — 기본 지평선이 화면 중앙 = **피치 0**이라 수심 유도가 `null`이고(이론서 2.2) **초기 스케치가 1점·2점뿐**이었다. 건축 투시의 3점(앙각·부감)은 흔한 구도이므로 **초기부터 서야 한다**. **기각한 쪽(a — 기본 피치를 0이 아닌 값으로 둔다)**: 상수 하나로 끝나 더 단순해 보이지만 **어느 값도 임의**이고 앙각/부감을 사용자가 못 고른다 — **구도를 도구가 정하는 것**이 된다. 끄는 것은 그 상수를 **사용자가 주는 것**이라 (a)를 포함한다. **선례**: 종이 위 투시에서 지평선은 **가장 먼저 긋는 선**이고 화가가 정한다 — 새 기전이 아니라 이론서 3.1의 **역방향 조작**이다(#37). **새 자유도도 아니다**: `RuleState.horizon`이 이미 값 하나였고 자료구조가 안 바뀐다. **잠금**: 유한 수평 소실점이 하나라도 서면 못 옮긴다 — 그 소실점은 지평선 **위에서** 정해진 것이라 지평선만 옮기면 자기 지지선에서 떨어지고 **그것이 D-L32가 실패한 자리다**. 그 뒤로는 궤도가 바꾼다. **반경은 `PICK_TOL`을 그대로 쓴다**(#17: 새 임계를 만들지 않는다). ⚠⚠ **2026-08-17 A-4가 이 항목의 절반을 폐기했다 — 아래 수치는 그 전 실행의 것이고 지금 코드의 값이 아니다.** 수심 유도를 뺐으므로 **지평선은 더 이상 3점을 만들지 않는다**(D-L47). 남은 뜻은 ① 수평 소실점이 놓이는 선 ② **2점에서는 주점 y**다. 재측정값은 D-L47과 AS-L19에 있다. **아래는 폐기된 기전의 기록으로만 읽는다**: **측정**: `horizon_pitch.json@f351839a` — 등록 조건이 **두 순서 · 세 구도 전부에서 통과**한다: 차수 3인 실행 **0/144 → 141/144**(`grouped`) · **0/144 → 136/144**(`drawn`), 축 오차 중앙 **25.24° → 2.11°**(`grouped`) · **11.12° → 2.57°**(`drawn`), 수평 축 둘만 잘라도 같은 방향이다. ⚠ **차수 3은 위약도 낸다**(지평선이 중앙만 아니면 수심 유도가 산다) — 가르는 것은 **참 ↔ 위약**(중앙에서 같은 크기·반대 방향)이고 2.11° 대 35.06°다. ⚠ 위약은 **참에서 2d**이므로 '위약 대 중앙'은 근거로 쓰지 않는다(리뷰어 [2]). **⭐ 채택의 진짜 근거는 허용 오차 곡선이다**: 참에서 **±120px까지** 중앙 팔보다 낫다(2.11~4.74° 대 25.24°). 소실점 핸들 예산이 **0.12~6.8px**이었던 것과 견주면 **자유도 하나를 사람 손이 닿는 단위로 옮긴 것**이다(#29). 창의 바깥 끝은 120~268px 사이이고 **안 쟀다**. ⚠ **참 지평선 팔은 판정 대상이자 오라클**이라 도달 가능성 수치로 못 쓴다(`reachability_absent`, #40) — **사람이 그 창에 드는지는 표본 0**이다(AS-L9·AS-L19). ⚠ '**새 자유도가 아니다**'는 **자료구조** 얘기다(`RuleState.horizon`이 이미 값 하나였다) — 자유도 회계(5.3)로는 **고정 상수(피치 0)가 사용자 입력이 된 것**이 맞다. ⚠ '중앙 = 피치 0'은 **AS-C5**(주점 = 이미지 중심, 미검증) 위에 있다 ⚠⚠ **2026-08-17 4차 D-L59로 절반이 더 폐기됐다**(항목 3 리뷰어 [4]): 확정 전 지평선 끌기는 이제 **소실점 확정에 아무 효과가 없다**(첫 소실점이 지평선을 덮어쓴다) — 남은 효과는 화면 표시뿐이고, 4차 지시 4가 초기 지평선 자체를 지우고 끌기를 "소실점 확정 후 피치 조작"(지시 4-e)으로 재정의할 예정이다 — **그 전까지 이 끌기는 휴면이다**(확정 전엔 지평선이 없고 확정 후엔 잠긴다 — 도달 불가, D-L60) ⚠ **[6차 주석]** 이 줄이 인용한 수치는 **6차 지시 11**(소실점은 **세 번째 선**이 정한다) **이전 실행**의 것이다 — 원장이 다시 움직였다(현행 값은 원장을 그 자리에서 읽는다, #1). |
| D-L46 | **오스냅을 라이노 그대로 넣고, 양 끝이 스냅되면 축 없이 두 점으로 놓는다** | 2026-08-16 사람 지시. 대상·우선순위·표시는 **Rhino Osnap 선례 그대로**이고 새로 설계하지 않았다(A-3) — `끝점 > 중점 > 교차점 > 수선 발 > 근처점 > 면 위 점`. **정점**은 이 자료구조에 대상이 없어서(선분뿐이고 메쉬가 없다) **여러 획이 만나는 끝점**에 그 이름을 줬다 — **위치는 끝점과 같으므로 붙는 자리가 안 바뀌고 표시만 갈린다**(그래서 순위 변경이 배치를 안 움직인다). ⚠⚠ **그리고 지금 기하에서는 한 번도 안 뜬다** — 끝점이 안 합쳐지기 때문이다(아래). **핵심은 양 끝 스냅이다**: 두 점이 선분을 정하므로 **축이 필요 없고**, 그래서 **면 위 사선과 자유 세그먼트가 이 경로로 놓인다**(추론이 아니라 사용자가 두 점을 지정하는 것이다). **우선순위**: 오스냅이 **축 스냅을 이긴다**(라이노에서 오스냅이 직교 모드를 덮는다). **두 점 배치는 축을 안 붙인다**(`axis: "free"`) — 방향이 맞아 보여도 추정하지 않는다(A-3). **승격에서도 같다**: 양 끝을 다시 풀어 둘 다 살아나면 두 점으로 놓고 **하나라도 못 살리면 안 놓는다** (반쪽은 조용히 틀린 자리다). `Stroke.snapEnd`를 신설했고 저장·복원이 넘긴다(옛 저장본은 `null`). **측정**: `osnap.json@f351839a` — 대각선 480개에서 시작점 붙음 **476/480**, 양 끝 붙음 **459/480**, 오붙음 **5/459**(절단 0.2, 잡음 0.05 행에 몰린다). ⚠ **하네스 동작점(반경 0.05)의 값이다** — 앱 조리개는 2026-08-17 지시 H(D-L56)로 15px이고 그 동작점의 값은 미측정이다. ⚠ **끝점 스냅은 정확한 대상만 쓴다**(라이노에서 Near가 기본 꺼짐인 것 그대로) — 안 그러면 `on_edge`가 언제나 걸려 **모서리를 따라 그은 획이 전부 두 점 배치**가 된다(종단 확인이 잡았고 하네스도 같은 규약으로 고쳤다, #17). ⚠⚠ **형태 오차 중앙이 0인 것은 측정이 아니라 보장이다** — 대상이 참 3D라 옳은 꼭짓점에 붙으면 오차가 정의상 0이다(#5·#2). **읽을 값은 오붙음률**이고 그것이 이 원장의 결론이다. ⚠⚠ **초판이 '축 경로로는 0개 = 정의의 귀결'이라 적었고 그것은 틀렸다**(리뷰어 [3]) — 판정을 실제로 돌리니 **480회 중 59(0.1229)가 축으로 배정된다**. 그 배정은 회수가 아니라 **조용히 틀린 배치**다(`face_diagonal.json`의 결론). 즉 두 점 배치의 이득은 '되게 하는 것'만이 아니라 **조용히 틀리는 12%를 안 만드는 것**이다. ⚠ **꺾인 획은 두 점으로도 안 놓는다**(굽음 > `AXIS_TOL.bend_max` → 2D 대기) — 축 경로가 걸러 주던 것이 우회되면 꺾인 획이 조용히 직선이 된다(#16). 이 픽스처에서는 **덮는 대상이 0**이라 효력은 미측정이다. ⚠⚠ ~~정점 종류는 지금 기하에서 한 번도 안 뜬다~~ **좁혀서 정정한다**(2026-08-17 지시 6, 리뷰어 [4]): 그 "한 번도"는 **지터 합성 픽스처의 성질**이었다 — 같은 픽셀에서 시작한 획들(사람이 같은 자리에 덧긋는 경우)은 끝점이 병합 반경 안이라 **정점이 실제로 뜬다**(`basic_flow.json@f351839a`의 `s4_result.snap_start = "vertex"`). ⚠ 그 원장의 `dist = 0`은 포인터가 대상과 같은 픽셀에서 시작한 귀결이라 **측정으로 읽지 않는다**(#40 ②). 지터 있는 끝점의 병합 없음은 그대로 남는다(0.03~0.12·G 대 0.002·G). 병합 반경을 넓히는 것으로 대신하지 않는다(이웃 꼭짓점과 같은 자릿수다) |
| D-L47 | **축 스냅을 카메라보다 먼저 켠다 · 수직축은 처음부터 화면 수직이다 · 2점에서 피치를 판정하지 않는다** (2026-08-17 사람 지시 A) | 아이패드 배포본 시험에서 셋이 함께 보고됐다: ① 세로선이 안 그어진다 ② 확정된 소실점 방향으로 스냅이 안 걸린다 ③ 지평선 아래에서 시작하면 도구가 '내려다보는 뷰'로 임의 판정한다. **원인은 하나가 아니라 셋이고 전부 순서 문제였다.** ⓐ **화면 직교 스냅이 없었다** — 앱의 실시간 경로가 `frame()`을 요구하고 그 함수가 `cam.locked`를 요구했다. 화면에 평행한 방향은 **소실점이 무한원이고 카메라와 무관한데**(이론서 2.2) 카메라를 기다렸다. 이제 `screenOrthoSnap`이 **첫 획부터** 돌고 임계는 `RULE_TOL.screen_axis_deg`(4°)를 그대로 쓴다(#17). **축 스냅과 달리 임계가 있다** — 없으면 45° 깊이선이 가로/세로로 끌려간다. ⓑ **수직 슬롯이 비어 있었다** — 롤 0이면 화면 수직은 **어떤 카메라에서도** 수직축인데 사용자의 선언을 기다렸다. `newRuleState`가 슬롯 2를 **화면 수직으로 채운다**. ⓒ **수심 유도를 뺐다** — `dy = P_y − 지평선`으로 나누는 식이라 **지평선 높이가 곧 3점 여부**였다. 3점은 이제 **사용자가 기울어진 세로 모서리를 '수직축'이라 답할 때만** 선다(`tilted_vertical`). ⓓ **2점에서 지평선은 주점 y다**(피치 0이면 강제된다, 3.1) — 옛 판은 주점을 언제나 이미지 중심에 두어 f²에 `(h−c_y)²`가 얹혔다. ⓔ **최소 길이 임계**(`min_vp_len_ratio` 0.04 = 화면 대각의 4%, **새 임계**. ⚠ 초판은 절대 px(40)였고 리뷰어가 **기존 `AXIS_TOL.min_len_ratio`(0.02)가 있다**는 것과 **이 저장소의 임계가 전부 비**라는 것을 짚어 비로 바꿨다 — 지시의 출처가 아이패드다) — 실수로 그은 작은 선이 소실점을 정의하던 자리. **측정**: `horizon_pitch.json@f351839a` — 참 지평선 축 오차 중앙 **3.39°**(몰아) · **4.88°**(번갈아), 중앙 지평선 **21.60°** · **12.26°**, 위약 **31.61°** · **33.39°**. **차수 3이 세 팔 전부에서 80~85/144** — 즉 **3점이 지평선과 무관해졌고 그것이 A-4의 요점이다.** ⚠⚠ **개선 인용을 리뷰어가 셋 정정했다**(2026-08-17). ① `rule_camera`의 표제 표는 **1pt 항등 구도를 뺀 `headline_no_identity`**다(그 구도는 축 방향이 f와 무관해 어떤 오차를 넣어도 0이 나온다, #5) — 그러므로 인용은 그 표에서 한다: 몰아 긋기 **14.42°** · 번갈아 **9.82°** · 검출 **9.17°**이고 **게이트(규칙 ≤ 검출)는 여전히 실패다**(`passed:false`). 옛 값(6.93 · 32.57)이 어느 표의 것인지 확인하지 못했으므로 **전후 비교를 적지 않는다.** ② 초판이 적은 '카메라가 서는 비율 0.7958 → 0.9292'는 **틀렸다** — 0.7958은 이번 실행의 **검출 팔** 값이고 0.9292는 `horizon.json`의 다른 값이다. 이번 실행의 몰아 긋기 값은 `headline_no_identity.rule_grouped.camera_ok_rate` **0.6875**다(2026-08-17 3차 재실행 값). ③ `axis_snap.json`의 '순서 의존 격차 1.26°'는 **축 스냅과 무관하다** — 같은 파일이 `snap_on__horizon_on`과 `snap_off__horizon_on`을 **똑같이 1.2614**로 적는다(껐다 켜도 같다). 그리고 그 값도 항등 구도를 포함한 층이다. **A의 성과로 적지 않는다.** ⚠⚠⚠ **`horizon_pitch`의 등록 조건 ①('중앙 지평선 팔은 차수 3이 0')은 이제 성립하지 않고 성립해서도 안 된다** — 그 조건은 '지평선이 3점을 만든다'는 **폐기된 기전**을 잰 것이다. ②·③(참이 중앙보다·위약보다 낫다)은 **두 순서 전부 통과**한다. **그러므로 그 원장의 `passed`는 이제 영원히 false다** — 도달 불가능한 기준을 남긴 것이고 그 사실을 `DEFERRED.md`에 등록했다(리뷰어 [8]). ⚠ **문서가 '②·③ 두 순서 전부 통과'라 적는 것과 원장의 `passed_orders: []`는 같은 말이 아니다** — 전자는 조건 둘, 후자는 조건 셋 전부다. **새 기전(기울어진 수직선 선언)의 정확도는 미측정이고 그것을 재는 팔이 원장에 없다**(리뷰어 [10]). ⚠ ~~**`vpVerticalFromOrthocenter`는 안 지운다** — `horizon.test.ts`가 D-L32·D-L43의 판정 근거를 계속 재현한다~~ **2026-08-17 지시 2가 둘 다 지웠다**(D-L54) — 판정 원장은 파일로만 남는다 ⚠⚠[D-L59 주석] 이 항목이 인용한 rule_camera·axis_snap·horizon_pitch 수치는 **4차 D-L59(첫 소실점 = 두 선 교점) 이전 실행의 것**이다 — 그 뒤 원장이 다시 움직였다(**5차 시점**: rule_camera headline 몰아 31.61°·번갈아 27.85°, axis_snap 배치 820, horizon_pitch 세 팔 동일 — ⚠ **6차로 또 움직였다**: headline 몰아(rule_grouped) **35** · 번갈아(rule_drawn) **31.6083**, axis_snap `placed` **1486/1403/1617**(820은 `placed`가 아니라 `snapped_strokes`였다 — 6차 리뷰어가 잡았다)). 현행 값은 원장을 그 자리에서 읽는다(#1). |
| D-L48 | **첫 획이 차수 후보를 좁히고, 그 좁힘을 되돌리지 않는다** (2026-08-17 사람 지시 B) | 소실점이 잡히기 **전에도** 획이 차수를 정한다: **화면 수평선 = 1점 투시 선언**(가로축이 화면에 평행하다는 것이 1점의 정의다, 이론서 2.3) · 화면 세로선은 **아무것도 안 좁힌다**(수직축은 1점·2점 둘 다에서 화면 수직) · 대각선은 2점으로 간다. 상태를 `orderCandidate`(`initial`·`one_point`·`two_point`) 하나로 만들고 **화면·앱·시험이 그 함수를 읽는다**(#17). **지운 것**: 두 번째 수평 소실점이 화면 가로축 선언을 밀어내던 `promoted` 승격 — 지시문이 **'되돌리기를 만들지 않는다. 잘못 그었으면 처음부터 다시 그린다'**이다. 남는 차수 승격은 **2점 → 3점** 하나다. **1점 후보에서는 '두 번째 수평인가 수직인가' 물음이 안 뜬다** — 3점이 불가능하므로 모호가 없다. ⚠⚠ **측정이 규칙을 하나 정정했다**(#23): 초판은 화면 수평선을 **어느 상태에서나** 축으로 받았고, 그러면 3점 구도의 깊이 모서리 중 화면 수평에 가까운 것 **하나**가 그림 전체를 1점으로 되돌렸다 — 그리고 되돌리기가 금지되어 있으므로 **3점으로 갈 입구가 사라진다**(`horizon_pitch.json`에서 차수 3이 141/144 → **21/144**로 무너졌다). 그래서 **1점 선언은 `initial`에서만 열린다** — 지시문의 '**첫 획**이 후보를 좁힌다' 그대로다. **전수 전이표**(6 상태 × 8 획 = 48 셀)를 `test/order_candidate.test.ts`가 잠근다 |
| D-L49 | **펜 채널 셋(보조선·결과선·주석)을 넣고 기본을 보조선으로 둔다** (2026-08-17 사람 지시 D) | 작도 순서 그대로다 — **구축선을 긋고 그 위에 확정선을 덧그린다.** 그래서 기본이 **보조선**이고 그것이 **작도의 본체**다. **스냅 강도를 채널로 가르지 않는다**(D-2): 보조선 단계에 정확한 교점·끝점이 없으면 작도가 안 된다. 채널이 정하는 것은 **표시와 내보내기**뿐이고 **f 추출은 채널과 무관하다**(C-4 — 45° 보조선이든 실제 대각선 벽이든 같은 기하다). **주석만 3D로 안 올라간다**(D-3): 해칭·지시선·메모는 기하가 아니고, 그것으로 카메라를 정하면 조용히 틀린 카메라가 된다. 판정은 `doc.liftable` **하나**가 하고 규칙 투입·배치·승격 연쇄가 그것을 본다(#17). **자동 판정을 하지 않는다**(D-4) — 사용자가 고르고 `고치기`에서 `1`·`2`·`3`으로 바꾼다. **결과선을 강제하지 않는다**(D-6): 보조선만 그려도 3D는 선다 — 그래서 내보내기가 빌 수 있고, 그때 **몇 개가 왜 빠졌는지 화면에 낸다**(#7). 저장 형식이 늘었다(`SStroke.channel`) — **옛 저장본은 보조선으로 열린다**. ⚠ **채널 표시 상수는 `test/constants.ts`에 안 넣는다**(D-C4의 예외) — `CHANNEL_UI`·`CHANNEL_3D`·`PREVIEW_INK`는 **어느 하네스도 안 읽는 표시 값**이라 STALE의 대상이 아니다. 그 사실을 여기 적는다 |
| D-L50 | **선 표시를 네 단계로 세우고 미리보기를 가장 연하게 한다** (2026-08-17 사람 지시 E) | 보고: **"지금 미리보기가 진하고 확정선이 흐리다."** 실제로 그랬다 — 실시간 미리보기가 알파 0.85·굵기 3으로 화면에서 가장 진했고 확정된 3D는 그보다 흐렸다. 순서를 뒤집었다: 결과선(3D) > 보조선(3D) > 미승격 2D > **미리보기**. **축 색은 미리보기에 붙으므로 연한 톤이어야 한다**(`paler()`가 흰색 쪽으로 섞는다) — 색이 축을 말하되 "이것이 결과물"이라고 말하면 안 된다. **§9.4의 "2D를 약하게 구분"**도 여기서 반영됐다(카메라가 서면 알파 0.35). **보조선 표시 토글**(기본 켬)과 **돌리면 자동으로 흐려짐**을 **다른 손잡이로 둔다** — 토글은 끄는 것이고 흐림은 약하게 하는 것이다. ⚠⚠ **처음 고른 흐림 값이 너무 옅어 돌리면 화면이 통째로 비었다**(기본 채널이 보조선이므로 갓 그린 그림은 전부 보조선이다). 종단 확인이 `gl_painted_px = 0`으로 잡았고 0.75/0.5로 올렸다 — **흐림은 약하게 하는 것이지 없애는 것이 아니다.** ⚠ **`paler()`가 `undefined`를 받아 던진 자리가 있었다**: 양 끝 스냅으로 놓이는 획은 세그먼트가 있어도 축이 미분류다(D-L46). 옛 판은 `strokeStyle = undefined`가 조용히 무시됐다 — **색을 계산으로 바꾸자 그 조용함이 예외가 됐다**(종단 확인이 `PAGEERROR`로 잡았다) |
| D-L51 | **터치 제스처를 직접 처리하되, 카메라는 `OrbitControls`의 공개 API로 움직인다** (2026-08-17 사람 지시 G) | 잉크 캔버스가 three 캔버스를 덮고 있어 터치가 `OrbitControls`까지 안 내려간다. 지시문이 후보 셋의 함정을 적어 두고 **"실제로 어느 것이 걸리는지 재고 정한다"**고 했다 — 그래서 셋을 같은 페이지에서 실행했다(`touch_route.json@f351839a`). **① 재발행**: 합성 `pointerId`에서 `setPointerCapture`가 실제로 던진다(`NotFoundError`). **② `OrbitControls`를 잉크 캔버스에**: 펜 사건 하나로 방위각 **1.1170 rad**(10차 재실행으로 다시 정정 — 8차의 1.1187도, 그 전의 1.117도 실행 환경마다 셋째 자리가 움직인다. 결론(펜이 궤도를 돈다)은 값에 안 달렸다) — 펜이 궤도를 돈다. **③ 직접 처리**: 적어 둔 함정("three 최신판은 `rotateLeft` 같은 공개 API가 없다")이 **사실이 아니다** — r185에 `rotateLeft`·`rotateUp`·`pan`·`dollyIn`·`dollyOut` 다섯 다 있다(#23: 계획과 실제가 어긋나면 실제를 따른다). **그래서 ③을 고르되 절반만 직접 한다**: 라우팅만 직접이고 궤도 중심·감쇠·각도 한계·구면 좌표는 `OrbitControls` 한 군데에 남는다(A-3 — 궤도를 새로 짜지 않는다). 환산도 three의 것 그대로다(회전 `2π·Δx/높이` · 팬 픽셀 · 핀치 거리비). **라우터는 `InkCanvas`다** — 펜·마우스·터치를 가르는 자리(`inkable`)가 이미 거기이고 좌표 변환이 `canvasFrame` 단일 출처를 지나야 하기 때문이다(#21, dpr 2에서 비 **1.0000**으로 확인). ⛔ **`pointerEvents = "none"` 전환을 지웠다** — 궤도 동안 잉크 캔버스를 비켜서면 그 사이 스냅·호버·펜이 전부 죽는다. **손가락 1 = 궤도 · 2 = 팬·줌은 선례다**(SketchUp for iPad · Shapr3D). **손가락이 `궤도` 버튼을 대신하고 핀도 푼다** — 다만 **도구는 안 바꾼다**(돌린 뒤 펜으로 바로 그린다). **기각**: `궤도` 버튼을 아예 없애는 것 — 마우스에는 여전히 모드가 필요하다(지시문 "마우스는 버튼 모드를 따른다"). ⚠ **`GESTURE_TOL`은 `test/constants.ts`에 안 넣었다**(D-L49의 예외와 같은 자리): 조작 감도이지 측정 임계가 아니고, 전역 해시가 하나뿐이라 넣으면 무관한 원장 40여 개가 STALE이 된다. 대신 **원장이 그 값을 자기 안에 적는다** |
| D-L52 | **팜 리젝션의 동작점은 둘이다 — 펜 접촉 중, 그리고 펜을 뗀 뒤에도 남아 있는 터치** (2026-08-17 G-2) | ①만 걸면 **쉬고 있던 손바닥이 펜을 떼는 순간 궤도가 된다**(#12: 동작점을 하나 고르지 않는다). 펜이 **나중에** 내려오는 경우도 있다(손가락으로 돌리다 그리기 시작) — 그때는 진행 중인 제스처를 취소하고 그 손가락들을 ②로 넘긴다(펜이 이긴다). 상태는 `PalmGate` 하나가 들고 DOM을 모른다(node에서 잰다). **발동 횟수를 센다** — 조용한 거부는 재발을 관측 불가로 만든다(#22의 교훈). **측정**: `touch_route.json@f351839a` — 세 팔 전부 이동 **0**이고 **대조군(펜 없이 같은 터치)은 0이 아니다**(`palm_rejection.control_no_pen.moved` — 8차 1.6688 · 10차 재실행 1.6664: 값은 실행마다 흔들리고 결론은 그 크기에 안 기댄다, 10차 리뷰어 2차 [10] 정정). 대조군이 없으면 "터치가 원래 안 먹는 것"과 구분이 안 된다(#6). ⚠ **이 줄은 8차 항목 1-c의 새 검사(`scan_cited_values`)가 첫 실행에서 잡았다** — 오래 인용하던 **1.2411**이 원장 재실행으로 낡아 있었고, 해시가 안 움직여 `scan_citation_hashes`는 못 잡았다(#42 ⑥ ①형). 대조군 값은 팔들이 **같은 페이지 상태를 이어 쓰므로**(원장 `arm_start_state`) 실행마다 흔들린다 — **결론이 기대는 것은 그 크기가 아니라 "0이 아니다"이다**(#6·#14) |
| D-L53 | **카메라 상태를 넷(NONE·P1·P2·P3)으로 확정하고 차수를 저장하지 않는다** — `perspectiveOrder(rules) = f(소실점 집합, 가로선 유무)` 하나를 표시·판정·저장이 전부 부른다 (2026-08-17 사람 지시 1) | 확정 방식을 네 번 바꾸며 네 세대가 코드에 겹쳐 있었고 오류 대부분이 거기서 났다. **후보 개념이 없다 — 확정이거나 아니거나다.** 전이는 셋뿐: NONE→P1(가로선) · NONE→P2(서로 다른 소실점 둘) · P2→P3(기울어진 수직선의 수직축 답). **P1은 불가역**이고(두 번째 수평 소실점은 서지 않는다 — 기존 소실점을 안 향하는 깊이선은 **거절**되고 획은 2D로 남는다) P1의 f는 **임의값**(`P1_F_RATIO`×화면폭)이라 거리점 없이 3D가 선다(지시 5-4 해소). ⚠ **차수와 "카메라 섬"은 서로 다른 계산이다**(리뷰어 [7]): 가로선만 그은 상태는 `order = 1`(P1 확정 — 불가역이므로 후보가 아니다)이되 `standing = false`다 — 깊이 소실점이 서야 임의 f로 3D가 선다. 지시문의 "P1: 축 셋 다 있음"은 그 완성 상태의 서술이고, 확정(차수)이 먼저 오고 카메라가 뒤따른다. "형태는 정확하고 깊이만 무차원"은 **지시문의 문장**이고 그 근거는 `frontalWorld` 게이지(화면 평행면의 기하가 f와 무관)다 — ⚠ 이론서 16.4는 일반 상황의 잔여 자유도를 **깊이 방향 아핀(전단+스케일)**이라 적는다. P1에서는 주점 = 소실점(측정)이라 전단 성분이 빠지고 **스케일만 남는다는 것이 이 설계의 주장**인데, **그 형태 오차를 잰 팔이 원장에 없다**(#37을 늦게 확인했다 — DEFERRED 등록) ✅ **부분 해소(2026-08-18 8차 2차 지시 항목 2 · 8-2R″ [4])**: `order_lock.json@f351839a`에 **f 팔 셋**을 넣었다 — `fixed_true_f`(참 f) · `fixed_f_half`(×0.5) · `fixed_f_double`(×2)와 개입 크기 `f_intervention`. **실측**: ×0.5에서 P1 중앙 1.6914 → **2.0866** · ×2에서 → **1.3061**이라 **형태 오차는 f에 반응한다**(자기 게이지가 지우지 않는다 — 비등방 오차다). **그러나 '형태는 정확하다'는 안 나왔다** — 참 f에서도 P1 중앙이 **1.6618**이다. ⚠ **완전 해소가 아니다**: 이 픽스처의 참 f(1000)와 임의 f(960)가 4%밖에 안 달라 **'임의 f 자체의 대가'는 여전히 미측정**이고, 동작점이 ×0.5·×1·×2 셋뿐이다(#12). **그러므로 '형태는 정확하고 깊이만 무차원'은 이 픽스처에서 지지되지 않는다** — P1의 형태 오차는 참 f에서도 크고, 그 크기를 정하는 것은 **소실점**이다(소실점만 참으로 주면 중앙 0.4782 · 절단 0.2 안 0 → 39). | `locked`·`lockedOrder`·`orderCandidate`(48셀 전이표)를 지웠다 — "확정됐는가"는 `standing()`이 계산한다. **확정·승격이 자동이다**: 카메라가 처음 서면 조용히 들고(무변화 전환 — 알리지 않는다), P2→P3 전이가 나면 전부 다시 풀고 **알린다**(형태가 실제로 움직인다, 지시 3-d). ⚠⚠ **측정이 규칙 하나를 정정했다**: 소실점이 하나 서 있는 상태에서 화면 수평선(≤4°)을 **조용히** P1 선언으로 받게 한 초판에서 몰아 긋기 축 오차 중앙이 세 자릿수로 뛰었고(기전: 손 오차로 얕아진 깊이 모서리가 그림 전체를 1점에 가둔다 — D-L48의 141/144→21과 같은 문), **소실점이 있으면 묻는다**(A-3)로 고쳐 현행 값(몰아 9.9543°, `axis_snap.json@f351839a`)으로 회복했다. ⚠ **중간 실행의 붕괴 크기(중앙 31.6°·`rule_camera` 몰아 33.9°)는 원장 밖 값이다**(#25 — 그 코드는 커밋되지 않아 재현 불가. DEFERRED "원장 밖 회귀 수치" 항목). 방향의 증거로는 **직전 커밋 원장과의 대조가 남는다**: `git show 47da6c4:stage0/out/axis_snap.json`의 몰아 10.0977° ↔ 현행 9.9543°(가드 있음) — 가드가 없던 중간판만 뛰었다. ⚠ **이 층은 1pt 항등 구도를 포함한 5구도 혼합이다**(D-L47이 인용을 금지한 그 층 — `axis_snap`에는 항등 제외 층이 없다. DEFERRED 등록). 항등 행은 두 팔에 똑같이 들어가 중앙값을 희석하는 방향이므로 **방향 판정은 산다**. 물음 빈도의 대가: `rule_camera.json@f351839a` `headline_no_identity`의 `asks_per_run` **3.18(번갈아)·4.02(몰아)**(직전 커밋 원장에서는 2.5~2.6 — `git show 47da6c4:...`로 재현된다). ⚠ P1 임의 f 값(`P1_F_RATIO`)은 임계가 아니라 게이지라 `test/constants.ts`에 안 넣는다(D-L49·D-L51과 같은 자리 — 전역 해시 하나의 결함은 DEFERRED에 있다) ⚠⚠[D-L59 주석] 이 항목이 인용한 rule_camera·axis_snap·horizon_pitch 수치는 **4차 D-L59(첫 소실점 = 두 선 교점) 이전 실행의 것**이다 — 그 뒤 원장이 다시 움직였다(**5차 시점**: rule_camera headline 몰아 31.61°·번갈아 27.85°, axis_snap 배치 820, horizon_pitch 세 팔 동일 — ⚠ **6차로 또 움직였다**: headline 몰아(rule_grouped) **35** · 번갈아(rule_drawn) **31.6083**, axis_snap `placed` **1486/1403/1617**(820은 `placed`가 아니라 `snapped_strokes`였다 — 6차 리뷰어가 잡았다)). 현행 값은 원장을 그 자리에서 읽는다(#1). |
| D-L54 | **폐기 코드를 지웠다** — 렌즈 경로(`DEFAULT_LENS_MM`·`lensMm`·`PRESETS`) · 거리점(`RuleState.distance`·`distancePoints`·`distance_point` 사건) · 차수 후보(`orderCandidate`·전이표 시험) · 사후 사영(`horizonRollZero.ts`) · 수심 유도(`vpVerticalFromOrthocenter`) · 검출 초안 호출부(`vpDraft.ts`·`vpSensitivity.ts`와 그 하네스 `vp_draft`·`vp_sensitivity`·`guide_budget_measure`·`draft_recover`·`horizon.test`) (2026-08-17 사람 지시 2) | 지시문: "지운다. 테스트가 깨지면 그 테스트도 폐기 대상이다. 근거는 DECISIONS에 이미 있다"(각각 지시 C-1 뒤집기·기하 구분 불가·D-L48 대체·D-L32/38·**지시 A-4**·D-L37 — ⚠ "A-4"는 CLAUDE.md의 품질 하한 절과 이름이 겹치므로 지시 쪽은 "지시 A-4"로 적는다, #41의 같은-이름 함정). **HANDOFF의 "폐기 코드 넷은 남긴다"를 사람 지시가 덮었다** — 그 원장들(`horizon.json`·`draft_recover.json`·`guide_budget.json`)은 파일로 남되 재실행 수단이 없어졌음을 여기 적는다. `clipToCanvas`만 `clip2d.ts`로 옮겼다(초안 로직이 아니라 종단 확인이 쓰는 범용 기하). ⚠ **`SHARED_CONSTANTS`의 `DRAFT_TOL`·`SENS_TOL`은 동결 리터럴로 남겼다** — 키를 빼면 전역 해시가 움직여 이 상수에 의존한 적 없는 원장 40여 개가 STALE이 된다(D-L51의 `GESTURE_TOL`과 같은 자리). 아무 코드도 안 읽는다. `HORIZON_TOL.min_vp_sep_ratio`도 같다. 의존 집합별 해시가 서면 뺀다. **남긴 것**: `pts2d` 보존(프리핸드 복귀 경로 — 모드 플래그는 원래 없었고 렌더 네 자리가 직선을 그린다, D-L41 그대로) · `vpDetect.ts`(측정 하네스의 검출 팔) · `fPixelsFrom35mm`(순수 환산 함수). ⚠ **이 삭제로 재현 문장이 거짓이 된 결정들**: D-L12(민감도 단일 출처) · D-L13 · D-L16 · D-L17(초안 회수) · D-L32/D-L38("`horizon.test.ts`가 계속 재현한다") · D-L47("`vpVerticalFromOrthocenter`는 안 지운다") — 각 항목에 대상 소멸을 표시했다(PITFALLS 머리말 규칙). CLAUDE.md A-4의 "폐기 코드를 지우기 전에 게이트를 통과시킨다"도 이 지시가 덮는다 — `rule_camera`의 게이트(규칙 ≤ 검출)는 여전히 `passed:false`인 채로 지웠고, 그 사실을 숨기지 않는다 |
| D-L55 | **상태 표시를 제거했다 — 전환이 느껴지면 안 된다** (2026-08-17 사람 지시 3) | 차수·후보·소실점 수·f와 출처·"다음에 그을 것"·슬롯 상태·스냅 반경·"그리는 중" 판정·지평선 캡션("피치 0"·"내려다봄")·복원/전환 요약을 전부 지웠다 — **시스템 사정이고, 알리면 "3D로 전환된다"는 인상을 준다.** 남긴 것: **채널·도구 버튼**(사용자가 조작하는 것) · **물음 패널**(답해야 진행된다) · **차수 승격 알림**(형태가 실제로 움직인다 — 예외 d) · **최소 안내 하나**(획 8개 이상인데 3D가 0일 때 한 줄 — 예외 d의 "빈도가 낮아야 한다"). ⚠ CLAUDE.md §1의 "fSource가 화면에 나온다"와 어긋난다 — 지시 3이 더 새로우므로 fSource는 **자료구조와 `S2S` 진단에만** 남는다(숨기는 것이 아니라 화면 밖으로 옮긴 것). 상태 문구를 잠그던 e2e 단언 넷은 표시 제거에 맞춰 뺐고, 결정론 스크린샷 비교는 **요소 스크린샷 → 잉크 캔버스 비트맵**으로 바꿨다(DOM 글자 안티에일리어싱이 로드 간 ±1 레벨로 흔들린 실측 22px — 재려는 것은 잉크 결정론이다) |
| D-L56 | **오스냅 조리개를 앱 층에서 15px(화면 px)로 줄이고 설정(4~40px)·종류 토글을 단다** — **측정이 반대 방향을 가리키는데도** (2026-08-17 지시 H) | 지시문: "반경이 너무 넓다. 기본을 줄이고 설정에서 조절 가능하게. 화면 픽셀 기준, 확대·축소와 무관. 종류별 켜고 끄기 — 라이노 방식." 15px는 라이노/SketchUp 조리개(10~15px) 선례다. ⛔ **맞음/미스는 넓은 쪽을, 의도치 않은 붙음은 좁은 쪽을 가리킨다**(4차 재검 [1-b]가 표제를 정정 — 초판은 전자만 봤다). 수치는 `snap.json@f351839a`의 **`net_excluding_nailed_endpoints`**(4차 재검 [1-a] 정정 — 초판이 인용한 `by_radius_all`은 못 박힌 끝점 1080셀이 섞여 원장 스스로 결론에 쓰지 말라 한 층이다): 15px 대역(비 0.008~0.012 — **앱 캔버스 1447px 대각 기준 환산**(15/1447≈0.0104), 재검 [1-c]. 하네스 프레임(1172px)으로 읽으면 0.012~0.018 행이 된다)에서 맞음 **1219~1656/3240(0.38~0.51)** · 미스 **782~1109/3240**, 옛 동작점 0.05에서 맞음 **2826/3240(0.87)** · 미스 56/3240 — 초판 기재(0.53~0.63/0.90)보다 축소의 대가가 15pp 더 크다. 반면 **의도치 않은 붙음**(반경의 유일한 측정 가능한 비용 — 0.05 채택의 등록 근거였던 그 지표)은 좁은 쪽이 낫다: k3에서 15px 대역 **909~1098/6480(0.14~0.17)** 대 0.05의 **1854/6480(0.29)**. 합성 잉크의 겨냥 오차(중앙 4.7/14.7/23.8px)가 커서 맞음이 깎이는 것이다. **그래도 줄인다**: ① 지시가 실사용 감각 보고이고 측정은 **합성 잉크**다(AS-C10 — 같은 것을 재지 않는다. D-L42와 같은 구조) ② 설정으로 되돌릴 수 있다 ③ **실획이 최종 판정한다** — `snapDistPx`(40px 프로브라 조리개 절단 없음)가 그 근거를 쌓는다. ⚠ **프레임 병기**(L-B.3 리뷰어 규칙): 옛 유효값은 하네스 58.6px(960×672)·**앱 72px(1280×675)**이고 축소는 앱 기준 **4.8배**다. ⚠ **측정 상수(`SNAP_TOL.radius_ratio` 0.05)는 안 바꿨다** — 전역 해시(#1 STALE 눈사태). 그 대가로 **`osnap.json`(붙음/오붙음·배치)·D-L46·AS-L4·AS-C2의 수치는 앱 동작점(15px)이 아니라 하네스 동작점(0.05) 전용**이다 — 각 문서에 표시했다(재검 [1-d]가 좁힘: `snap.json`의 반경 **스윕 행**은 합성 잉크 한에서 15px 대역의 값을 주고 위에 인용했다. "미측정"은 **실획**의 맞음/미스다). ⚠ 앱과 하네스는 조리개만 다른 것이 아니다(재검 [7]) — 하네스는 `on_face`(지면)를 끄고(#3) 앱은 켠다. 그래서 앱의 `snapDistPx`·40px 프로브는 on_face를 뺀다(4차에서 배선). `OSNAP` 상수의 constants.ts 비등재 사유도 같다(D-L51·54 선례 — 동작 임계인데도 전역 해시가 하나뿐이라 못 넣는다. 의존 집합별 해시가 서면 옮긴다). ⚠ CSS px 임계라 dpr에 시각 크기는 불변이지만 **실기(dpr 2) 확인은 없다**(#21 — DEFERRED). 그리는 시점의 **자동 분할은 배선하지 않았다**(지시 I의 분할은 지우개가 조각을 집을 때 계산된다) — 상시 분할은 획이 늘 때 O(n²)이고 조각 폭주를 안 쟀기 때문이다(DEFERRED) ⚠⚠ **판정 모집단이 좁아졌다**(2026-08-17 4차 항목 1, 리뷰어 [2]): 2D 오스냅(D-L57)은 `snapStart`를 못 만들므로 `snapDistPx`를 **안 적는다**(#11 — 적으면 미스냅 프로브와 섞인다). 그러므로 "실획이 최종 판정한다"의 표본은 **카메라 확정 후(3D 오스냅) 획으로만** 쌓인다 — 보조선 단계(확정 전)의 겨냥 분포는 이 계기로 안 잡히고, 그 단계가 작도의 본체(D-2)이므로 **이 판정의 덮는 범위는 보고보다 좁다**. 확정 전 겨냥을 재려면 별도 필드가 필요하다(DEFERRED "2D 오스냅의 겨냥 거리 기록") |
| D-L57 | **2D 오스냅 — 대기 획의 끝점·중점·교차점이 후보다. 3D 후보가 이긴다** | 2026-08-17 4차 지시 1. 카메라 확정 전에는 3D가 없어 오스냅 질의 자체가 안 돌았고(`frame() = null`), 보조선 단계(작도의 본체)가 스냅 없이 진행됐다 — D-2의 "보조선 단계가 작도의 본체"가 채널 중립로만 좁게 구현된 구멍이다. `snap2d.ts`가 화면 좌표로 끝점·중점·교차점을 내고(우선순위·조리개·종류 토글은 3D 판과 동일, #17), 확정 후에도 **미승격 2D 획은 계속 후보다**(지시 1-b). **잠정 결정 둘**: ① 3D 후보가 있으면 3D가 이긴다 — 3D에 붙으면 그 획의 3D가 그 자리에서 확정되므로 정보가 더 많다(두 모집단을 순위 섞기하는 복잡한 판은 기각 — A-3 가장 단순한 것). ② 2D 중점은 **화면 중점**이다. ⚠ **"아직 공간이 없어 투시 중점을 못 낸다"가 사유가 아니다**(리뷰어 [4]가 잡았다) — 이론서 11.1의 대각선법은 투시 2등분을 **카메라 없이 결합적으로** 작도한다(대각선 교점, 이론서 182행). **기각한 대안이 그것이다**: 자동으로 대각선법을 쓰려면 어느 두 획이 사각형의 짝인지를 도구가 정해야 하는데 그 정보가 없다(추정 금지, A-3) — 투시 중점이 필요하면 **사용자가 대각선을 직접 긋고 그 교차점 스냅**(이미 후보다)을 쓰는 것이 그 작도다. 화면 중점은 2D 작도 단계의 정렬 관행(끝점 정렬·등분)의 몫이다. **임계의 프레임**(리뷰어 [8], #24): 병합은 `merge_ratio`(0.002)를 **화면 대각**에 걸어 960×672에서 ≈2.3px(3D 판은 기하 크기 기준 — 프레임 병기), 연장은 `extend_ratio`(0.02)를 3D 판과 **같은 매개변수 여유**(선분 길이 대비 2% — 300px 획에서 6px, 조리개 15px보다 작다)로 쓴다. **알려진 위험**(리뷰어 [9], AS-L2): 2D 교차점은 화면 교차라 서로 다른 깊이의 두 획을 거기 붙이면 승격에서 3D 접합으로 굳는다 — 자동 배치가 아니라 **표식을 보고 사용자가 고른 것**이라는 점이 AS-L2의 "조용히 틀림"과 다르지만, 겉보기 교차를 참 교차로 오인할 수는 있다(실획 K가 볼 자리). 끝 스냅으로 획을 펴는 것은 3D 양 끝 스냅의 굽음 규약(`AXIS_TOL.bend_max`)을 그대로 쓴다(#34). 원장: `snap2d_flow.json` · 반례는 `test/snap2d.test.ts` |
| D-L58 | **소실점 방향 스냅(`vp_dir`) — 카메라가 안 서도 소실점이 있으면 그 방향으로 끌린다** | 2026-08-17 4차 지시 2. 소실점을 지나는 화면 직선은 그 축에 평행한 3D 직선의 상이므로(이론서 2장) f 없이 화면에서 성립한다 — 그런데 축 스냅(`snapToAxis`)은 3D 앵커·f를 요구해 NONE(소실점 1)·비확정 상태에서 통째로 안 돌았다. `axisSnap.vpDirSnap`: "향한다" 판정은 `AXIS_TOL.vp_dist_ratio`(지지선 판정·`axisOfStroke`와 같은 값, #17) · 끝점을 a→V 직선에 수직 투영(직교 스냅 규약) · 소실점 여럿이면 **이동량이 작은 쪽**(chooseAxis의 커서 규칙). 화면 직교와 겹치면 역시 이동량으로 가른다. **표 계약 변경**: `snapAxisTable`의 "vp+비확정 = null"이 `vp_dir`가 됐다 — 완화가 아니라 경로 구분이고(`axis_snap_table.test.ts`에 사유 명기), 축 스냅과 달리 **임계가 있다**(임계 밖은 그대로 — 둘째 소실점을 만들 대각선을 그을 수 있어야 한다). 미리보기·라벨은 그 축 색(지시 2-c). 스냅 후 misfit 0은 보장이다(#5 — 판별은 원장 `vp_dir_flow.json`의 회귀 팔이 한다). 원장: `vp_dir_flow.json` · 반례 `test/vp_dir.test.ts`. **리뷰어 대응**(4차 2-R): ① **프레임 병기**([B-6], #24) — "향한다" 판정은 `vpMisfit` **함수 그대로**이고 rep 구성(끝점 둘·bend 0·len=끝점 거리)도 `stepRule`의 지지선 검사와 **글자 그대로 같다** — 같은 식·같은 프레임이라 0.06의 각 환산(D-L2)도 그대로다. ② **자기참조 아님의 근거**([B-8], #5) — vp_dir로 붙은 획은 지지 **수**만 올린다. 소실점 **위치**의 재적합은 없다(슬롯은 확정 후 잠기고 support는 카운터다 — 예외는 `tilted_vertical`의 수직 슬롯뿐이고 vp_dir는 수평 소실점만 본다). ③ **규칙 입력은 스냅된 선이다**([B-9]) — 확정 경로가 `snapped2d`로 끝점을 옮긴 **뒤** `feedStroke`가 돌므로(미리보기=확정 — ⚠ 이 시점에는 onLive·snapped2d가 **같은 논리의 두 사본**이었고 원장들이 잰 것은 확정 경로다. 항목 5의 `resolve2d` 통합으로 구조적 보장이 됐다, 5-R [B-8]), 이동량 선택(직교 ↔ vp_dir)이 차수 판정의 입력을 정한다. 화면 수평으로 스냅되면 정확히 0°라 `screen_h`가 확실해지고(AS-L15의 조용한 오독 방향이 줄어든다), vp_dir로 스냅되면 misfit 0이라 지지선이 확실해진다 — P1 선언 문은 "소실점이 있으면 가로선을 묻는다"(D-L48 물음)가 계속 지킨다. ④ **A-3(3차)과의 관계**([B-7], #34) — 3차 A-3이 연 관문은 `cam.locked`(확정 플래그)였고, 남아 있던 관문이 `standing`(f 필요 — `snapToAxis`의 3D 앵커)이다. 그래서 A-3 뒤에도 NONE·소실점 상태의 증상은 남아 있었다 — 이번이 그 두 번째 관문을 화면 작도로 우회한 것이다. ⑤ 표의 덮는 수([B-10]) — NONE·소실점 하나 행의 via 수 1 → **2** |
| D-L59 | ⛔ **D-L109(2026-08-20 18차)가 대체했다 — 아래는 폐기된 기전의 기록이다.** 새 절차에서 소실점은 다시 «깊이선 × 지평선»이고, 그 지평선은 **사용자가 맨 먼저 그은 선**이다(기본 위치가 깔려 있지 않다 — 이 항목이 되돌리려 한 그 조건이 없다). 대기 풀·`resolvePool`·`sepDeg` 짝짓기·이음 제외 반경이 전부 지워졌다. ⚠ 이 항목이 남긴 **대가의 실측**(축 오차 중앙 몰아 14.42° → 31.61°)은 그 기전의 것이고 현행 값이 아니다 — 새 절차의 값은 `rule_camera.json`을 그 자리에서 읽는다(#1·#47). ~~**첫 소실점 = 그린 두 깊이선의 실제 교점(`two_lines`) — 2차의 "지평선이 먼저"를 되돌린다**~~ | 2026-08-17 4차 지시 3. 증상: 서로 다른 소실점을 향한 대각선들에서 소실점이 (선 × 미리 깔린 지평선)으로 확정되니 사용자가 겨냥한 교점과 다른 자리에 서고, 격자가 그 벗어난 소실점 기준으로 생겨 **그린 대각선이 격자와 어긋난 채 남았다**. 지시: "소실점은 그린 선의 교점이어야 하고 그린 선은 안 움직여야 한다. 격자가 그린 선을 따라야지 반대가 아니다." 새 규칙: ① 첫 유한 수평 소실점이 없는 동안 깊이선은 `RuleState.depthLines`에 **대기**하고, 새 깊이선과 **유한한 교점**을 내는 짝이 생기면 그 교점이 소실점이다(짝 여럿이면 `sepDeg` 최대 — ⚠ 이것은 **같은 축 안에서만** 조건수 최선이다(항목 3 리뷰어 [5]): 대기 집합에 다른 축의 선이 섞이면 각차 최대는 **다른 축 짝**을 고르고 그 교점은 소실점이 아니다 — **다른 축 짝을 고를 위험과 맞바꾼 선택**이고 합성 잉크의 실측이 그 형태다(번갈아 긋기 first_pair_sep 39.85°·오차 27.85°). 새 임계 없음, #17). ② 그 순간 **지평선 = 소실점 y**(롤 0 유지 — 순서만 바뀐다, 지시 4-c 앞당김). ③ 두 번째 수평 소실점부터는 종전대로 지평선 × 선 하나(지시 4-d). ④ ⚠⚠ **끝점을 공유한 이음(ㄱ자·T자)의 교점은 소실점이 아니다** — 2D 오스냅(D-L57)으로 잇는 것이 기본 동작이라, 제외하지 않으면 **모든 꼭짓점이 소실점이 된다**(구현 중 실제로 걸렸다). 제외 반경은 `SNAP_TOL.merge_ratio` × 화면 대각(D-L57과 같은 값·같은 프레임, #17). ⚠ **그 반경(≈2.3px)은 오스냅 조리개(15px)보다 훨씬 작다**(항목 3 리뷰어 [7]) — 오스냅을 끄거나 조리개 밖(3~15px 어긋남)에서 이어 그은 ㄱ자는 제외 대상이 아니고 그 꼭짓점이 첫 소실점이 될 수 있다(AS-L1·D-L3의 "상자 꼭짓점이 교점 조건을 완벽 만족"과 같은 자리). 반경을 조리개로 넓히면 소실점 근처를 지나는 정상 교차까지 제외되므로 안 넓혔다(A-3 — 실획 K가 볼 자리). **대가**(2차가 이 설계를 버린 이유가 돌아온다): 나란한 짝의 교점은 조건수가 나쁘다 — 유한 판정(`isFiniteVp`)에 걸리면 대기가 이어지지만, 통과하는 어중간한 짝은 흔들린 소실점을 준다. 그리고 서로 다른 소실점을 의도한 두 대각선을 연속으로 그으면 그 교차가 첫 소실점이 된다(대각선 = 소실점 선언이 이 설계의 뜻이다 — 지시 4-b "대각선 두 개의 교점"). 잘못 서면 지우고 다시 긋는다(P1 불가역과 같은 태도). **P1의 `standing`에 깊이선 둘이 필요해졌다**(항목 3 리뷰어 [11] 정정 — 차수(확정)는 가로선으로 여전히 P1이고, 바뀐 것은 카메라가 서는 조건이다. D-L53의 구분 그대로) — basic_flow 종단이 그 계약으로 갱신됐다. 검증: `vp_rules.test.ts`(교점 정확·대기·이음 제외·나란한 짝 반례·**오차 0 반례 테스트**(지시 3-d) — 회귀 팔은 옛 소실점(선 ∩ 기본 지평선)에 대해 부적합도가 0이 아님을 확인) · `grid.test.ts`(격자 조각 전부가 소실점으로 모인다, 지시 3-c) · e2e `vp_two_lines.json`. ⚠⚠ **대가의 실측**(합성 하네스 재실행 — 수치를 쓰기 전에 원장을 그 자리에서 읽었다): `rule_camera.json` `headline_no_identity` 축 오차 중앙 **몰아 14.42° → 31.61° · 번갈아 9.82° → 27.85°**(둘 다 검출 9.17°보다 나빠졌고 rule≤detect 게이트는 계속 `passed:false`) · `axis_snap.json` 배치 1322 → **820** · `horizon_pitch.json` 참 지평선 팔 **3.39° → 31.61°**(아래 참조). `first_pair_sep_deg_median`이 처음 기록됐다: 몰아 **6.58°**(같은 축 짝 = 나란해서 조건수가 죽는다) · 번갈아 **39.85°**(각은 좋지만 **다른 축의 두 선**이 짝이 된다 — 교점이 소실점이 아니다). 즉 합성 잉크(짝을 의도하지 않는 겨냥 잡음)에서는 두 순서 다 나쁘다 — **2차가 이 설계를 버린 그 사유가 수치로 돌아왔다.** 그래도 지시대로 간다(D-L42·D-L56과 같은 구조): ① 지시가 실사용 감각이고 측정은 합성 잉크다(AS-C10) — **사람은 짝을 의도해 긋고**(X 교차·연장 수렴), 합성 하네스는 그 의도가 없다 ② **하네스는 스냅 전 원시 선을 먹인다** — 앱 경로는 2D 오스냅·직교·vp_dir로 스냅된 선을 먹이므로(D-L58 ③) 하네스 값은 앱 동작점 밖이다(#17의 갈림 — 스냅 반영 하네스는 미구현, DEFERRED) ③ 되돌림 길: 지우고 다시 긋기 + 항목 4의 점 찍기 확정 경로. ⚠ **`horizon_pitch`의 조작 변수가 죽었다**: 지평선이 소실점 확정에 안 쓰이므로 참·중앙·위약 세 팔이 **동일한 값**을 낸다(`deg_true = deg_center = deg_placebo = 31.61°`) — 그 하네스가 재던 기전(지평선 높이의 효과)이 제거됐다. AS-L19의 '살아 있는 절반'이 이것으로 폐기된다(assumptions 갱신·DEFERRED 등록) | ⚠ **[D-L63 주석]** 이 갈림은 5차 이월-2가 닫았다 — 스냅을 먹여도 값이 같거나 나쁘다(위약 대조 포함). '하네스 값은 앱 동작점 밖'이라는 유보는 더 못 쓴다.
| D-L60 | ⛔ **D-L109(2026-08-20 18차)가 대체했다.** 지평선은 다시 **결과가 아니라 첫 동작**이다. ⚠ 그러나 이 항목이 막으려 한 것(«미리 깔린 지평선»·빈 종이 감각)은 **되살아나지 않았다**: 기본 위치가 없고(`horizon: null`에서 시작한다) 사용자가 긋기 전에는 화면에 아무것도 없다. 점 찍기 경로(`pickVpAt`)와 그 정확도 물음은 함께 폐기됐다. ~~**지평선은 결과다 — 확정 전에는 화면에 없다**(2차 "지평선 고정 제약"의 되돌림)~~ | 2026-08-17 4차 지시 4. 옛 판은 지평선이 처음부터 화면에 있었고 그것이 첫 제약이었다 — 빈 종이 감각을 해쳤고, 소실점을 (선 × 미리 깔린 지평선)에 가두는 원인이었다(항목 3). **지평선은 전제가 아니라 결과다**: 유한 수평 소실점이 서는 순간 그 y가 지평선이고(D-L59 ② — 롤 0 유지, 순서만 뒤집힌다, 지시 4-c) 그때부터 그린다. 확정 경로 둘(지시 4-b): ① 대각선 두 개의 교점(D-L59) ② **대각선 하나 + 점 찍기**(`pickVpAt` 신설 — 대기 깊이선 위의 근처점 또는 대기 선끼리의 교차점을 톡 찍으면 그 자리가 소실점. 옛 누산기의 "점 찍기 = 소실점 확정" 제스처의 자유도 회계 그대로, 이론서 5.3). 찍기의 판정은 획 길이 ≤ `PICK_TOL` 반경(#17 — 새 임계 없음), 찍기 조리개는 `OSNAP.radiusPx`. **끝점 이음 교점도 찍으면 받는다** — 자동 짝(D-L59 ④)의 제외는 조용한 오확정 방지이고, 찍기는 명시적 확인이다(A-3). 두 번째 소실점이 지평선 위에 놓이는 것(지시 4-d)·소실점 후 잠금은 그대로다. ⚠ **두 경로의 정보량이 다르다**(리뷰어 [4·2]): 교점 경로는 두 선의 데이터가 자유도 둘을 다 정하지만, **찍기 경로의 근처점은 선 방향 위치(자유도 하나)를 사용자의 점이 임의로 정한다** — 그 위치가 지평선 높이를 굳히고 이후 모든 수평 소실점이 그 위에 놓인다(4-d). 원장 what_not_say에도 적었다. ⚠ **임계 프레임 병기**(리뷰어 [1·6], #24): 찍기 판정 `PICK_TOL.radius_ratio`(0.0145, **화면 대각 비** — 앱 캔버스 1447px에서 ≈21px)와 찍기 조리개 `OSNAP.radiusPx`(**절대 15px**)는 프레임이 다르다 — 판정(≈21px)이 조리개(15px)보다 넓어, 짧은 획은 넉넉히 '찍기'로 읽히되 확정은 15px 안의 후보에만 붙는다. ⚠ **되돌림**(리뷰어 [4]): 찍기 확정은 `실행취소`가 카메라(rules 스냅샷)까지 되돌린다(pickVp가 pushUndo를 쌓는다). **호스트 획을 지워도 소실점은 안 풀린다**(확정 후 잠금, §1) — 그 사실을 여기 명시한다. 겉보기 2D 교차(AS-L2·D-L57 위험)를 찍으면 그대로 첫 소실점이 된다 — 명시적 확인의 대가다. ⚠ **찍기 경로의 정확도를 재는 팔이 원장에 없다**(리뷰어 [6] — horizon_pitch 하네스의 재목적 트리거가 이 항목에서 발화했는데 처리하지 못했다. 하네스 개조는 별도 작업이라 DEFERRED에 재등록 — e2e 배선 확인뿐이다). ⚠ **지시 4-e(확정 후 지평선 끌기 = 피치 조작)는 미뤘다**(DEFERRED): 확정 후 지평선을 옮기면 소실점이 자기 지지선에서 떨어진다 — D-L32가 배치 2049→792로 무너진 그 자리이고, 옳은 구현은 소실점·지평선 동반 이동 + **전부 다시 풀기**(승격 규약)다. 그 재풀이의 배치 손실을 안 잰 채 넣으면 조용히 틀린 배치가 된다(A-3). 그 결과 D-L45의 끌기는 **당분간 휴면**이다(확정 전엔 지평선이 없고 확정 후엔 잠긴다 — D-L45에 표시함) |
| D-L61 | **관계 스냅(정렬 가이드) — 확정 전(2D 단계) 전용, 오스냅이 이긴다** | 2026-08-17 4차 지시 5. 사각형 도구가 없으므로 선 넷으로 사각형을 만든다 — 둘째 선의 끝을 첫 선의 끝과 같은 높이에 맞추는 정렬이 필요하다. 인디자인·일러스트레이터·피그마 관행(지시 명시): 다른 선의 끝점·중점과 화면상 같은 x/y에 오면 그 좌표로 끌리고 근원점에서 가이드가 뻗는다. **화면 좌표 기준·3D 무관**(5-a) — 카메라가 서면 "같은 x"는 축 평행선이 되어 축 스냅이 잇는다: 그래서 **확정 전 전용**으로 배선했다. 우선순위 **오스냅 > 방향(직교·소실점) > 정렬**(5-d)이고, 방향 스냅과는 **성분으로 결합**한다(직교 세로선이면 y만 · 가로선이면 x만 · 소실점 방향이면 그 직선 위에서 미끄러져 가이드와의 교점으로 — 방향을 안 깬다). 허용치는 `OSNAP.radiusPx` 재사용(#17 — 한 조리개 개념. 오스냅과 같은 반경이라 우선순위가 반경 경쟁이 안 된다). 후보는 끝점·정점·중점뿐(5-b — 교차점 자리는 오스냅의 몫). 토글 기본 켬(5-e — `정렬` 버튼·`S2S.setRelSnap`). **구현 정리**: onLive와 확정이 따로 들고 있던 2D 판정을 `resolve2d` 하나로 합쳤다(#17·§11 — 미리보기=확정이 구조가 됐다). ⚠ **정렬은 규칙의 입력을 옮긴다**(리뷰어 [B-1]): "3D 무관"은 좌표계 서술일 뿐이다 — 확정 경로가 정렬된 끝점을 `feedStroke`에 먹이므로(D-L58 ③과 같은 원리) 15px 정렬이 300px 획에서 최대 ≈2.9° 방향을 바꾸고, 그 선의 교점이 **불가역인 첫 소실점**이다. 설계 의도다(정렬된 선이 사용자가 뜻한 선이다) — 그 사실을 원장 what_not_say에도 적었다. ⚠ **프레임·선례**(리뷰어 [B-5][B-6], #24): 정렬 허용치는 **절대 15px**(오스냅 조리개 재사용 — 화면 크기·dpr에 따라 상대 크기가 달라진다. dpr에는 CSS px라 시각 크기 불변). 값의 출처는 **선례가 아니라 자기 오스냅 조리개**다 — 인디자인류의 실제 허용치(보통 4~8px)와 같은지 확인하지 않았다(기전만 선례, 값은 내부 일관성. 실획 K가 볼 자리). ⚠ **알려진 상호작용**: 오스냅 종류를 전부 꺼도 정렬(x·y 동시)이 끝점 근처에서 사실상 끝점 스냅처럼 작동한다 — 항목 1 e2e의 양성 채널 팔이 실제로 그렇게 깨져 두 토글을 함께 끄게 고쳤고 **snap2d_flow.json의 게이트 문구도 두 토글 조건으로 갱신했다**([B-7]). 원장: `rel_snap_flow.json`([B-10]) |
| D-L62 | **궤도 중심 = 3D 경계 상자의 중심, 갱신은 궤도 시작 시** · **선 굵기 1.5/1.3px** | 2026-08-17 4차 지시 7·8. **7**: 옛 판은 끝점 평균(무게중심)이라 짧은 선이 몰린 쪽으로 끌렸다 — 경계 상자 중심으로 바꿨다(지시 7-a. 선례 7-d: SketchUp 커서 아래 기하·라이노 선택/뷰 중심 — 단순한 쪽). **갱신 시점은 궤도를 시작하는 순간**(7-b) — 그리는 순간 target을 옮기면 `OrbitControls.update()`가 카메라를 재겨냥해 시점이 튄다(AS-C7류의 놀람). 핀 해제(unpin)와 이미 풀린 상태의 재시작(begin) 둘 다 같은 `orbitTarget()`을 부른다(#34). 3D가 없으면 궤도가 안 열리므로 7-c(화면 중앙 깊이 한 점)는 방어 기본값 [0,0,4]로만 있다(미측정 — 원장에 명시). 원장: `orbit_center.json`(target=bbox<1e-6 · 평균(옛 동작)과의 대조 >0.05가 회귀 판별). **8**: `LINE_PX` 2.2/1.4 → **1.5/1.3**(지시 — "1~1.5px 정도, 결과선·보조선 굵기 차이는 최소로. 색과 톤으로 갈리면 된다" — 위계는 `CHANNEL_3D` 색·불투명도가 든다). 표시 상수라 constants.ts 비등재(기존 사유 그대로). 기존 픽셀 판정(basic_flow ④ 차등 어두움)이 얇아진 선에서도 전량 통과 — **판정 여유는 임계 70까지 10이다**(60/70 — 이월-1 재검 [7]이 정정: 60은 측정값이지 여유가 아니다). 60의 전후 불변은 **지표 구성상 기대되는 무변화**다(7×7 창 최암 = 선 중심 픽셀 — 굵기에 둔감, 재검 [19]). 재실행이 실측이었다는 증거는 **굵기에 반응한 필드**다: `s3_depth.gl_painted_px` 2406 → 1849(−23%, `git diff 73488a3 2059e43`). ⚠ s5_orbit의 2279 → 2334 증가는 같은 커밋의 궤도 중심 변경(D-L62 본문)과 교란돼 굵기 몫을 못 가른다. 시각 확인은 dev 5222에서 HMR로 모듈 값 실측(1.5/1.3). ⚠ **LINE_PX는 픽셀 원장들의 입력이다**(리뷰어 [8], D-C4의 틈): ink_px·row_ink·guide_px·dark 판정이 굵기에 딸려 움직이는데 비등재 상수라 **STALE이 못 잡는다** — 값을 바꾸면 Playwright 전량 재실행으로 원장을 함께 갱신해야 한다(이번에 그렇게 했다). ⚠ 지시 6-c의 '현재 도구 표시'는 기존 `.on`(검정 반전) 그대로다(리뷰어 [13] — 손대지 않았다) | ⚠ **[D-L65 주석]** 이 결정의 구현(target = 상자 중심 그대로)은 궤도 시작 재조준을 만들었고 5차 지시 2가 개정했다 — 현행은 **상자 중심의 시선 투영**(D-L65)이고 orbit_center.json의 계약도 그렇게 바뀌었다.

### D-L63 — 합성 하네스는 앱의 확정 전 2D 판정을 통과한 선을 함께 잰다 (2026-08-17 5차 이월-2)

`mainL.resolve2d`(오스냅>방향(직교·vp_dir)>정렬)를 순수 모듈 `s3d/resolve2d.ts`의
`resolve2dCore`로 추출했다(#17 — 앱은 상태만 채우는 포장, 동작 불변). `rule_camera.test.ts`에
`rule_*_snap` 팔 셋을 추가해 같은 획을 그 함수에 통과시켜 먹인다. 앱과 같은 조건으로 2D 판정은
확정(order≥1) 전에만 돌고, 확정 후 규칙 입력은 앱에서도 원시 선이다.

**판정: D-L59의 회귀는 실재다** — 스냅 입력이 구제하지 않는다(`rule_camera.json@f351839a`). ⛔⛔ **제자리 정정(2026-08-18 8차 2차 지시 항목 0 · 8-2R [4])**: 이 판정이 딛고 선 값(스냅 팔이 전부 31.61~35°)은 **8차 항목 1 이전의 것**이다. 현행 짝지은 층에서는 **부호가 세 팔 중 둘에서 뒤집혔다**(번갈아 7.3342→7.7742 악화 · 몰아 7.8135→7.4653 개선 · wide_pair 8.7129→8.0003 개선). **이 판정은 다시 열려 있다**(AS-L24 참조). ⚠ **[6차 주석]** 이 줄이 인용한 수치는 **6차 지시 11**(소실점은 **세 번째 선**이 정한다) **이전 실행**의 것이다 — 원장이 다시 움직였다(현행 값은 원장을 그 자리에서 읽는다, #1).
⚠ 리뷰어 6-R′(높음 4)에 답해 **위약·짝지은 부분집합·jit_0 층·조리개 스윕**을 추가했다:

| 팔(headline_no_identity) | 원시 | 스냅 | **위약**(같은 크기 임의 방향, #39) |
|---|---|---|---|
| 번갈아 | 27.85° | 29.66° | **26.76°** |
| 몰아 | 31.61° | 35.00° | **31.61°** |
| wide_pair | 34.64° | 35.00° | 32.86° |

⚠ **[지시 3 주석 — 재검 [3-1]]** 위 표는 **물음 오라클 판**(이월-2 시점 — 원장은 `git show d724ac0:stage0/out/rule_camera.json`, 8-R′가 병기)의 값이다. 항목 3이
물음을 무물음 선언으로 바꾼 재실행에서는 스냅 팔이 전부 35.00°(번갈아 격차 7.15°로 확대),
짝지은 번갈아 27.82→35.00(축 분모 211→128), jit_0 번갈아 스냅 36.35, 조리개 스윕은 세 동작점
동일(중앙값 둔감 — "비단조" 철회. ⚠ 8-R′ 재검 [M7]: 셋 다 35 = 오배정 고원의 서명 값이라
'둔감'이 아니라 **고원 포화**일 수 있다 — 안 갈랐다)이다. 방향은 같고 강도는 세졌다 — 현행 원장을 읽는다.

⚠⚠ **[7차 지시 1-a 정정 — 7-R 리뷰어 [9]]** 그 아래에 있던 "무물음 선언 판" 수치가 **재실행으로
낡았다**(상수 해시가 안 움직여 STALE이 안 뜬 자리 — PITFALLS 머리말 1 "알고리즘 변경은 사람이 본다").
**판이 셋이다**: ① 물음 오라클 `git show d724ac0:` ② 무물음 선언 `git show b52bc7c:`
③ **현행 — 강제 없음**(D-L70, 이 커밋). 아래 문단의 수치는 전부 **② 무물음 선언 판**의 것이고
그렇게 표시한다. **③의 값은 `stage0/out/rule_camera.json`을 그 자리에서 읽는다** —
`headline_no_identity`: 번갈아(rule_drawn) 원시 31.6083 · 스냅 **31.6083**(격차 0) ·
몰아(rule_grouped) 원시 35 · 스냅 **35** · 검출 **9.1742**. 짝지은 부분집합:
번갈아 31.6083→31.6083(축 분모 237→225) · 몰아 33.3331→35(216→205). jit_0 번갈아 스냅 **36.3492**.
화면 축 슬롯 번갈아 284→**296**. **즉 ③에서는 원시↔스냅 격차가 거의 사라졌다** —
격차의 출처가 스냅 좌표가 아니라 **강제 선언**이었다는 뜻이고, 그것이 D-L70의 근거와 같은 방향이다.

⛔⛔ **판이 넷이 됐다 — 위 ③의 수치는 낡았다**(2026-08-18 8차 2차 지시 항목 0 · #42 ⑥ ·
리뷰어 [14]). **④ P1 잠김을 연 판**(8차 항목 1, `d233ec4`)이 이 원장을 크게 움직였는데
어느 문서도 안 적었다. `rule_camera.json@f351839a`을 그 자리에서 읽은 **현행(④)**:
`headline_no_identity` 번갈아 원시 **7.3722** · 스냅 **7.7742** · 몰아 원시 **7.9567** ·
스냅 **7.4653** · 검출 **9.1742**(검출만 안 움직였다 — 규칙 경로를 안 지나므로 당연하다).
짝지은 부분집합: 번갈아 **7.3342→7.7742**(축 분모 340→338) · 몰아 **7.8135→7.4653**(328→342).
jit_0 번갈아 스냅 **3.7989**. 화면 축 슬롯 번갈아 208→**221**.
👉 **③에서 ④로 오며 표제 축 오차가 약 4배 줄었고**(31.6083 → 7.9567) 그 결과
**이 하네스의 등록 게이트가 실패에서 통과로 뒤집혔다**(`passed`·`passed_snapped` 둘 다
`false` → **`true`**, 규칙 몰아 7.9567 대 검출 9.1742). ⚠ 그 게이트는 **`rule_camera`가
스스로 등록한 것**이고 CLAUDE.md §2의 중단 조건(`camera_gate.json`)이 아니다(#41의 구분).

⚠⚠⚠ **같은 개입의 대가를 함께 적는다**(8-2R [1] · 8-2R′ [12] — 이 자리에 안 적혀 있었다):
같은 커밋에서 `order_lock`의 **절단 0.2 안에 든 배치가 176 → 46** · `camera_ok`가
**583/600 → 460/600** · 조용히 틀림 비율이 **0.9558 → 0.9848**로 **나빠졌다**(#15·#16).
그리고 **게이트 여유는 1.2175°**인데 같은 원장이 "1~3° 급 격차는 강도가 약하다"고 스스로 적고
**시드 폭은 미측정**이다(#14). **다른 규칙 팔은 여유가 0.1164°(`rule_wide_pair`)이거나
음이다(`rule_grouped_snap_r40` 10.557 > 검출 9.1742).** 그리고 **위약 팔
`rule_grouped_placebo` 7.116이 판정 팔보다 좋다**(#39). **통과를 개선의 근거로 쓰지 않는다.**

⛔⛔ **제자리 정정(2026-08-18 9차 항목 0 · D-L80 · #42 ⑥)** — 위 두 문장의 수치가 낡았다.
`resolve2dCore`에 판정 뒤집기 가드가 들어가 이 원장을 다시 돌렸고 **스냅·위약 팔만 움직였다**
(원시 팔과 검출은 불변 — 그 경로를 안 지난다). 현행(`rule_camera.json`을 그 자리에서 읽었다):
`rule_grouped_snap_r40` **10.557 → 6.9637**(검출 9.1742보다 **좋다**) ·
`rule_grouped_snap` **7.4653 → 6.9314** · `rule_grouped_placebo` **7.116 → 7.1615** ·
`rule_drawn_snap` **7.7742 → 8.33**(나빠졌다) · `rule_wide_pair_snap` **8.0003 → 7.5135**.
👉 **위약 대조가 뒤집혔다** — 이제 판정 팔(6.9314)이 위약(7.1615)을 **이긴다**.
⚠ 위약이 **원시 몰아**(7.9567, 불변)보다 좋은 것은 **그대로**이므로 #39 우려는 절반만 풀렸다.
⚠ **"통과를 개선의 근거로 쓰지 않는다"는 그대로 유지한다** — 게이트는 여전히 이 하네스가
스스로 등록한 것이고 CLAUDE.md §2의 중단 조건이 아니다(#41).

**아래는 ② 무물음 선언 판(`b52bc7c`)의 값이다 — 현행이 아니다.**
위약이 원시와 같으므로 나빠짐은 **이동 크기가 아니라 스냅 방향의 체계 효과**다([20] 답).
짝지은 부분집합(둘 다 카메라 선 픽스처만, [23] 답): 몰아 31.61→35.00 · 번갈아 26.67→27.55 —
분모 유입으로 부호가 안 뒤집힌다. 기전 분해([22] 답): 몰아에선 첫 짝에 스냅이 39/384만 닿아
(스냅 발동은 지지·축 선언 쪽) 스냅 팔 ≈ 원시이고, 번갈아는 133/384 닿고도 개선이 없다.
직교 편평화([23]② 답): 화면 축 슬롯 번갈아 149→205(물음 오라클 판 — 현행 무물음 선언 판은 **149→294**, 8-R′ 정정. 위약 158) — 선택 효과가 있으나 짝지은
표에서도 방향이 같다. 조리개 스윕(#12, [25] 답): 4px 35.00 · 15px 35.00 · 40px 31.61 — 비단조
(⚠ [지시 3 주석 · 8-R′ 재검 [L3]] 물음 오라클 판의 값 — 현행 무물음 선언 판은 35/35/35이고
"비단조"는 철회됐다, 위 [3-1] 주석).
jit_0 층([21] 답): '항등' 사유가 틀렸음을 등록문에 정정(등급 잉크 잡음이 남는다) — 그 층에선
스냅이 더 크게 나쁘다(번갈아 24.78→35.00).

⚠ **한계**: 값들이 오배정 고원(31.61·35 — DEFERRED 315의 서명) 사이를 오가고 **시드 폭
미확인**(#14)이라 1~3° 급 격차는 강도가 약하다. 확실한 것은 "스냅이 구제한다"의 **부재**다.
합성 잉크는 짝을 의도하지 않는다(AS-L13) — 실획(K)이 최종 판정자다.
원 게이트(passed)는 유지하고 `passed_snapped`를 추가 등록했다(#28 — 사유는 등록문. 몰아 팔
고정은 원 판정과 같은 팔이라는 연속성뿐이다, [36] 답 — 세 팔 중앙을 전부 적는다).
오스냅 조리개 15px(`OSNAP_RADIUS_PX`)는 전역 해시 밖이다(D-L51과 같은 자리 — 원장 how에 명시).

되돌림 길: 스냅 팔이 앱 동작을 잘못 근사한다는 증거가 나오면(§4.5 보장 위반, 3D 오스냅의
2D 근사 실패) 그 팔을 빼고 D-L59의 원시 팔 기록으로 돌아간다. 실획(K)이 최종 판정자다.

### D-L64 — 카메라 확정 전 획은 언제나 확정 뷰 소속이다 (2026-08-17 5차 항목 1)

`viewForDrawing()`이 확정 전 비핀 자유 자세로 **가짜 뷰를 만들어** 확정 전 획을 전부 거기
넣었고, 그래서 ① `standCamera`의 일괄 풀이가 빈 목록을 받았고(lifted 0 — 올라간 것은 확정
획 시작점이 지평선 아래일 때의 지면 스냅 연쇄뿐) ② 확정 순간 `viewIsCurrent()`가 거짓이 되어
**그린 획이 화면에서 통째로 사라졌다**(5차 항목 1의 보고 "화면 밖으로 나간다"의 실체).

결정: **확정 전에는 `viewForDrawing()`이 언제나 확정 뷰를 반환한다** — 그리는 화면이 곧
확정 카메라의 화면이 된다(§4.5 무변화 전환). `standCamera`는 `views[0]` 대신 `confirmView()`.
자유 자세 뷰 생성은 **확정 후 궤도 시점에서 그릴 때**(L-B.8의 원 목적)로 좁혀진다.

회귀 팔: `p1_invariance.json` — 눈높이 위 상자(지면 스냅 생존 경로 차단 구도)에서 확정 직전
·직후 중점 표시 대조 + lifted 3 + 재투영(§4.5). 버그 되살림 확인 완료(A-4).
⚠ 남긴 것: 옛 저장본에 가짜 뷰 소속 대기 획이 있으면 이 수리로도 확정 뷰로 안 옮겨진다 —
그런 문서는 그 뷰를 열어 그리면 되고, 마이그레이션은 실사용 보고가 있을 때 한다.

### D-L65 — 궤도 중심은 경계 상자 중심의 **시선 투영**이다 (2026-08-17 5차 지시 2, D-L62 개정)

D-L62(궤도 중심 = 경계 상자 중심)의 구현이 target을 그 점 **그대로** 놓아
`OrbitControls.update()`가 카메라를 재조준했고, 궤도를 시작하는 순간 그리던 뷰에서 튀었다.
사람 지시 2-a: **중심점만 바뀌고 카메라 위치·방향은 그대로여야 한다.**

결정: `stage.retarget(center)` — 중심을 **현재 시선에 수직 투영**한 점을 target으로 놓는다
(투영이 시선 뒤면 지금 거리 유지). target이 시선 위에 있으면 update()가 항등이라 첫 프레임이
안 튄다 — `setPose`가 §9.2 왕복에서 쓰던 그 규약을 한 함수로 빼서 unpin·begin이 함께 쓴다(#17).
회전 반지름(중심 깊이)은 경계 상자가 정하고, 시선은 사용자가 정한다 — 두 정보가 갈라졌다.

회귀 팔: `orbit_begin_invariance.json`(터치 다운 화면 불변 + begin 실개방 후 시선 변화 <1.5° —
옛 동작 9.78° — 원장 밖 값(#25, 되살림은 임시 판이라 재현 불가)으로 실패 확인. 본판 실측 시선 변화 0.241°) · `orbit_center.json` 개정판(target = 투영점 <1e-6 · 평균 투영과 판별).
⚠ 옛 등록(target = 상자 중심 그대로)의 갱신 사유는 사람 지시다(#28) — registered에 병기.

## 2026-08-18 6차 지시 (D-L66 ~ D-L69)

| # | 결정 | 근거 |
|---|---|---|
| **D-L66** | **`pose === null`은 확정 뷰의 정의다 — 결함이 아니다.** 6차 지시 1-a·1-c(확정 뷰에 자세를 저장하고, `null`이면 결함으로 기록한다)를 **따르지 않는다.** 대신 술어를 `doc.ts`의 `confirmViewOf`·`isConfirmView` 하나로 모으고(#17) 불변을 시험으로 잠근다 | 확정 카메라에 물려 있으면 `stage.pinTo`가 카메라를 원점·항등에 놓고 `stage.pose()`가 `null`을 낸다 — **자세가 항등이라 저장할 것이 없다.** 채우면 오히려 확정 뷰를 못 찾는다: 뷰 전환·일괄 풀이(`standCamera`)·저장 복원 셋이 그 술어로 찾고, `viewIsCurrent()`가 거짓이 되어 **2D 대기층이 안 그려진다**(5차 항목 1이 실측한 그 증상). 반례 팔이 원장에 있다(`confirm_rules.json@f351839a`의 `confirm_view.counter_example.is_confirm_after = false`). 확정 카메라의 실제 정보는 저장본의 `rules`·`cam`에 이미 있다. ⚠ 보고된 세 증상의 원인은 따로다 — 그리드 어긋남은 **그리드 원점이 임의**(`grid.ts`의 `groundGrid`), 회전 튐은 **D-L65에서 이미 고쳤다**, 그릴 때마다 다른 곳은 **`on_face` 임의 깊이**(항목 4) |
| **D-L67** | **끝점 겨냥 거리를 남긴다** — `SStroke.snapEndDistPx`, 조리개 밖까지 보는 40px 프로브(`SNAP_PROBE_PX`). 질의와 프로브가 **같은 종류 필터**(`endSnapKindOk`)를 지난다 | 실획 첫 표본에서 `snapEnd`가 전부 `null`이었는데 그것만으로는 **반경이 좁은가 · 후보가 없는가 · 판정이 안 도는가**가 안 갈린다(지시 2-b). 프로브 값이 있으면 ①, `null`이면 ②다. `on_face`를 빼는 이유는 그 화면 거리가 **정의상 0**이라 앞을 막으면 실패 사건 자체가 안 생기기 때문이다(#3·#5) — **`snapDistPx`가 전부 0이었던 것이 그 기전이다.** 원장 `confirm_rules.json@f351839a`의 `end_probe.verdict_counts`가 **붙었다 2 · 반경 2 · 후보 없음 2**를 낸다(#38). ⚠ **프로브가 가르는 것은 둘이다**(6-R 재검): ③(판정이 안 돎)도 출력이 `null`이라 ②와 안 갈리므로 `end_probe.probe_ran`(질의 6 · 대상 1 · 정적 후보 3)이 그것을 배제한다(#32). 그리고 2·2·2는 동작점 여섯을 조리개 15 / 프로브 40 양쪽에 둘씩 놓은 **구성의 배분**이다 ⚠ 구조적 원인(`on_face`가 언제나 성공한다)은 **항목 4-5가 지운다** |
| **D-L68** | **2점 확정에 화각 게이트를 건다**(`vpRules.fovGate`, 이론서 18.4·6.2). **f² ≤ 0이면 거부**하고, **`f/W < 0.87`**이면 **경고하되 확정한다**. 화면 밖 소실점은 **거르지 않는다** | 두 소실점 사이가 짧으면 f가 작아지고(P가 사이에 있을 때 최대 f = d/2) 화각이 서기 어려운 값이 된다. 막을 것은 **서 있을 수 없는 카메라**(f² ≤ 0) 하나뿐이다 — 넓은 화각은 이상하지만 존재하는 시점이고 이 도구는 투시를 아는 사용자를 전제한다(CLAUDE.md). **화면 밖은 정상이다**: `VP_INFINITE_RATIO = 50 × 대각`이라 클램프가 없고(1180 폭에서 문턱 67,800px) 건축 투시도는 폭의 두세 배가 보통이다. 실획 보고 값 d = 1133px · **f = 474.16 = 0.402W · 화각 102.43° · `severe`**. ⚠⚠ **f에 `− h²`가 빠져 있었다**(6-R 재검 — 이론서 6.2는 **주점도 지평선 위**를 전제한다. 일반형 `(V₁−P)·(V₂−P) = −f²`에서 롤 0이면 `f² = −Δx₁Δx₂ − h²`이고 이 행의 h = 177px이다). 빼먹으면 f가 6.7% 과대평가되고 **거부 조건이 좁아진다**. ⛔⛔ **제자리 정정(2026-08-19 14차 항목 2 · D-L95)**: 이 `− h²` 항은 **뒤집혔다** — 2점은 피치 0이라 주점이 지평선 위에 있고(주점 y = 지평선 — 앱 카메라 `ConstraintAccumulator.solve`가 이미 그 규약이었다) h는 0이다. h² 벌점은 "지평선이 화면 중앙에서 멀면 거부"라는 가짜 제약을 만들었다(지평선 높이는 시선 높이 — 구도 선택이다). 이 행의 f = 474.16·102.43°도 그 규약의 값이고 현행은 506.11·98.75°(confirm_rules를 그 자리에서 읽는다). ⚠⚠ **초판은 0.5·0.87을 소실점 사이 거리 d에 걸었고 6차 리뷰어가 잡았다** — 이론서 18.4의 그 값은 **시거리** 기준이고(`2·atan((W/2)/f)`가 90°·60°가 되는 자리), 주점 정중앙에서 `f = d/2`이므로 d에 걸면 실효 임계가 **정확히 두 배 느슨**해진다(초판 쓸기의 `d/W = 0.87` 행이 화각 97.95°인데 `ok`였다). f는 `|PV₁||PV₂|`라 **비대칭도 담는다**(입력은 **지시문이 보고한 수**이고 파일 측정이 아니다 — 파일이 저장소에 없다). ⚠⚠ **이 게이트가 저장소 안의 잘못된 픽스처를 잡았다**: `horizon_drag.test.ts`의 V1(520)·V2(1250)이 **둘 다 주점(480) 오른쪽**이라 대응 카메라가 없었다 |
| **D-L69** | **소실점은 세 번째 선이 정한다**(`vpRules.resolvePool`). 대각선 둘까지는 **대기**하고, 셋이 한 점으로 모이면 그 점이 소실점, 갈리면 **2점이 한 번에** 선다. 화면 가로축이 이미 선 P1에서만 둘로 충분하다 | **다른 축의 두 선이 만나는 것은 공간의 한 점이지 소실점이 아니다**(6차 지시 11). 옛 규칙(4차 지시 3)은 그 교점을 곧바로 받아 **2점을 그리려던 것을 1점으로 굳혔고**, P1은 불가역이라(지시 1) 그림 전체가 갇혔다. 원장 `confirm_rules.json@f351839a`(겨냥 잡음 1.7°): 지표는 **위치가 아니라 축 묶음**이다 — 잡음에서 위치 창은 교점의 **조건수**를 재지 규칙을 안 잰다(두 규칙 다 절대 40px 창에서 116~120/120 '틀림'). 옛 규칙 묶음 틀림 **120/120** · 새 규칙 **7/120** · **대기 120/120** · 2점이 한 번에 선 구도 113/120. **대조 팔**(같은 축 둘이 먼저 오는 120구도)에서 옛 규칙은 **0/120** — 그래서 120/120이 픽스처의 재진술이 아니다(6차 리뷰어 [3·1]). ⚠⚠ **대가**: 그 대조 팔에서 **새 규칙이 24/120 틀린다**. 바꾼 이유는 옛 규칙의 실패가 **불가역**(2점 → 1점 고착)이고 새 규칙의 실패는 **다음 획이 고칠 수 있는** 종류이기 때문이다. ⚠ **등록 지표를 위치(40px)에서 묶음으로 바꿨다**(#26·#24 — 낮춘 것이 아니라 **같은 양이 아니게 된 것**이다. 옛 창의 값도 원장의 `absolute_40px_window`에 남긴다). 남는 몫은 세 선이 두 축으로 갈리는 구도의 **본래적 모호성**이다. "모인다"의 판정은 **각**(`convergeDeg`, `RULE_TOL.concurrent_deg = 3°` — 실획 보고 Δ0.0~1.7°의 약 두 배, **동작점 하나** #12)이고 **교점이 두 선의 연장 위**에 있어야 한다(`beyondSegment` — 그린 구간 안의 교차는 모서리다. 무잡음 판에서 묶음 틀림 15 → 3. ⚠ **위약 팔을 안 돌렸다**(#39 — `DEFERRED`)). ⚠ 대가: 대기가 길어져 합성에서 화면 가로선이 먼저 선언되고 `order_median` 2 → 1 · `deg_median` 17.58 → **31.61**이 됐다(`rule_camera.json@f351839a`). **지시 11-1 다섯째 줄이 그 동작을 명시한다**(수평선 → 1점 확정) — 설계대로이고 판정자는 실획이다 |

## 2026-08-18 7차 지시 (D-L70 ~ )

| # | 결정 | 근거 |
|---|---|---|
| **D-L70** | **스냅은 좌표만 옮기고 선언은 규칙이 한다** — `mainL.onStrokeEnd`의 `snapForced`를 지웠다(5차 지시 3의 "스냅이 곧 선언이다"를 되돌린다). 하네스(`rule_camera`의 `forcedBySnap`)도 같이 지웠다(#17) | 그 우회가 `stepRule`의 **P1 가드**(소실점이 하나라도 서 있으면 화면 수평선을 묻는다)를 건너뛰었고, P1은 불가역이라(D-L53) **한 획이 그림 전체를 1점에 가둔다**(7차 지시 1-a). D-L69 ②가 **끝점 오스냅 가지에서만** 이 우회를 뺐고(`resolve2dCore`의 `ortho: null`) 주 경로에는 살아 있었다. ⚠⚠ **대가와 한계를 함께 적는다**: ⚠⚠ **[7차 항목 3 주석] 아래 수치는 항목 1 시점(커밋 `a62d10b`)의 원장이다** — 항목 2(각차 기준, `72e1951`)와 항목 3(3점 경로 삭제, `9bbceb7`)이 같은 원장을 재실행해 값이 움직였다(현행: P1 521 → **497** · P3 0 → 0 · `camera_ok` 590 → 583 · `placement` 4001 → 3979 · `first_declaration.screen_h` **495** · 형태 오차 1.3329 / 1.3511 / **0.2452**). **현행 값은 `stage0/out/order_lock.json`을 그 자리에서 읽는다**(#1 — 상수 해시가 안 움직여 STALE이 안 뜬다). `order_lock.json`에서 P1 519 → **499** · P3 42 → **50**으로 **거의 안 풀린다**. ⚠ **그 20의 내역에 손실이 섞여 있다**(7-R 리뷰어 [4] · #15·#16): P2 +7 · P3 +8 · **P0 0 → 5**(카메라가 아예 안 선다)이고 `camera_ok` 563 → **555** · `placement` 3935 → **3910**이다. P1 개수만 세면 그 손실이 안 보인다. 시드 여섯 전부에서 줄었으므로 **방향은 시드 폭 안이 아니다**(#14, `by_seed`). 주 원인은 따로였고 카운터가 그것을 짚는다 — `first_declaration.screen_h`가 **497/600**(잡음 0 층 **120/120**)이고 **우회를 지워도 안 움직인다**. 즉 P1 잠김은 `classifyLine`이 화면 수평 4° 안 깊이 모서리를 `screen_h`로 읽고 소실점이 없을 때 묻지 않고 받는 것에서 온다 → **항목 3의 대상**(지시 3-a·3-b). ⚠⚠ **요인 분해가 그 방향을 지지한다**: 축 라벨만 참으로 바꾸면 형태 오차가 안 움직이고(1.3876 → 1.4091) **소실점만 참으로 바꾸면 크게 움직인다**(1.3876 → **0.2466**) — 틀린 것은 축 배정이 아니라 **카메라**다(`order_lock.factor_split`). ⛔⛔ **제자리 정정(2026-08-18 8차 2차 지시 항목 0 · 8-2R [3] · #42 ⑥)**: 그 세 수는 **현행 원장에 없다**(8차 항목 1이 다시 돌렸다). **중앙값으로는 방향이 뒤집혔다** — `fixed` **0.5731** · 축 라벨만 참 **0.8148**(나빠진다) · 소실점만 참 **0.4543**. **결론은 유지되지만 지표가 바뀐다**: 중앙은 팔마다 **놓인 수가 달라**(4743 대 2524, 분모 7200) 비교 불가이고(#9·#15), 분모가 모든 팔에서 같은 `within_cut_0_2`로 읽으면 **fixed 46 · 축 라벨만 참 13 · 소실점만 참 908 · 오라클 4004**로 **소실점 쪽이 20배**다 — 그 지표에서 결론이 오히려 더 강하다. `factor_split.note`도 그렇게 고쳤다. ⚠ 5차 지시 3의 원 근거(스냅이 의도의 선언이다)는 여전히 유효하나, **그 선언이 불가역 전이를 조용히 통과시키는 자리**에서는 물음이 이긴다(A-3: 애매하면 놓지 않는다) |
| **D-L71** | **확정된 방향을 `pts2d`에 되쓴다** — `placeLive`가 놓인 3D 세그먼트를 되쏘아 양 끝으로 적고, `onStrokeEnd`는 카메라가 서 있으면 **먼저 놓고 그 다음에 규칙에 넣는다**(`placeStroke` 분리) | 옛 확정 경로는 `frame() ? raw : resolve2d(raw)`라 **카메라가 선 뒤에는 원시 커서 궤적**이 문서에 들어갔다. 3D는 축으로 놓이는데 문서의 2D 선은 안 놓인 방향이었고, 규칙(`feedStroke`)도 그 원시 방향을 받았다 — 사용자가 본 "미리보기는 스냅되고 확정은 안 된다"(7차 지시 1-c)가 이것이다. **새 규약이 아니다**: `applySnapToStart`·`applySnapToEnd`가 이미 `pts2d` 양 끝에 스냅 결과를 적고 있고(#17), 되쏘기 항등은 `lift.ts`의 `segGap = 0` 보장이다(#5). 회귀 팔 `confirm_dir.json` — **일부러 5° 튼** 겨냥에서 확정 `pts2d`가 소실점을 지나고(각차 < 0.01°) 미리보기와 각차 < 0.01°. **되살림**: 되쓰기를 지우면 `to_vp_deg = 4.5246°`로 실패한다(#25 — 값은 원장 밖이다). ⚠ `pts2d`가 **그 카메라의 화면 좌표가 된다**는 성질이 세진다(applySnapToStart의 기존 경고와 같은 자리) — 차수 승격의 재풀이는 `snapStart.ofId`를 새 카메라로 다시 풀어야 한다(L-C·`DEFERRED`) |
| **D-L72** | **"어느 둘이 같은 축인가"의 기준을 `far`(거리 최대)에서 **각차 최소**로 되돌린다**(`resolvePool` ④) — 7차 지시 2-2가 명시한 양 그대로 | 6차가 그 대체의 근거로 인용한 **"합성 400구도에서 각차 399 · 거리 400"이 어느 원장에도 없었다**(7-R 리뷰어 [①-a] · #25 — 원장 밖 측정). 같은 하네스에 두 선택자를 나란히 구현해 실제로 돌리니 **각차 0/240 · 거리 14/240**(`confirm_rules.json@f351839a`의 `angle_vs_far`, 잡음 1.7°·시드 20260818 — ⚠ **해시를 붙였다**(8차 리뷰어 [6]): 해시 없이 적으면 그 줄은 값 대조의 **모집단 밖**이라 `scan_cited_values`가 아예 안 본다. 6차의 "각차 399 · 거리 400"이 못 잡힌 이유가 그것이다)으로 **지시문 쪽이었다**(A-3: 측정을 따른다). 기전: `far`는 각차뿐 아니라 **두 선 사이의 간격**에도 비례하므로(간격 ÷ sinΔθ) 같은 각차라도 멀리 떨어져 그은 짝이 이긴다(#24 — 단위를 바꾸는 것과 판정을 바꾸는 것은 다르다). "소실점은 멀다"(6차 지시 11 머리말)는 성질은 `beyondSegment`·`isFiniteVp`가 이미 지킨다. ⚠⚠ **대가와 한계**: 이 정렬 변경은 그 픽스처에서 **아무것도 안 바꿨다** — 두 정렬 모두 구현 틀림이 7/120 · 대조 24/120으로 같다(되돌려 직접 확인). 격차의 출처는 ④가 아니라 **후보 문**이고 카운터가 그것을 짚는다(`true_pair_gate`: 참 짝이 `beyondSegment`에 **4/120** 걸리고 116 살아남는다 — 나머지 3은 살아남은 후보 중 오선택). 문을 무르는 것은 AS-L28(무잡음 15 → 3)을 되돌리는 일이라 안 했다(`DEFERRED`). ⚠ 등록 게이트는 여전히 `passed_correct: false`이고 **기준을 낮추지 않았다**(#26) |
| **D-L73** | **두 번째 축이 화각 게이트에 걸리면 알린다** — 옛 판은 그 축을 버리고 **조용히 1점**으로 남겼다 | 사용자는 2점을 그렸는데 1점이 된 것을 모른다(A-3: 조용히 틀린 배치를 만들지 않는다). 판정을 `vp_fixed` 사건에 실어 보내 `feedStroke`의 **기존 화각 알림 경로**가 그대로 낸다(#17: 새 경로를 만들지 않는다). ⚠ 이 알림에 시험이 없다 — 발화 조건(f² ≤ 0인 두 번째 축)을 합성으로 만드는 픽스처가 아직 없다 |
| **D-L74** | **3점은 시점의 성질이지 그리는 선의 종류가 아니다** — "수직축입니까" 물음(`second_horizontal_or_vertical`)·`forced === "vertical"`·`deriveVertical`·`tiltedVerticals`·`RuleState.verticalLines`를 전부 지웠다. **기울어진 선은 항상 깊이선이다** | 2026-08-18 7차 지시 3-a·3-b·3-c. 3점 투시는 2점이 서 있는 상태에서 **카메라를 기울여야** 나오고, 기울인 시점에서는 `viewCamera.viewPlaceCtx`가 세 축 방향을 전부 들고 있으므로 **축 스냅의 후보가 하나 늘 뿐 기전이 같다**. 그러면 도구가 사용자에게 물을 것이 없다. **세 세대가 이 자리에 겹쳐 있었다**: ① 수심 유도(D-L32·D-L43 — 지평선 높이가 곧 3점 여부가 되어 도구가 피치를 정했다) ② 사용자 선언(A-4 — 물음이 생겼다) ③ 없음(지금). **측정**(`order_lock.json`·`rule_camera.json` 재실행): 물음 합 1381 → **1319**(−4.5%) · `asks_per_run` 2.552 → 2.411 / 2.714 → 2.531 · 차수 P3 50 → **0** · **`camera_ok_rate` 0.9167·0.9115 → 1.000**(옛 판에서 카메라가 안 서던 실행의 상당수가 수직축 답 뒤 3점 카메라가 예각 조건(6.5)을 못 넘어 막힌 것이었다) · 배치 3911 → 3979 · 형태 오차 중앙 1.3892 → **1.3329**. ⚠ **지시 3-f의 "asked가 0에 가까워진다"에는 못 미친다** — 남는 물음이 전부 `screen_or_depth`(4~8° 애매 구간)이고 그것은 3점과 무관하다. 합성 잉크가 그 구간에 자주 떨어진다(AS-L25) — **판정자는 실획이다**. ⚠ **P1 잠김은 안 풀렸다**(`first_declaration.screen_h` 495/600) — 원인이 다르고 `DEFERRED`에 있다. ⚠⚠ **`camera_ok_rate 1.000`의 모집단은 `headline_no_identity` 192실행(1pt 구도 제외)이다**(7-R 리뷰어 [3/4-A]) — 같은 원장의 `headline.rule_drawn`은 **0.9792**이고 `by_composition`의 1pt 구도는 **0.9167**이다. `order_lock`에서도 카메라가 안 서는 실행이 **7**이다. '전부 2점으로 선다'는 과대 주장이고 그렇게 고쳤다. ⚠ **그 1.000은 위약 팔까지 포함해 열한 팔 전부에서 정확히 1이다**(#40 ② — 그 자리의 1은 대개 측정이 아니라 구성의 귀결이다). ⛔⛔ **제자리 정정(2026-08-18 8차 2차 지시 항목 0 · 8-2R [5] · #42 ⑥)**: **더 이상 열한 팔이 아니다.** 8차 항목 1 이후 `camera_ok_rate`가 정확히 1인 팔은 **열둘 중 넷**이다(`rule_drawn`·`rule_grouped`·`rule_wide_pair`·`rule_wide_pair_placebo`). 나머지는 0.8698~0.9948로 갈린다 — **그 지표는 이제 개입을 구분한다.** ⚠ **예각 조건(6.5)이 막았다는 기전은 세지 않았다**(7-R [3-C] · #7) — 카운터가 없다. ⚠ **`rule_camera`의 등록 게이트는 여전히 `passed: false`·`passed_snapped: false`다**(#41). ⛔⛔ **제자리 정정(2026-08-18 8차 2차 지시 항목 0 · 8-2R [2])**: 8차 항목 1이 둘 다 **`true`로 뒤집었다**(규칙 몰아 7.9567 대 검출 9.1742). ⚠⚠ **여유는 1.2175°이고 시드 폭은 미측정이다** — 같은 원장이 "값들이 오배정 고원 사이를 오가고 시드 폭 미확인이라 **1~3° 급 격차는 강도가 약하다**"라고 스스로 적는다. **그 대역 안에서 뒤집혔으므로 통과를 개선의 근거로 쓰지 않는다.** ⚠ **`asks_per_run` 2.411 / 2.531은 `*_snap` 팔이다**(7-R [1-D]) — 원시 팔은 `rule_drawn` **2.625** · `rule_grouped` **2.589**다. 두 열을 같은 팔로 적었던 것을 고쳤다. ⚠ **지시 3-a는 문자 그대로 이행하지 않았다** — 차수는 여전히 소실점 집합의 함수이고 피치의 함수가 아니다(`DEFERRED` 등재) |
| **D-L75** | **차수 승격 개념을 지운다**(7차 지시 3-d) — `autoPromoteOrder`·`relinkLostSnaps`·`orderMarks`·`revertToOrder`·`promoteReport`·`renderPromoteReport`·`drawPromoteLoss`·`N점으로 되돌리기` 버튼과 그 `S2S` 창을 전부 지웠다 | 남아 있던 전이는 **P2 → P3 하나**였고 그 입구를 D-L74가 없앴다. 전이가 없으면 승격도 없고 되돌릴 것도 없다 — 남는 되돌리기는 일반 `실행취소` 하나다(§6.2의 "차수로 이름 붙은 되돌리기"는 대상이 사라졌다). ⚠ **순수 모듈은 남긴다**: `s3d/promoteOrder.ts`·`s3d/promoteDiff.ts`와 단위 시험(`order_promote`·`order_undo`·`order_signal`·`promote`·`promoteDiff`)은 **왜 그 규칙이 있었는지가 근거**이므로 지우지 않는다(PITFALLS 머리말의 은퇴 규약). 차수 재풀이가 다시 필요해지면 그 자리에서 다시 잇는다. **은퇴로 표시한 시험 여섯**: `vp_rules` 둘 · `axis_snap_table` 하나 · `horizon_drag` 하나 · e2e `stage.spec` 둘. ⚠ `AppSnap.report` 필드는 **자료구조에 남긴다** — 옛 저장본이 그대로 열려야 하고 읽는 쪽이 무시한다 |

## 2026-08-18 8차 항목 0 — 원격 갈래 병합 (D-L76 ~ D-L79)

⚠⚠ **번호 충돌이 있었다.** 저장소가 `63e4831`에서 **두 갈래로 갈렸다** — 이 작업 트리의 갈래와
`origin/main`에 PR #1·#2로 병합된 원격 갈래다. **두 갈래가 각각 `D-L66`~`D-L69`를 서로 다른
결정에 붙였다.** 병합(8차 항목 0)에서 **이 문서의 갈래가 번호를 지킨다** — 8차 지시문이
`D-L70`~`D-L75`를 인용하며 이어지기 때문이다. **원격 갈래의 넷을 `D-L76`~`D-L79`로 옮겼다**:

| 원격 갈래의 옛 번호 | 새 번호 | 내용 |
|---|---|---|
| 원격 D-L66 | **D-L76** | 3D 뷰 큐브 |
| 원격 D-L67 | **D-L77** | 1점 직접 좌표 |
| 원격 D-L68 | **D-L78** | 확정 뷰 자세·복원 무대 |
| 원격 D-L69 | **D-L79** | 겨냥 거리 정의 교체(aimDistPx) |

**이 문서 갈래의 `D-L66`~`D-L69`는 위 표(6차 지시 절)의 뜻 그대로다.** 병합 전 원격 갈래의
문서에서 `D-L66`~`D-L69`를 읽을 때는 이 표로 옮겨 읽는다.

## D-L76 — 3D 뷰 큐브: 절대 시점 스냅과 "재탭 = 가장 가까운 1점" (원격 갈래 지시 1)

**결정**: 뷰 큐브를 평면 위젯에서 **3D 큐브**로 바꾼다(CAD 뷰 큐브 관행 — A-3 선례).
직교 투영으로 그리고 카메라 기저를 따라 돈다. 히트는 광선-슬랩 교차 후 밴드 분류
(`CUBE_TOL.feature = 0.62` — 면/모서리/꼭짓점). 절대 스냅은 전부 `stage.snapToDir`
(궤도 중심을 겨누고 **거리 유지** — 지시 1-3 "방향만 돌리면 그린 것이 화면 어디에 오는지
예측이 안 된다"), 상대 회전(드래그·화살표)은 종전대로 `stage.spinYaw`(#17).

- **면 넷(정면·배면·좌·우) = 1점 시점**: 피치 0·요 90° 격자·중심 조준. **위/아래 면은 히트점이
  면 중앙(`top_center = 0.5`) 안일 때만** 탑뷰/저면이다 — 바깥 고리는 수평 면으로 보낸다
  (지시 1-1 "있되 손이 잘 안 감"). 수직 시선은 피치 89.5°로 눌러 OrbitControls up=+Y 특이를
  피한다(CAD 탑뷰도 같은 처리).
- **모서리 = 2점 · 꼭짓점 = 3점**(요 45° 격자·피치 0/±45°/±35.26°).
- **재탭 = 가장 가까운 1점**(지시 1-3): 히트한 특징이 시선과 30° 안(`retap_cos`)이면 그
  특징으로의 스냅은 무이동이므로 **가장 가까운 세계 축(±X·±Z) 정렬 + 피치 0**으로 간다.
  면에서는 두 동작의 목표가 같아 무손실이고, 2점·3점 시점에서 탭 한 번이 1점 복귀가 된다 —
  지시 1-1(특징 스냅)과 1-3(탭=1점)이 이 규칙 하나로 겹치지 않게 성립한다. 인접 특징 간
  최소 각 35.26°(꼭짓점↔모서리)라 30°는 다른 특징 클릭을 안 침범한다. 빗나감(실루엣 밖)도
  가장 가까운 1점이다.
- **상대 90° 화살표는 유지**(지시 1-2 — "선택. 절대 정렬이 우선") — 좌우 가장자리 20px 띠.
- `CUBE_TOL`은 `test/constants.ts` **밖**(D-L49·D-L51의 예외와 같은 자리 — 조작 감도이고
  어느 원장 값도 이 상수에 의존하지 않는다. 값은 `view_cube.json@f351839a`의 `ui_constants`).

**기각**: ① 탭 전부를 가장 가까운 1점으로(1-3 문면 그대로) — 면/모서리/꼭짓점 스냅(1-1)이
불가능해진다. ② 순수 CAD 분류(재탭 규칙 없음) — 3점 시점에서 가운데 탭이 같은 꼭짓점을
돌려줘 1-3의 머리 동작("이것이 가장 자주 쓰일 것이다")이 죽는다. ③ 큐브 히트 임계를
constants.ts에 — 전역 해시가 바뀌어 원장 40여 개가 STALE이 된다(DRAFT_TOL 동결과 같은 자리,
의존 집합별 해시가 서면 재검토).

검증: `view_cube.json@f351839a`(면=1점·꼭짓점=3점·드래그 연속·상대 90°·재탭 — 8-R″
[M4]·[M5]·[M8] 반영: fy 전제 임계 등록·settle 서술·설계 보장 명시) ·
`view_cube_hit.test.ts` 11 (밴드 경계·위/아래 한정·빗나감 null·가장 가까운 축 — 반례 포함).

## D-L77 — 1점 직접 좌표: 정확 정렬 판정 + 손 정렬 자동 스냅 (원격 갈래 지시 2)

**결정**: 1점 시점(시점 좌표의 축 방향 셋이 기저의 부호 있는 순열 — `onePointFrame`)에서
축 스냅 획은 **카메라 투영을 거치지 않고** 화면 좌표에서 3D가 바로 나온다(`directSegment` —
화면축은 z₀/f 배율 하나, 깊이축은 그 역. 판정·미리보기·확정이 resolveLive 한 자리다 #17).
시작점 스냅이 없으면 **궤도 중심의 깊이 평면**에 놓는다(`planeAnchor` — 지시 2-3.
⚠ 확정 뷰의 지평선 아래는 지면 스냅이 먼저 받는다 — 무스냅 평면 배치는 지평선 위·기하
밖에서 도달한다). 미승격이 없다(지시 2).

- **판정은 정확 정렬만 통과한다**(`align_eps` 잔차 노름 1e-3 — ⚠ |내적| 판정은 각의
  2차라 0.5° 틀림도 통과시킨다. 반례 테스트가 잡아 잔차로 바꿨다). 근사 정렬에서 직접
  공식을 돌리면 오차가 좌표에 구워지고 화면 불변(§4.5)이 깨진다 — 음성 대조 실측: 2° 틀면
  직접↔lift가 **최소 0.0244**(세계 단위 · 보수 통계 min — 중앙 0.0561 · max 0.9418) 갈라진다(`one_point_direct.json@f351839a`의 `negative_tilt_2deg_yx`). ⚠ 원격 갈래가 적었던 **0.253**은 그 갈래의 2-R′가 통계를 max에서 min으로 바꾸기 전 값이라 현행 원장에 없다(8차 항목 0에서 원장을 다시 읽어 정정).
- **손 정렬 허용 오차(지시 2-1)는 자동 스냅이 처리한다**(`hand_deg` 2°): 궤도(터치 제스처·
  마우스 궤도 — 2-R′ [B-6]로 마우스 종료도 배선)가 끝나고 감쇠 꼬리가 멎으면, 시선이 세계
  축·수평의 2° 안일 때 **정확한** 1점 자세로 눌러 앉힌다. ⚠ 선례 정정(2-R′ [B-9]): 지정
  선례(SketchUp/Rhino)에는 이 동작이 없다 — 가장 가까운 것은 Blender의 **Alt+궤도 축 스냅**
  (가장 가까운 정사영으로 스냅)이지만 그쪽은 수정자 명시형이고 **자동형은 이 프로젝트의
  판단**이다(A-3 "선례 없으면 가장 단순한 것"의 자리로 옮긴다). 회전은 지금 궤도 target
  둘레이고 **중심 재조준은 하지 않는다**(D-L65의 시선 유지와 같은 이유 — 큐브 탭의 중심
  조준과 다르다). ⚠ 화면 이동량은 **미측정 추정이다**(2-R′ [B-5] — 회전 ≤2°에서 f·tan2°
  ≈ 수십 px 급이라는 기하 추정. 실측은 DEFERRED). 2°는 의도적으로 축을 트는 사용자
  (CLAUDE §전제)를 방해하지 않는 크기다.
- **돌린 시점의 축 방향 상실 수리**: `frame()`이 `c.axisDirs`를 쓰고 `viewPlaceCtx`가
  시점 좌표 축 방향을 함께 낸다 — 옛 판(vps만)은 1점 확정의 화면평행 축 둘이 돌린 시점에서
  사라져 우측면 입면에서 깊이(X축) 획을 못 올렸다(네 입면 흐름 2-4의 장애물, D-L40의 회전 판).
- `ONE_POINT_TOL`은 `test/constants.ts` 밖(D-L49·D-L51의 예외 — 전역 해시 동결).
  ⚠ 면제 사유 정정(2-R′ [B-3]): **동등성 팔은 align_eps에 의존하지 않으나
  frame_judgment(0.5° null)는 의존한다** — align_eps를 바꾸면 STALE이 못 잡으므로
  `one_point_direct.json`을 재실행한다(DEFERRED 의존 집합별 해시의 자리).

⚠⚠ **`planeAnchor`는 CLAUDE.md A-3과 정면으로 어긋난다 — 그 충돌을 여기 적는다**
(2026-08-18 8차 리뷰어 [13]. 병합이 이 동작을 받아들였는데 충돌이 어느 결정문에도 없었다):

> A-3: **"애매하면 놓지 않는다. 조용히 틀린 배치를 만들지 않는다."**
> **"놓지 않되 버리지 않는다"** — 미배치는 실패가 아니라 **대기 상태**다(개정 2 §9.1).

`planeAnchor`는 **시작점 스냅이 없는 획**, 즉 **깊이를 정할 정보가 하나도 없는 획**을
궤도 중심의 깊이 평면에 **놓는다**. 그 깊이는 관측이 아니라 **화면 밖에서 온 임의값**이므로
그 배치는 정의상 **조용히 틀린 배치**다 — A-3이 금지한 바로 그것이다.
**병합은 이 동작을 배포된 것이라는 이유로 유지했고**(항목 0: 배포 동작을 임의로 되돌리면
사용자가 보는 것이 항목 5 이전에 또 한 번 바뀐다), **그 판단은 A-3의 예외가 아니라 이월이다.**

- **전제 반증의 기록**: 원격 갈래는 지시 2-3("스냅이 없으면 어디에 놓나")을 **놓는 문제**로
  읽었다. 이 갈래의 계획서(개정 2 §9.1)는 같은 물음을 **놓지 않는 문제**로 읽는다 —
  대기 상태가 정답이고 좌표는 **연결이 정한다**(2차 지시 항목 5). 두 읽기 중
  **측정이 편드는 쪽은 후자다**: `p1_invariance.json@f351839a`의 `unanchored_place`가
  ~~놓인 넷 **전부**를 미앵커로 세고, 셋이 **같은 지면 y**를 공유한다 — 서로 다른 깊이에
  있어야 할 획들이 임의값 하나에 눌려 있다는 뜻이다.~~
  ⛔ **제자리 정정(10차 항목 0 · 리뷰어 [4][5])**: 가드를 켠 현행 원장은 `placed 3 ·
  unanchored_placed 3`이다(넷째는 대기). 그리고 **"같은 y"는 미앵커의 서명이 아니라
  이 픽스처의 성질이다** — 앵커된 획들(`elevation_flow`의 direct 경로)도 같은 y를 공유한다.
  D-L77을 뒤집는 실측 근거는 같은 y가 아니라 **placeUnanchored가 넣던 임의 z**
  (실측 3.499~6.995 — 궤도 중심이 정한 화면 밖 값)다.
- ~~**되살릴 조건(양성 채널 · #30)**: **항목 5-5가 `planeAnchor`를 지우면
  `unanchored_placed`가 0이 되어야 한다.** 0이 안 되면 5-5가 안 걸린 것이다.~~
  ⛔⛔ **제자리 정정(2026-08-18 9차 항목 4-5 · 10차 항목 0)**: 그 예측은 **틀렸다** —
  가드를 켠 실측은 4 → **3**이다(현행 `p1_invariance.json`). `snapStart == null` 배치의
  경로가 셋이고 가드는 둘만 막는다 — 남는 출처는 `solveInto` → `liftAll`(연결 성분의
  일괄 풀이)이고 **0으로 만드는 것은 4-3(연결과 확정)이다**(D-L83의 10차 절).
  판정자는 `lifted` **개수가 아니다** — 개수는 "임의 깊이로 놓임"과 "연결이 정한 깊이로
  놓임"을 못 가른다(8차 리뷰어 [4]).

**기각**: ① 허용 오차 안에서 직접 공식을 근사 카메라에 그대로 — 화면 불변 위반(위 실측).
② 오차만큼 카메라를 안 움직이고 해석만 축 격자로 — 잉크가 재투영과 어긋난다(같은 문제).
③ 자동 스냅을 궤도 중 실시간으로 — 감쇠와 싸운다. 정착 후 한 번이 단순하다(A-3).

검증: `one_point_direct.json@f351839a`(동등성 두 프레임 72표본 max 3.0e-14·8.1e-14 · 음성 대조 min 0.0244 · 판정 반례) ·
`elevation_flow.json@f351839a`(네 입면 종단 — 정면 X축·무스냅
평면·우측면 Z축·배면 X축 전부 direct, 자유 시점에서 lift 음성 대조) · vitest·e2e 전량.

---

## D-L78 (원격 갈래 항목 1) — 확정 뷰 자세: 복원이 무대를 재수립하고, 자유 시점이 확정 렌즈를 이어받고, 저장 무결성은 "표식의 전제"를 지킨다

**맥락**: 실획 첫 표본에서 사람이 `views[0].pose === null`을 "확정 뷰에 자세가 없다"로
읽고 세 증상(그리드 어긋남·궤도가 다른 카메라로 출발·그릴 때마다 다른 곳)의 근본 원인으로
지목했다(지시 1). 실측: **pose null은 설계 표식이다**(doc.ts — 확정 뷰 = 첫 카메라 자체,
세계 좌표계가 곧 그 카메라 좌표계라 자세가 항등). 전제는 반증됐고 실결함은 둘이었다.

**결정 셋**:
1. **복원(`applyDoc2`)이 무대 카메라를 재수립한다** — 현재 뷰가 확정 뷰면 `pinTo`,
   저장 시점이면 `setPose`, 카메라가 안 서면 unpin. 옛 판은 규칙·문서만 복원해 새로고침 뒤
   무대가 생성 기본 자세 `(3.2, 2.4, 3.6)`·비핀으로 남았고, frame()·궤도·viewForDrawing이
   전부 그 기본 자세를 읽었다(세 증상과 정합). 재수립 전에 캔버스 크기를 자가 치유한다(#22).
2. **자유 시점(궤도)이 확정 카메라의 내적 파라미터(주점·f)를 이어받는다**
   (`stage.freeIntrinsics()` — 단일 출처 #17, `frame()`의 배치 문맥도 같은 값). 옛 판은
   `unpin`/`flyTo`가 FREE 45°·중심 주점으로 갈아 끼워 궤도 진입 순간 화면이 점프했다
   ("다른 카메라로 전환 후 돌아감"의 실체). 선례: SketchUp도 궤도 진입에서 렌즈를 유지한다.
   ⚠ 이어받는 것은 **확정 해의 주점 그대로**다 — 2점은 `[W/2, 지평선 y]`(16.2 가정 + A-4),
   3점은 수심(이론서 6.3). 차수별 분기를 새로 만들지 않는다(#37 대조 — 1-R′ [6]).
3. **지시 1-c("저장 시 pose가 null이면 실패")의 재해석**: null은 표식이라 문자 그대로면
   모든 정상 저장이 실패한다(자기모순). 실패로 기록하는 것은 **표식의 전제가 깨진 문서**다 —
   확정 뷰(pose null)가 정확히 1개가 아니거나 자세에 비유한 값이 있으면 `serializeDoc2`가
   던지고, 자동 저장은 실패 사유를 화면에 남기며 `.brnl` 내보내기는 중단된다
   (`doc2PoseIntegrity`). 지시의 의도(무결성 위반을 조용히 넘기지 않는다)는 보존된다.

**기각**: ① 확정 뷰에 명시적 자세를 저장 — 항등의 중복 저장이고, `pose === null`을 확정 뷰
판정으로 쓰는 자리(복원 home·뷰 전환·삭제 금지) 전부를 옮겨야 한다. ② unpin에서 FREE 45°
유지 + 전환 애니메이션으로 가림 — 점프를 숨길 뿐 렌더와 배치가 계속 두 카메라다.

검증: `restore_pose.json@f351839a`(재핀·시점 왕복·렌즈 유지·주점 중심 밖 0.23H 동작점 ·
음성 팔 넷) · `doc_store.json`(무결성 반례 3+정상 1) · 수정 전 재현(#21): pinned=false ·
자세 차 6.10 · freeIntrinsics 부재.

---

## D-L79 (원격 갈래 항목 2) — 겨냥 거리의 정의 교체(aimDistPx)와 물음 억제의 범위(vpdir만)

**① 겨냥 거리(판정 지표 교체 — #28의 절차로 사유를 적는다).** 필드명 `snapDistPx`(.brnl)와
원장 지표명 `snap_dist_px`(real_ink)는 **그대로**이고 **정의가 바뀐다**:
- 옛: 스냅이 걸리면 그 후보의 `cand.dist`(on_face면 정의상 0 — #3·#5), 안 걸리면 40px
  프로브의 **우선순위 첫** 후보 거리. → 핀 상태에서 on_face가 항상 걸려 **전부 0**
  (실획 첫 표본), 프로브 분기는 죽어 있었다(D-L56 정정 병기 참조).
- 새(`aimDistPx` 한 함수, #17): **스냅 성패와 무관하게**, 스냅 전 원시 시작점에서 40px
  프로브 안 **최근접** 정밀 대상(on_face 제외)까지의 거리. 없으면 null.
  바뀐 것 셋 — 측정 시점(스냅 후→전) · 선택(우선순위 첫→최근접: 겨냥 거리는 최근접이다) ·
  on_face 처리(0 오염 제거). **7차 이전 저장본의 0은 옛 정의의 산물**이라 real_ink가
  `snap_dist_legacy_zero`로 갈라 센다(#11 — 두 정의를 한 분포에 섞지 않는다).

**② 물음 억제는 vpdir(기존 소실점 방향)만이다 — ortho(화면 직교)는 강제하지 않는다.**

> ⛔⛔ **대상 소멸 — 2026-08-18 8차 항목 0(병합).** 아래 ②는 `snapForced`로 방향을 넘기는
> 기전인데, **이 저장소 갈래의 D-L70이 `snapForced`를 통째로 지웠다**(같은 P1 가드 우회를
> 근거로 — 두 갈래가 **같은 결함을 각각 발견해 다른 크기로 대응**했다: 원격은 ortho만 빼고
> vpdir는 남겼고, 이 갈래는 **둘 다** 뺐다). 병합은 **더 큰 쪽(D-L70)을 택했다** — 남겨 두면
> vpdir가 여전히 P1 가드에 지지를 얹기 때문이다. 그러므로 **아래 실측값은 재현할 기전이 없다**:
> 발동 카운터 `osnap_vpdir_forced`는 대상이 사라져 하네스에서 **지웠고**(#40 — 영원히 0인 필드를
> 남기지 않는다), `asks_per_run`은 현행 원장의 다른 배선 값이다(`rule_camera.json@f351839a`
> `headline_no_identity`: r40 팔 **2.161** · 15px 팔 `rule_grouped_snap` **2.255** —
> ⛔ **제자리 정정**(8차 2차 지시 항목 0): 여기 적혀 있던 2.49 / 2.531은 **8차 항목 1 직전
> (`d85dca8`)의 값**이고, 항목 1·2가 이 원장을 다시 돌려 낡았다).
> 아래 문단은 **왜 그 규칙이 있었는지의 기록**으로 남긴다(PITFALLS 머리말의 은퇴 규약).

끝점 오스냅으로 두 끝이 정해진 현(弦)의 방향 판정을 `snapForced`로 넘기되:
- **vpdir**: stepRule에서 기존 소실점의 **지지(support)로만** 떨어진다(같은 vpMisfit 임계) —
  카메라를 못 바꾼다. 억제되는 물음은 모호 구간(4~8°)의 screen_or_depth뿐.
  실측(⛔ 위 대상 소멸 상자 참조 — 재현 불가, 원격 갈래 `509615a1` 시점의 기록이다): r40 팔 asks_per_run 2.521→2.475(headline) ·
  2.337→2.300(all_jitters), **deg_median·rejected 전 팔 불변**. **발동 카운터**
  (`osnap_vpdir_forced` — 3·4-R [5]로 신설·#32의 발동/효과 갈림): 15px 팔 **0**(불변의
  원인은 미발동이다) · r40 팔 **13**(전부 잡음 행 — asks 감소가 그 자리). 판 참조는 원장
  what_not_say(6차 = 513c33d · 초판 = b4f2d9b, 3·4-R [4]).
- **ortho 기각 사유**(2-R″ [1] — 리뷰어가 초판을 잡았다): forced="screen"은 stepRule의
  P1 가드("소실점이 서 있으면 화면 수평선을 **묻는다**" — D-L53: 이 물음 없이 받게 하자
  축 오차 중앙 10.1°→31.6° 붕괴, P1은 **불가역**)를 우회한다. 방향 스냅의 "스냅이 곧
  선언"(5차 지시 3)은 **방향을 스냅으로 만든** 경우고, 끝점 오스냅은 두 점을 이었을 뿐
  방향은 그 점들이 정한 우연이다 — 의도 선언이 아니다. 얕은 현의 물음은 남는다.
  초판(ortho 포함)의 asks 감소(15px 팔 2.337→2.321)는 **전부 이 우회에서 온 것**이었다.

검증(⛔ ②는 대상 소멸): 원격 갈래 `509615a1` 시점의 `rule_camera` 세 판 대조(6차 = 그 갈래 개정, 초판만 이탈) ·
`resolve2d_end2.test.ts`(vpdir 억제·ortho 비강제·점 불변 — 수정 전 실패 확인) ·
`snap_cache.json` aim_dist_px_sample(새 정의가 0/null 아닌 값을 내는 실측).

---

> ⚠⚠ **10차 항목 4(D-L85 — 반경 8) 재실행으로 이 절의 수치들이 움직였다.** 모순
> 108→72는 반경 15의 값이고, 현행(반경 8)은 `classify_leak.ablation` — `guard_off`(=가드 끔)
> **48/2880** ↔ 켬(`r8_rel_on`) **35/2880**이다. **가드의 대조(끔>켬)는 유지된다.**
> P0·camera_ok·절단 밖 등 order_lock 값도 전부 움직였다 — 현행은 원장을 그 자리에서 읽는다.
> ⛔ 아래 "위약 역전의 절반 해소"는 **반경 8에서 되돌아갔다**(위약 6.7125 < 판정 7.3165 —
> **몰아 팔에서만이다**: 번갈아·넓은짝은 스냅이 위약을 이긴다. D-L85 대가 ⑤).
> 이 절의 수는 그 시점의 기록이다.

## D-L80 — 방향·관계 스냅은 `depth` 판정을 뒤집지 못한다 (2026-08-18 9차 항목 0)

**D-L70의 나머지 절반이다.** D-L70이 지운 것은 `snapForced`(스냅이 곧 **선언**이다)인데,
**좌표 이동 자체가 선언이었다** — `screenOrthoSnap`·`alignAxes`가 획을 정확히 0°/90°로
옮겨 놓으면 `classifyLine`은 **반드시** 화면 축을 낸다. `axisSnap.ts`의 주석이 그 사실을
이미 적고 있었는데(*"스냅이 걸린 획은 정확히 0°/90°가 되어 `classifyLine`이 반드시
`screen_h`·`screen_v`를 낸다"*) **그것이 결함이라는 것을 아무도 안 읽었다.**

**규칙**: `resolve2dCore`에서 원본 획(`representative(raw)`)의 판정이 `depth`(≥8°)면
**화면 직교 스냅과 관계 정렬(`alignAxes`)을 끈다.**

**끄지 않는 것 둘**:
1. **끝점·시작점 오스냅** — "이 점이 저 점이다"는 **위치**의 진술이지 방향의 진술이 아니다.
   그리고 항목 4가 "연결이 좌표를 정한다"로 세우려는 기전이 바로 그것이다.
2. **소실점 방향 스냅(`vpdir`)** — `depth` 판정과 **같은 편**이라 모순이 아니다.

**`ambiguous`(4~8°)에는 안 건다** — 거기서 스냅이 가르는 것은 8차 지시 2-b가 정한 대로
**물음이 아니라 조작**이다. 가드가 `depth`에만 걸리므로 그 결정과 충돌하지 않는다.

**근거**(`classify_leak.json` — 3600획):
- 원본이 **8° 밖**인데 스냅 뒤 화면 축이 된 **모순** 획: **108/2880**, 그중 **71이 1점 구도**.
- 되돌림의 대가 **둘**(⛔ 초판은 첫 줄만, 그것도 가드 이전 값으로 적었다 — 9-R [H2]):
  ① 참 축이 화면인데 같은 모순 경로를 지난 획 **4/720**(0.6%).
  ② ⚠⚠ **참 축이 화면인 획의 미검출이 107/720 → 136/720(+29)**이다. 같은 개입의
  이득인 **총 누수는 1284 → 1246(−38/2880)**뿐이므로 **획 수로는 이득 38 대 손해 29**다.
  👉 **이 결정을 정당화하는 것은 총 누수가 아니다** — **모순 108 → 72**와
  **1점 구도 누수 107 → 91**이다. 총 누수로만 보면 **오스냅을 아예 끄는 편이 더 낫다**
  (`r0_rel_off` 1180/2880) — 그 선택을 안 한 이유는 항목 4가 오스냅 위에 서기 때문이다.
- 가드 투입 뒤 모순 **108 → 72**(1점 71 → 55). **0이 아니다** — 남은 72는 **끝점 오스냅**이
  만들고 그것은 **항목 2(반경)의 대상**이다(`ablation`의 `r8_rel_on`이 35/2880).

**귀결**(`order_lock.json`): P0 **140 → 122** · `camera_ok` 460 → **478/600** ·
`one_depth_line` **87 → 66**실행. ⚠⚠ **대가**: 절단 0.5 밖 배치가 **1923 → 1965**로 늘었다
(늘어난 배치 64 중 42가 파국). **병목이 "카메라 없음"에서 "나쁜 카메라"로 되돌아갔다**(#16) —
**이 결정만으로 배치가 좋아졌다고 적지 않는다.**

**측정 스위치**: `Resolve2dCtx.kindFlipGuard`(기본 **켬**). 끄면 옛 거동이고 그것이
**양성 채널**이다(#30 — `order_lock`의 `bypass`와 같은 자리). **앱 코드에서 안 끈다.**
⚠ **원장 두 곳에 그 팔이 있다**(9-R [H3]): `classify_leak.ablation.guard_off`(판정만)와
**`order_lock.arms.guard_off`**(차수·배치까지). 뒤엣것이 P0 **140 ↔ 122**를
**같은 실행 안에서** 낸다 — 초판은 이것을 이전 커밋의 원장과 비교해 놓고 양성 채널이라 불렀다.

⛔ **함께 철회된 것**: *"P0의 원인은 `classifyLine`의 4° 오독"*(8차 `HANDOFF` 3번).
4°가 먹는 깊이 획 1035/2880 중 **871은 참 투영선에서도 4° 안**이고, 임계를 0.25°까지
좁히면 누수는 68/2880으로 주는 대신 **화면 획의 92%(663/720)를 놓친다.**
**두 분포가 겹쳐 있어 각 하나로는 못 가른다**(`assumptions.md` AS-L25 정정).

---

> ⚠ **10차 항목 4(D-L85 — 반경 8)로 이 절의 동작점 수치가 낡았다** — 576/1900·28.5px 대
> 15px는 반경 15 행의 값이다. 현행 동작점 행(r8): 발동 **381/1796** · 겨냥 중앙 **35.59px**
> (**제자리 정정(2026-08-18 12차 항목 1 재실행 · #42 ⑥⑨, 12차 리뷰어 [3])**: 12차 재실행 행은 **381/1802 · 34.75px** — 수만 움직였다)
> 대 조리개 **8px**(`snap_stage.d2_by_radius`). "판정은 안 새고 조리개만이 병목"이라는
> 결론의 방향은 그대로다(0/381 — 조리개 안인데 안 걸린 획 없음).

## D-L81 — 2D 단계의 스냅을 별도 필드로 기록한다 (2026-08-18 9차 항목 1)

**확정 전(P0)에는 3D가 없으므로 `snapStart`/`snapEnd`에 넣을 수 없다** — 그 둘은 `at`이
`Vec3`이고 `ofId`가 3D 획을 가리키는 **3D 참조**다. 그래서 8차까지 2D 단계의 오스냅은
**좌표를 옮기고 표식까지 그리면서 문서에는 아무 기록도 안 남겼다.**

그 결과가 측정의 공백이었다: 실획의 `snapEnd` **0/5**를 보고 **"끝점 스냅이 안 걸린다"**인지
**"걸렸는데 3D 획이 아니라 안 세어진다"**인지 가를 수 없었다(8차 2차 항목 4가 그 자리에서
원인 귀속을 철회했다).

**규칙**: `SStroke.snap2dStart` · `snap2dEnd`(`Snap2Ref = { kind, at: Pt2, distPx, ofId? }`).
- **`at`이 화면 좌표다** — 그것이 별도 필드인 이유이고, `DEFERRED`가 4차에 적어 둔 답
  (*"별도 필드 `snap2DistPx` 등"*) 그대로다(A-3: 선례를 따른다).
- **`snapStart`/`snapEnd`를 대신하지 않는다.** 한 획이 둘 다 가질 수 있다 —
  2D에서 붙고 나중에 카메라가 서서 3D 참조도 붙는 경우다. **다른 시점의 다른 사실**이다.
- 기록은 **`resolve2d.snap2Refs()` 하나**가 하고 앱과 하네스가 그것을 부른다(#17).
  새로 판정하는 것이 없으므로 **`engaged == recorded`는 보장이고 임계를 안 건다**(#5).
- 옛 저장본에는 없다 → `real_ink`가 **`field_present`로 "안 걸렸다"와 "안 적혔다"를
  갈라 센다**(#32).

**근거**(`snap_stage.json`): 2D 단계가 전체 획의 **5668/10755**(52.7% — 12차 재실행 값,
화각 상한 신설로 소폭 이동)이고, 그 단계에서
끝점 오스냅이 **576/1900**(조리개 15) 걸리는데 **문서에 남던 수는 0**이었다.
조리개 안인데 안 걸린 획은 **0/576** — **판정은 안 새고 조리개만이 병목**이다
(가장 가까운 후보까지 중앙 **28.5px** 대 조리개 **15px**). ⚠ 그 표는 **"붙었는가"만** 세고
**"붙어야 할 곳에 붙었는가"는 안 잰다** — 반경 결정은 **항목 2**의 것이다.

⚠ **소비자를 같은 세션에서 붙인다**(#18): 항목 4-3의 연결·연쇄가 이 기록을 읽는다.
안 붙으면 쓰고 안 읽는 필드이므로 `HANDOFF`에 그렇게 남긴다.

---

## D-L82 — 오스냅 반경 15px을 유지한다 (2026-08-18 9차 항목 2)

> ⛔⛔ **D-L85(10차 항목 4)가 갈아 끼웠다 — 기본 반경은 8px이다.** 9차의 "미결" 판정을
> 10차 지시가 `classify_leak.ablation`(모순 72 → 35)을 근거로 끝냈다. 이 절은 연혁이다.

⛔⛔ **제자리 정정(9-R″ [1][2][3] — 같은 세션의 리뷰어 2차)**: 초판의 표제는
*"그리고 반경을 재는 방법을 바꾼다 / 바뀐 것은 근거다"*였고 **그 주장이 과했다.**
정정 셋을 먼저 적는다 — **아래 본문의 근거 하나(이유 1)는 무효다.**

1. ⛔ **"8차 스윕은 잘못된 연결을 만들 수가 없다"는 틀렸다.** `snap.json`에
   **`unintended_snap`**이 이미 있고 스스로 *"반경을 키우는 유일한 측정 가능한 비용 …
   이것이 없으면 '반경은 클수록 좋다'가 나오고 실제로 그렇게 나왔다"*고 적는다.
   착수 표에 그 원장을 올려 놓고 `by_radius_all`만 읽었다(#42 ⑪).
   → `osnap_two_sided`의 기여는 **"없던 반쪽"이 아니라 "다른 경로(2D 단계)·다른 질의
   (균등 난수가 아니라 실제 획 끝점)에서 같은 물음을 다시 묻는 것"**이다.
2. ⛔⛔ **아래 이유 1(1점·2점에서 잘못된 연결 0)을 철회한다.** 반경 × 구도 전부를 내 보니
   **1점·2점은 반경 40에서도 0**이다(`by_composition.*.reachable_any_radius: false`).
   그것은 "반경이 안전하다"가 아니라 **이 픽스처가 그 층에서 대상을 못 만든 것**이고
   (#35), **위 1에서 8차 스윕을 두고 지적한 편향과 같은 형태다.**
3. ⛔ **축소 쪽 측정을 안 읽었다.** `DEFERRED`가 *"항목 2가 반경을 정하면 여기가 따라
   준다"*로 넘긴 `classify_leak.ablation`에서 **반경 8이 D-L80 모순을 72 → 35**
   (1점 55 → 31), **1점 총 누수 91 → 59**로 줄인다. **네 지표 전부 15보다 낫다.**

👉 **그러므로 결정문을 다시 쓴다**:
> **반경을 15로 둔다. 그것은 "측정이 15를 지지한다"가 아니라 "이 세션이 반경을 정할
> 근거를 못 갖췄다"이다.** 남는 근거는 축소의 대가(`missed` 29.1% → 50.6%) 하나와
> **저장소 규칙**(잘못된 연결이 놓친 연결보다 나쁘다 — A-3) 하나뿐이고,
> 잘못된 연결의 비용은 **1점·2점에서 아직 못 매긴다.**
> **개선으로 인용하지 않는다.** 이 결정을 끝낼 셋은 `DEFERRED`에 이름으로 있다.

---

**아래는 초판이다 — 이유 1은 위 2로 철회됐다.**

8차까지의 반경 스윕(`snap.json`의 `by_radius_all`)은 **모든 획이 서로 이어지는 픽스처**
(`boxLattice` = 상자 하나의 12모서리)에서 **"붙는가"만** 쟀다. 그 조건에서는 반경이 클수록
좋아지는 것이 **측정이 아니라 설계의 귀결**이다 — 잘못된 연결을 **만들 수가 없기** 때문이다
(AS-L24·L25와 같은 계열이고 편향의 방향만 반대다).

**신설 `osnap_two_sided.json`**이 그 나머지 반쪽이다: 첫 덩어리에서 **화면으로 떨어진 자리**에
둘째 상자를 그린다(참 정점 사이 최소 40px, 겹치는 구도는 판정 전에 뺀다 — #20).
후보는 첫 덩어리에서만 나오므로 **둘째 덩어리의 끝이 걸리면 정의상 잘못된 연결**이다.

| 반경 | 잘못된 연결 | 놓친 연결 |
|---|---|---|
| 8 | 49/4608 | 838/1656 |
| **15**(현행) | **123/4608** | **481/1653** |
| 30 | 345/4608 | 112/1641 |
| 40 | 536/4608 | **0/1627** |

**두 곡선이 반대 방향으로 단조**이고(등록 게이트 ①) **`wrong`이 0이 아닌 반경이 있다**(②).
⚠ **두 열의 분모가 다르다**(#11) — `wrong`은 4608 고정, `missed`는 반경에 따라 1656 → 1627로
준다(스냅이 걸리면 다음 획의 후보가 달라진다). **두 수를 한 비율로 합치지 않는다.**

**15를 유지하는 이유 셋** — ⚠ **셋 중 둘은 측정이고 하나는 이 저장소의 규칙이다**(그 구분을
흐리지 않는다):
1. ~~**[측정]** 1점·2점 구도에서 반경 15의 잘못된 연결이 **0/1152 · 0/1152**다.~~
   ⛔⛔ **철회**(9-R″ [3]) — **반경 40에서도 0**이라 그 층은 **도달 불가**다.
   잘못된 연결이 실제로 나는 것은 3점 둘뿐이고(r15에서 57·66, r40에서 243·293),
   **1점·2점의 비용은 이 픽스처로 못 매긴다.**
   ⛔⛔⛔ **그 도달 불가를 11차 항목 3이 실측으로 확인하고 반쯤 열었다**(**D-L88**):
   분리 체제(`min_cand_px` > 40px)에서 **다섯 구도 전부 `wrong`이 난다** — 즉 **어떤
   반경에서도 0**은 오프셋 선택의 산물이었다. ⚠⚠ **그러나 앱 동작점(r8)에서는 1점·2점이
   여전히 0이고**, 도달은 r25~r40의 **지터 꼬리**에서 난다 — **그 층의 비용은 계속 못
   매긴다**(11차 리뷰어 [2]). 2점은 훑은 어떤 배율에서도 후보가 40px 안으로 안 들어와,
   낮은 누수가 이 픽스처 계열 안에서는 **기하의 성질**이다.
   값은 `osnap_two_sided.json`의 `by_composition_calibrated`·`by_composition_adjacent`를
   그 자리에서 읽는다(#47).
   👉 **이 줄의 철회는 유지된다. 얹히는 것은 "0은 도달 불가였다"이지 "이제 다 잰다"가 아니다.**
2. **[측정]** 축소(8)는 놓친 연결을 **29.1% → 50.6%**로 올린다 — 이으려는 시도의 절반이 실패한다.
3. **[규칙, 측정이 아니다]** 두 오류가 **대칭이 아니다**: 놓친 연결은 다시 그으면 되고
   잘못된 연결은 **조용히 틀린 기하**를 만든다(A-3 · CLAUDE.md §2의 상위 규칙).
   **이것은 원장에서 나온 수가 아니라 저장소의 결정 규칙이다.**

⚠⚠ **지시의 전제를 반쯤 반증한다.** 사용자 불만은 *"떨어진 곳에서 새로 시작하는 획이 근처
것에 끌려간다"*인데 **1점·2점에서는 반경이 그것을 안 만든다.** 남는 후보 둘을 이름으로
짚는다(둘 다 반경과 무관하다):
- **관계 스냅(`alignAxes`)** — 이 하네스는 껐다.
  ⛔ **초판이 "1점에서 79/240을 옮긴다 — 반경보다 크다"고 적었고 귀속이 틀렸다**(9-R″ [9]):
  79/240은 **`r15_rel_on` 팔의 합계**이고 그 블록의 기전 분해는 **끝점 오스냅 189/313 ·
  시작점 167/313 · 관계 정렬 153/313**이라 **오스냅 쪽 분자가 더 크다** — 순환이다.
  주장을 지지하는 팔은 `ablation`이다: 1점 누수 **91/240**(`r15_rel_on`) ↔
  **10/240**(`r15_rel_off`). ⚠ **그러나 반경도 같은 표에서 크게 움직인다**
  (`r8_rel_on` **59/240**) — **둘 다 지렛대이고 "반경보다 크다"는 철회한다.**
- **`on_face` 스냅** — 실획 첫 표본의 `start_kinds`가 **`on_face` 5/5**다. **항목 4-5의 대상**이다.

**실획이 오면 다시 본다**(지시 2-d). ⚠ **지금 실획으로는 못 한다**: `real_ink.snap_dist_px`의
**n이 0**이다(첫 표본 5획이 전부 D-L79 이전 오염).

**함께 넣은 것 — 2D 수선 발**(`snap2d.perp2dCandidates`, 항목 2-f):
화면 토글에는 "수선 발"이 있는데 **2D 단계에서는 한 번도 안 걸렸다** — 수선 발은
**기준점이 있어야 정해지므로**(라이노 `Perp`) 질의와 무관한 `static2dCandidates`에 자리가
없었다. 3D 판(`snap.snapCandidates`의 `ctx.from`)과 **같은 규약**으로 넣었고 새 임계를
안 만들었다(`SNAP_TOL.extend_ratio`를 그대로 쓴다). `resolve2dCore`가 **끝점 후보에만**
더한다(기준점 = 확정된 시작점). `ctx.segs`가 없으면 안 돈다 — 옛 부르는 쪽을 안 깬다.

---

## D-L83 — 좌표를 정할 자격은 정밀 대상에만 있다 (2026-08-18 9차 항목 4-5)
### ✅ **채택했다 — 2026-08-18 10차 항목 0이 되살렸다** (아래 "9차의 되돌림"은 연혁이다)

**10차 되살림에서 확정된 것 셋**:
1. **가드는 시작점 앵커의 두 호출부(확정 경로 `placeStroke` · 승격 연쇄 `promoteChain`)를
   다 지난다.** 9차 초판은 확정 경로만 명시했는데, 연쇄를 안 막으면 **같은 임의 앵커가
   한 획 뒤에 연쇄로 들어온다** — `basic_flow` 흐름 ①~③에서 실증됐다(아래 3).
2. **네 단언의 흐름별 판정**(지시 0-a): p1_invariance 구도를 쓰는 세 흐름(1점 확정·궤도
   시작·지우개)과 네 입면의 확정 단계는 **3이 옳다** — 접합 성분(밑선+깊이선 둘)만 일괄
   풀이가 올리고 넷째(지지선)는 어느 획과도 접합·스냅이 없어 대기가 맞다.
   ⚠ **기본 흐름 ①~③은 3도 아니고 2다** — 그 픽스처에서 연결이 있는 것은 가로선과 둘째
   깊이선(T접합)뿐이고, 옛 4는 **s2가 연쇄의 `on_face` 임의 앵커로, s4가 그 임의 기하의
   끝점에 양 끝 스냅으로** 올라간 값이었다. 9차가 "넷이 3을 받는다"고 적은 것은 연쇄를
   안 막은 구현의 값이다.
3. **네 입면 흐름의 무스냅 팔(6차 지시 2-3의 종단)은 "막힌다"를 재는 팔로 뒤집었다** —
   D-L77의 궤도 중심 깊이 배치가 이 지시로 뒤집혔기 때문이다(`elevation_flow.json`의
   게이트 조항을 제자리에서 갈아 끼웠다).

**측정(10차, 가드 켬)**: `p1_invariance.json` — `lifted` **3** · `anchor_guard`
`{start_rejected 0, unanchored_rejected 1}` · `unanchored_placed` **3**(전부 경로 ③ —
일괄 풀이. 0으로 만드는 것은 4-3이다). `basic_flow.json` 흐름 ① — `lifted` **2** ·
`s3_guard` 거절 발화(값은 원장을 읽는다 — 항목 1의 무조건 연쇄가 질의 누계를 3 → 9로 늘렸다.
0이 아니라는 것이 판정이다). `elevation_flow.json` —
`unanchored_rejected` **2**(확정 단계의 지지선 + 무스냅 팔).

### (연혁 — 9차) ⛔⛔ 구현·측정 뒤 되돌렸었다

**되돌린 이유는 결과가 아니라 검증이다**: 가드를 켜면 `basic_flow`의 **단언 넷**이
`4`를 기대하고 `3`을 받는다(배치가 하나씩 준다). 그 넷이 각각 "3이 옳다"인지는
흐름을 따로 읽어야 확인되고, **이 세션의 남은 컨텍스트로는 못 했다.**
확인 없이 단언을 낮추는 것은 **검사를 약화시키는 편집**(#19)이므로 되돌렸다.
~~**아래 규칙과 측정은 그대로 유효하고, 켜는 것이 다음 세션의 일이다.**~~ → 켰다(위).

**규칙**: 시작점 앵커로 **좌표를 확정할 수 있는 스냅**은 `vertex`·`endpoint`·`midpoint`·
`intersection`·`perpendicular` 다섯뿐이다. `on_face`(지면 위의 점)·`on_edge`(선 위 근처점)와
`placeUnanchored`(D-L77의 궤도 중심 깊이 평면)는 **좌표를 만들지 못한다** — 그 획은 **2D로 대기**한다.

**왜**: 그 셋은 **화면 어디서나 걸린다**(시선과 면의 교점이라 후보가 항상 있다). 그것으로
앵커하면 좌표가 **그 순간의 시점이 정하는 임의값**이 된다. 실획 첫 표본이 그 결과다 —
`start_kinds`가 **`on_face` 5/5**이고 같은 Y=1인데 깊이가 획마다 달랐다("그릴 때마다 다른 곳").
정밀 대상은 다르다: **이미 3D에 있는 점**이라 좌표를 **가져오는** 것이지 만드는 것이 아니다.

⚠ **표시·미리보기는 안 막는다.** 막는 것은 좌표 확정의 자격뿐이다.

⛔ **D-L77(6차 지시 2-3 *"없으면 궤도 중심의 깊이"*)을 뒤집는다.** 계획과 실제가 어긋나면
실제를 따르되 뒤집힌다는 사실을 적는다(#23) — 그 결정이 만든 것이 바로 `unanchored_placed`다.

**측정**(`p1_invariance.json`, 같은 실행): `lifted` **4 → 3** · `anchor_guard.rejected` **1**.
⚠⚠ **대가**(#16): 배치가 하나 줄고 **그 획은 지금 영영 안 놓인다** — 연결로 놓아 줄
**4-3(연쇄)을 안 했기 때문**이다. **이 결정만으로는 완결이 아니다.**

⛔⛔ **등록된 예측이 틀렸다**: `p1_invariance`가 *"4-5가 지우면 `unanchored_placed`가 0이 된다"*
고 미리 적었는데 실측은 **4 → 3**이다. `snapStart == null`인 배치를 만드는 경로가 **셋**이고
4-5는 둘만 막는다 — 셋째는 **`solveInto` → `liftAll`**(카메라가 서면 대기 획을 일괄로 올린다,
§6.1)이고 그것은 `on_face`가 아니다. **`unanchored_placed`를 0으로 만드는 것은 4-3이다.**

**측정 스위치**: `S2S.setAnchorGuard(false)`가 옛 거동이고 그것이 양성 채널이다(#30).
앱에서 안 끈다.

## D-L84 — 좌표는 연결이 정한다: 일괄 풀이는 첫 앵커 전용, 연쇄가 연결을 되푼다 (2026-08-18 10차 항목 1)

**지시 4-1~4-4·4-6의 구현 결정 넷.**

1. **세 상태는 계산이다**(4-1 — D-L53 "상태는 계산" 규약 그대로): `coord`(seg3d 있음) ·
   `dir`(카메라 서 있고 축이 정해짐 — 무한직선·좌표 미정·**정상 대기**) · `none`.
   저장 필드가 없다 — `strokeStateOf`가 매번 낸다(`S2S.strokeStates()`).
2. **무한직선 표시**(4-2): `dir` 획은 소실점(무한원 축이면 그린 방향)을 지나는 전체 직선을
   **아주 옅게**(알파 0.12) 깔고 그린 구간은 종전대로 진하다. 표시만이다 — 스냅 대상 아님
   (잠정 그리드와 같은 규약).
3. **일괄 풀이(`solveInto` → `liftAll`)는 3D가 하나도 없을 때만 돈다**(4-3·4-4).
   ⛔ **제자리 정정(2026-08-19 13차 항목 1·2 — D-L90)**: "일괄 풀이가 4-4(첫 앵커)의 답"은
   **끝점 접합 스타일에서만** 성립한다 — 실획 15-16-18은 몸통 교차 7/7(끝점 거리 최소 46px >
   touch 20.34px)이라 교점 0으로 일괄 풀이가 아무것도 못 올렸다(`first_anchor.json@f351839a`).
   그 스타일의 첫 앵커는 **지면 배치**(D-L90 — `placeBy.ground`)가 맡는다. (원문:)
   일괄 풀이의 스케일은 **지면 게이지**라, 확정 순간의 첫 성분(소실점을 만든 두 선 + 밑선)
   에서는 그것이 **첫 앵커**지만(4-4의 답 — "소실점을 만든 두 선(지면)"), 그 뒤에 생긴
   **떠 있는 성분에는 기존 기하와 무관한 임의 좌표**다. 실측: dir_state 픽스처에서 허공의
   깊이선+가로선 짝이 확정 뒤 일괄 풀이로 **지면에 내려가 붙었다**(조용히 틀린 배치 —
   A-3이 금지한 그것). 제한 후 그 짝은 대기했다가 **연결이 생기는 순간** 연쇄로 올라간다.
4. **연쇄가 연결을 되푼다**(4-3): `promoteChain`의 획별 시도 순서는
   ① **그린 시점의 연결 기록**(`snap2dStart`/`snap2dEnd` — ofId 대상이 3D면 연결점 광선과
   대상 직선의 최근접점이 앵커. **재탐색보다 앞선다** — 기록이 사용자의 실제 손짓이다.
   #18의 소비자) ② 시작점 오스냅(종전) ③ **끝점 오스냅**(신설 — 연결은 어느 끝에서든
   좌표를 정한다. 옛 연쇄는 시작점만 봐서 끝으로 이어진 획이 영영 안 올라갔다).
   끝 앵커 배치는 점열을 뒤집어 풀고 되뒤집는다(`placeAnchored` — 축 판정은 방향 대칭).
   ⚠ 2D 연결 기록은 이제 **확정 뒤에도** 적는다(대기 분기의 `snap2At` 결과 — D-L81 확장).

**측정**(`dir_state.json` 신설): 카메라 전 상태 전부 none · 확정 시 접합 성분 셋 coord
(batch 3)·지지선 dir · 무연결 가로선의 무한직선 픽셀 >0(빗나간 점 0) · V→D→F 연쇄
(D.snapStart.ofId=V · F.snapEnd.ofId=D — **chain 팔의 ref_anchor 1**) · **별도 팔 E의
end_anchor 1**(⚠ 두 팔을 묶어 적지 않는다 — 10차 리뷰어 2차 [8]) · **옛 연쇄 대조 팔**
(setChainExt(false)에서 안 올라가고 켜면 회수 — #30, recovered 1) · 무연결 둘은
끝까지 dir · **경로 합(7) = 배치 전체(7)** · unanchored 0(가드 켬의 보장 — 판정 아님).

**경로별 배치 카운터**(4-6 · 지시 "카운터를 경로별로 나눈다"): `placeBy`
{ref_anchor·start_anchor·two_point·end_anchor·cross_anchor(12차)·batch·ground(13차 —
D-L90)·extension(13차 항목 3)·unanchored} — 합=전체 검산을 원장이 스스로 낸다(⚠ 13차
정정: 초판 열거는 여섯이었고 12차 cross_anchor·13차 두 경로가 늘며 낡았다 — 1·2 리뷰어).
실행 누계라 실행취소를 되돌리지 않는다(원장에 명시).

**기각**: ① 상태를 저장 필드로 — 계산과 어긋나는 낡은 상태를 만든다(D-L53의 사유 그대로)
② 일괄 풀이를 아예 삭제 — 첫 앵커(확정 순간)가 사라져 아무것도 안 올라간다
③ 연쇄의 재탐색(오스냅)을 기록보다 앞에 — 더 가까운 다른 후보가 사용자의 연결을 덮는다.


## D-L85 — 오스냅 기본 반경 8px (2026-08-18 10차 항목 4 — D-L56·D-L82 개정)

**지시가 근거를 지목했다**: `classify_leak.json`의 `osnap_arm.ablation` — 반경 15에서
끝점 오스냅이 깊이 판정을 뒤집는 모순 **72/2880**(1점 55/240)이 8에서 **35/2880**(31/240)로
절반 이하가 된다. 화면 획 미검출도 소폭 낫다(136 → 133/720). 사용자 조절(4~40px 입력)과
종류별 토글(라이노 방식)은 그대로다 — 바뀌는 것은 **기본값 하나**다.

**출처는 한 곳**(`resolve2d.OSNAP_RADIUS_PX`, #17)이라 앱·하네스가 함께 8이 됐다.
하네스 원장의 이동(원장을 그 자리에서 읽었다): `order_lock.fixed` **P0 122 → 88 ·
P1 65 · P2 447 · camera_ok 512/600 · 형태 오차 중앙 0.5869** · `p0_leak` leak 63/404 ·
too_short 206/404. **모순이 줄어 카메라가 더 서는 방향**으로 움직였다.
⛔ **제자리 정정(2026-08-18 12차 항목 1 재실행 · #42 ⑥⑨, 12차 리뷰어 [3])**: 화각 상한 등록 뒤 `arms.fixed.headline`은 **P0 97 · P1 83 · P2 420 ·
camera_ok 503/600 · 형태 오차 중앙 0.583**이었다(항목 1 시점) — 상한이 극단 소실점 쌍의
P2 확정을 거부해 P2가 줄고 P0·P1이 는 이동이고, 이 문단의 D-L85 서술(반경 15→8의
이동)과는 **별개의 커밋 사이 이동**이다. ⛔ **재정정(같은 세션 12차 항목 4 — D-L89(물음 제거) 재실행으로 값이 또 움직였다 · #42 ⑥)**: 같은 세션 항목 4(D-L89 — 물음 제거) 뒤
현행은 **P0 92 · P1 90 · P2 418 · camera_ok 508/600 · 0.5863**이다(`order_lock.json@f351839a`).

⚠⚠ **대가**(#16 — 함께 읽는다. ①~② 초판 + ③~⑥ 10차 리뷰어 3차가 잡은 것들):
① 연결을 놓치는 몫이 는다 — **본 팔**(고정 오프셋)의 `sweep.missed_rate` 29.1% → 50.6%(그 분모의 40px 창
절단 결함은 DEFERRED) ② 2D 단계의 조리개 병목이 더 나빠진다 — **동작점 8의 행을 읽는다**
(`snap_stage.d2_by_radius` r8: 발동 **381/1796**(21.2%) · 겨냥 중앙 **35.59px** ↔ r15:
576/1900·28.54px. ⚠ 초판이 28.5px(r15 행)를 들었다 — 9-R″ [8]이 잡았던 "조리개 독립량이
아니다"의 **재발**이라 정정한다, #34. ⛔ **제자리 정정(2026-08-18 12차 항목 1 재실행 · #42 ⑥⑨, 12차 리뷰어 [3])**: 12차 현행 행은 r8 발동 **381/1802** ·
`nearest_end_px.median` **34.75px** ↔ r15 **578/1906** · **28.36px** — 이 인용이 낡은
**세 번째**다(9-R″ [8] · 10차 [13] · 이번). 방향 불변) ③ **소실점 찍기 실패가 는다** — `pick_vp.pick_0`
`pick_failed` 44 → **70**/240(#29 — 사용자 조작의 실패) ④ **파국 배치가 는다** —
`order_lock.gate.result` `beyond_cut_0_5_fixed` 1965 → **2115**(정확 52 → 85와 함께 —
정확도 늘고 파국도 늘었다, #15·#16. ⛔ **제자리 정정(2026-08-18 12차 항목 1 재실행 · #42 ⑥⑨, 12차 리뷰어 [3])**: 12차 항목 1 시점 **2108 · 정확 89** · D-L89 재실행 후 **2133 · 정확 89** — 방향 불변) ⑤ **`rule_camera`의 위약 역전이 되살아났다 — 몰아 팔에서만이다**(리뷰어 4차 [10]) —
`rule_grouped_placebo` **6.7125** < `rule_grouped_snap` **7.3165**(9차 D-L80이 절반 해소한
그 #39 우려가 반경 8에서 되돌아왔다. DEFERRED의 그 행을 다시 열었다). ⚠ 같은 원장의 나머지
두 팔은 반대다: 번갈아 위약 **8.4515 > 스냅 7.5752** · 넓은짝 위약 **10.3657 > 스냅 8.3491**
— 거기서는 스냅이 위약을 이긴다. ⛔ **제자리 정정(2026-08-18 12차 항목 1 재실행 · #42 ⑥⑨, 12차 리뷰어 [3])**: 12차 현행(D-L89 재실행)도 같은 그림이다 — 몰아 위약
**6.6539 < 스냅 7.068**(위약이 이긴다) · 번갈아 위약 **7.5944 > 스냅 7.3134** · 넓은짝 위약
**10.1448 > 스냅 8.0867**. 판정 방향 전부 유지·수만 이동. 전 팔의 사실처럼 적지 않는다(#12 동작점 하나).
⚠ **이 위약 비교 전부가 비짝지음이다**(리뷰어 5차 [15] · #11 — `axes_measured`가 팔 간
최대 14축 다르고 `paired_headline`은 raw↔snap에만 있었다. ⚠ **11차 항목 5가 `paired3_headline`을 넣었고 판정은 같다**(몰아만 위약이 이긴다). ⛔ **다만 짝짓기가 걷어낸 픽스처가 1~3개뿐이라 그 층으로 '짝지음 부재의 산물이 아니다'를 못 세운다** — 리뷰어 [2] · 원장의 `medians_moved_by_pairing`) ⑥ 같은 원장에서
**r4(6.9304)·r40(6.9637)이 r8(7.3165)보다 낫다** — 축 오차 지표는 8을 안 지지한다
(**제자리 정정(2026-08-18 12차 항목 1 재실행 · #42 ⑥⑨, 12차 리뷰어 [3])**: 12차 D-L89 재실행 r4 **6.7929** · r40 **6.9124** · r8 **7.068** — 방향 불변)
(⚠ 0.35~0.39° 격차의 시드 폭은 미측정 — #14. 결정의 근거는 이 지표가 아니라 지시가 지목한
모순 지표다 — **"측정이 8을 지지한다"로 읽지 않는다**).
**놓친 연결은 이제 임의 배치가 아니라 대기다**(D-L83·D-L84) — 그 획은 방향 확정 상태로
남고 연쇄가 회수하므로, 축소의 실패 모드가 "조용히 틀림"에서 "명시적 대기"로 바뀌었다.
그것이 15 시절보다 축소가 덜 위험한 이유다.

**픽스처 정합**: 오조준을 옛 조리개에 맞춰 두었던 e2e 넷을 고쳤다(stage.spec 셋 —
10→7px·11,9→5,4·10,8→5,4 / snap_cache far_aim 단언 15 → 8 경계). `osnap_config`의
15·40은 **메커니즘 확인용 명시값**이라 그대로다(기본 복원만 8로).

**기각**: ① 반경별 이중 기본(시작 15·끝 8) — 두 값이 두 곳에서 갈린다(#17)
② 실획 대기 — 지시가 결정을 명시했고 실획 표본은 D-L79 이후 저장본이 아직 없다.

**되돌릴 조건**(⛔ **11차에 갈아 끼웠다** — 리뷰어 2차 [2]·[9]): ~~겨냥 분포 **중앙**이 8px을
크게 넘으면~~ **그 조건은 되돌림이 예정된 조건이었다** — 같은 양의 합성 값이 이미 조리개의
네 배대이기 때문이다(`snap_stage.d2_by_radius`의 r8 행을 그 자리에서 읽는다). 사전등록은
**참·거짓이 실제로 갈리는 것**이어야 한다(#12·#35).
👉 **현행 조건**: 실획 재수집(`snap2d` 필드 배포 뒤)에서 **`real_ink.snap_dist_px`의 `p10`이
조리개(`OSNAP_RADIUS_PX`)를 넘으면** 다시 연다 — **가장 잘 겨냥한 10%조차 조리개 밖**이면
그 값이 손을 부순다는 뜻이다.
- **도달 가능성**(#35 — 등록문 옆에 적는다): 같은 양의 합성 값은 조리개보다 **한참 아래**다
  (`osnap_two_sided.aim_px_when_connecting.p10` · `snap_stage.d2_by_radius`의 p10). 즉 이 조건은
  **발화가 예정돼 있지도, 원천적으로 불가능하지도 않다** — 실획이 합성의 서너 배로 흩어져야 참이 된다.
- **표본 크기 요건**: `snap_dist_px.n`이 **한 자릿수면 `p10`은 사실상 최솟값 하나**라 획 하나로
  뒤집힌다. **`n ≥ 30`을 함께 요구한다**(그 아래면 판정을 미루고 표본을 더 받는다).
- ⚠ 그 분포는 **40px 프로브에서 절단된다**(#13) — `snap_dist_null`을 함께 읽는다.

## D-L89 — "화면에 나란합니까, 안으로 들어갑니까" 물음을 지운다 (2026-08-18 12차 항목 4)

**결정**: `screen_or_depth` 물음의 두 자리 중 ① 애매 구간(4~8°)의 물음(site "ambiguous")을
**지우고 깊이선으로 확정**한다 — 스냅 없이 나온 대각선은 깊이선이다. ② P1 가드(site
"p1_guard")는 **스냅이 잡은 가로선(hint "screen")을 통과**시킨다 — 붙은 것은 묻지 않는다.
잔여 물음은 둘이다(3차 리뷰어 [4] 정정 — 초판 "수정자·토글 끔뿐"은 불완전):
① 스냅 파이프라인을 안 지난 가로선 ② 끝점 오스냅의 좌표 이동으로 가로가 된 원 방향
depth 획(D-L80 잔여 모순 — hint가 안 붙어 가드가 **잡는다**. 그것이 옳은 동작이다).
내역 칸은 없다 — DEFERRED.

**근거**(12차 지시 4): 이 물음은 성립하지 않는다 —
- 화면에 나란한 선은 **스냅이 잡는다**(직교 스냅 ≤ `screen_axis_deg`). 붙은 것은 묻지 않는다.
- 2D로 두려면 **주석 채널**로 그린다 — 채널이 이미 그 역할이다(D-3).
- 그러므로 스냅 없이 나온 대각선은 깊이선이다. D-L74("기울어진 선은 항상 깊이선")의
  4~8° 구간 확장이다.
- 실측(사람 보고): 문구만 바뀌고 물음이 남아 asked **3/13 · 3/11**이었다.

**D-L70·D-L80과의 관계** — 5차 "스냅이 곧 선언"(D-L56)을 7차가 지운 이유는 `snapForced`가
**원 방향이 depth인 획**의 좌표 이동까지 선언으로 만들어 P1 가드를 우회한 것이었다
(축 오차 중앙 10.1° → 31.6° 붕괴). 지금은 다르다: **kindFlipGuard(D-L80)가 원 방향
depth 획에는 직교 스냅 자체를 안 건다** — hint "screen"은 원 방향이 화면 축 대역(≤4°)인
획에만 붙으므로, 그때의 붕괴 기전(조용한 방향 뒤집기)이 이 문으로 못 돌아온다.
잔여 위험은 **≤4°로 얕게 그은 진짜 깊이 모서리**가 무물음으로 P1을 세우는 것이고,
그 수용은 지시문의 결정이다(전제: 공간과 투시를 이해하는 사용자 — CLAUDE.md).

**함께 바꾼 것**(4-c): 주석 채널은 2D 판정(방향·관계 스냅)을 안 지나고, 주석으로 바꾼
획은 축 라벨(`axis`)도 지운다 — 실획 보고에서 note 획(s52·s53)이 판정을 탄 것의 수리.
4-d(두 축의 화면 겹침 → 커서로 가름)는 `chooseAxis`의 모호 구간 규칙이, 4-e(자유 선의
커서 판정)는 `vpDirSnap`의 이동량 최소 선택(12차 항목 2로 무앵커 확정 경로에도 배선)이
이미 그 자리다 — 새 구현 없음.

**배선·회귀**: `stepRule`(vpRules.ts) 두 자리 · `snap_declare.json`(e2e — 계약 뒤집힘:
스냅된 1.7° 가로선이 무물음으로 P1 확정, 되살림 확인은 가드의 `hint !== "screen"` 제거) ·
`vp_rules.test.ts`(애매 → waiting·깊이 풀). 남는 물음 수는 원장을 읽는다(#47 —
`order_lock`의 ask 내역·`real_ink.ask`).

## D-L88 — 분리는 **조리개와 같은 단위**로 재야 하고, 작은 조리개의 잘못된 연결은 **중점 후보**가 만든다 (2026-08-18 11차 항목 3)

**지시 3-a**: *"새 픽스처가 1점·2점에서 반경 40에도 잘못된 연결을 못 만드는 것 자체를
결함으로 등재한다. 그 픽스처는 그 양을 못 잰다. 픽스처를 고치거나 지표를 바꾼다."*
그 결함은 이미 등재돼 있었다(`DEFERRED` 9-R″ [3] · D-L82의 철회 ①) — **이 항목이 고쳤고,
고치는 과정에서 기준을 두 번 틀렸다. 그 둘도 함께 적는다**(11차 리뷰어 [1]·[5]가 잡았다).

### 기준을 세 판 만들었다 — 앞의 둘이 왜 틀렸는가

| 판 | 분리의 정의 | 무엇이 틀렸나 |
|---|---|---|
| 1판 | **참 정점 최소 거리 > 40px** | 오프셋을 줄일수록 두 덩어리가 **화면에서 포개진다** — 실측 상자 겹침 0.12~0.80. 포개진 덩어리에 붙는 것은 "떨어진 곳에서 새로 시작한 획"이 아니다. `wrong`의 정의가 그 층에서 무너진다(#5) |
| 2판 | **경계 상자 비겹침** | 축 정렬 상자가 **회전한 상자에 과대**해서 3점 셋이 어떤 배율에서도 안 남았다(`n = 0` — 도달 불가로 되돌아갔다) |
| **3판(현행)** | **`min_cand_px` > 40px** — 둘째 덩어리 끝점에서 첫 덩어리의 **가장 가까운 후보**까지가 UI 최대 반경 밖 | 후보가 어떤 조리개로도 안 닿으므로, 붙는 것은 **잉크 지터가 손을 그만큼 움직인 경우**뿐이다. 그것이 사용자가 말한 "안 붙어야 할 곳에 붙는다"다 |

👉 **교훈은 하나다**: 분리를 **조리개와 같은 단위**(가장 가까운 후보까지의 px)로 재야 한다.
덩어리 사이 거리도, 상자 겹침도 그 대리로 **안 듣는다** — 둘 다 실측으로 떨어졌다.

### 팔 셋 (본 팔은 안 건드린다 — 인용된 값이 움직이지 않게)

- `by_composition_adjacent` — **인접 체제**: `min_cand_px`를 **최소화**(제약 없음).
  후보가 조리개 안까지 들어온 층이다.
- `by_composition_calibrated` — **분리 체제**: `min_cand_px` > 40px 중 최소.
- `midpoint_off_adjacent` — **개입 팔**: 인접 체제에서 **중점 후보만 끈다**(#30 · #43).
  앱에 이미 있는 종류 토글(`OSNAP.kinds.midpoint`)이고 새 스위치가 아니다.

### 무엇이 나왔나 (값은 `osnap_two_sided.json`을 그 자리에서 읽는다 — **#47**)

1. **도달 가능성은 열렸다.** 분리 체제에서 **다섯 구도 전부** `wrong`이 난다
   (`calibrated_unreachable_compositions` 빈 배열 · 게이트 조항 ③이 단언한다).
   ⚠ **그러나 앱의 동작점(r8)에서는 1점·2점이 여전히 0이다** — 도달은 r25~r40의
   **지터 꼬리**에서 난다(11차 리뷰어 [2]). **그 층의 비용은 여전히 못 매긴다.**
2. **2점은 다르다.** 훑은 어떤 배율에서도 `min_cand_px`가 40px 안으로 안 들어온다 —
   즉 2점 구도에서는 **후보가 둘째 덩어리 쪽으로 안 뻗는다.** 그 층의 낮은 누수는
   (이 픽스처 계열 안에서는) **오프셋 선택의 산물이 아니라 기하의 성질**이다.
3. **작은 조리개의 잘못된 연결은 중점이 만든다.** 인접 체제 r8의 `wrong_kinds_r8`이
   **중점 지배**이고 끝점은 미미하다. 분리 체제에서는 반대로 **끝점만 남는다** —
   중점·교차점 후보가 그 거리까지 안 뻗기 때문이다.
4. **개입이 그 귀속을 확인한다.** `midpoint_off_adjacent`는 같은 배율·같은 잉크·같은 반경
   축에서 **종류 토글 하나만** 다른데, r8의 `wrong`이 네 구도에서 크게 준다(2점은 불변 —
   애초에 중점 누수가 없다). **관측(지배)과 개입(제거)이 같은 방향이다.**

⛔ **초판의 표제 주장은 철회한다**: *"순서를 정하는 것은 `min_cand_px`다."* 5점 위의
교락된 상관이고 **단조가 아니다**(11차 리뷰어 [4]). 남는 것은 위 1~4이고, 그중 3·4가
개입을 가진 유일한 인과 주장이다.

### 한계 (함께 읽는다)

⚠⚠ **값의 크기를 반경 결정의 근거로 인용하지 않는다**(#46 · #5) — 두 보정 팔의 크기는
**픽스처가 고른 인접도의 귀결**이다. 싣는 정보는 도달 가능성 · 반경에 대한 단조성 ·
**두 체제의 대조**다.
⚠ **구도 간 크기 비교는 짝지은 층이 아니다**(#11 · #24): 배율도 `min_cand_px`도 구도마다
다르다. 초판이 정보로 적었던 "1점이 3점보다 이르게 샌다"는 **자기 원장이 반증한다**
(인접 팔 r8에서 1점 < 3pt_yaw50). 원장에 그렇게 적었다.
⚠ **시드 폭 미측정**(#14) — 2점의 r25·r30 같은 한 자릿수 칸은 결론에 안 쓴다. 게이트 ③의
도달값도 **r40 단일 칸**이다.
⚠ **`wrong`은 여전히 픽스처 규약이다**(#5) — 사용자가 두 덩어리를 잇고 싶을 수도 있다.

### 기본 반경은 안 바꾼다

지시 3 머리("반경 8이 D-L80 모순을 72→35로 줄인다는 원장이 근거다 … 줄이는 쪽으로 간다")는
**D-L85가 이미 이행했다**(15 → 8). 이 항목의 측정은 그 결정을 뒤집지 않고, **강화하지도
않는다** — 분리 체제 r8의 누수는 세 구도에서 한 자릿수~열두 건이고 1점·2점은 0이다.
**새로 올라온 지렛대는 반경이 아니라 후보 종류다**(위 3·4). 다만 **중점을 끄자고 하지
않는다.** ⚙️ **편익과 대가를 같은 픽스처에서 잰다**(`sweep_midpoint_off` — 11차 리뷰어 2차 [7]로
고쳤다: 초판은 **편익을 인접 보정 팔, 대가를 본 팔 픽스처**에서 재고 그것을 "같은 축"이라
적었다. #27의 픽스처 판이다). 현행은 **본 팔 픽스처 하나에서 두 열을 다 낸다** —
`wrong`(편익)과 `missed`(대가)가 같은 반경 축 위에 있다. ⚠ **"같은 분모"는 `wrong`에만
참이다**(리뷰어 2차 [8] · #11): `missed`의 분모는 스냅 결과에 따라 달라져 반경 20 이상에서
두 팔이 2~5 다르다.
👉 **그 표가 말하는 것**: 잘 떨어진 본 팔 픽스처에서 중점을 끄면 **잘못된 연결이 줄고 놓친
연결이 그만큼 는다** — 앱 동작점(r8)에서는 거의 **맞바꿈**이다. 인접 체제에서 보이던 큰
편익은 **그 체제의 값**이지 이 픽스처의 값이 아니다. 값은 원장을 그 자리에서 읽는다(#47). ⚠⚠ **그런데 그 열이 중점의
효용을 구조적으로 과소평가한다**(#11·#5): 연결 팔의 분모는 **이미 그린 획의 끝점을 겨냥한
시작점**이라 **중점을 겨냥한 연결이 모집단에 아예 없다.** 그러므로 그 열이 무엇을 보이든
**"중점을 꺼도 된다"의 근거가 안 된다** — 중점을 겨냥하는 조작을 픽스처에 넣는 것이
선행이다(DEFERRED).

### 3-b·3-c·3-d 기록

**3-b**: **`snap.json`의 `unintended_snap`이 먼저 같은 쪽을 재고 있었다.** 캔버스 균등 난수
24점/픽스처에서 스냅이 끼어드는 비율이고 반경 축에서 단조 증가하며 **0인 칸이 없다**.
즉 "잘못된 연결"의 측정이 없었던 것이 아니라 **이 픽스처가 그 층에서 도달 불가였다.**
두 지표는 질의 분포가 다르다 — 전역 난수 ↔ **사람이 새 덩어리를 시작할 자리**.

**3-c**: 반경 조절(4~40px)과 종류 토글은 **이미 있다**(`data-osnap-radius`·`data-osnap-kind`,
라이노 선례 — A-3). ⚠ **UI 토글은 일곱이지만 2D 단계에서 실제로 가르는 것은 다섯**이다
(`on_edge`·`on_face`는 2D에서 안 난다 — DEFERRED의 9차 정정, 11차 리뷰어 [12ⓐ]).
이 항목이 한 것은 낡은 주석 하나 제거다(`OSNAP.radiusPx`의 "D-L56 15px" — **#47**).

**3-d — 수직점 팔이 픽스처 보장이었다**(#46의 재발): 기능은 있었는데
(`perp2dCandidates` · `test/perp2d.test.ts` 다섯 건) **e2e 팔이 판별을 못 했다** — 수선의
발을 **선분 중점**에 두고 `["perpendicular", "midpoint"]` 중 아무거나 받아서 **수직점이
안 돌아도 통과**했다. 발을 **t = 0.3**으로 옮겨 `perpendicular`를 정확히 요구한다.
⚠ **가르는 것은 존재이지 순위가 아니다**(11차 리뷰어 [10]): 중점이 조리개(8px) **밖**
이므로 이 팔은 "수직점이 돈다"를 가르지 "수직점이 중점을 이긴다"를 가르지 않는다.
`perp_foot_px`는 픽스처가 정한 값이라 `_fixture_determined`로 표시한다.

**기각**: ① 본 팔의 오프셋 갈아 끼우기 — 인용된 값이 전부 움직여 #42 ⑥의 비용을 만든다.
팔을 더하면 **같은 실행 안의 대조**가 된다(#30) ② 중점 토글의 기본값 변경 — 대가(놓친
연결)를 이 원장이 안 잰다 ③ 보정 팔의 값으로 기본 반경 재조정 — 인접도 동작점 하나다(#12).

## D-L86 — 그리는 중의 두 손가락은 종이를 민다(화면 팬), 궤도 뒤에는 공간을 민다 (2026-08-18 10차 항목 5)

**지시 문면 그대로**: "그리는 중에는 화면 팬(뷰 오프셋), 궤도 후에는 공간 팬."

**구현은 표시 오프셋 하나다**: `stage.viewPan`(css px). 핀 상태에서 두 손가락 이동은
카메라를 **열지 않고**(핀 불변 — `camGesture.screenPan`이 `open()` 앞에서 받는다)
오프셋만 쌓는다. 표시 두 층이 같은 값을 쓴다 — 잉크는 `InkCanvas.viewOffset`(redraw의
translate), 3D는 `setViewOffset` 창의 주점 이동(`pinTo` 훅 — project = principal + f·x/z라
주점 이동 = 전 투영의 평행이동). 입력은 **같은 훅으로 오프셋을 되돌려** 문서 좌표를
만든다 — `pts2d`에 오프셋이 절대 안 들어간다(§6.1 좌표계 단일성).

**`Camera.principal`은 안 건드린다**(#37 — 리뷰어 4차 [8]): 주점은 **복원의 값**이다 —
2점의 f²=|PV₁||PV₂|(이론서 6.2)와 3점의 주점=수심(6.3), 그리고 2점 경로의 주점=중심
**가정**(16.2)이 전부 **문서 좌표의 주점** 위에 서 있으므로, 표시 팬이 그 값을 옮기면
카메라 해 자체가 바뀐다. viewPan이 옮기는 것은 **three 표시 창의 주점**
(`pinTo`의 `principal+viewPan`)뿐이고, `view_pan.json`의 `screen_pan.principal_moved 0`이
그 불변의 실측이다.

⚠⚠ **카메라 제스처는 표시 좌표를 쓴다**(`localRaw`) — 입력 훅에서 빼면 팬이 쌓일수록
보고 좌표가 뒤로 밀리는 **되먹임**이 생긴다(view_pan 팔 개발 중 실측: 60px 손짓이 32px만
쌓였다). 카메라 전(P0)에도 화면 팬이 돈다 — 종이 밀기는 3D가 없어도 뜻이 있다.

**측정**(`view_pan.json` 신설 · 리뷰어 4차 [6][8][9]로 팔 셋 추가): **카메라 전(P0)
두 손가락 → viewPan 축적·카메라 불개방** · 핀 상태 두 손가락 (60,40) 뒤 — 핀 유지 ·
pose 이동 0 · 문서 이동 0 · **`Camera.principal` 이동 0** · viewPan (60,40) ·
**three 투영 이동 = viewPan(±0.5px — 두 층이 같이 민다)** ·
밀린 화면에서 끝점의 표시 위치를 겨냥한 획이 문서 끝점에 스냅(끝점/정점 · 격차 <1px —
⚠ 이 격차는 스냅이 저장 좌표를 돌려주므로 되돌림의 **성패**이지 정밀도가 아니다.
정밀도는 **스냅 없는 팔**이 잰다: 빈 자리 표시 겨냥 획의 `pts2d` = 표시 − viewPan, <1px) ·
궤도로 푼 뒤 두 손가락은 target을 옮기고(공간 팬) viewPan은 불변(음성 대조).

**미룬 것**(DEFERRED): ~~핀 상태의 핀치(화면 줌 — 표시 배율은 스냅 조리개의 화면 px 정의
전반에 닿는다)~~(⛔ **14차 D-L94로 닫힘** — 화면 줌 신설) · 화면 밖 소실점 가장자리 표식의
클리핑이 팬을 모른다(표시 결함 — 줌 포함, DEFERRED 잔여 행).

**기각**: 핀 해제 후 팬 재적용(공간 팬으로 흉내) — 카메라가 움직여 §5.4의 확정 시점
불변이 깨진다. 문서 좌표 이동 — 좌표계가 둘이 된다(§6.1이 금지한 그것).

## D-L87 — 큐브의 기준 좌표계는 그린 공간의 축이다 (2026-08-18 10차 항목 6 · 7차 지시 8)

옛 큐브는 three 세계 축이 기준이라 2·3점 구도에서 **면 탭이 그린 상자의 입면이 아니라
카메라의 초기 방향으로** 갔다. 이제 기준계는 `axisDirs`(그린 축 — 카메라가 서는 순간
고정)이고, **도는 것은 자세뿐이다**(지시의 "자세가 도는 것과 기준계가 도는 것을 구분한다").

**변환은 경계 한 곳이다**(#17): `mainL`의 큐브 호스트 — `basis`는 카메라 기저를 그린 축
좌표로 내리고(`cubeDown`), `snap`은 큐브 좌표를 세계로 되올린다(`cubeUp`). 그린 축은
그램-슈밋으로 직교화하고 +z는 X×Y(오른손 — 반사를 안 만든다). **P1에서는 항등**이라
옛 거동·옛 팔들이 그대로 남는다(view_cube 팔 전부 통과가 그 확인). 상대 회전(spinYaw)은
기준계 무관이라 안 건드린다. 빈 곳 탭의 "가장 가까운 1점"도 자동으로 그린 축 기준이 된다
— `onePointFrame(axisDirs)`의 직접 좌표 경로와 **이제 같은 축을 본다**(전에는 어긋났다).

**측정**(`cube_frame.json` 신설): 3점 구도(요 35°·피치 15°) 정면 탭 뒤 |시선·그린 깊이축|
**>0.99** · |시선·세계 z| **<0.95** — 판별은 **옛 기준계 팔**(`setCubeFrame(false)` 실행 —
리뷰어 4차 [1])이 든다: 거기서 두 값이 실측으로 자리를 바꾼다. 판별 간격은
**cos35°·cos15° ≈ 0.7912**(⛔ 초판의 "cos35° = 0.82"는 피치를 빠뜨린 구성 오류 — [11] 정정 ·
리뷰어 5차 [2]가 이 문서의 미전파를 잡았다). ⚠ **값의 크기는 픽스처 상수다**(5차 [1] · #40) —
정보는 자리바꿈(정확히 1이 어느 슬롯에 오는가)이고, 크기가 정보를 실으려면 요·피치를 흔든
둘째 구도가 필요하다 — ⛔ **11차 항목 5가 넣었고(요 20°·피치 25°) 그 조건이 틀렸음이 드러났다**
(리뷰어 [6]): 옛 배선의 시선이 세계 −z인 한 그 내적은 깊이축의 −z 성분이라 cos(요)·cos(피치)와
**같을 수밖에 없다**(일치 1e-16 = 항등). 둘째 구도가 잡는 것은 **픽스처 매개변수화의
자기확인**이고, **크기는 이 팔에서 원리적으로 정보를 못 싣는다.** 재는 것은 그대로 자리바꿈이다.

**기각**: 큐브 렌더 자체를 그린 축으로 다시 그리기(면 라벨 회전 등) — 표시는 이미 basis가
정하므로 변환 하나로 충분하다. 세계·그린 축 토글 — 사용자에게 두 기준계를 노출하는 것은
§9.1의 "좌표계 하나" 정신에 반한다.


## D-L90 — 첫 앵커는 지면 배치다: 카메라를 세운 수평 평면 축 보조선을 확정 순간 지면에 놓는다 (2026-08-19 13차 항목 2)

**무엇**: 카메라가 서는 순간(확정), 일괄 풀이가 아무것도 못 올렸고 문서에 3D가 하나도
없으면, **수평 평면 축(0·1) 대기 획을 지면(눈높이 게이지 아래 평면)에 놓는다**
(`groundAnchor.ts`의 `groundSegment` — 광선 × 지면 교점, 재투영은 잉크 그대로).
경로 카운터 `placeBy.ground` · 측정 스위치 `S2S.setGroundAnchor(false)`(#30 — D-L83 선례).

**왜**: 실획 15-16-18(13획·카메라 섰는데 seg3d 0)이 판 구멍 — 보조선을 길게 긋고 가운데서
가로지르는 스타일에서는 일괄 풀이의 접합(끝점 근접)이 한 쌍도 안 잡힌다
(`first_anchor.json@f351839a`: joints 0 · 몸통 교차 7). 지시 2-a의 근거 그대로:
**소실점을 세우는 보조선은 바닥 격자다** — 지면 가정이 정당한 첫 앵커를 만들고,
이후는 교차 앵커·오스냅 연쇄가 좌표를 잇는다(2-c — `ground_anchor.json` 종단).

**발동 조건 둘**(둘 다이어야 한다): ① 일괄 풀이 배치 0 ② `lifted(doc).length === 0`
(**첫 앵커 전용** — 10차가 실측으로 막은 "떠 있는 성분의 임의 지면 배치"(D-L84 ③)를
되살리지 않는다. `ground_anchor.json`의 허공 팔이 그 회귀를 잰다).

**대상**: 수평 평면 축(0·1)만(`groundEligible`) — 수직·미분류는 연결이 정한다.
⚠ 지시 2-a의 "소실점을 만든 두 선"보다 **넓은 집합**(확정 시점의 수평 평면 축 대기 획
전체 — 15-16-18에서 기여 선 2 대 지면 배치 `ground_recovery.ground_placed`, 값은
`first_anchor.json@f351839a`을 읽는다)이다 — 좁히기의 재료(규칙 상태의 depthLines 잔재·좌표 대조)는 있으나 확정 시점에
소비돼 있어 배선이 더 필요하고, 넓힘의 비용(전제가 틀린 획의 조용히 틀린 깊이)은
`first_anchor.json@f351839a`의 `ground_truth.offplane` 필드가 든다 — 깊이 1/(1−h/눈높이)배·
**방향 보존**(그래서 방향 지표에 안 나타난다). AS-L42가 이 전제를 든다. 좁히기는 DEFERRED(그 비용이 실사용에서
확인되면).

**잠정**(A-3 — 실사용이 판정): **지면 거리 상한을 두지 않았다** — 지평선 근처 끝점은
원거리로 배치된다. 옛 씨앗(`seedOnGround`)에도 상한이 없었고(선례), 상한이 만들
"조용한 미배치"가 대기보다 나쁘다고 봤다. 실사용에서 원거리 배치가 문제로 보고되면
이 잠정이 판정 대상이다.

**기각**: ① 모든 수평 획 상시 지면 배치 — 10차가 막은 임의 배치의 부활
② 발동을 소실점 기여 획 2개로 좁히기 — 위 "대상" 문단(식별 배선 비용 + 2-b의 화면
가로선도 대상이어야 함) ③ 지면 거리 상한 — 위 잠정.


## D-L91 — 연장선 스냅: 끝점에서 원 선의 방향 그대로 바깥이면 같은 3D 직선 위에 놓는다 (2026-08-19 13차 항목 3)

**무엇**: Rhino **Extension** 오스냅·SketchUp **선 위 연장** 추론의 이식(A-3 선례).
발동(좁게 — 지시 3-a): ① 시작점이 어느 3D 획의 **끝점·정점에 스냅**됐고(조리개 안 —
끝점 스냅이 앵커를 먼저 잡는 것이 우선순위 3-f의 구현이다) ② 현의 진행 방향이 그 선의
상(2D)의 **바깥 연장 방향**과 `LIFT_TOL.parallel_deg`(5°) 안일 때. 그러면 새 획은
원 선의 **무한 3D 직선 위**에 놓인다(`extension.ts` — 양 끝을 `endFromCursor`로,
좌표 즉시 확정 · 지시 3-c). 경로 `placeBy.extension` · `pathStats` 밖(축 경로 아님) ·
`SnapKind "extension"`(우선순위: 수선 발 다음·근처점 앞 — 토글 목록 자동 편입 3-g) ·
표시는 무한직선 표현 재사용(파선 — Rhino 관행, 발동 시 진해짐 3-d) ·
미리보기·확정·연쇄가 같은 판정을 지난다(#17 — `resolveLive` 안의 한 갈래).

**왜**: 첫 앵커(D-L90)·교차 앵커(12차)에 이은 **셋째 연결 수단**이고, 축이 아닌 방향
(면 위 대각선·자유 세그먼트)을 이어 그리는 **유일한** 수단이다(지시 3-b).

**임계는 재사용이다**(#17 — 전역 해시 불변): 나란함 판정 `LIFT_TOL.parallel_deg` —
`resolveLive`의 양 끝 스냅 라벨 판정이 쓰는 그 값이다. **단축 상 가드**: 원 선의 상이
`OSNAP_RADIUS_PX`보다 짧으면(시선과 나란한 선 — 입면의 깊이축) 방향이 잡음이라 발동하지
않는다 — 입면 흐름 e2e가 실측으로 요구한 가드다.

**우선순위 실측**: 양 끝 스냅(두 점이 기하를 정함) > 연장선 > 축 스냅. 그래서 기존 입면
흐름의 "이어 그리기"(같은 축 선의 연장)가 direct/lift 대신 extension 경로로 놓인다 —
⚠ **"기하는 같다"는 정확 겨냥 원 선에 한한 문장이다**(13차 항목 3 리뷰어 [5] 정정 —
어긋난 원 선에서는 연장이 **원 선을 따르고**(`ext_vs_orig_deg` 0) 축 경로는 축을 강제해
그 어긋남만큼 갈린다(`orig_off_axis_deg` 3에 `ext_vs_axis_deg` 2.9417 — 정확히 같지는
않다: 잔차는 사영 몫, 4-R [A-2] 정정): `extension_snap.json@f351839a`의 divergence_arm.
그 갈림이 이 기능의 정의다 —
같은 3D 직선(3-c) · AS-L41과 같은 방향 · '옳음'은 원 선에 상속·AS-L43).
`basic_flow` 네 입면 팔의 경로 단언을 그 합으로 고쳤다.
⚠ **단축 상 가드의 임계는 전역 해시 밖이다**(오스냅 조리개의 예외 규약 그대로 — D-C4) —
실행 시점 값·동작점 스윕은 `extension_snap.json`의 thresholds·guard_sweep이 스스로 든다
(리뷰어 [4] — 실획 저장본의 "기록 시점 조리개 미상" 결함을 새 원장이 반복하지 않게).

**측정**: unit `extension_snap.test.ts`(발동·공선성·임계 경계(parallel_deg±)·안쪽 배제·퇴화) ·
e2e `extension_snap.json`(발동 +1·공선성(배선 확인 — #5 보장이라 게이트 조건 아님,
값은 `arms.collinear`·`diagonal_arm.collinear_dev`. 4-R [A-1]로 "공선 1e-6"을 여기서도
내림)·**토글 끔 폴백**(#30 대조) — 값은 arms).

**기각**: ① 새 각도 임계 신설 — #17 위반 + 전역 해시 이동(원장 전량 재실행 비용)
② 시작점을 연장선 위 임의점으로(끝점 앵커 없이) — 발동 조건이 넓어져 다른 스냅과 싸운다
(지시 3-a "좁게"의 반대) ③ 2D 단계(카메라 전) 연장 후보 — 이번 범위 밖(지시 3-c의 3D
의미가 핵심 — 2D 후보는 DEFERRED).

## D-L92 — 확정 후 배치는 그은 현을 나란함 임계 안에서만 돌릴 수 있다 (2026-08-19 14차 항목 0)

**잠정 결정.** 원칙(14차 지시 머리말): 그리는 도중 스냅은 사용자가 보고 받아들인 것이고,
확정 후 재계산은 사용자가 안 본 변경이다.

- **미리보기와 확정의 앵커 자격을 하나로**(`anchorQualified` — mainL): 옛 판은 미리보기
  (`onLive`)가 자격 검사 없는 앵커(on_edge·on_face)로 3D 축 선을 보였는데 확정(D-L83)은
  그 앵커를 거부했다 — "미리보기는 붙는데 확정이 다르다"의 구조적 원인. 표식은 그대로 둔다.
- **승격 연쇄·교차 앵커의 되쓰기 가드**(`CHAIN_DIR_GUARD`): 연쇄가 대기 획을 축 경로로
  놓을 때 `pts2d` 되쓰기(D-L71)의 현 회전(`chordTurnDeg`)이 **`LIFT_TOL.parallel_deg`
  (5° — 나란함 판정 재사용 #17)**를 넘으면 놓지 않는다(대기 유지 §9.1). 축 스냅은
  "언제나 어느 축으로 간다"(각도 무제한)인데, 그리는 중에는 미리보기가 그것을 보여
  주지만 연쇄 시점에는 아무것도 안 보인다 — 각도 무제한 배치는 그리는 중에만 허용된다.
  ⚠⚠ **임계 선택은 리뷰어 두 라운드가 다퉜고 어느 등록 상수도 이 물음("안 보인 채
  얼마나 돌려도 되는가")의 실측값이 아니다**: 1차 [1]은 12°(`LIVE_TOL.axis_deg` —
  "축으로 인정")를 요구했고, 2차 [4·5]는 그 교체가 사용자가 못 본 회전의 허용치를
  2.4배 넓힌 것(원칙과 반대 방향)이라 기각했다. A-3(애매하면 놓지 않는다 — 미배치는
  대기라 비용이 낮다)대로 **엄격한 쪽(5° — 연장선 발동과 같은 "같은 방향" 판정)**으로
  둔다. 5~12° 대역의 실제 발생 빈도(분자/분모)는 **미측정**이고 실획 표본의
  `chainDirGuardStats.rejected/attempts`가 판정자다(DEFERRED 등재).
  실측 짝 대조는 `dir_state.json@f351839a`의 `chain_dir_guard`(끔 = 회전 배치 — 크기는
  픽스처가 정하므로 #46 근거로 안 쓴다, 켬 = 대기·좌표 불변·기록 되물림. 판정은 lifted
  범주다). ⚠ **교차 앵커 경로의 가드는 아직 발화 실측이 없다**(rejected_dir 0/…, #32·#35
  — 그 경로에서 도는지는 미실측. DEFERRED).
  교차 앵커는 분류(`cam.axisOf` — angleWiden·rank_margin 완화 포함)가 축을 고르므로
  **라벨 판정이 형태를 바꾸는 유일한 자리**였다(지시 0-b) — 같은 가드가 막는다.
- **기각한 대안**: 연쇄 배치를 광선 배치(liftAll식)로 바꿔 pts2d를 안 건드리는 것 —
  앵커의 정확한 3D 점과 광선 배치를 양립시키려면 축 아닌 방향을 허용해야 해서 축 배치의
  의미가 사라진다. 임계 가드가 단순하다(A-3).
- 되돌릴 조건: 실획에서 임계(`LIFT_TOL.parallel_deg`) 안 정렬이 체감으로 어긋나거나,
  대기 잔류가 과도해지면(`chainDirGuardStats.rejected/attempts` 비율이 판정자 — 특히
  5~12° 대역의 몫) 임계·방식을 재검토한다.
  측정 스위치 `S2S.setChainDirGuard(false)` = 옛 거동(#30).

## D-L93 — 화각 상한을 제거한다: 거부는 f² ≤ 0뿐이다 (2026-08-19 14차 항목 1)

**12차 지시 1-c(D 없음 — `FOV_GATE.reject_fov_deg = 120°` 신설)를 지시가 스스로 되돌렸다.**
14차 지시 1: "내 12차 지시가 틀렸다. reject_fov_deg = 120°는 임의 기준이었고 근거가 없다.
거부는 f² ≤ 0만이다 — 수학적으로 카메라가 존재하지 않는 경우. 물리적 한계이고 그 위로는
전부 유효하다. 극단적으로 넓게 그리는 것은 결함이 아니라 선택이다."

- **제거한 것**: `FOV_GATE.reject_fov_deg`(코드) · fovGate의 상한 거부 가지 ·
  화각 경고 표시(mainL feedStroke의 vp_fixed·fov 알림 — 지시 1-b: 결과는 돌려보면 즉시
  보이고, 상태 표시를 없앤 결정과 일관). 대역(ok/warn/severe)은 **참고 진단**으로만
  남는다(지시 1-d — 이론서 18.4는 판정이 아니라 참고. camera.ts의 `gate()` 판정도 확인:
  constraints.ts 경고에만 흘러가고 그 경고는 UI에 안 나온다 — 이미 참고였다).
- **해시 안정**: `reject_fov_deg`는 `SHARED_CONSTANTS`에 **동결 리터럴**로 남는다(D-L51·
  DRAFT_TOL 선례 — 키를 빼면 무관한 원장 60여 개가 STALE. 코드는 그 값을 안 읽는다).
  전역 해시 f351839a 불변 — 진짜로 낡은 원장(confirm_rules·delta_conflict·real_ink·
  first_anchor·vp_rules 배선 팔)은 이 항목이 재실행·재작성했다(#42 ⑥).
- **파급 정리**: 12차의 `reject_blocks_confirmation` 팔은 `extreme_fov_confirms`(166°가
  선다)로 반전 · f²≤0 차단은 `f2_negative_blocks` 신설 팔이 든다 · 13차 refeed_gate
  reject → severe(확정 통과) · **DEFERRED "복원 경로가 화각 상한 밖" 행 닫힘**(복원↔신규
  확정 비대칭 소멸 — 상한이 없으니 같은 조건이다) · real_ink의 fov_ok 분포 절단은 앱
  게이트가 아니라 **분석 층화 상수**(STABLE_FOV_CUTOFF_DEG 120, 하네스 지역)로 남는다.
- **판정 지표 변경의 사유**(#28·#26): 지시문 자체이고, **이론서가 같은 결론을 직접
  든다**(1-R [12] · #37): 8.8 — "사다리꼴은 틀리지 않는다. 다만 함의하는 시거리가
  비현실적일 수 있고, 도형만 보아서는 그것을 판단할 단서가 없다" · 18.5 — "어느 것도
  옳지 않고, 목적에 따른 선택만 있다". 즉 화각의 상한은 기하가 아니라 취향의 자리다.
  되돌릴 조건: 극단 화각 확정이 실사용에서 조용히 틀린 배치를 양산한다는 실측이 나오면 —
  그때도 상한이 아니라 다른 수단(예: 시점 복귀 보조, 8.8이 든 "원을 그려 판단")을 먼저
  검토한다(넓은 화각은 의도일 수 있다는 전제 유지).
- ⚠⚠ **전역 해시 f351839a는 이제 두 거동을 덮는다**(1-R [5] — #1의 "자동이 안 잡는 것:
  알고리즘 변경"이 이 자리다): 12~13차 원장은 상한이 **거부하던** 실행의 산물이고 14차
  이후 원장은 아닌데 constants 스냅샷이 같다(동결 리터럴의 대가 — D-L51 선례와 달리
  대상이 소멸한 게 아니라 **값이 남고 의미만 죽었다**). 구분자는 **원장의 생성 커밋**
  (git)과 이 결정문이다 — fov 판정을 딛는 원장은 confirm_rules·delta_conflict·real_ink·
  first_anchor·vp_rules·order_lock·rule_camera·camera_gate뿐이고(fovGate 소비자 grep)
  전부 14차가 재실행했다(그 외 원장은 fovGate를 안 지난다 — 값 불변을 재실행 diff로
  확인, 항목 1 후속 절).

### D-L92 후속 — 합성 대가의 실측 (14차 항목 0 검증, e2e 전량 재실행)

`stage_browser.json`의 `promote_chain` 팔(상자 둘 · medium 잉크)에서 가드가 연쇄 배치를
크게 줄였다: **lifted 16 → 8 · dir_guard rejected 21/24**(그 원장 `l_d3.promote_chain`을
그 자리에서 읽는다). ⚠ **상한값으로 읽는다** — 이 픽스처는 획을 `doc.strokes`에 직접
밀어 넣어 **그리는 중 2D 방향 스냅(resolve2d)을 우회한다**: 앱 경로에서는 소실점 임계 안
겨냥이 그리는 중에 정확히 정렬되므로 연쇄 되쓰기가 항등에 가깝다. 그 밖(원시 손떨림
2~6°)이 가드에 걸리는 몫이고, 실사용에서 그 몫이 얼마인지는 실획 `chainDirGuardStats`가
판정자다(DEFERRED 그 행). 나머지 e2e 원장 검증: elevation_flow·extension_snap·
ground_anchor·p1_invariance·promote는 **재실행에서 무변화**(git — 카운터 불변),
basic_flow 등 일곱은 부동소수 끝자리·시간 잡음뿐. e2e 49통과·5건너뜀·2실패(등재된 환경
실패 둘 그대로 — touch_route dpr2·static_deploy).

## D-L95 — fovGate의 주점은 [W/2, 지평선 y]다: h² 벌점 제거·f²≤0 문구 재작성 (2026-08-19 14차 항목 2)

**6차 리뷰어가 넣은 h² 항을 뒤집는다.** 그 항은 "주점 = 화면 중앙 [W/2, H/2]" 가정 아래
지평선과의 세로 어긋남 h를 f²에서 깎았다 — 그래서 지평선이 화면 중앙에서 멀면 카메라가
거부됐고, 안내문이 "지평선이 화면 중앙에서 너무 멀지 않게"를 조건으로 말했다.

- **근거(지시 2-a·2-b)**: 2점 투시는 피치 0이고 **주점은 지평선 위에 있다** — 주점 y는
  지평선이 주는 값이지 화면 중앙이 아니다. 지평선 높이는 시선 높이(구도 선택)다.
  **앱의 실제 카메라가 이미 이 규약이었다**: `ConstraintAccumulator.solve`가 주점을
  [W/2, 지평선 y]로 세우고 `real_ink` per_doc 유도도 같다 — fovGate만 달라서 판정과
  카메라가 어긋났고(#17·#24), delta_conflict가 그 어긋남을 `principal_convention`으로
  병기하고 있었다. 이 수리로 규약이 하나다 — 수렴한 쌍(2-R [4] 정정): refeed_gate
  **166.05 → 163.18**(per_doc 163.18과 일치) · real_ink_reported **102.43 → 98.75**
  (per_doc 98.76과 반올림 차). 배선은 실행으로 잠갔다(2-R [9] —
  `principal_on_horizon.wiring`: 같은 입력에서 앱 카메라 경로(fFromTwoVps + [W/2,
  지평선 y])와 fovGate의 f가 같다).
- **남는 가정**: 주점 **x = W/2**(이론서 16.2 · AS-C5 — x만 남도록 제자리 갱신했다).
  ⚠⚠ **지시 2-a의 "x는 두 소실점에서 나온다"는 자유도상 이행 불가다**(2-R [8] 명시 —
  16.2: 두 소실점만으로는 f² = |PV₁||PV₂| **하나의 방정식에 미지수가 둘**(x, f)이라
  x가 결정되지 않는다. 다음 세션이 원문을 재시도하지 않도록 여기 적는다 #23).
  그 귀결로 **지시 2-c의 "한쪽 몰림 정상"도 온전히는 못 연다**(2-R [1]): x = W/2를
  유지하는 한 두 소실점이 **둘 다 같은 쪽**인 구도는 원리적으로 f² ≤ 0이다. 연 것은
  "크게 치우쳤으되 W/2를 사이에 두는" 비대칭까지다 — 진짜 같은쪽 몰림은 x를 그린
  기하에서 가져오는 별도 설계가 필요하다(DEFERRED 등재). f² ≤ 0 = 두 소실점이 그 x의
  같은 쪽 = 직교하는 두 가로 축이 아니다.
- ⚠ **회귀 팔의 "지평선 높이 통과"는 측정이 아니라 설계 보장이다**(2-R [2] · #5):
  새 식에 지평선 y가 아예 없다 — 상단·하단 팔의 f가 같은 것이 그 표시다. 정보를
  나르는 것은 옛 식 되살림 `f2_old_formula`의 **부호**뿐이고, 그 값은 하네스가 옛 식을
  다시 쓴 (나)형·픽스처 상수다(#46 — 표시 필드 병기).
- **문구(지시 2-d)**: 화면 중앙·지평선 위치를 조건으로 말하지 않는다 — "두 소실점이
  시선의 같은 쪽에 모여 있습니다 … 서로 반대쪽으로 물러나야 합니다"로 교체
  (constraints.ts의 사문 경고도 같은 방향으로).
- **회귀 팔(지시 2-e)**: `confirm_rules.json`의 `principal_on_horizon` — 지평선 상단
  (y 5%) 구도 통과(옛 식 되살림 `f2_old_formula < 0` = 옛 판은 거부했다 #30·A-4) ·
  왼쪽 절반 몰림 구도 통과 · 문구 검사(화면 중심·지평선 부재).
- 되돌릴 조건: 실획에서 지평선-주점 y 어긋남이 실제 왜곡(피치 ≠ 0의 3점 기미)을 뜻하는
  표본이 나오면 — 그때는 h 벌점이 아니라 3점 해석(피치 유도)이 자리다.

### D-L93 후속 — 합성 원장 재실행의 실측 대가 (1-R [1][2] 이행)

**12차 상한은 합성에서 실제로 발화하고 있었다** — 상한 제거 재실행(항목 3 수정을 stash한
상태 — 귀속 오염 방지)에서 `order_lock`·`rule_camera`가 움직였다(값은 원장을 그 자리에서
읽는다 #47. camera_gate는 **무변화** — 중단 조건 게이트는 fovGate를 안 지난다):

- `order_lock`(600실행): camera_ok **519 → 524**(상한이 막던 f²>0 확정 5) · **P1 111 →
  70 · P2 408 → 454**(상한이 둘째 축을 거부해 P1에 머물던 실행 ~41이 P2로) · asks 9 → 3 ·
  배치 3433 → 3384 · within_cut_0_2 89 → 86 · beyond_cut_0_5 2166 → 2153. **등록 게이트
  조항 ④는 true 유지**(86 ≥ 65 · 2143 ≤ 2190 — 1-R [1]의 우려였던 되뒤집힘은 없다).
- `rule_camera`: **camera_ok 226 → 204 · deg_median 6.4634 → 6.9537 — 나빠졌다**(#16):
  극단 화각 2점이 **일찍 서서 잠기는**(소실점 잠금) 실행이 늘어, 상한이 사실상 막아 주던
  "나쁜 조기 확정"이 통과한다. 이것이 상한 제거의 실측 대가다 — 다만 합성 medium 잉크의
  값이고(AS-L25 계열) 게이트 `passed: true` 유지. 실사용 대응물(극단 카메라의 조기
  잠금)은 실획이 판정자다. AS-L24 팔 부호도 하나 뒤집혔다(제자리 정정 — 그 행).
- 이 대가에도 결정을 유지하는 이유: 지시가 명시한 원칙(넓은 화각은 선택)이고, 합성의
  "나쁜 조기 확정"은 **정확히 그리는 사용자**에게는 없는 비용이다. 되돌릴 조건(위)에
  "극단 화각 조기 잠금의 실획 빈도"를 추가한다.

## D-L96 — 화면 가로축이 서 있으면 깊이선 둘로 1점이 선다: 11-1 "+수평선 → 1점 확정"의 복원 (2026-08-19 14차 항목 3)

**8차 1-a가 죽인 `unambiguous`(resolvePool ②)를 되살리고, 가로선 선언 순간의 풀 재해결을
신설했다.** 실측 재현(14차 지시 3): 깊이선 둘(수렴)+수직+수평이 **넷 다 대기**였다 —
대기의 이유("어느 둘이 같은 축인가")는 화면 가로축이 서면 사라진다(남은 수평축이 하나 —
`horizontalTarget` 머리말이 처음부터 그렇게 적고 있었다).

- **바뀐 것 둘**: ① 깊이 풀 해결의 `unambiguous`를 가로축 슬롯 존재로 계산(8차의 상수
  false 폐기) ② **가로선이 선언되는 순간** 대기 풀(≥2)을 unambiguous로 재해결 — 순서
  무관(깊이 먼저·가로 먼저 둘 다 선다).
- **함께 뒤집힌 것**(⛔ 2차 [A] 정정 — 초판 문장은 슬롯-존재 게이트 시절의 것이라
  조건이 빠져 있었다): **선언된** 가로축 아래서는 잠정 가로축 밀어내기(displacedScreenH)
  입구에 못 간다 — 선언이 서면 깊이선 둘에서 확정이 나므로 "세 선이 두 축으로 갈리는"
  자리가 안 온다. **미선언(분류뿐) 가로축 아래서는 8차 1-d 그대로 도달 가능하고
  밀어내기가 산다**(progress 3-b·3-c와 이제 일치). CLAUDE.md의 제품 정의("P1(가로선
  확정)은 불가역", D-L53)와 코드가 일치하는 것도 선언 경로의 이야기다. 시험은 "새
  계약으로 재작성"이 아니라 **미선언 경로의 원 계약 복원 + 선언 팔 신설**이다(#28 —
  잠근 거동: 선언 아래 두-선 확정·미선언 아래 종전 대기/밀어내기).
- ⚠⚠ **초판(슬롯 존재로 열기)은 합성에서 오배정 고원 진입을 만들었고 그 자리에서
  의도 게이트로 고쳤다**(#16 — 두 단계를 다 기록한다): 슬롯 존재만으로 열면 **얕은(≤4°)
  참-깊이선의 우연 screen_h 분류**가 슬롯을 차지한 채 두 선 확정이 굳는다. 재현 픽스처는
  무잡음이었고, 붕괴 판정은 잡음층 표(`headline_no_identity` — 잡음 {0.005~0.05}, 잡음
  0 행 제외 — 리뷰어 2차 [10] 정정: 초판 산문 "무잡음 합성에서 31.6°"는 모집단 표기가
  틀렸다)에서 났다 — rule_camera 게이트 false·order_lock P2 급감·형태 오차 배증.
  ⚠ 이 초판 수치들은 최종 판 재실행으로 **덮어써져 원장에 없다 — 산문 기록이다**(#25.
  재현하려면 의도 게이트를 끄는 스위치가 필요한데 안 만들었다 — 범위). **최종 판은
  선언(declared) 게이트다**: 직교 스냅이 잡았거나(hint "screen" — D-L89 "스냅이 곧
  선언") 물음에 답한(forced) 가로축만 unambiguous를 연다.
- **최종 판의 실측 — 이득과 대가를 같은 원장이 든다**(리뷰어 2차 [1][3] 정정: 초판의
  "게이트 true·fixed 팔 동일값"은 통과한 조항만 인용했고 이동을 지웠다):
  · `rule_camera.json@f351839a` 등록 게이트는 **조항이 둘이다**(#40 ⑥): 원시 몰아
    `passed` **true**(7.6889 < detect 9.1742 — 기준선 복귀). 스냅 몰아 `passed_snapped`
    **false**(33.2719 > 9.1742) — **직전 커밋에서 true(7.1247)였고 이 항목이 뒤집었다.**
  · `order_lock.json@f351839a` **fixed 팔**(행 이름 명기 — 2차 [G]: 현행 원장의 3은
    옆 팔 `bypass.asks_total`의 값이라 헷갈리기 쉽다): P1 70 → **56**·P2 454 → **468**
    (앞값의 출처는 **D-L93 후속 절의 격리 실행** — 원장은 최종 판으로 덮어써져 산문
    기록이다. 14실행이 P1에서 P2로 옮겨 갔다. **이 항목이 의도한 기전의 실측이다**:
    가로선 선언으로 1점이 빨리 서고, 그 위에서 둘째 소실점까지 간다). fixed 팔 물음은
    3 → **23**(`screen_or_depth/p1_guard` — 사용자 비용). 배치 3384 → 3393 · beyond
    2153 → 2143 · 형태 중앙 0.5918 · within 86 · camera_ok 524/600은 그대로다.
- ⚠⚠ **남은 대가 — 스냅 모사 팔의 오배정 고원 재진입**: 스냅을 모사하는 팔에서는 얕은
  참-축 획이 직교 스냅으로 **정확히 수평이 되며 선언**되고, 같은 축의 깊이선 둘이 유한
  소실점으로 서면 **한 참 축이 두 슬롯(화면 평행 + 유한)을 이중 예약**한다 — 나머지 축이
  갈 곳을 잃는다. 8차 1-d의 밀어내기가 고치던 그 형태인데, 선언 아래서는 밀어내기
  입구(세 선 갈림)에 못 간다. ⚠ **크기가 아니라 범주로 읽는다**(리뷰어 2차 [4] — #46):
  스냅 팔 중앙 31.6/33.3/35·p90 전부 40.66은 **오배정 고원의 상수**다(DEFERRED "정확히
  35" 서명 — 값은 정보를 안 싣는다). ⚠ **분모도 함께 읽는다**(리뷰어 2차 [5] — #11):
  스냅 팔 axes 229~221·screen_axes 332~355 ↔ 원시 팔 axes 343·screen_axes 227 —
  눕혀진 축 ~114개가 분모에서 빠진 뒤의 값이라 원시 중앙과 같은 양의 비교가 아니다.
  ⚠ 등록문은 이 스냅 팔을 **"앱 동작점"이라 부른다**(리뷰어 2차 [2] — 앱의 확정 전
  경로가 같은 `resolve2dCore`를 지나므로 "앱 무스냅 경로"라는 위안은 없다). **알고
  유지하는 이유**: ① 지시 3이 명령한 동작이고 ② 선언의 정의(스냅을 보고 받아들임)는
  D-L89의 서 있는 결정이며 ③ 합성 급송은 스냅이 눕힌 선을 사람처럼 되무르지 않는다
  (AS-L25 — 위약 팔 6.65~10.03이 안 무너진 것이 귀속 증거다 #30: 눕히는 스냅 없이는
  고원에 안 간다). **실획이 판정자다**(DEFERRED — 얕은 2점 축의 이중 예약 빈도).
- 되돌릴 조건(리뷰어 2차 [12] — 관측 가능한 판정자로): ① 합성 — `rule_camera` 스냅
  팔의 `screen_axes_total` 초과분(현행 347−227=120 — 이중 예약 규모의 대리)이 커지거나
  `passed_snapped`가 계속 false인 채 실획 오차가 그 방향을 가리킬 때 ② 실획 채널
  (`real_ink`의 축 방향 오차)이 열리면 그쪽이 정본. 대응 후보는 unambiguous 폐기가
  아니라 **선언 신호를 좁히는 것**(명시 토글·스냅 유지 확인)이다.
- ⚠ camera_gate에 대한 진술(리뷰어 2차 [11] 정정): 초판의 "camera_gate 무변화"는 이
  항목의 실행이 아니었다 — 그 하네스는 소실점을 검출·직접 주입해(`detectVps`·
  `recoverCamera`) stepRule 확정 경로를 **안 지나므로** 이 변경이 닿지 않는다(사유이지
  실측이 아니다. #41의 중단 조건 게이트라는 위상은 그대로).

## D-L94 — 그리는 중의 팬·줌은 종이 조작이다: 화면 줌(표시 배율) 신설 (2026-08-19 14차 항목 5)

**D-L86(화면 팬)의 줌 확장.** 사용자 보고: 화면을 옮기고 확대하려는데 카메라가 3D에서
움직였다 — 팬은 D-L86이 이미 화면 조작이었으나 **줌이 없었다**(핀 상태 핀치의 배율은
버려졌고, 데스크톱 휠은 `begin()`으로 핀을 풀고 달리를 돌렸다).

- **구현**: `stage.viewZoom`(표시 배율) — 표시 = 문서 × z + 팬. three 층은 표시 창의
  내적 파라미터(주점' = z·주점 + 팬, f' = z·f — project = principal + f·x/z의 정확한
  화면 아핀)로, 잉크 층은 ctx transform으로, 입력은 (표시 − 팬)/z로 — 세 방향이
  `viewOffset`/`viewScale` 훅 하나씩을 지난다(#17). **f를 바꾸는 달리가 아니다**:
  카메라·문서·**주점** 전부 불변(`screen_zoom`의 pose_moved 0·doc_moved 0·
  principal_moved 0 — 리뷰어 [4]로 줌 팔에 주점 필드 추가). 핀치 중점·휠 커서가
  고정점인 것도 실측이다(`screen_zoom.fixed_point_gap`·`wheel_zoom.fixed_point_gap` —
  중점 아래 문서점의 전후 격차. 리뷰어 [1]: 초판은 이 문장이 원장 밖 주장이었다 #25).
- **가르는 방법은 지시 5-d의 (1)** — 확정 시점(핀·카메라 전)은 화면, 궤도로 풀리면
  공간(달리·팬). 기각 둘(리뷰어 [10] — 초판은 (2)를 안 적었다): **(2) "1점 시점에서는
  화면, 그 외에는 공간"** — 구분축이 투시 차수인데 "그리는 중"과 무관하다. 재현 보고의
  구도부터 2점이라 그리는 중 확대가 여전히 공간 조작으로 남는다 — 증상이 안 풀린다.
  **(3) "항상 화면 + 공간은 별도 제스처"** — 궤도 뒤의 접근(지시 5-c "궤도 후에는 공간
  조작이 필요하다")에 별도 제스처를 새로 발명해야 해서 선례(SketchUp 두 손가락 = 공간
  팬)와 어긋난다. D-L86이 이미 (1)의 갈래로 서 있으므로, A-3: 선 결정의 최소 확장이
  단순한 쪽이다.
- **조리개는 표시 px**(D-L56 유지): 스냅 질의의 문서 조리개 = radiusPx / z
  (`viewZoomNow` — osnapCfg·resolve2d 두 입구). e2e 판별 팔은 **양방향이다**(리뷰어
  [11] — #12): z=2에서 표시 12px(문서 6px) 겨냥이 **안 붙고**(문서 px 조리개였다면
  6<8이라 붙는다), z=0.5에서 표시 6px(문서 12px) 겨냥이 **붙는다**(문서 px 조리개였다면
  12>8이라 거부 — 축소는 문서 조리개가 커지는 반대 방향이다).
- **측정**(`view_pan.json@f351839a` — ③″ 팔): 핀치 비 2.0 → viewZoom∈[1.8,2.2] ·
  핀 유지·pose 0·문서 0·주점 0·고정점(문서 <0.5px) · three 투영 = 문서×z+팬(±0.5px —
  비배율 대안값 three_gap_if_unscaled ≈990px가 판별 간격) · 잉크 층 배율 위치 칠
  (픽셀 대조 — painted_alpha·gap_if_unscaled ≈574px) · 스냅/불스냅 양방향 판별(위) ·
  z=2 스냅 없는 자리 pts2d=(표시−팬)/z(<1px — gap_if_undivided ≈674px) · 휠 줌(핀
  유지·커서 고정점) · 궤도 뒤 공간 팬 불변(음성 대조). 배율 클램프는
  `screen_zoom.view_zoom_lim`([0.2, 8])이 들고 측정 동작점(0.5~2.26)은 안 닿았다.
  ⚠ 소실점 상대 벡터(vp_relative_gap)는 **독립 측정이 아니라** three_gap의 대수적
  귀결이라 임계를 안 건다(#5 유형 3 — 원장 주석).
- **알려진 표시 한계**(잔여는 DEFERRED 14차 행): 잉크 층 선 굵기는 배율을 따라
  확대되는데(종이 확대 — 프로크리에이트 선례) 3D 층 화면 굵기는 불변 — 배율≠1에서 두
  층의 굵기가 갈린다. 가장자리 소실점 표식·그리드 클리핑이 팬·줌을 모르는 것도 D-L86의
  그 한계 그대로. PICK_TOL(고르기·지평선 손잡이)은 표시 배율 미적용 — **표시 반경이
  z에 비례해 변한다**(줌인에서 과대·줌아웃에서 과소 — 리뷰어 [5]: 초판의 "체감 차가
  작다"는 방향이 반대였다. 측정 없음). 필요해지면 같은 `viewZoomNow`를 건다.

## D-L97 — 작도/모델링 화면의 구분과 복귀: 상태는 자세에서 계산한다 (2026-08-19 14차 항목 6)

⛔⛔ **2026-08-19 15차 항목 3(D-L102)이 이 결정의 절반을 되돌렸다** — 아래 본문의
"모델링 화면에서는 안 그린다"와 "작도 = 축 정렬 + 피치 0"은 **더 이상 현행이 아니다**.
그 조건이 **3점을 통째로 배제했기 때문이다**(3점 시점의 피치는 정의상 0이 아니다).
현행: **축 기준계가 서 있으면 작도**이고 피치는 「작도 시점으로」 **버튼의 조건**으로만 남는다.
살아 있는 것: 돌린 시점의 표시를 그 시점 값으로 다시 계산한다는 것(`viewOverlayCtx`),
큐브·복귀의 단일 출처, 「작도 시점으로」 버튼 자체.
⚠ "수평 유한 소실점의 y = 지평선"도 **D-L101로 개정됐다** — 지평선은 **두 수평 소실점을
잇는 선**이고 롤이 있으면 기운다.

**사용자 보고**: 화면이 돌아간 순간부터 모델링 화면이 된다 — 소실점·지평선이 사라지고
그리기가 안 되며 돌아올 길이 없다. 원인은 셋이 다 **핀(확정 시점) 전용**이었던 것이다:
표시 게이트(`drawBelowInk`의 `standing && !isPinned` 조기 반환), 복귀 수단(home 버튼 —
확정 프레임 정확 복귀뿐·마우스 전용), 그리고 궤도가 항상 핀을 푸는 구조(`snapToDir`).

- **작도 화면의 정의**(지시 6-d): 핀 **또는** 시선이 그린 수직축과 직교(피치 0)이고 —
  요가 축 정렬이면 **1점**, 축 사이면 **2점**. 상태는 저장하지 않는다 — §1의 규약
  그대로 **자세에서 매번 계산한다**(`judgeDraftPose` — `s3d/draftPose.ts`).
  기준계는 **큐브 기준계(그린 축 — D-L87)**다: 큐브 탭과 복귀가 같은 기준계를 쓰지
  않으면 두 조작이 다른 곳으로 간다. ⚠ 세계 축과의 항등은 **P1(화면 축 확정)뿐**이다
  (R2 [N1] 정정 — 초판의 "1·2점에서 구별이 없다"는 D-L87의 자기 기록("2·3점 구도에서
  갈렸다")과도, 이 항목의 2점 직립 픽스처 실측(그린 축이 세계 축에서 요 35°)과도
  어긋났다. 2점은 보통 상태다).
  판정 단위는 각도(#49 — 피치·요 이탈), 임계는 **D-L77**의 `ONE_POINT_TOL.hand_deg`
  재사용(⚠ 6-R1 [4] 정정 — 초판은 D-L66이라 적었다: 이 갈래의 D-L66은 무관한 결정이고
  재탭은 **D-L76**·hand_deg는 **D-L77**이다 — 병합 번호 번역표(D-L76 머리말)를 따른다.
  값은 `draft_return.json`의 `algo_constants`가 든다 — test/constants 밖이라 전역 해시가
  못 잡으므로 그 원장이 값을 싣고 바뀌면 재실행한다, 6-R1 [5]. ⚠ **이 상수는 이제 두
  판정을 정한다**: 손 정렬 자동 스냅 발동(D-L77)과 작도/모델링 구분 — 한쪽 목적으로
  움직이면 다른 쪽이 함께 움직이는 결합이다).
- **복귀**(지시 6-a): 피치를 접고(최소 회전 — 2점), 요가 임계 안이면 그 축으로 스냅
  (1점 — 큐브 재탭 D-L76과 같은 답). "가장 가까운 1점 **또는** 2점"은 이 규칙 하나로
  닫힌다 — 피치만 접는 쪽이 항상 더 가깝고, 이미 축 근처면 그 축이 곧 답이다.
  카메라 이동은 큐브와 같은 경로(`snapToDir` — 거리·중심 유지 #17)다.
  수직 응시 특이점(수평 성분 0)은 결정론적 기본값(정면)이다.
- **표시**(지시 6-c): 소실점·지평선·그리드를 돌린 작도 시점에서도 낸다 — 출처는 배치와
  같은 `frame()`(→`viewPlaceCtx`: 각 축을 지금 시점으로 투영 = 소실점, 수평 유한
  소실점의 y = 지평선)이다(#17 — `viewOverlayCtx` 하나가 핀/작도 시점을 가른다).
  격자는 `grid.ts`의 카메라 의존을 `GridCam`(ok·f·principalPoint)으로 좁혀 시점
  프레임을 그대로 받는다. **대기 풀 가이드·물음은 확정 카메라 화면 좌표라 핀에서만**
  그린다. 지평선 손잡이도 핀 전용 그대로(끌기는 확정 카메라 편집이다 — D-L45).
- **모델링 화면에서는 안 그린다**(지시 6-e — 지시 자체의 A-3 관정: 소실점이 화면 밖
  극단이면 스냅이 무의미하다. 단순한 쪽 = 복귀). `InkCanvas.inkAllowed` 게이트가 획
  시작 자체를 막고(호버 표식 포함 — 카메라 조작은 그대로) 복귀 안내를 낸다.
  ⚠⚠ **이것은 §9.3("돌린 시점에서 그리면 새 뷰")의 사거리 축소다**: "돌린 시점" =
  이제 **돌린 작도 시점**이다. stage.spec의 §9.3 팔과 basic_flow ⑥("돌린 뒤 이어
  긋는다")을 그 계약으로 갱신했다(차단 음성 대조 + 복귀 뒤 그리기 — #28: 사유는 지시
  6-e다). 시점 저장·전환(모델링 시점 포함)은 그대로 산다 — 확인과 조작의 자리다.
- **복귀 조작**(지시 6-f): 새 버튼 「작도 시점으로」(`draft`) — ⛔ ~~**모델링 상태에서만
  켜지고 그 켜짐이 상태 표시를 겸한다**~~ **뒤집혔다(15차 D-L102)**: 이제 **이미 정렬된
  작도 시점이 아닐 때** 켜진다(`alignedDraftNow`) — 돌린 **작도** 자세에서도 켜지므로
  **켜짐은 더 이상 모델링의 표시가 아니다**(실측: `draft_return.json@f351839a`의
  `rotated_three`가 `state: "draft_three"`인데 `draft_btn_enabled` 참). 상태 문구를
  안 두는 것(D-L55)은 그대로다. 터치에서도 보인다. 옛 home(확정 시점으로 —
  핀 복귀·마우스 전용)은 그대로 둔다 — 확정 프레임 정확 복귀는 다른 물음이고
  stage.spec 세계-좌표 팔이 그 경로를 쓴다.
- **큐브**(지시 6-b — 재탭 규약은 D-L76): 면 탭·재탭(가장 가까운 1점)은 원래 돌았다 — 안
  돌아 보인 것은 탭 결과가 늘 언핀이라 **표시가 안 따라온 것**이다(위 표시 게이트가 원인).
  모서리 탭(수평 모서리 = 2점)도 작도 화면으로 이어진다(종단 팔 ⑥).
  ⚠⚠ **`drawnBasisThree`의 빈/NaN 슬롯 보완**(6-R1 [1]의 뿌리 — 큐브 경로의 실수리):
  옛 판은 axisDirs에 널이 하나라도 있으면 통째로 **세계 축**으로 떨어졌고, 수직축
  미선언(P1·P2의 보통 상태)이나 수직 슬롯의 [NaN, NaN] 소실점(픽스처 계열 — NaN은
  truthy라 널 검사를 지나 그램-슈밋을 오염시키고 끝의 영벡터 가드가 조용히 null로
  접었다)에서 **큐브 탭·작도 복귀가 그린 축과 수십 도 어긋났다**(직립 픽스처 실측:
  정렬 축 소실점이 주점에서 ~0.7f — 수리 전 값은 **원장 밖 실행의 산문 기록**이다(#25:
  수리 전 코드가 필요해 재현 팔이 없다 — 항목 3 초판 붕괴와 같은 유형). 수리 후 잔차의
  정본은 `draft_return.json@f351839a`의 `one_point_overlay.aligned_vp_dist_to_principal`
  필드다 — 부동소수 꼬리이고 그 near-zero는 보장 서명이다, 원장 ws).
  수직이 비면 화면 수직(2점 = 수직 무한원의 전제 — 이론서 2.2), 가로축이 비면 외적으로
  세우고, 유한성 검사까지가 빈 슬롯 판정이다.
- **측정**(`draft_return.json@f351839a` — 종단 신설): 핀 작도(지평선 행 덮임) → 임의
  궤도(모델링 — 덮임 소멸·그리기 차단·안내) → 버튼 복귀(2점 — 수직축 무한원·수평 유한·
  재덮임·스냅 붙음) → 큐브 면 탭(1점 — 수평 유한 하나). 판정은 범주(#46), 왕복 덮임이
  판별자다. 단위 팔은 `draft_pose.test.ts`(경계 ±1°·반례·특이점).
- **기각**: ① 핀 확장(임의 작도 시점을 핀으로) — `pinTo`는 카메라를 원점·항등으로
  되돌리는 확정 프레임 전용이라, 자세 보존 핀을 새로 만드는 것은 표시 게이트 수리보다
  큰 수술이다(A-3 — 그리기는 `frame()`이 이미 어느 시점에서든 돈다). ② 모델링에서도
  그리게 두기 — 지시 6-e가 직접 기각했다. ③ home 버튼 재용도 — stage.spec 두 팔이
  핀 복귀 경로를 쓰고, 확정 프레임 복귀는 여전히 유효한 물음이다(추가가 단순하다).
- ~~**알려진 한계**: `armOnePointAlign`은 세계 기준계~~ ⛔ **R2 [N1]로 수리했다** —
  이월 사유("1·2점에서는 동일")가 틀린 것으로 판정돼(항등은 P1뿐 — 2점 보통 상태에서
  요 각도만큼 어긋난다) 미루지 않고 큐브·복귀와 같은 단일 출처(`draftGazeDrawn`·
  `judgeDraftPose` — #17)로 갈아 끼웠다. 종단 팔은 종전대로 없다(감쇠 꼬리 대기라
  결정론이 약하다 — basic_flow ws의 그 문장 그대로. 판정 자체는 draft_pose 단위 팔이
  덮는다).

## D-L98 — 확정 배치는 **그리며 선언된 연결**을 쓴다 (2026-08-19 15차 항목 1)

**문제**(사용자 보고 ②, 실측으로 기전 특정): 확정 순간 붙여 그은 두 획이 떨어진다.
스냅 **기록**은 안 없어진다(AS-L45 반증) — 없어지는 것은 기하다. 자리는 둘이고 **갈린다**:

```
(a) findJoints가 접합을 대표 직선 교점에서 다시 찾는다
    → 프리핸드에서 그 교점이 그린 모서리에서 접합 반경(1.5% ≈ 21.7px) 밖으로 나간다
    → 붙여 그은 획이 "구조에 이어지지 않았다"로 **2D에 남는다**
(b) liftAll이 3D 끝점을 rep(PCA 극단)의 광선에 놓는다
    → 오스냅 후보는 pts2d의 양 끝이라 그 둘이 다른 점이다(실측 중앙 6.5px — AS-L47)
    → 놓여도 두 획의 그 끝이 **3D에서 떨어진다**
```

**결정**:
- **(a) `DeclaredJoint`** — 기록된 연결(`snap2dStart`/`snap2dEnd`)을 제약으로 넘긴다.
  **선언이 검출을 이긴다**(같은 짝이면 검출 쪽을 버린다 — #17: 한 출처).
  같은 축 선언은 뺀다(3D에서 나란한 두 직선은 점을 안 나눈다 — `findJoints`와 한 규약).
  **이상점 제거에서도 안 뺀다** — 추정이 아니라 입력이다.
- **(b) `anchorToTarget`** — 대상 직선 위, 기록된 화면점의 광선에 가장 가까운 점이
  연결점이고 두 획의 그 끝이 **같은 점**이 된다. 대상은 자기 직선 위에서 미끄러지고
  (직선 불변), 이은 획은 **연결점을 앵커로 다시 놓는다**(`segmentFromAnchor` 규약 —
  **3D 방향이 안 돈다**). 되맞춤이 서면 3D 참조(`snapStart`/`snapEnd`)도 붙인다.
- **대상의 끝점을 함께 옮기는 것은 `endpoint`·`vertex` 기록뿐이다** — 중점·교차점·수선 발은
  대상의 **몸통**에 붙은 연결이라 옮기면 대상을 늘이거나 줄인다.

**초판을 측정이 뒤집었다**: 처음에는 끝점만 옮기고 반대 끝을 그대로 뒀는데, 그러면 3D
방향이 **접합 잔차 ÷ 획 길이**만큼 돌았다 — 그 분포가 축 나란함(`parallel_deg`) 밖까지
간다(`confirm_link.json@f351839a`의 `summary.no_keepdir_turn_deg_max`). 축 평행이 이
도구의 전제이므로 방향을 앵커에 물렸고, 그것이 그리는 중의 규약과 **같다**(#17).

**측정 스위치**(#30): `S2S.setLiftDeclared` · `S2S.setLiftAnchor` — 둘을 **따로** 끈다.
**기각**: ① 모든 끝점을 `pts2d`로 되맞추기(범위를 넓힌다 — 프리핸드를 버린 대가는 §1.1의
결정이다. `DEFERRED`) ② 연결점을 `ray(기록점)·λ`로 두기(대상 직선을 깨뜨린다 —
화면 위치는 정확해지나 대상의 축 방향이 돈다).

## D-L99 — 끝점을 공유한 깊이선 짝도, **선언 아래서는** 소실점을 만든다 (15차 항목 1)

**문제**(사용자 보고 ①·②′): *"깊이선 하나를 긋고 그 끝점을 소실점 삼아 새 깊이선을 뻗으면
공간이 안 선다."* 닫힌 삼각형(사선 → 수평 → 사선이 첫 점으로)도 같은 형태다.
재현: `order 0` · `standing false` — 가로축이 선언돼 있어도 **카메라가 영영 안 선다.**

**기전**: `poolCandidates`가 그 짝을 **두 문에서** 버렸다 — ① 교점이 어느 끝점의
`merge_ratio` 안이면 제외 ② `beyondSegment`(교점이 두 선분 **밖**). 이음점은 정확히
`t = 0`·`t = 1`이라 ②도 못 지난다.

**결정**: `unambiguous`(가로축이 선 P1 — 깊이축이 하나뿐임이 **선언된** 상태)일 때만
두 문을 푼다. 그 상태에서 대기 깊이선은 전부 같은 축이고, 3D에서 나란한 두 직선의 상은
**소실점에서만** 만나므로 그 교점은 이음점이든 아니든 소실점이다.
"모든 꼭짓점이 소실점이 된다"(4차 지시 3)는 위험은 **두 선이 다른 축일 수 있을 때**의
것이고, 선언이 **옳으면** 그 경우가 없다 — D-L96이 이미 그 신뢰를 세웠다(선언된 가로축
아래 깊이선 둘로 확정).

⚠⚠ **그러나 "같은 신뢰"는 아니다 — 노출이 는다**(1차 리뷰어 [3]이 잡았다. 초판은
"같은 신뢰이지 더 큰 신뢰가 아니다"라고 적었고 그것이 틀렸다). D-L96만 있을 때는
**끝점을 공유한 짝이 두 문에서 걸려** 선언이 틀려도 그 형태로는 안 굳었다. 이제 굳는다:
`shared_vertex_vp.test.ts`의 **완화의 대가** 팔이 그것을 실측으로 든다 — 선언이 틀린
상태에서 서로 다른 축의 모서리 둘이 꼭짓점을 공유하면 **그 꼭짓점이 소실점으로 선다**
(옛 문에서는 `null`이었다). 그것이 D-L3이 `min_reach`로 막은 실패이고 PITFALLS #23의 예다.

**그럼에도 받는 이유**: 선언이 틀리는 것은 D-L96의 물음이고 **그 물음이 이미 열려
있다**(DEFERRED 14차 행 — 얕은 2점 축의 스냅-선언). 이 완화는 그 잘못된 선언이 낳는
결과를 넓히지만 **새 종류를 만들지 않는다**. 반대편에는 사용자가 **공간을 아예 못 세우는**
확인된 실패가 있다(재현: `order 0` · `standing false`). 미배치는 대기지만 **미확정은
대기가 아니다** — 아무것도 안 일어난다. 판정자는 실획이고 조건은 DEFERRED에 있다.

**안 넓힌 것**(#32): **안쪽 교차는 선언 아래서도 막는다** — 두 선이 서로의 **몸통**을
가로지르면 같은 축일 수 없다(나란한 직선은 서로를 안 가로지른다). 그래서 새 술어
`insideSegment`는 `beyondSegment`의 **여집합이 아니다** — 그 사이에 **끝**이 있고,
푼 것은 그 "끝" 한 칸뿐이다. 반례 넷이 그 자리를 지킨다(몸통 교차·T자 접합·나란함·
술어 관계). 새 임계는 안 만들었다(`SNAP_TOL.extend_ratio` 재사용 — #17).

**남는 사실**(수리가 아니다): 닫힌 삼각형은 이제 **서지만** 자기 소실점까지 그은 획은
그 끝의 깊이가 무한이라 세그먼트가 안 정해진다 — 그 획은 2D 대기다(§9.1: 미배치는
실패가 아니라 대기). 종단 팔이 그것을 그대로 잠근다.

## D-L100 — "어긋납니다"는 **축을 겨냥한 획**에만 (2026-08-19 15차 항목 2)

**문제**(사용자 보고): 2점에서 축 스냅으로 그은 깊이선에 *"이 선이 어긋납니다 — 두 소실점
어느 쪽도 향하지 않습니다. 지우고 다시 그으세요 (수평 축은 둘뿐입니다)"*가 뜬다. 회전해 보면 안 어긋난다.

**재현으로 셋이 갈렸다**(지시 ③):

```
(a) 양 끝 스냅·연장선으로 놓인 획      3D에 정확히 있는데 "지우고 다시 그으세요"가 뜬다
(b) 유한 수직 소실점을 향한 깊이선      판정이 수평 둘만 본다(vpDirSnap은 셋을 본다)
(c) 앞 획의 경고가 다음 획에 남는다     note를 아무도 안 비운다
```

**결정**:
- **(a)** 알림의 대상은 **축을 겨냥한 획**이다. `placeLive`가 낸 경로가 `two_point`·
  `extension`이면 그 획의 기하는 **연결이 정했고**(D-L83) 축을 겨냥한 것이 아니다 —
  `resolveLive`가 그 경로를 두면서 스스로 "축을 벗어난 선(면 위 사선·자유 세그먼트)이 이
  경로로 놓인다. 각도는 표시용으로만 낸다"고 적었다. 규칙 **상태**는 그대로 거절한다
  (그 선은 소실점을 만들지 않는다) — 바뀌는 것은 알림뿐이다.
- **(b)** 깊이선 갈래가 슬롯 2를 함께 본다. **임계가 아니라 대상 집합의 수리**다(#24) —
  `AXIS_TOL.vp_dist_ratio`는 그대로이고, 스냅(`vpDirSnap`의 소실점 셋)과 판정이 이제
  같은 집합을 본다(#17).
- **(c)** `onStrokeEnd`가 획마다 `note`를 비운다. `lastSnapNote`가 이미 쓰는 규약이다.

**안 한 것**(#32): 경고를 없애지 않았다. 무앵커로 어느 소실점도 안 향하게 그은 획에는
그대로 뜬다 — `axis_warn.json@f351839a`의 ④ 팔이 그 자리를 지키고, 그 팔의 `misfit`이
이 원장의 도달 가능성이다.

⚠⚠ **사용자가 쓴 말 그대로의 조건은 재현되지 않았다**(2차 리뷰어 [1]): 2점·3점 어디서도
**축 스냅**으로 그은 깊이선(①②)은 경고를 안 낸다(`misfit_min` 0). 재현된 것은 **양 끝
스냅으로 놓인 획**이고, 그 획은 그리는 동안 축 색 미리보기를 보이다가 끝점이 붙는 순간
양 끝 스냅으로 넘어간다 — 사용자가 "축 스냅으로 그었다"로 읽었을 자리다. **그 동일시는
해석이고 측정이 아니다.** 남는 가능성은 `DEFERRED`에 조건과 함께 있다.

⚠ **대가**: 양 끝 스냅 경로에서 **정말로 잘못 놓인** 획에도 이제 경고가 안 뜬다. 그 경로의
기하가 옳은가는 D-L46의 자리이고 이 원장이 안 잰다(2차 리뷰어 [2]).

**기각**: ① 거절 자체를 없애기(11-3의 근거 — 셋째 방향이 쌓이면 어느 것이 틀렸는지 모른다 —
가 그대로 유효하다) ② `notify`를 통째로 끄기(진짜로 어긋난 획의 사유가 사라진다, #7).

## D-L101 — 소실점은 **그 시점의 축 방향에서** 나온다 (2026-08-19 15차 항목 8)

지시 8: *"어느 시점에서든 축 셋의 소실점을 계산해 표시한다 … 수평 소실점 둘을 잇는 선이
지평선이다. 배면을 보든 위를 보든 그 선은 계산된다."*

**결정**: `axisVp.ts` — 축 방향 `d`(시점 좌표)의 소실점은 `principal + f·(dx,dy)/dz`이고
`d`·`−d`가 같은 점이다(축은 부호가 없다). `dz → 0`이면 무한원이고 그때 남는 **화면 방향**이
표시의 재료다(이론서 2.2, c=0). 무한원 판정은 `isFiniteVp`(`VP_INFINITE_RATIO`) 그대로 —
**새 임계를 안 만든다**(#17).

**지평선을 가로선에서 선분으로 바꿨다**: 옛 판은 `vps[0][1]`(첫 수평 소실점의 y)로
화면 가로선을 그었다. 확정 시점은 롤 0이라 두 수평 소실점의 y가 같아 그것이 맞았지만
**돌린 시점에서는 다르다** — 크기는 `view_axes.json@f351839a`의
`summary.old_flat_gap_px_rotated`다(⛔ 초판은 `old_flat_gap_px`를 인용했는데 그 배열의
첫 원소는 **핀 자세의 0**이라 "돌린 시점에서는 다르다"의 반례를 자기 인용에 담았다 —
2차 리뷰어 [4]).
하나만 유한이면 그 점을 지나 무한원 축의 화면 방향으로, 둘 다 무한원이면 안 그린다.

⚠ **격자의 지평선은 아직 스칼라 y다**(`gridGuides`) — 두 규약이 공존한다. 갈리는 것은
롤이 있는 시점뿐이고, 격자 쪽 개정은 `DEFERRED`.

## D-L102 — **축 기준계가 서 있으면 작도다** (2026-08-19 15차 항목 3)

**문제**(사용자 보고 3): *"3점 투시에서 그림이 안 그려진다."*

**기전**: 14차의 작도 조건이 **"축 정렬 + 피치 0"**이었고(D-L97), 3점 시점의 피치는
**정의상 0이 아니다** — 기운 카메라가 곧 3점이다. 그래서 3점이 통째로 모델링으로 읽혔고
표시가 사라지고 그리기가 막혔다. 재현: 참 3점 픽스처에서 `draftJudge().kind` = `null`
(`pitchDeg` −15°), **0.15 rad만 돌려도** `viewState()`가 `"model"`.

**결정**(지시 3-c 그대로): 작도 = **카메라가 서 있고 축 기준계가 선다** — 1점·2점·3점 전부.
`judgeDraftPose`는 `null` 대신 **`three_point`**를 내고, 피치는 **이름을 정할 뿐**이다.
`model`은 기준계가 없을 때만이다.

**차단의 원래 이유가 사라졌기 때문에 넓힐 수 있었다**: "모델링 시점에 그리면 화면에 붙어
따라다니는 유령이 된다"였는데, 소실점·지평선·격자가 이미 **그 시점의 값**으로 다시
계산된다(`viewOverlayCtx`·`drawGrid`·D-L101). 지시가 "3과 8은 맞물린다"고 한 자리다.

**피치 조건은 버튼으로 물러났다**: 「작도 시점으로」는 **이미 정렬된 작도 시점이 아닐 때**
켜진다(`alignedDraftNow`) — 갈 곳이 있는가의 물음이고, 그리기의 문이 아니다.

**안 고친 것**: 획으로 **3점 카메라를 만드는 것**. 규칙에 P2 → P3 전이가 없다(7차 지시
3-d로 삭제) — 기울어진 세로 모서리가 항상 깊이선이라 엉뚱한 수평 소실점이 선다
(`view_axes.json@f351839a`의 `stroke_built`). CLAUDE.md §1이 "남는 승격은 P2 → P3
하나다"(D-L53)라고 적은 것과 **현행 코드가 어긋나 있다** — `DEFERRED`.

---

## D-L103 — **호버에서 본 후보를 펜이 닿는 순간 잠근다** (2026-08-19 15차 항목 4)

사용자 보고 4: *"오스냅이 호버부터 걸려야 한다."* 지시 4-b: *"펜을 대면 그 시점의
후보가 확정된다. 호버에서 본 것과 댄 뒤가 같아야 한다."*

**재현**: 놓인 획의 끝점을 호버하면 `vertex` 표식이 뜬다. 그 자리에서 펜을 대되 착지가
**12.5px 빗나가면** 시작점이 안 붙는다(`snapStart` null · `seg3d` 없음). 옛 경로는 착지점으로
스냅을 **다시 물었고** 조리개(8px) 밖이었기 때문이다.
값은 `hover_lock.json@f351839a`의 `summary.off_start_kind`가 든다.

**원인**: 표시(`hoverSnap`)와 확정(`anchorCandAt`)이 **다른 질의점**을 썼다 — 표시는 호버
좌표, 확정은 착지 좌표. 두 좌표가 같다는 전제가 마우스에서는 성립하고(누르는 동안 커서가
안 움직인다) **스타일러스에서는 성립하지 않는다**: 펜은 공중에서 내려오면서 몇 px 미끄러지고,
**화면에 닿는 순간 이미 그리기가 시작되므로 댄 뒤에는 조준할 수 없다.**

**수리**: 획이 시작되는 순간(`InkCanvas.onStrokeStart` — 새 콜백) 그때의 `hoverSnap`
(없으면 `hover2d`)을 **잠근다**. 잠근 것을 미리보기(`onLive`)·확정(`placeStroke`)·2D 판
(`resolve2dCore`의 `pinStart`)이 **전부** 쓴다 — 세 경로가 같은 후보를 본다(#17).

- **잠금 조건은 하나**: 착지가 마지막 호버 자리에서 `PICK_TOL.radius_ratio × 화면 대각`
  이내일 것. **새 임계를 안 만든다**(#17 — 손잡이 잡기·탭 판정과 같은 값). 그보다 멀면
  호버가 낡았고 종전대로 착지점으로 묻는다(`stale`). ⚠ **px 값을 여기 안 적는다**(#47) —
  캔버스가 바뀌면 낡는다. 값은 `hover_lock.json`의 `setup.lock_radius_px`가 들고,
  실측 문턱 밴드는 `summary.threshold_band_px`(훑기)가 든다.
- **자격 검사는 그대로 지난다** — 잠근 후보도 `anchorQualified`(D-L83 ①)를 통과해야
  좌표를 정한다. on_edge·on_face는 표시만이라는 규약이 안 바뀐다.
- **호버가 없는 기기는 종전 그대로다**(지시 4-c) — 터치는 `onHover`가 안 불려 잠글 것이
  없다(`no_hover`가 그 갈래를 센다, #35).

**측정 스위치**(#30): `S2S.setHoverLock(false)`가 수리 전 거동이다. 카운터는 분모와 함께다
(#43: `locked3d + locked2d + no_hover + stale = downs`).

**대가**: 조리개가 **착지 기준으로는** 사실상 넓어진다. ⚠ **두 프레임을 갈라 적는다**
(1차 리뷰어 [7] · #49):
  · **호버 자리 기준** 잠금 반경 = `setup.lock_radius_px`
  · **착지 기준** 유효 반경의 **상한** = 조리개 + 잠금 반경 = `setup.effective_from_landing_px_max`
    (후보는 호버점에서 조리개 안이고 착지는 호버점에서 잠금 반경 안이므로 그 합을 못 넘는다.
    **상한이지 실측이 아니다** — 실제로 회수된 거리는 `gate.recovered_land_gap_px_reference`다)
그 넓어짐이 조용히 틀린 배치가 아닌 이유는 **사용자가 표식을 보고 받아들인 자리**이기
때문이다(A-3의 "스냅으로 사용자가 보고 받아들인 것은 확정에서 안 바꾼다"). 표식 없이 붙는
일은 없다 — 잠금의 재료가 표식 자신이다.

**호버가 없는 갈래는 둘로 갈린다**(1차 리뷰어 [6]): ⚠ **터치는 애초에 그리기가 아니다**
(`InkCanvas.inkable` — 손가락 하나는 궤도, 둘은 팬·줌: 2026-08-17 지시 G). 그러므로
지시 4-c가 뜻하는 자리는 **호버를 안 거친 펜**이고, 그 팔이 `arms.no_hover.pen_no_hover`다.
둘 다 잰다 — 터치는 획이 안 생기고(`touch_downs` 0), 무호버 펜은 착지 질의로 간다.

**마우스는 왜 안 깨졌나** — 누르는 동안 커서가 안 움직여 호버 좌표와 착지 좌표가 **정확히
같다**. 이것은 실측이다(`summary.mouse_land_gap_px`) — 1차 리뷰어 [10] 전까지는 산문의
단정이었다.

**안 한 것**: 실기(아이패드·애플펜슬) 확인. 합성 `PointerEvent`(`pointerType: "pen"` ·
호버 중 `buttons: 0`)로 잰다 — 실제 호버 표본이 오면 그때 판정한다. `DEFERRED`.

---

## D-L104 — **지평선 토글이 «보인다»와 «끌 수 있다»의 충돌을 푼다** (2026-08-19 15차 항목 5)

사용자 지시 5(압축 기록): *"선 위에서 드래그 · 토글 부착 · 확정 후 조작 가능 여부 판단."*

**재현**: `horizonGrab`이 **어느 상태에서도** 참이 안 됐다. 두 술어가 상호배타이기 때문이다 —

- `horizonVisible()` = 슬롯 0·1 중 하나가 `kind: "vp"` (소실점이 **서야** 참, 4차 지시 4-a)
- `horizonAdjustable()` = 어느 슬롯도 `kind: "vp"`가 아님 (소실점이 **서면** 거짓, D-L45)

`horizonGrab`은 **둘 다** 요구했다. 손잡이가 그려지지도(`drawHorizon`이 같은 술어로 먼저
돌아간다), 잡히지도 않았다. 값은 `horizon_drag.json`의 `summary.off_grab_empty`가 든다.

⚠ **이것은 D-L45가 등재한 "확정 후 잠금"이 아니다.** 잠금은 확정 **뒤**의 규약인데
확정 **전에도** 안 잡혔다 — 4차 지시 4-a(빈 종이 감각)가 표시 조건을 넣으면서
**끌기 경로 전체가 죽었다.** 두 지시가 각각 옳았고 교집합이 공집합이었다.

⛔ **그런데 이것은 «발견»이 아니다**(1차 리뷰어 [3] — 초판이 그렇게 적었다). 저장소가
이미 그 상태를 이름 붙여 적어 뒀다 — D-L45 말미: *"그 전까지 이 끌기는 **휴면**이다
(확정 전엔 지평선이 없고 확정 후엔 잠긴다 — **도달 불가**, D-L60)"*. D-L60도 같은 문장을
든다. **이 항목이 한 것은 그 휴면을 깨기로 한 결정**이지 죽은 코드를 찾아낸 것이 아니다.

**수리 — 토글이 그 충돌을 푼다**: `SHOW_HORIZON`(기본 **끔**).

- **끔**: 소실점이 서기 전에는 지평선이 없다 — 4차 지시 4-a 그대로다(빈 종이).
- **켬**: 소실점이 서기 전의 지평선이 보이고 **선 위 어디서나 끌린다**.
  ⛔⛔ **그 끌기가 무엇인지 정확히 적는다**(1차 리뷰어 [4] — 초판은 "D-L45의 원래 의도"라고
  적었는데 그 의도는 **A-4·D-L47로 폐기됐다**: 지평선은 더 이상 3점을 만들지 않는다).
  D-L45의 같은 줄이 남은 것을 적는다 — *"확정 전 지평선 끌기는 이제 **소실점 확정에 아무
  효과가 없다**(첫 소실점이 지평선을 덮어쓴다) — 남은 효과는 **화면 표시뿐**"*(D-L59 ②).
  실측이 그대로다: `horizon_drag.json`의 `summary.on_drag_y`가 끈 y가 소실점이 서면서
  **덮이는 것**을 든다(`on_drag_overwritten` 참).
  **그러므로 이 항목이 연 것은 «구도 안내선»이다** — 종이 위 투시에서 지평선을 먼저 긋는
  것과 같은 자리이고(D-L45가 든 선례 그대로), **카메라를 정하는 조작이 아니다.**
- **소실점이 선 뒤**: 토글과 무관하게 보이고(결과로서의 지평선 — D-L59 ②), 무관하게
  **안 끌린다**. 버튼이 `지평선 잠김`으로 바뀌고 `disabled`가 된다.

**«확정 후 조작 가능 여부»의 답: 못 옮긴다.** ⛔ 근거는 **D-L32의 실측이 아니다**
(1차 리뷰어 [1][2] — 초판이 그렇게 적었고 틀렸다). 갈라 적는다:

- D-L32가 무너진 팔은 **사후 사영**이다 — 옳은 구현이 아닌 연산의 값이다. 게다가 그 값을
  든 원장(`horizon.json`)은 지금 **STALE**이다(selfcheck의 `n_stale`) — 해시 없이 인용하면
  `scan_cited_values`의 모집단 밖이라 값 대조를 한 번도 안 지난다(#42 ⑥·#27).
- **옳은 구현**(수평 소실점·지평선 동반 이동 + §6.1 "전부 다시 풀기")의 배치 손실은
  D-L60과 `DEFERRED`의 4차 행이 **미측정으로 등재해 뒀다**.

**근거는 그 미측정이다** — A-3의 "애매하면 놓지 않는다. 조용히 틀린 배치를 만들지 않는다".
값이 아니라 **값의 부재**가 이유이고, 그렇게 적어야 다음 세션이 "이미 쟀다"로 읽지 않는다.

이 항목이 더한 것은 **그 잠김을 화면이 말하게 한 것**이다: 옛 판은 아무 말 없이 안 잡혔고,
그것은 사용자에게 고장으로 읽힌다.

**측정 스위치**(#30): `S2S.setShowHorizon(false)`가 수리 전 거동(끌기 도달 불가)이다.
토글 자체가 스위치를 겸한다 — 기본값이 곧 옛 거동이다.

**안 한 것**:

- **확정 후 끌기를 열지 않았다.** 열려면 카메라 재조정 = §6.1의 "전부 처음부터 다시
  올린다"가 필요하고, 그것은 차수 승격의 몫이다(CLAUDE.md §1: "차수 승격만이 카메라를
  바꾼다"). 범위를 안 넓힌다(A-3). **먼저 재야 하는 것은 그 재풀이의 배치 손실**이다.
- **끌기가 풀이에 미치는 효과를 되살리지 않았다.** 첫 소실점이 지평선을 덮어쓰는 것
  (D-L59 ②)은 그대로다 — 이 항목은 **표시만** 열었다.
- **잡기를 점-선분 거리로 안 옮겼다.** 그려지는 지평선은 두 소실점을 잇는 **선분**인데
  (D-L101) 잡기는 스칼라 y 밴드다. 둘이 갈리는 것은 롤이 있는 시점이고 **그때는 이미
  잠겨 있어** 갈릴 자리가 없다. 확정 후 끌기가 열리면 그때 옮긴다 — `DEFERRED`.

---

## D-L105 — **꺼진 오스냅 종류를 닫힌 화면이 말한다** (2026-08-19 15차 항목 6)

사용자 지시 6(압축 기록): *"라이노 방식 패널 … **현재 켜진 것 표시** · 반경 조절."*

**패널은 이미 있었다**(2026-08-17 지시 H). 지시가 든 일곱 종류가 전부 있고 반경 입력도
있다. 이 항목이 고친 것은 둘이다.

**① 덮는 범위**(#36). ⛔ 초판은 "여덟 중 다섯이 안 재졌다"라고 적었는데 **과소 계상이다**
(1차 리뷰어 [6]): 옛 원장(`osnap_config.json`)의 `no_endpoint`는 `endpoint`와 `vertex`를
**한 팔에서 동시에** 껐고(그러면 어느 쪽 단독 토글도 안 갈린다), `perp_kind`는 그 원장이
스스로 *"가르는 것은 **존재**이지 순위가 아니다"*라고 적은 **발화 팔**이지 토글 팔이 아니다.
**개별 토글로 재진 종류는 0이었다** — "개별 활성화가 된다"의 근거가 통째로 없었다. `osnap_kinds.json`이 각 종류가 **실제로 이기는 자리**를 격자로
장면에서 찾고 거기서 그 종류만 꺼서 **앱의 승자**가 바뀌는지 본다(동작점을 손으로 안 만든다
— #46). ⚠ **판정은 `S2S.snap`이 한다**(1차 리뷰어 [1]): 초판은 후보 목록에 **하네스가**
종류 필터를 다시 걸어서, 앱의 필터(`appSnapAt`의 `.find`)를 지워도 같은 답이 나왔다 —
그러면 재는 것은 토글이 아니라 자기 필터다. `S2S.snap`은 그 `.find`를 지난다(#17).

일곱이 갈린다. `perpendicular`는 **앵커가 있어야 정의되므로** 시작점 질의의 격자에 안
나오고 끝점 스냅 경로(`S2S.endSnap` → `endSnapKindOk`, 같은 `OSNAP.kinds`)에서 잰다 —
자리는 `osnap_config`가 쓰던 t = 0.3 발 그대로다. `extension`은 **점 후보가 아니라 방향
스냅**이라(`extension.ts`) 구성상 여기서 못 재며 `extension_snap.json`의 `toggle_contrast`가
그 자리를 든다 — ⚠ **단위가 다르고**(배치 경로 증분 ↔ 승자 종류) 그 게이트의 도달 가능성은
selfcheck가 **"픽스처 결정 = 통과가 아니다"**로 분류한 값이다(1차 리뷰어 [5]).
**덮는 강도가 여기와 같지 않다**는 것을 원장이 적는다.

**② 보임.** 옛 판은 꺼진 종류를 알려면 「표시·스냅 ▾」 → 「스냅」으로 **두 번 열어야**
했다. 닫힌 화면에서는 아무 표시가 없어 "스냅이 안 걸린다"가 **고장으로 읽힌다**.
하단바 라벨이 이제 **꺼진 수**를 말한다(문자열은 `osnap_kinds.json`의 `label_arm`이 든다 —
#47: 기본 반경에 매인 글자를 산문에 안 박는다).
⚠ **라이노의 상시 막대와 같은 자리가 아니다**(1차 리뷰어 [7]): 라이노는 **어느 것이**
켜졌는지가 늘 보이고, 여기서 닫힌 화면이 주는 것은 **개수 하나**다. 사거리를 그렇게 적고
상시 막대는 `DEFERRED`에 둔다.

**③ 이름**(부수). 지시의 목록이 라이노 이름이라 툴팁에 그것을 병기했다 — 특히 **근처점**은
이 저장소에서 `on_edge`("선 위")다. 라벨 자체는 안 바꾼다(다른 자리들이 그 이름을 쓴다).

**⚠ 작업 중 #17에 걸렸다.** 새 질의(`S2S.snapCands`)의 초판이 `snapCandidates` 결과를
그대로 냈는데, **종류 필터는 `snapCandidates`가 아니라 `appSnapAt`이 건다** — 첫 실행이
"여덟 전부 안 갈린다"를 냈다. 측정이 앱보다 넓은 것을 보고 있었다. 필터를 측정 쪽에도
붙여 앱 경로와 맞췄다. **하네스의 결함이지 앱의 결함이 아니었다.**

**안 한 것**: 패널을 **늘 떠 있는 막대로** 만들지 않았다(라이노는 그렇다). 지금은 두 번
열어야 종류별 버튼이 보이고, 닫힌 화면이 주는 것은 **꺼진 수** 하나다. 화면 폭 예산과
접이식 규약(5차 지시 6-d)을 건드리는 일이라 범위 밖이다(A-3) — `DEFERRED`.

---

## D-L106 — **보조 소실점: 각도가 상태이고 자리는 계산이다** (2026-08-19 15차 항목 7)

사용자 지시 7(압축 기록): *"보조 소실점 — **미구현**. 지평선 위 틱 · 탈레스 반원 ·
각도 입력 · 화면 밖 처리 · 스냅 후보 · 경사 소실점 · 위계."*

**미구현이 맞았다** — 저장소에 보조 소실점이 없었다. 그러므로 이 항목은 재현이 아니라
**신설**이고, 재는 것은 "고쳤는가"가 아니라 **"지시대로 도는가"**다.

### 자료구조 — 각도만 저장한다

축 셋의 소실점은 카메라가 정한다(D-L101). 보조 소실점은 그 카메라 아래에서 **사용자가
고른 다른 방향**의 소실점이다. **상태를 늘리지 않는다**: 저장하는 것은 `{of, yawDeg,
pitchDeg}`이고 자리는 매번 `axisVpAt`(D-L101의 그 함수, #17)이 낸다. 카메라가 바뀌면
보조도 따라 움직인다 — 따로 고칠 것이 없다. CLAUDE.md §1이 차수를 계산으로 둔 것과 같은 자리.

### 위계 — 보조는 방향만 준다

- 축을 **배정하지 않는다**(`st.axis`는 `free`, `auxId`가 이력이다).
- 카메라에 **기여하지 않는다** — 소실점 집합에 안 들어가고 차수를 안 바꾼다.
- 경쟁에서 **비기면 주 축이 이긴다**(엄격 비교) — 보조는 사용자가 만든 것이라 틀릴 수
  있고, 틀린 보조가 축을 이기면 조용히 틀린 배치다(A-3).
  **이것은 측정된다**: `aux_vp.json`의 팔 ⑦이 각 0° 보조(축 0과 **같은 소실점**)로 경쟁을
  열고 보조가 한 번도 안 이기는 것을 든다(`tie_competed` > 0 · `tie_won` = 0).
- 강제 축(`Shift`·축 키)이 있으면 경쟁 자체를 안 한다.

⚠ **경쟁에 넣은 것이 유일한 방법이었다.** 축 스냅은 *"각도로 거르지 않는다 — 언제나 어느
축으로 간다"*(`resolveLive`의 규약)이므로, 45° 벽을 모서리에서 그으면 **반드시 어느 축으로
끌려간다.** 나중에 걸러낼 «거부» 상태가 없다. 비교는 `nearestDirOnScreen` **하나**가 한다
— 축용 `nearestAxisOnScreen`을 그 함수의 얇은 포장으로 바꿨다(#17: 축과 보조가 다른 자로
재면 안 된다).

### 이론서를 일반형으로 **잘못 읽은** 자리 둘 — 측정이 잡았다

⚠ **저장소 문서는 이미 옳게 적고 있었다**(1차 리뷰어 [13]): CLAUDE.md §7의 이론서 참조
지도가 *"6.2 | f² = |PV₁||PV₂| | L-A **2점 해**"*라고 4차 전부터 적는다. 그러므로 이것은
«문서가 몰랐다»가 아니라 **구현 한 줄의 오적용**이다.

⚠ **이론서가 틀린 것이 아니다.** 두 절 다 **2점 문맥에서 쓰였고**, 초판 구현이 그것을
일반형으로 읽었다. 어디에 그 문맥이 적혀 있는지를 갈라 적는다 —
**6.2는 제목과 전제에 명시**돼 있고(*"### 6.2 **2점 투시**"* · *"두 소실점이 지평선 위에
있고 **주점도 지평선 위에 있으므로**"*), **15.1은 좌표 규약에 들어 있다**(아래 ②).

**① 탈레스의 `|PE| = f`는 2점 전용이다**(이론서 6.2의 전제: 주점이 지평선 **위에** 있다).
3점에서는 주점이 수심이라 어느 변 위에도 없다. 반원이 주는 것은 **그 변까지의 회전 거리**
`edgeDist = |foot−E|`이고, 발–주점–눈이 직각삼각형을 이루므로

```
f² = edgeDist² − principalOffset²        (2점에서는 offset = 0 → f = |PE|)
```

이것이 6.3의 수심 정리와 7.6의 *"E의 위치도 변마다 다르다"*와 같은 말이다.
초판은 `|PE|`를 그대로 f로 썼고 **앱 픽스처(3점)에서 23% 틀렸다** — 단위 팔이 2점만 돌아서
안 잡혔다. 3점 팔을 넣어 잡았다.

**② 15.1의 «바로 위/아래»도 2점 전용이다** — 그런데 **그 절에 «2점»이라고 안 적혀 있다.**
문맥은 **좌표 규약**에 있다: 15.1은 *"**수평 방향 (a, 0, c)**를 기울여 (a, b, c)로 만들면"*
이라고 쓴다. 여기서 `(a, 0, c)`는 **카메라 좌표**이고 y 성분이 0이다 — 즉 **세계의 수평면이
카메라의 y = 0 평면과 같다**는 것이고, 그것이 곧 **피치 0(2점)**이다. 그 가정 아래에서는
"기울인다"가 카메라 y를 더하는 것과 같아 상 좌표의 u가 안 변한다(= 화면 세로).

피치가 있으면 둘이 갈린다: 세계-수직 방향은 카메라 좌표에서 y와 z 성분을 **둘 다** 가지므로
`b`를 더하는 것과 다르다. 일반형은 **경사 소실점이 «대응 수평 소실점 ↔ 수직축 소실점»을
잇는 직선 위에 있다**이고(그 직선은 `VP(h + t·u)`가 `t`에 대해 그리는 직선이다),
수직축이 무한원(2점)일 때 그것이 화면 세로가 되어 15.1의 문면이 된다.
⛔ 초판은 여기에 *"15.2가 «작도로는 측점을 쓴다»라고 적은 것도 같은 자리다 — 일반 경우는
측점 작도가 필요하다"*라고 적었는데 **오독이다**(1차 리뷰어 [5]): **15.2도 같은 2점 규약
안이다** — 측점이 지평선 위에 놓이고 실척 각도를 그리는 것은 7.5의 2점 rabatment 전제를
물려받는다. **15.2는 3점의 구제책이 아니다.** 3점에서 각도로 경사 소실점을 얻으려면 7.6대로
**변마다 다른 E**를 쓰거나 방향 벡터를 직접 투영해야 하고, 이 구현이 후자다.
이론서 3.4·15.1·15.2·15.3 네 자리에 그 단서를 달았다(#42 ⑨: 같은 문면을 전부 훑었다).

실측이 `aux_vp.json`의 `slope_dx_screen_px`(3점 픽스처에서 0이 아니다)와
`slope_line_off_px`(0이다)로 그것을 가른다. AS-L54에 적었다.

### 화면

- **틱**(지시 7-a): 화면 안이면 짧은 세로 선분 + 각도 라벨. **점이 아니다** — 점은 축의
  것이다(위계가 눈에 보여야 한다). 색도 축 색과 다르다.
- **화면 밖**(지시 7-d): 가장자리 화살촉. 규약·여백은 주 소실점의 것을 그대로 쓴다(#17).
- **경사 보조**: 대응 수평 보조와 파선으로 잇는다(그 관계가 15.1의 내용이다).
  경사를 만들면 **대응 수평 보조를 함께 만든다** — 그 선을 그으려면 그 점이 있어야 한다.
- **탈레스·측점**(지시 7-b): 토글로 켠다. 기본 **끔** — 작도선은 요청할 때 긋는 것이지
  늘 떠 있는 것이 아니다(종이에서도 그렇다).

### 안 한 것

- **6차 지시 12의 «만드는 경로 셋»(직접 찍기 · 45° 거리점 제시 · 비율 지정)과 각도기
  조작을 안 만들었다** — 기각 사유는 그 블록 머리말에 적었다(각도 입력이 b를 포함하고
  c는 각도의 다른 표기다 · a는 찍기 조리개의 실패를 물려받는다 · 각도기보다 숫자 입력이
  단순하다). ⚠ **그 블록을 초판이 인용조차 안 했다**(1차 리뷰어 [3]) — 6차·11차가 등재해 둔
  설계가 있었고 그 세 문장이 이번 실측에 반증됐다. 지금은 제자리 정정이 붙어 있다.
- **보조는 저장에 안 들어간다**(`.brnl`) — `Camera`가 아니라 앱 상태다. `DEFERRED`.
- **측점으로 길이를 재는 절차**(이론서 7.5의 7·8단계)는 **표시까지만** 했다 — 눈금을
  찍고 투사하는 도구는 범위 밖이다(A-3).
- **경계 스윕이 없다** — 비긴 자리(각 0°)는 팔 ⑦이 재지만, 축에서 1~3° 떨어진 대역은
  안 쟀다. `DEFERRED`.
- **무한원 보조**는 이 픽스처에서 안 났다(#35 — 도달 불가를 안전으로 안 읽는다).

## D-L107 — **보조 소실점을 탭이 만든다 · 토글이 선 위에 있다 · 스냅 패널이 한 번에 열린다** (2026-08-20 16차 항목 0)

16차 지시 0이 15차 지시 5·6·7의 **원문**을 다시 줬다(`docs/instructions/16.md` — 15차는
압축 기록으로 해석했고 그 유보를 스스로 적어 뒀다). 대조에서 어긋난 곳 넷을 고쳤다.

### ① 보조 소실점의 만드는 경로 — 하나가 아니라 **셋**이다 (지시 7 · 0-b)

원문: *"만드는 경로 셋 — 지평선 위 탭 · 각도기 광선 · 각도 입력 → **같은 것이다**."*
15차는 각도 입력 하나만 내고 나머지 둘을 «각도의 다른 표기»라며 기각했는데, 0-b가
그 기각을 뒤집었다 — **각도를 어떻게 주는지가 핵심이다.**

```
① 지평선 위 탭    yawFromScreenPoint — 탭점의 역투영을 수평면에 사영한 각.
                   지평선을 벗어난 성분은 pitch라 버려진다(수평 보조가 된다).
                   탈레스가 없어도 선다(1점에서도 돈다 — ②와 다른 경로라는 증거)
② 각도기 광선      protractorVp/Angle — 평면상 카메라 위치 E(탈레스 반원의 직각점)에서
                   쏜 광선의 각이 곧 세계 각이다(지시 원문·이론서 7.5의 rabatment).
                   근거: 회전이 등거리사상이고 소실점은 회전축(지평선) 위라 제자리다
③ 각도 입력        addAux — 15차의 그것
```

셋은 **같은 `addAux`로 모인다**(«같은 것»이 구조로 선다). 앱 배선: 보조 패널이 열려
있을 때 지평선 띠(`PICK_TOL`) 안 탭이 ①, 탈레스 켠 상태의 띠 밖 탭이 ②다 —
패널이 닫혀 있으면 탭은 그리기의 것이다(조용히 다른 뜻을 만들지 않는다, A-3).

**«같은 것»의 판정은 단위 팔이 든다**(`test/aux_vp.test.ts` «만드는 경로 셋» —
**2점·3점·롤 3점 전부**, #51). 배선은 `aux_tap.json`이 든다(공선성 판정 — #52).

⚠ 원문의 *"주점에서 아래로 f만큼 내려간 점"*에서 **2점 문면인 것은 «f만큼»(거리)뿐이다**
(AS-L55 — |PE| = f가 2점 전용). «주점에서 내려간다»는 전 차수에서 맞다: `thales()`의
구성상 E·발·주점이 **한 수선 위**에 있다(E = 발 + h·n̂, P = 발 + off·n̂ — 롤 0이면 그
수선이 화면 세로라 앱 픽스처(3점·롤 0)에서도 E.x = P.x다. ⛔ 초판이 여기에 «3점에서 E는
주점 수직선 위에 없다»라고 적었는데 **자기 원장 값이 반증한다**(`aux_vp.json`의
E = [640, …] · principal = [640, …] — 1차 리뷰어 [2]). x가 갈리는 것은 **롤**이고,
그것이 단위 팔 «롤이 있는 3점」이 재는 자리다). 내려가는 거리는 f가 아니라 `edgeDist`이고
광선 논증은 `|PE| = f`를 안 쓰므로 **②는 전 차수에서 성립한다** — 그것도 단위 팔이 잰다. 원문의 경사 작도(*"측점에서 30도로 쏜다"* — 15.2)는 **2점
전용임을 반례 팔이 실측한다**(`slopeByMeasuringPoint` — 3점에서 >1px 어긋난다). 앱이
쓰는 것은 3D 투영(`auxDirVec`)이라 전 차수에서 옳고, 그 작도 함수는 원장이 어긋남을
재는 팔로만 산다.

**기각 하나**: 화면 밖 E의 «가장자리 다이얼» — 표시는 가장자리 표식으로 하고(주 소실점
규약 그대로 #17) **계산은 실제 위치**(원문 그대로 — 원래부터 그랬다). 끌어서 각을 주는
다이얼 조작은 안 만들었다: 각을 주는 조작이 이미 둘(탭·입력) 있고 화면 밖 E의 다이얼은
좌표계를 하나 더 만든다(A-3 — 실사용에서 각도기 조작이 아쉬우면 되살린다, `DEFERRED`).

### ② 경사 연결선을 일반형으로 (지시 7)

`drawAuxVps`가 경사 보조와 수평 짝을 **화면 세로선**으로 이었다 — 15.1의 2점 문면이다.
**짝 소실점의 실제 좌표로 잇는다**: 경사 보조는 «수평 짝 ↔ 수직축 소실점» 직선 위에
있으므로(일반형 — 단위 팔이 잰다) 2점에서는 저절로 세로선으로 되돌아온다. 두 문면이
한 코드가 됐다.

### ③ 지평선 토글이 선 위에 있다 (지시 5)

원문: *"지평선에 토글을 붙인다 — **켜고 끄기를 그 자리에서**."* 15차(D-L104)는 토글을
하단바 접이식 메뉴에만 뒀다. 고친 것: **가장자리 손잡이 자리의 탭**이 켜고(끔 상태에도
흐린 고리가 그 자리에 남는다), **같은 손잡이의 무이동 탭**이 끈다(움직이면 끌기 —
갈림은 `PICK_TOL` 무이동 × 손잡이 위 시작의 논리곱). 하단바 버튼은 남는다(상태는
`SHOW_HORIZON` 하나 — 자리만 늘었다). 원장: `horizon_chip.json`.
⚠ 탭 판정 띠(`horizon_chip.json`의 `setup.band_px` — PICK_TOL 유도값)보다 짧은,
손잡이에서 시작한 끌기는 토글로 읽힌다 — 비용을 알고 고른 규약이다(끌기는 보통 선
가운데서 시작한다).

### ④ 스냅 버튼이 접이식 밖으로 (지시 6)

원문: *"현재 켜진 것이 **한눈에** 보인다. 반경 조절도 같은 자리에."* 15차(D-L105)는
꺼진 수를 라벨에 냈지만 버튼 자체가 「표시·스냅 ▾」 안에 접혀 있어 **두 번 열어야**
했다. 버튼을 상시 노출로 빼고 패널을 메뉴와 무관하게 한 번의 탭으로 펼친다 —
라이노의 상시 오스냅 막대에 한 단계 더 가깝다(A-3: 선례).

### 어긋나지 않았던 것 (대조의 나머지)

지시 5의 «드래그 = 시선 높이» — 확정 후는 «못 옮긴다»로 정한 것이 맞고 근거(재풀이
손실 미측정)도 적절하다고 **지시가 확인했다**. 확정 전 끌기가 구도 안내선인 것(첫
소실점이 덮는다 — D-L59 ②)은 그대로다. 지시 6의 일곱 종류·반경 조절, 지시 7의 틱·
스냅 후보(축이 아니다 — 두 축의 조합)·경사 여러 개·위계(채널·상태 없음, 승격·내보내기·
지우개 대상 아님 — 보조는 획이 아니라 앱 상태의 각도라 그 경로들이 아예 안 본다)는
15차 구현이 원문과 맞았다.

---

## D-L108 — 화면의 소실점은 **한 함수**에서 온다 (2026-08-20 17차, 사람 지시)

**결정**: 소실점을 읽는 자리는 `ui/vpSource.screenVps` **하나**다. 규칙은
「핀·미확정이면 규칙 상태의 값(`CamState.vps()`), 그 밖이면 지금 시점의 투영
(`frame().ctx.vps`)」이고, **표시**(소실점 표식·`viewOverlayCtx`·`drawDirLines`)와
**스냅**(`resolve2d` → `vpDirSnap`)이 함께 그 함수를 지난다.

**왜**: 옛 판은 표시가 시점 투영을, 스냅이 확정 시점 값을 읽었다. 갈리는 조건은 «비핀»이
아니라 **«자세가 확정 카메라와 다름»**이다(핀을 풀되 자세를 보존하면 여전히 같다 —
`vp_source.json@f351839a`의 자세 ③이 그 음성 대조다). 크기는 같은 원장의
`summary.legacy_source_aim_deg_rotated`(도 — 스냅이 보는 단위)가 든다.
사용자 보고: *"축 스냅을 켜고 그으면 선들이 한 점으로 모이는데 그 점이 화면에 표시된
소실점이 아니다."* 같은 화면 안에서도 `drawDirLines`만 확정 시점 값을 읽었다.

**관측이 그 상태였다는 근거**: 상태 표시 「뷰 1」은 뷰의 **이름**이고 `viewForDrawing()`은
**카메라가 서 있고 핀이 아닐 때만** 그 이름을 만든다. 회전은 필요 없다.
⚠ 그 자세에 **어떤 조작으로** 도달했는지는 특정하지 못했다(`vp_source.json`의
`observation_match.what_is_not_shown`).

**어느 값이 옳은가**: 지금 시점의 투영이다 — 사용자가 겨냥하는 것은 눈앞의 화면이고,
3D 축 스냅(`snapToAxis`)은 이미 그 시점의 축 방향을 쓴다. **기하의 논거이지 측정이 아니다.**

**잠정인 것**: `cam.rules.horizon`(확정 시점의 지평선 y)도 같은 형태의 파생 상태다.
지금은 읽는 자리가 핀으로 막혀 있어 안 갈린다 — **막힘이 풀리면 같은 자리가 다시 열린다.**
이 항목에서는 범위를 안 넓혔다(A-3).

---

## D-L109 — **지평선을 먼저 긋는다.** 선 하나가 소실점 하나를 만든다 (2026-08-20 18차, 사람 지시)

**결정**: 공간 정의 절차를 바꾼다.

```
1. 지평선을 긋는다            시선 높이가 정해진다. 롤 0
2. 깊이선 하나                지평선과의 교점 = 첫 소실점. 1점 확정
3. 다른 방향 깊이선 하나      그 교점 = 두 번째 소실점. 2점 확정
4. 확정 순간 내가 그은 지평선과 초기 선이 사라진다
   (⛔ **19차 D-L114가 시점을 옮겼다** — 초기 선은 «그리기 첫 획»까지 **작도선으로 옅게
   남는다**. 그은 지평선은 종전대로 확정 순간 계산된 지평선에 자리를 넘긴다.
   이 줄만 읽고 현행으로 삼지 않는다)
```

**왜**: 손으로 투시도를 그릴 때도 소실점과 지평선을 정하고 시작한다. 종이에서 안 긋는 것은
**펜으로 그으면 못 지우기 때문**이고, 지울 수 있으면 긋는 것이 자연스럽다.
그리고 그 한 줄이 **판정을 계산으로 바꾼다** — 소실점 = 깊이선 × 지평선이다.

**함께 폐기한 것**(그 기계 전부가 «어느 둘이 같은 축인가»를 푸는 것이었다):
`RuleState.depthLines`(대기 풀) · `resolvePool` · `poolCandidates` · `resolveDeclaredPool` ·
`beyondSegment` · `insideSegment` · `convergeDeg` · `pickVpAt`(점 찍기) · `defaultHorizon` ·
`screen_or_depth` 물음(`RuleEvent.ask`·`answerAsk`·`drawAsk`·물음 패널) · 잠정 그리드 ·
D-L96(확정 조건 완화) · **D-L53의 «P1 불가역»**.

**D-L53을 대체하는 자리**: 1점 → 2점 **승격이 열린다**. 첫 깊이선이 서면 남는 수평축은
정의상 화면 평행이라 규칙이 그 슬롯을 **자동으로** 채우고(`Slot.implicit`), 두 번째 깊이선이
그것을 밀어낸다. ⚠ **사용자가 선언한 가로축(`declared`)은 안 밀어낸다** — D-L53이 막던
«손 오차로 얕아진 깊이 모서리가 조용히 1점에 가둔다»는 그 구분이 그대로 막는다.

**지평선은 `null`에서 시작한다**(지시 j — 기본 위치를 두지 않는다). 그리기는 그 뒤에 열린다
(지시 l). ⚠ 그것이 실사용을 막는지는 **안 쟀다** — `DEFERRED`에 되살릴 조건을 적었다.

**전환이 안 느껴지는 이유는 구성이다**: 소실점 = 선 × 지평선이므로 소실점의 y가 곧 지평선이다.
확정 뒤 계산된 지평선(D-L102)이 **그은 자리와 같은 y**에 그려진다 — 값이 아니라 배선이다(#5).

## D-L110 — 알림은 **화면 최상단 한 줄**이다 (2026-08-20 18차, 사람 지시 f~i)

**결정**: 박스를 만들지 않는다. 문구 한 줄이고, 선택이 필요하면 **밑줄 친 단어**를 누른다
(캐드 명령행의 선례 — A-3). 평소에는 **아무것도 안 띄운다**(지시 g) — 안내를 두지 않고
오류·혼동이 있을 때만 뜬다. 한 줄에 안 들어가면 **문구를 줄인다**(지시 i) — 두 줄로 안 늘린다.

**강제 수단이 CSS다**: `white-space: nowrap` + 말줄임 + `:empty{display:none}`.
문구를 늘리면 **잘려서 보이므로** 규칙이 코드로 지켜진다.

**동작은 문구의 것이다**(#54): `noteActsFor`가 그 문구를 들고 있어 **문구가 바뀌면 동작이
자동으로 죽는다** — 둘을 따로 두면 낡은 동작이 다른 문구에 붙는다.

**함께 사라진 것**: 승격 요약 패널 · 물음 패널 · 「선이 아직 입체를 정하지 않았습니다」 안내.

## D-L111 — 렌즈는 **정렬·확인 시점에서만** 바꾼다 (2026-08-20 18차, 사람 지시 r)

**결정**: 초점거리 `f`를 사람이 조절한다. 단, **작도 중에는 못 바꾼다** —
확정 시점의 `f`는 사용자가 그은 소실점이 정한 값이고(이론서 6.2), 그것을 손으로 밀면
**그린 투시도와 카메라가 어긋난다.** 조절이 열리는 자리는 큐브로 옮긴 시점(정렬·확인)뿐이고,
「작도 시점으로」 돌아오면 원래 `f`로 **복귀한다**.

**표현은 둘이다**(지시 r-2): **mm**(35mm 환산, `36·f/W`)와 **가로 화각**(`2·atan(W/2f)`).
클램프는 `LENS_MM = {min:14, max:200}`. 조작은 **mm 하나만** 받는다 — 화각은 그 값에서
계산된다(#54: 같은 양을 두 곳에서 안 만든다).

**자리는 시점 패널이다**(r-5는 "큐브 옆이나 시점 패널"을 골랐다). 시점 패널(`#views`)이
**큐브 바로 아래**라 둘을 동시에 만족한다. ⛔ **초판은 하단바에 뒀는데 그것은 지시가 고른
두 자리가 아니다** — 게다가 D-L113으로 줄바꿈이 된 바에 슬라이더를 얹으면 바가 화면을
더 먹는다. 자리도 잰다(`lens_roll_osnap.json@f351839a`의 `r_place`: 시점 패널 안 · 큐브 아래 ·
확정 시점에서 비활성).

**3D는 안 움직이고 화면은 움직인다**(r-3): `f`는 **투영만** 바꾼다. 실측 —
`lens_roll_osnap.json@f351839a`에서 28.1mm → 24mm로 바꿔도 `geom_same = true`(놓인 획 13개 전부).
⚠ 그것만으로는 부족하다(#30): **f 숫자만 바뀌고 렌더가 그것을 안 읽는 상태**와 안 갈린다.
그래서 **양성 채널**을 함께 잰다 — 같은 획(`ofId` 대조)의 끝점이 화면에서 **12.59px**
움직인다(`r_lens.marked_end`). 자리는 앱의 스냅 후보가 낸다(#17).

**양방향이다**(r-2): mm 칸과 **가로 화각 칸**을 둘 다 둔다. `setLensFovDeg(90)`을 부르면
18mm가 된다(`r_lens.by_fov`). 죄는 자리는 `setLensMm` 하나다(#54).
⚠ **잰 것은 함수 호출까지다** — 칸에 입력 사건을 던져 다른 쪽이 따라오는지는 안 쟀다
(칸이 보이는 것과 그 값은 DOM으로 쟀다 — `r_lens.ui`).

**복귀도 배선이다**(r-4·#5): `pinTo`가 `freeIntr`를 비우므로 확정 시점의 `f`는
저장된 값 그대로다. 실측 1000 → 853.3 → 1000, 그리고 그 자리의 `adjustable = false`.

**안 잰 것**: 렌즈를 바꾼 화면이 «옳은가». 카메라 조작이므로 옳고 그름이 없다.

## D-L112 — 큐브 정렬은 **롤을 0으로 접는다** (2026-08-20 18차, 사람 지시 s)

**결정**: 면·모서리·꼭짓점 스냅과 「작도 시점으로」가 도착한 자세는 **그린 축 기준으로**
롤 0이어야 한다. 화면 가로가 한 축, 화면 세로가 다른 축이다.

**원인은 three.js의 기본값이었다**: OrbitControls는 `camera.up`을 +Y에 고정한다 —
`setPose`가 방향만 주면 **세계 +Y가 화면 위**가 되므로, 그린 세로축이 세계 Y와 어긋난 만큼
화면이 기운다. 수리는 한 줄이다 — `setPose`가 `cam.up`을 **그린 기준계의 Y**로 세운다.

**실측**(`lens_roll_osnap.json@f351839a`): 면 탭 뒤 축 셋의 화면 방향이
수리 전 **−8.7° · 0° · −98.7°** → 수리 후 **0° · 0° · −90°**(직교 이탈 전부 0).
그 상태에서 끝점에서 그은 화면 수평선이 **축1로 확정(축과 0.1°)** 된다.

⚠ **수리가 든 뒤 그 0은 구성의 귀결에 가깝다**(#5) — `cam.up`을 그린 Y로 세우고 시선을
면 법선에 맞추면 축의 상은 정확히 직교한다. 그러므로 이 0이 재는 것은 «각»이 아니라
**배선이 그 자리에 있는가**이고, 정보량은 «수리 전 8.7° ↔ 수리 후 0»의 대조에서 나온다.

**동작점을 하나로 두지 않았다**(#12): 모서리(2점)·꼭짓점(3점)·「작도 시점으로」(s-4)에서도
**세로축 이탈이 0**이다(`more_vertical_off_ortho_deg`). 그 탭들이 시선을 각각
**44.4°·54.6°** 옮겼다는 것을 함께 낸다 — 안 움직인 탭의 0은 아무것도 안 재기 때문이다
(초판이 실제로 그 함정에 빠졌다). 「작도 시점으로」의 피치는 **그린 기준계에서** 0이다
(`return_judge.pitchDeg` -0.227° — ⚠ **정확히 0이 아니다**.
그 잔차와 «애니메이션이 아니다»의 근거는 원장 `return_residual`이 든다. **원인은 모른다**) — 세계 Y 기준의 `polar`은 그린 세로축이 기운 만큼
90°에서 벗어나므로 그 프레임으로 판정하면 틀린 것을 잰다(#49).

**특이 경우**(s-3): 위·아래에서 내려다볼 때는 시선과 그린 Y가 나란해 롤이 정의되지 않는다 —
`snapToDir`가 그람-슈미트로 **그린 X를 화면 가로에 맞춘다**.
⚠⚠ **그 갈래에 도달하지 못했다**(`top_reached: false`). 시도 둘(위 모서리 반복 탭 ·
궤도로 내려다본 뒤 중앙 탭)이 둘 다 «가장 가까운 수평 면»으로 되돌아갔다 —
큐브의 윗면은 `CUBE_TOL.top_center`상 **이미 가파르게 내려다볼 때만** 열린다.
**그러므로 그람-슈미트 갈래는 안 덮인다**(#32 — 안 잰 것이지 «없다»가 아니다).

**안 잰 것**: 안 붙은 시작점의 획은 그래도 `free`로 대기한다 — 그것은 **앵커 가드의 설계**
(D-L83)이지 이 항목이 고칠 것이 아니다. 원장의 `s_unanchored_probe`가 그 대조다.

## D-L113 — 하단바는 **줄바꿈한다** (2026-08-20 18차, 사람 지시 t)

**결정**: `#bar`를 `flex-wrap: wrap`으로 바꾼다(`max-height:40vh` + 세로 스크롤).
옛 판은 `overflow-x:auto` 한 줄이라 **펼친 오스냅 패널이 스크롤 밖으로 밀렸다**.

**실측**(`lens_roll_osnap.json@f351839a`): 폭 500px에서 종류 버튼 여덟 중
수리 전 **4/8**(가로 넘침 948px) → 수리 후 **8/8**(넘침 0, 두 줄).
더 좁은 **360px**에서도 8/8이다. 「보인다」의 정의는 **버튼 상자가 `#bar` 상자 안에
가로·세로 둘 다 드는가**이므로, 줄바꿈 뒤 세로로 밀리는 경우도 그 판정에 들어 있다.
1280px에서는 여덟이 다 보여서 **안 드러났다** — 좁은 폭이 아니면 안 잡히는 결함이다(#21의 이웃).

**왜 줄바꿈인가**(A-3): 스크롤이면 «찾아서 미는» 조작이 남고 폭마다 다시 잘린다.
줄바꿈이면 **어느 폭에서도 안 잘린다.**

**안 잰 것**: 폭 둘(500·360px)·한 글꼴 크기다. `max-height:40vh`를 넘길 만큼 좁거나
낮은 창은 안 쟀다 — 그 자리에서는 세로 스크롤이 생기고, 그것이 이 결정의 남은 한계다.

## D-L114 — 소실점을 만든 획은 **작도선으로 남았다가 그리기 첫 획에서 사라진다** (2026-08-20 19차, 사람 지시 1·2)

**결정**: 18차가 «소실점을 만든 획은 **그 순간** 사라진다»(지시 4·m)로 둔 삭제 시점을
**사용자가 그리기로 넘어가는 첫 획**으로 옮긴다. 그때까지 그 획들은 `seedLines`에
표시 전용 자취로 남아 **옅게 보인다**(19차 지시 2-c).

**왜 바꾸나 — 사람이 그려 보니 그 «순간»이 둘로 걸렸다**:
① **확정 후 그린 선이 즉시 사라진다**(지시 1) — 삭제가 `ruleEvent === "vp_fixed"` 가지에
   있어 **획마다** 돈다. 1점이 선 **뒤에** 그은 둘째 깊이선(1점 → 2점 승격)도 같은 가지에
   걸린다. 사용자에게는 «공간이 정의된 뒤 그은 선이 눈앞에서 없어지는 것»이다.
② **초기 선이 너무 일찍 사라진다**(지시 2) — 첫 깊이선이 1점 확정 순간 없어져
   둘째를 겨냥할 방향이 화면에 없다.

**실측**: 재현(수리 **전**, 브라우저·실제 포인터)에서 깊이선2를 긋자 `doc.strokes`가
**0**이고 `seeds`도 0이었다(그 자리의 픽셀은 19차 팔보다 609·644 적다 — ⚠ **«0»이 아니라
«적다»다**: 그 띠에 그리드가 지나 무잉크 기준선이 없다, 2차 리뷰어 [R8]). ⛔ **초판은 «그은 자리 픽셀 −1098»을 표제로 썼고
그 수는 오염돼 있었다**(리뷰어 [1]) — 확정이 그리드를 다시 그리므로 **단계 사이의 차**에는
그리드 몫이 섞인다. 정본은 **같은 단계·같은 띠의 두 팔 차**다:
`seed_ink` = **[609, 644]**(19차 팔 − 18차 거동 팔 — 그리드가 상쇄된다).
수리 후 `seeds`가 1 → 2 → (그리기 첫 획) 0이다
(`procedure_repair.json@f351839a`의 `arms.A` · `seed_ink`).
⚠ «저장된 뒤 지워진다»(지시 1-a의 셋 중 셋째)는 **이 결정이 고친 코드 경로의 읽기이지
실측이 아니다** — 밖에서 잰 것은 «남지 않는다»까지다.

**사거리**(리뷰어 [3]): 사라지는 것은 **소실점을 만드는 획**뿐이다.
⛔ **그 근거는 두 팔의 등식이 아니다**(2차 리뷰어 [R2] — `plain_ink` new == old는
`setSeedLines`가 그 경로를 안 지나므로 **구성상 보장**이다, #5). 서는 것은 셋:
① **측정** — 지금 보통 획의 잉크가 `plain_ink.new` **954**로 0보다 크다
② **변경의 범위** — 이 결정이 만진 가지는 `ruleEvent === "vp_fixed"` 하나이고 보통 획은
   거기 안 들어간다(코드 읽기다)
③ **손으로 한 확인** — 착수 때의 수리 전 브라우저 실행에서 보통 획 셋이 남았다(원장 밖).
⚠ **B·C에는 코드를 되돌린 `revert_check`가 있는데 이 대역에는 없다.**

**왜 «2점까지 기다린다»가 아닌가**(지시 2-b): **1점으로 끝낼 수도 있으므로** 그 조건은
안 선다. «그리기 첫 획»은 차수와 무관하게 선다.

**획이 아니라 표시다**: `doc.strokes`에서 빼는 것은 **그대로 둔다**(18차 지시 4의
«2D 레이어에도 안 남는다»). 자취는 스냅 대상도, 일괄 풀이 대상도, 저장 대상도 아니다.
⚠ 다만 **되돌리기는 함께 되돌린다**(`AppSnap.seeds`) — 안 그러면 실행 취소로 지평선
단계에 돌아갔는데 주인 없는 옅은 선이 남는다. `snapDiff`가 그 자리를 이름으로 짚는다(#30).

**대가**: 확정과 «그리기 첫 획» 사이에 **화면이 한 번 바뀐다**(작도선이 사라진다).
18차 지시 m의 «전환이 안 느껴져야 한다»는 그 순간을 **뒤로 미룬 것**이지 없앤 것이 아니다.
사람이 그것을 요구했으므로 그대로 두고, 여기 적는다.

**안 잰 것**: 옅기(알파 0.28)를 사람이 «보인다»로 받는지는 안 쟀다 — 값 하나를 골랐고
스윕이 없다(#12). 판정은 다음 표본이다.

## D-L115 — **소실점이 얹혀 있지 않은 지평선은 문서에서 안 읽는다** (2026-08-20 19차, 사람 지시 3)

**결정**: 저장본을 열 때(`loadRulesFromDocument`) `rules.horizon`이 있는데
**유한 수평 소실점이 하나도 없으면** 그 지평선을 `null`로 만든다.
**되돌리기는 이 게이트를 안 지난다**(`loadRules`가 그 입구다).

**무엇이 났나**(지시 3): *"상단에 그어도 화면 중간 높이에 지평선이 생긴다. 내가 그은 y가
안 쓰인다."* — 원인은 코드의 기본값이 아니라 **저장본**이었다. 18차 이전의
`newRuleState`가 `horizon: defaultHorizon(imgSize) = imgSize[1] / 2`(**화면 중앙**)를
기본값으로 넣었고(`git show b9000fc~1:web/src/s3d/vpRules.ts`에서 확인 — #55),
그 값이 자동 저장본에 담겼다. 복원이 그것을 그대로 읽으면 `hasHorizon()`이 **처음부터
참**이라 **지평선 단계가 통째로 안 열린다.**

**실측**(재현 — 수리 **전**): 옛 형식 저장본(획 하나 + `horizon: 674/2`)을 심고
새로고침하니 `rules.horizon` **337**로 복원되고, 그 상태에서 상단 y=120에 그은 획이
**가로축 선언**(`slots[0] = screen/h/declared`)으로 먹혔다. 수리 후 같은 입력에서
`horizon_y`가 `null`이고, 상단에 그은 y가 그대로 지평선이 된다
(`procedure_repair.json@f351839a`의 `horizon`).

**판별자는 소실점이다**: 새 절차에서 수평 소실점은 **선 × 지평선**이라 정의상 그 지평선
위에 있다 — 소실점이 서 있으면 그 지평선은 **쓰이고 있는 것**이므로 반드시 지킨다
(지우면 확정된 축이 소리 없이 무너진다). 소실점이 없으면 그 지평선은 아무것도 안
떠받치고 있고, **사용자가 그은 것인지 옛 기본값인지 저장본만으로는 못 가른다** —
A-3이 그 자리를 정한다(**애매하면 놓지 않는다**). 사용자는 하나를 다시 긋는다.

**왜 되돌리기는 제외하나**: 실행 취소 스냅샷은 **이 세션이 방금 만든 상태**라 출처가 이미
알려져 있다. 게이트를 공통 입구(`loadRules`)에 두자 **undo 세 번째에서 지평선이
150 → `null`이 됐다**(실측으로 걸렸다) — 18차 지시 p(«실행 취소로 지평선 단계로
돌아갈 수 있어야 한다»)가 깨진다. 그래서 입구를 둘로 나눴다.
⚠ 이것은 #54(파생값을 둘로 나누면 갈린다)와 **다른 자리**다: 나눈 것은 계산이 아니라
**신뢰 경계**이고, 두 입구가 같은 값을 내야 할 이유가 없다.

**대가**(⛔ **초판이 이것을 과소평가했다 — 리뷰어 [6]이 잡았다**): 손실 조건은
「지평선 있음 + **유한** 수평 소실점 없음」이고, 그 상태로 저장된 문서를 열면 지평선이
사라진다. 초판은 *"그 창은 좁다 — 자동 저장은 획이 하나라도 있어야 복원된다"*고 적었는데
**그것은 창을 좁히는 근거가 아니라 손실이 실제로 일어나는 조건**이다: 획이 있어야
복원되고, 복원되어야 이 게이트를 지난다. 원장의 `restored_old_save_no_vp`
(`strokes` 1 · `horizon_y` null)가 **바로 그 모양**이다.

**닿는 상태는 둘이다**(둘 다 «유한 소실점 없음»):
① 지평선만 긋고 깊이선을 아직 안 그은 문서(대기 획은 있다).
② **화면 가로축이 선언된 문서**(`slots[0] = screen/h/declared`) — 무한원 소실점(c=0,
   이론서 2.2)은 «유한»이 아니므로 판별자에 안 걸린다. 재현이 실제로 그 상태를 냈다.
그 둘에서는 **축은 남고 지평선만 사라진다.** 사용자가 지평선 하나를 다시 긋는다.

**그래도 이 판별자를 고른 이유**: 반대 방향의 대가는 **모든 옛 저장본에서 절차가 아예
안 열리는 것**이고(사람이 보고한 그것), 위 둘은 **획 하나를 다시 긋는 것**이다.
무한원 축까지 «지평선이 쓰이고 있다»로 세면 옛 저장본 중 가로축이 선언된 것들이
그대로 통과해 **결함이 절반 남는다** — 실제로 그 상태가 결함 3의 재현에서 나왔다.

**안 잰 것**: 실제 사용자 파일이 그 형식인지는 **모른다** — 재현한 것은 «그 형식이 오면
어떻게 되는가»이지 «사용자 파일이 그 형식이다»가 아니다. 사람 보고의 증상과 이 재현이
같은 모양이라는 것까지가 근거다.

## D-L116 — **3D 기하가 없으면 「면 위」는 후보에도 없다** (2026-08-20 19차, 사람 지시 4)

**결정**: `snapCtx`가 지면 평면을 내는 조건에 **`lifted(doc).length`**를 더한다.
기하가 하나도 없으면 `ground: null`이고 `on_face` 후보가 아예 안 생긴다.

**무엇이 났나**: *"기하가 없는 상태에서 「면 위」 스냅이 표시된다."* 지면과 시선의 교점은
**언제나 있으므로** 후보가 언제나 나왔다. **D-L83이 막은 것은 앵커 자격(좌표 확정)이고
후보 목록이 아니었다** — 표식은 그대로 떴다(지시 4-a의 답: 되살아난 것이 아니라 **다른 층**).

**실측**(재현 — 수리 **전**): 1점 확정 직후 `doc.strokes` 0 · `lifted` 0인데
`S2S.snap([500,400])`이 `on_face`를 낸다(`dist 0` · `ofId null` · 세계 y=1 평면).
네 점 전부에서 났다. 수리 후 같은 자리에서 후보 **0**이고, 기하가 생기면 **돌아온다**
(`procedure_repair.json@f351839a`의 `face` — 막은 것이지 죽인 것이 아니다).

**«면이 있다»의 대리**: 지면은 그 위에 놓인 기하를 짚기 위한 면이고, 짚을 것이 없으면
그 면은 **임의 좌표를 만드는 장치**일 뿐이다 — D-L83이 `on_face`를 앵커에서 뺀 것과
같은 논거의 표시 층 판본이다.

**안 건드린 것**: **첫 앵커의 지면 배치**(D-L90)는 그대로다 — 그 경로는 `groundAnchor.ts`가
자기 `groundFrame`을 직접 세우고 조건이 «3D가 하나도 없을 때»라 **정반대 자리**다.
⛔ **초판은 이 확인을 «종단에서 같은 실행이 한다»고 적었는데 그 원장에 `placeBy`가
없었다**(리뷰어 [11] — #55: 확인 못 한 것을 확인한 것으로 적었다). **필드를 넣어 실제로
같은 실행이 들게 했다**: `procedure_repair.json@f351839a`의 `face.empty.placeBy.ground`
**0** → `face.with_geometry.placeBy.ground` **1**.
⚠ 지시 4-c(«새 절차에서 지평선이 평면으로 취급되는가»)의 답은 **아니다** — 이 평면은
`groundFrame`(눈높이 1)이고 새 절차 이전부터 있었다.


## D-L117 — 그은 지평선의 y는 **끝점의 y**다 (2026-08-20 19차 2차 리뷰어 [R6])

**결정**: 지평선 획에서 시선 높이로 삼는 값은 **끝점(마지막 점)의 y**다.
18차가 `drawHorizonAt(raw[raw.length - 1][1])`로 그렇게 구현했고 **어디에도 안 적혀
있었다** — 리뷰어가 그것을 물어 여기 적는다(결정을 새로 하는 것이 아니라 **등재**다).

**왜 하나의 y인가**: 지평선은 화면 수평으로 강제되므로(18차 지시 j — 롤 0) y 하나로
표현된다. 그 하나를 무엇으로 정하느냐가 남는다.

**왜 끝점인가**: **손이 마지막으로 있던 자리**이고, 그리는 내내 미리보기(`horizonDraft`)가
그 y에 선을 보인다 — 사용자가 **보면서 놓은 자리**가 곧 결과다(§11의 «보인 대로 놓인다»).
평균·중점을 쓰면 **보이던 선과 놓인 선이 갈린다.**

**대가**(적어 둔다): **방향 의존이다.** 같은 획을 반대로 그으면 반대쪽 끝의 y가 된다 —
기울여 그은 만큼 시선 높이가 달라진다. 실측: `procedure_repair.json@f351839a`의
`horizon.tilted`가 임계 아래·위 두 점에서 그 규칙을 값으로 든다(둘 다 `got == end_y`이고
`start_y`와 다르다). 기울기가 크면 그 차도 크다 — 그것이 사용자에게 보이는 대가다.

**안 잰 것**: 사람이 실제로 지평선을 얼마나 기울여 긋는지(#12 — 동작점 둘을 합성으로
만들었을 뿐이다). 그 분포는 실획 표본이 낸다.


## D-L118 — **국면 분리: 그리기 국면의 획은 카메라 규칙에 넣지 않는다** (2026-08-20 20차, 사람 지시 2)

**결정**: 확정 후 사용자가 **그리기로 넘어간 뒤**(19차가 seeds를 지우는 그 경계 — 소실점을
안 만드는 첫 획)에는 `onStrokeEnd`가 획을 `feedStroke`(카메라 규칙)에 **넣지 않는다.**
작도 국면(확정 전 + 획마다 소실점을 만드는 동안)은 종전대로 넣는다 — 확정(NONE→P1·P2)과
둘째 깊이선의 1점→2점 승격이 그 경로다. 전이는 둘뿐이다: `vp_fixed` → 작도 유지 ·
그 외 획 → 그리기. 측정 스위치 `S2S.setDraftGate(false)`가 수리 전 거동이다(#30).

**무엇이 났나**(사람 보고 — 세 번 고쳐 세 번 재발): *"확정 후 그은 획이 화면에서
사라진다."* 표식 원장 초판(수리 전) 실행이 특정했다 — **P1 상태에서 그은 자유 방향
보통 획(A3)이 `vp_fixed`를 내고 작도선으로 소비됐다**(문서에서 빠지는 유일한 자리
`vp_fixed_delete`가 발화). ⚠ 그 초판 실행은 커밋에 없다(#25) — 커밋에 남는 근거는
`post_confirm_trace.json@f351839a`의 **C1 되살림 팔**(같은 좌표·수리 전 거동:
`in_doc` false · order 1→2)과 `post_confirm_invariant.json@f351839a`의
`revived_bug`·`revived_fan`(24방향 중 소비 6 — 가르는 조건의 서술은 **후보**다:
거절 행의 `note`가 f² ≤ 0 문구를 드는 것이 방증이고, 행별 f² 부호 팔은 없다 · 2차 [R5])이다.
기전은 둘의 겹침이다:

1. `vpRules.ts`의 깊이선 가지(18차 지시 3)는 **암묵 화면 가로축(implicit)이 남아 있는 한
   언제든** 그것을 밀어내고 소실점을 세운다 — 1점→2점 승격의 문. P1 상태에서는 지평선에
   닿는 **모든 사선**이 이 문에 걸린다.
2. `onStrokeEnd`는 **획마다** 규칙에 넣었다. 그 자리 주석의 근거는 «지지선 세기와 3점
   입구가 살아야 하기 때문»(7차 3-d 무렵)인데, **3점 입구는 같은 7차(3-b)가 지웠다** —
   남은 근거는 지지선 세기뿐이었다. 카메라를 만드는 판정이 카메라를 쓰는 국면에서도
   돌았고, 그것이 지시 2-a의 답이다.

세 번의 수리(D-L98·L99 연결, D-L114 삭제 시점)가 전부 **이 문 아래층의 증상**이었다 —
문 자체(규칙이 그리기 획을 계속 받는 것)는 그대로였으므로 재발했다.

**뺀 것과 그 대가**(지시 2-e — 지우는 것이 고치는 것보다 위험하다):

| 뺀 것 | 그리기 국면에서 잃는 것 | 판단 |
|---|---|---|
| `vp_fixed`(슬롯 채움) | 그리기 중 사선의 조용한 1점→2점 승격 | **이것이 버그였다.** 카메라 변경이 의도라면 작도 국면(확정 직후 연속 작도)에서 한다 |
| `support`(지지선 세기) | 확정 후 획의 지지선 카운트 증가 | 표시·진단용이다 — 차수(`perspectiveOrder`)를 안 바꾼다. 원장 영향은 전량 검증이 확인 |
| `rejected` 알림(«어디도 향하지 않습니다» 등) | 그리기 획에 대한 규칙 경고 | D-L100이 좁히던 그 소음의 소멸 — 그리기 획은 «잘못 그은 작도선»이 아니다 |

**되돌릴 조건**: ① 실사용 표본에서 «그리기 도중 2점 승격» 의도가 확인되면(사선을 긋고
승격을 기대한 손짓) — 그때는 게이트 삭제가 아니라 **명시적 승격 입구**(작도 재진입)를
설계한다. ② 지지선 세기의 부재가 어느 표시·판정을 실제로 굶기면 — `feedStroke`를 빼는
대신 `stepRule`에 «슬롯 불변 모드»를 만든다(지금은 A-3의 가장 단순한 쪽을 골랐다).

**대가(닫지 않은 것)**: 1점으로 **저장한 문서를 다시 열면** 작도 자취가 저장되지 않으므로
그리기 국면으로 복원된다 — 둘째 깊이선로 2점 승격하는 길이 막힌다(그 획은 2D 대기로
남고, 사라지지는 않는다). DEFERRED 20차 행이 이것을 든다.

**불변식**(지시 3 — 이제 구조가 보증하고 테스트가 잠근다): *그리기 국면에 들어간 뒤
그은 획은 반드시 화면에 남는다.* ⚠ 문면을 «확정 후 전부»로 적지 않는다(1차 리뷰어 [2])
— 작도 국면(확정 직후, 획마다 소실점을 만드는 동안)의 사선은 설계대로 작도선이 되어
사라진다(18차 지시 4 · DEFERRED 20차 3행이 그 잔존 사거리를 든다). ⚠ **보고된 손짓이
어느 국면이었는지는 모른다**(2차 [R1] · #55 — 보고 문면은 «확정 후»뿐이다). 그리기
국면이었다면 이 불변식이 그 자리를 막았고, 작도 국면(확정 직후 첫 사선)이었다면
DEFERRED 3행이 그 자리다 — 같은 증상이 또 오면 표식(`strokeTrace`)과 `draftPhase()`가
그 자리에서 가른다. `post_confirm_invariant.json@f351839a` —
P1·P2에서 고정 표의 수십 획 전수 생존(문서 + 대기 획의 중점 픽셀 + 3D 층 등재), 그리고
`setDraftGate(false)` 팔이 버그를 되살려 **실제로 사라지는 것**(방향별 24장면 중 6)을
잡는다(A-4의 회귀 자격).

---

## web2 라인

### D-W1 — 「2D는 내가 그린 것, 3D는 확정된 기하」 (2026-08-26 web2-11 2-a)

brush 렌더러(종이 질감)는 **2D 오버레이의 획에만** 얹고, 3D(`Line2`)는 안 건드린다.
겹의 몫: 대기 획의 몸체와 승격 획의 «재료 질감»은 `#brushc`(p5.brush)가, 승격 획의
선 본체는 종전대로 three.js `Line2`가, 작도선·표식·미리보기는 종전대로 `#ink`(2D)가 그린다.

> ⚠ **개정(2026-08-26 web2-12 2번 — #65: 뒤집힌 결정의 근거를 현재 사실로)**: 위 문장의
> «미리보기» 몫이 뒤집혔다 — 그 범위 설정 자체가 web2-12 지시의 대상이었다(「그리는 동안
> 검은 벡터선」 관측). 현행: **미리보기(draft)도 확정과 같은 두 겹으로 그린다** — 질감은
> `#brushc`(draft 전용 모드 — 확정 획은 `#brushsnap` 스냅샷이 든다), 몸체는 **Line2 그
> 자체**(`render3d.setDraftLine` — 화면 끝점의 역사영). INK·작도선·카메라 미확정은 종전
> `#ink` 벡터 미리보기 그대로다. 술어는 `state.ts`의 `draftBrushed` 하나(#54).

**왜**: 손 획(내가 그린 것)은 종이의 느낌이 목적이고, 확정 기하(자로 그은 것)는 정확성이
목적이다 — 한 화면에서 두 표상이 **다른 재료로 보이는 것**이 이 앱의 제품 정의(투시도 ↔
입체의 동등)와 맞는다. 기각한 대안: ① Line2까지 brush로(3D 조작마다 WebGL 획 전량
재그리기 — 수백 ms@500획 대역이 궤도 프레임마다 든다: 값은 `brush_perf_web2.json`의
full_redraw_ms가 정본 · 확정 기하가 «손 그림»처럼 보여 두 표상의
구분이 사라진다) ② 질감 전면 보류(사람의 결정 「무겁더라도 좋은 걸 먼저」와 반대).
되돌릴 조건: 실기기에서 「승격된 선이 종이와 안 어울린다」 관측 — 그때 Line2 재질을
연다(이 결정의 개정이지 brush 겹의 확장이 아니다).

### D-W2 — 제도 매체 — 잠정 (2026-08-27 web2-13 1부)

⚠ **이 절 이름은 web2-12·web2-13 지시가 참조해 온 자리다** — 그러나 **실재한 적이
없었다**(web2-12 지시가 「근거는 DECISIONS.md의 「제도 매체 — 잠정」 절」이라 적었을 때도
그 절은 없었고, 3부 근거는 NOTES의 각 항목 절에 흩어져 적혔다). web2-13 1부가 그 자리를
이제 채운다 — 두 지시가 전제한 내용을 한곳에 모은 것이지 새 결정이 아니다.

**이 앱의 화면은 제도판 위의 종이다.** 표현 계층의 결정들이 전부 이 은유에서 나온다:

- **재료가 둘이다**(D-W1): 손 획은 흑연/잉크(brush 질감), 확정 기하는 자로 그은 선(Line2).
- **종이는 한 장이다**(web2-12 10번): 결은 문서에 위상이 고정된 CSS 마스크 하나 —
  획마다 종이를 새로 뜨지 않는다.
- **제도 관행이 표현을 정한다**(web2-12 8번 모서리 넘김 — 만나는 끝만 · 화면 고정 7px).
- **대기 획 = 그 시점의 트레이싱지 위 잉크. 승격 = 정서(正書)** — 종이의 선이 공간의
  선이 된다(개정 3 초안 §5 — web2-13 1-e). 대기 획이 자기 시점에서만 보이는 것(3-a)은
  새 기전이 아니라 **이미 있는 이 구조를 정직하게 보이는 것**이다.
- 역할 분담(개정 3 초안 §6): **본 도구는 엄격하다**(정의 안 되는 획을 안 받는 방향) ·
  자유로운 낙서·탐색은 **트레이스 층**(미구현 — DEFERRED)이 맡는다.

잠정인 이유: 트레이스 층이 미구현이라 은유의 절반이 화면에 없다. 되돌릴 조건:
실기기에서 「시점마다 잉크가 갈리는 것이 종이가 아니라 결함으로 읽힌다」는 관측.

### D-W3 — 개정 3 초안: 정의는 사건이다 (2026-08-27 web2-13 1부 · **잠정 — 사람 승인 대기**)

정본은 [`docs/line_plan_r3_draft.md`](docs/line_plan_r3_draft.md)(초안 — 승인 전까지
현행은 개정 2다). 요약:

- **원칙 b에 예외 하나**: 획의 3D 위치는 파생이 아니라 **소유물**이 된다. 근거(점 연결·
  교점)는 얻는 수단이고, 얻고 나면 근거가 사라져도 위치는 남는다(**한 번 얻으면 영구**).
  카메라·차수·축·스케일은 여전히 파생이다. 발단: 실기기 관측 — 가이드라인을 지우면
  그 위에 정의된 창문이 대기로 떨어진다(초안 §1).
- **3D를 주는 근거는 둘**: 점 연결 · 교점. **면은 아니다**(사람이 기각 — 이유 셋, 초안 §2).
- **교점 정의는 끝점이 닿았을 때만 센다**(잠정 ㉯ — 초안 §3.1. 우연한 스침의 「의도하지
  않은 정의」를 막는다. 제도 관행: 수선은 만나는 데까지 긋는다).
- **대기 획은 자기 시점에서만 보인다**(잠정: 각도로 흐려지기 — 초안 §5. 3-a가 구현).
- **검증 축**: 저장된 3D를 지금 카메라로 사영하면 그 획의 `pts2d`와 맞아야 한다 —
  획 하나로 판정(초안 §7. 4-a가 자립 구현보다 먼저 팔로 세운다).
- **열린 물음(승격)은 2부 측정이 닫았다**(원장 `promote_freeze_web2.json`):
  굳힌 3D는 승격을 못 살아남는다(승격이 주점·f를 함께 옮긴다 — 주점 이동 20px 구도에서도
  f 재확정만으로 판정선의 두 자릿수 배) → **자립은 «카메라 닫힘 후»**(닫힘 =
  constructionDone — 초안 §9.2 · AS-C46) · 승격 사건이 나면 버리고 재굳힘.
- **구현됐다 — 깃발 뒤 · 기본 꺼짐**(4부 · `core/own3d.ts` · `Stroke.own3` ·
  설정 «실험» 체크박스 · 진단 「3D 경로」). 교점 정의(4-g — 끝점만·B의 시점에서만·
  방향 있는 대기선만)까지 같은 깃발 뒤에 섰다. 만드는 것은 세션, **켜는 것은 사람**
  (DEFERRED 「기본값 전환」 표가 판정 안내).

### D-W4 — 진입로 재설계: 지평선은 긋는 것이 아니라 이미 있다 (2026-08-27 web2-17)

- **무엇**: 지평선 = 프레임 상수(문서 y = H/2, `horizonDocY` 한 자리). 「지평선 획」·수평
  강제·«이 수평선은 지평선인가»의 판정이 통째로 없어진다. 팬이 눈높이 선언이고(종이가
  움직인다), 첫 획이 그 선언을 굳힌다(`drawView`). 선언 전에는 줌이 없다(3-a — 화각의
  두 번째 선언을 조용히 안 끼운다).
- **web2-16 4부는 폐기**(지시 ⛔⛔ — ㉯ «지평선을 긋고 그 위에 찍는다»가 설계째 사라졌다).
  4-a(지면 규칙 확대)·4-b(틱은 획이 아니다)만 이 회차 4부가 잇는다.
- **지시에 없던 한 줄**: 지평선 위(양 끝 |y−H/2| ≤ OSNAP_RADIUS_PX) «따라긋기» 획은
  퇴화 — 아무것도 선언하지 않는다. 근거는 기하(지평선 위의 선은 모든 수평 소실점의 살)와
  회귀(1-e ⑥ — 옛 손버릇이 1점으로 잠기지 않아야 한다). `entry17_web2.json` band_boundary.
- **.brnl version 2**: v1은 읽으며 변환(지평선 획 폐기 + 전체 평행이동 + savedViews oy 보정
  + 첫 획이 지평선이 아니면 거부 — 조용히 어긋난 문서를 안 연다). v2는 옛 파서가 거부한다
  (의도 — 조용히 다른 그림보다 빈 화면). **자동 저장 열쇠도 갈랐다**(b2-autosave2 — 옛
  PWA가 새 자동 저장을 «빈 문서 정리» 경로로 지우는 소실을 막는다. 옛 열쇠는 읽기 이행만).
- **drawView는 Doc이 아니라 App/BrnlData**: savedViews와 같은 급(선언하던 순간의 «화면»).
  선언의 결과는 이미 문서 좌표에 있다 — 잃어도 기하가 안 변하는 값은 Doc 밖(썸네일 판정).
  반대 의견(「그림의 선언이니 Doc」)은 NOTES web2-17 절에 기각 근거와 함께 있다.
- 프레임 맞춤과의 합성은 `composeView` 한 자리(s = s_fit·s_draw, o = s_fit·o_draw + o_fit).

### D-W4 부기 — 4·5부 (2026-08-27 web2-17)

- **지면 규칙 확대의 판별자 둘**(지시 문면에 없던 조임 — 기존 팔이 강제했다):
  ① 판정은 연결 패스 소진 뒤 모델 전체(중간 판정은 문서 순서가 답을 바꾼다)
  ② 대기가 전부 소실점 축일 때만(사슬 끊긴 높이 획 무리가 지면에 앉는 조용한
  오배치 차단 — A-3). 근거·실행 기록은 NOTES web2-17 4부 절.
- **지평선 자동 숨김**: horizonPref(null=자동). «화면 안» 판정은 ✕ 컬링과 한 함수
  (vpOnScreen — 여백 0/50만 다름). 제스처 동결은 fadePose의 뷰판(fadeView)을 짝으로.

## D-W5 — 2026-08-27 (web2-18): 겹의 소유 · 연장선 획득 · 제스처 타일

- **잉크 확정선의 몸체는 `#ink`가 그린다**(1부). 겹 순서가 `#gl(1) → #brushc(1, DOM 나중) →
  #ink(2)`인데 `#brushc`가 연필 흑연을 그리므로, 잉크 몸체가 `#gl`에 있으면 연필이 잉크를
  덮는다(사람 관측 「펜이 연필선에 먹혀 거의 안 보인다」 — 실측 비 0.149).
  **기각한 두 길**(다음 사람이 다시 제안할 것이므로 사유를 남긴다):
  - ⓐ `#brushc`를 `#gl` 아래로 내린다 → 연필의 Line2 몸체가 자기 흑연 **위**로 올라와
    연필이 균일해 보인다. **표현이 바뀐다.**
  - ⓑ brush 모드에서 연필을 `#gl`에서 뺀다 → 연필이 「몸체+질감」에서 「질감」만으로 옅어지고,
    draft는 여전히 몸체를 그리므로(`setDraftLine`) **뗄 때 튄다** — web2-11의 「긋는 동안
    검은 벡터선」이 뒤집혀 돌아온다.
  - ⓒ **잉크를 `#ink`로 옮긴다** → 잉크는 균일선이라 Canvas 2D가 정확히 같은 것을 그리고,
    연필은 손도 안 대며 `z-index`도 안 건드린다. 꼬리(넘김)·번짐은 **그때도 `#ink`였다**.
  ⚠ 이 갈래는 **렌더러 모드와 무관**하다(classic에서도 잉크는 위다).
- **연장선 오스냅은 획득식이다**(2부 · A-3 선례 — AutoCAD Extension · Rhino Ext 둘 다
  상시가 아니다). 끝점 위에 `EXT_ACQUIRE_MS` 머물러야 그 선의 연장이 살고, 획득한 끝에서
  `EXT_MAX_RATIO × 선분 길이`까지만이며, 최대 둘(LRU)이고 **획을 확정하면 비운다**.
  근거는 사람 관측(「허공에서도 어딘가에 스냅이 잡혀 끌려다닌다」)과 실측(허공 855칸에서
  `ext` 75 → 0)이다. `near`·`xint`·`OSNAP_ORDER`는 안 건드렸다.
  ⚠ **상시 `ext`는 조용히 확정 좌표를 옮기고 있었다** — `test/pivot.test.ts`의 자유
  구축선이 (870.164,407.459)로 확정되던 것이 (870.118,407.471)이 됐다(2D 0.048 px,
  3D 무게중심 반경 17.335→17.316). 「애매하면 놓지 않는다」의 반대편 사례다.
- **제스처 중 흑연은 «구운 타일의 아핀 변환»이다**(3부 ㉢). 사람이 정한 구속은
  「돌리는 중에도 스케치 질감을 포기할 수 없다」이므로 **질감을 끄는 답은 없다** —
  줄인 것은 **획당 비용**이다. `#brushc`를 아틀라스로 한 번 구워(한 번의 `render()`)
  획마다 2D 타일로 뜨고, 제스처 동안은 `#brushsnap`이 그 타일을 두 끝점으로 변환해 붙인다.
  놓으면 **정확히 다시 굽는다 — 정본은 언제나 놓은 뒤 화면이다.**
  ⚠ **캐시가 원칙 b 위반이 아닌 근거**(4부의 같은 물음): 같은 입력에서 같은 값이 나오고
  **무효화 열쇠가 명시적**이다 — `tileSig = 문서(docVersion)·렌더러·배율(view.s)·**캔버스
  크기(cw×ch)**·감쇠 깃발`. **크기를 반드시 넣는다**(AS-C7 가족 — 캔버스 크기가 굳어 생긴
  결함이 이 저장소에 네 번 있었다. 1차 리뷰어 [17]이 문서 두 곳의 목록이 갈린 것을 잡았다). 저장이 아니라
  기억이고, 열쇠가 갈리면 그 자리에서 다시 굽는다. 파생 상태를 문서에 안 넣는다는 원칙은
  «문서가 파생을 들지 않는다»이지 «계산을 두 번 하지 않으면 안 된다»가 아니다.

## D-W6 — 2026-08-28 (web2-19): 몸체에서 안내색을 뺐다 · 종이 탭 · 네 자리

- **획 몸체에서 안내 파랑을 뺐다**(1부). web2-10이 세운 색 규칙(「색을 갖는 것은 그리는
  중의 상태 예고 둘뿐」)의 첫째 항(작도 안내 파랑)을 web2-17이 낡게 만들었다 —
  'horizon'은 획이 아니게 됐고(지평선 = 프레임 상수), 남은 'vp'는 **진짜 모서리**다
  (방 실루엣의 후퇴선은 벽 모서리이면서 소실점을 만든다). 모서리의 몸체가 파랗게 그려지고
  떼는 순간 흑연이 되는 것은 「전환이 무변화」(CLAUDE.md)의 정반대였다.
  - 예고 채널은 **생길 자리의 파선 ✕**로 옮겼다(파선 = 아직/숨은 — web2-16 3-a 어법).
    좌표는 classifyNext가 낸 값 그대로(원칙 d — 커밋 후 vps[0]과 일치를 팔이 잰다).
  - **면 미리보기 색은 남는다** — 그것은 「탭하면 무엇이 되는가」의 예고이지 획의 몸체가
    아니다(지시 1-b ⚠). 색 규칙 정본(render2d 머리주석)의 예고 항은 이제 면 하나다.
  - 반증은 상주 팔이다: diag.forceConstructing(D-3 손잡이 — coalesce의 선례)이 없앤
    파랑을 되살려 «파랑 0» 격자가 실패 가능함을 매 실행 증명한다.
- **종이 탭(2부)**: 층위 셋(파일=대상 하나 / 종이=한 장·하나만 활성 / 겹=web2-20).
  비유의 눈금은 도면집 — 여러 장인데 건물은 하나(종이가 늘어도 3D는 하나).
  **작도 종이가 pose를 안 담는 근거**: 작도 시점의 정본은 DRAW_POSE·drawView 하나씩이고
  (#54 — 출처 둘 금지), 반증 팔이 실제로 담아 ④(작도 탭 복귀)가 어긋나는 것을 확인했다.
  **Doc.sheets인 근거**: 겹(다음 회차)이 종이를 가리키고 겹은 획을 소유한다 — 종이가
  문서 밖이면 참조가 문서 경계를 넘는다. **#notice는 띠 위에 겹쳐 뜬다**(2-c ⚠ 결정):
  알림은 오류가 있을 때만 뜨므로 상시 자리를 비워 두는 쪽이 손해다.
  **작도 종이 id = 상수 0**(카운터 밖 예약 — nextId 1부터라 충돌 없음. 둘째 카운터가
  아니라 «늘 있는 유일한 것»의 정체다).
- **네 자리 배치(3부)**: 종이(위 왼쪽 — 어느 장인가) · 눈(위 오른쪽 — 이 종이를 어떻게
  보는가는 종이 옆이 제자리) · 손(오른쪽 세로 — 매 획마다 쓰는 것, 펜이 닿는 쪽) ·
  서랍(맨 아래 접힘 — 가끔 연다). 가르는 채널은 공백이 아니다: 도구는 종이 위에 떠 있고,
  서랍은 닫혀 있고, 종이 띠는 종이의 가장자리이고, 되돌리기는 구분선이 가른다.
  - **되돌리기의 자리**: 은유가 없다(제도판에서는 지우개가 그 역할) — 손 띠 맨 위,
    도구와 구분선으로 가른다. 서랍은 너무 멀다(매 획마다 쓸 수 있는 조작이다).
  - **자 아이콘 = 스냅 묶음의 입구**: 오스냅은 「기능적 도구」 층위인데 화면에 자리가
    없었다(설정 자루 안). 제도판에서 자는 손이 닿는 데 있다 — 실물(삼각자)이 입구가 되면
    그 층위가 처음으로 화면에 존재한다. ⚠ 자를 치우는 토글은 안 만들었다(대응표 §4 —
    자리만 비웠다. 아이콘이 실물이면 동작도 실물이어야 한다는 규칙과의 어긋남은 DEFERRED).
  - **리본을 만들지 않는다**: 선례(Procreate·Krita·Concepts·Clip Studio)가 전부 반대
    방향이다 — 떠 있는 크롬을 줄이고 필요할 때 펜 끝에 불러낸다(지시 §5 ⛔ 그대로).
- **아이콘 두 갈래와 라이선스(4부)**: ㉠ 직접 그리는 도구(연필통·펜·지우개 둘·면)는
  실물 그림 유지 — **연필만** 3-b′로 바뀌었다(접힘 + 뾰족한 끝 — 정본은
  docs/instrument-icons.md, 홀더펜 기각 사유 셋도 거기). ㉡ 그 외 전부 선 문법 하나 —
  Phosphor light(MIT — LICENSE에 줄) + 같은 문법의 자작(삼각자·삼각 스케일·트레이싱지 롤
  — md 정본). mdi set-square 폐기(채운 면 스타일 — npm은 Phosphor 하나). ⛔ CC BY 금지 준수.
  **굵기 light의 근거는 잉크량 실측 + 사람의 사전 방향이고, 표본이 좁다**(AS-C66 ⏳와
  같은 등급 — 2차 리뷰 [6]): 잉크량(알파 합·64px) 최근접 = light(원장 icon_weight_web2).
  ⚠ 반대 척도가 있다 — 선 굵기 비(stroke_frac)로는 **regular가 7배 가깝다**(Δ 0.0025 대
  light 0.0181). 잉크량을 채택한 사유: 지시가 명명한 척도이고, regular는 잉크량에서 ref보다
  25% 무거워 사람의 사전 예상(「regular는 새 것만 굵어 보인다」)과 어긋난다. 두 척도가
  다른 글리프 비교라는 한계는 원장 def에 있고 최종 판정은 실기기다(DEFERRED 아이콘 판독).
  ⚠ 첫 판(32px 문턱 계수)은 세 굵기가 같은 수를 내 아무것도 안 쟀다(#71 ㉢) — 분해능
  단언을 팔에 박았다.
- **이름은 넷만 바꿨다(4-a)**: 대응표 §3이 정본 — 순화가 아니라 «사용자가 신경 쓸 이유가
  없는 기술 서술»만 뺐다. 오스냅·축 스냅 등 정확한 말의 **존재**를 팔이 단언한다.

## D-W7 — 2026-08-28 (web2-20): 겹(트레이싱지·옐로) — 종속 탭 · 곱 합성 · 갈림들

- **겹 = 종속 탭**(사람이 정했다 — 대응표 §6-b가 정본): 겹은 도구가 아니라 **종이**라
  네 자리 중 위 왼쪽(종이 띠 아래 줄)이다. 종이는 배타(하나만 활성)·겹은 가산(여럿
  동시) — 형태도 갈랐다: 부모 탭은 겹침 0, 종속 탭은 카드가 **겹쳐 쌓인다**(margin
  −14px)·활성만 앞으로 나온다(잰 값: 부모 겹침 0 / 자식 >0 — layerbar.spec).
- **상태는 켬/끔 둘**(1-b — dim 없음): 겹이 쌓이면 아래는 **곱이 저절로** 어둡게 한다.
  셋째 상태는 곱 합성 채택의 귀결로 불필요해졌다.
- **곱 합성(3-a)**: 종이는 빛을 빼앗는 감산 — 겹칠수록 어두워지고 짙어져야 한다. 알파는
  겹칠수록 막 색으로 수렴(실물과 반대). 잰 값(paper.spec ④ 곡선): V 단조 감소(곱의 성질) ·
  옐로 둘 S(HSV) 상승 · 옐로+트레이싱지 S 하강 — 알파로 바꾸면(반증 손잡이) 실패 실측.
  ⚠ S의 방향은 **이 색조 쌍에서의 실측**이다(트레이싱지 한색이 만든다 — AS-C68 ·
  1차 리뷰 [12]) — 곱 일반의 성질로 읽지 않는다.
  ⚠ **자리는 CSS가 아니라 캔버스 안이다** — CSS mix-blend-mode가 이 앱의 합성 트리
  (WebGL 형제)에서 발화하지 않았다(#73 ㉠ 실측). 막 영역을 재조립(아래 캔버스 사본)해
  in-canvas multiply로 곱는다.
- **3-b에서 고른 길 = 영역 재조립**(지시의 ⓐ·ⓑ 둘 다 아님): ⓐ(#brushc 분할)만으로는
  활성 겹 획의 **Line2 몸체가 #gl에** 있어 몸체가 막에 물든다(⑨ 위반 — 구조가 먼저
  말했다). ⓑ는 선 없는 자리가 안 물든다(옐로 값의 절반 상실). 고른 길: 막 영역에
  아래(#gl·#brushsnap·#brushc)를 사본으로 깔고 곱한 #film + 활성 이상 획을 그리는
  #layerc. **잰 값(cost20_web2{,_dpr2}.json — 분자/분모 그대로, #14. 초판의 #16은
  오인용이었다: 2차 리뷰 [11])**: 400획 궤도 겹+막/겹 없음 =
  1.9/1.8ms(dpr2 1.7/1.9) ·
  겹 셋·**위 획 100**(above_strokes 실측 — 활성 맨 아래) 칸도 2.0/1.8 — 같은 대역 ·
  막 보이는 국면 호버 막 켬/겹 없음 = 13.6/1.1ms(dpr2 39.7/1.0) ·
  알파/곱 = 13.5/13.6(「비용은 알파와 같다」 실측). 막 국면 값은
  drawImage 셋(#gl은 WebGL 판독)+패턴의 본질 비용, 소프트웨어 GL 컨테이너 값이라
  실기기 표(DEFERRED)에 행을 올렸다. ⚠ 첫 판은 궤도 32.8ms — 동결 포즈 게이트(#73 ㉡ —
  수리 전 판은 원장 파일로 안 남았다: NOTES ⑩의 원문 인용이 그 기록이다).
  - **층별 시드(3-c)**: rng32(layer.id) — id는 nextId에서 나와 새로 꺼낼 때마다 다르고
  문서에 저장돼 다시 열어도 같은 결(⑥ 실측). 같은 종류 두 장의 타일 픽셀이 다르다(⑤).
- **카메라와 리프팅의 갈림(4부)**: analyze는 **모든 획**을 본다(겹 무관 — 소실점은 그 ⚠ **web2-22 제자리 정정: 트레이싱지 한정** — 옐로 겹의 획은 analyze·liftAll 밖이다(매체가 2D — D-W9).
  종이가 아니라 대상의 것) · liftAll은 **꺼진 겹의 획을 뺀다**(보이지 않는 획이 3D·오스냅
  ·면에 나오면 조용히 틀린 배치다). 갈림의 자리는 lift.ts 한 곳(#54) — 오스냅·조각·면은
  lifted 파생이라 자동으로 따라온다. 잠긴 겹: 보이고 물리되 지우개가 못 지운다.
- **「카메라 닫힌 뒤에만」(2-a)**: 겹의 rect·막은 작도 시점의 화면 좌표에 산다 — 카메라가
  서기 전에는 그 좌표가 아직 무를 수 있다. + 버튼은 그 전에 굳어 있고 눌리면 이유를
  알린다(「소실점 작도가 끝나야…」).
- **종이 삭제 규약 변경(2-c — 예고대로)**: 겹이 종이에 붙으므로 종이 삭제 = 그 종이의
  겹·소속 획까지 지우는 일이 됐다 — **실행취소 대상으로 편입**(Op에 removed·layersRemoved
  ·sheetRemoved). 확인 한 번은 유지(팝업의 획 수 고지 포함).
- **5부(잉크 질감) — 버렸다**(1차 리뷰 [4][5]로 근거 정정 — 정본은 NOTES 5부 절):
  채널 회계 — **활성 겹**(그 종이 위에 그리는 그 획)에는 채널이 없고(#layerc 몸체만 —
  drawAbove 경로에 브러시 매개변수가 안 들어간다: 코드 경로 산술, 실측 아님) **아래 겹**
  획에는 있다(미실측). 아래에만 값을 넣으면 같은 획의 질감이 활성/비활성에서 나타났다
  사라져 결함으로 읽힌다 — 채택의 전제는 #layerc 질감이고 그 구현은 이 회차 밖.
  표와 재개 조건은 DEFERRED(「5부 기각」 행) — #layerc에 질감이 생기면 그때 잰다.

## D-W8 — 2026-08-28 (web2-21): 겹의 규율 · 스케일 · 면 일괄

### 스케일은 바탕 종이가 정한다 — 겹은 그 스케일 위에서 잰다 (1-b)

`scaleOf`(lift.ts)의 후보 집합을 **`layer`가 없는 획**(종이에 직접 그린 것)으로 좁혔다.
겹 획에 치수를 줘도 `scaleRef`가 되지 않고(`setDimension` → `'baseScale'` + 안내
「축척은 바탕 종이의 치수가 정한다」), 그 치수는 바탕이 정한 스케일로 **읽히는** 값이다.
근거: 실물이 그렇다(트레이싱지에 자를 대도 축척은 밑그림의 것) + 재현된 결함 —
겹 획이 기준이면 그 겹을 끄는 순간 **문서 전체가 무척도**가 됐다(물러가지도 못하고
null — `scale_layer_web2_before.json` A·B행. 지시의 「다음 치수 획으로 물러난다」보다
나빴다). 원칙 b(스케일은 파생)는 불변 — **후보 집합만** 좁혔다(#54: scaleOf 한 자리 +
scaleRef 부여 입구 하나). 옛 파일의 겹 scaleRef도 같은 자리에서 걸러져 바탕 첫 치수
획으로 물러난다(dim.test 옛 파일 방어 팔 — 판정값은 `scale_layer_web2.json` legacy_defense).
⚠ **알려진 귀결(1차 리뷰 [7])**: 바탕에 치수가 없고 **겹에만** 치수가 있는 옛 문서는
열면 표시 치수가 사라진다(수리 전 98.82 → 수리 후 null — 원장 A행). 물러날 바탕 치수가
없으니 무치수 문서가 되는 것이 규칙의 문면이고, 안내 한 줄(「축척은 바탕 종이의 치수가
정한다」)이 그 상태를 말한다 — 바탕 획에 치수를 하나 주면 복귀한다.

### 겹은 아래를 읽고, 아래를 안 바꾼다 (2부) — 그리고 소급 불가

- **읽기는 산다**: 겹의 획이 바탕의 3D에 오스냅으로 붙어 **자기가** 승격되는 것은
  그대로다(matchPoint 경로). 따라 그리려면 읽어야 한다.
- **쓰기를 막는다**: `defineByTouch`는 **같은 층 안에서만** 정의한다(바탕은 바탕
  안에서, 겹은 그 겹 안에서 — missed.layer로 계수). 트레이싱지의 요지는 「원래 모델에
  영향이 없다」(사람 문면)인데, 겹 획이 바탕 대기선을 굳히면 대안을 지워도 바탕 3D가
  바뀌어 있었다(재현 — layerrule.test ①).
- **지우개는 활성 층의 획만** 지운다 — 트레이싱지를 통해 밑그림을 지울 수 없다.
  아래를 고치려면 그 층으로 내려간다(실물). 잠금 가드와 별개의 규칙.
- **역방향(바탕이 겹을 읽는다)은 막지 않는다**(2차 리뷰 [1]): 연결(matchPoint)은
  무방향이고 사람이 붙인 점이다 — 실물의 «전사». 귀결: own3d 기본 켜짐에서는 붙는 즉시
  굳으므로 겹을 꺼도 그 바탕 3D가 남는다(layerrule ⑥이 값으로). 굳기 전이면 대기로
  내려간다(불변식 j — 사라지지 않는다).
- **잠금·끔은 활성을 풀고, 그때 종이가 활성이다**(2차 리뷰 [3]): 겹을 잠근 채 지우개를
  쓰면 바탕 획이 지워진다 — 「아래를 고치려면 그 층으로 내려간다」의 «내려간» 상태이므로
  정상이다(사람 문면의 「깔았을 땐」은 «활성일 때»로 읽는다). 실기기 표에 확인 행.
- ⚠⚠ **소급이 안 된다**: `own3`에 「누가 정의했는지」가 없어 이미 굳은 3D는 어느 겹
  때문인지 모른다. **규칙은 앞으로만 걸린다** — web2-21 이전에 겹 획이 굳힌 바탕
  3D는 옛 문서에 그대로 남아 있고, 그것이 잉크 심판을 통과하는 한 유효한 기하다.
  옛 문서를 열었을 때 「겹을 껐는데 바탕이 굳어 있다」가 보이면 이 조항이 설명이다.

### 롤은 도구다 · 인셋과 층별 흔들림 · rect가 자라는 것은 결함이 아니다 (3부)

- **롤 둘이 손 띠에 상주한다** — 층위가 안 깨진다: 롤은 **도구**다(책상 위에 놓인
  물건이고 «한 장 뜯는» 행위). 쌓인 것을 관리하는 종속 탭 줄과 다른 일이다. 같은 일을
  두 자리에서 할 수 있는 것은 흠이 아니다(연필을 랙에서도 접힌 아이콘에서도 고른다).
- **인셋(짧은 변 5%) + 층별 흔들림(rng32(layer.id) — 이동 ±6·크기 ±4px)**: 화면 전체를
  덮으면 종이가 아니라 필터로 보인다 — 「올라간 종이」의 정체는 **가장자리가 보이는 것**.
  흔들림은 몇 장 쌓였는지를 가장자리로 읽게 한다. 그림자 ⛔(그라데이션 기각 이력) ·
  회전 ⛔(rect에 자리가 없다).
- **rect가 자라는 것은 결함이 아니다**: 종이 밖에 그으면 확정 시점에 rect가 자라고,
  자라다 화면 가장자리에 닿아 「올라간 종이」 느낌이 옅어지는 것은 실제 책상에서 종이를
  이어 붙이는 것과 같다(지시 문면 — 오독 방지 조항).
- **후보(면 일괄)의 무효화 사유**(3·4부 리뷰 [8]): 문서 변경(recompute) · **궤도·팬
  시작**(후보 폴리곤은 «찾은 그 시점»의 화면 좌표라 포즈가 바뀌면 낡는다 — beginNavHold)
  · 도구 이탈(물음을 접는 몸짓). 잃는 것은 빼기 탭뿐이고 「전부 찾기」 한 번으로
  복구된다 — 낡은 폴리곤을 탭 대상으로 남기는 쪽이 조용히 틀린 배제를 만든다.

### 면 일괄 — 원칙을 안 깨는 근거(지정의 방향) · 안쪽 고리 기본값 (4부)

- **「자동으로 안 만든다」(types.ts)는 그대로다.** 일괄은 후보를 전부 내놓고 사용자가
  **아닌 것만 탭해서 빼는** 것 — 지정이 「포함」에서 「배제」로 바뀔 뿐, 사용자가 지정한
  것만 면이 되는 것은 불변이다(방 스물·개구부 셋이면 탭 스무 번 → 세 번 — 사람 문면).
  확정 전에는 면이 아니고(테두리만·저장 안 됨) 확정이 한 op다(실행취소 한 번).
- **안쪽 고리의 기본값은 구멍**(사람이 정했다) — 건축에서 벽 안 사각형은 거의 언제나
  개구부다. 채우기는 확정 뒤 기존 탭 하나(별도 동작 없음). 포함 판정은 같은 평면
  안에서만(다른 평면의 순환은 무관).
- **범위는 모든 평면**(사람 문면 「'보이는 면'이라는 개념 자체가 애매하다」 — 면끼리 안
  가리고 깊이도 안 쓰니 이 앱에 그 정의가 없다 AS-C19).

### 1-a 개구부 — 갈래 ③ · 소급 판단의 기록

재기만 했고 안 고쳤다(지시 ⚠⚠ · 근거는 NOTES 1부 절과 DEFERRED 그 행). **web2-23
게이트 미통과 — 사람 판단 필요.**

## D-W9 — 2026-08-28 (web2-22): 옐로는 작도가 멈춘 종이

### web2-20의 「목적을 제안하지 않는다」가 뒤집힌 근거

사람이 정정했다(지시 0 원문): 두 종이는 취향이 아니라 **매체의 성질**이 다르다 —
트레이싱지는 「종이와 같은 메커니즘으로 축스냅 선을 고정하고 겹쳐 더 그리는」 것,
옐로는 「make2d로 눌러놓은 선을 일러스트레이터로 가져와 자유롭게 스케치하는」 것.
연필과 펜이 다른 것과 같은 급이다. 형태(종속 탭·가산성·켬끔·잠금)는 안 바뀐다.

### 두 종이의 성질 표(1-a) — 구현 그대로

| | 트레이싱지 | 옐로 |
|---|---|---|
| 축 스냅 | 걸린다 | 안 걸린다(입력 우회) |
| 3D 승격 | 올라간다 | 안 올라간다(lift 제외 — 대기도 아님) |
| 오스냅 대상 | 된다 | 안 된다(lift 제외의 자동 귀결 #54) |
| 소실점 | 쓴다 | 안 쓴다(analyze 제외 — 카메라 오염 방지) |
| 다른 종이에서 | 보인다(3D) | 안 보인다(2D — 그 종이·그 시점만) |

### 대응표 §4가 닫힌 방식 · 오스냅에서 뺀 근거 · 안내의 형태

- **「자를 치울 수 없다」(§4)의 답이 옐로다**: 자를 치운 획은 2D 종이 위에 있다.
  별도 토글 ⛔(지시) — 옐로를 얹는 것이 곧 자를 치우는 몸짓이다.
- **오스냅 제외**: 2D 자유 획에 붙을 이유가 없다 — 그게 자유의 정의(지시 1-c). 덤:
  옐로 수백 획이 스냅 후보(대기 몸통 near)에서 통째로 빠진다(cost22 전/후 — 대기
  588→200).
- **1-d 안내는 «떠나는 순간 한 줄»**: 종속 탭 상시 표시 대신 — 「사라졌다」로 읽히는
  순간이 종이를 옮긴 직후이므로 그 순간에 말하는 쪽이 읽힌다(상시 표시는 목적 제안
  같은 상시 채널을 하나 늘린다).
- **옛 저장분의 귀결**(0·1부 리뷰 [10]): web2-20·21 시기에 옐로 겹에 그려 저장된 획은
  그때는 3D였고, 지금 열면 **2D로 내려간다**(오스냅·3D 대상에서 빠짐). 매체 정정의
  의도된 귀결이다 — 마이그레이션은 없다(그때의 옐로는 트레이싱지와 같은 물건이었으므로,
  3D가 필요하면 트레이싱지에 다시 긋는다). 조용히 잃는 배치가 아니라 문서화된 규칙
  변경이다 — 이 줄이 그 기록이다.
- **「다시 뜨기」 없음(1-e)**: 실제 제도에서 못 하는 일(사람 문면). 쌓인 순서가 곧
  작업 이력 — 아래를 고쳤으면 새 옐로를 얹는다. ⚠ 23의 밑그림은 .brnl에 **저장**돼야
  한다(열 때 재계산 = 자동 다시 뜨기 — 사람이 거부한 동작).

## D-W10 — 2026-08-28 (web2-22 2부): 후행 확정 — 선례·기각·직선화의 경계

### 후행 확정을 고른 근거와 선례 둘(지시 §5 요구 — 2차 리뷰 [2]로 뒤늦게 정본화)

- **채택: Procreate QuickShape** — 자유롭게 긋고 **끝에서 떼지 않고 잠깐 머무르면**
  반듯해진다. **토글이 아니라 제스처다.** 머무름 시간은 설정 0.10~1.50초·기본 중간.
- **기각: Clip Studio 자** — 자 객체를 놓으면 붙지만 **메뉴에서 스냅을 켜야 한다 —
  토글이 하나 더 생긴다.** 사람이 싫다고 한 감각(「스냅 토글을 띄워놓고 스케치하는 것
  자체가 뭔가뭔가」)이 정확히 이쪽이다. ⛔ 다음 세션이 토글 안을 다시 꺼내지 말 것 —
  이 기각은 취향이 아니라 사람의 문면이다.

### 직선화 = raw 소멸 — 근거와 규약 경계(2차 리뷰 [3])

머무름이 성립한 채 뗀 획은 **raw(손 점렬)를 [a,b]로 줄여** 확정한다(= raw 없음).
- **근거**: 반듯함은 그 제스처의 **의미론**이다(QuickShape도 곡선을 버린다). 곡선
  raw가 남으면 표현 계층(잉크 번짐 등 raw 유도)과 23의 밑그림이 «반듯하게 하라»고
  명시한 획에 손 곡선을 되살린다 — 사용자 의도의 반대다.
- **규약 경계**: ① 지시 §4의 「raw를 솎지 마라」는 **3부(저장 절약 목적의 일괄 솎기)**
  금지다 — 제스처 의미론의 단일 획 직선화와 다른 자리다(3부는 실제로 측정만 했다).
  ② CLAUDE.md §1의 «pts2d 보존»의 목적(표현 계층 복귀)은 반듯해진 획에서는 반듯함이
  곧 의도라 충돌하지 않는다 — 머무르지 **않은** 옐로 획의 raw는 전부 남는다.
- 되돌릴 조건(실기기): 「반듯하게 한 획의 잉크 표정이 밋밋하다」 관측이 오면 raw 보존
  + 표현만 직선화하는 갈래를 다시 판단한다.


## web2-24 (2026-08-28) — 관통선이 실제 작도의 경로다 · 23 게이트 재판정

### 창은 관통선으로 뚫는다 (지시 0절의 근거 — 판정 ㉠의 전제)

- **제도에서 창은 띄우지 않는다.** 벽의 좌우 수직에 **양 끝이 닿는** 인방선·창대선을
  긋고 그 사이에 수직 둘을 그은 뒤 바깥 토막을 지운다(선따기). 이때 창 네 변은 전부
  연결돼 있다 — 앵커 없는 «떠 있는 사각»은 실제 작도에 없는 픽스처다.
- web2-21 1-a의 0/4는 **측정도 거절도 옳다**(앵커 없는 화면 사각은 시선 광선 위
  일변수 가족 — 앱이 정할 수 없다). 틀린 것은 그 픽스처를 실사용 경로로 읽은 해석이다.
- 근거를 지워도 정의가 남는 것(`own3` — 정의는 사건)이 선따기를 가능하게 한다. 이
  앱이 own3를 만든 이유가 정확히 이 경로다(지시 0절).

### web2-23 게이트 판정 — **통과(갈래 ㉠ · 관통선 경로)**

`passthru24_web2.json`(1차 리뷰 16건 대응 후 재작업 판 — NOTES 대응 절): 관통선 14/14 ·
수직 14/14 · 선따기 후 네 변 3D 유지 14/14(실측 spanRatio가 기대값(expectedSpan — 수직
두 교점 사이 부모 3D 구간)과 전 칸 일치 · 구간 위치 endGap3d ≤2.8e-14(aggregate.endGap3dMax)) · **벽 면이 창을
구멍으로 갖는다**(holes=1) 14/14. 대조 8칸(floating 0/4 ×4 · touching 4/4 ×4 — #69 ㉣) ·
반증 ㉰(own3 끔+수직 지움 → 조각 0/4·구멍 0 — ③④의 실패 가능성 실증) · 반증 ㉱(통째
복사 오배치 → spanRatio ≈1·endGap3d 큰 값 — 지표 분해능 실증). ⚠ 이 통과는 **확대
지우기(view.s=2 · 문서 실효 반경 6px) 전제** 위다 — s=1이면 14칸 중 4칸이 부수 삭제
대역(gate.registered.conditions_note·실기기 표가 잇는다). **2부(평면
지정)는 열지 않았다**(지시 문면 — 「실제로 그리다 답답할 때 여는 게 값을 정확히 안다」).
떠 있는 고리는 결함이 아니라 알려진 한계로 DEFERRED에 남는다.

### 4부 — 옐로가 실제로 프리핸드가 된다 · 테두리를 없앤다 (사람 문면 2026-08-28)

- **옐로만 raw가 정본 기하다**: 옐로는 «자를 치운 2D 종이»(D-W9)라 손이 지나간 자리가
  곧 그림이다 — 축·3D·오스냅 매체인 트레이싱지·바탕은 §1(손떨림은 버린다 — 확정 기하
  {a,b})이 그대로 산다(지시 4-b ⚠ «프리핸드는 옐로만이다»). 새 필드 없음 — `Stroke.raw`
  재사용, 확정 시점 RDP 솎기(AS-C82 0.5px·rawIn 같은 인덱스 동행).
  ⚠ **D-W10(머무름 = raw 소멸)은 그대로 산다** — 갈음·«솎으니 2점»(직선 손 획)은 raw를
  안 싣는다(2점 raw는 {a,b}와 동치 — 정보 0. e2e yellow.spec 2부 팔의 raw=0 단언 유지).
- **web2-22 2부(후행 확정)의 재해석 기록**(지시 4-a ⚠): 22 시점의 옐로는 이미 현으로
  확정됐므로 머무름의 「직선화」 몫은 무변화였다 — 실제로 재고 있던 것은 **수평·수직
  붙임(끝 이동)과 raw 소멸(질감 채널)**이다. 4부에서 비로소 「점렬 → 직선 갈음」이
  기하의 변화가 된다(hold 규칙 자체는 22 그대로 — 코드 불변).
- **옐로에서 오스냅을 아예 안 부른다**(4-c): 22 1-c(옐로 획이 후보에서 빠짐)는 «남이
  옐로에 못 붙는다»였고, 사람 문면(「스냅도 잡지 말자」)은 그 반대쪽까지다 — 옐로 활성이면
  그리기(기존)에 더해 **호버의 osnap 호출·연장선 획득(ext)·표식까지 전부 쉰다**(input.ts
  호버 갈래 문). 「자를 치운 종이」의 정의가 그것이다.
- **겹 테두리를 없앴다**(4-d — web2-21 3-b를 사람이 뒤집었다): 실물 트레이싱지에 1px
  윤곽선은 없다 — **색조가 끝나는 자리**(막 tint 경계·인셋·층별 흔들림)가 가장자리다.
  활성 표시는 종속 탭이 이미 한다. rect 끌기의 손잡이만 남긴다 — **가장자리 가까이
  갔을 때만**(rectEdgesAt — 끌기와 같은 판정·AS-C84) 그 변이 옅게 뜬다(순간 피드백 —
  COL.snap 대역). 트레이싱지·옐로 둘 다(사람이 둘 다 지목).
- **옐로 지우개는 점렬을 따라 재고 통째로 지운다**: 히트는 raw 폴리라인 거리(현으로
  재면 곡선 안쪽이 안 지워진다 — yellowraw.test ⑥의 격자가 그 반증), 삭제 단위는 대기
  획과 같은 «통째»(2D 획에 조각 기전이 없다 — A-3 선례·부분 지우기는 DEFERRED).

---

## D-W11 — 2026-08-28 (web2-23): 굽기(make2d)와 은선 — 갈림 다섯

### ⛔ 화면 표시와 은선 계산은 **다른 계산**이다 (1-a)

**면은 지금대로 서로를 안 가린다**(`depthTest:false`의 옅은 채색 — AS-C19). 은선의
깊이 비교는 **굽기 안에서만** 산다. 이 갈림을 여기 적는 까닭은 다음 사람이 「면끼리
안 가린다」를 결함으로 읽고 고치려 들 것이기 때문이다 — 그것은 **화면의 규약**이고
(면이 서로를 가리면 무엇을 그리고 있는지 안 보인다: 이 도구는 렌더러가 아니라 제도판이다),
은선은 **밑그림의 계산**이다. 둘을 한 손잡이로 묶으면 어느 쪽도 못 고친다.
⛔ 법선은 안 쓴다 — 가림은 「이 조각이 이 면보다 뒤인가」이고 면의 앞뒤가 아니다.
법선이 필요해지는 것은 **그림자**이고 그것은 이 회차 밖이다(DEFERRED).

### 같은 평면 조항 (1-c) — 임계는 `PLANAR_RATIO` 하나

선분이 면과 같은 평면에 있으면(두 끝이 다 그 두께 안) **그 면과는 깊이를 안 잰다**.
안 그러면 벽의 모서리가 자기 벽에 가려 사라진다. 임계를 면의 평면성 허용과 **같은
값**으로 둔 근거: 그 두께 안에서 만난 선들로 만든 면이므로 더 엄해질 수 없다(#54 —
새 숫자를 안 짓는다). ⚠ 이 조항이 **실제로 일하는 자리**는 정확한 평면이 아니라
**비스듬히 보는 «거의 평면»**이다: 면의 깊이가 `t = (d − n·o)/(n·rd)`라서 `n·rd`가
작아질수록 평면 계수의 작은 어긋남이 깊이에서 `1/(n·rd)`배로 커진다. 정확히 같은
평면이면 어느 쪽으로도 안 기운다 — 반증 팔이 그 사실 때문에 **장면을 따로 세운다**
(`make2d.test.ts` ㉮ · #69 ㉣의 처방).

### 경도로만 가른다 — **파선 ⛔** (2-a)

보이는 선 `F` · 가린 선 `H`(둘 다 `MAT` 그대로 — 새 색·새 굵기 ⛔ #54).
**은선을 파선으로 만들지 않는다**: 이 앱에서 파선은 이미 「대기」의 채널이다
(web2-16 3-a — 3D가 아직 안 정해진 획). 채널이 겹치면 **둘 다 안 읽힌다** —
파선을 본 사람이 「대기인가 은선인가」를 물어야 하는 순간 두 표시가 함께 죽는다.
「가린 선 빼기」는 **표시 손잡이**다(표시 팝오버) — 끄고 켜도 다시 안 굽는다.

### 밑그림은 **사건의 기록**이다 · 「다시 뜨기」 ⛔ (2-b·2-c)

`Doc.underlays`는 파생이 아니다(`own3`와 같은 급) — 굽는 계기는 **옐로를 얹는 그
순간** 하나이고 그래서 **저장한다**. 안 저장하고 열 때 다시 계산하면 그것이 곧
「자동으로 다시 뜨기」이고, 사람이 폐기한 동작이다(web2-22 1-e). 아래를 고쳤으면
**새 옐로를 한 장 더 얹는다** — 그러면 쌓인 순서가 곧 작업 이력이다.
파일 규약: `.brnl` v6. 모양이 틀린 밑그림은 **그것만 버린다**(겹은 산다 — 썸네일의
선례). 밑그림을 잃었다고 획까지 못 열게 하는 것이 더 큰 손실이다.
⚠ 3부 안내의 「한 번만」은 **세션 단위**다(`App.underlayNoticed` — 런타임 상태라 저장하지
않는다). 파일을 닫았다 열면 다시 뜬다: 새 세션은 새 사람일 수 있다. 문서 단위로 옮기려면
`.brnl`에 한 필드가 늘고, 그것은 「성가시다」가 관측된 뒤의 일이다(DEFERRED).

### 밑그림은 **자기가 대체하는 선 자리만 도려낸다** (2부 표시 — 전량 e2e가 초판을 뒤집었다)

「치환」이 화면에서 서려면 **원래 획을 지워야 한다**. 안 지우면 와이어프레임이 그대로
비쳐 2-a의 옵션이 아무 일도 안 한다(가린 선을 H로 바꾸거나 빼도 원래 선이 그 자리에 있다).
초판은 그래서 겹의 **rect 전체**를 종이색으로 덮었다. 그것이 **web2-20 3부의 게이트 ⑧**
(「세 장을 겹쳐도 아래 획이 읽힌다」)을 깼다 — 그 종이 안의 **대기 획과 아래 겹의 획까지**
함께 사라졌고, 전량 e2e가 대비 0.0002로 잡았다(#71 ㉤의 형태: 겹에 한 단계를 끼우면 그
겹을 읽던 팔이 «사라졌다»로 읽는다).

**답은 범위를 좁히는 것**이다: 조각마다 그 선을 종이색으로 한 번 지우고(그 자리의 3D 획이
곧 밑그림이 대체하는 대상이다) 그 위에 F·H를 긋는다. 「가린 선 빼기」에서도 **지우기는
한다** — 안 그러면 옵션이 무효다. 지우는 굵기는 «그 자리에 있을 수 있는 가장 굵은 선»
(`C.NIB_MAX`)이고 새 숫자를 안 지었다(#54) — 단위는 **화면 CSS px**(줌·dpr 무관).
그래서 **선 자리 밖은 종전대로 비친다** — 대기 획도, 아래 겹의 획도 남는다.
⚠ 알려진 대가: 아래 겹의 획이 3D 선과 **겹치는 자리**에서 `NIB_MAX` 폭만큼 지워진다
(DEFERRED web2-23). ⚠ **대기 획은 밑그림에 안 든다**(3D가 없다 — 지시 1-a는 「3D 선분
전부」다) — 그러나 이제 그 자리가 안 덮이므로 종전대로 보인다.

### 면 위에 직접 그리지 않는다 (지시 4 · 근거 셋)

① 「투시도는 면에 의해 스냅되거나 정의되는 경우 자체가 없다」 ② 면 위에 그린 획은
그 면에 **종속**된다 — 사람이 창문 논의에서 거부한 구조 ③ 그건 텍스처링이지 제도가
아니다. 면은 깨끗한 지오메트리로만 남고 소묘는 옐로에서 한다(2026-08-27 합의).

## D-W12 — 2026-08-28 (web2-25): 실내에서 은선이 선다 · 종이와 겹의 손

### [1] **근평면 잘라내기는 새 개념이 아니라 이미 있는 규약의 확장이다**

`projectSeg`는 처음부터 선분을 근평면에서 잘라내고 있었다 — 「한쪽이 뒤로 넘어가도 그
앞부분은 보여야 한다」(격자처럼 발밑에서 지평선까지 뻗는 선). 면 다각형만 그 규약 밖에
있었고 그래서 「꼭짓점 하나가 뒤면 면을 통째로 버린다」였다.

**그러므로 이것은 «새 기능»이 아니라 «빠져 있던 자리»다.** 판단의 내용은 셋이다:

1. **값의 출처를 하나로 올렸다**(#54) — `projectSeg` 안의 지역 상수 `-1e-3`이
   `camera.ts`의 `export const NEAR_Z`가 됐다. 값은 안 바꿨다. 출처가 둘이면 잘라낸 자리가
   어긋나 **선은 살고 면은 죽는**(또는 그 반대) 국면이 난다.
2. **자르는 것은 «화면 그림자»이지 평면이 아니다** — 면의 방정식 `n·x = d`는 원래 3D
   꼭짓점에서 그대로 계산한다. 그래서 깊이 비교도 같은 평면 조항도 **한 줄도 안 바뀌었고**,
   web2-23이 낸 두 조항(정확한 동일평면은 부등식이 안 기운다 · 경계 위 점은 포함 판정이
   밖)이 **그대로 산다**.
3. **여전히 보수적이다** — 전부 카메라 뒤인 면은 빠진다. 그것은 「조용히 틀린 가림을 안
   만든다」의 유지이지 예외가 아니다(그 면은 화면에 없다).

⚠ **왜 이것이 «기본 국면»인가**: 사람의 문면이 「가장 많은 출발은 방의 실루엣」이고
실내 투시에서는 좌우 벽과 바닥이 거의 언제나 꼭짓점 하나를 카메라 뒤에 둔다. 「어떤 벽은
가려지고 어떤 벽은 안 가려진다」는 **안 되는 것보다 헷갈린다** — 규칙이 없어 보이기 때문이다.
그래서 web2-23의 DEFERRED 한 행이 아니라 이 회차의 1부가 됐다.

⚠ **오목 다각형의 «다리»는 대가가 아니다**: 한 평면 클리핑이 여러 조각을 클립 경계로
이으면 같은 자리를 반대 방향으로 두 번 지나는 변이 생긴다. `inPoly`가 **짝홀**이라 그 변을
두 번 세고 홀짝이 안 바뀐다 — **포함 판정이 그대로다.** 산문으로 두지 않고 팔이 값으로
낸다(`nearclip.test.ts` ⑥: 오목 하나와 볼록 둘로 쪼갠 기준의 깃발 배열이 같다).
### [2] **롤을 누르는 것도 「이 각도에서 시작한다」는 선언이다** — 그래서 시점을 굳힌다

종이 띠의 「+」는 **빈 장을 먼저 만들지 않는다** — 지금 보고 있는 각도를 한 장으로 저장한다
(web2-19 2-c · 사람이 답한 셋의 3). 롤(겹)을 누르는 몸짓은 그것과 **다른 규칙이 아니다**:
「이 시점 위에 트레이싱지를 얹겠다」는 곧 「이 시점에서 시작하겠다」이므로, 각도를 먼저
굳히고 그 위에 얹는다. 새 규칙을 세운 것이 아니라 **이미 있는 규칙을 그 자리에도 적용한** 것이다.

- **판정은 `atSheetPose` 하나**(#54) — 그것이 곧 「막이 보이는가」의 게이트다. 두 자리가
  같은 술어를 읽으므로 **「얹었는데 안 보인다」가 구조적으로 불가능**해진다. 새 비교를
  지었다면 그 둘이 갈리는 국면이 언젠가 다시 이 결함을 만든다.
- ⚠ **지시가 준 판정 방법은 안 섰다**(D-4): 「객체 동일성을 쓴다」였는데 `addSheet`가
  포즈를 **복사해** 넣으므로 굳힌 직후에도 거짓이고, 그러면 누를 때마다 종이가 는다.
  사람이 준 근거는 확인 대상이지 결론이 아니다 — 측정(팔 ②)이 갈랐다.
- **겹을 얹는 두 자리(손 띠 롤 · 종속 탭 「+」)가 같은 앞처리를 부른다.** 한쪽만 굳히면
  「어떤 길로는 보이고 어떤 길로는 안 보인다」가 나고 그것은 지금 고치는 결함과 같은 종류다.
  ⛔ 앞처리를 `addLayer`(state) 안으로 넣지 않았다 — 원장 하네스(cost20)가 그 함수를
  직접 부르므로 조건이 바뀐다(#71 ㉠). **몸짓의 규칙은 몸짓이 있는 계층에 둔다.**
### [3] **셔터 은유** — 「+」는 «빈 것을 만든다»는 뜻이다

크롬의 「+」가 약속하는 것은 «빈 탭을 하나 더»이고, 이 도구의 그 단추가 하는 일은 «지금
보이는 것을 한 장으로 남긴다»이다. **뜻이 달랐고 그래서 안 직관적이었다.** 카메라(셔터)는
정확히 그 일의 은유다 — 지금 보이는 것을 찍는다.

- 누르는 순간 **화면이 한 번 짧게 번쩍**한다. 무엇이 저장됐는지가 **그 자리에서** 보인다 —
  스트립에 들어온 썸네일이 그 답이다.
- ⚠ 번쩍임은 **짧고 무채색**이다(순간 피드백 대역). 이 도구는 색을 세 번 뺐고
  (소실점 ✕ → 오스냅 → 축 넷) 색을 갖는 것은 「탭하면 무엇이 되는가」의 예고뿐이다 —
  **되돌리지 않는다**(대응표 규칙 둘).
- **시점 갱신도 같은 번쩍임**을 쓴다. 그것도 «다시 찍는» 동작이므로 다른 피드백을 짓지 않는다.

### [4] **시점 갱신은 겹이 있으면 막는다** — 「다시 뜨기 없음」과 같은 결

밑그림(옐로)은 «얹은 그 시점»의 사영이다(web2-23 2-c). 종이의 시점을 갈아 끼우면 그
밑그림이 어긋나고, 어긋난 밑그림을 고치는 길은 **다시 굽는 것**뿐인데 그것이 바로 사람이
금지한 「다시 뜨기」다. 그러므로 **갱신 자체를 막고 새 종이를 만들라고 안내한다** —
같은 규칙의 다른 얼굴이다(D-W11: 「아래를 고쳤으면 새 옐로를 한 장 더 얹는다」).

- 작도 종이는 다른 이유로 막힌다 — 그 종이의 시점은 **정의**다(pose를 안 담고 정본이
  DRAW_POSE·drawView다). 지금이 작도 시점일 때만 갱신할 것이 있다(팬·줌·썸네일).
- **실행취소 대상이 아니다.** 종이의 시점을 다루는 몸짓이 이미 전부 스택 밖이고
  (저장·삭제·이름) 스택에 드는 것은 «그린 것»이다. 시점은 «보기»다. 되돌릴 필요가 있는
  상태(어긋난 밑그림)는 위 조항이 애초에 안 만든다.
- 이것이 `DEFERRED`의 「종이 썸네일이 낡는다」를 **부분적으로** 닫는다: 갱신할 때 다시
  굽고, **저절로 낡는 것은 그대로 둔다**(자동 재굽기는 변경마다 비용이 든다).
### [5] **「접으면 요약, 펼치면 목록」이 앱 안에서 하나의 규칙이 됐다** — 연필통과 겹

연필통이 이미 그 어법이었다(접히면 연필 하나, 펼치면 경도 줄). web2-25 4부가 겹을 같은
어법으로 옮기면서 **앱 안의 규칙이 하나 줄었다**: 「여러 개가 있는 것은 평소에 요약 하나로
있다가 누르면 세로로 펼쳐진다.」

- 사람이 배울 것이 하나 줄고, 다음에 «여러 개»가 생기면 어떻게 놓을지도 이미 정해져 있다.
- ⚠ 종이(탭)는 **그 규칙 밖**이고 그것이 맞다 — 종이는 «지금 어느 장인가»라 **상태 표시**다
  (web2-19 2-c: 팝업은 열어야 안다). 배타적인 것은 늘 보이고, 가산적인 것이 접힌다.
  그래서 두 형태의 차이가 **행동의 차이**를 그대로 말한다.
- 옛 형태(겹쳐 놓은 탭 더미)가 말하던 «가산적»은 **요약의 수**가 이어받는다.

### [6] **솔로는 3D에도 걸린다 — 그렇게 «만든» 것이 아니라 구조가 그렇게 되게 지었다**

지시가 「정하고 적으라」 한 자리다. 답은 **같이 걸린다**이고, 구현이 그 답을 **강제**한다:

> 솔로 = «나머지를 끈 것»이므로 `setLayerOn`을 그대로 부르고 되돌릴 켬/끔을 기억한다.
> 「꺼진 겹은 3D에서도 빠진다」(web2-20 4-b)에 **자동으로** 같이 걸린다.

표시 전용 필드를 두는 길도 있었지만 **안 골랐다**(#54): 그러면 「보이는가」의 게이트가 둘이
되고, 둘이 갈리는 국면이 언젠가 「솔로인데 3D에는 남아 있다」를 만든다. 2부에서 정확히 같은
형태의 결함(표시 게이트와 얹는 자리가 갈렸다)을 고쳤으므로 여기서 다시 만들지 않는다.

- 대가: **솔로인 채로 저장하면 «나머지를 끈 문서»가 저장된다**(`on`은 문서 상태다).
  되돌릴 조건은 `assumptions.md` AS-C91 — 실기기에서 「솔로로 보다가 저장했더니 겹이 꺼져
  있었다」가 나오면 저장 직전에 되돌리거나 솔로를 표시 전용으로 옮긴다.
- 손으로 눈을 건드리면 **기억만 버린다**(되돌리지 않는다) — 지금 화면이 사람이 만든 상태다.
- 종이를 바꾸면 **되돌린다** — 솔로는 그 종이 안의 상태다.
### [7] **파일 크기는 «재고 나서» 고쳤다 — 그리고 두 번 좁혔다**

지시가 순서를 못 박았다(5-a → 5-b): 「어디가 큰지 모르고 줄이면 헛수고다.」 표가 말한 것은
셋이다 — 점렬 **73.6%** · `rawIn` **23.7%** · **밑그림 0.1%**. 그래서 밑그림은 **안 건드렸다**
(web2-23이 저장하기 시작한 자리라 의심이 갔지만 표가 아니라고 했다).

그리고 고치는 중에 **두 번 좁혔고 두 번 다 팔이 잡았다**:

- **확정 끝점 `a`·`b`를 깎았다가 `own3d.test` 넷이 빨개졌다.** 자립 3D의 **잉크 심판**
  (own3d.ts §7 · `OWN3_TOL_PX` 0.01px)이 「그 3D를 다시 사영하면 획의 끝점에 떨어진다」를
  지키는데, 0.1 단위 이동은 최대 0.05px라 왕복에서 그 불변식이 깨진다. → **`raw`만 깎는다.**
- **밑그림 마디를 깎았다가 `underlay.test` ④(왕복 동일성)가 빨개졌다.** → 되돌렸다.
  ⚠ 되돌린 **사유는 그 팔 하나**다. 「밑그림은 0.1%라 얻을 것도 없다」는 **이 픽스처(마디 8개)의
  관측**이고 실사용 대역이 아니다 — 최악 밑그림은 마디 609·약 90KB이고 그런 문서에서는 20%
  대역이다(`cost23_web2.json` projected_worst · DEFERRED에 등재).

두 번 다 **#74 ㉢의 판별 물음**이 갈랐다: 「그 팔이 지키던 사람의 요구가 지금도 유효한가.」
유효했다. 그래서 팔이 아니라 **수리의 범위**를 고쳤다 — 그리고 표가 이미 그렇게 하라고
말하고 있었다(큰 몫은 `raw` 하나다).

⚠ **D-4가 또 걸렸다**: 지시 5-b의 「트레이싱지·바탕에서는 기울기가 표현에 쓰인다」는 **지금은
참이 아니다** — `tiltX`/`tiltY`/`twist`를 읽는 렌더 코드가 없다(`brushmap.ts`의 `rawIn.press`
하나뿐). 그래도 **그쪽은 안 건드렸다**: 지시의 ⛔이고, 「솎지 않는 원본을 남겨 둔다」가
web2-24 4-b의 결정이었다. 측정이 가리키는 방향은 `assumptions.md` AS-C93에 반증으로 적었다.


---

## web2-30 · 32 · 31 (2026-08-29)

### [1] **손가락의 뜻 — 두 손가락은 안 바뀌고, 한 손가락만 뜻을 하나 «더 얻는다»** (30-1)

```
두 손가락 = 이동 + 확대        (2D·3D 두 상태에서 **동일**)
한 손가락 = 3D 있으면 궤도, 없으면 이동
펜 획이 진행 중이면 손가락 제스처를 전부 무시   (팜 리젝션 — 종전 그대로)
```

**web2-26 5번의 `penUsed` 항을 걷었다.** 그 회차는 「손가락 = 이동」이라고만 적고 궤도를
언급하지 않았고, 그 결과 **펜을 한 번이라도 쓰면 3D가 선 뒤에도 궤도를 못 돌았다**(H4).

- **근거는 선례다**(A-3): three.js `OrbitControls`의 터치 기본값이 한 손가락 회전 ·
  두 손가락 이동/벌리기 확대이고, 모바일 3D 뷰어 관행이 같다. **3D가 선 뒤에는 궤도가
  이동보다 잦으므로 싼 제스처를 궤도가 가져간다.**
- **두 손가락을 두 상태에서 같게 두는 이유**(잠정 결정): 2D→3D 전환에서 뜻이 바뀌는
  제스처를 하나로 줄인다. 계획서가 「전환이 무변화이므로 사용자가 그 순간을 인지하지
  못한다」를 전제하는데, 제스처가 통째로 뒤바뀌면 그 전제가 깨진다. 한 손가락만
  **뜻을 하나 더 얻는** 형태(맞바꿈이 아니라 덧붙임)로 둔다.
- **대가**: 3D가 선 뒤에는 한 손가락으로 화면을 못 민다(두 손가락이 그 자리다).
  되돌릴 조건 — 실기기에서 「3D 상태에서 화면을 못 민다」가 보고되면 두 손가락 이동의
  발견 가능성부터 본다(제스처를 도로 바꾸지 않는다).
- ⚠ **지시가 나눈 두 갈래는 결과가 같은 한 식으로 합쳐진다.** 「펜 안 쓴 세션 = 현행
  유지」의 현행이 이미 `lifted.size === 0`이었기 때문이다. 그래서 `fingerPans`는
  **`app.lift.lifted.size === 0` 하나**이고 `penUsed`는 판정에서 빠진다(관측으로는 남는다).
- ⚠⚠ **D-4** — 지시 게이트의 「펜 미사용 세션: 한 손가락이 **여전히 그린다**」는 이 코드의
  사실이 아니다. 손가락은 이 앱에서 **한 번도 그린 적이 없다**(web2-26 5번이 이미 못 박았다).
  팔은 「거동이 안 바뀐다 = 획이 안 는다」를 잰다.

### [2] **펜도 펼치는 통이 된다 — web2-19의 「펜은 하나뿐이니 아이콘 하나」를 뒤집는다** (30-2)

**사유**: 슬라이더가 **틀린 물건**이었다. 제도 펜의 굵기는 연속값이 아니므로, 실물에 없는
조작을 만든 이상 기준이 있을 수 없다(사람 관측: 「어떤 기준도 없이 허공에 있어서, 이걸로
굵기를 조절한다는 직관이 하나도 서지 않는다」). `DRAFTING-MAP` 규칙 하나 —
**아이콘이 실물이면 동작도 실물이어야 한다.**

- 값은 **ISO 128 / 이소그라프 계열** 다섯(0.18 · 0.25 · 0.35 · 0.5 · 0.7 mm — 한 칸 √2배).
  계열의 나머지(0.13 · 1.0 · 1.4 · 2.0)는 **안 넣었다**(범위를 안 넓힌다).
- mm → px는 **0.35 mm = 지금의 기본 1.5 px**로 못 박았다 — 이 회차가 **선의 굵기를 안 바꾼다**.
- 두 도구의 문법이 같아졌다: **연필은 등급으로, 펜은 촉으로** 고른다. 둘 다 실물의 선택이다.
- 대가: 0.18~0.7 밖의 굵기를 못 쓴다(슬라이더는 0.4~4를 열어 뒀다). 되돌릴 조건 —
  「더 굵은 선이 필요하다」가 관측되면 **계열의 다음 눈금**(1.0 · 1.4 · 2.0)을 더한다.
  자유값으로 돌아가지 않는다(그것이 이 결정이 없앤 것이다).

### [3] **되돌릴 수 없는 것을 묻는 자리에서는 판단 근거가 화면에 남는다 — 그리고 근거는 대상을 밝혀야 근거다** (30-4)

28-3의 규칙(「화면의 말은 이름이거나 짧은 동사구다. 설명은 툴팁으로」)에 **예외 조항**을 세웠다.
그 순간 손은 단추 위에 있지 호버 상태가 아니라 **툴팁이 닿지 않는다.**

⚠ 그리고 「같이 간다」는 **무엇이** 같이 가는지를 말하지 않는다 — 근거라서 남긴다는 논리가
성립하려면 대상을 말해야 한다. 종이 삭제는 「이 종이에 얹은 옐로 N장과 그 위의 획 M개도
함께 지워진다」로, 겹 삭제는 「이 종이를 지운다」(틀린 말이었다)를 「이 옐로를 지운다」로 고쳤다.
**얹은 것이 없으면 그 말을 안 붙인다** — 없는 경고는 근거가 아니라 잡음이다.

### [4] **자유 스케치는 옐로에서, 종이는 작도판이다** (32-8 · 사용자가 정리한 규칙)

> **자유 스케치는 옐로에서 한다.** 옐로에서 숫자를 쓰더라도 치수를 정할 일은 없다.
> **종이는 작도판이다.** 여기서 쓰는 숫자는 **치수 의도로 봐도 된다**.

이 갈림이 web2-32의 두 결정을 정당화한다 — **32-1**(종이의 획을 글씨/작도선으로 가른다)과
**32-2**(읽히면 **즉시** 치수선으로 바꾼다: 종이에서 숫자처럼 생긴 자유 스케치를 할 일이
적으므로 「먼저 바꾸면 방해가 된다」는 29-2의 근거가 약해진다).

⚠ 이 결정은 **이미 코드에 반쯤 서 있다**: web2-29 2단계의 늦은 인식이 **옐로에서는 제안하지
않는다**(`handwritingGroup`의 첫 문 — 「거기 숫자는 치수가 아니라 메모다」). 32가 하는 일은
그 갈림을 **종이 쪽에서** 마저 세우는 것이다.
⛳ ~~이 세션은 이 결정만 등록했다~~ — **web2-32가 구현했다**(2026-08-29 다음 회차):
32-1이 종이의 획을 뭉치로 갈라 `Stroke.text`를 세웠고(옐로 획과 **같은 규격**), 32-2가
제안 층을 걷고 즉시 변환으로 바꿨다. **옐로에서는 둘 다 아예 안 돈다** — 이 결정의 갈림이
팔 둘로 못 박혔다(`scribble32.test` ⑤ · `writedim32.test` ④).

#### ⛔⛔ **32-2의 둘째 근거가 틀렸다** (2026-08-31 · web2-39 배경 — 사용자가 실사용에서 찾았다)

> 「세로선을 긋다가 이를 숫자 1로 인식하여 1·11·111mm 등의 수치를 의도치 않게 부여하는 문제.」

**어떤 논증이 왜 틀렸나.** 32-2가 승인 층(29-2의 제안)을 걷어내고 즉시 변환으로 간 근거는
둘이었다: ㉠ **사후 수정이 승인을 대신한다**(고칠 수 있으면 미리 물을 이유가 없다) ㉡ 위
[4]의 「**종이에서 자유 스케치를 하지 않으므로** 숫자로 읽어도 안전하다」. **㉡이 틀렸다.**

- ㉡은 위험의 출처를 **자유 스케치**로 잡았다. 실제 출처는 그것이 아니라 **작도선 자체**다.
- **작도선이 숫자와 구별되지 않는다**: 「1」은 세로선이고, 짧은 세로선 셋은 「111」이다.
  그리고 제도에서 **짧은 평행 세로선은 널려 있다**(기둥·문설주·해칭).
- 그러므로 **인식을 아무리 고쳐도 못 막는다.** 궤적을 봐도 1과 세로 작도선은 **같은 궤적**이다
  — 32-4(가로세로비 정규화)와 web2-35(궤적 시야)가 인식에 쏟은 것과 **무관하게** 이 실패는 남는다.
  ⚠ 이것은 「인식률이 낮다」가 아니라 **「입력이 같다」**이다. 같은 입력에 다른 답을 요구하는
  것이므로 분류기의 문제가 아니다(#61의 비용 비대칭도 여기서는 아무것도 안 막는다 —
  틀린 답이 아니라 **옳은 답**이 재앙이다).
- ㉠은 그대로 선다 — 고치는 길은 있다. **무너진 것은 ㉡ 하나이고, 그것이 「즉시」의 근거였다.**

**그래서 web2-39가 고친 자리는 «읽고 나서 무엇을 하는가»가 아니라 «언제 읽는가»다**:
글씨는 **선을 꾹 눌러 명시적으로 진입**한 상태에서만 읽힌다. 상태 밖의 모든 획은 작도선이다
(추측이 없어지므로 32-1의 분류기가 진입 판정에서 물러나고 재판정 기제가 사라진다).
**32-2가 만든 즉시 변환 자체는 유지된다.** 자세한 것은 `web2/NOTES.md`의 web2-39 절 · [[AS-C143]].

### [5] **잰 값과 적은 값은 자료구조에서 갈린다 — 파생은 저장하지 않는다** (32-6 · 지시 문면)

```
적은 값(Stroke.dim)  사람이 정했다. 축척의 근거이고 어긋남 판정의 한쪽이다. **저장한다**
잰 값(재기)          모델에서 나왔다. 축척이 바뀌면 따라 바뀐다. **저장하지 않는다**
```

저장하는 것은 **「어느 두 점을 재는가」**뿐이고, 그 점의 «정체»는 **면의 선례** 그대로다
(`types.ts`: 「저장하는 것은 경계의 정체이고 좌표가 아니다 — 그래서 차수 승격으로 전부
다시 올라가도 면이 따라온다」). 형태는 **획 id + 그 3D 선분 위의 매개변수 t**(0..1)다.

**왜 숫자를 담으면 안 되나**: 잰 값은 축척이 바뀌면 따라 바뀌어야 하는데, 담는 순간 굳어
그 뒤로는 **조용히 틀린 치수**가 된다(#61이 막으려는 그것). 실측이 그 갈림을 낸다 —
축척을 두 배로 바꾸면 같은 두 점의 잰 값이 정확히 두 배가 된다(`scale32_web2.json`).

대가(알려짐): **소실점 오스냅으로는 못 잰다**(무한원이라 3D 점이 아니다) · 어느 선분에도
안 붙는 점은 **도면에 못 남긴다**(패널 표시는 된다).

### [6] **축척을 다시 정하는 별도 조작을 만들지 않는다** (32-5 · 지시 문면)

축척은 파생이다 — 첫 치수가 정하고, 그 치수를 **지우거나 고치면** 다시 정해진다.
「축척 재설정」 단추를 두면 그 순간 축척이 **저장되는 값**처럼 보이고, 실제로 저장하지
않으므로 화면과 모형이 갈린다.

⚠ 그래서 32-5가 한 일은 **표시**뿐이다: 「축척 미정」 ↔ 「**축척 1단위 = 1250 mm** · 기준 2400 mm」(⚠ 「1 : N」이 아니다 — 그 표기는 도면 길이 : 실제 길이이고 여기 분모는 **임의 모델 단위**다. 1차 리뷰어 [8]).
사람이 되돌리는 길은 이미 있는 길(그 치수 지우기·고치기)이고, e2e가 **지우개로 지워서**
그 왕복을 잰다 — UI에 실제로 있는 길로 재야 그 길이 있다는 증거가 된다.
---

## 화면 규칙 R1~R8 — **한 자리에 세운 규칙은 전수로 쓸어낸다** (2026-08-30 · web2-34)

web2-28·30이 세운 규칙 다섯이 **세운 그 자리에만** 적용돼 있었다. 사용자가 배포판에서
찾아 온 것들이 전부 그 형태다 — 규칙이 틀린 것이 아니라 **쓸어내지 않은 것**이다.
그래서 규칙을 한자리에 모으고 **전수 대조표**를 이 회차의 결과물로 둔다
(`web2/NOTES.md`의 「34-0 전수 대조표」 — 요소 40 × 규칙 8).

```
R1 실물에 없는 연속 조작을 만들지 마라. 고르는 것으로 바꾸고 견본을 1:1로 보여라  (30-2)
R2 리본 안의 무엇도 리본의 길이를 바꾸지 않는다. 펼침은 옆으로 겹쳐 뜬다          (30-3)
R3 명령을 실행하면 패널이 접힌다. 상태 토글·연속값은 안 접힌다                    (28-1)
R4 화면의 말은 이름이거나 짧은 동사구다. 예외 — 되돌릴 수 없는 것을 묻는 자리      (28-3+30-4)
R5 화면의 한 모서리는 한 가지만 쓴다. 겹치면 자리를 나누고 z로 풀려 하지 마라      (#79)
R6 접힌 통은 지금 고른 것을 말한다                                              ← 이 회차
R7 열린 통은 그 통 바깥의 무엇을 눌러도 접힌다                                  ← 이 회차
R8 늘 보이는 것은 잠깐 얹히는 것보다 약하다                                     ← 이 회차
```

### R5 정정 — **«오래 떠 있는 것»끼리는 자리를 나누고, «잠깐 얹히는 것»은 위에 온다**
(2026-08-30 · web2-34 6번 후속 · 리뷰어 [1]이 물어 측정이 갈랐다)

R5의 초판 문면(「한 모서리는 한 가지만 쓴다. 겹치면 자리를 나누고 z로 풀려 하지 마라」)은
#79가 잡은 사례 — **우하단에 오래 떠 있는 넷**(빌드 식별자·진단 패널·자립 깃발·서랍) —
에서 나왔다. 34-6이 그것을 자리로 갈랐다. 그런데 같은 문면을 **펼침(통)**에 그대로 대면
답이 없다: 치수 리본은 x 872..1104 · y 96..669의 **세로로 긴 기둥**이고 세로바의 통은
그 단추 옆에 떠야 하므로 **둘은 반드시 겹친다.**

측정이 그 자리에서 규칙을 갈랐다:

- **둘 다 오래 떠 있으면** → **자리를 나눈다.** 위 띠에서 내려오는 것(표시 팝업·파일·설정
  서랍)은 기둥의 왼쪽 길(`--top-lane`)에 선다.
- **한쪽이 잠깐 얹히는 것이면**(통·팝오버) → **나눌 것이 아니라 «위»에 있어야 한다.**
  잠깐 얹히는 것이 오래 있는 것을 덮는 것은 모든 앱의 기본 거동이다(A-3).

**그리고 위에 있지 않았다** — 그것이 이 정정의 발화다. `#sidebar`가 `position:fixed +
z-index`라 **자기 쌓임 맥락**을 만들어, 그 «안»의 `#tray { z-index: 12 }`가 맥락 **밖**의
`#dimpanel`(z 11)을 못 넘었다. 실측: 겹치는 자리의 `elementFromPoint`가 리본의 자식을 냈다
(연필통 → `pad-keys` · 촉통 → `dimpanel` · 크기통·롤통도 같다). **넷이 통째로 리본 뒤로
깔려 있었다** — 연필통·촉통은 web2-30 이래, 크기통은 34-3 이래다.

답은 통 넷을 **맥락에서 꺼내는 것**이다(`#app`의 직계로). ⚠ **z를 «올린» 것이 아니다** —
선언은 처음부터 12였고 맥락이 그것을 삼켰다. 자리는 `placeFlyout`이 `position: fixed`로
잡으므로 화면 자리는 한 픽셀도 안 변한다.

**판별 물음이 둘로 늘었다**: ① 이 둘이 동시에 열릴 수 있는가 → ② 둘 다 오래 떠 있는가.
①이 예이고 ②도 예면 자리를 나눈다. ①이 예이고 ②가 아니면 **잠깐인 쪽이 위에 오는지를
값으로 잰다**(`elementFromPoint` — 겹치는 자리의 한 점이 누구 것인가).

### R6 — **접힌 통은 지금 고른 것을 말한다**

접힘은 «자리를 아낀다»가 목적이지 «상태를 감춘다»가 아니다. 통을 접었을 때 지금 무엇이
골라져 있는지 말하지 않으면, 사람은 **확인하려고 통을 다시 연다** — 접힘이 자기 목적을
잃는다. 연필은 처음부터 이 규칙을 지켰다(접힌 몸통의 창에 등급 각인 `HB`). 펜은 안 지켰다
— 촉 굵기가 니브 사각형의 **폭 1.4px 차이**로만 있어서 화면에서 읽히지 않는다(34-2).

⚠ 대상은 **고를 것이 있는 통**이다. 파일·설정 서랍은 명령 모음이라 «고른 것»이 없다 —
R6 비대상. 자 팝업·눈 팝업은 **동시에 여럿이 켜지는 상태 토글 모음**이라 접힌 표시
하나로 못 줄인다 — 역시 비대상(34-2의 전수 판정표가 그 목록이다).

⚠⚠ **정정(2026-08-30 · 34-2 전수 점검 · D-4)**: 이 절의 초판은 「겹 요약(`#layer-summary`)은
**이미 지킨다**(롤 아이콘 + 쌓인 장수)」로 적었는데 **실측이 뒤집었다.** 그 롤은 «맨 위 겹»을
그리고 있었고 **«지금 그리는 겹»(활성)이 아니었다** — 아래 겹을 골라 두면 접힌 요약이
「지금 무엇에 그리는가」를 **틀리게 말했다**(활성 3 = 트레이싱지인데 요약은 옐로 · 반증까지
`layerbar.spec`의 R6 팔). 34-2가 **그림만** 활성을 따르게 고쳤다. 수(`.lsum-n`)는 그대로
«장수»다 — 그것은 «겹은 가산적»을 말하는 **다른 채널**이고 기존 팔들이 그 뜻으로 읽는다
(#77 ㉠). ⚠ **남은 것**: 같은 종이 둘이면 그림만으로는 못 가른다 — `DEFERRED.md`.

**연필·펜의 문법이 정본이다**(34-2): 접힌 몸통에 **각인 창 + 글자**. 펜은 몸통이 좁아
(10 → 8.8) `textLength`로 가로만 좁혔고 글자 높이는 연필과 **같다**(둘 다 렌더 15.00 px ·
비 1.000 · 문턱 `C.FOLD_MARK_MIN_RATIO` 0.9). 종전 판은 촉이 **니브 사각형의 폭**으로만
있어서(이웃 칸 차 0.44~1.29 px) 채널은 있으나 **글자가 아니라서 안 읽혔다** — R6의 게이트가
「채널이 있는가」가 아니라 **「연필과 같은 크기의 글자인가」**인 이유다.

### R7 — **열린 통은 그 통 바깥의 무엇을 눌러도 접힌다**

28-1은 「패널 **안에서** 명령을 실행했을 때」만 다뤘고 **바깥이 비어 있었다.** 사용자의
말이 그 구멍이다: 「필통이나 볼펜통을 열어놓은 상태에서 지우개 등 다른 버튼을 누르면
통이 접혀야 한다.」 선례는 **모든 팝오버의 기본 거동**이고, 이 저장소 안에도 이미 있다 —
`paperbar.ts`·`layerbar.ts`의 `away` 핸들러(window pointerdown 캡처)가 그것이다.
그 둘을 **정본으로 삼아 나머지로 옮긴다**(#54 — 새 기제를 만들지 않는다).

**R3과 안 부딪힌다**: R3은 «패널 **안**의 거동»(무엇을 누르면 접히나)이고 R7은 «패널
**밖**의 거동»(누가 접히게 하나)이다. 오스냅 체크처럼 R3의 「안 접힘」 대상도
**바깥을 누르면 접힌다**.

**동시에 둘이 열리지 않는다** — 다른 통을 열면 앞의 통이 닫힌다.

✅ **2026-08-30 · 34-4가 실행했다.** 등록부는 `web2/src/app/boxes.ts` 한 자리이고
통 열이 거기 있다(연필통·촉통·크기통·자·표시·면·파일 서랍·설정 서랍·겹·종이).
28-1의 `FOLD_PANELS`가 그 표를 겸한다 — **한 자리가 R3·R7·「하나만」 셋을 다 든다.**
팔은 `web2/e2e/ui34r7.spec.ts`(전수 ① · 캔버스 ② · 하나만 ③ · 제 일 ④ · 예외 ⑤).

**판정 문면 — 무엇이 «통»인가**: **겹쳐 떠서 딴 것을 가리는 것**(flyout·팝오버·서랍)이
통이다. 띠 자체를 늘렸다 줄이는 **인라인 펼침**(`#layer-list` 겹 목록 · `#sidebar` 접기)은
비대상이고, 조작이 없는 **판독 전용**(`#diagpanel`)도 비대상이다.

⚠ **예외 둘 — 판정 문면은 하나다: 「바깥을 눌러야 그 패널의 일이 되는가」.**

1. **치수 리본(`#dimpanel`)은 «통»이 아니라 «작업대»다.** 치수의 대상은 캔버스의 획이고
   재기의 두 점도 캔버스다 — 바깥 누름에 접으면 그 패널의 유일한 사용법이 죽는다
   (#77 ㉠). 도구 상태에 묶여 있다는 것이 그 표시다(`setTool('measure')`가 편다).
2. **설정 서랍(`#pane-settings`)의 «필압 보정 절차 중»**(2026-08-30 · 34-4의 D-4가
   찾았다 — 지시문의 표는 이 서랍을 그냥 R7 대상으로 들었다). 그 절차의 문면이
   「캔버스에 두 획을 그으세요」이고 **다음 지시와 그만두기 손잡이가 이 패널 안**에 있다
   (30-7이 스쳐 지나가는 알림 한 줄에서 여기로 옮긴 것이 그 항목의 전부다) — 첫 획에
   접히면 그 수리가 통째로 죽는다. ⚠ **예외는 패널이 아니라 그 구간에 건다**
   (`pinned: () => app.pressCalib !== null`) — 절차가 끝나면 이 서랍도 보통 통이다.

⚠⚠ **캡처로 잡되 «접기»만 한다** — 바깥 누름을 `preventDefault`하거나 삼키면 캔버스의
획·탭이 통째로 죽는다. 첫 누름은 **접기와 그 누름의 제 일**을 둘 다 한다(팝오버의 선례).
반증 짝이 그 자리를 지킨다(34-4 §⑤): 삼키는 판을 실제로 넣으면 「접힌다」를 재는 팔
①③은 **여전히 초록인데** 캔버스를 재는 ②만 빨개진다.

### R8 — **늘 보이는 것은 잠깐 얹히는 것보다 약하다**

같은 진폭·같은 대비가 **자리에 따라 다르게 읽힌다.** 겹(트레이싱지·옐로)은 사람이
«한 장 얹은» 것이라 잠깐 있고 그 결이 곧 «그 종이가 있다»는 표시다. 바탕 종이는
**화면 전체를 늘 덮는다** — 같은 진폭이면 그림보다 종이가 먼저 읽힌다.

30-9의 판정(「종이만 결이 없는 것이 결함이다」)은 **유효하다** — 사람은 결을 없애라고
하지 않았고 「과하다」고 했다. 그래서 값을 **낮추고 끌 수 있게 한다**(기본은 켜짐).
30-9의 게이트가 「셋의 진폭이 서로 20% 이내」였는데 그 게이트는 «보이느냐»를 묻고
있었다 — 34-1이 그 물음을 **「보이되 겹보다 약한가」**로 다시 적는다(#76 ㉣의 내리는 판:
값을 바꾸면 그 값을 다른 뜻으로 읽던 팔이 깨진다. 임계를 무르는 것이 아니라 **팔이
무엇을 묻는지**를 고친다).

⚠ 이 규칙은 **알림·툴팁·셔터**에도 걸린다 — 그쪽은 이미 «강한 쪽»에 있고 정당하다
(오류가 있을 때만 뜬다 — 4-b).

## web2-35 — 인식기는 «시야를 잇는다». 그리고 궤적은 그중 하나일 뿐이다 (2026-08-30)

**결정**: 글리프 하나를 읽는 경로를 **네 시야의 사슬**로 둔다. 앞이 거부할 때만 다음이 본다.

```
① 비 보존 래스터(digitnet)  ② 비를 편 래스터(web2-32)  ③ 궤적(web2-35)  ④ $P 점군(web2-08)
```

**근거**(원장 `glyph35_web2.json`의 `decomposition`): 합성 표본 880칸에서
506 → 695(④만 @0.095) / **725**(④만 @실린 0.10 · 오답 **3**) → 707(③만) → **770**(③④ 둘 다).
채택판의 오답은 **2**로 기준선과 같고 잡음 수용은 0이다.

**⚠ 이 결정이 «궤적을 채택한다»가 아니라는 것을 여기 못 박는다.**
올린 것의 대부분(+189)은 «시야를 하나 더 둔 것»이다. ⚠⚠ 그리고 **「궤적이 $P보다 낫다」는
이 표본으로 못 가른다** — 전량으로는 707 대 695(12칸)이지만 **씨별로 [-1, 0, 7, 4, 2]**라
폭 8에 부호가 뒤집힌다. #14가 그 형태다(2차 리뷰어 [4]가 잡았고 초판은 이것을 헤드라인에 썼다).

둘을 **함께** 두는 근거는 「궤적이 낫다」가 아니라 **「둘이 서로 다른 자형을 살리고, 그 몫이
씨마다 선다」**이다(런타임과 **같은 자**로 잰 값 — 초판은 $P를 0.095로 둔 표를 770 옆에
세웠다. #83 ㉡의 재발):

- ④가 이미 있어도 ③이 **+45** — 전부 「4·닫힌·2획」이다(세로획이 사선의 **꼭짓점에서**
  시작한다: **획의 시작 자리와 순서**가 갈랐다. ⚠ 되짚기가 아니다 — 그 자형은 $P도 80칸
  전부 살린다). 씨별 **[9, 8, 10, 9, 9] · 폭 2**.
- ③이 이미 있어도 ④가 **+63** — 세리프 「1」이다. 씨별 [17, 14, 10, 11, 11].
- 검산: 45 + 63 + 156(둘 다) = **264** = 770 − 506 ✔

⚠ 「궤적이 앞에 서면 $P의 오답이 3 → 2로 내려간다」는 **채택 근거에서 뺐다** — PITFALLS
**#20**(거름을 선택 전에 걸면 경쟁자가 죽어 상대 순위가 거저 통과한다)의 모양이고, 씨별로는
$P의 오답이 [0, 0, 1, 0, 2]로 **2를 넘는 씨가 하나도 없다**(「불법」은 다섯을 합쳤을 때만
나오는 판정이다). 사실로만 기록한다.

✅ **그 45의 귀속은 갈렸다**: ④를 켠 채 ③ 자리에 **궤적 항을 끈 판**을 넣으면 얹는 몫이
**0**이다(씨별 [0,0,0,0,0]). 같은 구조·같은 원형 표·같은 자리인데 **궤적 항만 끄면 한 칸도
못 얹는다**(씨 다섯 각각) — 그러므로 45는 **«방향·순서를 보는 항»**에서 온다.
⚠⚠ **다만 「네 번째 시야면 아무거나 된다」를 반증한 것은 아니다**(3차 리뷰어 [23]): 그 위약은
④($P)와 **같은 계열**(같은 원형 표 · 순서·방향을 안 봄)이다. 모양 계열이 아닌 대조기가
저장소에 없어 **미결**이고 DEFERRED에 올렸다.
⚠ 다만 그 45는 **자형 하나에 통째로** 있고, 씨가 자형 구성을 안 바꾸므로(흔들기만 바꾼다)
**자형을 넘는 일반성은 이 표본이 못 말한다**.

**기각한 것 셋**:
- **궤적을 판정자로 두기**(래스터를 뒤집게) — 오답 2 → **295**. 원형 표가 자리마다 필체
  하나뿐이라 「맞게 읽힌 것」을 자주 뒤집는다.
- **$P가 답하고 궤적이 거부권을 갖는 판** — 686으로 둘 다보다 못하다(둘의 답이 겹치는
  칸이 적다 — 서로 다른 자형을 살리므로 «동의»를 요구하면 둘 다 죽는다).
- **세리프 「1」 원형을 원형 표에 더하기** — 그러면 이 회차의 평가 픽스처를 외우는 것이
  되어 표본 밖 팔이 통째로 무의미해진다. 원형 표를 넓히는 것은 **표본이 다른 회차**의 일이다.

**되돌릴 조건**: 실기기에서 ③이 오답을 만들면 ③을 끄고 ④만 남긴다 — 그때 $P 문턱을
0.10 → 0.095로 내려야 오답 불변식이 선다(원장 `arms.p_dollar`).

**⛳ 사람이 판정할 것**(지시 35-4): 손으로 특징을 짜맞추는 방식의 한계가 이 회차의 수로
드러났다 — **궤적이 순서 무시 대조기보다 나은지조차 이 표본으로는 못 가른다**(씨 변동폭 안).
다음 갈래는 **획 순서열에 작은 분류기를 얹는 쪽**이고, 지시가 이 회차에서 착수하지 말라고
못 박았다.

---

## web2-37 (2026-08-31) — 앱이 더할 수 있는 것 · 교차의 층위 · 대기의 수명

### 앱은 기록에 없던 것을 더하지 않는다. **그리기에는 제도 관습을 더할 수 있다.**

(web2-37 3번의 규칙 정정 — 지시문이 DECISIONS에 올리라고 못 박았다.)

삐져나오기가 이미 그 형태다: 사용자가 그린 것이 아니지만 제도 관습이라 앱이 더한다.
가르는 것은 **층위**다 — 저장되는 것(기록)에는 안 더하고, **그리는 단계**에서는 더한다.
`core/overshoot.ts`가 그 경계의 본보기다(판정은 3D 좌표에서 하고 꼬리는 `render2d`가
그 자리에서 긋는다 — `a·b`도 `a3·b3`도 안 움직인다. 실측으로 확인했다 — 37-0 ㉡).

⚠ 37-3(교차 강조)이 같은 자리다. **이 회차에서는 착수하지 않았다** — 규칙만 여기 세운다.

### 가상 교차는 **오스냅이 아니다** — 층위가 다르다

- 오스냅 = 「**커서**가 무엇에 물리는가」. 획을 시작할 때는 아직 아무것도 안 지났다.
- 가상 교차 = 「**획**이 무엇을 지나갔는가」. 획 중간에 일어난다.

그래서 자리가 `osnap.ts`가 아니라 `lift.ts`다. 30-11이 연장선을 «후보 목록»에서 빼내
«선언된 구속»으로 옮긴 것과 같은 이동이다.

**딸린 규칙 — 교차의 후보는 «옛 선»뿐이다**(문서 순서가 앞선 획). 나중 획이 먼저 있던
대기선을 **되짚어** 굳히는 것은 다른 물음이고, 이 앱은 그것을 이미 다르게 답해 뒀다:
「지나가기만 한 교차는 사건이 아니다 · 끝점이 닿아야 센다」(`own3d` 4-g)와
「겹은 아래를 안 바꾼다」(web2-21 층 소유). 그 조건을 빼면 **둘 다 조용히 뒤집힌다**(실측).

### 대기 획의 수명은 **광선**이 정한다

「애매하면 놓지 않되 버리지 않는다」(개정 2 §9.1)는 **아직 쓸 수 있는** 정보를 버리지
말라는 것이다. 대기 획의 유일한 내용은 「그 시점의 화면 위 어디」이므로 시점이 바뀌면
그 정보는 **거짓**이 된다 — 버리는 것이 정확성이다.

가르는 기준은 손잡이 이름이 아니라 **「광선이 바뀌는가」** 하나다:
포즈(궤도·뷰 큐브·궤도 중의 팬/줌)와 **렌즈**는 바꾸고, 작도 포즈의 이동·확대는 안 바꾼다.

⚠ **실행취소는 그 시점까지 무른다.** 획만 되돌리면 되살림이 한 프레임짜리다(실측 — AS-C139).


## web2-40 (2026-08-31) — 겹의 결과 까는 동작

### 겹은 **한쪽에서 밀려 들어온다** — 전자책에서 가져오는 것은 앞 가장자리뿐 (web2-40 2번)

사람이 참고로 준 것은 「전자책의 페이지 넘김」이었고 지시문이 그 자리에서 갈랐다:
그것은 **경첩처럼 접히는 동작**이라 그대로 쓰면 종이가 어디선가 돌아 나오는 것으로 보인다.
롤에서 뽑아 덮는 것이므로 실물에 가까운 것은 **밀려 들어오는** 쪽이다.
가져오는 것은 동작이 아니라 **앞 가장자리의 표현** — 살짝 말린 그림자로 두께가 읽히는 부분.

- **방향은 하나다**(`SLIDE_FROM = 'left'`). 겹이 여러 장이어도 같은 쪽에서 들어온다 —
  장마다 방향이 달라지면 어지럽다. 왼쪽인 근거: 겹을 얹는 자리 둘(종속 탭 「+」·손 띠
  롤통)이 **둘 다 화면 왼쪽 위**라 종이가 그 롤에서 뽑혀 나오는 것으로 읽힌다.
- **앞 가장자리는 «진행 방향»이 아니라 «종이의 성질»이다.** 들어올 때 앞섰던 변이
  물러날 때도 같은 변이다 — 그래서 말린 그늘은 언제나 **오른쪽 변**에 진다.
- **길이는 `FOLD_ANIM_MS`를 가리킨다**(300 ms · 새 숫자 ⛔ — #54). `TURN_ANIM_MS`·
  `SETTLE_ANIM_MS`가 낸 선례 그대로이고, 같은 물음이기 때문이다: 앱이 스스로 화면을
  바꿀 때 사람이 따라갈 수 있는 시간.

### 동작은 **입력을 막지 않는다** — 그리고 그것은 «막지 않기»가 아니라 «표현만 들기»다 (web2-40 2번)

「동작 중에 그리기를 막지 마라」를 **코드로 지키는 방법**은 «막는 코드를 안 쓰는 것»이
아니라 **동작이 상태를 안 지는 것**이다. 겹은 창이 열리는 그 순간 이미 문서에 있고(깔기)
또는 이미 없다(치우기) — 창은 `settledAt`(web2-37 2번)과 같은 급의 **표현 표**이고
판정·좌표·저장 어느 것도 그것을 안 읽는다.

- **치우기에서 «지우기를 미루는» 길을 안 간다.** 겹을 걷는 순간 `doc`은 바로 옳아야 한다
  (실행취소·저장·3D가 그것을 읽는다). 문서에서 즉시 빼고 **화면에만 사본**을 남긴다.
- 그래서 「획이 들어오면 즉시 자리를 잡는다」는 **화면을 앞당기는 한 줄**이지 입력 경로에
  손대는 것이 아니다.

## D-W13 — 2026-09-01 (web2-42): 투영은 «포즈»에 산다 · 큐브의 가운데가 「투시」다

지시문은 `docs/instructions/web2-42.md`. 사람이 라이노 뷰포트를 기준으로 정리했다 —
**Top·Front·Right는 「이름 붙은 뷰」이고 평행은 그 뷰에 딸려 오는 성질**이지 토글이 아니다.

### [1] 투영 상태를 어디에 둘 것인가 — **`CamPose.proj = {w, D}`** (채택)

| 안 | 판정 | 사유 |
|---|---|---|
| **포즈에 둔다**(채택) | ✅ | 리프팅이 「그 획이 그려진 시점」(`s.view`)으로 광선을 되짚는다. 투영이 그 시점에 실려야 평행에서 그은 획이 평행으로 되풀린다. 저장·복원·되돌리기·종이 시점이 전부 포즈를 통째로 다루므로 **배선할 자리가 없다** |
| 앱 상태 깃발(`App.parallel`) | ⛔ | 평행에서 그은 획을 나중에 원근으로 되푼다 — **조용히 틀린 좌표**(CLAUDE.md §1이 금지하는 그것). 증거: 저장본에서 `view.proj`를 지우면 그 획의 3D가 실제로 달라진다(`view42_web2.json`의 `gate10`) |
| `Stroke`에 새 필드 | ⛔ | 포즈에 넣는 것과 뜻이 같은데 배선이 하나 더 는다(획·종이·되돌리기 셋을 따로 고쳐야 한다) |
| `Camera.f`를 크게 만들어 근사 | ⛔ | 지시가 못 박은 금지(`f`·`fSource` ⛔)이고, **수치로 평행이 아니다** — 게이트가 「평행선이 평행하게 찍히는가」를 재므로 근사는 통과 못 한다 |

**사영은 분모 하나로 합쳤다**: `den = (1−w)(−z) + w·D`. `w=0`이면 옛 식과 문자 그대로 같고
(구성상 항등 — 그래서 임계를 안 건다), `w=1`이면 배율 `f/D`의 평행이다. **사이 값도 정당한
사영**이라(눈을 뒤로 빼며 조인 원근) 전환을 그대로 보간한다 — 그것이 지시의 「전환은 보간한다」다.

### [2] 배율을 무엇으로 잡을 것인가 — **`f / D`** (채택)

`D` = 눈에서 pivot까지의 **축방향 거리**. 그러면 **pivot 면에서 원근과 크기가 정확히 같아**
전환이 안 튄다. 상수(예: 「화면 폭의 1/10」)로 두면 면을 누를 때마다 대상이 뛴다 —
예약값을 상수로 두지 말고 대상에서 유도하라는 #88이 이 자리다.
따라오는 것 하나: **평행에서 휠은 눈이 아니라 `D`를 움직인다**(눈만 옮기면 상이 안 변한다).

### [3] 큐브의 가운데는 「투시」다 — **꼭짓점보다 먼저** (채택)

아이소메트릭에 가까운 자세에서는 **앞 꼭짓점이 정확히 화면 중심에 사영된다**. 둘 중 하나가
이겨야 하고 **투시가 이긴다** — 투시로 돌아가는 길은 언제나 열려 있어야 한다(지시 문면).
잃는 것은 그 자세의 앞 꼭짓점 하나이고 그 시점은 **지금 보고 있는 자세**라 누를 이유가 없다.
⚠ 꼭짓점·모서리 거동 자체는 31-1 그대로다(지시: 「꼭짓점 방향을 되살리지 마라」) —
**다만 «짚히는 자리»는 그만큼 좁아졌다**: 모서리 하나는 중점이 원 안이고 길이의 47.6%가
덮인다(짚을 수 없게 된 모서리는 0이다 — 2차 리뷰어 [1]이 그 서술의 오류를 잡았다).
⚠⚠ **실측**: 표준 2점 구도에서는 덮이는 꼭짓점이 **0**이다(가장 가까운 것 31.7 px · 원 16 px) —
덮이는 국면은 아이소메트릭 자세뿐이고 거기서 하나(거리 0.000000)다.

### [4] 잠금·버리기는 31-2와 **같은 술어·같은 자리** (채택)

`parallelAllowed = lensAllowed`이고, 버리는 코드는 `recompute`의 렌즈 블록 **안**이다
(조건을 새로 안 지었다 — #54). ⚠ **도달 가능성을 재 보니 절반이 안전망이었다**: 평행 뷰에서
그은 획은 `analyze`가 내용으로 돌리므로(「작도는 작도 포즈에서만」) **평행에 있는 동안 승격이
일어날 길이 없고**, 확정 해제는 되돌리기도 지우개도 못 한다(둘 다 작도 획을 거부한다).
그 사실을 원장에 적고 팔은 **기제**를 잰다.

### [5] 읽는 값은 대체된다 — 렌즈길이 mm ↔ 축척 1:N (채택)

원근에서 축척은 정의되지 않고(깊이마다 배율이 다르다) 평행에서 렌즈길이는 무의미하다(눈이
없다). 그래서 **한 자리를 서로 대신한다**. 환산 기준은 35mm 판형 대각 43.27 mm이고 **36×24에서
유도**한다. 축척의 출처는 32-5의 `doc.scaleRef`(= `lift.mmPerUnit`) 하나다 — 새 기제 ⛔.
화면 1 CSS px를 `25.4/96` mm로 보는 것이 가정이고 그 자리는 [[AS-C147]]이다.

## D-W14 — 2026-09-01 (web2-44): 잡기는 꾹 누름의 연장이다 · 옮김은 «2D 재사영 + own3 갱신»이다

**같은 제스처(꾹 누름)에 두 번째 뜻이 얹혔다** — 39의 글씨 진입 위에 44의 잡기가 선다.
가르는 규칙은 «다음 동작»이다(제스처 자체가 아니라):

```
꾹 누름의 대상   치수 숫자(39-5) > 조작 값 표찰 > 선 > 잠긴 선 > 면    — holdTargetAt 하나(#54)
선을 처음 꾹     잡힘 + 글씨 상태(39 그대로 — 숫자를 쓰면 그 선의 치수)
같은 선 또 꾹    이어진 것까지(connectedIds — 3D 끝점 접촉의 연결 성분)
다른 선 꾹       잡음에 더한다(여럿)
잡힌 것 위 끌기  손이다(몸통=옮김·끝점=돌림) — 글씨가 아니다   ← 두 뜻을 가르는 문
잡힌 것 밖 획    글씨다(39 그대로) · 먼 곳이면 세션 종료(39-3 far — 잡음도 함께 놓인다)
```

**잡기 세션 = 글씨 세션**(함께 살고 `endWriting`이 둘 다 놓는다). 따로 놓으면 「글씨는
끝났는데 잡힘이 남아 다음 획이 조작으로 먹히는」 자리가 생긴다.

**옮김·돌림·맺음의 좌표 규약**: 문서는 2D가 정본이고 3D는 굳힘(own3)이므로, 조작은
① 3D를 옮기고 ② 그 획의 «자기 시점»(s.view ?? DRAW_POSE)으로 2D를 다시 사영하고
③ own3를 새 3D로 갱신한다 — 그래야 잉크 심판(own3d §7)이 왕복에서 그대로 선다
(`grip44.test.ts` ②가 값으로 지킨다: 옮긴 뒤 own3Deviation ≤ 0.01px). raw(손 잉크)는
옛 끝점 → 새 끝점의 **유사변환**으로 따라간다(모양 보존 — 점별 재사영은 손떨림을 뒤튼다).

**기각 셋**:
- «자동 따라가기»(의존 갈래 ㉠의 자동판) — **기각의 근거는 설계다**: own3에는 «누가
  정의했는지»가 없고(D-W8) 만들면 잡지 않은 것이 움직인다. ⚠ ㉠의 «접점 보존 0 끊김»
  (`grip44_web2.json`의 `b1_follow_geometry`)은 **구성상 보장**이라(#5 — 같은 변환을 전부에
  걸면 접점이 사는 것은 정의) 우열의 측정이 아니다 — 측정이 가른 것은 ㉢의 정보 파괴와
  ㉡의 틈 크기·비례뿐이다(그 원장의 `verdict` 필드가 경계의 정본 문면).
- «B를 대기로»(㉢) — 실측이 최악: 딸린 셋 중 둘이 대기로 떨어지고 하나가 **2.34유닛
  딴 자리에 조용히 다시 선다**(b3_waiting) — 이 저장소가 가장 경계하는 형태다.
- «명령 목록/컨텍스트 메뉴» — 손잡이는 세로바의 손통(핀셋) 하나다. 알림 줄의 밑줄 단어는
  물음(잠긴 선의 「해제」)에만 쓴다.

**소실점을 만든 획(role 'vp')은 못 잡는다** — 카메라가 그 잉크에서 나오므로(analyze)
그것을 옮기면 camSig가 움직여 굳힌 3D 전부가 낡는다. 면 경계로 잡힐 때도 빠진다.
대가: 깊이선이 곧 벽 밑변인 벽은 밑변 없이 잡힌다(DEFERRED — 관측 판정자).

**돌림은 수직축(세계 Y) 둘레 하나다** — 제도의 「돌린다」는 평면(도) 위 회전이다.
축점은 잡은 끝의 **반대 끝**. 다른 축 둘레는 범위 밖(DEFERRED).

**맺기(R=0)는 연장만 한다** — P의 발이 선분 밖이면 가까운 끝을 그리로, 안이면 무변경
(자르기 ⛔ — 삐져나오기는 이 앱의 값이다). 같은 평면 판정은 면 평면성과 같은 자
(`C.PLANAR_RATIO` — 같은 물음·같은 자 #54). 치수가 길이를 쥔 획은 거부한다 — 연장해도
다음 리프팅에서 치수가 길이를 도로 세워 조용히 풀리기 때문이다.

**면 정면 뷰는 «면이 정한 뷰»다** — 자세는 faceFrontPose(법선 정면·거리 유지·세로 면은
up=세계 Y), 투영은 parallelPose(D-W13의 그 길 — 새 상태 ⛔). 42의 일곱 뷰와 같은 보간.

## D-W15 — 2026-09-01 (web2-45): 칠은 «면 위에서의 lift»다 · 채움은 면의 함수다

**칠 획의 자료 규약**: `raw`(그은 시점의 문서 좌표)가 정본이고 3D 점렬은 **파생**이다
(`core/paint.liftPaint` — 광선 ∩ 면 평면 · 원칙 b: 저장하지 않는다). `Stroke.paint = {f}`
하나가 태그다. 옐로·글씨의 자리(analyze·liftAll의 그 필터)에서 같이 걸러져 소실점·리프팅·
오스냅에 안 낀다 — **선과 같은 문법, 다른 판**이라는 지시 문면이 그대로 코드 형태가 됐다.

- **한 붓 → 면별 조각**(`splitByFace`): 연속 점들이 같은 «맨 앞 면»(frontFaceAt —
  화면 폴리곤 ∩ 최소 평면 깊이)을 짚는 동안 한 획이다. 허공 점은 세고 버린다(조용히 ⛔).
  실행취소는 붓 하나가 한 칸이다.
- **면이 못 풀리면 칠도 안 보인다** — 면의 규약(버리지 않고 빠진다) 그대로. paintGeo에
  항이 없을 뿐 문서에는 남고, 경계가 돌아오면 같이 돌아온다.
- **렌더**: 파생 3D를 지금 포즈로 사영해 `drawStrokeRaw`(옐로의 점렬 몸체 — 같은 흑연
  질감)로 긋는다. ⚠ classic 렌더러에서는 안 보인다(알려진 강등 — DEFERRED).
- **지우개**: 통째 규약(2D 획의 선례). ⚠ 판정은 raw(그은 시점)가 아니라 **파생 3D의 지금
  사영**으로 — 시점이 바뀌어도 «보이는 그 자리»가 지워진다.

**분류(45-2)는 법선 하나·출처 한 자리다**: `Face.cls ?? faceClassOf(normal, FACE_CLASS_DEG)`.
임계 10°(실사용 표본 없음 — 안전판은 «사람이 고친다» 그 자체다). 정정은 손통 「분류」가
자동→슬라브→벽→경사→자동으로 돌리고 실행취소에 걸린다. 두께·외벽/내벽은 지시대로 ⛔.

**채움(45-4)은 면의 함수다**: `Face.fill = 1`뿐이고 해칭은 경계에서 매번 생성된다
(`core/hatch.hatchSegments` — 짝수-홀수 절단이라 개구부가 자동으로 뚫린다). **판이 둘이고
⚑다**: 화면 고정(기본 · 시점마다 재생성 — 무늬가 면 위에서 미끄러진다) ↔ 면 고정(간격을
작도 포즈 환산으로 굳혀 면에 붙는다). 토글은 표시 팝업 「해칭을 면에 붙인다」 — 사람이
실기기에서 고른다. **→ 2026-09-01 답이 왔다: 화면 고정.** 기본값이 그대로 서고 «임의의
자리 표시» 딱지를 뗀다(⚑ 해소 — 판정자는 사람 눈, 45 마감·배포 뒤 46 착수 전에 물었다).
면 고정 판은 토글 뒤에 남는다 — 지우지 않는다(둘 다 만들라는 지시 문면이 그대로 살아 있다). 렌더 자리는 면과 선 «사이»(sortFaces가 제 면 +1 순서) — 톤은 선 아래.

**깊이 정렬(45-1)**: 기준선 실측이 «지정 순서가 나쁘면 뒤집힘 33/33»을 냈고, 수리는
**화가 알고리즘**(render3d.sortFaces — 매 프레임 중심의 시선 깊이로 renderOrder 배정 ·
전부 음수 대역이라 선(0)은 언제나 위)이다. depthTest는 여전히 없다 — 면이 선을 가리지
않는 설계(6-h)가 불변이고, 순서만 참이 됐다.

**기각**: 픽셀로 굽기(경계 변화에 못 따라간다 — 지시가 이름으로 금지) · 광원/자동 명암
(렌더링이 된다 — 지시 ⛔) · 전개도 · 두께 추론 · 칠 획의 3D 저장(파생 — 원칙 b).

## D-W16 — 재료: 색이 아니라 재료로 고른다 (web2-46 · 2026-09-01)

**팔레트는 재료 목록이다**(지시 문면): `core/palette.ts`의 `MATERIALS` 다섯(벽돌·콘크리트·
유리·나무·금속) = 톤 둘셋(밝음·중간·그림자 — 광도 단조를 팔이 잰다) + 해칭 무늬
((각도·간격·교차) 다섯 짝 전부 상이 — 상호 판별을 팔이 잰다). 색 hex 자체는 표시 문법이라
임계가 아니다 — 쓸 만한가는 실기기 눈(46 ⚑ — 멈추지 않고 확인 항목으로).

**칠 도구 셋**: 붓(45의 흑연 그대로 — 기본값·무회귀) · 마커 · 색연필. 저장은
`Stroke.paint.m/t/i`(셋이 같이 성해야 받고, 하나라도 틀리면 셋을 함께 버려 흑연 강등 —
조용히 틀린 색 ⛔). 면 재료는 `Face.mat`(채움 해칭의 무늬·색 — 손통 「재료」 순환,
cls 순환의 문법).

**마커 = marker46**: 내장 marker에서 **spacing 하나만** 0.03 → 0.2(`C.MARKER_SPACING`).
내장 그대로는 「겹치면 진해진다」가 포화로 죽어 있었다 — **정본 근거는 원장의 대조 팔이다**
(mats46_e2e_web2_*.json `marker_overlap` — 같은 획·같은 상자에서 출하값의
상대 계단 rel_step_1to2_02 ↔ 내장 0.03의 rel_step_1to2_003. 후자는 **크게 준다** —
(⚠ web2-50 갱신: 마커가 면 텍스처의 canvas multiply로 옮겨 가 spacing 축 자체가 소멸 — 이 절의 수치는 46 시점 실행의 역사 기록이고, 현행 반증은 mats46_e2e_*의 rel_step_1to2_flat이다.)
dpr1은 비단조(음수)까지, dpr2는 +3% 대역 vs 출하 +51%. 2차 [3]: «죽는다»는 dpr1의
서술이고 결정이 딛는 것은 두 dpr 공통의 1/10 대역 대조다. 값은 원장이 정본·실행마다 잰다).
후보 대역은 브라우저 탐색 스윕이 골랐다(NOTES 46-0 후속 — **탐색이지 원장이 아니다**).
다른 파라미터는 내장 값 그대로(근거 없는 수 ⛔ — BRUSH_OF 규약).

**톤 «자동» = 분류의 제안**: slab→밝음 · slope→중간 · wall→그림자(위에서 오는 빛의 코사인
차례 — 구성적 근거, 측정 아님 · AS-C152). **제안이 기본값으로 박히지 않는 배선**: 자동일
때만 칠하는 순간의 면 분류가 톤을 정하고, 사람이 견본을 고르면 그 값이 그대로 남는다
(측정: `mats46_web2.json@2d46120e` suggest_seq — 자동 0·2 / 수동 1·1). 면에 자동으로 칠해지는 일은
없다(칠은 사람이 한다 — 지시 문면).

**기각**: ① 수채(지시 ⛔ — 유체 문제) ② 팔레트를 색 목록으로(지시 ⛔) ③ 스포이드(지시 —
배경 사진이 생기면 값이 생긴다 · DEFERRED) ④ 마커의 진짜 곱셈 합성(GL 블렌드 개조 —
spacing 하나로 겹 계단이 서므로 라이브러리를 안 연다 · 복잡한 대안은 DEFERRED)
⑤ 새 브러시 의존(p5.brush 재고 조사 — 11종 내장 확인 뒤 brush.add 1로 끝).

## D-W17 — 숫자와 표시: 크기가 보이고, 세어진다 (web2-47 · 2026-09-01)

**스케일바는 공간에 놓인 자다**(지시): 닻(화면 좌하 고정점)의 접지 깊이에서 1·2·5×10ⁿ
내림, **끝점은 정확한 재사영**(선형 근사 ⛔ — #61). 42-3(화면 축척 문자)과 안 겹친다.
축척 미정이면 안 뜬다 — 무스케일을 숫자로 위장하지 않는다(lenMm의 규약 그대로).

**사람은 사용자가 놓는다**(자동 ⛔ — 지시): 표시 「사람 놓기」 무장 → 지면 탭 하나.
스텐실은 기기(localStorage — 그리는 사람에게 붙는다), 놓은 자리는 문서(Doc.persons —
접지점뿐 · 43 왕복 팔 등재). 그리기 판은 설정 안(찾는 사람만 찾는다 — btn-stencil).
눈높이 가로선 규칙: 눈점 = eyeAbove(접지) — **눈이 지평선에 얹히는 것이 구성이다**
(카메라가 눈높이에 있다는 이 앱의 근본 사실). **기본 스텐실 몇 벌은 비워 뒀다** — 지시
문면(사용자가 그려 줄 것 · 없으면 자리만)대로 보고 항목이다.

**숫자는 근거와 같이 간다**(#61 — 이 회차의 중심): 면적·부피의 모든 표시가 근거 면
id를 들고 그 면을 밝힌다(hlFaces 1.6초). 부피는 전제(벽고 균일 ±2%)가 안 서면 사유를
말하고 **안 낸다**. 신뢰의 근거는 45 기준선(faces45 — 놓친 면은 합에 못 들고 그만큼
모자란다 — area.note_45).

**실 = 벽 바닥 자취의 평면 그래프에서 유계 둘레**(면 찾기와 같은 기제 — 반변 회전).
⚑는 비발동 — 합성 시나리오 전수 일치·헛것 0(nums47_web2.json rooms). 연결 = 개구부
있는 공유 벽. 다이어그램 = 버블(√면적 반경)+선, 표시 토글(파생 — 문서 아님).

⚠ **절대 수치의 출처**(2차 [H1]): 이 회차가 내는 모든 mm·m²·m³는 mmPerUnit(사용자 치수)과
f(카메라 — AS-C4의 전역 배율) 위에 있다. 원장 fixtures 블록이 그 값을 들고, 1점 문서
(f 임의)의 감도는 DEFERRED다 — 숫자의 신뢰는 축척의 신뢰를 넘지 못한다.

**부수 수리**: pointOnGround/Ceiling의 광선 원점(pose.p → r.o) — 평행 사영에서 모든
화면 점이 같은 접지점을 내던 결함(47-1 평행 팔이 잡았다 · 원근 무회귀 — r.o==p 구성 등가).

**기각**: ① 사람 자동 세우기(지시 ⛔) ② 그림자·태양(지시 ⛔ — 자동 렌더링 쪽)
③ 근거 없는 면적(지시 ⛔) ④ 스케일바의 선형 배율 근사(#61 — 끝점 재사영으로)
⑤ 부피의 경사 지붕(프리즘 전제 밖 — 안 내는 쪽) ⑥ 기본 스텐실 지어내기(지시 문면 위반).

---

## D-W18 — web2-48 (다듬기: 쓰면서 나온 것들)

**칠의 「하얗게 뜸」은 색이 아니라 «합성»이었다** — 원인이 우리 코드 밖이다. `p5.brush`의
블렌드 셰이더가 `bgColor = mix(vec3(1.0), source.rgb, source.a)`로 **빈 자리의 배경을 흰색
으로 가정**하고 `outColor.a = 1`로 **불투명하게** 쓴다(라이브러리는 «흰 종이 위에 그린다»를
전제로 짜여 있다). D-1 단독 렌더 실측: 마커 한 획의 상자에서 칠한 픽셀 **1360개가 전부
알파 255**이고 **948개(69.7%)가 종이보다 밝았다**. 처방으로 `#brushc`·`#brushsnap`을
**곱**으로 얹어 보았고 장막은 실제로 사라졌지만(918 → 0) **마감 전량 e2e가 그 수리를 되돌렸다** — 이 겹은 «종이 위의 잉크»가 아니라 **#gl의 몸체 위에 얹히는 질감**이라 질감 × 몸체로 흑연이 두 번 어두워졌다(press26 ② · 꺼짐 185.3 → 128.9 · 켬 196.0 → 127.1 · 보정의 순서까지 뒤집혔다 — AS-C163). **48-1은 진단까지다**: 원인과 수리의 존재를 값으로 남겼고 화면은 45·46과 같다. 올바른 수리는 **칠 전용 겹**(DEFERRED) — 흰색이 곱의 항등이라 장막이 사라진다. 감산 매체의 올바른 연산이
애초에 곱이고 이 앱은 이미 그것을 알고 있었다(`#film`의 in-canvas 곱).
**기각**: ㉠ 라이브러리 패치(포크 유지 비용 · 라이선스 아님) ㉡ 픽셀 되읽어 알파 복원
(프레임마다 GPU 판독 — 18의 비용 표가 금한 자리) ㉢ 「48-9로 면이 종이색이 되니 안 보인다」
(면 위에서만 참이고 선 위에서는 여전히 지운다).

⚠⚠ **#73의 범위를 좁혔다**(D-4 — 지시문이 준 근거가 이 겹에 안 걸렸다). #73 ㉠이 「CSS
`mix-blend-mode: multiply`가 이 앱의 합성 트리에서 발화하지 않는다」로 적혀 있는데, 그
실측은 **`#film`**의 것이다. 같은 실행에서 `#brushc`에 걸어 재니 **산다** — 합성 광도
244 → 241 · 종이보다 밝은 픽셀 **918 → 0** · 벗기면 되돌아온다. **겹이 선 자리의 성질이지
트리 전체의 성질이 아니었다**(assumptions AS-C157 · PITFALLS #73에 범위 줄).

**칠은 면의 «한쪽»에 붙는다**(48-5 — 사람 지적이 맞고 45가 틀렸다). 벽이 단일 평면이고
두께가 나중에 오므로 그 평면의 앞뒤는 **장차 두 표면이 될 자리**다. 저장은
`Stroke.paint.s = sign(n·P − d)`이고 `n`은 **저장된 법선**(루프 감김) — 그래서 부호가
«평면의 쪽»을 가리키지 «어느 화면»을 가리키지 않는다. **두께가 와도 다시 해석할 필요가
없다**(`+1` → 법선 쪽 표면 · `−1` → 반대쪽). 쪽은 **칠할 때 카메라가 있던 쪽**이 정하고
사용자는 의식하지 않는다. 부호 없는 옛 획은 양쪽에서 보인다(조용한 변형 ⛔).
**기각**: ㉠ 「앞/뒤」를 화면 기준으로 저장(포즈가 바뀌면 뜻이 바뀐다) ㉡ 면을 둘로 쪼개기
(면의 정체가 둘이 되어 43의 왕복·45의 깊이가 전부 갈린다) ㉢ 칠을 면의 속성으로 옮기기
(칠은 획이고 획은 사건이다 — 원칙 b).

**팔레트의 좁힘을 정정했다**(48-7). **web2-46의 지시문**이 「팔레트를 색 목록으로 만들지
마라, 재료 목록이어야 한다」고 못 박았고 46 세션이 그대로 지어 **견본 14개**만 남았다.
그 좁힘은 **사용자와 상의 없이 지시문이 한 것**이고 판단도 틀렸다 — 재료가 색을 정하는
것은 맞지만 그것은 **여러 길 중 하나**이고, 전문 드로잉 툴이면 임의의 색을 뽑을 수 있어야
한다. 지금은 **색상 휠이 기본**(고리 = 색상 · 안쪽 판 = 채도·명도 · 프로크리에이트 클래식의
형태 · 슬라이더 0)이고 재료 프리셋이 곁이다. 색 공간은 `culori`(직접 안 짠다).
저장은 `paint.{m,t}` → **`paint.c`(hex)** — 색의 출처가 둘이면 「고른 색」과 「나가는 색」이
갈린다(#54). 46 파일의 (m,t)는 파서가 **무손실로 색으로 옮겨 받는다**.

**「톤 자동」을 함수째 없앴다**(48-8). 46의 「분류가 기본 톤을 제안한다」도 지시문이 넣은
것이고 사용자가 원하지 않았다. **자리를 안 남긴다** — 남겨 두면 다음 회차가 «있으니 쓴다»로
되살린다(#65의 형태). 팔도 겨눔을 옮겼다(재던 양이 없어진 팔은 아무것도 안 재는 초록이
된다 — #92).

**면은 칠하기 전까지 존재하지 않는다**(48-9). 제도에서 면은 선이 둘러싼 빈 종이일 뿐이고,
투명 회색은 「여기 면이 있다」는 UI 표시지 그림이 아니다. 안 칠한 면은 **칠·면 도구를 든
동안만** 옅게 드러나고, **칠한 면은 불투명하며 뒤를 가린다**(가리는 기제는 renderOrder가
아니라 **깊이 버퍼** — 칠한 면의 재질만 `depthWrite`를 켠다. 안 칠한 면은 종전대로 아무것도
안 가린다: 작도를 계속해야 한다). ⚑ **갈래**: 지시는 「칠하는 도구」인데 **면 도구도** 넣었다 —
면이 안 보이면 「탭해서 없애기」가 눈먼 조작이 된다.

**타일 캐시에 칠을 «넣을» 수 없다**(48-6 · D-4 — 지시문의 처방이 기하적으로 안 된다).
타일은 한 획을 **누운 직선**으로 굽고 두 끝점으로 **아핀 변환**해 붙이며, 그 근거는
「확정 기하가 직선이므로 사영도 직선」이다. 칠은 **평면 위 점렬**이라 포즈 변환이
**호모그래피**이고 아핀으로 못 적는다 — 두 끝점으로 타일을 만들면 굴곡이 펴진 곧은 막대가
된다(조용히 틀린 그림 ⛔). 대신 **제스처 동안 칠만 살려서 그린다**(비용 실측 0.4ms 중앙).

**단색 채움은 «또 하나의 무늬»다**(48-3). `Face.fill`이 1(해칭)·2(단색)이 되고 손통 「채움」
줄이 없음→해칭→단색→없음으로 돈다 — 새 손잡이도 새 저장 축도 안 생긴다. 색은
`solidHexOf`(재료의 중간 톤 · 해칭 선이 마지막 톤을 쓰는 것과 짝). ⚑ **갈래**: 「면마다
임의의 색」은 **안 했다** — 임의 색은 칠의 것이고 면의 색은 `Face.mat`이 정한다(49의 물음).

**손잡이를 추가하는 라운드는 34-0 대조표를 다시 돌린다**(48-10 — 규칙으로 등재).
같은 병이 네 번째였다(R1이 지우개로 안 쓸렸고 · R6이 펜 촉으로 · #88이 검사 팔로 ·
이번엔 툴팁). **한 번 쓸고 끝나는 검사는 반드시 샌다.** 이번에 44~47의 손잡이를 전수로
돌렸고 **28개 중 19개에 지적이 나왔다**(문구 없음 9 · 뒤집힌 거동 8 · 선택자 밖 2). 고친
방식은 낱낱이 아니라 **규칙**이다(선택자에 `label`을 더했다 — 두 요소만 고치면 셋째가 샌다).
남은 지적 하나는 R6(접힌 칠통이 지금 색을 안 말한다) — `DEFERRED.md`.

**한 띠의 배열축은 하나다**(48-11 — 34-0에 **R9**로 등재). 44가 자와 손통을 눕힌 근거는
「왼쪽으로 자라도 안전하다」였는데 **안전한 것과 읽히는 것은 다른 물음이었다** — 무리에서
혼자 축을 트는 것은 「다른 무리」로 읽힌다. **배열은 무리를 말하는 채널이다.**

## D-W19 — 재료 표현: 벽돌은 그림이 아니라 숫자다 (web2-49 · 2026-09-02)

**45의 「해칭은 화면 고정」을 절반만 물린다** — 사용자가 갈랐다(지시 원문):
도면 해칭(단면 규격 기호 · ISO 128)은 상징이라 **화면 고정 그대로**이고,
**재료 표현**(입면·투시에서 그 면이 무슨 재료인가)은 **면 고정**이다 — 벽돌은 벽에
붙어 있고 원근을 받아야 한다.

1. **이미지가 아니라 절차다.** 축척이 이미 있으므로(D-W17 · `lift.scaleOf`) 실치수
   (벽돌 190×57·줄눈 10, 모듈 200×67 …)를 mm 좌표로 그대로 긋는다 — 이미지를 붙이면
   줌에서 안 맞는다. 사진 PBR ⛔(지시 「하지 말 것」 — 문법이 안 맞는다).
2. **필드를 가른다** — `Face.rep = { m, s }` ↔ `Face.mat`(46 해칭 기호). 둘은 다른 물건
   (기호 ↔ 치수 무늬)이고 합치면 정당한 조합(유리 기호 + 벽돌 표현)이 못 선다.
   46의 다섯(유리·금속 포함)과 49의 여섯은 **집합이 다르다** — 유리·금속의 재료 표현은
   치수 무늬가 아직 없다(커튼월 격자 등 — 필요가 오면 REP_IDS에 늘린다 · DEFERRED).
3. **쪽은 48-5의 규약 그대로**(`s` — `paintSideAt` 한 자리 #54). 무늬도 칠이다.
4. **축척 미정이면 안 그린다**(값은 저장·표시만 대기 — 47 스케일바·면적의 선례.
   A-3 «미배치는 실패가 아니라 대기 상태»).
5. **밀도 하한**(`repVisibleFamilies` · C.REP_MIN_PX 4px) — 작은 축척에서 재료 표현을
   생략하는 제도 관례. **부선은 주선 없이 못 선다**(계층 — 켜 없는 줄눈은 말뚝으로 읽힌다).
6. **원점·방향은 자동만**(⚑ — 지시가 멈추라 한 자리): up×n 수평 · 최소 모서리 위상.
   규칙이 유일·자연한 답을 못 내는 형태 2/13 — 방향 퇴화(슬라브)·위상 모호(밑변 없는 벽)
   (`rep49_web2.json` — ⚠ «수평 켜 어긋남 0» 류는 공식의 항등이라 근거로 안 쓴다 #5 ·
   1차 리뷰어 [2]). 조절 UI는 사람의 답을 기다린다(실기기 ⑳ · AS-C164).

기각: 사진 텍스처(문법) · 픽셀 굽기(원칙 b — 면의 함수여야 경계를 따라간다) ·
반사·범프·러프니스(광원이 없어 죽은 데이터 — glTF 내보내기 때 재론) ·
도면 해칭의 면 고정 전환(45 결정의 살아 있는 절반).

## D-W20 — 자국의 뿌리: 칠은 면 텍스처에 산다 (web2-50 · 2026-09-02)

**결정**: 칠의 정본을 «면 위 좌표(uv · 세계 단위) 획 목록»으로, 표시를 «면+쪽마다
텍스처 한 장(파생 — 굽는다)»으로 바꾼다. 텍스처는 면 메시(삼각분할 + uv 속성)에
**곱하기**(MultiplyBlending + premultipliedAlpha)로 입는다.

**왜**: 45~48의 칠(화면 겹 #brushc에 매 프레임 사영-재그리기)은 여섯 증상이 한 뿌리였다 —
흰 뜸(48-1 장막) · 블렌드 불가 · 회전 중 소실(48-6의 우회) · 원근 미적용 · 개구부 깨짐 ·
양면(48-5는 판정만 있고 표시가 겹에 있었다). 합성을 2D 캔버스 «안»으로 넣고 면을 3D에
두면 원근·깊이·회전·절단이 사영의 구성에서 나온다(3D 페인팅 도구들의 선례 — A-3).

**세부 결정 다섯**:
1. **uv 기저 = repBasis**(#54) — 무늬(49)와 같은 기저·같은 원점. 기저가 둘이면 무늬와
   칠이 다른 방향 규칙을 갖게 된다.
2. **uv 단위 = 세계**(mm 아님) — 축척 미정에도 칠한다(무늬는 실치수라 mm·축척 대기,
   칠은 잉크라 즉시 — 47 스케일바의 「축척 없으면 안 뜸」과 다른 자리다).
3. **칠 굵기 = 면 고정(세계 단위)** — 원칙 e(선폭 화면 고정)와의 «다름»의 결정.
   칠은 면 위 안료라 가까운 쪽이 굵어야 한다(증상 ④의 답). 환산은 그은 순간 한 번.
4. **옛 칠은 버리고 센다** — 사용자 확정(「잃어도 상관없다」). 마이그레이션 ⛔ ·
   여는 순간 한 줄 알림(43-1). 파서 재현 규칙: uv·쪽이 같이 서야 받는다.
5. **흰 바탕 + 곱** — 흰 텍셀이 항등이라 «안 칠한 자리는 아래를 안 바꾼다»가 합성식의
   구성이 된다. 48-1의 장막·DEFERRED «칠은 제 겹을 가져야 한다»가 이 형태로 닫혔다.

**기각한 것**: ㉠ p5.brush로 텍스처 굽기(라이브러리가 캔버스 하나를 전제 — 질은 51이
절차 생성으로 다시 세우므로 지금 옮길 값이 없다) ㉡ 텍스처 저장(파생이다 — 원칙 b)
㉢ 마이그레이션(지시 문면 ⛔) ㉣ 화면 고정 해칭의 이관(지시 문면 — 다른 물건).

**함정 둘(실측)**: three r185의 MultiplyBlending은 premultipliedAlpha 없이 **조용히
over로 그려진다**(blend 상태 미세팅 — NOTES 50의 그 절) · sortFaces의 «면 둘부터»
이르기 반환이 칠·무늬 층 차례를 죽였다(면 하나여도 층이 서야 한다).
