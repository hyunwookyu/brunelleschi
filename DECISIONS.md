# DECISIONS.md — 잠정 결정

계획서(`docs/line_plan.md`)를 따르되 **계획서에 없거나 계획서와 어긋나는** 판단을 여기 적는다.
근거와 **기각 사유**를 함께 적는다(CLAUDE.md A-3).

> **2026-08-15 L 전환에서 새로 시작했다.** D-S1~D-S24 · D-G1~D-G4는
> `docs/archive/DECISIONS_SG.md`에 있고 **대부분 무효**다(배치 계층이 폐기됐다).
> 살아남은 것은 아래 "이월"에 다시 적었다 — 아카이브를 판단 근거로 인용하지 않는다.

---

## L-A

### D-L1 — 문서 원장을 아카이브하고 새로 시작했다

`progress.md`·`DECISIONS.md`·`DEFERRED.md`·`assumptions.md`를 `docs/archive/*_SG.md`로 옮기고
유효 항목만 옮겨 적었다(계획서 §8.3).

**왜**: 축적된 D-S1~D-S24 · AS-1~AS-20의 대부분이 **배치 계층 전제 위에 있었고** 그 계층이
폐기됐다. 새 세션이 그것을 읽으면 이미 없는 기전에 대한 판단을 근거로 삼는다.
실제로 그 위험이 관측됐다 — G-A 세션이 "지지 1"을 D-S22("2D 증거로 구분 불가")로 설명하며
같은 완화를 네 번 다시 시도했다.

**기각**: 원장을 그대로 두고 무효 표시만 다는 것. 3286줄(progress) + 889줄(DECISIONS)이
컨텍스트에 들어오면 표시가 있어도 읽힌다. 파일을 옮기는 것이 유일하게 확실한 격리다.

**보존**: 삭제하지 않는다. 접근을 세 번 바꾼 이유가 그 안에 있고, 측정 산출물
(`stage0/out/*.json`)은 손대지 않는다.

### D-L2 — 소실점 지지 임계를 **각도로 적는다**(단위 변경이고, 고친 것은 아니다)

계획서 §5.1은 "산포 임계는 화면 대각 비율"이라 적었다. **그대로 하지 않았다.**
화면 대각 대비 픽셀 거리는 **먼 소실점에서 발산한다** — 각도 오차 θ에서 소실점까지의 수직거리는
`D·sin θ`이고 2점 투시의 소실점은 화면 몇 배 밖에 있는 것이 정상이다. 그 단위로는
어떤 임계도 전부 기각하거나 전부 통과한다. 그래서 **각도**(유계, 0~90°)로 적는다. 값은 2.5°.

**⚠ 이 항목의 초판은 틀렸다**(리뷰어가 짚었다). 초판은 "획 길이 대비(`vp_dist_ratio` 0.06)도
같은 이유로 발산하고, 잉크 노이즈에서 참 소실점의 부적합도 0.061이 임계 0.06을 넘어
검출이 통째로 실패했다 → 각도로 바꿔 고쳤다"고 적었다. 두 군데가 틀렸다.

1. **`vpMisfit`은 이미 각도량이다.** 코드가 재는 것은 중점에서 「먼 끝점→소실점」 직선까지의
   수직거리 ÷ 획 길이이고, 중점은 끝점에서 길이의 절반이므로
   `vpMisfit = sin φ / 2`이며 `φ = asin(2·misfit)`이다(수치로 확인:
   획 (0,0)–(100,0)·소실점 (1000,50)에서 0.02497 = sin φ/2). 상한이 0.5라 발산하지 않는다.
   그러므로 **부적합도 → 각도는 단조 재매개화이고 아무것도 고치지 않는다**(PITFALLS #24).
2. **자릿수도 반대다.** `misfit 0.06`은 **6.892°**이므로 채택한 2.5°는 실패한 임계보다
   **약 3배 엄격하다**(`vp_detect.json@34db7279`의 `unit_equivalence`).

**검출 실패를 실제로 고친 것은 D-L3(도달거리)과 D-L4(가중합·재배정)다.**
첫 실패에서 검출된 소실점 셋은 전부 **상자의 꼭짓점**이었다 — 임계의 문제가 아니었다.

_`axis.ts`의 `angle_max_deg` 주석은 `misfit = (D/L)·sin θ`라 적어 두었는데 그 유도도 같은
오해다. 기본값이 0(꺼짐)이고 그 경로는 L에서 폐기되므로 고치지 않고 적어 둔다._

### D-L3 — "선 3개가 한 점에서 만난다"에 **도달거리 조건**을 더했다

**상자의 꼭짓점이 소실점으로 잡힌다.** 꼭짓점에는 모서리 셋이 **정확히** 한 점에서 만나므로
§5.1의 문장을 완벽하게 만족하고, **노이즈가 있으면 참 소실점보다 더 잘 맞는다** —
꼭짓점의 산포는 끝점 오차 몇 px이고 소실점의 산포는 지렛대로 증폭된다.
실제로 잉크 노이즈를 넣은 첫 실행에서 검출된 "소실점" 셋이 전부 상자의 꼭짓점이었다.

계획서가 "**같은 방향 클러스터**의 선 3개"라 쓴 것이 빠진 조건이다. 방향을 직접 군집화하는
대신 **소실점이 획의 가까운 끝에서 획 길이의 1배 이상 밖에 있어야 한다**로 뒀다(`min_reach`) —
같은 방향으로 수렴하는 획들만 그 성질을 가지며 근·원 소실점에서 똑같이 작동한다(A-3).
반례를 `test/vp_detect.test.ts`에 박았다(`min_reach: 0`으로 내리면 꼭짓점이 실제로 잡힌다).

### D-L4 — 지지를 **개수가 아니라 가중합**으로 세고, 마지막에 다시 배정한다

개수로만 세면 **지평선 근처 모서리가 두 수평 소실점을 거의 다 지나므로**(둘 다 지평선 위에
있다) 엉뚱한 중간 지점이 양쪽 축의 획을 긁어모아 지지 6을 만든다. 참 소실점의 지지 4보다 많고,
그때 검출은 소실점을 **둘밖에** 못 찾았다. `Σ(1 − φ/tol)`로 세면 0°짜리 4개가 이긴다.

**탐욕적 제거의 순서 의존도 남는다** — 먼저 뽑힌 소실점이 경계선을 데려가면 두 번째 군집이
지지 3을 못 채운다. 그래서 전부 찾은 뒤 **최소 각도로 다시 가르고** 군집마다 최소제곱을
한 번 더 돌린다. 그것이 §5.1의 "카메라는 최소제곱으로 다시 푼다"의 자리다.

_"개수만 세면 반대로 간다"는 이 프로젝트에서 세 번째다(D-C6의 구제 경로 둘)._

### D-L5 — 최소제곱에 **길이² 가중을 넣는다** (초판을 뒤집었다)

**초판은 "가중을 넣지 않는다"였고 틀렸다.** 근거로 "가중 없음/`len`/`len²`에서 f 상대오차
중앙이 0.094로 전혀 안 움직이므로 f 오차의 출처는 추정기가 아니라 입력"이라 적었다.

리뷰어가 대안 설명을 짚었다 — **구도가 하나(직육면체 하나)면 12모서리의 화면 길이가
서로 비슷해 길이 가중이 균등 가중과 같아진다.** 그것을 직접 재고 구도를 다섯으로 늘려
다시 돌렸다(`vp_detect.json@34db7279`):

| 가중 | f 상대오차 중앙 | p90 |
|---|---|---|
| 균등 | 0.0921 | 0.4055 |
| `len` | 0.0860 | 0.4044 |
| **`len²`** | **0.0793** | **0.3942** |

화면 길이 최대/최소 비는 구도마다 **2.03 / 4.37 / 2.07 / 2.03 / 4.44**다 — 1.5 미만이 아니므로
가중은 항등이 아니고, 그래서 중앙값이 실제로 움직인다. **`len²`를 채택한다**(D-L8과 같은 근거:
짧은 획의 방향 분산이 `1/len²`에 비례하므로 그 역수가 가중이다).

**그리고 "f 오차는 알고리즘의 몫이 아니다"라는 결론은 철회한다.** 그것은 근거가 없었다.
_다만 L-A.4가 더 중요한 것을 밝혔다 — **형태를 무너뜨리는 지배항은 f 크기가 아니라 축 방향
오차(중앙 4.18°)다.** f를 10% 틀리게 줘도 형태 오차 중앙은 0.098 → 0.115뿐이다._

### D-L6 — **카메라 일괄 적합을 기각했다** (회전 3 + f 1)

L-A.4가 "카메라가 형태를 무너뜨린다"를 확인한 뒤, 소실점을 하나씩 맞추는 대신
**회전 3 + f 1을 획 전부로 한 번에** 맞춰 봤다(`web/src/s3d/cameraFit.ts`).
잔차는 소실점을 거치지 않는다 — 해석 평면 법선 `n`과 축 방향 `d`의 내적이며,
유한·무한원 소실점을 같은 식으로 다룬다. 최소화는 Nelder-Mead(결정론적).

**f가 나빠졌다**(`lift_gate.json@34db7279`의 `fitted_camera`):

| 구도 | 검출 f 오차 | 적합 후 |
|---|---|---|
| 3점 요35°·피치15° | 0.0762 | **0.0827** |
| 3점 요50°·피치8° | 0.1141 | **0.3829** |
| 2점 | 0.1864 | **0.3943** |
| 1점 | (설정값) | **2.0** (상한까지 달아났다) |

형태도 나빠졌다(기준 구도 조용히 틀림 0.641 → 0.655).
**기각한다.** 코드는 남기고 앱에 배선하지 않는다(측정 근거를 재현할 수 있어야 한다).
_추정: 목적함수가 f에 대해 거의 평탄한데(입사 관계의 사영 불변성과 같은 뿌리) 축 배정 오차
11%가 최소화를 끌고 간다. 확인하지 않았다._

### D-L7 — **국소화 가능성 조건**을 더했다 (`min_spread_mult`)

**화면 평행 축의 획들은 서로 나란하므로 노이즈만으로도 아주 먼 데서 만난다.** 그것을 유한
소실점으로 받으면 2점 투시가 3점으로 읽히고 `fFromThreeVps`가 엉뚱한 f를 낸다 —
실제로 2점 구도에서 수직 4획이 `[245, −9252]`에 모여 지지 4를 만들었다.

교점의 위치 불확실도는 `노이즈 / 방향 산포`에 비례하므로 **지지선 방향의 최대 각차**를
임계로 쓴다. `support_deg`의 1.5배(=3.75°).

| 배수 | 참 소실점이 유한인 다발을 살림 | 참 무한원 다발을 잘못 받음 |
|---|---|---|
| 0 | 221/221 = 1.00 | 67/67 = 1.00 |
| 1.0 | 208/221 = 0.94 | 45/67 = 0.67 |
| **1.5** | **199/221 = 0.90** | **33/67 = 0.49** |
| 2.0 | 190/221 = 0.86 | 22/67 = 0.33 |
| 3.0 | 171/221 = 0.77 | 15/67 = 0.22 |

⚠ **깨끗한 분리가 아니다.** 산포 분포가 겹친다(참 유한 최소 0.43° · 참 무한원 최대 17.1°).
_초판 주석은 "1.3~3.4° 대 4.2~54.5°로 갈린다"고 적었는데 그것은 **손으로 고른 4장면**의
값이었다. 전 모집단에서는 겹친다 — 고쳤다._
걸린 다발은 버리지 않고 `infinite`로 표시해 그 획들을 **`screen` 축**으로 보낸다(이론서 2.2).

### D-L8 — **`min_reach = 1.0`은 양방향으로 재고 정했다**

초판은 값의 근거가 한쪽뿐이었다(꼭짓점을 막는가 = 거짓 양성만). 리뷰어가 짚었고,
**소실점이 그림 안에 있는 구도(1점 투시)를 넣어 거짓 음성 방향도 쟀다**:

| `min_reach` | 먼 구도 3점 검출 | 먼 구도 오배정 | **1점 구도** 3점 검출 | 1점 오배정 |
|---|---|---|---|---|
| 0 | 46/96 | 245/852 | 11/24 | 29/199 |
| 0.5 | 49/96 | 114/828 | 9/24 | 9/183 |
| **1.0** | **50/96** | **113/826** | 8/24 | **8/180** |
| 2.0 | 39/96 | 113/782 | 8/24 | 8/180 |
| 4.0 | 19/96 | 85/648 | **0/24** | 6/146 |

0으로 내리면 꼭짓점 오검출로 오배정이 2배가 되고, 4로 올리면 **1점 구도의 참 소실점이
아예 죽는다** — 거짓 음성이 실재함을 확인했다. 1.0이 양쪽에서 최적이다.

## 이월 (S·G에서 살아남은 결정)

| # | 결정 | 근거 | 원래 번호 |
|---|---|---|---|
| D-C1 | **깊이 출처를 `Camera.fSource` 하나가 들고 화면에 나온다.** provenance를 획마다 나누지 않는다 | f 오차 10%가 앵커 체인 전체보다 크고 **전역 배율 편향**이다(이론서 16.4). 출처가 여럿이 아니라 하나이므로 자료구조를 늘릴 이유가 없다 | D-S5 |
| D-C2 | **애매하면 놓지 않는다. 다만 버리지 않는다** — 미배치는 화면에 남기고 지정 경로를 준다 | 조용히 틀린 배치가 미배치보다 나쁘다(A-3). "놓지 않는다"를 "버린다"로 읽으면 사용자가 그린 것이 사라진다 | D-S15 |
| D-C3 | **좌표 규약의 단일 출처는 `canvasFrame.ts`다.** dpr은 `setTransform`에서 한 번만 적용한다 | 같은 캔버스를 둘이 다른 규약으로 쓰다 iPad 좌표 버그가 났고 **dpr 1인 데스크톱·Playwright 어디서도 안 보였다**. `test/canvas_frame.test.ts`·`e2e/coords.spec.ts`가 dpr 1·2·3에서 잠근다 | S-10 후속 |
| D-C4 | **새 임계는 `web/test/constants.ts`에 넣는다** | 빠지면 STALE 탐지가 안 걸리고, 상수가 바뀌어도 그것에 의존한 측정이 낡은 줄 모른다. 같은 유형이 세 번 재발해서 만든 규칙 | CLAUDE.md §5 |
| D-C5 | **거름은 선택 뒤에 건다** | 거부권을 선택 **전에** 걸었더니 경쟁자가 죽어 상대 순위가 거저 통과했고 오배정이 79% → 90%로 **늘었다**. 반례가 `test/prefilter.test.ts`에 있다 | D-S21 / S-8b |
| D-C6 | **`retry_as_face` 같은 "구제 경로"는 구제의 질을 재고 켠다** | 되돌려진 사선 자체는 정확했으나 **제대로 배정된 것 143획을 함께 끌어들여 32%를 틀렸다**(기저 6%의 5배). 회수율만 보면 반대로 간다 | D-S9 / D-S19 |


## L-B에서 정한 것

| # | 결정 | 근거 |
|---|---|---|
| D-L9 | **three 카메라를 확정 투시 카메라와 같게 만드는 것은 `sceneCam.ts` 하나가 한다.** 주점이 화면 중심이 아닌 경우는 `setViewOffset`으로 푼다 | 단일 뷰포트(§10.3)의 전제다. 출처가 둘이면 창 크기 변경에서 갈라진다 — `viewport.resize()`가 `aspect = w/h`로 덮어쓰던 자리가 실제로 그것이었다. `projectionHook`으로 한 곳에 묶었다 |
| D-L10 | **`FREE_FOV_DEG`(자유 궤도 화각 45°)는 `test/constants.ts`에 넣지 않는다** | D-C4의 목적은 "**상수가 바뀌었는데 그것에 의존한 측정이 낡은 줄 모르는 것**"을 막는 것이다. 자유 궤도 화각은 **어느 하네스도 읽지 않으므로** 그 실패 방식이 성립하지 않는다. ⚠ **"해시가 바뀌어 기존 산출물이 STALE이 된다"는 근거로 쓰지 않는다** — L-A.2가 `VP_TOL`을 등록하며 20건을 STALE로 만들고 받아들였다(리뷰어 지적). 그 근거를 인정하면 어떤 상수도 등록할 수 없다. **하네스가 이 값을 읽게 되는 순간 넣는다** |
| D-L12 | **핸들 예산을 화면에 낸다**(축별 °/px와 px). 1px 미만이면 "손으로 맞출 수 없다"고 알린다 | 자동 판정이 원리적으로 불가능해 사용자의 눈이 유일한 판정 수단인데(§5.3) **눈은 자기 손이 얼마나 정밀해야 하는지를 못 본다.** L-A.7이 잰 값이 원장에만 있었다. 계산은 `vpSensitivity.ts` 하나가 하고 **앱과 하네스가 같이 쓴다**(#17) |
| D-L13 | **가이드는 선 자체를 잡아 옮기고, `소실점 추정`이 캔버스 끝까지 늘린다** | 라이노·SketchUp에서 선을 끄는 것과 같다(A-3). 늘리기는 **방향과 소실점을 안 바꾸고 지렛대만** 늘린다 — 예산이 길이에 거의 반비례하기 때문이다. ⚠ **이득은 중앙 1.33배뿐이다**(`guide_budget.json`): 늘린 뒤 길이는 그 방향의 캔버스 현이 상한이라 세로 가이드는 588px에서 막힌다 |
| D-L11 | **`chord_dev`(옛 `ink_gap`)에 임계를 걸지 않는다** — 측정이 아니다 | f를 ±20% 틀리게 줘도 **소수 넷째 자리까지 그대로**다(`stage_cam.json`의 `control_wrong_camera`, 5구도 전부). `seg_gap = 0`이므로 재투영 세그먼트는 언제나 대표 직선이고, 값을 정하는 것은 **잉크 모델의 활(`lf_bow`)과 획 길이**뿐이다. AS-L3이 "사용자에게 실제로 보이는 변화"라 적어 둔 것을 **파이프라인의 측정으로 읽으면 안 된다** |
| D-L14 | **스냅 반경은 `SNAP_TOL.radius_ratio = 0.05`**(하네스 960×672에서 58.6px · 앱 1280×675에서 **72px**). 선례(SketchUp/Rhino 10~15px)의 5~7배다 | 손 획의 겨냥 오차 자체가 크다 — 시작 잉크 점이 참 모서리의 상에서 어긋나는 거리가 중앙 **4.7 / 14.7 / 23.8px**(`endJitter` 0.01/0.03/0.05, `snap.json@34db7279`). ⚠ **사전 등록한 두 규칙이 둘 다 실패했다**(순이득 최대는 픽스처에 비용이 없어 계속 커지고, 틀림 ≤ 10%는 대상이 촘촘하면 해가 없다). 그래서 **비용을 측정에 넣었다** — `unintended_snap`(무관한 자리에서 시작하려 할 때 끼어드는 비율). 0.05 → 0.10은 맞음 **+27**에 끼어듦 **+918**이다. 규칙 C(포화점)도 절단 0.5%·1%에서 k3에 대해 0.05를 고른다(**2%면 0.035** — 그 사실도 적는다) |
| D-L15 | **한계를 정하는 것은 반경이 아니라 (겨냥 오차 / 대상 간격)의 비다** — 스냅으로 못 고치는 몫이 있다 | 격자 k3(꼭짓점 간격 중앙 21.3px)에서 **틀린 붙음이 반경과 무관하게 15076/38880에서 포화한다.** 전부 `endpoint`이고 **이웃 꼭짓점을 문 것**이다. 겨냥 오차 중앙 12.2px이 간격의 절반을 넘기 때문이다. 반경을 줄여도 늘려도 안 없어진다. AS-C2를 "겨냥 오차의 절대 크기" 문제에서 **비의 문제로** 다시 적었다 |
| D-L16 | **초안 지지선은 `len_x_sep`**(`len_i · len_j · sin(각차)` 최대)이고, **빈 축만 중립 위치로 채운다** | 소실점은 **두 선의 교점**이라 조건을 정하는 것이 길이만이 아니다 — `longest`의 가이드 쌍 각차는 **최소 0.03°**였고 그런 축은 무한원 스냅이 비운다. 지지선만 바꾸면 카메라 성립 **31/90 → 43/90**, 축 오차 중앙 12.35° → **9.05°**, f 0.3007 → **0.2432**(`draft_recover.json@34db7279`). `widest`가 확정률 1건 높지만 축 오차·f는 `len_x_sep`이 낫다. **채우기**는 `guides_lt_4` 21/90 때문이다 — 그 축은 손잡이가 아예 없어 **사용자가 끌 것이 없고** 조정 UI의 전제가 깨진다. 중립은 축 오차 **13.83°**로 초안보다 나쁘므로 **있는 축은 안 건드린다**. 합쳐서 `guide_budget`의 카메라 성립이 **31/90 → 58/90**이 됐다(상한은 72/90이다). ⚠ **출하 구성**(`len_x_sep_filled`)의 f 상대오차는 **0.3152로 기준선 0.3007보다 나쁘다** — 채운 축이 f 산출에 들어가기 때문이다(리뷰어 2회차 [9]). **'f가 나아졌다'는 지지선만 바꾼 팔의 성질이고 출하 구성에는 해당하지 않는다.** 채택 근거는 확정률과 축 오차이지 f가 아니다 |
| D-L17 | **무한원 스냅(`HOMOG_TOL.snap_deg` 3°)은 유지한다** — 초안 실패의 지배항이 아니었다 | 끄면 31/90 → 40/90이다(**9건**). 지지선만 바꾼 팔에서는 스냅된 축이 42 → **0**이 된다. ⚠ **출하 구성에서는 다시 58이다**(리뷰어 2회차 [10]) — 채우기가 넣는 중립 수직 둘이 **정확히 나란해서**(각차 0°) 무한원으로 판정되기 때문이고, **그것이 옳은 동작이다**(화면 평행 축에는 소실점이 없다, 이론서 2.2). 즉 스냅이 비우는 것은 검출 실패가 아니라 **채운 기본값**이다. 유지 근거는 원래대로다: 스냅은 **좁은 각차에서 조작이 불가능해지는 것**을 막는 장치다(각차 3°에서 0.39°/px = 0.5° 예산의 78%) |
| D-L18 | **`AXIS_TOL`과 `LIVE_TOL.axis_deg`를 이번에 바꾸지 않는다** — 다만 **조이는 방향이 유력하고 L-B.7 뒤에 다시 본다** | 45조합 전부에서 배치와 조용히 틀림이 **단조로 맞바뀐다**(`axis_live.json@34db7279`). 등록 규칙("조용히 틀림 ≤ 기준이면서 배치 최대")이 고르는 것은 기준보다 **+26/4320**이고 시드 폭 **31**(387~418, 실측) 안이다. ⚠ **그 규칙은 완화 방향만 볼 수 있다** — 반대로 보면 `vp0.03_bend0.18_amb2`가 배치 −237에 **축 오배정 −94 · cut 0.2 −8.3pp · cut 0.5 −39%**다. 이 프로젝트의 정책(A-3·§9.1: 미배치는 대기, 승격이 연쇄)에서는 **그쪽이 맞다.** 그러나 **회수 기전이 아직 없으므로**(L-B.7) 지금 바꾸면 검증되지 않은 전제 위에 서게 된다 |
| D-L19 | **축 고정은 `resolveLive` 안에 둔다.** 바깥에서 축을 덮지 않는다 | 미리보기와 확정이 **같은 함수**를 부르는 것이 §11 게이트의 "일치 0"을 **항등**으로 만드는 근거다(그래서 그 항목을 게이트에서 뺐다). 바깥에서 덮으면 그 항등이 깨지고, **깨진 것을 아무도 못 본다** — 둘이 다른 코드가 되는 순간 배선 테스트가 잡을 대상이 사라진다 |
| D-L20 | **승격은 "다시 푸는 것"이 아니라 "앵커가 생기는 것"이다** — 일괄 재풀이 경로를 쓰지 않는다 | 계획서 §9.1의 "§5.4의 일괄 풀이를 재사용한다"를 그대로 만들어 돌렸더니 **회수 0/1904**이다(`promote.json@34db7279`, 세 팔 전부). 대기 사유가 `축이 미분류다`(1116)와 `구조에 이어지지 않았다`(620)인데 **둘 다 획 하나의 성질**이라 문맥을 더 준다고 안 바뀐다. 앵커 경로는 **720/1904 = 37.8%**를 회수한다. ⚠ **회수된 것이 더 정확하지 않다**(cut 0.2에서 **392/720 = 0.544** 대 확정 **874/2416 = 0.362**. ⚠ 그 격차는 **전부 게이지 추정기**다 — 형태만 보면 시드 폭 안이라 결론이 없다, D-L22) — 개수의 기전이다. _초판은 게이지를 안 없앤 `segErr`로 43.6% 대 42.3%라 '같다'고 적었다 — **D-L22가 고쳤다.**_ ⚠ **D-L24가 이 결정의 범위를 좁혔다**: 일괄 기전은 반증된 적이 없고 막고 있던 것은 축 판정이다. "일괄 재풀이 경로를 쓰지 않는다"는 **지금 구현에서 실제로 회수하는 경로가 앵커라는 뜻**이다. `promote()`(일괄)는 **대조군으로 남긴다** — 지우면 다음 세션이 계획서 원문을 다시 시도한다 |
| D-L21 | **D-L18 기각.** 축 판정 임계를 안 바꾼다 | 등록 규칙(#26: (배치+회수)가 기준 이상 **이면서** 조용히 틀림이 낮을 것)에서 조이기의 (배치+회수)가 **2976 대 기준 3136**으로 작다 — 회수율이 두 팔에서 37.8% 대 37.2%로 거의 같아 차이를 못 메운다. ⚠ 조이기는 조용히 틀림을 **cut 0.2 0.4037 → 0.329**로 낮춘다(고정 게이지, D-L22. _옛 지표로는 0.426 → 0.385였다_). ⚠ **"규칙이 개수를 우선하도록 짜여 있어서"는 틀린 서술이고 고친다**(리뷰어 [10]) — 규칙은 **두 조건의 연언**이었다. 단조 맞바꿈 아래서 통과하려면 회수가 그 단조성을 깨야 했고 안 깼다. **충족 가능하되 좁은 규칙**이다. 품질 우선에서는 여전히 조이기가 이긴다. **규칙을 사후에 바꾸지 않는다**(#28) — 목적함수를 바꾸려면 사유가 측정 밖에서 와야 하고 그 표본이 없다 |
| D-L22 | **형태 오차의 정의와 게이지는 `web/test/metrics.ts` 하나가 정한다.** 한 번에 푸는 집합은 그 집합에서 배율을 적합하고, **승격 회수 획은 확정 집합의 배율을 고정으로 받는다** | `liftAll`이 깊이를 `gauge_height`로 정규화하므로 **절대 배율은 자유도**다(이론서 16.2·16.4). 그것을 안 없애고 참값과 빼면 그 차이는 형태 오차가 아니라 **게이지 차이**다 — 옛 `promote.segErr`가 게이지 1이었고, 같은 2416획이 `axis_live.json` **0.1222** 대 `promote.json` **0.1533**으로 갈렸다(리뷰어 L-B.7 [1]). ⚠ **회수 획만 따로 적합하면 안 된다** — 그러면 "이미 확정된 틀에 붙는다"는 성질이 지워진다. 고정 게이지로 다시 재니 회수의 조용히 틀림이 cut 0.2 **392/720 = 0.544** 대 확정 **874/2416 = 0.362**다. ⚠ **그 1.5배를 배치의 성질로 읽으면 안 된다**(리뷰어 [1]): 확정은 `fit`이고 회수는 `고정`이라 **추정기가 다르고**, 최소제곱 적합은 정의상 오차를 낮추므로 **편향의 방향이 정해져 있다**(#31). 같은 720획을 **자기 게이지**로 재면 **286/720 = 0.397**이고 그 배수는 **시드별로 0.865~1.150으로 1을 가로지른다** — **시드 폭 안이므로 형태 격차라는 결론은 없다**(#14). 고정 게이지 배수는 시드 여섯 전부에서 1.20~1.57이다. 반대 방향(확정을 회수의 게이지로)도 929/1979로 나빠지고, 같은 부분집합의 자기 게이지는 743/1979이라 **추정기 9.4pp · 부분집합 1.3pp**로 갈린다. 그러므로 정확한 문장은 **"승격이 더 내는 오차는 형태가 아니라 배율이다"**이다 — ⚠ 다만 **꼬리는 자기 게이지에서도 나쁘다**(p90 0.9144 대 0.4468 ≈ 2배). **cut 0.2 비율 · 중앙 · p90의 배수가 서로 달라 한 수로 적지 않는다**(#8) — 앱이 회수 획에 배율을 다시 안 맞추므로 **사용자가 보는 것은 고정 게이지 쪽**이다. ⚠ **구도로 가르면 1점 구도가 90/93 = 0.968이다**(리뷰어 [2]). 확정이 0이라 게이지가 안 서는 장면(기준 팔 14개)은 **형태 오차 분모에서 뺀다**(#11) |
| D-L23 | **정의 해시를 산출물에 넣는다**(`metric_defs: metricsSnapshot()`). 상수 스냅샷과 같은 자리, 다른 대상 | 상수 스냅샷은 **임계값**만 본다. L-B.7에서 상수는 같은 채 **식**이 갈렸고 STALE은 아무것도 안 짚었다. 해시는 **`metrics.ts`의 소스 텍스트**(주석·공백 제외)에서 낸다 — ⚠ 첫 판은 `Function.prototype.toString()`을 썼는데 **vitest(esbuild)와 Playwright의 TS 변환이 같은 코드에 다른 문자열을 내서** 두 러너의 산출물이 서로 STALE로 떴다(`0ef2a685` 대 `ead43fb8`). 그것은 **다시 돌려도 안 지워지는 거짓 양성**이다. 소스 파일은 러너와 무관하다. 반례는 `tests/test_selfcheck_stale_metrics.py` 8건 |
| D-L24 | **§9.1의 일괄 기전을 폐기하지 않는다** — 반증된 적이 없다 | 초판의 "회수 0"은 **미실행이었다**(PITFALLS #32). 대기 1904가 거름에서 전부 걸려 **솔버에 한 획도 안 닿았다**. 축 라벨을 주고 다시 돌리니 오라클 축에서 입력 1904 · 솔버가 놓은 것 **1154** · 채택 **1154(60.6%)**다. ⚠ **앵커 720과 직접 견주지 않는다**(리뷰어 [4]) — 그것은 실제 축 라벨이라 기전 차이와 축 정보 차이가 섞인다. **축을 맞춘 앵커 대조군은 804/1904**이고 일괄이 그보다 **1.44배** 많다(나머지 804 대 720이 축 정보의 몫이다). 동작점을 헐겁게 해도 **축을 맞춘 채로** 앵커는 **968**이라 1154에 못 미친다(2회차 [2]가 실 라벨 934를 대체했다 — 오라클 축에서는 `axis_deg`가 무효라 흔드는 것은 스냅 반경 하나다). ⚠ **그러나 일괄은 더 많이, 더 틀리게 놓는다**(#15·#16). **추정기를 맞춰**(셋 다 자기 게이지) 보면 일괄 **558/1154 = 0.484** · 축을 맞춘 앵커 **269/804 = 0.335** · 확정 **0.362**다 — 일괄만 나쁘다(1.34배는 시드 밴드 밖이다). ⚠ **축을 맞춘 앵커 0.335는 확정 0.362와 구분되지 않는다** — 비가 0.925이고 그것은 같은 세션이 "결론 없음"으로 판정한 시드 밴드 0.865~1.150 **한가운데**다(3회차 [5]). 초판의 '앵커도 확정보다 나쁘다'(고정 게이지 대 fit)도, 1회차 교정의 '앵커가 확정보다 낫다'도 **둘 다 못 적는다.** **§9.1이 살아난 것은 개수 기전으로서다.** 즉 막고 있던 것은 기전이 아니라 **축 판정**이고, 그것이 L-B.3(a)·L-B.4가 지배항으로 지목한 바로 그것이다. ⚠ **오라클은 상한이다** — 연쇄가 실제로 붙인 축 라벨로는 **24/966**뿐이다. D-L20의 "앵커가 생기는 것"은 **지금 구현에서 실제로 회수하는 경로**라는 뜻으로 좁혀 읽는다 |

## L-C에서 정한 것

| # | 결정 | 근거 |
|---|---|---|
| D-L25 | **비용비 λ = 3이고 판정 절단은 0.5다.** 두 값을 **사전 등록 목적함수**로 박는다: `점수 = (놓였고 맞은 것 − 3 × 조용히 틀린 것) / 전체`, 조용히 틀림은 **형태 오차 > 0.5**. 앵커와 확정이 겨루는 자리에서는 **확정(보수적) 쪽이 기본값**이다. ⚠ **근거는 정책 하나뿐이고 측정이 아니다** — 아래를 반드시 함께 읽는다 | **λ는 측정 밖에서 와야 하고**(DEFERRED, AS-C1: 표본 0) **사람이 정했다**(2026-08-16 지시). 근거: 이 도구에서 **미배치는 2D 레이어로 가고 승격 대기 상태가 된다**(§9.1) — 비용이 **유예**된다. **틀린 배치는 조용히 남고 그 위에 구조가 쌓인다** — 비용이 **영구**이고 **복리**다. 비용이 대칭이 아니다. A-3의 "애매하면 놓지 않는다 / 놓지 않되 버리지 않는다"가 같은 판단이다. ⚠⚠ **초판은 "절단 곡선이 그것을 수치로 확인했다"고 적었다 — 철회한다**(L-C.2 리뷰어 [2][3]). 그 문장이 인용한 칸(cut 0.5 · λ=3: 앵커 0.2688 대 확정 0.2713)의 **여유는 0.0025**이고 n = 3240에서 **8획어치**다. **시드별로 다시 재니 부호가 3 대 3으로 갈린다** (`axis_live.json@34db7279`의 `seed_spread_at_dl25_point`: 시드 1·3·4는 확정, 2·5·6은 앵커. 여유 폭 **0.2166**). **그 칸은 근거가 못 된다**(#14). ⚠ **절단 0.5도 측정이 고른 것이 아니다**(#13) — λ=3에서 확정이 이기는 것은 **cut 0.5~0.6뿐**이고 cut 0.4와 0.7·1.0에서는 앵커가 이긴다. 0.5는 `metrics.ts`의 **등록 절단 셋 {0.1, 0.2, 0.5} 중 큰 것**이고 그 이상의 근거는 없다. ⚠ 두 팔의 **추정기도 다르다**(확정=적합 게이지, 앵커=게이지 1)라 그 하네스의 `estimator_warning`이 "'확정이 낫다'는 결론은 할인해서 읽는다"고 미리 적어 두었다. **그러므로 D-L25는 정책 결정이고 측정 지지가 없다.** 그것이 이 결정의 성격이고, 그렇게 적는 것이 이 항목이 하는 일이다 — λ는 원래 측정 밖에서 오기로 되어 있었다. ⚠⚠ **그렇다면 그 정책의 사실 전제도 대조해야 한다**(L-C.2 리뷰어 2회차): 전제는 "미배치는 대기이므로 비용이 **유예**된다"이고 그것이 **AS-L11**인데, 그 항목의 현재 상태는 ⚠️ **"개수는 성립·연쇄는 수렴(픽스처 한정). 품질은 반증. 1점 구도에서 거의 전부 틀린다"**다. 즉 **유예는 절반만 참이다** — 회수율이 동작점 하나에서 37.8%(연쇄 포함 49.3%)이고 **1점 구도는 연쇄가 0**이며, 회수된 것의 **배율 어긋남이 사용자가 보는 오차로 남는다**. **그래도 λ ≥ 3을 유지한다**: 회수가 절반이어도 미배치의 기대 비용은 **틀린 배치보다 작다**(틀린 배치는 회수 확률이 0이고 그 위에 구조가 쌓인다). 다만 **1점 구도에서는 그 논증이 안 선다** — 연쇄가 0이면 유예가 아니라 **영구 미배치**다. **구도별 λ는 미측정이고 DEFERRED다.** ⚠ **#26 범위**: "사전 등록"은 **앞으로의 측정**에 대해서다. D-L25로 다시 채점한 것(D-L21·D-L26)은 전부 **이미 수집된 데이터**이므로 그 재채점은 사전 등록이 아니라 **사후 재해석**이고, 그래서 판정을 뒤집는 데 쓰지 않았다(둘 다 기존 판정을 유지했다). ⚠ **옛 등록 규칙**("조용히 틀림 ≤ 기준이면서 배치 최대")은 **완화 방향만 볼 수 있었다**(#31). D-L25가 그것을 **대체한다** — 새 규칙은 한 수라 양방향이다. **사후 변경이 아니다**(#28): 근거가 측정 밖(사람의 값 판단)에서 왔고 그 사실을 여기 적는다. **대체 범위는 둘이고 둘 다 다시 채점했다** — D-L18/D-L21(축 판정 임계, `DEFERRED`)과 L-C.1의 승격 규칙(D-L26) |
| D-L26 | **L-C.1의 판정("차수 승격을 자동으로 걸지 않는다")은 유지한다** — 다만 근거를 바꾼다. 옛 근거는 "배치가 −168로 줄었다"였고, 새 근거는 **D-L25 목적함수에서 결론이 안 선다**는 것이다 | D-L25가 옛 등록 규칙을 대체했으므로 **L-C.1의 규칙도 다시 채점해야 한다**(L-C.2 리뷰어 [4] — 초판은 D-L18만 다시 채점하고 이것을 빠뜨렸다). 다시 채점하려면 **한쪽만 놓인 획의 형태 오차**가 필요한데 초판 하네스가 `both_placed`에서만 쟀다. 넣어서 다시 재니(`order_promote.json@34db7279`의 `dl25_rescore`) 합산으로는 **승격이 이긴다** — 절단 0.5·λ=3에서 승격 전 **0.1443** 대 후 **0.1705**(여유 +0.0262, n = 2592에서 68획어치)이고 λ ≥ 2의 모든 절단에서 그렇다(λ=1에서만 진다). ⚠ **그런데 시드별로 보면 부호가 3 대 3으로 갈린다**(#14, D-L25가 같은 자리에서 걸린 것과 **같은 형태**다): 시드 2·4·6은 +0.0787/+0.1134/+0.0694, 시드 1·3·5는 −0.0463/−0.0254/−0.0324이고 **폭이 0.1597로 합산 여유의 6배**다. **그러므로 D-L25 목적함수는 승격에 대해 아무 말도 안 한다.** ⚠⚠ **초판은 여기서 "그러면 A-3이 남고 그것은 자동으로 안 거는 쪽"이라 적었는데 그것은 틀렸다** (L-C.2 리뷰어 2회차). 같은 원장에서 **승격은 모든 절단에서 조용히 틀림을 줄인다** (cut 0.1 628 → 485 · cut 0.2 379 → 308 · cut 0.5 187 → 128, 분모 2592 공통)이므로 A-3의 두 조항("애매하면 놓지 않는다" · "조용히 틀린 배치를 만들지 않는다")은 **둘 다 승격 쪽**을 가리킨다. **A-3은 이 판정을 지지하지 않는다.** 판정을 세우는 것은 **계획서 §6.2**다 — "사용자가 즉시 보고 되돌린다. 임계를 정교하게 만드는 것보다 싸다"이고, **무결론이면 자동화의 근거가 없으므로 현상유지(= 수동)**다. 그리고 승격은 **화면이 실제로 움직이는** 조작이라(§6.2) 근거 없이 자동으로 거는 것은 사용자가 안 시킨 큰 변화를 일으키는 것이다. ⚠ **양성 채널은 별개로 산다**(#30, 2회차 리뷰어): 같은 산식이 **가짜 승격**에는 절단 여섯 전부에서 벌을 매기고 등록 동작점에서 **−295획**이다(`dl25_rescore.spurious_control_rescore`) — 즉 **목적함수는 눈이 떠 있고**, 결론이 없는 것은 참 승격의 효과가 **시드 폭 안**이기 때문이다. ⚠ **판정은 같지만 근거가 달라졌다는 것을 적는다**(#28) — 옛 근거("배치가 준다")는 D-L25 아래에서 더 이상 기각 사유가 아니다. 배치와 틀림이 한 수로 합쳐졌기 때문이다. ⚠ **`only_*` 획은 짝이 없다**(#11) — 승격 전후가 다른 획 집합이라 이 점수는 짝지은 비교가 아니다. 분모(전체 2592획)만 같다 |
| D-L27 | **`PICK_TOL.radius_ratio = 0.0145`는 측정이 정하지 않았다 — 선례에서 온 잠정값이다** | SketchUp/Rhino의 픽 조리개가 10~15px이고 앱 1280×675에서 이 비가 **약 21px**이다. ⚠ **`SNAP_TOL`과 다르다**: 그쪽은 `unintended_snap`(무관한 자리에서 시작할 때 끼어드는 비율)으로 **측정이 정했고**(D-L14, 0.05 → 0.10이 맞음 +27에 끼어듦 +918) 근거가 원장에 있다. **고르기에는 그런 측정이 없다** — 커서로 짚는 것이라 손 획의 겨냥 오차(중앙 4.7~23.8px)가 안 들어오고, 대신 **촘촘한 기하에서 엉뚱한 획을 지우는 비용**이 있는데 그것을 안 쟀다. **측정 전에 박은 값이 아니라 선례를 따른 값이라고 적는다**(#26의 취지: 임계가 사후에 움직이지 않게 한다). 재려면 `unintended_pick`(고르려던 것과 다른 것이 잡히는 비율)이 필요하고 그것은 L-D.3의 자리다 |
| D-L28 | **§9.5에서 "면 대각선"의 지정 경로는 축이 아니다** — L-D.1이 축 지정을 안 올린 것은 결과적으로 맞았다. 다만 **면 지정은 아직 없다**(면 생성이 범위 밖이므로 §6과 함께 다시 본다). 지금 상태: 지우는 경로만 서 있고 지정 경로는 **없다** | **새로 재지 않고 기존 원장으로 결론했다**(2026-08-16 지시). `face_diagonal.json@34db7279`가 같은 픽스처에서 둘을 갈라 놨다 — **축으로 배정된 사선**은 15.83%이고 그렇게 놓인 것의 `corner_err_ratio`(끝점 거리 ÷ 상자 대각, **게이지 없음** — D-L22의 형태 오차와 다른 양이다) 중앙이 **0.4483**인데, **면 경로로 회수된 85획(끝점 170)의 중앙**은 **0.0216**이다(**20배**). 그 원장이 직접 `**축으로 배정된 사선이 놓인 것은 회수가 아니라 조용히 틀린 배치다**`라고 적었다. 같은 원장의 면 경로(**축 둘이 펴는 평면 + 앵커가 위치**)는 미분류 사선의 **84.16%**를 놓고 오차 중앙이 0.0216이다.
⚠ **결론을 사선으로 좁힌다**(리뷰어): 그 원장의 모집단은 **면 대각선 120개**뿐이고,
§9.5가 다루는 대기 획에는 **사선이 아니면서 축이 미분류라 대기 중인 것**이 더 많다
(`promote` 계열: 축 미분류 1116 · 구조 미연결 620). 그 집단에 대해 이 원장은 **아무 말도 안 한다** —
"축 지정 자체가 불필요한가"는 **미측정**이다.
⚠ **동작점 하나다**: 시드 9700 · `medium` · `end_jitter 0.01` 하나이고 **시드 폭을 안 쟀다**(#12·#14).
**두 수는 그 조건의 값이다.**
⚠⚠ **항등으로 확인됐다**(리뷰어 2회차, #3). `by_reason`은 `{no_axis_matches 96, ok 19,
multi_axis 4, ambiguous 1}`이고 합이 **120 = `n_diagonals`**다. `axis_assigned_rate 0.1583 ×
120 = 19 = axis_misplaced.n`. 즉 **`axis_misplaced`는 `by_reason.ok`의 다른 이름**이고,
"축으로 배정된 사선은 전부 오배정"은 측정이 아니라 **정의다**(사선에 축이 붙으면 그것이 곧 오배정).
**그러므로 20배의 성격이 바뀐다** — 두 집단(19획과 85획)은 **판정 대상인 그 분류기가 갈라 놓은
disjoint 부분집합**이고, 같은 획에 두 경로를 걸어 본 것이 **아니다**. 짝지은 비교가 아니므로
"면이 축보다 20배 낫다"고 읽으면 안 된다(#11: 분모가 다른 두 양).
⚠ **다만 선택 효과의 방향은 적어 둔다** — 19획은 분류기가 **가장 축답다고 고른** 사선이므로
축 경로의 **자기 최선 사례**다. 그 최선에서도 오차 중앙이 0.4483이다. 이것은
"면이 20배 낫다"보다 약하지만 **"축 경로는 자기가 고른 것에서도 크게 틀린다"**는 말은 된다.
**D-L28의 결론은 그 약한 형태로만 선다.** 짝지은 비교(같은 획에 두 경로)는 **미측정**이다.
그리고 그 19획은
**자동 판정이 틀리게 배정한 것**이지 사용자가 지정한 것이 아니다 — **사용자 지정의 대리값**이다(#2).
⚠ **#35의 오라클이 없다**: 이 원장은 §9.5 조건이 아니라 **자동 경로**의 것이다.
⚠⚠ **원장을 그대로 인용할 수 없다는 단서가 `DEFERRED.md`에만 있었다 — 여기 옮긴다**(리뷰어 [5]).
그 측정은 **폐기된 배치 계층**(D-S14·S-6·S-8)의 것이고 **AS-5는 지금 "무효"로 분류돼 있다**
(`assumptions.md`). D-L1이 "아카이브를 판단 근거로 인용하지 않는다"고 적었으므로,
이 결정은 **무효 표시된 가정 위의 원장을 근거로 쓴 것**이고 그 사실이 결론의 등급을 정한다 —
§6(면)을 열 때 **그 조건에서 다시 재야** 한다. ⚠ 상수 해시가 현재값(`34db7279`)이라 **STALE로는 안 잡힌다** — 알고리즘·가정 변경은 기계가 못 본다. ⚠ 이 문장이 **맨 해시**라 `scan_citation_hashes`도 못 봤다(리뷰어 [11], #38이 경고한 이름 기반 사각지대). 상수 해시를 문서에 적을 때는 **원장 인용 형식**(`파일.json@해시`)을 쓰거나 여기처럼 사람이 갱신한다. — PITFALLS 표의 "알고리즘 변경은 사람이 본다"가 이 사각지대다.
⚠ **"0/2는 결함"이라는 L-D.1의 읽기를 철회한다** — 축 지정이 0/2로 안 올라간 것은 결함이 아니라 **옳은 거절**일 수 있다. **근거는 위의 약한 형태다**: 축 지정은 방향만 주고 위치를 안 주며, 축 경로는 **자기가 가장 축답다고 고른 사선에서도** 오차 중앙 0.4483을 낸다. (초판이 쓴 "20배"는 짝지은 비교가 아니므로 이 자리에서 빠진다.) ⚠ **면 지정을 지금 넣지 않는다**: 면 경로는 **평면(축 둘) + 앵커**를 요구하는데 L에는 면 개념이 없고(§6 범위 밖) 스냅이 앵커를 대신하는 구조다. 넣으려면 §6과 함께 봐야 하므로 `DEFERRED.md`로 간다. ⚠ **개수만 세지 않는다**(#9·#15): 면 경로를 붙이면 회수가 늘 것처럼 보이지만 그 원장은 **회수와 조용히 틀림을 갈라 세야** 한다고 적었다 |
| D-L29 | **저장 v2는 v1 문서를 열지 않는다 — 이전(migration) 경로를 안 짠다** | L-D.2. v1에는 **뷰가 없다**. `viewRef`를 만들어 내려면 "이 획이 어느 시점의 것인가"를 지어내야 하는데 그 정보가 문서에 없다 — **없는 소유 관계를 지어내는 것**이고 A-3("애매하면 놓지 않는다")에 어긋난다. 대신 **키를 갈랐다**(`current` 대 `current2`) — 같은 DB에서 서로 덮지 않으므로 v1 문서는 **남아 있고 옛 UI로 열린다**(#34: 같은 조건을 쓰는 다른 호출부를 함께 봤다). ⚠ **귀결을 적는다**: **옛 UI(`index.html`)로 그린 저장은 새 UI(`l.html`)에서 안 뜬다.** 옛 UI를 지울 때(L-D.3 통과 후) 그 문서들이 **열 수단을 잃는다** — 지우기 전에 `JSON` 내보내기로 옮기는 절차를 README에 적어야 한다. 원장: `doc_store.json@34db7279`의 `v1_rejected` |
| D-L30 | **내보내기는 3D만 낸다 — 2D 레이어는 안 나간다** | L-D.2. 대기 획(§9.1)은 **좌표가 없다** — 화면 좌표만 있고 3D가 없다. OBJ·glTF에 넣으려면 임의의 깊이를 정해야 하고 그것이 조용히 틀린 배치를 파일로 내보내는 것이다. **대신 뷰 이름을 주석·노드 이름에 남긴다**(`view=확정 뷰` · `s1@확정 뷰`) — 어느 시점에서 그린 것인지가 후속 작업에 필요하다. ⚠ **`skippedUnplaced`를 세어 낸다**(#7: 추측 대신 카운터) — 몇 획이 빠졌는지 사용자가 안다 |
| D-L31 | **`Stroke.layer`는 지우지 않고 그대로 둔다 — 아직 아무도 안 쓴다** | selfcheck가 **쓰기 0 · 읽기 0**으로 잡고 있고(#18) 저장 v2도 안 담는다. 지우는 것이 #18의 방향처럼 보이지만, 이 필드는 **`QUESTIONS.md` (b)의 자리**다 — "승격 대상이 아닌 획"(해칭·지시선·치수선·메모)을 가르는 상태가 필요하고, 그때 `layer`가 `pending`/`placed`/`surface`/`view`를 담는다. **지금 지우면 그 결정이 왔을 때 CLAUDE.md §1의 자료구조를 다시 고쳐야 한다.** ⚠ **그러므로 selfcheck의 플래그는 유지한다** — 끄지 않는다(#19: 설명이 검사를 끄면 안 된다). 플래그가 남아 있는 것이 "아직 안 정해졌다"의 표시다 |
| D-L32 | **롤 0 제약(지평선 수평 고정)을 앱에 배선하지 않는다** — 코드는 남긴다 | L-D.0(사람 지시). 이론은 맞다: 이론서 3.1(수평 방향의 소실점은 지평선 위) + 롤 0이면 그 직선이 화면 수평이고, §5.3의 자세 자유도가 3 → 2가 된다. 수심 조건(6.3)으로 세 번째 소실점이 `V₃ = (Pₓ, h − (Pₓ−x₁)(Pₓ−x₂)/(P_y−h))`로 **직선 하나가 아니라 점 하나로** 정해지고, 참 카메라에서 **오차 0으로 성립한다**(`horizon.json@34db7279`의 `orthocenter_identity`). ⚠ **그러나 그것은 항등이고 정보량이 0이다**(#5). **게이트는 실패했다**(⚠ 등록 임계 ≈1°는 **정정 전 값**이고 현행은 0.5°다 — 사후에 안 고친다(#26·#28). 방향이 한쪽이라 결론은 안 바뀐다): L-A.6과 같은 조건에서 축 방향 오차 중앙이 7.4048 → **6.1460**(축당 4획)으로 **17% 줄 뿐**이고 임계 ≈1°의 **6배**다. 오라클 축 라벨을 줘도 **4.4599°**로 4.5배다. 꼬리는 거의 안 움직인다(p90 35 → 36, 오라클 12.5, #8). ⚠⚠ **그리고 배치가 붕괴한다** — 놓인 획이 2049 → 792(축당 4획), 6035 → 522(18획), 2045 → 9(48획), 692 → **0**(100획)이고 조용히 틀림도 오른다. **붕괴가 사영의 성질인지 제약의 성질인지 갈랐다**(#32): 사후 사영은 소실점을 자기 지지선에서 떼어 놓으므로, 제약을 **적합 안에** 넣은 팔(`fitHorizonPair` — 미지수 `(x₁,x₂,h)`를 길이² 가중 최소제곱 한 번으로 푼다)을 함께 냈다. **안 살아난다**(848 / 730 / 3 / 0). 소실점 이동량 중앙이 **78~98px**이고, 즉 검출이 낸 두 "수평" 소실점의 y가 실제로 그만큼 어긋나 있다 — ⚠⚠ **초판은 여기서 "붕괴는 제약의 성질이다"라고 적었고 철회한다**(리뷰어 [4][5]). **위약 대조군**(소실점을 같은 크기로 **임의 방향** 이동)이 똑같이 붕괴한다 — 832 / 802 / 14 / 0. 그러므로 붕괴는 **롤 0의 성질이 아니라 검출 소실점을 그 지지선에서 떼어 놓는 것 자체**의 성질이고 **방향과 무관하다.** 옳은 대조군은 적합 팔이 아니라 **위약 팔**이었다. ⚠ 그래도 함의는 남는다 — 롤 0을 걸려면 **반드시 그만큼 옮겨야** 하므로 **검출 해를 사후 교정하는 어떤 방법도 이 대가를 낸다.** ⚠ Δy는 **24~53px**이다(78~98은 적합 팔의 **2D** 이동량이고 Δx를 포함한다 — 성분 혼동을 고쳤다). ⚠ **무한원 축 구도가 원인이 아니다**: 수직축 합성을 끈 팔(`vertical_synthesized` 0)의 배치가 사영 팔과 **완전히 같고**(792/522/9/0), 구도별로는 **3점 구도가 가장 심하다**(424 → 56). ⚠ **`HORIZON_TOL` 스윕은 공허하다**(#31): 아홉 격자점이 같은 수를 낸다 — `applyRollZero`가 `min_slope_deg`를 아예 안 부른다. **그 두 임계는 결론에 안 닿는다.** ⚠ **양성 채널**(#30): 참 축도 같은 각으로 돌려 기준계를 맞추면 손익분기가 **롤 5° 근처**이고 10°에서는 제약이 해를 끼친다(4.47 / 5.06 / 5.93 / **10.01**° 대 기준 6.0933° 고정). **초판은 참 축을 안 돌려 이 대조군이 무효였다** — `roll_zero` 팔이 롤을 되감아 "기울여도 좋아진다"로 읽혔고, 재던 것이 품질이 아니라 **기준계 불일치**였다(#35의 ⚠⚠ · #37). **그러므로 자동 확정 경로를 되살리지 않고 사용자 확정을 유지한다**(§5.4). `horizon.ts`는 D-L6(카메라 일괄 적합)과 같은 처리다 — 측정 근거를 재현할 수 있어야 하므로 코드는 남기고 경로는 안 연다. ⚠ **"초안 품질이 올랐다"고 적지 않는다**: 축 방향 오차는 초안의 지표가 아니다(초안이 하는 일은 **끌기 시작 위치**이고 그 지표는 `draft_recover`의 확정률·축 오차다, D-L16·D-L17). 그 하네스로 안 쟀으므로 적으면 #2다. `DEFERRED.md`에 트리거를 달았다 |
| D-L33 | **MIT로 낸다**(`LICENSE`). D-S24를 **다시 확인해 옮겨 적는다** | ⚠ 옛 근거는 아카이브(D-S24)에 있고 **D-L1이 아카이브를 판단 근거로 인용하지 말라고 했다** — 그래서 여기 다시 세운다. **① 참조 저장소 코드를 한 줄도 안 옮겼다**: `notebysebas/sketchbook-v8`은 **LICENSE가 없고 소스에 저작권 표시도 없다**(= 모든 권리 유보). GitHub 약관(D.5)이 열람·포크까지는 허용하지만 수정·재배포·제품 편입은 아니다(CLAUDE.md §3). 필요한 것(튜브 메쉬·속도 기반 굵기·실행 취소·PWA)은 **전부 표준 구현이라 직접 짰다.** **② 의존성이 전부 허용적이다**: three.js(MIT) · Vite(MIT) · TypeScript(Apache-2.0) · vitest(MIT) · Playwright(Apache-2.0). **③ 이 도구의 가치는 코드가 아니라 측정 원장에 있다** — `stage0/out/`의 40개 원장과 그 판정 과정이 재현 가능한 것이 이 저장소의 내용이고, 그것을 닫아 둘 이유가 없다. ⚠ **Quick,Draw 데이터는 별개다** — Google의 CC BY 4.0이고 `sessions/`에 들어올 실획도 **제공자의 것**이다. 코드 라이선스가 데이터에 미치지 않는다는 것을 `README` 0절에 적었다 |
| D-L34 | **자체 저장 확장자는 `.brnl`이고 내용은 JSON 그대로다** | 이름이 정해지며 함께 정했다(사람 지시). **확장자를 따로 두는 이유**: OBJ·glTF는 **3D만** 내보내므로(D-L30) 뷰와 2D 레이어를 담는 유일한 형식이 이것이고, `.json`으로 두면 아무 JSON이나 열리는 것처럼 보인다(`isDoc2`가 거절한다, D-L29). **내용을 JSON으로 두는 이유**: 텍스트 편집기로 열리고 `git diff`가 되고 디버깅이 쉽다 — **이 저장소의 원장이 전부 그런 이유로 JSON이다.** MIME도 `application/json`으로 낸다 |
| D-L35 | **`wiring.test.ts`를 지우지 않고 검사 목적을 뒤집었다** — "앱이 옵션을 직접 안 적는다" → "**새 UI가 폐기된 배치 계층으로 안 돌아간다**" | 원 검사의 대상이 사라졌다(새 UI는 `settle` 계층을 안 쓴다). PITFALLS가 "대상이 사라진 항목은 지우지 말고 그렇게 표시한다"이므로 방향을 뒤집었다. **공허해지지 않게 짝을 달았다**(#32): `settle(` 이 없다 + `liftAll`·`placeLive`·`solveInto`가 **실제로 있다**. ⚠⚠ **초판은 "원래 보호가 `stage.spec`으로 옮겨졌다"고 적었고 철회했다**(리뷰어) — `stage.spec`은 앱 함수를 부르지만 **참값 대조를 안 한다**(그 원장이 스스로 "측정 경로와 앱 경로를 섞는 일"이라 적는다). **그러므로 지금 그 확인은 없다**: L의 배치 옵션을 하네스와 앱이 같은 출처에서 받는지 아무도 안 본다. `DEFERRED.md`에 트리거를 달았다 |
| D-L36 | **오프라인 양성 채널을 팔 둘에서 하나로 줄이고 기존 읽기를 철회했다** | 팔 A(사전 캐시에서 `.js`를 뺀 **별도 워커**)는 교락됐다 — **앱이 페이지마다 자기 서비스 워커를 다시 등록**하므로 같은 scope에 워커 둘이 얹힌다(캐시 이름 둘의 공존으로 확인. ⚠ 어느 쪽이 제어했는지는 **안 봤다** — "경쟁"은 추론이다). 빌드가 바뀌자 결과가 뒤집혔고, **교락 가능성만으로 그 팔의 결론은 못 쓴다.** 거기서 읽은 "③의 통과는 사전 캐시 목록의 효력이 아니다"를 **철회한다.** 남은 팔 하나(앱의 워커를 두고 **캐시의 `.js`만 지운다**)는 결정론적이고 **문서는 살고 앱은 죽는다**(`doc` true · `app` false) — 그것이 ③이 배제하는 것이다. ⚠ **사전 캐시 목록만의 몫은 이 설계로 안 갈린다**(앱의 등록을 못 끈다) — **미측정**이고 `what_this_does_not_say`에 올렸다 |
| D-L37 | **소실점 확정을 규칙 기반으로 전면 교체한다** — 검출 초안·가이드·민감도 경로를 뺀다 | 2026-08-16 사람 지시. **막고 있던 것은 정확도가 아니라 입구였다**: 검출이 초안을 못 내면 가이드가 안 생기고, 가이드가 없으면 카메라가 없고, 카메라가 없으면 **무엇을 그려도 대기**다. 새 규칙은 추정을 안 한다 — ⓐ 화면 가로·세로 선은 **축 자체**(무한원, 이론서 2.2) ⓑ 깊이선 둘의 교점이 첫 소실점이고 **그것이 지평선을 정의한다** ⓒ 다른 방향 깊이선 **하나**와 지평선의 교점이 두 번째다(셋이 아니다 — 지평선이 자유도 하나를 이미 먹었다, 이론서 3.1) ⓓ 세 번째는 수심 조건으로 **유도**한다(6.3). **유일한 판단은 깊이인가 화면 가로세로인가**이고 임계 둘(4°·8°) 사이는 **애매**로 두어 **묻는다**. 지운 것: 방향 클러스터링·3선 교점 산포 임계·검출 초안 생성(전부 추정 경로). ⚠ **측정이 지시문을 하나 정정했다**(#23): "오차의 출처가 그은 선의 정확도뿐"이 아니라 **두 선의 각차**가 함께 정한다 — 나란한 두 선의 교점은 조건수가 죽으므로 `min_sep_deg` 3° 아래에서는 소실점을 안 만들고 기다린다. ⚠⚠ **축 오차는 4~10° 대역 안에서 자리를 바꾼 것뿐이다**(`rule_camera.json`: 규칙 6.93° 대 검출 9.17°, 같은 픽스처). **임계 0.5°에는 어느 팔도 못 간다** — 자동 확정이 없다는 사실(AS-L10)은 안 바뀌었다. ⚠⚠⚠ **순서가 지배한다**: 축별로 몰아 그으면 6.93°, 번갈아 그으면 **32.57°**다. 규칙 ⓑ가 "**처음** 두 깊이선"이라 그 둘이 다른 축이면 엉뚱한 점이 나온다 |
| D-L38 | **`horizon.ts`를 앞 순서로 다시 쓰고 배선한다.** 폐기된 사후 사영은 `horizonRollZero.ts`로 옮긴다 | 2026-08-16 사람 지시. D-L32가 게이트에 실패한 이유는 제약이 틀려서가 아니라 **순서가 반대**였기 때문이다 — 검출된 두 소실점에 수평을 **사후로 강제**하면 소실점이 자기 지지선에서 떨어지고 `liftAll`이 전부 거절한다(배치 2049 → 792). 바른 순서는 **첫 소실점이 지평선을 정의하고 두 번째가 거기 놓이는 것**이고, 그러면 사영이 아예 없다(두 번째 소실점도 자기 지지선 위에 있다 — 그 선과 지평선의 교점이므로). **`applyRollZero`·`fitHorizonPair`는 지우지 않았다** — `horizon.test.ts`가 D-L32의 판정 근거를 계속 재현한다(CLAUDE.md의 폐기 코드 규칙). ⚠ D-L32의 결론은 **철회하지 않는다**: 그것은 "사후 사영을 배선하지 않는다"이고 지금 배선한 것은 **다른 경로**다 |
| D-L39 | **1점 투시의 f를 기본 렌즈 35mm 환산으로 채운다**(설정이지 측정이 아니다) | 소실점 하나면 자유도 1(f)이 남는다(이론서 5.3). 지금까지 `lensF`를 **아무도 안 세웠고** 그래서 `recoverCamera`가 `ok:false`를 내고 `ctx()`가 `null`이 되어 **1점 투시에서는 무엇을 그려도 대기**였다 — 규칙 ⓑ가 "1점 투시 확정"이라고 하려면 이 자리가 채워져 있어야 한다. **출처를 화면에 낸다**(`fSource = "setting(렌즈)"`, CLAUDE.md §1). 슬라이더는 **소실점이 하나일 때만** 뜬다 — 2·3점에서는 측정이 f를 정하고 설정이 이기면 안 된다 |
| D-L40 | **무한원 축의 3D 방향을 `PlaceCtx.axisDirs`로 넘긴다** | 화면 평행 축은 소실점이 없어 `vps[i] = null`인데 **방향은 정해져 있다**(가로 `(1,0,0)` · 세로 `(0,1,0)`, 이론서 2.2). 그것을 넘길 자리가 없어서 **1점 투시의 가로·세로 획이 못 올라가고 있었다**. `axis: "screen"`으로 두는 길도 있었지만 그러면 방향이 **그 획의 대표 방향**이 되어 같은 축의 두 획이 서로 안 나란해진다. 규칙을 안 쓰는 옛 하네스에서는 `undefined`라 **동작이 같다** |
| D-L41 | **획을 렌더하는 자리 셋을 직선으로 바꾼다.** `pts2d`·`pts3d`는 보존한다 | 2026-08-16 사람 지시. 계획서 §1.1이 "획이 직선 세그먼트가 되고 손떨림은 버린다"인데 **렌더 층이 그 결정을 안 따르고 있었다** — 화면에 손 획이 그대로 보였다. 고친 자리: `inkCanvas`(진행 중 · 확정) · `mainL.drawPending`·승격 손실 표시 · `strokeView` 튜브 중심선(양 끝 둘). ⚠ **3D 레이어는 이미 직선이었다**(`stage.ts`가 `LineSegments`를 쓴다) — 보이던 것은 **2D 잉크 층**이다. ⚠ 진행 중 미리보기는 **증분 그리기를 못 쓴다**(커서가 움직이면 앞 직선을 지워야 한다) → 전체 재그리기로 바꿨다. `pts2d`는 계속 보존하므로 프리핸드로 돌아갈 때 이 네 자리만 되돌리면 된다 |
