# DECISIONS.md — 잠정 결정과 근거

W 전환(2026-08-14) 이후 **사람에게 묻지 않고** 내린 판단을 근거와 함께 남긴다(A-3).
프로토타입 완주(W-8) 후 한 번에 검토한다. 기각한 선택지도 적는다 — 나중에 되돌리려면
무엇을 버렸는지 알아야 한다.

---

## W-A. 전환 정리

### D-A1. 폐기 범위를 계획서 §2.3 표 전체로 잡았다

지시문은 8개를 열거했고(1-bis 불확실성 전파, 사전분포 채움, 도메인 상수 테이블,
2D 각도 축 클러스터링, footprint 압출, 매스/보이드 분류기, 다중 뷰 정합, axisToggle),
계획서 §2.3 표는 여기에 **8.8 시거리 게이트**와 **Track 2 2F/2H 성능 판정**을 더한다.

지시문이 "wireframe_plan.md §2.3의 폐기 항목"을 가리키므로 표를 기준으로 삼았다.
실질적으로도 갈라지지 않는다 — Track 2 판정 모듈은 전부 축 클러스터링·footprint·
불확실성 전파에 의존해 그 셋을 지우면 함께 죽는다.

**기각**: 8개만 지우고 Track 2 모듈을 남기는 안. 반쯤 도는 좀비 코드가 남는다.

### D-A2. 평면 파이프라인 전체(stage1~stage6, ir/, score.py)를 제거했다

축 클러스터링과 footprint 압출이 그 파이프라인의 바닥이다. 둘을 빼면 stage1이 서지 않고,
stage2(발화 앵커)·stage4(다중화)·stage5(관계)·stage6(질의)는 전부 IR 볼륨 위에 서 있는데
IR은 §5.1에서 Vertex/Edge/Face/Camera로 대체된다.

**보존**: 측정 산출물 37건을 `stage0/out/archive_pre_W/`로 옮겼다. 접근을 바꾼 근거가
그 안에 있으므로 지우지 않는다. progress.md·reports/도 그대로 둔다.

**기각**: stage2(발화)를 남기는 안. 발화는 이번 프로토타입 범위 밖이고(DEFERRED),
남겨도 IR 없이는 붙일 데가 없다.

### D-A3. 렌더 계층(threeAdapter/sceneDiff/viewer)도 함께 제거했다

계획서 §2.1은 "Three.js 씬 + sceneDiff"를 유지 항목으로 적었으나, 실제 코드는
`MeshSpec{footprint, base, height}` — **압출 전용 자료구조**에 묶여 있었다.
`threeAdapter`는 90줄 중 대부분이 `ExtrudeGeometry` 처리다. 와이어프레임(엣지·면)을
그리려면 어차피 다시 쓴다. 유지 판단의 실질은 "코드"가 아니라 "검증된 지식"(증분 diff가
전체 재생성과 같은 결과를 낸다, 렌더 몫이 지연 예산 안에 든다)이고 그것은 문서에 남았다.

**대가**: W-4에서 THREE 보일러플레이트를 다시 쓴다. W-8에서 지연을 재측정해야 한다.

### D-A4. 8.7/8.8 시거리 함수는 코드에 남긴다

§2.3은 "8.8 시거리 게이트(형태 가정 역산)"를 폐기하되 **화각 경고는 유지**한다고 적는다.
`viewdist.py`에서 화각 판정(`gate`, 18.4)과 시거리 산출(`d_from_trapezoid`, `d_from_ellipse`)이
한 모듈에 있고 `evaluate_quad`가 둘을 함께 쓴다. 함수를 지우면 화각 경로가 깨진다.
→ **모듈 유지, 게이트로서의 사용만 중단.** W-1은 `gate`만 호출한다.

### D-A5. W-0 측정을 TypeScript로 구현한다

획→엣지 매핑은 harness가 아니라 **앱에 들어갈 코드**다(§6.2 처리 = 덧그림 병합·지나침 절단·
분절). Python으로 재면 같은 것을 두 번 구현하게 되고, 두 구현이 갈라지면 측정이 앱을
대표하지 않는다. W-1 이후가 전부 TS이므로 TS로 짠다.

**데이터**: 저장소의 실획 코퍼스는 Quick,Draw raw ndjson뿐이다. TU-Berlin은 래스터 PNG라
획이 없고, 세션 JSON은 집계본만 있고 원본 획이 없다. 로더를 `web/src/data/quickdraw.ts`로
꺼내 보존했다.

---

## W-0. 획 → 엣지 매핑

### D-01. 중단 조건의 **대상을 바꾸고** 발동하지 않는다

**가장 중요한 판단이다.** A-2는 유일한 중단 조건을 이렇게 정한다:
"W-0에서 획→엣지 매핑 비율이 0.5 미만일 때. 이 방식 전체의 전제가 무너지므로 보고하고 멈춘다."

#### 1차 판단과 그 결함

처음에는 "1획=1엣지가 0.415지만 그 지표가 능력에 반비례하므로 발동하지 않는다"고 적었다.
리뷰어가 세 가지를 짚었고 **전부 맞았다**.

- 반비례의 근거가 **두 점뿐**이었고 그중 하나(45°/σ3.0)는 스윕 격자 밖이었다.
- 대체 근거로 든 **덮임률도 반대 방향으로 같은 결함**이 있다 — 과민한 검출기(임계 10°,
  없는 코너 0.05)가 채택안보다 높은 덮임률을 받는다.
- "지표가 교란돼 있다"에서 "전제가 성립한다"로 건너뛰었고, 결과적으로 **중단 조건이
  대체물 없이 사라졌다**.

덮임률은 그 뒤 분모를 범위 안으로 제한하자 **0.974로 거의 자명해졌다**(범위 안 = 모든 런이
직선인 획이므로 정의상 1에 가깝다). 곡선을 넣으면 오염되고 빼면 자명하다 — **어느 쪽으로도
쓸 수 없다.** 근거에서 뺐다.

#### 2차 — 양방향 지표를 만들었다

한쪽으로만 속지 않으려면 **잘게 쪼개는 것과 놓치는 것을 동시에 벌해야** 한다.
검출기가 낸 엣지 수를, **같은 허용오차를 지키는 데 필요한 최소 엣지 수**(DP로 계산)와 비교한다.
쪼개면 detected가 늘고, 놓치면 조각이 허용오차를 못 지켜 minimal이 늘어난다.
**정답 라벨이 필요 없다** — 실획에서 직접 잰다(assumptions V-o: 합성으로 실제를 주장하지 말 것).

실획 폐곡선 n=1819(채택 설정), 파라미터 격자 12개(각 n=932):

| 설정 | optimal | over_split | under_fit | 1획=1엣지 |
|---|---|---|---|---|
| 10°/σ0.5 (가장 과민) | 0.4512 | **0.3861** | 0.0954 | 0.2169 |
| 10°/σ3 (격자 최댓값) | 0.7245 | 0.0629 | 0.1735 | 0.3167 |
| **20°/σ1.5 (채택)** | 0.7028 | 0.0304 | 0.2082 | 0.3232 |
| 45°/σ3 (가장 둔감) | 0.5011 | 0.0022 | 0.4534 | 0.3449 |

채택 설정 전체 표본(n=1819) 기준 **optimal_rate = 0.7125**.

- `optimal_rate`는 **내부 최댓값**을 가진다. 양쪽 끝에서 무너진다(0.45 / 0.50).
- 1획=1엣지는 격자 전체에서 **단조 증가**한다(0.2169 → 0.3449). 두 점이 아니라 곡선으로 확인했다.

> **정정 (리뷰어 2차 지적)**. 처음에 "채택 설정이 이 지표의 argmax이며 독립적인 두 기준이
> 같은 곳을 가리킨다"고 적었다. **원장에서 성립하지 않는다** — 격자 최댓값은 10°/σ3(0.7245)이고
> 채택안은 0.7028로 그 아래다. **그 논증을 철회한다.** 채택 설정은 합성 정답 스윕이 고른 것이고,
> 양방향 지표에서는 최댓값 근처(차이 0.022)일 뿐이다.
>
> 또 하나 정정: 문서에 오래 남아 있던 0.692/0.694는 **이전 실행의 값**이었다. 원장을 갱신하고
> 문서를 안 고쳤다 — D-1b-3에서 한 번 겪은 유형(같은 양이 여러 값으로 유통)이 재발했다.
> 이번 값은 DP 최소분할이 검출기와 **같은 전처리(평활)**를 거치도록 고친 뒤의 것이다.
> 그 전에는 DP가 평활 전 원본을 봐서 두 값이 다른 곡선에서 나왔다(리뷰어가
> `out_of_scope_rate`가 전 설정에서 소수점 넷째 자리까지 같은 것을 보고 짚었다).

#### 판단

**중단 조건의 대상을 `optimal_rate`로 옮기고, 임계 0.5는 그대로 둔다.** 측정 **0.7125** → 통과.

임계를 옮기지 않은 것이 중요하다. 지표를 바꾸면서 임계까지 낮추면 통과시키려고 고친 것이 된다.

**기각한 선택지**
- *문자 그대로 0.415 < 0.5 이므로 중단한다.* 그 값이 격자 전체에서 나쁜 검출기 쪽으로
  단조 증가함이 확인됐다. 그 값에 걸면 45°/σ3(놓침 0.453)이 통과하고 채택안이 걸린다.
- *결합 게이트로 바꾼다(1획=1엣지 ≥0.5 AND 합성 exact ≥x).* 리뷰어가 제시한 안이다.
  기각 사유: 결합해도 **1획=1엣지 항이 여전히 나쁜 검출기를 편든다.**
- *덮임률로 갈아끼운다.* 오염되거나 자명하다(위 참조).

#### 이 게이트에 남아 있는 결함 (리뷰어 2차, **해소되지 않음**)

정직하게 적는다. 아래 넷은 지적이 맞고 아직 답하지 못했다.

1. **사실상 발동하지 않는다.** 격자 12개 중 0.5 미만은 양 극단 2개뿐이고 나머지는 0.6을 넘는다.
   구 지표는 어떤 설정에서도 0.5에 못 닿아 **항상** 발동했고, 신 지표는 거의 발동하지 않는다.
   척도가 다른 두 지표에 같은 숫자를 쓴 것은 보존이 아니라 우연이다.
2. **전제와 구현 품질이 한 수치에 섞여 있다.** optimal_rate는 검출기와 DP 기준선이
   **같은 tol로** 얼마나 일치하는가다. 낮게 나올 때 "전제가 무너졌다"인지
   "검출기가 나쁘다"인지 구분되지 않는다.
3. **개수만 비교하므로 상쇄된다.** 없는 코너 1개 + 놓친 코너 1개인 획은 optimal로 세어진다.
   위치 오차를 재는 항목이 없다.
4. **곡선 계수가 두 원장에서 어긋난다.** 획 분류로는 곡선이 28%인데
   out_of_scope는 6~7%다. 차이만큼의 획이 분모에 in-scope로 들어가 값을 부풀릴 수 있다.

→ **W-3에서 셋을 잰다**: (a) optimal 판정에서 곡선 획을 뺀 값, (b) 위치 오차 기반 지표,
(c) `straight_tol` 0.05/0.10/0.15에서의 optimal_rate. 그 결과로 게이트를 다시 정한다.
그때까지 이 게이트는 **통과 기록이지 강한 관문이 아니다**.

### D-02. 코너 임계·평활을 스윕으로 골랐다 (20° / σ1.5)

선택 규칙을 **측정 전에** 고정했다: ① 없는 코너 생성 ≤0.02 ② 그 안에서 합성 정확도 최대
③ 최대와 0.02(유효 자릿수, V-H) 이내 동점이면 평활이 큰 쪽(거친 잉크 대비).

초기값 35°/2.0은 얕은 코너(45~70°)를 절반 넘게 놓쳤다(정확도 0.71, 얕은 코너 0.25).
격자 끝을 고르지 않았는지 확인하려고 10°까지 내렸고, 거기서는 다시 나빠진다
(정확도 0.86, 없는 코너 0.05) — 20°는 내부 최적이다.

### D-03. 곡선은 걸러내되 얕은 호는 직선으로 읽는다 (straight_tol 0.10)

직선 판정을 5%로 두면 실측 저주파 활(precise σ=0.031) 때문에 **곧게 그은 획도 11% 탈락**한다.
0.10은 1/4원(0.207)보다 얕은 호를 직선으로 읽는 대가를 진다. 얕은 호와 휜 직선은 실제로
구분이 어렵고, 이 도구의 범위는 직선이다(계획서 §1.2).

---

## S 전환 (2026-08-14, 두 번째)

### D-S1. three.js는 0.185를 쓴다 (계획서는 r128)

r128은 **참조 구현의 버전**이지 요구사항이 아니다. 필요한 것(`TubeGeometry`, `OrbitControls`)은
그대로 있고, 이미 저장소에 0.185가 들어 있다. 낮추면 최신 브라우저 대응만 잃는다.

### D-S2. §4.1 "크게 휜 획" — 억지 배정하지 않고 미분류로 둔다

계획서 후보 셋 중 **곡률 임계 초과 시 미분류**를 골랐다. 기각한 것:
- *최소제곱 직선으로 배정*: 크게 휜 획은 어느 축에도 우연히 잘 맞을 수 있다.
  A-3이 금지하는 "조용히 틀린 축에 배정"이 바로 이것이다.
- *사용자 지정만*: 곧은 획까지 물어보게 된다.

**임계 0.18은 측정이 정당화하지 못한다.** 스윕(0.08~0.40)에서 0.12 이상이 전부 같은 값을
낸다 — 상자 모서리는 그만큼 휘지 않아 **이 표본이 임계를 시험하지 못한다**. 원장에 그렇게 적었다.
실제로 휜 획이 생기는 S-10 이후에 다시 정한다.

### D-S3. §4.1 "한 획에 여러 축" — 검출해서 미분류로 두고, **분절하지 않는다**

계획서 후보는 "방향 급변점에서 분절"과 "축마다 끊어 그린다고 가정"이었다.
**둘 다 아닌 셋째를 골랐다** — 급변을 검출하되 쪼개지 않고 미분류로 두고 사용자에게 알린다.

- *분절*은 §1("획을 버리지 않는다")을 깨뜨린다. 획을 쪼개는 순간 이번 접근의 전제가 무너진다.
- *가정만 하고 검출 안 함*은 사용자가 실수로 두 축을 한 획에 그으면 조용히 틀리게 된다.

검출은 방향 급변(창 양쪽 현의 각도차 > 45°)으로 한다. 곡률로는 못 가른다 —
크게 휜 곡선도 곡률이 크기 때문이다. 순서도 급변을 먼저 본다(원인을 바르게 말하기 위해).

### D-S4. 합성 잉크의 활 마루를 옮길 수 있게 했다 — **자기확인을 깨기 위해**

S-2 측정이 이상하게 좋았다(조용히 틀림 0.0013). 원인은 합성 활이 `sin(πt)`로 **대칭**이라
최소제곱 주축이 참 방향과 **정확히** 나란해지는 것이었다(편차가 상쇄된다. 반례 테스트로 확인).
그 조건에서만 재면 판정 규칙이 아니라 **노이즈 모델의 성질**을 재게 된다.

마루 위치(`skew`)를 인자로 빼고 0.5 / 0.25 / 0.12에서 함께 잰다. 기본값은 0.5로 두되
**보고의 대표값은 치우친 조건**으로 읽는다.

### D-S5. provenance를 폐기하되, **깊이의 출처가 setting임을 문서에 남긴다**

계획서 §10이 provenance를 폐기 목록에 넣었고 CLAUDE.md에 "추정이 없으므로 출처를 나눌
대상이 없다"고 적었다. **그 문장은 과했다**(리뷰어, 이론서 16.2·16.4).

- 1점에서 f는 **설정값**이다(§3.2가 그렇게 적는다).
- 2점에서 주점 = 이미지 중심은 **가정**이고 W-y는 검증되지 않았다.
- 이론서 16.4: 단일 시점에는 **깊이 방향 아핀 자유도**가 남는다.

따라서 모든 획의 **깊이 성분은 measured가 아니라 setting**에서 온다. 없어진 것은
불확실성 **보고**(1-bis 폐기)이지 불확실성이 아니다. S-5(회전 후 이어 그리기)에서
사용자가 돌리는 순간 그 왜곡은 **화면에 직접 보인다**.

**판단**: 필드는 되살리지 않는다(자료구조를 늘리지 않는다, A-3). 대신
- `Camera.fSource`가 이미 `setting(렌즈)` / `two_vps(6.2)` / `orthocenter(6.3)`를 구분해 들고 있고
  화면에 그대로 나온다 — 깊이의 출처는 **카메라 하나에서 결정되므로 획마다 적을 필요가 없다**.
- CLAUDE.md의 "추정이 없다"를 "**형태 추정이 없다. 깊이 스케일은 카메라 설정에서 온다**"로 고친다.

### D-S6. AS-3(유일한 중단 조건)의 임계를 S-3 착수 **전에** 숫자로 박았다

리뷰어 지적: W 접근의 중단 조건에는 수치가 있었는데(optimal_rate ≥ 0.5) 새 것은
"사용 불가능할 만큼 다르거나"뿐이라 반증될 수 없다. DEFERRED에 이미 같은 유형의
미해소 지적("게이트가 사실상 발동하지 않는다")이 있었고 그것이 반복되는 자리였다.

**임계**: 재투영 화면 좌표차의 **중앙값 > 획 길이의 5%** 또는 **p90 > 15%**면 중단하고 보고한다.

- **화면 대각이 아니라 획 길이 대비**다. 대각 대비로 하면 짧은 획에서 한없이 관대해진다.
- 5%는 획 굵기의 몇 배 수준이다 — 그보다 크면 사용자가 "다른 데로 갔다"고 느낀다.
- 값을 **측정 전에** 정했다. 측정 후에 정하면 통과시키려고 고른 것이 된다.

### D-S7. AS-3의 **임계값은 그대로 두고 적용 대상만 정정한다**

D-S6이 "재투영 화면 좌표차의 중앙값 > 획 길이 5%"를 중단 임계로 박았다. 측정 전에 박은 것은
옳았으나, **그 양이 이 설계의 보장이라는 것을 그때는 몰랐다.** 결함이 아니라 성질이다 —
계획서 §9의 우려("미리보기와 확정이 크게 달라 놀란다")가 **구조적으로 발생하지 않는다**. 각 점은 어떤 떨림 평면을
골라도 **자기 광선 위에** 놓이므로 그린 카메라에서 다시 투영하면 원본 `pts2d`로 정확히 돌아온다
(실측 487획, 중앙·p90·최대 전부 0px). 보장을 게이트로 쓰면 원리적으로 발동하지 않는다 —
리뷰어가 지적한 "반증 불가"가 숫자를 박은 뒤에도 남아 있었던 것이다.
**보장 자체는 불변식 테스트로 잠갔다**(역투영 방식이 바뀌면 깨진다).

**임계값(중앙 5% / p90 15%)은 건드리지 않는다.** 통과시키려고 값을 고르는 것이 되기 때문이다.
대신 실제로 화면을 움직이는 두 양에 같은 값을 건다.

- **끝점 접합 이동량 / 획 길이** — §1.2가 허용하는 유일한 위치 개입이다.
- **미배치율** — 그린 획이 3D에 안 놓이면 사용자에게는 "사라진" 것이다.

**단서**: 접합은 접합 반경 안에서만 일어나므로 이동량은 구조적으로 반경(화면 대각 1.5% =
17.6px)에 갇혀 있다. 크게 어긋난 획은 큰 이동량으로 잡히는 대신 **미배치로 새어 나간다.**
그래서 이동량 하나로는 발동하기 어렵다.

**두 번째 대상에는 숫자를 뒀다 — 그리고 그것이 사후에 정한 값임을 표시한다.**
"축이 정해졌는데도 못 놓인 비율 > **20%**". 리뷰어가 "새 대상도 발동할 수 없는 쪽에 가깝다"고
지적했고 맞는 말이었다. 사전에 정할 수 없었던 이유는 **원래 지정한 양이 항등이라 이 양이
존재한다는 것 자체를 몰랐기 때문**이다. 값 20%에 측정 근거는 없다("다섯 중 하나가 안 놓이면
도구로 못 쓴다"는 판단이다). 지금 이 임계가 하는 일은 하나다 — **발동 가능한 관측 조합이 실제로
존재함을 보이는 것**(끝점 겨냥 오차 2%에서 **0.234**, 4%에서 **0.702**로 넘는다 — 임계 0.20까지의 여유가 크지 않다).
**S-10에서 실획으로 재정하기 전까지 판정 근거로 쓰지 않는다.**

**그리고 동작점을 하나 고르지 않는다.** 처음에는 끝점 겨냥 오차 2%를 "대표 조건"으로 적었는데,
그것도 측정 후에 고른 값이다(리뷰어 지적). 판정은 **스윕 전체**로 읽는다 —
1% 이하면 성립하고, 2%면 축이 정해진 획의 **23%**가 안 놓이며, 4%면 76%가 안 놓인다.
즉 **중단 조건은 도구가 아니라 입력에 걸린다**: 이 방식은 끝점 겨냥 오차가 그림 대각의
1% 근처일 때 성립한다. 숙련자의 그 값은 **미측정**이다(AS-1, S-10 첫 측정 대상).

돌린 뒤에 보이는 차이(미리보기 평면 ↔ 확정 평면)에는 임계를 걸지 않는다.
그것은 결함이 아니라 "획이 깊이로 들어갔다"는 뜻이고, 걸면 이 도구가 하려는 일을 금지하게 된다.

### D-S8. 떨림 평면은 **`facing`** — 계획서의 두 후보 어느 쪽도 아니다

계획서 §4.4는 "시선을 포함하는 평면"과 "화면 평행 평면" 둘을 뒀다. 구현해 보니 셋이었다.

- *눈을 지나는 평면*: **불가능하다.** 그 평면은 화면에 직선 하나로 맺히므로 축을 벗어난 점
  (=떨림)의 광선이 원점에서만 만난다 — 한 점도 놓이지 않는다(반례 테스트).
  계획서의 "시선 포함"은 눈이 아니라 **광축 방향**을 담는 것으로 읽어야 성립한다(`sight`).
- *`sight`*: 성립하지만 평면이 시선과 거의 나란해 조건수가 나쁘다. 광선이 스쳐 지나 아예 안
  만나는 획이 생겨 배치율이 낮고(참 축 0.9806 vs `facing` 1.000), 연쇄 깊이 오차가 7배로 자란다.
- *`screen`*(계획 나): **L을 담지 못한다** — 축 각도 중앙 34°, 획이 깊이로 안 들어간다.
  이것이 미리보기 평면이다.
- **`facing`**: L을 담되 시선에 가장 정면으로 서는 평면.

**대표 조건**(판정 통과·끝점 겨냥 오차 2%·skew 0.12·medium, 중앙값)에서
축 각도 **2.34°**(sight 15.27 / screen 34.03), 모서리 오차 **0.0597**(0.2789 / 0.3690),
깊이 오차 **0.0279**(0.1482 / 0.2029). **A-3(측정이 가리키는 방향)에 따라 채택.**

**떨림비는 근거에서 뺐다.** 리뷰어가 "`facing`이 그 지표에서 이기는 것은 평면 정의의 귀결이
아닌가"라고 물었고, 평면 족을 각도로 훑어 보니 맞았다 — 떨림비는 0°(=`facing`)에서 극값이다.
**다만 모서리 오차도 같은 자리에서 극값이고 그것은 참값 대조다.** 그래서 채택은 유지하되
근거를 축 각도·모서리 오차·깊이 오차 셋으로 좁힌다. `sight`는 이 족의 79.6°에 해당한다.

계획서 §4.4를 이 셋으로 고쳐 적는다(A-3 "계획 문서와 실제가 어긋나면 실제를 따르고 문서를 갱신").

> **꼬리 보강(S-6 2b 리뷰어).** 위 셋은 전부 **중앙값**이고, 이 프로젝트가 반복해서 걸린 것이
> "중앙값만 읽기"다. 같은 원장의 p90은 `facing` **0.7284** · `screen` 0.8221 · `sight` 1.2301로
> **순서가 유지된다**(다만 중앙값의 5~6배 격차가 p90에서는 1.1~1.7배로 좁혀진다).
> 최댓값만 `screen`(0.9689)이 `facing`(1.6887)보다 낮은데, 그것은 `screen`이 **깊이를 아예 안
> 주기 때문에 오차가 구조적으로 갇힌 것**이다(축 각도 34°가 같은 사실의 다른 얼굴이다) —
> 품질 우위로 읽을 수 없다. **채택은 유지된다.**

### D-S9. S-2b — a·b는 켜고 **c는 끈다**. 예상이 측정에 반증됐다

지시는 (c) 획 간 일관성이 "가장 효과가 클 것"으로 예상해 우선 구현을 지시했다.
구현하고 쌍대 비교(같은 시드·같은 그림)로 재니 **b(상대 순위)가 가장 크다**.

| | a 위치 의존 | b 상대 순위 | c 획 간 일관 |
|---|---|---|---|
| 판정률 증가(warp 0.3) | +0.019 | **+0.043** | +0.002 |
| 조용히 틀림 증가 | +0.000 | +0.000 | +0.000~0.002 |

**c를 끄는 이유는 효과가 작아서가 아니라 구제의 질 때문이다** —
전 조건 합산 **구제 29획 중 8획이 틀렸다(28%)**. 기저 오류율 3~7%의 네 배가 넘고,
A-3("조용히 틀린 축에 배정하지 않는다")이 금지하는 거래다. 판정률 +0.002를 얻으려고
오배정을 28% 확률로 만드는 것은 이 프로젝트가 계속 거절해 온 것과 같은 종류다.

**기전을 기각하는 것이 아니다.** 이 표본이 c에게 근거를 주지 못한다 — 상자 하나에 축당 4획뿐이라
`min_support: 3`이 사실상 그 축 전체이고, 국소 편향이 자기 자신과 거의 같은 것을 본다.
획이 많은 실제 그림에서 다시 잰다(S-5·S-10). **코드는 남기고 기본값만 끈다.**

A-3("사전 예상과 어긋나면 측정을 따르고 예상을 반증으로 기록한다")에 따라 여기 적는다.

### D-S10. 접합 반경을 **키우지 않는다** — 측정이 키우라고 말해도

(e) 스윕에서 접합 반경 0.015 → 0.025면 앵커 실패가 0.013 → **0**이 된다. 그런데 안 키운다.

- AS-13: 지금 tolerance를 정하는 근거가 전부 **대표성 없는 표본**이다. 넉넉히 잡아 두면
  실획이 들어왔을 때 그것이 옳았는지 알 수 없다.
- 이 측정은 **접합 정확도를 재지 않았다.** 반경을 키우면 엉뚱한 모서리에 붙기 시작하는데,
  판정 정확도(0.9)는 그 위험을 재는 지표가 아니다.

측정만 원장에 남기고 값은 그대로 둔다. 실획이 들어오면 다시 본다.

### D-S11. 돌린 시점에서 그리기 시작하면 **카메라를 잠근다** — 자료구조를 늘리지 않기 위해

§5는 "카메라가 재조정되면 `pts2d`에서 전체를 다시 투영한다"고 요구한다. 시점이 하나일 때는
성립하는데, S-5가 **여러 시점**을 열면 각 획의 `pts2d`가 **어느 시점의 화면 좌표인가**가 붙어야 한다.

- *`Stroke`에 시점 참조를 넣는다*: 자료구조가 는다. A-3이 마지막에 두라는 것.
- ***돌린 시점에서 그리면 카메라를 잠근다*(채택)**: §3.7 소프트 락이 이미 있는 자리다.
  잠근 뒤에는 첫 카메라를 재조정할 일이 없으므로 혼합 시점 재투영이 **생기지 않는다.**

사용자가 명시적으로 풀면 그때 문제가 되지만, 그것은 "기하가 따라 움직이는 것은 예상된 동작"
(§3.7)의 범위다. 혼합 시점 재투영은 DEFERRED에 올린다 — 필요해지면 그때 자료구조를 늘린다.

### D-S12. 깊이 가림 가드는 **돌린 시점 경로에만** 켠다

> **부분 정정(D-S16).** 아래의 "첫 시점에는 켜지 않는다"는 **깊이 가드에만 유지된다.**
> 같은 절의 "그 조건에서는 얻을 것이 없다"를 **정합성 검사에 대해서는 S-6 (2b)가 반증했다**
> — 첫 시점에서도 놓인 모서리의 12.4%가 상자 대각의 0.2 넘게 틀려 있었고 검사를 켜니 5.8%다.

S-5가 발견한 것: 접합을 화면에서 판정하므로 돌린 시점에서는 가림 때문에 깊이가 전혀 다른
획에 붙는다(모서리 오차 p90 0.39~0.92). 대응은 **애매하면 놓지 않는다**(A-3).

**첫 시점에는 켜지 않는다.** 그 경로는 씨앗에서 바깥으로 자라 겹침이 드물고,
켜면 S-3의 측정이 전부 바뀐다(그리고 그 조건에서는 얻을 것이 없다).
경로별로 다른 값을 쓰는 것이 옳은 이유가 **가림이 시점에 달린 현상**이기 때문이다.

값 0.05는 **잠정**이다(AS-13). 0.05와 0.15가 같은 결과를 내고 0.30은 무력하다 —
그 띠 안이면 되고 정확한 값은 실획에서 정한다.

### D-S13. 돌린 시점의 앵커는 **검사 둘**로 고른다 — 하나로는 각도에 따라 구멍이 남는다

- **정합성**(`view_far_end_check = 1`): 축과 앵커가 획 전체를 정하므로 반대쪽 끝점이 예측된다.
  그 예측이 반대쪽 끝의 기존 기하와 만나야 한다. **양 끝에 후보가 있을 때만** 쓸 수 있다.
- **깊이 타당성**(`view_depth_envelope = 0.5`): 정합성을 못 쓴 획(자유단)이 기존 기하의 깊이
  범위를 벗어나면 놓지 않는다.

**둘 다 필요한 이유를 측정이 정했다**: 궤도 45°에서 **38.5%가 자유단**이라 정합성 검사가
아예 안 걸렸고 그 경로가 그대로 꼬리였다(p90 0.71). 깊이 타당성을 붙이니 0.075가 됐다.
75°·110°는 정합성만으로 0.069·0.064였으므로, 각도마다 듣는 검사가 다르다.

> **⚠ 조건 명시(D-S21).** 위 수치는 전부 **고각 35°**다(`continue_after_rotate.json`).
> 고각 **18°**로 다시 재니 궤도 75°에서 모서리 p90이 **0.591**이었다(`view_plane.json`).
> **"세 각도가 모두 해소된다"는 고각 35° 전용 진술이고, 낮은 고각은 미해소다.**
> 두 원장의 조건이 그렇게 다른 줄 모르고 일반 진술로 적어 두었던 것이다.

S-5의 깊이 가드(`view_depth_guard`)는 **대체됐다**. 상수는 측정 재현용으로 남긴다.
세 값 모두 잠정이다(AS-13) — `view_depth_envelope`는 0.5가 듣고 1.0은 느슨하다.

### D-S14. §4.3의 "면"은 **축 두 개가 펴는 평면**이다 — 면을 만들지 않는다

지시가 두 선택지를 줬다: 닫힌 획 루프를 임시 면으로 볼 것인가, S-8(면 생성)까지 미룰 것인가.
**셋째를 골랐다** — 상자의 면은 **축 두 개가 펴는 평면**이고 앵커가 위치를 정한다.

- *닫힌 루프 검출*: 루프를 찾아야 하고, 사선이 그어지는 시점에 루프가 닫혀 있으리라는 보장이 없다.
- *S-8까지 미루기*: §4.3의 첫 경우(가장 값어치 있는 것)를 통째로 미루게 된다.
- **축 평면(채택)**: 검출이 필요 없다. 앵커 후보 × 축 쌍 세 개면 후보 평면이 다 나온다.
  **면 + 시선 광선 = 3D 점이 유일하게 결정된다**는 §4.3의 근거를 그대로 쓴다.

어느 평면인지는 **S-6 (1)의 정합성 검사**로 고른다 — 새 판정 규칙을 만들지 않는다.
**양 끝에 후보가 있을 때만** 시도한다: 없으면 검증 수단이 없고, 검증 없이 놓는 것은 추측이다(A-3).

측정: 미분류로 남은 사선의 **84.2%**가 놓이고 모서리 오차 p90 **0.057**. _**출하 구성에서는 이 값이 내려간다**(D-S16이 정합성 검사를 첫 시점에도 켰다). **같은 범위끼리 대조한다** — 시드 9700 한 판으로는 0.842 → **0.802**, 시드 셋 합산으로는 0.860 → **0.821**이다(`misassign_retry.json@272b5143`, 끝점 오차 0.01)._
**병목은 판정이다** — 사선의 15.8%가 축으로 오인되고 그렇게 놓이면 오차 중앙이 0.448이다.

### D-S15. 미배치 획은 **놓지 않되 버리지 않는다** — 구현은 S-7 (지금 결정만)

배치율이 0.65~0.76이다. A-3에 따라 조용히 틀린 것보다 낫지만 그린 것의 넷 중 하나가 3D에 없다.

**(a) 지금 어떻게 되는가 — 확인했다.**
첫 시점에서 그린 미배치 획은 왼쪽 캔버스에 **회색 파선으로 남는다**(사라지지 않는다).
그러나 **돌린 시점에서 그린 획은 `pts2d`가 뷰 좌표라 왼쪽에 엉뚱한 자리로 그려지고 있었다** —
S-6에서 고쳤다(배치된 것은 `pts3d`를 첫 카메라로 투영, 미배치 view 획은 안 그리고 개수만 알린다).

**(b)~(d) S-7에서 할 것** — 결정만 여기 적는다.
- **미배치 획을 그린 시점에서 보이게 한다.** 지금은 "몇 획이 숨어 있다"만 알린다.
  그 시점으로 돌아가면 보이게 하거나, 뷰에 파선으로 겹쳐 그린다.
- **사용자가 지정한다**: 어느 끝점에 붙일지(후보를 표시하고 고르게), 또는 깊이를 직접.
  S-6 (1)이 이미 후보 목록(`anchorCandidates`)을 갖고 있으므로 표시할 것이 있다.
- **S-6 (1)의 잔여 한계도 같은 경로로 처리한다** — 두 검사는 **내부 정합성**만 보장하므로
  엉뚱한 두 모서리에 일관되게 붙으면 통과한다(최댓값 0.88~1.06). **보이면 사용자가 판별한다.**

**이것이 A-3("애매하면 놓지 않는다")의 완결이다 — 놓지 않되 버리지 않는다.**

### D-S16. 정합성 검사는 **첫 시점에도 켠다**. 축 오배정 **되돌리기는 기각한다**

산출: `misassign_retry.json@272b5143`(끝점 겨냥 오차 0.005·0.01·0.02 × 시드 셋 × 팔 여섯).

**(1) 검사를 첫 시점에도 켠다.** S-6 (1)은 이것을 **돌린 시점 전용**으로 만들었고 근거는
D-S12의 "가림은 시점에 달린 현상"이었다. 그 논리는 **깊이 가드**에 해당하고 **정합성 검사에는
해당하지 않는다** — 틀린 앵커는 시점에 매이지 않는다. 첫 시점에서도 놓인 모서리의
**12.4%**가 상자 대각의 0.2 넘게 틀려 있었고(끝점 오차 0.01, 면 경로를 꺼도 12.1%),
검사를 켜면 5.8%다. 축으로 오인된 사선이 놓이는 비율은 **1.000 → 0.231**이다.

근거는 **떨어뜨리는 집단의 농축비**다(떨어뜨린 것의 오류율 ÷ 남긴 것의 오류율).
절단 0.2에서 **13.6 / 7.5 / 2.8배**(끝점 오차 0.005/0.01/0.02)이고, **절단값 셋 × 시드 셋 ×
동작점 셋 27조합의 최솟값이 1.72다.** 사선을 뺀 모서리만으로도 유지된다(0.41/0.48/0.52 @ 0.2).
_떨어뜨리는 것이 전부 틀린 것은 아니다 — 절반가량은 옳게 놓였던 배치도 함께 버린다.
그것이 미배치로 남는 대가이고 **D-S15가 처리한다**(놓지 않되 버리지 않는다)._

**⚠ 이득은 끝점 오차에 단조 감소한다.** 놓인 모서리의 오류율은 0.077→0.024(3.2배) /
0.124→0.058(2.2배) / **0.236→0.208(1.13배)**이고, 대가인 D-S7 ② 미배치율은
0.000→0.139 / 0.001→0.153 / **0.246→0.313**이다. **채택은 방법이 성립한다고 주장하는
구간(끝점 오차 ≲ 1%)에 걸린다** — 0.02에서는 검사가 아니라 입력이 문제이고(base 자체가
모서리의 32%를 못 놓는다) **AS-12의 실획 자릿수는 그보다도 밖이다. S-10에서 다시 낸다.**

상수 이름을 `view_far_end_check` → **`far_end_check`**로 바꿨다(`view_depth_*`는 그대로 —
그쪽은 정말로 시점에 매인다). **`preview_gap.json`의 `unanchored_of_classified`는 검사를 끈
구성의 값이다** — 출하 구성의 값은 `misassign_retry.json`에 있다.

**(2) 되돌리기는 기각한다.** 근거는 "선을 넘었다"가 아니라 **"어느 자리에서도 안전 신호가
없다"**이다.

| 끝점 오차 | 되돌린 사선(틀림) | 되돌린 모서리(틀림) | 전체 오류율 | 시드 범위 | 기저 대비 |
|---|---|---|---|---|---|
| 0.005 | 48 (6%) | 149 (30%) | 0.244 | 0.22~0.30 | 6.6배 |
| 0.01 | 34 (3%) | 143 (32%) | 0.266 | 0.20~0.34 | 3.7배 |
| 0.02 | 10 (30%) | 37 (62%) | **0.553** | 0.42~0.62 | 2.6배 |

_기저 = 검사만 켠 팔이 남긴 집단의 오류율(`vs.base.kept`의 >0.2 비율: 0.037·0.071·0.214).
절단 0.1로 읽으면 배수가 1.8~2.4로 줄지만 방향은 같다._

**기전은 의도한 대상에서 정확하다** — 0.005·0.01에서 되돌린 사선은 3~6%만 틀린다.
막는 것은 **겨냥할 수단이 없다**는 것이다: 같은 신호가 정상 모서리를 3.1~4.2배 더 많이
끌어들이고 그쪽이 27~38%(유리한 동작점)·60~63%(0.02) 틀린다. 허용치를 조여도 **회수량만
줄고 질은 개선되지 않는다**(0.266 → 0.309 → 0.313). **갈리지 않는데 놓는 것은 A-3이
금지한다.** → **코드는 남기고 `PLACE_TOL.retry_as_face = 0`.**

**D-S9와 같은 종류의 판단이지만 같은 양의 비교는 아니다.** D-S9의 28%는 **축 오배정률**이고
여기 26.6%는 **배치 오차율**이다. 초판은 그 둘을 3pp 차이로 비교해 기각했는데 **그 비교는
성립하지 않았다**(리뷰어). 이 판은 선을 넘었는지로 판정하지 않는다.

**기각의 범위**: 기전을 기각하는 것이 아니다. 판정의 **여유(margin)**를 함께 신호로 쓰면
사선과 모서리가 갈릴 수 있다 — 그러나 새 기전이고 새 임계다(A-3) → DEFERRED.

**정정: D-S12의 "첫 시점에는 얻을 것이 없다"는 반증됐다**(12.4% → 5.8%, 끝점 오차 0.01).
D-S12의 판단은 `view_depth_guard`에 대해서만 유지된다.

### D-S17. §4.3의 나머지 둘 — **경사 소실점은 만들지 않는다. 자유 곡선 폴백은 곧은 획에만 켠다.**
### 그리고 **굽은 획은 어느 평면 경로에도 넣지 않는다**

산출: `incline_vp.json@0803f3fe` · `free_curve.json@0803f3fe`.
_초판은 리뷰어가 18건을 지적했고(높음 7) 그중 둘이 결론을 바꿨다 — 아래 ⚠ 표시._

**(1) 경사 소실점 — 만들지 않는다.** 이론서 15.1을 정확히 읽으면 경사 VP 후보는 점이 아니라
**직선**이다(`cos t·e_h − sin t·e_v`의 소실점은 전부 `VP_h`–`VP_v` 선 위. 불변식 테스트).
그래서 **획 하나로는 못 가른다** — 대표 직선은 그 후보 직선과 반드시 만나므로 어떤 획에도
부적합도 0인 경사 VP가 존재한다(반례 테스트). _단 이 항등은 **획을 대표 직선 하나로 볼 때**
성립한다. 점별 잔차로 정의하면 0이 아니다 — 진술의 범위를 그렇게 좁힌다(리뷰어)._

⚠ **초판은 획 둘의 교점만 쟀다.** 그것은 가능한 추정기 중 가장 약한 것이고, 15.4·15.5와
§3.1 누산기는 여러 획을 묶는 경로를 준다. **획 2·3·4·6으로 최소제곱 추정을 다시 쟀다**
(밑변은 양 끝 서까래로 고정 — 획 수만 바꾼다):

| 경사각 | 추정이 **참 경사 VP 쪽**에 있는 비율 (획 2 / 3 / 4 / 6) |
|---|---|
| 10° | 0.53 / 0.25 / 0.10 / **0.01** |
| 20° | 0.62 / 0.35 / 0.17 / **0.03** |
| 30° | 0.59 / 0.35 / 0.16 / **0.04** |
| 45° | 0.39 / 0.19 / 0.08 / **0.02** |

**획을 더 묶으면 오히려 나빠진다.** p90 산포는 줄지만(획 6에서 ratio p90이 3.9~1.4) 추정이
**수평 VP 쪽으로 편향된 점에 수렴**한다. 원인은 AS-14다 — 잉크의 계통 왜곡은 획마다 독립이
아니라 **공유**되므로 평균이 그것을 못 지운다. **어느 획 수에서도 쓸 수 있는 분리가 없다.**

_판정 기준을 밝힌다: `ratio < 1`(추정 오차가 두 VP 간격보다 작다)은 30°·45°에서 중앙값이
통과한다(0.82·0.84). 그것만 보면 "갈린다"로 읽힌다. 그러나 **같은 표본에서 추정이 참 경사 VP보다
수평 VP에 가까운 비율이 39~62%**다 — 절반 가까이가 반대편이면 검출기가 아니다.
그래서 기준을 `ratio`가 아니라 **어느 쪽에 서는가**로 잡는다. 초판은 이 구분을 안 했다(리뷰어)._

**지금(경사 VP 없이) 어떻게 되는가** — 이쪽이 더 쓸모 있었다. 다만 **대가를 다 적는다**:

| 경사각 | 판정 | 배치율 | 놓인 것 중 >0.1 / >0.2 |
|---|---|---|---|
| 0°(대조군) | 97% 축0 | 0.892 | 2.3% / 0.9% |
| 10° | 96% 축0 | **0.533** | **10.9%** / 1.2% |
| 20° | 95% 미분류 | 0.442 | 12.7% / 10.4% |
| 30° | 100% 미분류 | 0.171 | 36.6% / 36.6% |
| 45° | 100% 미분류 | 0.121 | 50.0% / 48.3% |

⚠ **"얕은 경사는 대가가 작다"는 절단 0.2에서만 맞다.** 10°에서 획의 **47%가 미배치**가 되고
(0.892 → 0.533), 절단 0.1로 읽으면 오류율이 대조군의 **4.7배**다. 초판은 0.2 하나만 읽었다.

**(2) 자유 곡선 폴백 — 켠다. 단 곧은 획에만**(`span_far_end = 1` + `freeBendMax`).
계획서의 "임시 평면(시선 수직)"은 후보가 아니다(깊이가 임의가 되고 D-S8이 기각한 `screen`이다).
**양 끝 앵커가 정하는 `facing` 평면**으로 구현했다.

⚠ **초판은 이것을 껐고, 근거가 틀렸다.** "정확도가 안 오른다(0.337 → 0.317)"고 적었는데
그 중앙값은 **면 경로가 놓은 것과 폴백이 놓은 것을 섞은 값**이다. per-stroke로 가르니
(포함관계 확인: 어긋난 획 1개) **폴백이 놓은 획은 0.2 초과가 38%이고 면 경로가 놓은 것은
98%**였다 — 폴백 쪽이 오히려 깨끗하다. **총계의 차로 집합을 만들지 말라는 S-6 (2b)의 교훈이
같은 형태로 재발했다**(리뷰어).

곧은 획으로 한정한 실제 후보 구성에서:

| 구성 | 곧은 미분류 획 배치율 | 그중 >0.2 |
|---|---|---|
| 면 경로만 + 제한 | 0.529 | 1/37 |
| **면 경로 + 폴백 + 제한(채택)** | **0.671** | **1/47** |

**더 놓은 10획 중 0.2를 넘는 것이 없다.** 켠다.

**(3) 굽은 획은 어느 평면 경로에도 넣지 않는다**(`freeBendMax = AXIS_TOL.bend_max`).
**두 경로의 검사가 모두 끝점 둘을 본다** — 곧은 획이면 끝점 둘 + 평면이 획을 유일하게
결정하므로 검사가 획 전체를 덮지만, 굽은 획은 가운데가 어디로 가든 끝점이 같다.
**굽음은 어느 쪽에서도 검증되지 않는다.**

측정: 굽은 획에서 면 경로는 **62%**, 폴백은 **51%**가 최대 이탈 0.2를 넘는다.
**참 끝점을 지나는 평면 다발을 훑어 얻은 최선은 0.078**이므로(오라클 바닥) 답이 없는 것이
아니라 **두 방법 다 못 고르는 것**이다. 확인 수단 없이 반이 틀리는 배치는 놓지 않는다(A-3).

_**이 표본은 임계값 0.18을 시험하지 못한다.** 굽은 픽스처는 획의 85~98%가 임계 위, 곧은
픽스처는 대부분 아래인 이봉 구성이라 그 사이 어디에 두어도 같은 결과다(D-S2가 같은 상수에
대해 이미 같은 말을 적었다). 이 측정이 지지하는 것은 **값이 아니라 "굽음은 검증 밖"이라는
구분**이다. 값은 S-10에서 재정한다. 또 등급이 medium 하나다 — `coarse`의 저주파 휘어짐
0.0937은 임계의 절반이므로 곧은 획이 더 많이 잘려 나갈 수 있다(미측정)._

**세 결정의 공통점**: 셋 다 "놓을 수 있는가"가 아니라 **"놓은 것이 맞는지 확인할 수단이
있는가"**로 갈렸다. 확인 수단이 없으면 놓지 않는다 — 남은 길은 S-7의 사용자 지정이다(D-S15).

### D-S18. S-7 — 사용자 지정은 **검사하지 않는다.** 대신 **무엇이 미확정인지 표시한다**

D-S15가 "놓지 않되 버리지 않는다"를 결정했고 구현을 S-7에 미뤘다. 그 구현의 설계 결정이다.

**(1) 사용자가 고른 앵커에는 정합성 검사를 걸지 않는다.** 자동 경로의 검사(D-S13·D-S16)는
"기계가 고른 것이 맞는지"를 묻는다. 사용자가 고른 것에 같은 검사를 걸면, 검사가 못 푸는
경우(S-6 1의 잔여 한계 — 엉뚱한 두 모서리에 **일관되게** 붙은 배치)를 사용자가 고칠 수
없다. **그 경우를 풀라고 만든 경로에 같은 검사를 다시 거는 것은 자기모순이다.**

**(2) 대신 무엇이 정해졌는지 표시한다.** 고른 개수와 획의 축에 따라 결정성이 다르다:

| 상황 | 무엇이 정하는가 | 표시 |
|---|---|---|
| 축이 있고 한쪽을 고름 | 축 + 앵커가 획 전체 | 확정 |
| 축이 없고 **양쪽**을 고름 | 두 앵커가 평면(§4.3 폴백과 같은 평면) | 확정 |
| 축이 없고 한쪽만 고름 | 앵커의 깊이뿐 | **미확정** |
| 깊이를 직접 줌 | 사용자의 눈 | **미확정** |

미확정 배치는 **주황색**으로 3D에 나온다. **자료구조를 늘리지 않는다** — `Stroke.color`가
이미 있고, 미확정 여부 자체는 앱의 `Set`에 둔다(`rawPoints`·`viewOrigin`과 같은 방식).

**(3) 놓인 획도 다시 지정할 수 있다**(D-S15 d). 선택은 미배치 획에 한정되지 않는다 —
**보이면 사용자가 판별한다**가 그 결정의 문장이었고, 판별한 뒤에 고칠 수단이 있어야 완결된다.

**(4) 후보 반경은 접합 반경의 3배다.** 자동 경로의 반경(`join_ratio` 0.015)은 "닿았다고
볼 것인가"의 임계이고, 여기서는 **사용자에게 보여 줄 후보**를 모으는 것이므로 넉넉해야 한다.
넉넉해서 생기는 위험(엉뚱한 후보가 섞임)은 **사용자가 보고 거른다** — 자동 경로에서
D-S10이 반경을 못 키운 이유(정확도를 잴 수단이 없다)가 여기서는 사라진다.

### D-S19. 측정 경로와 앱 경로를 **한 곳에서** 낸다. 돌린 시점의 평면 경로는 **안 켠다**

산출: `wiring.json@0803f3fe` · `view_plane.json@0803f3fe`.

**(1) 배선의 단일 출처를 만든다.** §4.3 면 위 사선을 S-6 (2)가 측정해 놓고 **앱은 그 경로를
안 켜고 있었다** — `settle(drawn, ctx)`가 옵션 없이 불렸고 두 단계가 그대로 지나갔다.
원인은 **측정 하네스와 앱이 각자 옵션 객체를 적는 구조**였다. 이제 `src/s3d/appPlace.ts`의
`FIRST_VIEW_OPTS`·`VIEW_OPTS` 하나에서 가져오고 하네스도 그것을 쓴다.

**기계로 잠근다** — 이것은 STALE과 같은 성격이고 사람이 기억할 종류가 아니다:
- `appPlace.ts`의 `_EVERY_OPTION_REGISTERED`가 **컴파일 단계**에서 미등록 옵션을 잡는다.
  `PlaceOpts`에 키를 늘리면 배선 표에 등록하기 전까지 `tsc`가 깨진다.
- `test/wiring.test.ts`가 약속을 확인한다: `both`는 두 경로에 다 있고, `first`/`view`는
  **근거 원장을 인용**해야 하며, `measure`는 앱에 없어야 하고 왜 안 켜는지가 적혀 있어야 한다.
- 같은 테스트가 `main.ts`에 **옵션 없는 `settle` 호출**이 남아 있으면 실패시킨다.

전수 점검 결과 어긋난 것은 §4.3 하나였고(이미 S-6 2b에서 고쳤다), 그 밖에 하네스 둘이
앱 옵션을 **다시 적고** 있었다(`incline`·`user_place` — 하나는 `freeBendMax`가 빠져 있었다).

**(2) 돌린 시점에는 평면 경로를 안 켠다.** 배선 원칙("같은 획이 시점에 따라 갈리면 안 된다")
으로는 켜야 하는데 **측정이 막았다**:

| 궤도 | 미분류 사선 | 회수 | 놓인 것 중 0.2 초과 | 나쁜 쪽 끝 p90 |
|---|---|---|---|---|
| 45° | 50 | 0.80 | **33/40 (82%)** | 0.746 |
| 75° | 150 | 0.74 | 5/111 (5%) | 0.090 |
| 110° | 128 | 0.61 | 1/78 (1%) | 0.053 |

**궤도 45°에서 놓인 것의 82%가 크게 틀린다.** 가림이 가장 심한 각이고, S-6 (1)이 남긴
"엉뚱한 두 모서리에 **일관되게** 붙으면 통과한다"는 한계가 그대로 나온 것이다.
_깊이 타당성을 평면 경로에도 걸어 봤다 — **안 듣는다**(0.2 초과 33 → 33). 틀린 면도 앵커를
지나므로 깊이가 기존 범위 안에 들어온다. 되돌렸다._

**각도에 따라 갈리는 것을 각도 모르고 켤 수 없다.** 75°·110°만 보면 켤 만한데 45°가 섞이면
사용자는 어느 쪽인지 알 수 없다. → **돌린 시점의 사선은 `고치기`로 간다**(D-S18).
대조군(같은 시점의 모서리)은 평면 경로를 켜도 안 나빠진다 — 이 결정의 대상은 사선뿐이다.

**표에 `first`라는 자리를 만든 이유가 이것이다.** "첫 시점에만"이 **측정의 결론**임을
원장 인용으로 강제한다 — 안 그러면 다음 사람이 배선 누락으로 오해하고 켠다.

### D-S20. §6 면 — **A안(선만 유지)으로 간다. B안(면 생성)은 만들지 않는다**

계획서 §6은 "입체로 인식되는 순간 면이 생기는가"를 미결로 두고 **A로 먼저 만들고 판단하라**고
적었다. 판단한다 — **A로 간다.** 근거 넷이고, 셋은 이번에 확인했다.

**(1) 형태가 읽힌다.** 브라우저에서 상자를 돌려 보고 확인했다. 지면 격자 + 축 색 +
튜브 음영이 앞뒤를 잡아 준다. 선뿐이라 **속이 비쳐 보이지만 형태는 모호하지 않다.**

**(2) 가림은 이미 있다.** 계획서가 B를 요구한 이유가 "가림 처리"였는데, **획이 튜브 메쉬라
서로 가린다**(S-4). 앞뒤 세로 모서리를 다른 색으로 칠하고 픽셀을 세니 뒤 모서리가
**혼자 그릴 때 1568px → 앞 모서리가 있을 때 1097px**, 즉 **30%가 가려진다.**
B가 더할 수 있는 것은 **면에 의한 가림**뿐이고, 그것은 "속이 비쳐 보이는가"의 문제이지
"형태가 읽히는가"의 문제가 아니다.

**(3) 면은 사용자가 그리지 않은 것이다.** §1이 "시스템이 형태에 개입하지 않는다"이고
"안 하는 것: 획을 직선으로 펴기, 중간 점 이동"이다. 면을 지어내는 것은 **획에서 추론한
새 기하**를 화면에 올리는 것이고, 이 프로젝트는 같은 종류를 네 번 거절했다
(D-S14 면 생성 · D-S16 되돌리기 · D-S17 굽은 획의 평면 · D-S19 돌린 시점의 면).

**(4) B의 핵심 난점은 이미 답이 나와 있다.** 계획서가 스스로 적은 문제가
"곡선 획이 만드는 루프는 완전한 평면이 아니다"인데, **S-6 (3)이 그것을 정면으로 쟀다** —
굽은 획의 참 평면을 고르는 두 방법이 51%·62% 틀렸고 **오라클 바닥은 0.078**이었다
(답이 없는 게 아니라 못 고른다). 루프의 근사 평면도 같은 문제를 만난다.

**언제 다시 보는가**: 실획으로 복잡한 그림(획 수십 개)을 받았을 때 형태가 안 읽히면.
그때 필요한 것은 면이 아니라 **깊이 단서**일 수 있다(안개·깊이 페이드·그림자) —
그것들은 **그리지 않은 기하를 만들지 않으므로** §1과 충돌하지 않는다. DEFERRED에 적어 둔다.

_확인의 한계: **구도 하나·물체 하나**(상자)다. 획이 수십 개인 그림에서 같은지는 안 봤다._

### D-S21. 리뷰어가 S-8 판의 결론 셋을 좁혔다 — 그중 하나는 **새 위험**이다

리뷰어 18건 중 결론을 바꾼 것만 적는다. 나머지는 문서 정정이다.

**(1) ⚠ 돌린 시점의 사선은 "안 놓인다"가 아니라 "조용히 틀리게 놓인다".**
D-S19가 평면 경로를 안 켰으므로 "돌린 시점의 사선은 `고치기`로 간다"고 적었는데,
**그 진술이 미분류로 남은 사선(21~64%)만 보고 있었다.** 나머지는 축으로 배정된다.
지적을 받고 그 집단을 쟀다(`view_plane.json@503a75cb`):

| 궤도 | 사선 중 **축으로 배정** | 그중 놓인 비율 | 나쁜 쪽 끝 중앙 | 0.2 초과 |
|---|---|---|---|---|
| 45° | **0.788** | 0.747 | **0.640** | **139/139** |
| 75° | 0.364 | 0.407 | 0.581 | 35/35 |
| 110° | 0.462 | 0.327 | 0.984 | 36/36 |

**놓인 것이 전부 크게 틀린다.** 첫 시점의 같은 양은 15.8%·중앙 0.448이었고(D-S14),
돌린 시점은 **비율이 최대 5배**다. 즉 **돌린 시점 사선의 진짜 위험은 §4.3이 아니라 §4.1**이고,
D-S19가 평면 경로를 안 켠 것은 그 위험을 **줄이지도 늘리지도 않는다**(다른 경로다).

→ README·D-S19의 "돌린 시점의 사선은 `고치기`로 간다"를 **"미분류로 남은 것만 그렇다"**로
좁힌다. 이 위험 자체는 **실획 대기 항목에 넣는다** — 판정 임계를 건드리는 일이고
AS-13이 금지한다. **자동으로 줄일 방법이 지금 없다.**

**(2) "D-S17이 새로 뺀 것은 1.2%뿐"은 틀렸다.** 근거로 쓴 논증이 "꺾인 획은 평면 경로와
무관하다"였는데 **아니다** — 평면 경로의 대상은 **미분류 획 전체**이고 꺾인 획도 미분류다
(D-S3). 게다가 꺾인 획의 **11.7%는 `bend ≤ 0.18`이라 제한을 통과한다**.
바른 진술은 이것이다: **`freeBendMax`가 평면 경로에서 빼는 것은 획 전체의 60.25%**이고,
그중 88%가 꺾인 획이다. 매끄러운 획만 보면 1.2%다. **두 수를 다 적는다.**

**(3) D-S13의 "세 각도가 모두 해소된다"는 고각 35° 전용이다.** 이번 원장은 고각 18°에서
돌린 시점 모서리의 p90이 궤도 75°에서 **0.591**이다(`continue_after_rotate`의 0.051과 11배).
**고각이 다르면 결과가 다르다** — 두 원장의 조건이 그렇게 다른 줄 몰랐다.
D-S13·AS-16의 "0.05~0.075로 해소"에 **고각 35° 조건을 명시**하고, 고각 18°는 미해소로 둔다.

_그 밖에 고친 것_: `app` 팔이 `now`와 같은 구성이라 일치가 검증이 아니라 **항등**이다(원장에
그렇게 적었다) · "45°가 가림이 가장 심하다"는 **이번 측정에 없는 주장**이라 뺐다(같은 원장의
모서리 대조군은 45°가 가장 좋다) · "대조군은 안 나빠진다"는 75°에서 0.2 초과가 10→12로
늘므로 좁혔다 · 후보 반경 배수 3과 `same_view_px` 2를 **`PLACE_TOL`에 등록**했다(임계인데
빠져 있었다 — 상수 해시가 `503a75cb`로 바뀌었다).

### D-S22. 돌린 시점의 사선 오배정은 **투영의 애매성**이다 — 해결이 아니라 완화한다

산출: `view_plane.json@1671e540`. D-S21이 찾은 위험(궤도 45°에서 사선의 79%가 축으로
배정되고 놓인 것이 **전부** 크게 틀림)에 대응한다. 지시가 (a) 검출 (b) 미배치 (c) 보수적 판정
순으로 지시했고, **(a)와 (b)가 안 되고 (c)만 부분적으로 된다.**

**(a) 검출 — 안 된다.** 시도 넷을 다 쟀다.
- `requireFarEnd`(정합성 검사로 못 고른 앵커를 버린다): 45°에서 139 → 125.
  **75°·110°에서는 크다**(35 → 8, 36 → 13) — 그쪽은 자유단 경로가 통로였다.
- `ambiguity_margin` 1.5 → 3.0: 139 → 133. **거의 안 듣는다.**
- **각도 거부권**(부적합도를 각도로 환산해 상한): 5°·3°·2° 어디서도 **거의 안 걸린다.**
  _이것이 원인을 말해 준다 — **그 사선들은 축 소실점에 2° 안으로 맞는다.**_
- 깊이 타당성(이미 켜져 있다)·평면 경로 되돌리기: 앞서 안 들었다.

**원인은 판정이 관대해서가 아니다.** 화면에서 **소실점을 지나는 선**은 그 축과 구분되지
않는다 — 어떤 시점에서는 면 위 사선이 실제로 축 소실점을 지난다. **그 시점의 2D 증거만으로는
갈 수 없는 정보**이고, 임계를 어떻게 조여도 안 갈린다. 길이 탓도 아니다(오배정된 사선의
화면 길이가 미분류 사선과 같거나 더 길다).

_실패를 하나 기록한다: 각도 거부권을 처음에 **후보의 부적합도를 `Infinity`로 만들어** 걸었더니
오배정이 79% → **90%로 늘었다**. 경쟁자를 죽이면 상대 순위(`rank_margin`)가 거저 통과한다.
거름은 **선택 뒤에** 걸어야 한다 — 순위에 손대면 반대로 간다._

**(c) 보수적 판정으로 완화한다.** 돌린 시점에만 `vp_dist_ratio` 0.06 → **0.03**(`VIEW_AXIS_CFG`)
+ `requireFarEnd`. 지시가 "배치율이 떨어져도 139/139 오배치보다 낫다"고 했다.

| 궤도 | 크게 틀린 배치 | 모서리 배치율 | 모서리 오류율 |
|---|---|---|---|
| 45° | 139 → **99** | 0.780 → 0.458 | 0 → 0 |
| 75° | 35 → **7** | 0.653 → 0.246 | 13% → 7% |
| 110° | 36 → **13** | 0.689 → 0.294 | 1.2% → 0% |

**조용히 틀린 배치가 45°에서 29%, 75°에서 80%, 110°에서 64% 준다.** 대가는 크다 —
**돌린 시점의 모서리 배치율이 절반 아래로 떨어진다.** 그 획들은 미배치로 남고
`고치기`로 간다(D-S15·D-S18). 모서리의 **오류율도 함께 내려간다**(13% → 7%)는 점은 덧붙인다.

**이것은 완화이지 해결이 아니다.** 45°에서 99획이 여전히 크게 틀리게 놓인다.
**남은 길은 2D 밖의 정보다** — 가림 정합성(그린 획이 기존 기하 뒤로 가면 안 보였어야 한다)이
후보이고, 그것은 새 기전이라 DEFERRED에 둔다. **S-10 시나리오에 이 조건을 넣는다.**
