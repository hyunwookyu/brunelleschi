# 스케치-발화 기반 공간 확인 시스템 제작계획

작성 2026-08-11 / 버전 1.0

---

## 1. 목적

손으로 그린 스케치와 그때의 발화를 실치수 3D 볼륨으로 옮기고, **그린 시점에서 원본 위에 겹쳐 확인**한다.

검증할 명제는 하나다.

> 평면 스케치와 투시 스케치, 그리고 그리며 한 말이 하나의 중간표현으로 환원되고,
> 그로부터 생성된 기하가 원본 스케치와 겹쳐 확인 가능한가.

### 동기

이미 상상한 형태를 Rhino에서 좌표로 옮기는 작업이 반복 노동이다. 판단이 필요한 부분(코어, 법규, 프로그램)이 아니라 **확정된 형태를 옮기는 구간**만 대상으로 한다.

### 측정 대상

절감 시간이 아니라 **왕복 횟수**다.

- 세션당 형태 대안 검토 횟수 (손 모델링 대비)
- 첫 볼륨 확인까지의 시간
- 시스템이 채운 값 중 사용자가 뒤집은 비율

세 번째가 질의 루프의 튜닝 지표다. 낮으면 가정이 타당했고, 높으면 물었어야 할 항목을 가정한 것이다.

---

## 2. 범위

### 만든다

1. **카메라 정합** — 평면의 바닥 폴리곤을 투시 스케치에 맞춰 시점 복원
2. **스케일 앵커** — 발화 한 마디로 전체 절대 치수 확정
3. **수정 반영** — 평면을 고치면 투시 뷰포트가 따라오는 양방향 갱신

기하 생성은 압출 수준. 새로 개발할 것이 없다.

### 만들지 않는다

메쉬 생성, 형태 추론, 코어·그리드, 법규 검토, 주차, 프로그램 배치, 대지 데이터 연동, 스타일 학습.

이미 있거나(생성형 3D), 필요 없거나, 다음 단계다.

### 선행 도구 확인 (착수 전 필수, 1일)

본인 스케치 3장을 Tripo/Rodin 등 생성형 도구에 넣고 확인한다.

- 나온 메쉬를 Rhino에 가져와 원본 시점으로 카메라를 맞출 수 있는가
- 치수가 의미를 갖는가
- 평면 스케치와 대응시킬 수 있는가

**결과 예상**: 메쉬는 나오지만 카메라가 없고 치수가 없다. 확인되면 범위가 위 세 항목으로 확정된다.

---

## 3. 아키텍처

### 3.1 원칙

**스케치에서 메쉬를 복원하지 않는다.** 스케치 → 중간표현(IR) → 기하의 3단계로 분리한다.

- LLM은 기하를 만들지 않는다. IR만 만든다.
- 기하는 결정론적 솔버가 만든다.
- 이 분리가 시스템 전체의 성패를 가른다.

### 3.2 IR 스키마 (초기 최소형)

```json
{
  "volumes": [
    {"id": "hall", "footprint": [[x,y],...],
     "base": 0.0, "height": 9.0,
     "height_src": "utterance", "confidence": 0.8}
  ],
  "anchors": [
    {"target": "hall", "prop": "width",
     "value": 12.0, "tol": 1.2, "src": "utterance:t=142.3"}
  ],
  "views": [
    {"src": "sk_03", "camera": {...}, "fit_error": 0.06}
  ],
  "unresolved": ["deck 높이", "lobby 개구부 위치"],
  "notes": [{"target": "hall", "text": "..."}]
}
```

`unresolved`가 핵심 필드다. 추정하지 않고 남겨두는 항목이며, 질의 루프의 대상이자 대안 생성의 축이 된다.

`relations`는 1차 범위에서 제외한다. 필요가 확인되면 추가.

> **개정(2026-08-12, 5단계)**: relations 추가됨(루트 6키). 유형은 유튜브 22자막 실측으로
> 계획 4종(인접·상하·관입·이격) + **aligned(정렬, 13회)** = **5종**. `Relation{a,b,type,src}`.
> src=geometry|utterance. 상세 `ir/schema_version.md`.

### 3.3 채널별 권한

각 채널이 무엇을 주장할 자격이 있는지 고정한다.

| 채널 | 절대치수 | 비례 | 관계 | 시점 |
|---|---|---|---|---|
| 손으로 쓴 숫자 | ○ | — | — | — |
| 발화 (확정) | ○ | — | ○ | — |
| 발화 (완화) | 범위 | — | ○ | — |
| 평면 스케치 | × | ○ | ○ | — |
| 투시 스케치 | × | △ | ○ | ○ |
| 다이어그램 | × | × | ○ | — |

**투시도는 치수 채널에서 제외**한다. 원근 왜곡 때문에 비례도 신뢰할 수 없다. 관계와 시점만 받는다.

**평면은 비례만 준다.** 절대값이 없으므로 앵커 하나가 들어오면 전체가 확정된다.

### 3.4 충돌 처리

우선순위는 위 표의 순서. 낮은 쪽이 양보하되, **완화 발화는 범위 안에서 먼저 조정**하고 범위를 벗어날 때만 충돌로 보고한다.

### 3.5 치수 표시

오버레이에서 톤을 구분한다.

- 명시 치수 — 실선, 숫자 표기
- 범위 내 확정 — 실선, 숫자에 `~`
- 비례 전파 — 파선

한 번 보면 어디를 더 말해야 하는지 알 수 있다. 질의 없이 표시만으로 해결되는 부분.

**앵커가 하나도 없으면 단위 없이 세운다.** 기본값을 넣지 않는다. 임의 가정은 나중에 실제 치수와 충돌하고, 그때는 이미 그 가정 위에서 판단을 한 뒤가 된다.

### 3.6 발화 번역

조사·어미 사전을 만들지 않는다. LLM이 발화를 읽고 IR 조각으로 번역한다. 입력은 무한하고 **출력은 닫혀 있어야** 한다.

```
set(id, prop, value)
range(id, prop, min, max)
min(id, prop, value)
relate(a, b, type)
retract(target)
unresolved(id, prop)
note(id, text)
```

일곱 개 조합으로만 출력. 스키마 검증 실패 시 재시도.

**번역 결과를 표시한다.**

```
"홀 폭이 한 12미터쯤"  →  range(hall, width, 10.8, 13.2)
```

틀렸으면 그 줄만 고친다. 이 정정이 학습 신호가 된다.

**프롬프트 입력**: 현재 IR 전체 + 발화 시점 ±3초의 획 + 직전 몇 턴. IR을 통째로 넣어야 "그것보다 조금 크게"의 지시가 해소된다.

**세션 후 일괄 번역.** 실시간 파싱하지 않는다. 뒤의 발화가 앞의 해석을 고치므로, 전사문 전체를 한 번에 읽어야 정정이 반영된다. 세션 중에는 획 기반으로만 모델을 갱신하고 발화는 기록만 한다.

### 3.7 어휘 수준 전제

건축가는 "답답하지 않게"가 아니라 "여기 개구부"라고 말한다. **추상→건축 번역은 필요 없다.**

필요한 것은 건축→기하 확정이다. "개구부"에는 위치·크기·높이가 없다.

```
"여기 개구부"  →  place(wall_3, opening, pos=?, w=?, h=?)
                 →  unresolved 3건
```

LLM의 역할은 어휘 해석이 아니라 **미지정 항목 식별**이다. `note`는 예외 처리이며 로그 용도. 쌓이지 않으면 스키마가 맞았다는 증거다.

**이 전제는 코퍼스로 1차 확인한다.** 다만 유튜브 발화는 카메라 앞의 설명이라 정연한 쪽으로 치우친다. 혼자 그리며 말할 때는 상대가 없어 느슨해질 수 있다.

→ 코퍼스에서 얻은 분포를 상한으로 보고, `note` 처리 경로를 열어둔 채 시작한다. 실사용에서 `note`가 쌓이면 번역 층 보강이 필요하다는 신호다.

### 3.8 투시 복원 — 입력은 투시뿐이다

_전면 개정 2026-08-14 (지시 2.6). 구판의 "평면 폴리곤 4점 ↔ 투시 4점" 전제를 폐기한다._

**사용자는 평면을 그리지 않는다.** 입력은 투시 스케치뿐이며 1점~3점이다.
따라서 평면은 입력이 아니라 **파생 출력**이다 — 투시에서 바닥면을 검출하고 Z=0으로
역투영한 것이 평면이다(`stage_perspective/to_ir.py`). §3.2의 평면 프레임은 입력을 받지
않고 이 복원 결과를 표시·편집한다.

#### 3.8.1 하나의 이론, 소실점 개수로 나뉠 뿐

이론서 2.3: 1·2·3점은 별개 이론이 아니라 직육면체 세 주축 중 화면에 평행한(c=0) 축이
몇 개냐의 차이다. 분기 구현을 두지 않고 **유한 소실점 개수**로만 나눈다
(`stage_perspective/camera_unified.py`). 자유도 회계는 이론서 5.3:

| 유한 소실점 | 남는 자유도 | 처리 |
|---|---|---|
| 3개 | 없음 | 주점=소실점 삼각형 수심(6.3), f²=\|PVᵢ\|·\|PHᵢ\|(6.4). 세 조합 잔차가 신뢰도. **둔각이면 f²<0 → 결정적 무효**(6.5) |
| 2개 | 주점 x 또는 f | f²=\|PV₁\|·\|PV₂\|(6.2). **주점=이미지 중심 가정**(16.2)으로 f 결정 — 가정을 IR에 명시 기록 |
| 1개 | f | 소실점=주점. **깊이 미결정.** f 경로: ⓐ타원 d=(A/B)√(ab)(8.7) → ⓑ사다리꼴 정사각 가정 d=2/[s(1/b′−1/a′)](8.8, 게이트 통과 시) → ⓒ발화·수동 입력 → ⓓ다중 뷰 → ⓔ unresolved |
| 0개 | — | 축측 극한(2.4, d→∞). 투시로 깊이를 얻을 수 없다 |

#### 3.8.2 실척이 공짜인 방향 (7.7)

화면에 평행한 방향은 축소가 없으므로 **카메라 복원 전에도 비례가 확정**된다.

| | 실척 직접 |
|---|---|
| 1점 | 폭, 높이 |
| 2점 | **높이만** |
| 3점 | 없음 |

앵커 하나가 들어왔을 때 어느 축까지 즉시 확정되는지가 이 표로 갈린다.
**높이는 축 라벨 모호가 없다**(§3.8.4와 맞물림).

#### 3.8.3 무엇이 얼마나 복원되는가 (실측, 지시 0.4·1-ter A)

수치는 **실측 코너 오차**(precise 1.68% / medium 2.69% / coarse 4.21% of 도형 대각,
`stage0/12_corner_error.py`) 기준이다. 시드 변동폭이 ±0.01~0.02이므로 **두 자리까지만 유의**.
plane_iou는 종횡비 오차에 둔감하므로(1-ter A) **종횡비 상대오차를 반드시 병기**한다.

| 경로 | plane_iou (P/M/C) | **종횡비 상대오차 (P/M/C)** | 판정 |
|---|---|---|---|
| 대응 있음(2A/2C) | 0.97 / 0.95 / 0.94 | **0.02 / 0.03 / 0.03** | 견고 |
| 직각가정·단일 직사각(2F) | 0.87 / 0.81 / 0.78 | **0.06 / 0.13 / 0.15** | 성립하되 10%대 오차 감수 |
| 직각가정·비직교 | 0.66 / 0.61 / 0.65 | 0.30 / 0.28 / 0.32 | 붕괴 |
| **복합 직교폴리곤 분해(2H)** | L 0.49/0.45, U 0.43/0.19, 계단 0.29/0.22 | L 0.05/0.15, U 0.31/0.92, 계단 0.47/1.27 | **폐기** |

**2H 폐기**(구 "L자는 L1에서 IoU 0.92"는 파서가 낼 수 없는 코너 정밀도 0.48%에서만 나오던 값).
L자는 **외곽 비례는 복원되나(종횡비 오차 5~15%) 내부 형태가 틀린다**(IoU 0.45~0.49).
건축 용도에 필요한 것은 평면 형태이므로 쓸 수 없다. U·계단은 두 지표 모두 붕괴.

#### 3.8.4 2점 단독으로는 종횡비가 확정되지 않는다 (지시 1-bis)

실측 코너 오차를 종횡비까지 전파하면(몬테카를로, `stage_perspective/uncertainty.py`)
**90% 구간의 상대 폭 중앙값이 0.894**다. 종횡비 1.5면 대략 [1.0, 2.4]다.
구간은 잘 보정돼 있다(커버리지 0.94 @ CI 0.90).

- 이 실패는 **사전 판정이 불가능**하다. 기하적 서명이 없기 때문이다(무노이즈에서도 f/W가
  종횡비와 무관하게 1.000). 구간 폭조차 개별 실패를 예측하지 못한다(AUC 0.496).
  → 게이트를 두지 않고 **구간으로 표현**하고 unresolved에 올린다.
- **축 라벨(폭/깊이)도 기하가 정하지 못한다.** `rect(a)`와 `rect(1/a)`는 90° 회전한 같은
  도형이라 8way 대응 탐색이 두 해를 동점으로 본다. 실측 뒤바뀜 **28.75%**, 그 경우
  평면 IoU 0.527(정상 0.838) — **최대 식별 가능 오차원**이다. 추정하지 않고 두 해를 기록해
  질의한다(§6, `Volume.axis`).

#### 3.8.5 함의 시거리 게이트 — 1점 형태가정 전용 (지시 1)

형태 가정에서 d를 역산하는 경로에서만 게이트가 정보를 갖는다(가정이 틀린 만큼 d가 틀리므로).
2점 경로에는 **적용되지 않는다**(f가 소실점만으로 결정돼 형태 가정과 무관).

| 시거리 | 판정 |
|---|---|
| d ≥ 0.87W | 화각 60° 이내 — 신뢰 |
| d ≥ 0.5W | 60~90° — 경고 |
| d < 0.5W | 90° 초과 — 신뢰 불가 |
| **d > 3W** | **시점 과다 원거리(화각 19° 미만) — 프로젝트 추가 휴리스틱**(18.4는 광각 단측만 다룸) |
| d ≤ 0 / 발산 | 무효 |

실측(1점 형태가정, n=600): 하한+상한 병용 시 recall 0.477, **precision ≈0.998**.
단측이면 절반을 놓친다 — 가정보다 깊은 쪽은 하한이(51.7%), 얕은 쪽은 상한이(39.8%) 잡는다.

#### 3.8.6 원리적 한계 (16.4)

> 한 장의 사진에서 3차원 형태를 완전히 복원하는 것은 불가능하다. 코엔데링크의 회화적 부조
> 연구에 따르면 지각되는 3차원 형태는 관찰자마다 다르며 **깊이 방향의 아핀변환(전단+스케일)
> 만큼의 자유도가 남는다.**

즉 **그림은 3차원 형태를 결정하지 않고 일정 범위까지만 제한한다.** 위의 미확정 항목들은
구현의 미비가 아니라 이 한계의 구체적 형태다. 부족분은 추정하지 않고 unresolved로 남겨
질의·추가 뷰·치수 입력으로 메운다(§3.5 "앵커 부재 시 무차원"의 확장).


### 3.9 확인 방식

- **투시** — Rhino 뷰포트를 추정 카메라로 세팅, 모델을 원본 스케치 위에 반투명 중첩
- **평면** — Top view 정사영, 좌표계 동일하므로 별도 처리 없음
- 두 뷰가 같은 IR을 본다. 평면에서 벽을 옮기면 투시 뷰포트도 움직인다.

**오버레이는 어떤 압축에서도 제거하지 않는다.** 이게 없으면 결과가 맞는지 알 방법이 없고, 시스템 자체가 무의미해진다.

---

## 4. 입력 계층

### 4.1 1차 — Apple Pencil / 웹

iPad 앱을 만들지 않는다. 웹으로 간다. Pointer Events에서 `pressure`, `tiltX`가 나오고 iPad 사파리에서 동작한다. 앱 개발·배포 과정이 빠진다.

**스트로크 포맷**

```json
{"points": [[x, y, t, p]], "pen": "id", "frame": "id"}
```

InkML은 필드가 과하다. 자체 JSON으로 시작하고 필요하면 나중에 익스포트를 붙인다.

### 4.2 2차 — 종이 (Neo smartpen / Ncode)

_개정 2026-08-13 (하드웨어 조사 반영, 조사 출처: 사용자). 아래 §4.2a 폐기 경로 포함._

최종 목표는 종이다. 이식 비용은 거의 없다 — 입력 계층만 교체되고 정규화 이후는 공통.

**Neo smartpen(Ncode)을 2차 입력 계층으로 확정한다.**

**확정 근거**
- 현재 판매 중: A1 / R1 / dimo / Lamy Ncode.
- **WEB-SDK2.0이 GitHub 공개**, 실시간 **점 단위 콜백**(`PenHelper.dotCallback`) 제공.
- 현재 아키텍처가 전부 웹 기반(지시서 V: 파서·뷰어 브라우저 이식 완료)이므로 **네이티브 앱 없이 브라우저에서 직접 연결**된다. 입력 계층만 교체하면 정규화 이후는 공통.

**얻는 것**
- 120FPS 이상, 256단계 필압
- Ncode 2mm×2mm 패턴에 페이지·매체 식별자 인코딩 → 프레임 태깅과 세션 ID가 종이에 내장
- 종이 좌표가 절대 주소 → 촬영·원근보정·정합 불필요

**잃는 것 / 제약**
- **호버 없음.** 지시어("여기", "이쪽") 해소 채널이 사라진다.
  → 우회: 발화 시점 직전·직후의 획 위치로 근사. 설명하며 그 부분을 덧그리는 동작이 자주 나오므로 실용상 충분할 것.
- 기울기 없음. 필압만으로 확정선 판별은 가능.
- **Ncode 전용 종이 필요** (자가 인쇄 가능).
- **SDK 라이선스 GPL-3.0** — 배포 시 검토 필요(현재는 사내 프로토타입이라 미해당).

**도입 시점: V-8 이후 유지.** 지시서 V 프로토타입(마우스·펜 웹 경로) 완결 후.
tolerance는 V-5b에서 **입력 장치별 세트가 불필요**함이 실획으로 확인됐다(단일 세트 평탄 최적) —
Ncode 도입 시에도 tolerance 재튜닝보다 **실획 자기정합 IoU로 먼저 검증**한다.

#### 4.2a 폐기 — EMR 스마트패드 (Wacom Bamboo Slate/Folio)

**폐기 확정 (2026-08-13).** 전용 종이가 불필요하다는 것이 유일한 장점이었으나 무의미해졌다.
- 동반 앱 **Inkspace가 2025-12-18자 EOL**, 앱스토어에서 제거(2026년 1월 말로 연기).
- 기기 재고는 남아 있으나 **앱 없이 사용 불가** → 데이터 취득 경로 자체가 없다.
- 대안 경로(자체 BLE 프로토콜 역설계)는 비용·불확실성이 Ncode 대비 열위.

### 4.3 펜 채널

펜 색/종류를 의미 채널로 배정한다. 이미 하는 행위라 학습 비용이 0이고 분류 문제 하나를 제거한다.

- 검정 = 매스 윤곽
- 빨강 = 동선
- 파랑 = 축·그리드
- 초록 = 주석

영역 분할보다 효율이 높다. 둘을 같이 쓰면 남는 인식 과제는 투시 기하 복원 하나로 좁혀진다.

---

## 5. 정규화

### 5.1 재구현하지 않는다

| 항목 | 활용 |
|---|---|
| 프리미티브 인식 | PaleoSketch (8종, 98.56%) |
| 정규화 파이프라인 | FEAsy (리샘플링→분절→인식→병합→제약해결) |
| 제약 솔버 | SolveSpace solver 또는 FreeCAD planegcs |
| 카메라 정합 | fSpy 참조 + OpenCV findHomography/solvePnP |
| IR→기하 | Rhino MCP (기존 자산) |

알고리즘을 그대로 구현하면 5주 항목이 2주로 준다.

### 5.2 확정을 미룬다

개별 획을 그리는 시점에 직선으로 스냅하면, 나중에 전체 맥락상 곡선이었다는 판단이 불가능해진다.

```
raw stroke → primitive 후보 (line/arc/spline, 각각 score)
           → 제약 추론 (평행/직교/일치/대칭, tolerance 내)
           → 전역 제약 솔버
```

단, **검증용 1차 구현에서는 즉시 스냅해도 명제 확인에 지장 없다.** 후보 유지는 2차로 미룬다.

### 5.3 register 판정

지렁이 스케치와 작도 스케치를 하나의 tolerance로 처리할 수 없다. 전자에 맞추면 후자의 미세한 각도 차이를 뭉개고, 후자에 맞추면 전자가 파싱되지 않는다.

**세션 또는 프레임 단위로 정밀도 등급을 먼저 판정하고 등급별 tolerance를 적용한다.**

- 등급 판정 자체가 획 통계로 된다 (선 직진성 분산, 그리기 속도, 덧그림 비율)
- 유튜브 코퍼스에서 등급별 분포를 각각 추정
- 실사용 시 첫 몇 획으로 등급 결정

**파싱 가능 경계도 여기서 나온다.** 어느 등급 이하는 정보량 부족으로 어떤 알고리즘으로도 기하가 안 나온다. 경계를 알면 "이 스케치는 기하로 못 바꾼다, 관계만 받겠다"고 응답할 수 있다. 실패가 아니라 정상 동작이다.

---

## 6. 데이터

**원칙: 만들지 않고 가져온다.** 영상에서 획을 추출하는 파이프라인은 폐기했다. 필요한 데이터가 이미 공개되어 있고, 표본 규모가 자체 수집보다 네 자릿수 크다.

### 6.1 Quick, Draw! — 프리핸드 노이즈 분포

345개 범주 5,000만 장, 1,500만 명 이상이 그린 타임스탬프 벡터. 원본 포맷이 획별 `[[x...],[y...],[t...]]`로 계획의 스트로크 포맷과 그대로 대응한다.

- 사용 범주: `house`, `square`, `line`, `triangle`, `door`, `window`, `skyscraper` 등 기하·건축 계열
- 추출: 직선 편차, 모서리 닫힘 간격, 획 속도, 축 기울기 분포
- **개발자 개인 필체가 아니라 다수 인구의 분포**를 얻는다. 일반화 요건에 직접 대응

한계: 20초 제한, 마우스·손가락 입력 혼재, 필압 없음. **등급 스펙트럼의 거친 쪽 상한**으로 쓰고 정밀한 쪽은 6.2로 채운다.

### 6.2 SketchGraphs — 정규화 정답 라벨

실제 CAD 모델에서 추출한 1,500만 스케치. 각각이 기하 제약 그래프(노드=프리미티브, 엣지=설계자가 부여한 관계)로 표현되며, 구성 방식이 정답으로 제공된다.

```
SketchGraphs 제약 그래프 → 렌더링 → 프리핸드 노이즈(6.1 분포) 부여
                        → 파서 → 복원된 제약 그래프
                        → 원본과 대조
```

무작위 합성이 아니라 **실제 설계 의도가 담긴** 정답이다. 기계 부품 스케치라 건축 평면과 도메인이 다르나, 건축 평면은 직교 폴리곤 위주라 부분집합에 가깝다. 제약 종류가 과잉일 뿐 부족하지 않다.

전처리본 사용. 원본 JSON은 43GB로 불필요하다. 원저작권은 Onshape 사용자에게 있으며 연구 목적 표준 데이터셋이다.

### 6.3 유튜브 — 자막만

Quick,Draw에도 SketchGraphs에도 음성이 없다. 발화 층은 여기서만 얻는다.

**영상을 받지 않는다.** 자막 API로 전사문만 수집한다. 타임랩스 판정, 카메라 안정성 검사, 프레임 차분, 세선화가 전부 불필요.

- 발화 유형 분포 (명명·관계·치수·정정·의도·미정의 실제 비율)
- 발화-획 시간 지연 (정렬 윈도우 근거)
- 정정 표현 목록
- 번역 프롬프트에 넣을 실제 발화 예시 20개

한국어 검색어 비중을 높인다. 파싱 대상이 한국어인데 코퍼스가 영어뿐이면 3.7절 확인이 안 된다.

### 6.4 v1에는 학습이 없다

1단계 파서는 전 구간이 결정론적이다.

```
획 → Douglas-Peucker → 선분 적합 → 축 클러스터링 → 직교 스냅
   → 제약 추론 → SolveSpace 솔버 → 폴리곤
```

PaleoSketch도 FEAsy도 ML이 아니다. **조정 대상은 tolerance 스칼라 10개 남짓뿐이다.**

10개 숫자에 판별기 테스트, 합성 생성기, 자동 반복 루프는 과설계다. 6.1 분포에 그리드 서치를 돌리면 몇 시간에 끝난다.

**합성 루프와 판별기 검증은 5단계(관계 추론) 이후로 미룬다.** 그때는 학습할 것이 실제로 생긴다.

### 6.5 검증

| 대상 | 방법 |
|---|---|
| 정규화 | SketchGraphs 홀드아웃 대비 제약 그래프 일치율 |
| 노이즈 모델 | 합성 획 vs Quick,Draw 획 통계 이표본 검정 |
| 카메라 정합 | 재투영 오차 (fit_error) |
| 시간 단조성 | IR(t+1) ⊇ IR(t) |
| 발화 번역 | 자막 치수 언급 vs 파싱된 anchors |

전부 라벨 작성 없이 수치가 나온다. 개발자 개인 데이터로 판정하는 항목이 없다.

**최종 판정은 런타임으로 이관된다.** 결과가 의도와 맞는지는 오버레이로 각 사용자가 확인한다.


### 6.6 본인 세션 — 보류

1차 범위에서 제외한다. 6.1의 등급 전체 범위를 그대로 지원하고, 실사용 시 register 판정으로 등급을 고른다. 개발자 대역을 미리 특정하지 않아도 시스템이 성립한다.

나중에 넣을 때의 조건: 실제 진행 중인 과제 / 20~30분 / 평면과 투시를 같은 세션에 / 한 세션은 종이에.

---

## 7. 일정

증분 방식. 며칠짜리를 먼저 세우고 붙여나간다. 스키마가 무엇을 담아야 하는지도 한 번 돌려봐야 안다.

### 0단계 — 착수 전 (1일)

- [ ] Quick,Draw 기하 범주 다운로드 → 등급 클러스터링 → 노이즈 파라미터 (2시간)
- [ ] SketchGraphs 전처리본 다운로드 + 렌더러 (3시간)
- [ ] 유튜브 자막 수집 (API만) → 발화 유형 분포, 정렬 윈도우, 번역 예시 20개 (2시간)
- [ ] tolerance 그리드 서치 하네스
- [ ] IR 확정

**영상 처리 코드 0줄.** 전 항목 위임 가능하다.

### 1단계 — 평면 → 압출 (3일)

잉크 캡처 → 직교 스냅 → 폴리곤 → 고정 높이 압출 → Rhino → Top view 오버레이

발화 없음, 투시 없음, IR은 폴리곤 리스트. 이 상태로 그려보면 tolerance가 얼마여야 하는지, 본인 필체의 어떤 습관이 문제인지가 하루 만에 드러난다.

### 2단계 — 발화 스케일 앵커 (3일)

Whisper → 시간 정렬 → LLM 번역(7문법) → 치수 확정 → 비례 전파

### 3단계 — 투시 카메라 정합 (1~2일)

4점 대응 전수 탐색 → findHomography → solvePnP → 뷰포트 세팅 → 중첩 표시

**여기까지가 목표 항목의 완성. 누적 7~8일.**

### 4단계 — 볼륨 다중화, 명명 (3일)

### 5단계 — 관계 추론 (5일)

### 6단계 — 질의 루프 (5일)

### 7단계 — 정정 학습 (미정)

파서가 안정된 뒤에 켠다. 흔들리는 상태에서 규칙을 쌓으면 그 규칙이 버그를 고착시킨다. **기록은 1단계부터 남긴다** — 비용이 거의 없고 소급 적용된다.

---

## 8. 판정 기준

착수 전에 확정한다. 나중에 쓰면 나온 결과에 맞춰 기준이 움직인다.

| 항목 | 기준 |
|---|---|
| 오버레이 어긋남 | 획의 __% 이하 (등급별 직선 편차 중앙값 기준) |
| 카메라 정합 실패 | fit_error > __ 이면 실패 선언, 근사 모드 전환 |
| 수정 왕복 | __회 안에 원하는 형태 도달 |
| 전체 실패 | 수정 시간이 손 모델링 시간을 넘으면 실패 |

**중단 조건**: 2주 안에 1단계(평면→압출→오버레이)가 성립하지 않으면 접근을 재검토한다.

---

## 9. 위임 구분

### Claude Code에 넘긴다

잉크 캡처·직렬화 / 정규화 알고리즘 구현 / SolveSpace 바인딩 / 호모그래피 카메라 정합 / Whisper 연동·시간 정렬 / Rhino MCP 출력 / 오버레이 렌더링 / 데이터셋 수집·전처리 / tolerance 그리드 서치 / 테스트 하네스

### 넘길 수 없다

IR 스키마 설계 / tolerance 튜닝 / 발화 유형 분류 기준 / 통합 판단 / **정규화 결과가 의도와 맞는지의 최종 판정**

사양이 없어서 위임이 안 되는 종류다. 사양을 쓰는 순간 작업의 대부분이 끝나 있다.

### 자동 루프의 전제

6.5절 검증 지표가 수치로 나와야 "좋아졌는지 나빠졌는지" 판정이 되고, 그래야 방치가 성립한다.

단 v1에서는 방치할 대상이 tolerance 10개뿐이라 그리드 서치로 끝난다. 자동 루프가 실제로 필요해지는 것은 5단계 이후다.

---

## 10. 위험

**IR이 사후적으로 자란다.** 기능을 붙일 때마다 필드를 추가하면 5~6단계에서 일관성이 무너지고 전면 재작성이 온다. 개인 프로젝트의 가장 흔한 실패 지점.
→ 매 단계 종료 시 스키마를 문서로 고정. 이 문서가 다음 단계의 사양이 된다.
→ SketchGraphs 홀드아웃 일치율을 단계마다 재측정. 스키마 변경 후 떨어지면 잘못 자란 것.

**합성 노이즈가 실제와 다르면** 파서가 합성에만 맞춰진다. 자동화의 전부가 여기 걸린다.
→ 2~3회 반복마다 실측 1세션 (10~15분).

**모델링이 하던 역할의 소실.** 스케치는 미정으로 둘 수 있는 것을 모델링은 강제로 결정하게 만든다. 그 강제가 형태를 바꾸는 일이 실제로 자주 생긴다. 시스템이 조용히 채우면 그 계기가 사라진다.
→ 채우되 무엇을 채웠는지 표시하고 되묻는다. 질의 루프를 유지하는 이유.

**범위 확장.** 오피스에서 되니 주거도, 코어도, 법규도 — 이 판단이 나오는 순간 기간이 배로 는다.

---

## 11. 선행 연구 대비 위치

**가져다 쓰는 것**
- Quick,Draw (프리핸드 노이즈), SketchGraphs (제약 그래프 정답), fSpy (카메라 보정), SolveSpace/planegcs (제약 솔버), PaleoSketch·FEAsy (정규화 알고리즘)

**있지만 목적이 다른 것**
- 생성형 스케치→3D: 메쉬는 나오나 카메라·치수·편집성이 없다
- CAD 복원 (Free2CAD, Sketch2CAD): 편집 가능하나 대상이 기계 부품, scaffold 규범 요구
- 건축 상용 (Snaptrude, Finch3D): 입력이 이미 디지털 매스거나 프로그램표
- 발화+스케치 (Adler & Davis 2007, SketchGPT UIST'25, TalkSketch): 이미지 생성이나 범용 LLM 상호작용으로 끝남

**없는 것**
- 발화를 건축 IR의 제약으로 인코딩해 기하 솔버로 넘기는 경로
- **그린 시점에서 본 모델을 원본 위에 겹쳐 확인하는 것**

두 번째가 이 프로젝트의 유일한 질문이다.

---

## 12. 착수

0단계 전 항목을 위임한다. 데이터셋 세 개를 받아 통계를 뽑고 tolerance를 정하는 작업이며, 영상 다운로드도 프레임 처리도 없다.

유튜브 자막 수집용 시드:

```
architecture design process sketch explained
건축 설계 스케치 과정 설명
architectural studio critique full recording
esquisse architecture croquis conception
建築 エスキス スケッチ 過程
architecture sketch tutorial perspective plan
design charrette architecture live
건축 설계 크리틱 녹화
architectural concept sketch walkthrough narrated
sketching architecture lecture demonstration
```

본인이 개입하는 지점은 두 곳이다.

1. IR 확정 — 0단계 말
2. 1단계 오버레이 결과 확인 — 실제로 그려보고 tolerance 판정

그 외에는 개입하지 않는다.

---

## 13. 자율 운용 규칙

### 전이 판정

| 전이 | 판정 | 자동 |
|---|---|---|
| 정규화 보강 여부 | SketchGraphs 일치율 임계 | ○ |
| tolerance 수렴 | 그리드 서치 최적값 | ○ |
| 0 → 1단계 (IR 확정) | 아래 승격 규칙 | ○ |
| 1 → 2단계 | 오버레이가 의도와 맞는가 | × |
| 2 → 3단계 | 치수 전파 정확도 | 부분 |
| 3 → 4단계 | **외부 데이터셋 IoU 게이트**(아래 개정) | ○ |

### IR 필드 승격 규칙

- `unmappable` 집계에서 **3회 이상** 반복 → 필드로 승격
- 1~2회 → `note` 처리, 목록 보존
- 필드 총수 **12개 초과 시 멈추고 보고**

판단이 아니라 계산이 되도록 숫자로 고정한다. 잘못되면 스키마 문서 수정으로 되돌린다.

### 진행 규칙

```
각 단계 완료 시:
  score.py 실행 → progress.md 기록
  임계 통과 → 다음 단계
  미통과 → 3회까지 자동 수정, 실패 시 멈추고 보고

사람 확인 필요 항목 → checkpoints.md에 누적
  확인을 기다리지 않고, 확인 없이 진행 가능한 작업을 계속한다
```

2단계(발화 앵커)와 3단계(카메라 정합)는 1단계 출력 형식만 알면 병렬 개발된다. 오버레이 확인이 밀려 있어도 진행한다.

### 멈춤 조건

- IR 필드 12개 초과
- score.py 수치가 이전 단계 대비 하락
- 같은 오류로 3회 실패
- **4단계 진입 시점 — 외부 데이터셋 IoU 게이트 통과 없이 넘어가지 않는다** (2026-08-12 개정)

**게이트 개정(구: 오버레이 육안 확인)**: 정규화가 어긋난 상태 위에 관계 추론과 질의 루프를
쌓으면 되돌릴 것이 많아지므로 4단계 진입 전 검증은 유지하되, 주관적 오버레이 육안 확인 대신
**외부 실인간 데이터셋(TU-Berlin, Quick,Draw와 독립) 직교 형태의 IoU를 합성 예측밴드와 대조**하는
객관 게이트로 대체한다. 밴드 안이면 통과, 벗어나면 §6.5(노이즈모델 재검토) 복귀.
구현 `stage0/08_external_validation.py`, 통과 기록 progress.md. cp1_overlays.html은 참고 자료로 남기되 게이트에서 제외.

### 단계 게이트 이력 (2026-08-12)

- **4단계 진입**: 외부 IoU 게이트(위) 통과 — 완료.
- **5단계 진입**: 관계 어휘 종수 확정(코퍼스 5종) + 부분획 검증(크래시0) — 완료. relations·openings 필드 추가(루트 7).
- **6단계 진입**: 단조성 불변식 재정의(confidence≥0.5만 철회금지) + 표시정책(confidence 흐림) + opening typed 승격 — 완료. 질의 루프 본체(분기영향도 결정론적).
- **7단계(정정 학습) 진입 — 정지**. 파서 안정 후 켠다(§7). 실세션 로깅(§14) 데이터 필요:
  질의 임계(ASK_THRESHOLD 잠정 0.15)의 '뒤집힌 비율'(§1) 튜닝 + 정정유형(오독/설계변경) 실데이터.
  단조성은 재정의됨: **IR(t+1) ⊇ IR(t)가 아니라, confidence≥0.5 볼륨만 철회 금지**(나중 정보로 저confidence 추론 철회는 정상).

### 예상 방치 구간

0단계 전체 + 1~3단계 구현. 3~4일.

---

## 14. 지속 수집·갱신 (2026-08-12 추가)

시스템이 실사용될수록 실측 분포·수정 패턴·미매핑이 쌓인다. 이를 체계적으로 기록해
노이즈모델·tolerance·규칙층을 갱신하는 파이프라인의 **사양**을 여기 고정한다. 단
**구현은 7단계**(정정 학습)이며, **지금은 로깅 스키마만 확정**하고 기록만 시작한다.

### 14.1 수집 대상

| 항목 | 내용 |
|---|---|
| 획 | 정규화 전 원 스트로크(포맷 §4.1), 프레임·펜채널 |
| IR before/after | 각 연산 전후 IR 스냅샷(시간 단조성 §6.5 추적) |
| 수정 유형 | **오독**(파서가 틀림) vs **설계 변경**(사용자 의도 변경) 구분 라벨 |
| 발화 | 원문·번역(7문법)·정정 표현(§3.6) |
| unmappable | 미매핑 항목과 정규화 키(§13 승격 집계) |
| 지표 | score.py 산출(f1/IoU/앵커오차/fit_error 등) |

### 14.2 원칙

- **옵트인 기본꺼짐.** 수집은 명시적 동의 없이는 비활성.
- **기하 익명화.** 좌표 정규화(스케일·이동 제거) + 라벨 해시. 원 치수·위치 미보존.
- **로컬 우선.** 기본 로컬 저장. 외부 전송은 별도 결정.
- **발화는 별도 동의.** 음성/전사는 기하와 분리된 추가 동의 게이트.

### 14.3 갱신 주기

- **노이즈모델·tolerance**: 배치 갱신(축적분으로 재튜닝 후 교체).
- **규칙층**: 온라인 갱신 허용하되 **가역**(롤백 가능, 규칙 버전 태깅).
- **가중치(학습)**: 데이터 충족 후에만. 조기 학습 금지(§6.4).

### 14.4 회귀 방어

- **고정 홀드아웃 동결**: SketchGraphs + OpenSketch(또는 TU-Berlin) 홀드아웃을 동결하고
  단계마다 재측정. 하락 시 갱신 롤백.
- **수집 데이터를 홀드아웃에 섞지 않는다.** 학습·튜닝 데이터와 평가 데이터 완전 분리.

### 14.5 구현 시점

로깅 스키마(§14.1 항목의 직렬화 형식)는 **지금 확정하고 1~3단계에 소급 적용**(기록만).
학습 파이프라인·정정 학습은 **7단계**. 그 전까지는 순수 기록이며 소급 적용된다(§7 7단계 주석).
