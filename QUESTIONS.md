# QUESTIONS.md — 사람 판단이 필요한 항목

**측정으로 못 정하는 것**만 여기 쌓는다. 측정이 정할 수 있는 것은 재고
`DECISIONS.md`(잠정 결정)나 `DEFERRED.md`(미룬 것)로 간다.

각 항목은 **잠정값으로 진행 중**이다(CLAUDE.md A-3: 제품 판단이 필요하면 여기 쌓고
잠정값으로 간다). 답이 오면 그 항목의 "지금 하는 것"이 바뀐다.

> 2026-08-16 L-D.4에서 처음 만들었다. 다섯은 사람이 지정했다.

---

## a. 면 위 임의 점 스냅

**물음**: 스냅 대상에 **면**을 넣을 것인가?

벽에 창을 그릴 때 **모서리에서 시작하지 않는다.** 지금 스냅 대상은 획의 끝점·중점·교점이라
면 한가운데서 시작하는 획은 앵커가 없고 그래서 2D 레이어에 남는다.
**면이 스냅 대상이면 그 위 임의 점의 3D가 결정된다** — 시선 광선과 면의 교점이 하나다.

**D-S20(면을 만들지 않는다)과 충돌하지 않는다.** 그것은 **표시용** 판단이었다
("그리지 않은 기하를 만드는 것"). **스냅용 면은 표시하지 않아도 된다** — 축 둘이 펴는
평면 + 앵커 하나로 정해지고 그것은 이미 `face_diagonal`이 쓰던 구조다.

같은 구조가 **벽면 패턴 · 바닥 타일 · 천장 조명 · 가구 윤곽**에 그대로 적용된다.
이론서 **9.5절 등간격 판별**을 붙이면 규칙적 배치의 자동 채움도 가능해진다.

**지금 하는 것**: 안 넣는다. 면 개념이 §6(범위 밖)이라 L에는 자료구조가 없다.
**근거**: D-L28 — §9.5의 지정 경로는 축이 아니라 **면**이고, 면 지정은 §6과 함께 본다.

---

## b. 승격 대상이 아닌 획

**물음**: 획에 **네 상태**를 둘 것인가? 구분은 무엇으로 하는가?

**해칭 · 지시선 · 치수선 · 메모는 애초에 3D가 되면 안 된다.** 그런데 지금 2D 레이어의
뜻은 "**승격 대기**" 하나뿐이라 이것들이 전부 **미배치로 집계된다.**

필요할 수 있는 상태 넷:

| 상태 | 뜻 |
|---|---|
| `pending` | 승격 대기 — 조건이 갖춰지면 3D로 올라간다 |
| `placed` | 3D에 놓였다 |
| `surface` | **면에 붙은 주석** — 해칭처럼 그 면 위에 사는 것 |
| `view` | **뷰에 붙은 주석** — 메모처럼 그 시점에만 있는 것 |

**구분은 펜 채널이 유력하다** — 색으로 갈리고 사후에 바꿀 수 있다.
⚠ **자동 판정은 하지 않는다.** 틀린 분류가 조용히 남으면 A-3 위반이다
("애매하면 놓지 않는다"의 분류판).

⚠⚠ **지표 정의가 바뀐다**: 미배치율의 분모가 **`pending`만**이 된다. 지금까지의
미배치 수치는 전부 주석 획을 분모에 넣은 값이므로 **그 시점에 다시 읽어야 한다**(#11).

**지금 하는 것**: 상태는 둘(`pending`/`placed`)이다. `Stroke.layer` 필드를 **비워 둔 채
남겨 두었고**(D-L31) selfcheck가 "쓰기 0 · 읽기 0"으로 들고 있다 —
**그 플래그가 "아직 안 정해졌다"의 표시다.**

---

## c. 근경 자동완성

**물음**: 왜곡 때문에 생략된 근경을 **채울 것인가?**

2점 이상에서 **근경은 생략하는 것이 관행이다**(가장자리로 갈수록 왜곡이 커진다).
3D 입장에서는 그 형태가 **미완결**이라 돌려 보면 잘려 보인다.

⚠ **생략이 구도 선택이기도 하다.** 조용히 채우면 사용자가 안 그린 것이 생긴다.
**채우되 다르게 표시하는 쪽**(파선 · 흐리게)이 A-3과 맞다 —
"놓지 않되 버리지 않는다"의 반대 방향 판이다.

**기하적으로 유일하게 결정되는 것만 채운다**:
· 열린 루프의 닫힘 · 축 대칭 · 이미 정해진 면의 경계.
**유일하지 않으면 안 채운다.**

**지금 하는 것**: 안 채운다.

---

## d. 지평선 수평 가정 — **반증됐다. 실획 확인 대상으로 남긴다.**

**물음**: **사람이 그은 지평선이 실제로 화면 수평인가?**

L-D.0에서 롤 0(지평선 수평)을 카메라 제약으로 걸어 보고 **게이트에 실패했다**(D-L32).
**검출이 낸 두 "수평" 소실점의 y가 24~53px 어긋나** 수평 가정이 데이터와 맞지 않았다.
이론(수심 유도, 이론서 6.3)은 **참 카메라에서 오차 0**이지만 그것은 **항등이라 정보량이 0**이다(#5).

**남은 물음은 자동 검출이 아니라 사람이다.** 실획에서 사람이 그은 지평선이 수평이면
**사용자 확정 경로에서 제약으로 쓸 수 있다** — 가이드를 끌 때 y를 묶어 주면
핸들 자유도가 줄고 그만큼 조정이 쉬워진다. 자동 검출 경로에서는 이미 실패했다.

**지금 하는 것**: 제약을 안 건다. `horizon.ts`는 **배선하지 않고 남겨 두었다**
(D-L6·D-L32와 같은 처리 — 측정 근거를 재현할 수 있어야 한다).

**답을 얻는 법**: `sessions/`에 실획이 들어오면 그 세션의 가이드에서
**수평 축 두 소실점의 y 차이**를 잰다. 합성이 아니라 사람 손의 값이어야 한다.

---

## e. 카메라 롤

**물음**: 사용자가 **종이를 얼마나 기울여** 그리는가?

롤 0을 **배선하지 않았으므로 롤은 현재 자유도로 남아 있다** — 지금은 아무 제약이 없다.
L-D.0의 양성 채널이 **손익분기를 롤 ≈5°**로 냈다: 0/2/5/10°에서 롤 0 제약을 건 팔의
축 오차 중앙이 4.4693 / 5.0614 / 5.9275 / **10.0103**°이고 기준선은 6.0933°로 고정이다.
**10°에서는 제약이 해를 끼친다.**

그러므로 (d)의 답이 "수평이다"여도, **사용자가 5° 넘게 기울여 그린다면 제약을 걸면 안 된다.**
둘은 같은 표본으로 함께 답이 나온다.

**지금 하는 것**: 롤은 자유다(제약 없음).
**되돌릴 길**: 초기 확정 시 한 자유도로 두고 **확정 후 잠그는** 방식이 중간 지점이다.

---

## 이 문서를 쓰는 법

- **여기 있는 것을 측정으로 답하려 하지 않는다.** 다섯 다 **사람의 선택**이거나
  **실획 표본**이 필요한 것이다(AS-C1: 표본이 0이다).
- 답이 오면 그 항목을 `DECISIONS.md`로 옮기고 **여기서는 링크만 남긴다.**
- ⚠ **잠정값으로 진행한 사실이 결론에 붙는다** — 지금 수치는 전부 "상태 둘 · 면 없음 ·
  근경 안 채움 · 롤 자유" 위에서 나온 것이다.
