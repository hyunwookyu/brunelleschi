# PITFALLS.md — 이 프로젝트에서 반복해서 걸린 것

**살아 있는 문서다.** 이전에는 `HANDOFF.md` 안에 있었고, HANDOFF는 세션마다 다시 쓰이므로
전환할 때 함께 지워질 위치였다. 리뷰어가 짚었다 —
**L-A.2가 이 목록의 셋(#12·#14·#16)을 그대로 어겼다.** 그래서 따로 뺐다.

⚡ **착수 전 필독인 「최근 다섯」 표는 이 파일의 «끝»에 있다** — `tail -40 PITFALLS.md`.
파일이 길어질수록 `tail`이 자연스러운 읽기라 그 자리로 옮겼다(17차 후속 · #55).

**새 항목은 그 ⚡ 절 «앞»에 잇는다**(뒤에 이으면 표가 다시 위로 밀린다 —
`selfcheck.py`의 `scan_pitfalls_table_last`가 그 자리를 지킨다).
**대상이 사라진 항목은 지우지 말고 그렇게 표시한다** — 왜 그 규칙이 생겼는지가 근거이기 때문이다.

---

### 자동 검사로 옮긴 것 — **덮는 범위와 안 덮는 범위**

| # | 자동이 잡는 것 | **안 잡는 것(사람이 본다)** |
|---|---|---|
| #1 | 공유 **상수**가 바뀐 원장(`scan_stale_constants`) | **알고리즘 변경** · 문서에 손으로 적힌 수치 |
| #18 | `Stroke` 필드의 쓰기/읽기(`scan_unread_fields`) | 다른 자료구조의 필드 · **문자열 리터럴 안**의 이름 |
| #19 | 주석이 읽기로 세어지는 것(`strip_ts_comments`) | 검사를 약화시키는 **다른 형태**의 편집 |
| #5 | `project(`류 왕복 호출(`scan_roundtrip_metrics`) | **판별법 자체**(틀린 카메라를 넣어 지표가 움직이는지) |
| #23 | 지표 **정의**가 낡은 원장(`scan_stale_metrics`) | `metrics.ts`를 **안 부르는** 하네스 |
| **#33** | 선언 밖 **확장자**(`scan_sweep_coverage`) | **값 대조**(같은 계산의 다른 표현·인라인 식) |
| **#35** | 원장 `gate` 블록의 도달 가능성 필드 | **다른 이름으로 기록된 게이트** |
| **#40** | 도달 가능성이 **산문뿐**인가 · 값이 **정확히 0·1**인가 · 적은 값이 **원장과 맞는가** · 훑기 패턴이 **한 번도 안 맞는가** · **없는 원장**을 인용하는가 | **여섯이 샌다**(실증) — ① **지표 불일치**(값이 등록 기준과 다른 양) ② **프레임·동작점 불일치**(등록은 실측 각, 값은 준 각) ③ **모집단 불일치**(등록 5구도 ↔ 값 단일 구도) ④ **다른 원장에서 복사한 값**(실행 정보 0) ⑤ **`absent` 면제의 무조건 통과** ⑥ **다지표 게이트에서 한 값만 강제**(#28은 못 고르면 둘 다 적으라 한다) |
| **#36** | `Math.max(1, n)` **문법 하나** | `n \|\| 1` · 삼항 · 사전 필터된 분모 — **#33이 규정한 사각지대다** |
| **#42 ⑥** | 인용 줄의 수치가 **하나도** 원장에 없는가(`scan_cited_values`) | **줄에 원장 값이 하나라도 있으면 나머지 수치의 낡음은 안 본다** · **해시 없는 인용은 모집단 밖이다**(②형 227줄) |
| **#42 ④⑦** | 원장 전문의 `#N`을 원장별 표로 낸다(`scan_pitfall_citations`) · **죽은 참조**(PITFALLS에 없는 번호) | ⛔ **어느 원장이 이번 항목의 것인지 모른다** — 착수 표의 누락을 **판정하지 않는다**. 문서(`.md`)의 인용도 안 본다 |

반례 테스트: `tests/test_selfcheck_new_scans.py`(#33·#35·**덮는 대상 수**·
**#42 ④⑦ 넷**(원장 전문 `gate.reachability` 읽기 · 죽은 참조 발화 · 덮는 수 0 · 문서 미열람)·
**[5] 모집단 분해 둘**(같은 모집단 위 두 판정의 수)),
`tests/test_selfcheck_scan.py`(#19), `web/test/gate.test.ts`(빈 `reachability`를 막는다).
**플래그 0건이 "깨끗함"인지 "안 돎"인지 가르는 것이 그 테스트들이다**(#32).

### ⚠ 자동 검사는 **덮는 대상 수**를 함께 낸다 (2026-08-16, 사람 지시)

**#35가 실제로 그 함정에 빠져 있었다** — 검사는 동작했고(반례 테스트가 확인했다) 원장 37개를
훑었는데, **`gate`라는 이름으로 적는 하네스가 하나도 없어 덮는 대상이 0**이었다.
플래그 0건이 나왔고 그것이 "깨끗함"으로 읽혔다. 이것이 #32의 상황이다.

고친 쪽은 **검사가 아니라 기록하는 쪽**이다(이름 목록을 넓혀 0을 지우면 #19다).
`web/test/gate.ts`의 `gate()`가 `registered`·`reachability` 둘을 강제하고, 여섯 하네스가
그 이름으로 적는다. `selfcheck.py`의 `_cover()`가 검사마다 덮는 수를 `selfcheck.json`의
`coverage`에 남기고 **콘솔에 찍는다**. **0이면 그 자체가 플래그로 뜬다.**

| 검사 | 단위 | 현재 덮는 수 |
|---|---|---|
| `scan_sweep_coverage`(#33) | 훑기 | **4** (훑기별 파일 수는 `sweeps[]`) |
| `scan_citation_hashes`(#33 값 대조) | 인용 | ⚠ **수를 여기 적지 않는다 — `selfcheck.json`의 `coverage`를 그 자리에서 읽는다**(이 칸의 129도, HANDOFF의 131도 적힐 때 이미 낡아 있었다 — 4차 항목 1 리뷰어 [11]이 잡았다. 커밋된 원장은 126이었다). 연혁: 항목 0이 해시 없는 인용에 해시를 달아 99 → 129대. 옛 해시 23곳을 이 검사가 찾아냈다. ⚠ **"값 대조"는 해시 대조다** — 해시 없는 인용은 덮는 대상 밖이고, 해시가 맞아도 옮겨 적은 수치의 낡음은 안 잡는다(그 사각지대의 실증과 현행 방어는 `DEFERRED.md` 2026-08-17 절) |
| `scan_gate_reachability`(#35·**#40**) | 게이트 블록 | ⚠ **수를 여기 적지 않는다 — `coverage`를 그 자리에서 읽는다**(연혁: 2026-08-17 `basic_flow`·`osnap_config`·`eraser` 등록으로 16, 4차에서 `snap2d_flow`·`vp_dir_flow` 등이 잇따라 는다) — ⚠ **값 대조를 지난 것과 면제의 수는 `selfcheck.json`의 `coverage`를 그 자리에서 읽는다**(이 칸에 적은 수가 이번 세션에만 네 번 낡았다). 면제(`reachability_absent`)(**`horizon_pitch`·`osnap`** 포함). **면제는 통과가 아니다** — 도달 가능성이 **미상**이라는 뜻이고 근거로 쓰지 않는다. ⚠⚠ **이 표는 두 세션 연속으로 낡았다**(리뷰어가 7에서, 다음 항목에서 12에서 잡았다) — **수를 인용하기 전에 `stage0/out/selfcheck.json`의 `coverage`를 그 자리에서 다시 읽는다** |
| `scan_zero_denominator`(#36) | 하네스 파일 | ⚠ **수를 여기 적지 않는다 — `stage0/out/selfcheck.json`의 `coverage`를 그 자리에서 읽는다**(이 칸의 41도 적힐 때부터 세 번째로 낡았다 — 7-R 리뷰어 [13]이 잡았다. 하네스 파일이 늘면 이 수가 는다. 병합된 원격 갈래도 같은 자리에서 낡았다 — 8-R″ 재검 [10]이 41 → 42를 잡았다). 연혁: D-L54 삭제 반영 시점에 41. ⚠ `web/e2e/`는 이 검사 밖이다 — `basic_flow`의 분모는 사람이 본다. 대상은 원장을 쓰는 `web/test/**.ts` |

⚠ **덮는 수가 0이 아니라고 전수인 것은 아니다.** 게이트 발견은 **이름**으로 하므로
다른 이름으로 적힌 게이트는 여전히 안 보인다 — #33이 규정한 그 사각지대다.

---

## 측정이 자기를 확인한다

1. **문서의 수치가 원장(JSON)보다 뒤처진다.** 수치를 쓰기 전에 `stage0/out/`의 JSON을
   그 자리에서 다시 읽는다. 재측정했으면 인용 문서를 `grep`으로 전부 고친다.
   → 상수는 기계가 잡는다(STALE). **알고리즘 변경은 여전히 사람이 본다.**
2. **측정이 자기확인이 된다.** S-2는 합성 활의 대칭성, S-3은 합성 잉크가 끝점을 참 모서리에
   못으로 박고 있던 것. **수치가 이상하게 좋으면 노이즈 모델의 성질을 재는 것은 아닌지 먼저 의심한다.**
3. **지표가 정의의 귀결일 수 있다.** S-3의 떨림비. **하나가 이기면 왜 이기는지 먼저 묻는다.**
4. **모델이 문제를 만들지 못할 수 있다.** S-2b의 첫 왜곡 모델은 정상 카메라였다.
   **반례 테스트를 모델 자체에도 건다.**
5. **자기참조 유형 3** — 복원한 값을 그 복원에 쓴 연산으로 되검사하는가.
   구 `reprojectionGap`이 그것이었고(실측 0px), **L에도 그대로 있다**:
   계획서 §5.3의 전환 픽셀 차이와 §6.2의 재투영 어긋남이 같은 유형이다.
   보장이면 원장에 그렇게 적고 **임계를 걸지 않는다.**
   판별법: **틀린 카메라를 넣어 그 지표가 움직이는지 본다. 안 움직이면 측정이 아니다.**
6. **"이래서 필요하다"는 주장에는 대조군을 붙인다.** S-4에서 둘, S-9에서 하나
   ("`pts3d`는 재생성된다" → 지우개 스윕이 그 말을 갈랐다).
   **대조군이 무변화를 보이면 "차이가 없다"가 아니라 "이 표본에서 두 방법이 같아진다"일 수 있다**
   (D-L5에서 걸렸다 — 상자 12모서리는 화면 길이가 비슷해 길이 가중이 균등 가중과 같아진다).

## 지표가 무엇을 세는지

7. **"왜 안 되는지" 추측하지 말고 카운터를 넣는다.** S-6의 자유단 38.5%.
8. **분포의 꼬리를 본다.** 중앙값이 좋아도 p90·최댓값을 함께 찍는다.
9. **성공률을 셀 때 무엇이 섞였는지 먼저 가른다.** 회수와 조용히 틀린 배치를 갈랐다.
10. **한계 집합을 뺄셈으로 만들지 않는다.** 직접 센 집합으로만 비율을 낸다.
11. **분모가 전부인지 본다.** S-8의 "미분류로 남은 사선만 재고 있었다", S-9의 생존자 43 대
    모집단 46. **L-A.2에서 또 걸렸다** — 오배정 분모가 행마다 399~456이고 모집단은 480인데
    빠진 21~81획이 무엇인지 안 적었다. **행마다 분모가 다르면 행 간 비교가 같은 양의 비교가 아니다.**
12. **동작점을 하나 고르지 않는다.** S-9에서 끝점 오차 0.01만 쟀다가 0.02를 넣으니
    사용자 지정 배치가 9 → 34로 늘었다. **L-A.2에서 또 걸렸다** — 끝점 오차 0~0.01만 쟀고
    **유일한 실획 자릿수(0.028~0.051)는 그 구간 전부 바깥이다.**
13. **절단값이 결론을 정하면 그 결론은 없다.** 0.1·0.2·0.5를 다 낸다.
14. **시드 변동폭이 결론의 여유보다 크면 그 결론은 없다.** 비율보다 **분자/분모**로 적는다 —
    S-9의 "22%"가 시드 하나에 0.11~0.33으로 흔들렸다. **L-A.2에서 또 걸렸다** —
    1건/400의 오배정 차이와 3장면/40의 검출 차이로 임계를 골랐다.
15. **개수만 세면 반대로 간다.** 회수율만 보면 구제 경로가 좋아 보인다(D-C6).
    소실점 검출에서도 같았다 — 지지를 개수로 세면 엉뚱한 중간점이 이긴다(D-L4).
16. **병목이 옮겨간 것을 없어진 것으로 읽지 않는다.** G-A.1: 끝점 의존은 실제로 사라졌는데
    (배치율 1.7배) 조용히 틀린 배치는 늘었다.

## 코드와 측정이 갈린다

17. **측정 경로와 앱 경로가 갈라진다.** → 단일 출처 + 배선 테스트가 잡는다.
    **덮는 범위를 함께 적는다** — 구 `wiring.test.ts`는 `PlaceOpts`만 덮었다.
18. **써 놓고 안 읽는다.** `Stroke.color`. → `selfcheck.scan_unread_fields`가 잡는다.
19. **⚠ 설명이 검사를 끈다.** 주석에 "`Stroke.layer`는 담지 않는다"고 적었더니
    **그 문장이 읽기 1건으로 잡혀 플래그가 사라졌다.** 검사를 약화시키는 방향으로 고치지 않는다.
20. **거름을 선택 전에 걸면 경쟁자가 죽어 상대 순위가 거저 통과한다**(D-C5).
    각도 거부권을 `Infinity`로 걸었더니 오배정이 79% → 90%로 **늘었다**. 거름은 **선택 뒤에**.
21. **dpr=1에서만 확인하면 dpr 규약 버그를 못 본다**(D-C3). 데스크톱·Playwright
    (deviceScaleFactor 1) 어디서도 안 보였다. **회귀 테스트를 쓰면 버그를 되살려 실제로
    실패하는지 확인한다** — 첫 판은 배경 층이 제자리에 그리는 탓에 통과하고 있었다.
22. **캔버스 크기가 굳는다 — 세 번 걸렸다**(AS-C7). `ResizeObserver`는 초기 콜백을 안 주고
    **`requestAnimationFrame`은 숨은 탭에서 완전히 멈춘다**(600ms에 0프레임). 창이 뒤에서
    열리면 둘 다 발화하지 않는다. → **관찰자를 더 걸지 말고 쓰는 자리에서 자가 치유한다.**
    확인은 **창을 앞에 두고** 한다. ⚠ **자가 치유는 재발을 관측 불가로 만든다** —
    발동 횟수를 세어 남긴다(#7·#19).

## 계획과 실제

23. **계획서 문장이 기하적으로 성립하지 않을 수 있다.** §5.1의 "선 3개가 한 점에서 만나면
    소실점"은 **상자의 꼭짓점**이 완벽히 만족한다(D-L3). §5.2의 "2D 교점은 손실 없는 정보"는
    **가림 교차**를 포함한다(상자 하나에서 27개 중 3개). **계획과 실제가 어긋나면 실제를 따르고
    계획서를 고친다**(A-3) — 고치지 않으면 다음 세션이 원문을 다시 시도한다.
24. **단위를 바꾸는 것과 임계를 바꾸는 것을 혼동하지 않는다.** D-L2 초판이 그랬다 —
    부적합도 → 각도가 **단조 재매개화**(φ = asin(2·misfit))인데 그것이 실패를 고쳤다고 적었다.
    **재매개화는 아무것도 고치지 않는다.** 고친 것은 다른 변경이었다.
25. **원장 밖 측정은 규칙이 있어도 안 걸린다.** L-A.2의 스윕 표가 `stage0/out/` 없이
    `progress.md`에만 있었다. selfcheck의 1.0/0.0·분포단일·STALE 검사를 한 번도 안 지났다.
27. **대역 기준값을 다른 하네스에서 가져오지 않는다.** L-A.5d가 `lift_gate`의 0.0791을
    대역으로 썼는데 그것은 **검출 오배정이 섞인** 값이고, 같은 하네스의 무오차 행은 0.0673이었다.
    그 차이 안에서 임계가 ≈1°냐 ≈0.5°냐가 갈렸다. **대역은 같은 조건·같은 하네스에서 낸다.**
28. **판정 지표를 사유 없이 바꾸지 않는다.** §5.5가 등록한 기준은 형태 오차 0.026인데
    판정은 조용히 틀림 0.079로 했다. 두 지표가 **같은 곳을 안 가리킨다**(0.48°에서 1.03배 대 2.07배).
    바꾸려면 사유를 적고, 못 고르면 **둘 다 적는다**.
29. **여유(budget)는 사용자가 만지는 양으로 환산한다.** "소실점 좌표 29px 여유"는
    핸들 여유가 아니다 — 같은 배치에서 핸들 예산은 **0.29px**였다(1/100).
    지표를 사용자의 손이 닿는 단위로 옮기기 전에는 "도달 가능하다"고 적지 않는다.
26. **게이트 수치는 측정 전에 박는다.** 구 D-S6이 그렇게 했다. L-A 게이트는 "안 튀고",
    "전부 놓인다"로만 적혀 있어 사후에 정할 여지가 있었다 → `progress.md`의 사전 등록 절.
31. **탐색 공간을 목적함수가 좁히지 않았는지 확인한다.** L-B.4의 45조합 스윕이
    **완화 방향만 볼 수 있게** 짜여 있었고 거기서 "아무 방향도 안 가리킨다"가 나왔다.
    **측정이 무결론일 때 그것이 현상인지 설계의 산물인지 먼저 본다** —
    스윕 격자·등록 규칙의 목적함수·거부 규칙이 한쪽 방향을 아예 못 만들면
    그 방향의 부재는 관측이 아니다. → 판별법: **반대 방향의 격자점을 하나라도 낼 수 있는지**
    스윕 정의를 되읽는다. 못 내면 결론이 아니라 설계를 고친다.
32. **미실행을 반증으로 처리하지 않는다.** L-B.7 §9.1 일괄 기전의 회수 0이
    "기전이 틀렸다"가 아니라 **거름이 대기 집합 1904 전부를 비워 솔버에 한 획도 안 닿은 것**
    이었다. **기전을 폐기하기 전에 실제로 실행됐는지 확인한다** —
    실행 카운터(입력 개수·솔버 호출 횟수)를 결과와 함께 남긴다.
    입력이 0이면 그 실행의 결과는 **기전에 대한 정보가 0이다.**
30. **카메라 오차를 재려는 지표는 사영 불변인지 먼저 확인한다.** 네 번 반복됐다 —
    전환 픽셀 차이 · 교점 잔차 · `chord_dev` · 계획서 §6.2의 재투영 잔차.
    **입사 관계에서 유도한 양은 전부 f에 반응하지 않는다.**
    → 규칙: **양성 채널을 함께 둔다.** 지표가 안 움직일 때
    *섭동이 안 걸린 것*인지 *지표가 둔감한 것*인지 구분되어야 한다.
    L-B.1이 그 예다 — f×0.8→1.2에서 `chord_dev`는 소수 넷째 자리까지 불변인데
    같은 섭동에서 **3D 세그먼트 길이는 1.04 → 0.74**로 움직였다(`stage_cam.json@f351839a`).
    양성 채널이 없으면 #5의 판별법("틀린 카메라를 넣어 본다")이 **무효 결과와 구분되지 않는다.**

## 점검 자체가 새는 자리 (사람 지시, 2026-08-16)

33. **전수 확인이 함수만 찾지 않았는지 본다.** L-B.M의 지표 정의 통일 전수 점검이
    **인라인 식 하나를 놓쳤고**(`axis_live`) 나중에 절단 곡선에서 발견됐다.
    **문법 형태로 찾는 점검은 같은 계산의 다른 표현을 놓친다** —
    `grep`이 함수 이름을 찾으면 인라인으로 펼쳐 쓴 같은 식은 안 걸린다.
    → 규칙: **값 대조를 함께 한다.** 같은 입력에 같은 값이 나오는지 본다.
    정의 해시(D-L23)도 같은 사각지대다 — 하네스가 `metrics.ts`를 **아예 안 부르면**
    해시가 안 움직인다. 해시는 **부르는 하네스만** 덮는다.
34. **고친 결함이 같은 자리에 재발하지 않았는지 본다.** 앵커 팔 분모의
    **`end_jitter = 0` 혼입**이 L-B.3 2회차에서 한 번 고쳐졌고 **절단 곡선에서 재발했다**
    (리뷰어 3회차 [1], 서명은 `p10 = 0`).
    → 규칙: **결함을 고칠 때 그 조건을 쓰는 다른 호출부를 함께 확인한다.**
    한 자리에서 고친 것은 그 자리에서만 고쳐진 것이다.
35. **등록한 기준이 도달 가능한지 함께 박는다.**(L-C.3, 2026-08-16)
    #26이 "게이트 수치는 측정 전에 박는다"인데, L-C.3이 그 **반대 방향으로** 실패했다 —
    측정 전에 박긴 했는데 **판별력 0.5**가 그 픽스처에서 도달 가능한지 모르는 채였다.
    참값으로 가른 대리 상한이 **0.4979**여서, 기준을 못 넘은 것이 신호의 성질인지
    기준의 성질인지 갈리지 않았다.
    → 규칙: **기준을 박을 때 "무엇이 그 기준을 넘을 수 있는가"를 함께 적는다.**
    오라클(참값을 쓴 팔)이 그 자리다. ⚠ **오라클이 못 넘어도 기준을 낮추지 않는다** —
    그것은 #26의 반대편 문(사후에 기준을 낮추는 문)을 여는 것이다. 적기만 한다.
    ⚠ 오라클을 "상한"이라 부르지 않는다 — **한 대리 참값의 성능**이지 도달 가능성의 증명이 아니다.
    ⚠⚠ **그리고 오라클의 지표를 먼저 의심한다.** L-C.3의 첫 오라클(0.4979 · 0.1265)은
    **자기 게이지**였고, 최소제곱이 전역 배율을 적합해 **사용자가 보는 성분을 지운 뒤**의 수였다.
    고정 게이지로 다시 재니 **0.9694 · 0.5446**으로 둘 다 기준을 넘었다 —
    **"기준이 도달 불가능했다"가 통째로 지표의 산물이었다.**
36. **한 팔이 빈 층에서 비율을 내지 않는다.**(L-C.3, 2026-08-16)
    층화 표에서 `분자 / Math.max(1, 분모)`를 쓰면 **분모 0이 1로 바뀌어** 비율이 나온다 —
    L-C.3의 `bestOf`가 그래서 가짜 팔이 없는 3점 구도 층에 **판별력 −0.03**을,
    참 팔이 없는 2점 구도 층에 **판별력 1**을 냈다. 둘 다 없는 수다.
    → 규칙: **분모가 0이면 `null`을 낸다.** `Math.max(1, n)`은 **표시용 나눗셈 보호**이지
    **없는 관측을 만드는 데 쓰면 안 된다**(#11의 변종이다).
37. **새 지표나 기전을 제안하기 전에 이론서를 확인한다.**(L-C.3, 2026-08-16)
    L-C.3의 대체 신호로 **f² 편차**를 제안했는데, 그것은 **수심의 항등식이라 정보량이 0**이고
    `docs/perspective_theory.md` 6.4절이 이미 그렇게 적어 두었다. 제안 전에 봤어야 했다.
    → 규칙: **투시 기하와 관련된 양을 도입할 때 이론서에 그 성질이 적혀 있는지 먼저 본다.**
    이론서 지도는 `CLAUDE.md` §7에 있다. 같은 세션에서 `tiltDeg`도 **게이지 문제**였고
    (지표가 아니라 기준계가 흔들린 것) 그것도 이론서가 다루는 종류였다.

38. **자동 검사는 덮는 대상 수를 함께 낸다.**(L-D 잔여, 2026-08-16 사람 지시)
    **#35 검사가 실제로 그 함정에 빠져 있었다** — 검사는 동작했고(반례 테스트가 확인했다)
    원장 37개를 훑었는데 **`gate`라는 이름으로 적는 하네스가 하나도 없어 덮는 대상이 0**이었다.
    플래그 0건이 나왔고 그것이 "깨끗함"으로 읽혔다. #32의 상황이다.
    → 규칙: **검사마다 `_cover(scan, unit, targets, hits)`를 부른다.** `targets`가 0이면
    그 자체가 플래그다. ⚠ **고칠 곳은 검사가 아니라 기록하는 쪽이다** — 검사의 이름 목록을
    넓혀 0을 지우는 것은 #19(검사 약화)와 같은 방향이다.
    ⚠ **0이 아니라고 전수인 것은 아니다**: 발견이 **이름**으로 이뤄지면 다른 이름으로 적힌
    대상은 여전히 안 보인다(#33의 사각지대). 그래서 `scan_citation_hashes`를 **값 대조**로
    만들었다 — 인용 해시를 원장의 현재 해시와 맞춰 본다. 첫 실행에서 **23곳**이 걸렸다.
39. **제약을 걸 때 사후 사영이 아니라 적합 안에 넣은 판을 함께 낸다.**(L-D.0, 2026-08-16)
    롤 0 제약을 검출 소실점에 **나중에 사영**했더니 배치가 2049 → 792로 붕괴했다.
    그런데 사영은 소실점을 **자기 지지선에서 떼어 놓으므로**, 그 붕괴가 **제약의 성질**인지
    **사영의 성질**인지 갈리지 않는다 — #32("미실행을 반증으로 처리하지 않는다")의 변종이다.
    → 규칙: **제약을 목적함수 안에 넣은 팔을 함께 돌린다.** L-D.0에서는
    `fitHorizonPair`(미지수 `(x₁,x₂,h)`를 한 번의 최소제곱으로)가 그 팔이다.
    ⚠⚠ **그러나 그것만으로는 부족했다 — 리뷰어가 잡았다.** 적합 팔도 붕괴하자
    "붕괴는 제약의 성질"이라 적었는데, **위약 팔**(소실점을 같은 크기로 **임의 방향** 이동)이
    똑같이 붕괴했다. 즉 재던 것은 "롤 0"이 아니라 **"검출 해를 80px 옮기는 것"**이었다.
    → **규칙의 나머지 절반**: 제약이 해를 얼마나 움직이는지 재고, **같은 크기의 무의미한
    이동**을 위약으로 함께 돌린다(#30의 양성 채널을 붕괴 쪽에도 건다).
    적합 팔은 "사영이 원인인가"만 가르고 **"그 크기의 이동 일반이 원인인가"는 못 가른다.**

40. **자동 검사의 필수 필드를 항등이나 자명한 값으로 채우지 않는다.**(2026-08-16, 사람 지시)
    **채운 값이 실제로 정보를 갖는지 확인한다.**

    실제로 걸린 것: **#35를 자동 검사로 만든 바로 그 세션이 같은 세션 안에서 그 필드를
    정보량 0인 항등으로 채웠다** — `horizon`의 `reachability`에 "참 카메라에서 수심 관계가
    오차 0으로 성립한다"를 적었는데 그것은 항등이라 **아무 말도 안 한다**(#5).
    필드가 비지 않았으므로 검사는 통과했다. **검사를 만들면서 그 검사를 무력화한 것**이고,
    이 유형이 새롭다 — #38(덮는 수 0)의 **다음 단계**다: 덮는 수가 0이 아니어도
    **값이 자명하면 같은 상태**다.

    점검해 보니 하나가 더 있었다: `camera_gate`가 "`deg_0` 행이 **정의상** 1.0배·1.0배"라
    적었는데 **그 행이 대역의 출처 자체**다. 자기 자신과의 비가 1인 것을 도달 가능성이라
    부른 것이다. (고친 뒤 드러난 것: 자명하지 않은 팔은 `deg_0.25` 행이고
    **등록 지점인 `deg_0.5`는 통과선을 못 넘는다** — 실효 요구가 등록값의 두 배로 엄격하다.)

    → 규칙 셋:
    **① 산문 대신 수치 + 출처를 요구한다.** `gate()`가 `reachability_value` +
       `reachability_source`(원장 안 `/` 경로)를 강제하고, 오라클이 없으면
       `reachability_absent`에 **그 사실을 명시**하게 한다 — "없다"도 결론이고 산문에 묻히면 안 된다.
    **② 값이 정확히 0이나 1이면 항등을 의심한다.** 그 자리의 0·1은 대개 측정이 아니라 보장이다.
    **③ 값 대조를 한다**(#33) — 적은 수치가 원장의 그 경로에 실제로 있는지 본다.
       재측정 뒤 손으로 적은 수치를 안 고친 자리를 이것이 잡는다.

    같은 형태를 다른 검사에도 걸었다:
    · **훑기 패턴이 한 번도 안 맞으면** 그 훑기는 공허하다(파일은 훑었는데 대조한 것이 0).
    · **없는 원장을 인용하면** 그 인용은 값 대조를 한 번도 안 지나므로 조용히 건너뛰지 않고 센다.

    ⚠ **아직 자동이 못 보는 것**: 0·1이 아니면서도 자명한 값(다른 형태의 항등)과,
    `registered`가 실제 상수를 가리키는지. **사람이 본다.**

    ⚠⚠ **이 항목 자체가 같은 함정에 세 번 더 빠졌고 리뷰어가 잡았다** — 규칙을 만든 사람이
    그 규칙을 못 지킨다는 것이 이 항목의 두 번째 교훈이다:
    · `segment_gate`에 **판정의 기준선 자체**를 도달 가능성으로 적었다 —
      `camera_gate`가 "정의상 1.0배"로 ❌ 받은 것과 **같은 구조**인데 **절대값으로 적어
      "정확히 0·1" 규칙을 피해 갔다.** → `reachability_absent`로 고쳤다.
    · `guide_budget`에 **다른 지표**(캔버스 현 길이 588.18px)를 적었다. 기준은 "예산이 **늘어난다**"다.
    · `lift_gate`에 **등록 지표가 아닌 값**(조용히 틀림)을 적었다. 등록 지표의 오라클은
      배치율 **1.000**인데 그 1은 **분모가 최대 연결 성분**이라 나오는 구성의 귀결이다(#27).
    → **값이 있다고 검증된 것이 아니다.** 그 값이 (가) 등록 기준과 **같은 지표**인지
      (나) **같은 모집단·같은 프레임**인지 (다) **이 실행에서 나온 것**인지는 사람이 본다.

41. **중단 조건에 걸렸다고 적었으면 실제로 멈춘다.**(2026-08-16, 사람 지시)
    **판정과 행동이 어긋나면 둘 중 하나가 틀린 것이므로 먼저 확인한다.**

    실제로 걸린 것 **둘**(리뷰어가 두 번째를 짚었다):

    **①** #40 항목이 `camera_gate`를 고치다가 "**등록 지점 `deg_0.5`가 통과선을 못 넘는다**"고
    적었다. CLAUDE.md §2는 **그 게이트 실패를 이 프로젝트의 유일한 중단 조건**으로 정해 두었는데
    (`camera_gate`가 곧 L-A.7 재등록 게이트다) **멈추지 않고 다음 항목으로 갔다.**

    **②** 그 앞의 **L-D.0은 `horizon`에 대해 "게이트 실패"라 적었고 거기서도 안 멈췄다.**
    ⚠ **그것은 어긋남이 아니다** — `horizon`의 게이트는 L-D.0이 **스스로 등록한 것**이고
    CLAUDE.md §2의 중단 조건(L-A.7 축 오차 0.5°)과 **다른 게이트**다.
    **그러나 같은 말("게이트 실패")을 써서 구분이 안 됐다.**
    → **중단 조건인 게이트와 항목이 스스로 등록한 게이트를 같은 말로 부르지 않는다.**

    리뷰어가 확인해 보니 **판정 쪽이 틀렸다** — 원장 키 `deg_N`은 소실점에 **준** 각도이고
    등록문은 **실측** 축 오차다. 실측 0.5°에 대응하는 행은 `deg_0.25`(실측 0.4838°)이고
    **그 행은 통과한다.** 즉 중단 사유가 애초에 없었다.

    → 규칙: **"중단 조건에 걸렸다"를 쓰는 순간 두 가지 중 하나를 반드시 한다** —
    ① 정말로 멈추고 보고하거나, ② **판정을 다시 확인한다**(단위·프레임·모집단이 등록문과
    같은가). 어느 쪽도 안 하고 지나가면 **판정이 장식이 된다.**
    ⚠ 이 프로젝트에서 판정이 틀리는 첫 번째 이유는 **프레임 혼동**이었다(#24와 같은 뿌리):
    준 값 ↔ 실측 값, 자기 게이지 ↔ 고정 게이지, 단일 구도 ↔ 5구도.
    **등록문이 무엇을 재라고 했는지부터 다시 읽는다.**

42. **착수 표의 "해당 없음"을 완료 시 자기 원장과 대조한다.**(2026-08-17 4차, 리뷰어)
    4차 세션에서 "해당 없음" 오판이 다섯 항목 연속으로 났다: 항목 1(#41·#40·#37) ·
    항목 2(#41·#40·#12) · 항목 3(#39·#37 부분) · 항목 4(#24·#12) · 항목 5(#12·#24) ·
    항목 7(#5·#12·#21·#40 — **자기 원장이 그 네 번호를 인용하는데** 표는 해당 없음이라 적었다) ·
    항목 8(#12). 전부 리뷰어가 잡았다.
    → 규칙: ① "해당 없음"은 그 번호의 **본문**을 다시 읽고 나서만 적는다(HANDOFF의 기존 경고).
    ② **완료 시 원장의 `what_this_does_not_say`·gate가 인용한 #번호를 착수 표와 대조한다** —
    원장이 인용하면 해당이었던 것이다. ③ 특히 **#12(동작점 하나)는 측정·픽스처가 있는 항목이면
    거의 항상 해당이다** — "스윕을 안 만들었다"는 면제 사유가 아니다.
    ④ (2026-08-17 5차, 8-R′) **완료 대조의 grep은 원장 전문을 대상으로 한다 —
    `gate.reachability` 포함.** 5차에서 다섯 원장 전부 reachability가 "(#40 규칙 ①)"을
    인용하는데 본문만 grep해 다섯 번 다 놓쳤다(여덟 자리 재발의 주범).
    ⑤ (8-R′ 재검) **정정은 정정 표에만 적지 않는다** — 초판의 착수 표·완료 대조 줄도
    제자리에서 고친다. 표만 고치면 다음 읽기가 초판을 그대로 믿는다.
    ⑥ ⛔⛔ **반례 — "원래 인용하던 것이니 면제"는 면제가 아니다.**(2026-08-18 8차 지시 1-a)
    7차 항목 3이 이 논리를 실제로 썼다: 그 수치는 **이전부터 인용하던 것**이므로 이번 항목의
    대조 대상이 아니라고. **틀렸다.** 대조 대상을 정하는 것은 **인용의 나이가 아니라
    원장의 재실행**이다 — 원장을 다시 돌리면 그 원장을 인용한 **모든** 문서가 그 순간 낡고,
    "언제부터 인용했는가"는 그 사실을 하나도 안 바꾼다. **출처가 오래됐다는 것은
    면제 사유가 아니라 오히려 여러 번 낡았을 확률이 높다는 뜻이다.**
    실증: 8차 항목 0의 병합에서 원격 갈래의 인용 **20곳**이 이 논리로 넘어갈 뻔했다 —
    다시 읽으니 **`0.253`은 현행 원장에 없었고**(통계를 max에서 min으로 바꾼 뒤 값이
    `0.0244`) **`rule_camera`의 before→after 쌍은 기전 자체가 사라져** 재현 불가였다.
    → **원장을 재실행했으면 그 원장을 인용한 문서를 `grep`으로 전부 찾아 값부터 다시 읽는다.**
    ⚠ **자동 검사가 이 형태를 다 못 잡는다** — 아래 "인용 검사가 안 잡는 세 형태"를 본다.

    ⑦ ⚙️ **완료 대조를 손 grep에서 자동으로 옮겼다**(2026-08-18 8차 2차 지시 항목 0 · 리뷰어 [11]).
    ④를 **세 세션 연속 손으로** 했고 **세 번 다 틀렸다**(4차 다섯 항목 · 5차 여덟 자리 ·
    8차 항목 0·1·2). 반복해서 손이 틀리면 고칠 곳은 사람이 아니라 **기록·검사 쪽**이다(#38).
    → `selfcheck.py`의 `scan_pitfall_citations`가 **원장별 `#N` 목록**을
    `selfcheck.json`의 `pitfall_citations`에 남긴다. 착수 표를 쓸 때 저장소 전체 grep이 아니라
    **그 표를 읽는다.** 죽은 참조(PITFALLS에 없는 번호)는 플래그한다.
    ⚠ **자동이 판정하지는 않는다** — 어느 원장이 이번 항목의 것인지 검사는 모른다.

    ⑩ ⛔⛔⛔ **규칙을 만든 절의 **바로 다음 절**이 그 규칙을 어긴다.**(2026-08-18 8차 2차, 8-2R″)
    실증 셋이 한 세션 안에서 났다 — 항목 0이 세운 것을 항목 1·2가 어겼다:
    ① "본 팔과 **글자 그대로 같은 값**은 정보를 안 나른다"(8-2R′ [1])를 세워 놓고,
       2-b가 `287/708`이 같은 것을 **근거로** 삼았다.
    ② `pitfall_citations`로 **손 대조를 없앴는데**(⑦) 그 표를 **손으로 옮기며 두 번호를 흘렸다.**
    ③ "②형(해시 없는 인용)은 모집단 밖 — **고칠 쪽은 적는 쪽**"이라 적고,
       바로 다음 절이 **해시 없는 인용을 수십 줄 새로 추가했다.**
    → 규칙: **새 규칙을 세운 항목은, 그 규칙을 자기 다음 항목의 착수 표에 옮겨 적는다.**
    새로 만든 규칙은 `PITFALLS`의 "최근 다섯"에 들어가기까지 시차가 있고, 그 시차 동안
    **가장 어기기 쉬운 사람은 그 규칙을 만든 사람이다**(#40의 "검사를 만들면서 그 검사를
    무력화한 것"과 같은 뿌리).

    ⑪ ⛔⛔ **"이 항목이 인용하는 원장은 없다"(공집합)는 ⑧의 반대편 실패다.**(8-2R‴)
    ⑧이 "전부 재실행한 항목은 **결론이 인용하는 원장**만 고른다"인데, 8차 2차 항목 4가
    그것을 **"코드 독해라 원장이 없다"**로 답했다. **실제로는 넷이 있었다** —
    `real_ink`(그 관측의 성격과 분모) · `confirm_rules`(프로브가 "붙었다"를 낸다) ·
    `snap`·`osnap_config`(반경 스윕). 그 넷을 안 읽어 **두 개의 높음**이 났다:
    원인 귀속이 분모 불일치로 무너졌고, "근거가 없다"고 적은 자리에 **반대 방향의 측정이
    이미 있었다.**
    → **코드를 읽어 낸 결론에도 원장이 있다.** 그 결론이 **설명하려는 현상**이 어느 원장에
    적혀 있는지부터 찾는다 — `grep -rn "<현상의 지표 이름>" stage0/out/`.
    ⚠ 특히 **사람 보고를 설명하려는 항목**은 그 보고가 원장에 어떻게 적혀 있는지를
    반드시 읽는다(표본 수 · 분모 · "보고이지 기준선이 아니다" 같은 단서가 거기 있다).

    ⑨ ⛔⛔ **"어느 원장이 움직였는지"를 한 원장만 보고 정하지 않는다.**(2026-08-18 8차 2차, 8-2R [3])
    ⑦⑧을 신설한 그 항목이 **⑥을 다시 어겼다** — `rule_camera`가 재실행으로 낡은 것을
    스스로 발견해 일곱 자리를 고쳐 놓고, **같은 커밋이 `order_lock`도 다시 돌렸다는 것은
    안 봤다.** 그 원장의 `factor_split`이 **뒤집혀 있었고**(축 라벨 참 팔이 안 움직이는 것이
    아니라 나빠진다) 그 결론은 `D-L70`·`AS-L29`·`HANDOFF`·`progress` 넷을 떠받치고 있었다.
    → **한 원장의 낡음을 발견하면 그 커밋이 건드린 원장 전부를 목록으로 만든다** —
    `git show <커밋> --stat -- stage0/out/`이 그 목록이다.
    ⚠⚠ **원장 단위로 훑는 것으로는 부족하다 — 인용 단위로도 훑는다**(8-2R′ [6]):
    ⑨를 신설한 그 절이 `horizon_pitch`를 목록에 올려 놓고 **AS-L19만 고치고 AS-L20을
    놓쳤다.** 한 원장을 **여러 문서 줄이 인용**하고, `@해시` 없는 인용(②형)은
    **자동 모집단 밖**이라 세어지지도 않는다. → 원장 이름으로 `grep -rn "이름.json"`을
    돌려 **줄 전부**를 본다.
    ⚠⚠ **그리고 값을 읽을 때 옆 행과 헷갈리지 않는다**(8-2R′ [1]): 같은 절이
    `axis_snap`의 **위약 행을 끔 행으로 읽어** "격차가 사라졌다"를 적었다(끔 948, 위약 1091).
    **위약이 본 팔과 같은 값이면 표가 세로로 오독을 유도한다** — 그때는 행 이름을
    함께 적어 인용한다. **발견한 하나만 고치면
    나머지는 "이번엔 대상이 아니었다"로 조용히 넘어간다**(⑥의 면제 논리가 다른 옷을 입은 것).

    ⑧ ⛔ **"원장을 다시 돌렸으면 그 원장의 번호가 전부 해당"은 전부를 다시 돌리면 무너진다.**
    8차 항목 0은 원장 45개를 **전부** 다시 돌렸고, 그 합집합은 **번호 38개** —
    이 목록의 사실상 전부다. 그러면 착수 표는 **판별력이 0**이 된다(#11의 형태: 분모가
    전부면 비율이 아무 말도 안 한다).
    → **대조 대상은 "다시 돌린 원장"이 아니라 "그 항목의 결론이 인용하는 원장"이다.**
    항목 1은 `order_lock` 하나(14번), 항목 2는 `order_lock`+`rule_camera`(21번)이고
    그 크기는 손으로 대조할 만하다. **전부 다시 돌린 항목은 결론이 실제로 딛고 선
    원장만 고른다** — 고른 사유를 착수 표에 함께 적는다.

### 인용 검사가 안 잡는 세 형태 (2026-08-18 8차 지시 1-b — 실제로 갈라 확인했다)

`scan_citation_hashes`는 **`원장.json@해시` 꼴만** 보고 **해시만** 대조한다. 그러므로:

| 형태 | 자동이 잡나 | 왜 |
|---|---|---|
| ① **해시가 있고 값이 다르다** | ⚠ **거의 못 잡는다**(8차 리뷰어 [9]로 정정) | ⚠ 옛 서술 "상수가 같이 바뀐 경우에만 잡는다"는 **과대였다.** `scan_citation_hashes`는 **값을 대조하지 않는다** — 잡는 것은 **해시 불일치**뿐이고, 상수가 바뀐 경우조차 값은 못 보며 **해시를 고치는 순간 침묵한다**. 알고리즘·픽스처만 바뀌면 해시가 그대로라 아예 안 걸린다(7차가 실제로 그랬다). **`scan_cited_values`가 조건부로 덮는다** — 그 줄의 수치가 **하나도** 원장에 없을 때만이다. `D-L52`의 `1.2411`이 그렇게 잡혔다 |
| ② **해시가 아예 없다** | ⛔ **못 잡는다** | 정규식이 `@해시`를 요구하므로 `snap.json의 by_density` 같은 인용은 **덮는 대상 밖**이다 — 세지도 않는다. ⚠⚠ **이 구멍이 실제로 뚫려 있었다**(8차 리뷰어 [6]): 되살리려던 6차의 "각차 399 · 거리 400"을 **저장소의 실제 문장으로** 넣어 보니 그 줄이 `confirm_rules.json`을 **해시 없이** 인용해서 `scan_cited_values`가 **아예 안 봤다**(덮는 대상 0). 같은 주장을 **인용 규약대로** 적으면 잡는다(3개 전부 ∉). 즉 **원본은 못 잡았을 것이다** — 검사가 아니라 **적는 형식**이 판정한다. 저장소의 ②형은 **227줄**이고(`.md` 약 140 · 나머지는 코드 주석) 플래그로 만들면 홍수라 안 만들었다(#38·#32) |
| ③ **원장에 아예 없는 수치** | ⛔ **못 잡았다 → 8차 항목 1-c에서 `scan_cited_values`를 만들었다** | 가장 나쁘다. 6차의 “합성 400구도에서 각차 399 · 거리 400”이 **어느 원장에도 없었고 그것으로 설계가 뒤집혔다**(7-R [①-a]·#25). 옛 검사는 **원장 이름**이 없을 때만 걸었지(`unresolved`) **수치의 존재**는 안 봤다 |

⚠ ①·②는 여전히 **사람이 본다.** ②를 정규식으로 넓히면 해시 없는 인용을 “인용”으로 세게 되어
**덮는 수만 늘고 대조는 여전히 안 된다** — 그것은 `#38`·`#40`이 금지하는 **덮는 수 부풀리기**다
(8차 리뷰어 [15]가 여기 있던 `#19` 인용을 정정했다 — #19는 **검사를 약화**시키는 편집이라
번호가 안 맞았다).
고칠 쪽은 **검사가 아니라 적는 쪽**이다: **인용은 `원장.json@해시` 꼴로 적는다**(CLAUDE.md §5).

43. **카운터를 경로별로 나눈다. 한 경로만 세면 0이 부재의 증거로 읽힌다. 합이 전체와
    맞는지 확인한다.**(2026-08-18 10차, 사람 지시)
    9차 항목 4-5의 초판 카운터(`anchorRejected`)가 경로 하나(①)만 세어 **0**이 나왔는데
    배치는 줄었다 — 실제로 줄인 것은 ②였고, 초판대로였으면 "가드가 안 걸렸다"는
    **정반대의 결론**을 적었을 것이다. 같은 세션이 `unanchored_placed`를 하나의 수로 읽다
    경로가 **셋**인 것을 놓쳤다(솔버 일괄 풀이가 셋째였다).
    → 배치·차단·물음처럼 **여러 경로가 한 지표에 합쳐지는 자리**는 경로별 칸을 두고
    **합 = 전체**를 원장이 스스로 검산한다(`placeBy`·`anchorGuardStats`가 선례).
    ⚠ 분자만으로도 부족하다 — **분모(시도 수)가 없으면 0이 "시도 없음"인지 "전부 통과"인지
    안 갈린다**(10차 리뷰어 [3] — #11의 형태).

44. **사전 등록한 양성 채널이 그 항목의 것인지 확인한다.**(2026-08-18 10차, 사람 지시)
    8차가 `p1_invariance`에 *"항목 4-5가 지우면 `unanchored_placed`가 0이 된다 — 이 필드가
    판정자다"*라고 미리 적었는데, 그 필드를 0으로 만드는 것은 4-5가 아니라 **4-3**이었다
    (실측 4 → 3 — 경로 셋 중 4-5는 둘만 막는다). 그리고 10차에서 **그 예측도 틀렸다** —
    4-3 뒤에도 3이다(남은 셋은 첫 앵커의 정당 배치라 0이 될 수 없다).
    ⚠ **필드를 이름으로 구분한다**(11차 리뷰어 [9]): 같은 원장(`p1_invariance.json`)에서
    `unanchored_place/unanchored_placed`도 4 → 3이고 `after_state/lifted`도 4 → 3인데
    **다른 항목의 것**이다 — 앞엣것이 이 항목(9차 4-5)의 값이고, 뒤엣것은 D-L83 되살림
    (10차 항목 0)이 임의 깊이 배치를 막아 되돌린 값이다. 경로 이름 없이 "4 → 3"만 적으면
    두 감소가 한 성과로 읽힌다(#43의 형태 — 경로가 합쳐진 자리).
    → 양성 채널을 등록할 때 **그 지표를 움직이는 경로 전부**를 먼저 갈라 적고, 등록한
    항목이 그중 **어느 경로를 막는지**를 명시한다. 예측이 틀리면 틀렸다고 그 자리에
    적는다(#26) — 조용히 다음 항목의 채널로 옮겨 달지 않는다.

45. **느려졌다는 보고를 받으면 디스크 여유부터 본다.**(2026-08-18 10차, 사람 지시)
    9차에서 전량 검증이 **121초 → 1시간 52분**(59배)이 됐고, 원인은 하네스가 아니라
    **디스크 여유 공간 0**이었다. 하네스 탓으로 읽었으면 검증 구조를 헛되게 뜯었을 것이다.
    → `stage0/out/test_cost.json`의 `environment`가 그 자리다 —
    실행 시간의 회귀를 의심하기 전에 그 값부터 읽는다(`npm run test:cost`).

    ⛔⛔ **그런데 그 자리가 판정하는 양을 안 적고 있었다**(2026-08-19 12차 후속 · #49의 형태).
    옛 필드 `disk_free_bytes`는 `statfs`의 **`bfree`**(파일시스템 자유 블록)인데,
    이 컨테이너에서 쓰기를 막는 것은 **세션 할당량**이다. 실측:
    **bfree 244.5G ↔ bavail 29.6G**(= `df` Avail 30G) — **한 자릿수가 다르다.**
    즉 `bavail`이 0으로 가도 그 필드는 **244G를 내며 "여유 충분"이라 말한다** —
    자기가 잡으려고 만든 실패를 못 잡는 상태다(#38의 "덮는 대상이 사실상 0"과 같은 자리).
    9차에 맞아떨어진 것은 그때 **두 양이 함께 0**이었기 때문이고, 그것을 일반 보장으로 읽었다.
    → **고친 쪽은 검사가 아니라 기록하는 쪽이다**(#38): `disk_avail_bytes`(**판정**)를 넣고
    `disk_free_bytes`는 연혁으로 남기되 **판정에 쓰지 않는다**고 원장이 스스로 적는다.
    ⚠ **그리고 그 블록은 하네스가 마지막으로 돈 시각의 스냅샷이다** — 보고를 받은 **그
    시점**의 값은 `df`로 직접 읽는다(이번에 원장은 08-18 16:02의 값이었다).

46. **판별값의 크기가 픽스처 상수에서 오면 정보량이 0이다.**(2026-08-18 11차, 사람 지시)
    `cube_frame`의 옛 기준계 팔이 낸 두 값은 **항등 1**(`dot_gaze_world_z` — 옛 배선이
    세계 −z로 나는 한 다른 값이 안 나온다)과 **cos35°·cos15° ≈ 0.7912**
    (`dot_gaze_depth` — 이 픽스처의 요·피치 곱 외의 값이 안 나온다)뿐이다.
    (`cube_frame.json@f351839a`의 `gate.reachability_value` — **연혁이 아니라 현행 값이다**.
    ⚠ 그러므로 이 인용 자체가 #47의 대상이다: 요·피치를 흔든 둘째 구도가 생기면 낡는다.
    낡으면 그 자리에서 원장을 다시 읽는다.)
    도달 가능성 값으로 처음 적은 것도, 리뷰어 [1]이 그것을 기각해 갈아 끼운 대체값도
    **같은 종류의 상수**였다 — 두 번 연속 크기를 근거로 썼다.
    → 원장이 **실제로 재는 양**이 무엇인지 그 자리에 적는다. `cube_frame`이 재는 것은
    크기가 아니라 **자리바꿈**(정확히 1이 어느 슬롯에 오는가 — 비행 방향의 범주 사실)이고,
    두 팔의 실행이 그 바꿈을 실측한다. 크기가 정보를 실으려면 **그 상수를 움직이는
    둘째 구도**가 필요하다(요·피치를 흔들어 값이 cos(요)·cos(피치)를 따라가는지 — DEFERRED).
    ⚙️ **측정량이 범주(1비트)일 때 `reachability_value`에 무엇을 넣는가** — 한 값으로는 못 적는다.
    ① 범주를 가르는 **두 팔의 값을 쌍으로** `reachability` 산문에 적고(#28: 못 고르면 둘 다),
    ② `reachability_value`에는 **실행이 정하는 쪽**을 넣되 ③ `reachability_value_fixture_determined: true`로
    표시한다. 자동 검사(`scan_gate_reachability`)의 **값 대조 통과를 근거로 쓰지 않는다** —
    그 검사는 값이 원장에 있는지만 보지 그 값이 실행의 양인지는 모른다.
    ⚠ #40의 ④("다른 원장에서 복사한 값")와 다른 누출이다 — 값은 **이 실행의 것**인데
    실행이 그 값을 정하지 않는다. 판별법: **픽스처를 안 바꾸고 코드만 틀리게 해서 그 값이
    움직이는가**(#5의 유형 3과 같은 물음, 대상이 도달 가능성 값이다).

47. **실행마다 낡는 수치를 산문에 박지 않는다 — 필드 참조로 쓴다.**(2026-08-18 11차, 사람 지시)
    `order_lock` 하네스의 게이트 산문이 자기 원장의 값(1923/1965/46/52)을 문자열로 들고
    있었고, D-L85(반경 8)로 그 넷이 전부 낡았다 — **원장이 자기 자신보다 낡는다.**
    (그 넷은 `gate.result`의 `beyond_cut_0_5_*`·`within_cut_0_2_*`다. ⚠ **팔 이름 없이
    늘어놓으면 어느 팔의 값인지 안 갈린다**(#42 ⑨) — 현행은 `beyond_cut_0_5_fixed` ·
    `_bypass` · `_guard_off`가 각각 다른 값이고, 산문의 화살표가 어느 팔의 시간 변화인지
    필드 이름 없이는 못 읽는다. 인용할 때 **팔 이름을 붙인다**.)
    같은 형태가 문서에서는 #34·#42 ⑥로 반복해 잡혔는데, 하네스 산문은 원장을 재생성해도
    함께 안 고쳐지므로 **자동 검사(`scan_cited_values`)의 사각지대**다(같은 파일 안이라
    "원장 값이 하나라도 있으면" 조건을 늘 만족한다).
    → 산문은 **어느 필드를 그 자리에서 읽으라**는 지시와 **연혁**(무엇이 왜 움직였는가)만
    담는다. 수치는 필드가 든다. 임계처럼 값 자체가 결정인 것은 `thresholds`에 두고
    산문이 그것을 이름으로 가리킨다.
    ⚠ 문서 쪽의 같은 규칙은 `산출물.json@해시` 인용(§5)이고, 이 항목은 **원장 안쪽**이다.

48. **검정의 단위를 먼저 정한다 — 한 실행 안의 여러 관측은 독립 시행이 아니다.**
    (2026-08-18 11차, 리뷰어 지적으로 결론이 뒤집혔다)
    AS-L35의 희소성 논증을 "기대 4.1인데 관측 0"으로 **철회**했는데, 그 기대는
    **배치 단위** 포아송이었다. 한 실행의 획 12~24개는 **같은 카메라**를 공유하므로
    독립이 아니다 — 실제로 `two_box`의 성공 33건은 **다섯 실행**에서 났다.
    **실행 단위**로 다시 내니 기대가 1.4로 줄고 P(0) = 0.24라 **안 기각된다.**
    → 검정을 쓰기 전에 **무엇이 한 번의 시행인가**를 적는다. 같은 카메라·같은 픽스처·
    같은 시드를 공유하는 관측은 **묶어 센다**. 원장에 그 단위를 필드로 남긴다
    (`runs_within_cut_0_2`가 그 자리다).
    ⚠ 반대 함정도 있다 — 묻는 것이 "**하나도 없는 것이 놀라운가**"이면 단위가 실행인 것이
    맞고, "**얼마나 자주 나는가**"이면 배치 단위가 맞다. **물음이 단위를 정한다.**
    ⚙️ 그리고 **편향의 방향을 함께 적는다**: 두 팔의 획 수가 다르면 실행 단위 성공률도
    다르다 — 그 방향이 결론을 강화하는 쪽인지 약화하는 쪽인지가 판정의 강도다.

49. **분리·차단을 그 결정이 쓰는 단위로 잰다.**(2026-08-18 11차, D-L88)
    `osnap_two_sided`가 "두 덩어리가 떨어져 있다"를 **참 정점 사이 거리 40px**로 쟀는데,
    스냅이 실제로 보는 것은 **가장 가까운 후보까지의 거리**다. 그 둘이 두 자릿수 어긋나
    ① 1점·2점 구도에서 **어떤 반경에서도 잘못된 연결이 안 났고**(도달 불가를 안전으로 오독)
    ② 오프셋을 줄여 고치려 하자 두 덩어리가 **화면에서 포개졌다**(상자 겹침 0.12~0.80).
    **경계 상자 비겹침**으로 바꾼 2판도 실패했다 — 축 정렬 상자가 회전한 상자에 과대해서
    구도 셋이 통째로 빠졌다. **3판이 되어서야** 조리개와 같은 단위(`min_cand_px`)가 됐다.
    → 판정을 정하는 양과 **다른 단위**로 조건을 걸면, 그 조건은 조여도 느슨해도 엉뚱한 곳을
    자른다. **먼저 "무엇이 이 판정을 정하는가"를 적고 그 단위로 조건을 쓴다.**
    ⚠ 실패한 기준도 원장에 남긴다 — 다음 사람이 같은 순서로 두 번 틀린다.

50. **대기·폴링 루프가 자기 자신을 매칭한다. 그리고 타임아웃이 없으면 그것은 무한이다.**
    (2026-08-19 12차 후속, 사람 지시)

    실제로 걸린 것: 리뷰어 서브에이전트의 종료를 기다리는 셸을
    `until [ -f OUT ] && ! pgrep -f "<에이전트 id>"; do sleep 20; done`으로 짰다.
    **그 루프의 명령줄에 `<에이전트 id>` 문자열이 그대로 들어 있으므로 `pgrep -f`가
    자기 자신을 잡는다** — `! pgrep`이 영원히 거짓이고 조건은 **구성상 만족 불가**다.
    앞선 라운드가 8~17분이었는데 이 셸은 **2시간 13분** 돌았고, 사람이 그것을 보고 멈췄다.
    재현(존재하지 않는 문자열로도 성립한다):
    `bash -c 'pgrep -f "SELFMATCH_PROBE_XYZ" && echo SELF-MATCH'` → **SELF-MATCH**.

    → 규칙 셋:
    **① 에이전트·백그라운드 작업의 종료는 폴링하지 않는다.** 하니스가 완료 통지를 주고
       `TaskOutput(block, timeout)`이 그 대기를 대신한다. 이 사건에서 **결과는 이미
       그 경로로 받아 대응까지 끝나 있었고**(커밋 완료) 루프는 순수 낭비였다.
    **② 굳이 셸로 기다린다면 패턴이 자기 명령줄에 안 뜨게 한다** — 변수로 조립하거나
       `[a]08c…` 꼴로 쪼갠다. **그리고 프로세스 생존이 아니라 산출물의 내용**(완료 표식)을
       조건으로 쓴다.
    **③ 타임아웃 없는 폴링을 만들지 않는다.** 상한 회차를 박고(예: 20분) 넘으면 **포기하고
       진행한다** — 대기가 진행을 막고 있지 않다면 포기는 손실이 아니다.
    ⚠ **#45와의 순서**: "느려졌다"는 보고에서 **디스크를 먼저** 보는 것은 그대로다.
    이번에는 디스크가 아니었고(가용 29.6G) 그다음이 **폴링 조건 자체**였다 —
    조건이 만족 불가면 그 대기는 느린 것이 아니라 **끝나지 않는 것**이다.

51. **이론서 인용이 어느 차수 전용인지 확인한다.**(2026-08-20 16차, 사람 지시 1-a)
    6.2의 |PE|=f와 15.1의 '바로 위/아래'가 **2점 전용**인데 3점에 썼고,
    단위 팔이 2점만 돌아 못 잡았다. **팔이 전 차수를 도는지 본다.**
    실제로 걸린 것(15차 항목 7): `|PE| = f`를 일반형으로 읽은 초판이 단위에서는
    통과했다 — 픽스처가 2점뿐이라 항등이 성립했기 때문이다. 종단(3점 픽스처)에서
    상대오차 0.23으로 났다(AS-L55). 15.1의 «바로 위/아래»도 같은 모양이었다(AS-L54) —
    그 절에는 «2점»이라는 말조차 없고 문맥이 좌표 규약(`(a,0,c)`의 y=0)에 숨어 있다.
    → 이론서를 인용할 때 **그 절의 전제(차수·주점 위치·롤)를 먼저 적고**, 그 인용을
    재는 팔은 **2점·3점(·롤 3점)을 전부** 돈다. 16차의 «만드는 경로 셋» 단위 팔이
    그 형태다(`test/aux_vp.test.ts`의 `ORDERS` — 픽스처 하나가 세 차수를 다 낸다).
    ⚠ #12(동작점 하나)의 특수형이다 — 차수는 «그중 하나를 고르는 축»이라 명시한다.

52. **하네스가 앱과 같은 필터·조건을 다시 걸면 앱 쪽을 지워도 같은 답이 나온다.
    개입이 실제로 닿는지 확인한다.**(2026-08-20 16차, 사람 지시 1-b)
    실제로 걸린 것(15차 항목 6): `S2S.snapCands`의 초판이 종류 필터를 스스로
    붙였다 — 종류 필터는 `appSnapAt`이 거는데 하네스가 같은 필터를 다시 걸어서,
    앱의 필터를 지워도 «그 종류만 끄면 사라진다»가 그대로 나왔다. 측정이 앱보다
    넓은 것을 보다가 하네스 자신이 좁힌 것이다. 판정을 앱의 승자(`S2S.snap`)로
    옮겨 고쳤다.
    → 판정 함수는 **앱이 실제로 지나는 경로 하나**를 쓴다(#17의 하네스판).
    검산: **앱 쪽 개입(필터 제거·스위치 끔)이 원장 값을 실제로 바꾸는지** 한 번
    뒤집어 본다 — 안 바뀌면 하네스가 자기 자신을 재고 있는 것이다.
    같은 이유로 16차 `aux_tap`의 광선 판정은 각도 공식을 다시 쓰지 않고
    **공선성**(E·탭점·소실점 점-직선 px)으로 한다.

53. **팔 순서가 장면을 바꾸는지 확인한다.**(2026-08-20 16차, 사람 지시 1-c)
    실제로 걸린 것 둘(15차 항목 4·7): ① `hover_lock`의 터치 팔이 **궤도로 가
    시점을 돌렸고**(손가락 하나 = 궤도), 뒤 팔(무호버 펜)이 돌기 전 좌표로 대는
    바람에 **전량 실행에서만** 실패했다(단독 실행은 회전이 작아 통과). ② `aux_vp`의
    정점 팔이 획을 더해 뒤 팔의 분모가 움직였다.
    → **팔마다 장면을 초기화하거나 순서 무관을 검증한다.** 초기화가 비싸면
    각 팔이 **자기 앞 상태를 그 자리에서 읽어**(`state_before`·`strokes_before`)
    기준선을 팔 안에 둔다 — 16차 `horizon_chip`·`aux_tap`이 그 형태다.
    ⚠ 판별 신호: **단독 실행과 전량 실행의 결과가 다르면** 십중팔구 이 함정이다 —
    흔들림(타이밍)으로 읽고 재시도를 늘리면 원인이 숨는다.

54. **파생값을 «표시용»과 «판정용»으로 나눠 계산하면 그 둘이 갈린다.**
    (2026-08-20 17차, 사람 지시)

    실제로 걸린 것: 소실점을 **표시**는 `viewOverlayCtx().vps`(지금 시점의 투영)에서,
    **스냅**은 `cam.vps()`(확정 시점의 값)에서 읽었다. 둘 다 «파생값을 저장하지 않고
    계산한다»는 규약을 지켰는데도 **계산이 둘이라 갈렸다** — 갈리는 조건은 «비핀»이 아니라
    **«자세가 확정 카메라와 다름»**이고(핀을 풀되 자세를 보존하면 여전히 같다 — 음성 대조가
    원장에 있다), 그때 **스냅이 보는 각**이 벌어진다(`vp_source.json@f351839a`의
    `summary.legacy_source_aim_deg_rotated` — **수는 원장이 정본**, #47).
    사용자는 «그은 선이 모이는 점이 표식이 아니다»로 봤다. 표시 안에서도 `drawDirLines` 하나만 다른 쪽을 읽어 **한 화면에서**
    두 소실점이 공존했다.
    → **«저장하지 않는다»로는 부족하다. «한 함수에서만 계산한다»까지 가야 한다**(#17의
    파생값 판). 같은 양을 두 번 계산하는 코드를 보면 그 둘이 **언제 갈리는지**(여기서는
    «자세가 확정 카메라와 다를 때») 먼저 적는다 — 갈리는 조건이 있으면 그것은 하나로 모을 자리다.
    ⚠ **되살림 스위치를 공유 함수 안에 두지 않는다**(#52의 이 항목판): 초판이 스위치를
    `screenVps` 안에 넣어 **표시까지 함께** 옛 배선으로 옮겼고, 그러면 되살려도 두 쪽이
    같이 움직여 어긋남이 안 나온다(회귀 팔이 **전건 0**으로 잡았다 — 개입이 값을 실제로
    바꾸는지 뒤집어 본 검산이다). 스위치는 **되살릴 쪽 한 곳**에만 건다.
    ⚠ 그러면 진입점이 둘이 된다(`vpsOnScreen`·`vpsForSnap`) — **스위치가 켜진 동안 둘이
    같은 값임을 팔이 강제한다**(`entries_agree`). 안 강제하면 «하나로 모았다»가 문서상의
    주장으로만 남는다. ⚠⚠ 그리고 그 일치는 **보장이 아니라 측정이다** — 코드 형태가
    강제하는 것이 아니라 두 함수가 같은 규칙을 부르기로 한 것뿐이라, 누가 한쪽만 고치면
    갈린다. 게이트에 **등록하고** 검사를 건다(초판이 «보장»으로 적었다가 2차 검토에서
    되돌렸다 — #5의 반대 방향 오분류다).
    ⚠⚠ **어긋남의 크기는 그 판정이 쓰는 단위로 적는다**(#49): 초판이 px로 적었는데
    `vpDirSnap`이 보는 것은 **수직거리 ÷ 길이**(`AXIS_TOL.vp_dist_ratio`)라 px는 소실점이
    멀수록 부풀어 **임계와 단조가 아니다**(실측에서 2551px가 1.9°, 437px가 5.2°로 역전했다).

55. **사후에 이유를 재구성하면 자기에게 유리한 쪽으로 기운다.**
    (2026-08-20 17차 후속, 사람 지시)

    근거를 물으면 «그럴듯한 출처»가 먼저 떠오르고, 그것이 **그 시점에 실제로 있었는지**는
    확인하지 않은 채 근거로 나간다. 재구성은 **기억이 아니라 추론**이고, 추론은 자기 행동을
    정당화하는 쪽으로 기운다.

    실제로 걸린 것(이 항목): 착수 근거로 든 둘이 **그 시점 커밋에서 반증됐다**.
    ① «착수 시 최근 다섯에 #54가 있었다» → `a467191^`(= `02d1bc1`)의 최근 다섯은
       **#53·#52·#51·#50·#49**이고 `#54`는 **0건**이다. **#54는 이 세션이 만든 번호다** —
       자기가 등재한 것을 «착수 시 읽은 것»으로 되돌려 적었다.
    ② «HANDOFF가 버전 표시·서비스 워커를 들고 있었다» → 같은 커밋에서 **grep 0건**.
       (그리고 그 작업은 이 세션이 한 적이 없다 — 시작보다 앞선 커밋에 이미 있었다.)
    사람이 두 커밋을 직접 열어 확인했고, **내 재구성은 둘 다 틀렸다.**

    → 규칙 셋:
    **① 근거를 댈 때는 «그 시점 커밋»에서 확인한다** — `git show <sha>:<file>`로 그 자리에서
       읽고, 확인한 명령을 함께 적는다. «있었을 것이다»는 근거가 아니다.
    **② 확인 못 하면 «모른다»로 적는다.** 그럴듯한 경로를 채워 넣지 않는다 —
       빈 칸이 틀린 답보다 낫다.
    **③ 착수 기록과 사후 판정을 한 표에 섞지 않는다.** 「착수 시 읽었다」로 머리를 단 표에
       **나중에 생긴 번호**가 행으로 들어가면 그 표 전체가 사후 재구성이 된다.
       읽은 것은 **파일 목록**으로, 판정은 **언제 들어왔는지 표시해서** 따로 적는다.
    ⚙️ **읽는 절차 자체도 고쳤다**(사후 지시 1): 이 파일의 「최근 다섯」 표가 **상단**에
    있어서 `tail`로 읽으면 지나쳤다. 표를 **파일 끝**으로 옮기고 `CLAUDE.md` §2의 착수
    명령을 `tail -40 PITFALLS.md`로 못 박았으며, `selfcheck.py`의
    `scan_pitfalls_table_last`가 «표가 마지막 절인가 · 그 명령이 절을 다 덮는가»를 지킨다.
    **유인을 이기려 하지 말고 읽는 자리에 두는 쪽**을 골랐다(A-3).
    ⚠ **#32(미실행을 관측으로 세지 않는다)의 자기 자신 판**이다 — 거기서는 하네스가 안 돈
    것을 «봤다»로 셌고, 여기서는 안 읽은 것을 «읽었다»로 셌다.
    ⚠⚠ **#42 ⑪(공집합도 위반)과 방향이 반대다**: ⑪은 «안 적는 것»을 막고, 이것은
    «없던 것을 적는 것»을 막는다. 둘 다 착수 표가 **사실의 기록**이어야 한다는 하나의 규약이다.

---

## #56 — 깨지지 않고 죽는 원장

56. **깨지지 않고 죽는 원장이 있다.** 갈래를 나눈 검증은 안 도는 갈래에서 조용히 썩는다.
    (2026-08-20 18차 · 이 줄이 `selfcheck.scan_pitfall_citations`의 등록부다 — 19차가 이 형식이 빠져 «#57은 없는 번호»로 잡히는 것을 만났다. **새 항목은 이 줄을 갖는다.**)

**무엇이 났나**(2026-08-20 18차): 새 절차(D-L109)가 지평선을 풀이의 전제로 만들자
측정 하네스 셋이 함께 무너졌다. 둘(`rule_camera`·`axis_snap_measure`)은 **깨져서**
드러났고, 하나(`order_lock`)는 **통과한 채로 죽었다** — `camera_ok`가 **0/300**인데
그 자리에 하한 검사가 없었다. 그 원장은 그 상태로 커밋됐고 다음 세션이 인용할 수 있었다.

**왜 늦게 드러났나** — 두 겹이다:
**① 갈래 분리**(9차 지시 b·d)로 항목마다 도는 것은 `npm run test:unit`이고
   측정 갈래는 **원장을 건드린 항목만** 돈다. 그러면 «아무도 안 건드린 원장»은
   **한 세션 내내 안 돈다** — 그 사이에 죽어도 아무 데도 안 나온다.
**② 통과가 침묵과 같다** — 원장이 0으로 가득 차도 시험이 안 깨지면 사람이 볼 이유가 없다.

**규칙 셋:**
**① 성공률에는 하한을 건다.** `camera_ok`·`placed`류를 내는 하네스는 그 수가
   **0이 아님**을 스스로 단언한다(#38의 「덮는 대상 0을 통과로 읽지 않는다」와 같은 자리).
**② 절차를 바꾼 항목은 «그 절차를 전제로 하는 원장 전부»를 한 번 돌린다.**
   깨지는 것만 보지 말고 **값이 죽었는지**를 본다. 이번에 셋 중 하나가 그 차이였다.
**③ 손 스캔으로 끝내지 않는다** — `selfcheck.py`의 `scan_dead_ledger`가
   성공률 0을 원장별로 세어 낸다. **의심이지 오류가 아니다**(특정 팔의 0은 정상) —
   그래서 자리 수를 함께 내고, 원장 전체가 0이면 그 수가 크게 나온다.
   ⚠ 반례로 확인했다: 죽은 원장을 흉내 낸 임시 파일에서 6자리가 떴고,
   정상 원장 셋은 2·3·5자리(전부 특정 팔·구도)다.

⚠ **#32의 이웃이다** — 거기서는 «안 돈 것»을 관측으로 셌고, 여기서는 «죽은 것»을
통과로 셌다. 둘 다 **침묵을 성공으로 읽는** 하나의 형태다.

---

## #57 — 폐기가 지표를 죽인다

57. **폐기가 지표를 죽인다.** 기전을 지운 항목은 그 기전을 재던 게이트를 함께 처리한다.
    (2026-08-20 18차 · 등록부 줄 — 위 #56의 주와 같다)

**무엇이 났나**(2026-08-20 18차): 새 절차가 `screen_or_depth` 물음을 지웠는데,
`order_lock`의 등록 조항 ①·③은 **그 물음의 «우회 살림»으로 두 팔을 가르고 있었다.**
물음이 없어지자 `bypass`와 `fixed`가 **전 필드 동일**이 됐고
`gate.result.pass_order`가 `false`로 남았다 — «나빠졌다»가 아니라 **«가를 것이 없다»**인데
필드는 둘을 구분하지 못한다. 같은 세션의 문서 셋이 그 false를 **«통과»로 적었다**(#41 재발).

**규칙 셋:**
**① 기전을 지우면 그 기전을 재던 게이트를 찾는다.** 지우는 항목의 폐기 목록에
   «이 기전을 판별자로 쓰는 원장»을 함께 적는다. 코드만 지우고 게이트를 두면
   **판정이 영원히 도달 불가**가 된다(D-L47의 `horizon_pitch` 조항 ①이 그 선례다).
**② 판별력이 죽은 조항은 «통과/실패»로 적지 않는다.** 두 팔이 동일하면 그 조항은
   **판정 불가**다 — 원장에 `..._discriminates: false`류를 내고, 산문은 «가를 것이 없다»로
   적는다. 부등호의 참·거짓을 결론으로 옮기지 않는다.
**③ 판정은 필드를 그 자리에서 읽고 적는다**(#41·#47). 「통과」·「실패」를 산문에 박으면
   다음 실행에서 뒤집혀도 아무도 모른다. 이 원장은 **12차에 같은 형태를 한 번 냈고**
   (산문 «실패한다» ↔ 필드 true) 18차에 **부호만 반대로 재발했다**.

⚠ **되살아나는 방향도 같다**: 같은 절차 변경이 `horizon_pitch`의 죽은 조작 변수를
**되살렸다**(참 3.4499° · 중앙 21.1871° · 위약 37.4237° — AS-L19 부활).
지표는 기전과 함께 죽고 함께 산다 — **폐기·부활 둘 다 그 원장을 찾아서 적는다.**

---

## #58 — 고친 것이 배포되지 않으면 안 고친 것이다

58. **작업 갈래에 푸시한 것으로 «끝났다»고 적지 않는다. 배포까지가 한 항목이다.**
    (2026-08-21 · 사람이 잡았다 — 이 줄이 `selfcheck.scan_pitfall_citations`의 등록부다)

**무엇이 났나**: 비우기 작업을 갈래(`claude/file-bundle-clear-feature-4h54uj`)에 커밋·푸시하고
**«완료»로 보고했다.** 사람이 앱을 열었더니 화면의 빌드 식별자가 `760ba38`(작업 전 커밋)
그대로였다 — **main 병합·푸시가 없었으므로 Pages가 돌지 않았다.** 검증(단위 82건·e2e 12건·
타입·빌드)은 전부 통과했고, 그 통과가 «배포됐다»로 읽혔다.

**왜 났나** — 둘이 겹쳤다:
**① 규약이 읽는 자리에 없었다.** 「main 푸시가 배포다」는 **커밋 메시지와 `progress.md`의
   회차 마감 절**에만 있다(`19차 마감 — 병합·푸시 기록`·`20차 마감 …` 등). 착수 시 읽는
   `CLAUDE.md`·`HANDOFF.md`에는 마감 절차가 **없었다**(둘 다 grep 0건).
   ⚠ 그리고 이 세션의 실행 환경 지침은 «지정 갈래에 푸시하라 · 다른 갈래에 푸시하지 말라»고
   적혀 있어, **갈래 푸시에서 멈추는 쪽이 오히려 규칙을 지키는 것처럼 보였다.**
   둘이 어긋날 때 어느 쪽인지 아무 데도 안 적혀 있었다.
**② 완료 보고에 배포 판정자가 없었다.** 보고에 검증 수치만 있고 **배포된 커밋 해시**가
   없으면, 빠졌다는 사실이 보고 안에서 드러나지 않는다. 「무엇을 쟀나」는 있고
   「그것이 사람 손에 갔나」는 없었다.

**규칙 셋:**
**① 마감은 «main 병합 → 푸시 → Pages 초록 → 화면 빌드 식별자 == 그 커밋»이다.**
   갈래 푸시는 중간 상태다. `CLAUDE.md` §2에 절차를 못 박았다(읽는 자리에 둔다 — A-3).
**② 완료 보고에 배포된 커밋 해시를 반드시 적는다.** 없으면 그 보고는 미완이다.
   ⚠ 적는 것은 **HEAD 해시가 아니라 화면에서 읽은 식별자**다 — 둘이 같아야 배포다
   (다르면 아직 안 퍼졌거나 캐시에 묶인 것이고, `?reset`이 가른다 — web2/NOTES 「배포 전환」).
**③ 「검증 통과」를 「배포 완료」로 쓰지 않는다.** 시험은 저장소 안의 사실이고
   배포는 저장소 밖의 사실이다. **#32(미실행을 관측으로 세지 않는다)의 배포판**이다.

---

## #59 — 기준에 여유를 지어 넣으면 그 대가를 함께 잰다

59. **사람이 정한 기준 위에 자기 여유를 얹었으면, 그 여유가 무엇을 얼마나 비싸게
    만드는지 수치로 낸다. 못 내면 문면대로 둔다.** (2026-08-21 · web2-04)

**무엇이 났나**: 지시는 접을 때 「**대상이 화면에 남도록** 조정할지 판단하라」였다.
구현은 거기에 **「무게중심 주변으로 화면 세로 ¼이 남아야 한다」**는 여유를 얹었다
(기준을 H/2 대신 **H/4**로). 근거는 「pivot은 무게중심이고 기하가 그 둘레에 퍼진다」라는
**말**이었고 측정이 아니었다. 그 여유가 물러나는 거리를 **정확히 두 배**로 만든다는 것을
**나중에 재고서야** 알았다 — 먼 소실점 구도에서 배수 **11.242 → 5.621**.

**왜 났나**: 여유는 늘 «안전한 쪽»으로 보인다. 대가가 **다른 축**(여기서는 카메라가
얼마나 뒤로 날아가는가)에 나타나면, 여유를 넣는 순간에는 그 축이 시야에 없다.
그리고 그 대가가 **구도에 따라 다르다** — 24행을 같은 하네스로 내니 **23행에서 정확히
×2.000**이고, **넓은 화각(f/W = 0.32)의 완만한 앙각 한 행에서만 ×1.931**이다.
그 한 행은 `max(1,…)` 클램프에 걸쳐 있어 **여유가 있으나 없으나 거의 같다** —
그 대역만 봤으면 「비용이 거의 없다」로 읽혔을 자리다(D-5와 짝이다).

⚠ **처음 이 항목을 적을 때 그 행을 「좁은 화각」이라고 적었다** — 픽스처 이름
(`drawnWide` = 소실점이 멀다 = **넓게 그린다**)과 **화각**을 섞었다. 소실점이 멀수록
f가 크고 화각은 **좁다**. 2차 리뷰어가 잡았다. **대역 이름은 화각으로 적는다.**

⚠ **「두 배」는 측정이 아니라 대수적 귀결이다** — `rMin ∝ 1/(H/k)`이므로 분모가 반이면
2배다. 측정이 하는 일은 **어디서 그것이 안 나타나는가**(클램프)를 보이는 것이다.

⛔ **2026-08-21 web2-05: 이 항목의 예시가 폐기됐다.** 여기 인용한 「물러나기」 기전
(눈높이를 유지하고 대상이 화면에 남도록 수평거리를 늘린다)이 **통째로 죽었다** —
사람이 「궤도로 바뀐 높이는 사용자가 정한 눈높이가 아니다」로 지시를 뒤집었고,
접기가 궤도 전 포즈로 통째로 돌아가면서 늘릴 일이 없어졌다.
**교훈은 그대로이고 예시가 폐기된 것**이다 — 그 수(×2.000·1.000·11.242 등)를
현재형으로 인용하지 않는다(#42 ⑥·⑨). 폐기 기록은 `web2/NOTES.md`의 web2-05 절.

**규칙 셋:**
**① 지시가 기준을 문면으로 줬으면 그것이 기본값이다.** 여유를 얹는 것은 **변경**이고,
   변경에는 근거가 아니라 **수치**가 필요하다.
**② 그 수치는 「여유를 넣은 값 vs 뺀 값」의 대비여야 한다.** 「넣은 값이 합리적이다」는
   대비가 아니다 — 뺀 값을 같은 하네스로 함께 내야 두 배인지 1.0배인지 갈린다.
   ⚠⚠ **그 대비를 산문에 적지 말고 하네스가 매 실행에 함께 내게 한다.** 산문에 적으면
   규칙이 또 바뀌었을 때 **낡은 값이 현재형으로 남는다** — 이 항목이 실제로 그랬다
   (기준을 두 번째로 바꾼 뒤 폐기된 판의 수 둘이 문서 넷에 그대로 있었다, #42⑥).
   지금은 `fold_measure.test.ts`가 **기준 셋을 한 줄에** 낸다.
**③ 대가가 구도에 따라 다르면 대역 양끝에서 낸다.** 한 픽스처의 1.000은
   「비용이 없다」가 아니라 「그 갈래가 안 돌았다」일 수 있다.
   ⚠ **양끝만으로도 모자란다** — 이 항목의 양끝은 화각 114°와 20°였고 **둘 다 실무 밖**이다
   (이론서 18.4: 90° 초과는 지각과 어긋나고, 실무 관행은 d ≥ W = 53°). **관행 값을
   가운데에 넣어야** 실사용 대역의 답이 나온다. 그러고 나니 「비용 없음(1.000)」은
   **실무 밖 구도의 성질**이었다 — 실무 대역에서는 접기가 언제나 2.4배 이상 물러났다.
   ⛔ **그 「2.4배 이상」은 폐기된 기전의 값이다**(web2-05가 그 기전을 지웠다 — 지금 접기는
   물러나지 않는다). 여기서는 **방법론의 예시**로만 읽고 현재 동작으로 인용하지 않는다.

⚠ **반대 방향도 같다** — 여유를 «뺄» 때도 대가를 적는다. 여기서는 H/2로 되돌리면서
「대상이 화면 아래쪽에 낮게 앉는다」를 `HANDOFF`의 「화면에서 볼 것」에 올렸다.

---

## #60 — 「유지한다」가 「그 값이 사용자가 정한 것이다」를 뜻하지 않는다

60. **어떤 값을 «유지»하기로 정할 때, 그 값이 사용자가 정한 것인지 조작의 부수 효과인지
    먼저 가른다. 부수 효과를 유지하면 부수 효과를 보존하는 것이다.** (2026-08-21 · web2-05)

**무엇이 났나**: 접기(정렬 복귀)를 만들면서 「**눈높이를 유지한다** — 부감으로 높이
올라갔으면 그 높이에서 수평으로 본다」로 정했다. 그런데 **그 눈높이는 궤도가 만든 것**이다 —
궤도는 카메라를 pivot 둘레로 돌리므로 올려다보면 눈이 내려가고 내려다보면 올라간다.
사용자가 정한 적이 없는 값이다. 실측: 궤도 전 1.600 → 궤도 후 6.590 → **접은 뒤 6.590**.
화면에서는 「지평선이 화면 위로 심하게 올라간다」로 보였다(그림이 그 아래로 내려간다).

**왜 났나**: 「유지한다」는 **보수적이고 안전해 보인다**. 그리고 그 값이 상태에 실제로
들어 있으므로 «있는 것을 지킨다»로 읽힌다. 빠진 물음은 **「그 값이 어디서 왔는가」**다.
카메라 상태처럼 **여러 조작이 같은 필드를 쓰는** 자리에서 특히 샌다 — 필드 하나에
사용자의 의도와 조작의 부산물이 섞여 들어가고, 「유지」는 둘을 구별하지 않는다.

**규칙 셋:**
**① 「유지한다」를 정하기 전에 그 값의 **출처**를 적는다.** 사용자가 정한 것인가,
   다른 조작이 부수적으로 바꾼 것인가. 못 적으면 아직 정한 것이 아니다.
**② 부수 효과를 되돌릴 자리는 «그 조작이 시작되기 직전»이다.** 그 상태를 잡아 두면
   무엇이 의도이고 무엇이 부산물인지 **저장 없이** 갈린다 — 여기서는 「정렬을 떠나기
   직전의 포즈」 하나로 궤도·뷰 큐브·저장 시점이 같은 규칙이 됐다.
**③ 그렇게 고치면 «그 부수 효과를 보정하려고» 세운 기전이 통째로 죽는다.** 여기서는
   「대상이 화면에 남도록 물러난다」가 죽었다 — 원인을 없애면 보정이 필요 없다.
   **죽은 기전을 재던 게이트를 함께 처리한다**(#57). 남겨 두면 전부 통과한 채 아무것도
   안 재는 팔이 된다(#56).

⚠ **징후**: 보정 기전이 자꾸 비싸지면 그 앞단을 의심한다. 이 항목의 보정은 실무 구도에서
카메라를 **2.4~30배 뒤로 날렸고**, 그 비용이 「원인이 잘못됐다」는 신호였다 — 그때는
보정의 기준값만 조정했고 원인은 안 봤다.
⛔ **그 2.4~30배는 폐기된 기전의 값이다**(web2-05가 그 기전을 지웠다). 여기서는 «비용이
컸다»는 크기의 근거로만 읽고, **현재 동작의 수로 인용하지 않는다**.

⚠⚠ **그리고 이 규칙 ③에는 함정이 하나 더 있다**(web2-05 2차 리뷰어 [2]): 원인을 없애면
**보정**이 필요 없어지는 것이지 **요구**가 없어지는 것이 아니다. 여기서 죽은 보정이 답하던
물음(「대상이 화면에 남는가」)은 새 규칙에서도 살아 있고, 다만 **보장이 아니라 대역**이 된다.
그러니 보정을 지울 때 **그 보정이 답하던 물음을 따로 적고 새 규칙에서 어디까지인지 잰다.**

---

## #61 — 두 의도가 한 축에서 겹치면 임계를 옮기지 말고 **물음을 나눈다**

61. **한 임계가 두 의도를 가르는데 그 둘이 같은 축에서 겹치면, 임계를 옮기는 것으로는
    못 고친다 — 축을 하나 더 재서 묶는다. 겹친다는 것 자체가 「축이 모자란다」는 신호다.**

`classifyNext`가 「이 획이 수평인가, 먼 소실점을 겨눈 것인가」를 **처짐 하나**로 갈랐다.
web2-03이 그 임계를 비(2.87°)에서 px(1.5)로 옮겨 «먼 소실점»을 살렸고, web2-06에서
**반대쪽이 깨졌다** — 지평선을 긋고 수평선을 그으면 처짐 **2px**에 33W 소실점이 섰다
(그 획은 작도 획이라 **실행취소도 안 된다**).

**임계를 어느 쪽으로 옮겨도 못 고친다.** 실측 두 점이 역전돼 있기 때문이다:

    수평 의도  h=200 · R=400 · 처짐 5px    → 그 획이 뜻하는 소실점 13.3W
    소실점 의도 h=150 · R=600 · 처짐 3.75px → 20W   ← **더 «평평한» 획인데 소실점이어야 한다**

한 축(처짐이든 각도든)에 임계 하나를 두는 한, 한쪽을 살리면 다른 쪽이 죽는다.
**두 의도가 겹친다는 것 자체가 「축이 모자란다」는 신호**다.

- **고치는 법**: 임계를 옮기지 말고 **다른 축을 하나 더 재서 OR/AND로 묶는다.** 여기서는
  「그 소실점이 작도 대역 안인가」(`VP_FAR_W`)를 더했고, 경계가 `min(6W, h·R/1.5)`가 됐다.
  두 항이 **다른 물음에 답하므로** 상수가 둘인 것이 #12 위반이 아니다 — 다만 팔이
  **어느 항이 이겼는지까지** 재야 그 말이 성립한다.
- ⚠⚠ **새 축의 «자르는 자리»에는 여전히 근거가 필요하고, 대비표가 그것을 대신하지 못한다**
  (web2-06 2차 리뷰어 [N1]·[N2]). 값을 4·6·8·10으로 갈아 끼운 표는 **교환비**를 보일 뿐
  «어디서 자를지»를 말하지 않는다. 이 회차의 하한(1~5W)은 **앞 회차의 게이트**에서 왔고
  (사람 문면에는 상한이 없었다), 상한(6 vs 8)에는 **측정이 없다** — 손 겨냥 오차를 잰 적이
  없기 때문이다. 그럴 때는 **없다고 적고 어느 쪽으로 기울였는지**(보고된 증상 쪽)를 적는다.
  «표가 있으니 근거가 있다»로 닫지 않는다.
- **징후 ①** 「임계를 옮겼더니 반대쪽이 깨졌다」가 두 번 나오면 그때가 축을 더할 때다.
- **징후 ②** 임계를 정하는 근거가 **기계의 성질**(포인터 잡음)인데 실제로 그 대역을 정하는
  것은 **사람의 성질**(겨냥 오차)일 때. `PARALLEL_PX`가 그것이었다(assumptions **AS-C15** 반증).
- ⚠ **어느 쪽으로 틀리는 것이 싼가를 함께 적는다.** 여기서는 갈랐다 — 「수평으로 잘못 읽음」은
  **화면에 보이고**(획이 눕는다) **되돌릴 수 있다**, 「소실점으로 잘못 읽음」은 **조용하고**
  (손 그대로 보인다) **못 되돌린다**(작도 획은 실행취소 대상이 아니다). 그래서 애매한 대역을
  수평 쪽에 붙였다. 대칭인 임계는 없다 — **비용이 비대칭이면 경계도 비대칭이다.**
  ⚠⚠ **「되돌릴 수 있다」는 `web2/` 라인 한정이다**(web2-06 1차 리뷰어 [15]). 이 문서 머리말의
  D-L53은 「**P1(가로선 확정)은 불가역이다**」인데, `web2/`에서는 `p1Locked`가 **저장이 아니라
  계산**이라 그 획을 실행취소하면 잠금이 풀린다(`web2/NOTES.md` web2-02 지시 2 절).
  **두 라인이 갈린 자리이므로 비대칭 논거를 다른 라인에 그대로 옮기지 않는다.**

## #62 — **지우는 것도 유지하는 것만큼 물어야 한다** (#60의 반대편)

62. **되돌리는 규칙을 세울 때 그 조작의 «불변량»을 먼저 적는다. 조작이 바꿀 수 없는 값이
    변해 있으면 그것은 사람이 정한 것이고, 되돌리면 사람의 입력을 지우는 것이다.**

#60은 「궤도가 만든 값을 «유지»하지 마라」였다. 그 규칙대로 접기가 **앵커로 통째로**
돌아가게 고쳤더니, 이번에는 사람이 **궤도 중에 줌으로 맞춘 거리까지 지웠다**
(실측 7.225 → 줌 3.613 → **접은 뒤 7.225**).

**두 잘못은 같은 물음의 양쪽이다: 「그 값이 어디서 왔는가.」**
답을 주는 것은 말이 아니라 **불변량**이다 — 궤도는 pivot 둘레의 **회전**이므로
**반경을 구성상 보존한다**(실측: 여덟 방향 전부 7.225 → 7.225). 그러니

    궤도가 **바꿀 수 있는** 값(피치·롤·베어링) → 궤도 중의 변화는 부산물이다. 되돌린다.
    궤도가 **바꿀 수 없는** 값(반경)          → 변했다면 사람이 정한 것이다. **지킨다.**
    ⚠ **눈높이는 둘 중 하나가 아니다** — 반경에 걸린 배율을 그대로 탄다
      (`pivot.y + (anchor.y − pivot.y)·k`). 줌이 없으면 k = 1이라 앵커 그대로다.

- **고치는 법**: 되돌리는 규칙을 세울 때 **그 조작의 불변량을 먼저 적는다.** 불변량이
  달라져 있으면 그것은 그 조작의 것이 아니다. 상태를 더 저장할 필요가 없다(#54를 안 부른다).
- ⚠ **근거 없이 넓히지 않는다** — 같은 자리의 «팬»은 안 열었다. 팬은 반경을 바꾸므로
  불변량 논증이 안 서고, 「기울어진 화면의 옆」이 접힌 뒤 어디인지는 잰 것이 없다.
  대신 **새는 몫을 수로 적었다** — 팬이 카메라를 2.133 옮기면 반경이 **7.225 → 7.012**가 되고
  접기가 그것을 «줌»으로 읽어 지킨다. ⚠⚠ **그 몫은 눈높이까지 움직인다**(1.600 → **1.562**;
  팬을 200px 주면 **1.879**까지 — 2차 리뷰어 [N4] 실측). #60이 보던 그 양이 다른 경로로
  새는 것이므로 **«안 열었다»가 «안 샌다»를 뜻하지 않는다.**
- ⚠⚠ **이 결함이 한 번도 안 재진 이유는 계산이 `input.ts` 안에 있어서다** — 시험이 앱의 줌을
  **못 불렀다**. 「입력과 시험이 같은 함수를 부른다」는 편의가 아니라 **판별력의 조건**이다.
  옮기고 정적 검사로 막았다(#54의 입력판).

---

## 「최근 다섯」 표의 이력 — 무엇이 밀려났고 왜 본문에서 읽히는가

목록이 36개가 되자 **같은 세션 안에서 #33·#32·#35가 재발했다.** 매 항목마다 전부
대조하는 것은 실패했으므로, **바로 앞 항목들이 만든 규칙**을 앞에 따로 둔다.
(새 항목이 생기면 여기를 갱신하고 가장 오래된 것을 뺀다.)
⚠ **11차 정정(리뷰어 [1]·[2])**: 초판은 #42·#41을 규칙대로 밀어냈는데 **#42는 밀어내면 안 된다** —
`DEFERRED`의 8차 리뷰어 [10]이 *"⑥과 '세 형태'가 착수 시 읽히는 자리(최근 다섯)에 없다"*를
결함으로 올려 **여기 넣는 것으로 해소**했고, `.claude/agents/reviewer.md`도 재발 점검 범위를
**이 표**로 정의한다. 밀어내면 그 해소가 무효가 된다. 그래서 **#42는 📌 상시 행으로 고정**하고
표는 "최근 다섯 + 상시 하나"다. ⚠ 자동 검사는 #42를 **강제하지 않는다** — `scan_cited_values`는
현재 해시가 붙은 인용 줄만 보고(해시 없는 ②형은 모집단 밖), `scan_pitfall_citations`는
**대조 자료를 낼 뿐 착수 표의 누락을 판정하지 않는다**. ⑥·⑧·⑨·⑩·⑪은 전부 사람이 본다.
**#41**(중단 조건에 걸렸으면 실제로 멈춘다)만 밀려났고, 그것은 게이트 산문마다 인용되어
본문에서 읽힌다. ⚙️ **11차 말에 #48·#49가 들어오며 #44·#43도 밀려났다** — 둘 다 이 세션이
계속 인용한 규칙이라(경로별 카운터·양성 채널 소유) 본문에서 읽는다.
⚙️ **12차 후속에 #50이 들어오며 #45가 밀려났다** — 그런데 **#50이 #45를 인용한다**
("디스크 먼저, 그다음이 폴링 조건"). 즉 밀려나도 **읽히는 자리에 남는다**.
⚠ 그리고 #45는 **본문이 이번에 바뀌었다**(읽던 필드가 판정하는 양이 아니었다) — 표에서
빠졌다고 옛 판을 기억으로 쓰면 안 된다.
⚙️ **16차 항목 1에 #51·#52·#53이 들어오며 #46·#47·#48이 밀려났다** — 셋 다 이 세션의
게이트·산문 규약이 계속 인용하는 규칙이라 본문에서 읽힌다(#47은 16차 2차 리뷰어 [11]이
재발을 잡았다 — 표에서 빠지기 전의 마지막 경고로 남긴다).
⚙️ **17차 후속에 #55가 들어오며 #50이 밀려났다** — #50(폴링 루프)은 «에이전트 종료는
폴링하지 않는다»로 이 세션도 그대로 지켰고 본문에서 읽힌다. **등재와 이 표의 갱신을 한 손에** 했다.
⚙️ **17차에 #54가 들어오며 #49가 밀려났다** — #49(판정을 정하는 양의 단위로 조건을 쓴다)는
이 세션의 팔 설계가 **그 규칙에 걸려 한 번 고쳐졌기 때문에** 본문에서 읽힌다 —
초판이 어긋남을 px로 쟀는데 스냅이 보는 양은 **각/비율**(`AXIS_TOL.vp_dist_ratio`)이라
단위를 도로 바꿨다(1차 리뷰어 [2]). **등재와 이 표의 갱신을 한 손에** 했다(아래 ⛔의 재발 방지).
⚙️ **2026-08-21에 #58이 들어오며 #53이 밀려났다** — #53(팔 순서가 장면을 바꾼다)은 그 세션이
**해당으로 판정하고 실제로 걸려 한 번 고쳤다**(비우기 팔이 뒤 팔의 장면을 비우고 새로고침까지 해서,
뒤 팔이 접힌 패널에서 깨졌다 — web2/NOTES 「비우기 · 파일 묶음 대조」). 그래서 본문에서 읽힌다.
**등재와 이 표의 갱신을 한 손에** 했다.
⚙️ **18차에 #56·#57이 들어오며 #52·#51이 밀려났다** — #52(하네스가 앱 필터를 다시 걸면
앱 쪽을 지워도 같은 답)는 이 세션이 **그 규칙에 걸려 한 번 고쳐졌기 때문에** 본문에서
읽힌다: `s_start_check`에 뒤집기 검산을 넣자 **`snapCands`가 종류 필터를 안 건다**는 것이
드러났다(끝점을 꺼도 목록 머리는 `endpoint`, 앱 승자만 `on_edge`로 바뀐다).
#51(이론서 인용의 차수 전제)은 이 세션이 «해당 없음»으로 판정했고 근거를 적었다.
**등재와 이 표의 갱신을 한 손에** 했다.
⚙️ **2026-08-21 web2-05에 #60이 들어오며 #55가 밀려났다** — #55(사후 재구성 금지)는 그
회차가 **해당으로 판정하고 실제로 지켰다**: 두 증상을 재현하고 표식을 낸 뒤에야 고쳤고,
②는 짐작한 자리(스냅 판정)와 실제 자리(화면 표식)가 **달랐다** — 재현이 없었으면 엉뚱한
데를 고쳤을 자리다. 그래서 본문에서 읽힌다. **등재와 이 표의 갱신을 한 손에** 했다.
⚙️ **2026-08-21 web2-04에 #59가 들어오며 #54가 밀려났다** — #54(파생값을 표시용·판정용으로
나눠 계산하면 갈린다)는 그 회차가 **해당으로 판정하고 실제로 그 길을 안 골랐다**:
탑뷰의 요를 「마지막 값을 기억」으로 풀지 않고 **포즈에서 매번 계산**했다(`yawDir` —
화면 위 방향이 답한다). 그래서 본문에서 읽힌다. **등재와 이 표의 갱신을 한 손에** 했다.
⚙️ **2026-08-21 web2-06에 #61·#62가 들어오며 #56·#57이 밀려났다** — 둘 다 그 회차가
**해당으로 판정하고 실제로 걸렸기 때문에** 본문에서 읽힌다: #57(폐기가 지표를 죽인다)은
지시 2가 web2-03의 조항 둘을 **뒤집으면서** 그 규칙대로 «통과»로 안 적고 뒤집힌 값을
박게 했고, 지시 5가 `fold_measure` 원장을 함께 고치게 했다. #56(안 깨지고 죽는 원장)은
지시 4가 `orbitPivot`을 바꾸면서 그 원장을 **고치기 전후로 나란히 돌려** 전 행이 같은 것을
확인하게 했다 — 그리고 그 «같음»이 통과가 아니라 **판별력 없음**이라는 것까지 #57이 말했다.
**등재와 이 표의 갱신을 한 손에** 했다.
⛔ 이 갱신 자체가 **한 항목 늦었다**(16차 2차 리뷰어 [6]) — 항목 1이 셋을 등재하면서
이 표를 안 고쳐, 같은 세션 안에서 «최근 다섯»의 정의가 둘로 갈렸다(항목 0은 #46~50을,
항목 2·4는 #49~53을 읽었다). 등재와 표 갱신은 **한 손에** 한다.

⚙️ **2026-08-28 web2-25에 #75가 들어오며 #70을 내렸다**(본문은 그대로 있다 — 표만 다섯).
⚙️ **2026-08-28 web2-20에 #73이 들어오며 표를 다섯으로 정리했다(#66·#68 내림)** —
①#71(web2-18)·#72(web2-19) 등재 때 표를 다섯으로 안 줄여 여섯 행이 쌓여 있었다(그 자체가
이 절 ⛔의 재발 형태다 — 리뷰어 [16]이 잡았다). #66(반복 제목 앵커)은 이 회차의 문서 패치
전부가 그 규칙(`count==1` 단언)을 걸고 지나갔고, #68(손 오차 픽스처)은 cost20 픽스처가
끝점 지터를 태우며 인용한다 — 둘 다 본문에서 읽힌다. **등재와 표 갱신을 한 손에** 했다
(이번에는 — 늦게 몰아서 한 것 자체가 어김이었다는 기록을 남긴다).
⚙️ **2026-08-27 web2-18에 #71이, 2026-08-28 web2-19에 #72가 들어왔다** — 그때 밀려난
#67(합성 화면으로 판정)·#66은 각각 web2-20 3-e ⑨(기전+가시 병행)·위 줄의 사유로 본문에서
읽힌다.
⚙️ **2026-08-27 web2-16에 #70이 들어오며 #65가 밀려났다** — #65(「색이 유일한 정보」는
대체 채널을 설계하면 무너진다)는 web2-16 3부(대기선 파선)가 그 규칙의 파선 채널을 그대로
이어 쓰고, 지시 자체가 「진하기로 가르지 않는다 — 경도가 이미 쓴다」로 인용한다. 그래서
본문에서 읽힌다. **등재와 이 표의 갱신을 한 손에** 했다.
⚙️ **2026-08-21 web2-07(면 생성)에 #63·#64가 들어오며 #58·#59가 밀려났다** — 둘 다 본문에서
읽힌다. **#58**(마감은 배포까지)은 「⛳ 마감」이 `CLAUDE.md` §2에 못 박혀 있어 **착수 시 읽는
자리**에 그대로 있고, **#59**(여유를 지어 넣으면 대가를 함께 잰다)는 이 회차가 새 임계
(`PLANAR_RATIO`)를 만들면서 그대로 걸렸다 — 값을 지어내지 않고 기존 임계와 같게 두었고,
그 정합의 근거(**분모가 같은 양인가**)를 리뷰어가 묻자 산문이 아니라 팔로 답했다.
밀려난 것이 죽은 것이 아니라 **두 번째 자리에서 계속 읽힌다**는 것이 이 절의 내용이다.

⚙️ **2026-08-26 web2-11(입력 계층·brush)에 #66이 들어오며 #61이 밀려났다** — #61(임계를
옮기지 말고 물음을 나눈다)은 이 회차 착수 표들이 재독·판정했고(해당 없음 근거 기록),
임계 신설 자리(PRESS_Q)의 되돌릴 조건 재설계가 그 정신을 탔다. **등재와 표 갱신을 한 손에** 했다.
⚙️ **2026-08-26 web2-10(표시 마감·색 빼기)에 #65가 들어오며 #60이 밀려났다** — #60
(「유지한다」와 값의 출처)은 #62가 「#60의 반대편」으로 본문에서 되짚고, 접기·궤도를
건드리는 항목이면 어차피 #62를 타고 읽힌다. 이 회차(표시 계층)에는 걸릴 자리가 없었다.

---

## #63 · #64 — 2026-08-21 (web2-07 · 면 생성)

### #63. **재계산이 사람이 붙인 점을 덮어쓴다**

확정 경로가 오스냅으로 «정확히 그 점»을 냈는데 **뒤에 도는 재계산이 그것을 옮겼다.**
`liftAll`이 끝점을 풀 때 **축 풀이를 끝점 매칭보다 먼저** 했고, 축 판정에는 각도 허용
(3.4°)이 있어 «이미 붙은 획»도 그 축으로 읽혀 끝점이 축 위로 미끄러졌다.
실측: 확정 2D `(500,520)` → lift가 놓은 3D의 사영 **`(503.409, 510.455)`**.
증상은 엉뚱한 데서 났다 — **오목 다각형이 안 닫힌다**(마디 7·순환 1·면적 0 = 나무).

- **원칙 d(붙은 좌표가 그대로 확정된다)는 확정 경로에만 걸려 있었다.** 파생 계층에도 같은
  **순서**가 필요하다 — `draft.ts`가 「점이 방향을 이긴다」면 `lift.ts`도 그래야 한다.
- 짐작한 자리(「오스냅이 안 걸렸다」)와 실제 자리가 달랐다. `resolveEnd`를 직접 찍어
  **정확히 (500,520)을 내는 것**을 본 뒤에야 lift로 옮겨갔다(D-1).
- ⚠ **허용각이 있는 판정은 «이미 정해진 것»에 다시 걸면 안 된다.** 배정(어느 축인가)에는
  허용이 필요하고, 좌표에는 필요 없다.

### #64. **화면에서만 겹치는 것이 평면 그래프 전제를 깬다**(가림)

조각들을 **한** 화면 배치로 훑어 최소 순환을 찾았다. 각도 정렬 기반 순환 훑기는
«평면 그래프»를 전제하는데, 3D에서 안 만나고 **화면에서만** 겹치는 선이 있으면 그 전제가
깨진다. 실측(벽 하나 그린 장면): 세로 기둥이 지면 대각선을 화면에서만 가로질러
**마디 10 · 변 11**(순환이 둘 있어야 한다)인데 **순환 1개 · 반변 22 · 면적 0**이 나왔다 —
나무처럼 읽혔고 **벽도 바닥도 면이 안 됐다.**

- **깨진 것은 계산이 아니라 전제다.** 값이 이상한 게 아니라 «이 그래프는 평면이다»가 틀렸다.
  마디를 3D로 합치는 것(가림을 마디로 안 만드는 것)이 **오히려 원인**이었다 —
  옳은 규칙이 다른 층의 전제를 깬 자리다.
- 답은 **전제가 성립하는 부분으로 좁히는 것**: 면은 한 평면에 있으므로 **평면마다** 훑는다.
  한 평면 위의 두 선분이 화면에서 가로지르면 3D에서도 가로지른다. 덤으로 평면성이 구성상
  보장된다.
- ⚠ **차수 2인 마디만 있는 픽스처로는 순환 방향 규칙을 못 잰다** — 「twin에서 시계 다음」과
  「반시계 다음」이 그때 결과가 같다. 삼각형·사각형 팔은 **둘 다 통과한다**(#12의 친척:
  픽스처가 동작점 하나만 덮은 것이 아니라 **가르는 축 자체를 안 덮었다**). 갈래가 있는
  장면을 픽스처에 넣는다(D-5).
- ⚠ **좌표만으로는 못 잡았다.** 단위 팔은 전부 통과했고 스크린샷을 눈으로 보고서야 났다
  (#22의 반대 방향 — 픽셀이 잡은 것).

---

## #65 — 2026-08-26 (web2-10 · 표시 마감·색 빼기)

### #65. **「색이 유일한 정보」는 대체 채널을 설계하면 무너진다**(문서화된 결정의 뒤집힘)

web2-08이 오스냅을 무채색으로 바꾸며 **「축 색 넷은 남는다 — 그쪽은 형태가 없어 색이
유일한 정보다」**를 근거 주석으로 박아 뒀는데, web2-10에서 사람이 실기기를 써 보고
그것을 뒤집었다(「축 스냅이 걸렸을 때 선에 색이 남는다. 색을 뺀다」). 대체는 **파선
안내**다(붙으면 양끝 너머로 무채색 파선 — SketchUp 추론선의 무채색판. 실측: 선 채도
174 → 0 · 너머 painted 0 → 32).

- **「색이 유일한 정보」라는 논증은 «지금 채널이 색뿐이다»의 서술이지 «색이어야 한다»의
  증명이 아니다.** 정보를 지키는 것과 채널을 고르는 것은 별개 물음이다 — 지울 수 없는
  정보라도 실을 채널은 바꿀 수 있다(파선·굵기·형태·진하기 대역).
- **문서화된 결정이 뒤집히면 그 근거 주석을 «현재 사실»로 고친다.** 낡은 근거를 남겨
  두면 다음 사람이 그 논증을 읽고 되돌린다 — 주석이 결정의 화석이 되게 하지 않는다.
  이런 결정 근거가 회차마다 흩어지는 것 자체가 위험이라(색 빼기가 세 번째였다),
  web2-10이 「무엇이 색을 갖는가」 규칙을 render2d.ts COL 머리주석 **한 곳**에 모았다.
- ⚠ 순간 피드백이 둘 이상이면 **진하기 대역도 정보다** — 오스냅(2H·알파 0.5)과
  축 안내(#555 불투명)를 같은 대역에 두면 「점에 붙었다」와 「축에 붙었다」가 안 갈린다
  (web2-08이 COL.snap에서 한 실수의 재발 방지).


## #66 — 2026-08-26 (web2-11 · 입력 계층·brush 렌더러)

### #66. **여러 절에 반복되는 제목을 편집 앵커로 쓰면 문서가 조용히 오염된다**

web2-11 1부가 NOTES에 리뷰 대응 소절을 넣으면서 «### 헤드리스/실기기 가름» 제목을
치환 앵커로 썼다. 그 제목은 **다섯 절에 있었고**, 전역 치환이 web2-10 지시 4·5·8-a·8-b
절에 같은 블록을 **4중 복제**했다 — 그 절들이 자기 회차의 리뷰 지적을 «coalesced·PRESS_Q»
같은 남의 회차 내용으로 말하는 **허위 기록**이 됐다. 2차 리뷰어가 잡을 때까지 아무 검사도
안 걸렸다(원문은 안 지워졌으므로 — 순수 삽입은 diff 통계로도 안 보인다).

- **규칙**: 문서 치환 전에 **앵커의 출현 수를 센다**(`count==1` 단언). 회차 절 안을
  고칠 때는 그 절에만 있는 문자열(제목이면 회차 표식이 든 것)을 앵커로 쓴다.
- **복구 검증은 «원본 삭제 0줄»**이다 — git diff에서 `-`줄이 파일 머리말뿐인 것을 본다.
- ⚠ 같은 형태의 사촌: 원장 재실행이 문서 인용을 낡게 하는 것(#42 ⑥)과 달리 이것은
  **문서가 문서를 오염**시킨다 — 검사가 원장 대조만 보면 영영 안 걸린다.

---

## #67 — 2026-08-26 (web2-12 · 잉크 획 소실)

### #67. **캔버스별 판독만 있으면 «합성이 가리는» 결함은 영영 안 걸린다 — 그리고 팔이 기본 재료로만 그으면 재료축 결함도 못 잡는다**

web2-11의 #brushc(WebGL2)가 `premultipliedAlpha:true`로 선언된 채 버퍼를 (1,1,1,0)
(흰색·알파 0 — 규약 위반 값)으로 지워, 합성기가 흰색을 **가산**해 겹 아래 #gl(Line2·면)
전체가 흰 장막에 덮였다. 잉크 확정선은 Line2만 그리므로 사람 관측은 「펜 획이 떼면
사라진다」였고 — 실제로 사라진 것은 **3D 겹 전체**였다(연필은 brushc가 몸체를 자기 겹에
그려서 «되는» 것처럼 보였다). **e2e 116팔이 전부 통과 중이었다.** 두 구멍이 겹쳤다:

- **구멍 ①**: 모든 픽셀 팔이 «캔버스별 판독»(drawImage → getImageData)이었다 — 캔버스에는
  올바르게 그려졌으므로 전부 통과. **사람이 보는 것은 캔버스가 아니라 합성**이다.
  스크린샷(`page.screenshot`)만이 그것을 잰다(`materials.spec.ts`의 screenBox/diffCount).
- **구멍 ②**: 팔이 전부 기본 재료(HB)로만 그었다 — «재료 하나만 다른 경로를 타는» 결함
  (잉크만 Line2 단독)은 재료 전수 없이는 구조적으로 못 잡는다(D-5의 재료축 판).
- **규칙**: 겹(캔버스 스택)을 더할 때는 **합성 화면을 재는 팔을 같이 세운다.**
  경로가 재료·상태에 따라 갈리는 곳은 **그 축을 전수로** 돈다(GRADES 일곱 × 렌더러 둘).
- ⚠ 진단 순서의 교훈(D-1 재확인): 지시가 준 후보 둘(㉮ grain 관문·㉯ mat.w)은 둘 다
  틀렸다 — 표식(두 경로 나란히, 단계별 화면/캔버스/문서)이 세 홉 만에 실제 갈림
  (#brushc 숨기면 보인다)을 냈다. 후보부터 팠으면 둘 다 허탕이었다.

---

## #68 — 2026-08-27 (web2-14 · 교점 정의가 손으로는 안 된다)

### #68. **팔이 이상적 좌표로만 그리면 «도달 불가»가 통과로 남는다 — 손 픽스처에는 오차를 심는다**

web2-13 4-g(교점 정의)는 팔 다섯이 전부 통과한 채 배포됐는데 실기기에서 «손으로는
안 된다»가 나왔다. 원인: 판정 임계(사영 왕복 0.5px — 수치 동일성)는 옳았지만 그 임계에
견주는 **값**(A의 뗀 끝의 사영)에 손의 뗌 오차(오스냅 반경 8px 대역)가 그대로 실렸다 —
0.5px 문에 8px 오차 값이 들어가면 기능은 산 채로 죽는다. 팔이 그것을 못 본 이유가 이
항목의 내용이다:

- **팔의 픽스처가 전부 «이상적 손»이었다** — 끝점을 오스냅 정확값·축선 위 정확값으로
  그었고, 심지어 몸통 겨냥 팔은 교차점이 **우연히 중점**이라 중점 오스냅이 대신
  잡아 줬다. 오차 0의 손은 0.5px 문을 통과한다 — 실사용 대역(D-5)을 아무 팔도 안 덮었다.
- 게다가 그 좁음이 주석에 **「의도된 보수성」으로 서술**돼 있었다(4차 [41] — «실효 문은
  0.5px · 도달 집합은 끝점·중점뿐»). 서술된 한계는 결함으로 안 읽힌다 — 그 문장이
  실기기 판정으로 뒤집혔다(#65의 판정판: «지금 그렇다»의 서술이 «그래야 한다»의 증명이
  아니다).
- **규칙**: 사람 손이 여는 문을 재는 팔에는 **손의 오차를 심는다** — 겨냥점에서
  오스냅 반경 안쪽의 어긋난 점(2~5px)으로 긋는 픽스처가 기본이다(vptouch.test의 형태).
  그리고 «의도된 보수성»으로 좁힌 도달 집합은 그 좁음 자체를 실기기 판정 표(DEFERRED)에
  올린다 — 주석은 판정을 대신하지 못한다.
- 수리의 형태도 일반화된다: 임계를 넓히는 것이 아니라 **손 오차가 안 실리는 값**으로
  바꿨다(뗀 끝 → A선∩B해석면 교점 — 오차는 «어디서 뗐나»에만 남는다). 임계 완화는
  «조용히 틀린 배치»를 여는 반면 값 교정은 정확도를 유지한다.

### #69. **후보로는 잡히는데 «한 번도 안 이기는» 것은 «없는 것»과 화면에서 구별되지 않는다 — 그리고 한 구속을 채우려고 다른 구속을 깨면 조용히 죽는다**

web2-14가 「대기선 몸통을 `near` 오스냅 후보로」 넣고 팔 넷을 통과시켜 배포했는데
실기기에서 **여전히** 「스냅이 안 잡힌다」였다(#68의 바로 다음 회차 — 같은 기능, 같은
증상). 표식(D-1)이 낸 기전이 **둘**이고 둘 다 「후보 목록에 있는가」 바깥이었다.

**㉠ 우선순위가 가린다 — 후보인 것과 이기는 것은 다르다.**
`OSNAP_ORDER`에서 `ext`·`perp`가 `near`보다 앞선다. 그런데 사람이 실제로 하는 몸짓은
**다른 3D 선에서 이어 긋기**이고, 그러면 그 선의 연장(`ext`)이 조준 경로 **내내**
잡힌다 → `near`는 한 번도 못 이긴다. 실측 분포: ext 220 · near 44 · 없음 11.
「목록에 넣었다」는 코드 리뷰로 확인되고 팔로도 확인되지만(단독으로 부르면 잡힌다),
**사람이 겪는 경로에서 이기는가**는 다른 물음이다. 후보 목록 팔은 그것을 안 잰다.
→ **규칙: 새 후보를 더하면 «그 후보가 이기는 실측 분포»를 낸다.** 「목록에 있다」로
끝내지 않는다. 분포가 0이면 더한 것이 아니다.
⚠ **가림은 «표식»을 죽이지 «결과»를 꼭 죽이지는 않는다** — 그래서 더 나쁘다. 이 사례의
실측(1차 리뷰가 갈라냈다): ext가 이긴 220칸에서 **정의는 전부 섰다**.
⚠ **그 판정은 «237 ≥ 220»이 아니라 2×2 교차표가 낸다**(2차 리뷰가 잡았다 — 부등식은
교차표를 대신 못 한다: ext 220 중 20이 죽고 나머지 55 중 37이 살아도 237이다.
같은 원장이 반례를 든다 — 반경 밖에서는 ext가 22칸 이겼는데 정의는 0이다).
교차표 실측: `ext∧정의 220 · ext∧미정의 0 · 비ext∧정의 17 · 비ext∧미정의 38`.
사람이 본 「안 잡힌다」는 **표식이 없는 것**이었고 결과는 뒤에서 소리 없이 서기도 했다.
**«후보 분포»와 «결과 성립률»을 같은 표에 나란히 낸다** — 하나만 보면 기전을 오진한다.
⚠ **분포를 잴 때 순서를 뒤집지 마라**: 획을 **커밋한 뒤** 미리보기를 재면 그 획 자신의
끝점이 커서 자리에 있어 `end`가 무조건 이긴다(이 회차 첫 판의 `edge_band`가 그랬고,
대조군이 «안 넘어가는» 것이 그 증상이었다). **미리보기 → 커밋** 순서를 지킨다.

**㉡ 한 구속을 채우려고 다른 구속을 깨면 무산 계수조차 안 오른다.**
`near`는 커서를 대상 선에 **수직으로** 붙인다 — 그것으로 「선 위에 있다」는 채워진다.
그런데 그 획은 축 스냅으로 **방향**이 이미 정해져 있었고, 수직 이동이 그 방향을 깬다:
밀림÷획 길이가 축 허용각을 넘으면 축 배정이 사라지고 → 획 자신이 3D로 안 올라가고 →
그것을 근거로 삼던 다음 단계가 **첫 줄에서 빠져나간다**. 그래서 「무산 사유」 계수가
하나도 안 오른다 — 진단에도 안 보이는 실패다. 실측: 죽은 38칸 중 **37칸**이 이 형태.
→ **규칙: 무산 계수는 «후보였던 것»의 무산만 센다.** 「후보도 못 된 채 죽은 것」이
있는지 따로 묻는다. 계수 0은 「무산이 없다」가 아니라 「셀 자리에 오지도 않았다」일 수 있다.
→ **수리의 형태**: 두 구속을 **동시에** 만족하는 답을 구한다. 여기서는 조준선(축이 정한
화면 직선) ∩ 대상 선 = **겉보기 교차**(AutoCAD apparent intersection · Rhino — A-3 선례)
하나뿐이었고, 그 점은 구성상 축선 위이자 잉크 위다. 「점이 방향을 이긴다」의 대가가 없다.

**㉢ (같이 난 것) 반증 손잡이는 코드가 바뀌면 «재는 것을 그만둘» 수 있고, 그것은 통과로 보인다.**
web2-14의 팔 둘이 `kinds.near = false`로 「몸통 스냅 없이 손 오차가 남은 채」를 세웠다.
web2-15가 몸통의 답을 `near` → `xint`로 바꾸자 그 팔은 **여전히 초록인 채 아무것도 안
재게 됐다**(xint가 붙어 오차가 0이 된다). 초록이므로 아무도 안 본다.
→ **규칙: 어떤 기전을 «끄는» 팔은 그 기전의 이름이 아니라 «그 자리의 답 전부»를 끈다.**
그리고 한 기능의 답을 갈아치우는 회차는 **그 답을 끄던 팔을 찾아 같이 고친다**(`grep`).

**㉣ (2차 리뷰가 잡았다) 「고쳤다」의 범위를 재는 격자가 «실패가 불가능한 자리»면 그것은
통과가 아니다.** 이 회차의 끝점-대역 표는 **획 길이 하나(≈140px)**에서 돌았고, 그 길이에서는
점 오스냅의 밀어냄(≤4px)이 4/140 = 2.9%로 축 허용각(0.05) **아래**라 축 손실이 산술적으로
불가능했다 — 「축 손실 0」은 실행 결과가 아니라 **상수와 길이가 정한 값**이었다(#46).
길이를 40·70·110으로 흔들고 대역을 셋으로 늘리자 **297칸 중 52칸이 죽었다**(L40에서 28).
→ **규칙: 「0이 나왔다」를 적기 전에 «이 격자에서 0이 아닌 값이 나올 수 있는가»를 산술로
확인한다.** 나올 수 없으면 그 표는 아무것도 안 잰 것이다. 같은 형태로 「대가 0」도
뒤집혔다 — 대기선이 **하나뿐인** 격자에서 「안 겨눈 선에 붙는가」를 물었다(둘로 늘리니
1/20에서 발생한다).
**㉤ 통과 수를 전량으로 적지 마라 — passed/failed/total 셋을 다 적는다.**
앞 회차가 「164케이스 **전량** 통과」로 보고했는데 그 커밋에서 `--list`는 **166**이었다:
**깨진 2케이스가 기록 없이 배포를 지나갔다**(A-4의 미기록 예외). ⚠ web2-16 재검이 「164 =
passed」 단정을 판별 불가로 내렸다 — 보고문의 「82팔 × dpr 둘」은 팔 오산(82×2=164)으로도
같은 수를 낸다(`diag_occlusion_web2.json` misreports_found). 전량 보고가 거짓인 것만 확정이다.
그 산술은 다음 회차가 팔 수를 셀 때 **저절로 드러났다** — 그래서 팔 수의 정본은
`--list` 출력이고, **파일 수까지** 맞아야 한다(#70 부기의 사례 — 케이스 수만 맞는 우연이 있다).

⚠ #68과 짝이다. #68은 「팔이 손을 안 태운다」이고 이것은 「손을 태워도 사람이 겪는
**경로**를 안 탄다」다 — 오차를 심은 픽스처로도 ㉠은 안 걸린다(단독 호출이면 이긴다).
경로를 태우려면 **앞선 획들까지 사람이 하는 순서로** 놓아야 한다(여기서는 「다른 3D
선에서 이어 긋기」의 그 3D 선).

## #70 — 2026-08-27 (web2-16 · e2e 선행 실패의 진짜 나이)

### #70. **남은 dev 서버 + `reuseExistingServer`는 «과거 커밋 재현»을 조용히 현재 코드 측정으로 바꾼다**

web2-16 1부가 dim.spec 실패 2케이스의 나이를 재려고 `git worktree`로 옛 커밋 다섯을
돌렸고 다섯 전부가 같은 실패를 냈다 — 그 팔의 **태생 커밋까지 실패**라는, 의심스러운
답이었다. 표식은 태생 커밋(12ec827)에서 돌린 계측 팔이 **HEAD와 소수점까지 같은 진단
패널 크기(412.4375px)**를 낸 것이다 — 태생 커밋은 패널 줄이 훨씬 적어 불가능한 값이다
(⚠ 높이를 잰 오염 실행은 이 하나다 — 다섯 커밋에서 잰 것은 팔의 동일 실패다).
원인: playwright 설정이 `reuseExistingServer: true`(포트 5301)이고, **전날 세션이 남긴
vite 서버가 그 포트에 살아 있었다**. vite는 디스크의 메인 작업 트리를 실시간으로
서빙하므로, 워크트리에서 돌린 «과거 재현» 전부(12ec827·b6980c9·d959148·b70b655·22b2bb4)가
실제로는 **현재 HEAD 코드**를 재고 있었다. 서버를 죽이고 다시 재니 답이 뒤집혔다 —
「태어날 때부터 실패」가 아니라 **web2-14 e224d82부터 실패**였다.

- **같은 이유로 web2-15의 「380c3f9 워크트리 재현」 증거도 오염이다** — 그 서버는
  web2-15 세션(8-27)보다 먼저(8-26 12:03) 떠 있었다. 결론은 깨끗한 재실행으로 다시
  서긴 했지만(⚠ 380c3f9 자체를 돌린 것이 아니라 **c0faaaf 실측 실패 + «380c3f9 =
  c0faaaf + HANDOFF.md 문서 커밋» 관계**로 — `diag_occlusion_web2.json` chain_note),
  **당시 증거로는 그 결론이 서지 않았다.**
  ⚠ 같은 세션이 자기 작업 트리를 재는 실행은 오염이 아니다(서버가 서빙하는 디스크가
  곧 그 코드다) — 오염되는 것은 **다른 체크아웃에서 도는 실행**이다.
- **규칙 ①: 과거 커밋을 재기 전에 포트 점유부터 본다**(`Get-NetTCPConnection -LocalPort
  5301` / lsof). 남은 서버는 죽이고 시작한다 — `reuseExistingServer`는 같은 세션의
  반복 실행을 아끼는 손잡이지, 버전 경계를 넘어도 되는 보증이 아니다.
- **규칙 ②: 버전 비교 측정에는 «버전이 갈라야 하는 값»을 하나 끼운다**(여기서는 패널
  높이). 서로 다른 커밋에서 그 값이 소수점까지 같으면 **오염을 의심하고 그 구간의
  diff부터 확인한다** — 그 값을 낳는 코드에 diff가 없으면 정당한 동일값이고(이 사슬의
  b70b655·32b2e99가 그랬다 — diagpanel diff 0줄), diff가 있는데 같으면 측정이 한 버전을
  치고 있는 것이다. 후자면 결과를 버리고 배관부터 본다.
- D-4의 하네스판이다: 「재현 실행이 어느 코드를 재는가」는 가정이 아니라 확인 대상이다.

⚠ **부기(같은 회차의 둘째 사고 — 워크트리 위생의 다른 반쪽)**: 오염을 잡고 깨끗이 재던
그 세션이, **cwd가 워크트리에 남은 채** 메인 몫의 회귀 팔·반증을 워크트리에 세웠다.
워크트리 제거로 팔이 유실됐고, 그때의 전량 실행 «176 passed»는 «회귀 팔 0 + 임시 표식
팔 2»를 센 **우연의 일치**였다(2차 리뷰어가 파일 수 산술 176/22로 잡았다 — 기대는 21).
**워크트리를 쓰는 세션은 편집·실행마다 pwd를 출력에 남기고, 워크트리를 지우기 전에
거기 남은 산출물 diff를 본다.** 그리고 팔 수 회계는 «케이스 수»만이 아니라 **파일 수까지**
맞아야 통과다.

---

## #71 — 2026-08-27 (web2-18 · 비용 원장·팔 조건)

### #71. **시간·픽셀을 재는 팔은 «무엇을 재는 조건인가»가 값의 절반이다**

한 회차에서 같은 형태가 **넷** 났다. 전부 「팔은 초록인데 재는 것이 주장과 다르다」다.

- **㉠ 실행 조건(워커 수)이 시간 비교를 무효로 만든다.** 0부 비용 원장의 «수리 전» 판을
  `--project=dpr1`(워커 1)로 냈는데, 그 뒤 **전량 e2e**가 워커 둘로 돌며 같은 파일을
  덮어썼다 — dpr1·dpr2가 CPU를 다퉈 400획 전량 재그리기가 **186.5 ms → 343.3 ms**로 부풀었다.
  커밋에서 되살려 겨우 잡았다. **시간을 재는 원장은 실행 조건(`--workers`, 동시 프로젝트)을
  원장 안에 적고, 전/후는 같은 조건으로 돌린다.** 조건이 다르면 그 비교는 없는 것이다.
- **㉡ `afterAll`에 원장을 쓰면 팔이 실패한 판은 통째로 빈다.** playwright는 시험이 실패하면
  워커를 갈아 치우므로 모듈 상태(누적 원장)가 날아간다 — 「수리 전」 판의 핵심 칸이
  실제로 사라졌다. **판정보다 먼저 쓴다.** 여러 팔의 값을 한 판에 모으려면 **한 시험** 안에
  두고 측정 → 기록 → 판정 순서로 간다.
- **㉢ 임계가 지표의 분해능 아래면 그 팔은 마지막 ulp를 잰다.** 「구성상 평행」 팔이
  `convergenceDeg < 1e-9°`였는데 그 함수가 `acos(|cos|)`라 **0 근처의 바닥이 1.2e-6°**다
  (cos가 1의 한 ulp 아래로 반올림되기만 해도 √(2ε)이 그 값이다). 그 팔은 **cos가 정확히
  1.0으로 반올림되는 동안만** 통과하고 있었다 — 기하가 아니라 반올림을 재고 있었다.
  `atan2(|외적|, 내적)`로 바꿔 고쳤다. **새 임계를 걸기 전에 그 지표가 그 대역에서
  분해능이 있는지 본다.**
- **㉣ 격자가 «실패 불가»면 여유 값은 아무 근거도 아니다**(#69 ㉣의 재발). 화면 밖
  잘라내기 «여유»의 경계 팔이, 여유를 **0으로 두고 돌려도 같은 수**를 냈다 — 그 팬에서는
  획의 한 끝이 늘 화면 안이라 상자 검사가 애초에 안 걸렸다. 여유를 재려면 **두 끝이 모두
  화면 밖이면서 입자는 안으로 들어오는** 자리를 만들어야 했다(세로획 · 중심 −1 px ·
  실측 번짐 2 px). 그렇게 세우니 여유 0에서 실제로 빨개졌다.

⚠ **㉤ 겹을 옮기면 그 겹을 읽던 팔이 «사라졌다»로 오독한다**(#67의 다른 반쪽). 잉크 몸체를
`#gl`→`#ink`로, 제스처 흑연을 `#brushc`→`#brushsnap`으로 옮기자 캔버스 이름을 못 박은 팔
셋이 빨개졌다(flow·drafting·waitfade). **셋 다 옳게 빨개진 것**이고 답은 임계가 아니라
**읽는 자리**다. 흑연처럼 «보이는 겹이 국면에 따라 바뀌는» 것은 이름이 아니라 **지금 보이는
겹**을 읽어야 한다. 겹의 소유는 이제 한 자리에 적혀 있다(`docs/line_plan_r3_draft.md` §9.6).

⚠ **재발 부기(web2-21 · 2026-08-28)**: ㉠·㉢이 같은 회차에서 또 났다 — 면 일괄 비용
원장(faces_bulk)을 신설하며 착수 표에 「--workers=1·조건 명기·분해능 단언」을 스스로
적고도 첫 판이 셋 다 빠뜨렸고, 전량 vitest가 병렬로 덮어쓴 판(96.1ms)이 문서 인용
(61ms)과 갈린 것을 리뷰어가 잡았다. 수리: 원장에 «단독 실행» 조건 명기 + 전량 판은
정본 아님 규율(run.note) + 분해능 단언(400 med > 100 max)·저역 겹침 값 기재 + 마감에서
규정 명령으로 재생성. ㉤도 한 번(면 채움을 후보 겹(#ink)에서 재려다 — 채움은 #gl).
⚠⚠ **web2-22 후속 — 규율을 쓰기 문으로 대체했다**: 사람 손 규율(마감 재생성·복원)은
네 번 다 뚫렸고, 뿌리를 캐니 **web2-19 마감 커밋부터 cost18 정본 자체가 오염**돼
있었다(400획 295.5ms — 이후의 «git checkout 복원»들은 오염 기준선으로 돌아간 것).
이제 시간 원장 하네스는 전부(vitest faces_bulk · playwright cost18/20/22) **`LEDGER=1`
환경변수에서만 원장을 쓴다** — 전량·병렬 실행은 팔만 돌고 원장을 못 덮는다. 정본
명령이 「LEDGER=1 … --workers=1」로 바뀌었고 셋을 단독 재생성해 기준선을 회복했다
(cost18 200획 osnap 0.82ms — web2-18 DEFERRED 기록 0.82~0.86과 재일치).

## #72 — 2026-08-28 (web2-19 · 연필통 접힘)

### #72. **같은 선택자의 CSS 규칙을 두 자리에 두면 뒤가 조용히 이긴다**

3부가 연필통 접힘을 `#tray { display: none }`으로 세웠는데, 그 규칙을 스타일 블록 **앞쪽**
(팝업들 곁)에 넣었고 원래의 `#tray { display: flex; … }`가 **뒤에** 남아 있었다. 같은
특이도는 나중 규칙이 이기므로 접힘이 **소리 없이 죽었다** — 연필통이 늘 펼쳐진 채였다.

증상은 멀리서 났다: 세로바가 822px로 화면(800)을 넘쳤고, 맨 아래 서랍·치수 트리거가
빌드 식별자(z-12) 아래 깔려 **클릭이 가로채였다**(playwright 「intercepts pointer events」).
겹침 팔은 「펜 상태 요소 수 17(기대 11)」이라는 **수수께끼 수**를 냈다 — 원인(접힘 죽음)과
증상(가로채기·요소 수) 사이가 세 홉이라 후보 추리로는 안 닿았고, **표식**(일회용 probe
스펙 — «행 너비 > 0인데 `.open` 클래스는 0»)이 한 번에 갈랐다(D-1).

- **규칙 ①: 상태 규칙(display 등)은 원래 규칙의 자리에서 바꾼다.** 기본값을 원래 자리에서
  고치고(`display: none`으로) 상태 클래스(`.open`)를 그 **바로 아래**에 둔다 — 같은
  선택자가 파일에 두 번 나오면 그 시점부터 순서 의존이다.
- **규칙 ②: «요소 수»가 기대와 다른 팔은 어느 요소가 남았는지 목록으로 낸다.** 수만 보면
  (17 vs 11) 아무것도 못 가른다 — probe가 id 목록을 찍자 tray 행 여섯이 그대로 보였다.
- 짝: #71 ㉢(분해능)이 **같은 회차에서 재발했다** — 아이콘 굵기 첫 판(32px·문턱 계수)이
  thin/light/regular에 전부 188을 냈다(굵기 차가 AA 문턱 아래). 알파 합·64px로 바꾸고
  **분해능 단언**(thin < light < regular)을 팔에 박았다 — 새 척도를 세울 때 「그 축에서
  값이 실제로 갈리는가」를 팔 안에서 먼저 묻는 형태가 답이다.

### #73. **화면 합성의 상태는 문면이 아니라 실측이 정한다 — CSS blend 사망 · 동결 참조 게이트**

web2-20 3부(막 — 종이 겹 표현)에서 같은 형태가 두 번 났다: **«이 조합이면 이렇게
보인다/돈다»는 문면의 약속이 실제 합성 트리·실제 프레임에서 다르게 섰고, 둘 다
후보 추리가 아니라 실측·표식이 갈랐다**(D-1).

㉠ **CSS `mix-blend-mode: multiply`가 이 앱의 합성 트리에서 발화하지 않았다.**
   위생 검사(순수 페이지 — div 둘)에서는 곱이 선다([255,0,0]×[0,0,255]→[0,0,0] 실측).
   같은 선언을 앱의 #film에 얹으니 **막이 불투명 덮개로 찍혔다** — 곱이면 [232,216,167]일
   자리가 막 원색 [242,227,179]. WebGL 캔버스 형제가 낀 스택 컨텍스트 조합에서 blend가
   죽는 형태다(어느 명세 조항인지 특정하지 않았다 — 특정보다 우회가 쌌다).
   → 곱을 **우리가 읽을 수 있는 자리로 내렸다**: 막 영역에 아래 캔버스들을 사본으로
   재조립하고 in-canvas `globalCompositeOperation='multiply'`로 곱한다(filmlayer.ts).
   **blend·filter·backdrop 같은 «합성기가 해 주는» 효과를 새로 쓸 때는 위생 검사가
   아니라 실제 겹 스택에서 픽셀을 읽고 나서 설계에 넣는다** — 위생 검사 통과는
   «명세가 그렇다»이지 «이 트리에서 그렇다»가 아니다.

㉡ **게이트 판정에 동결 참조(fadeRef)를 쓰면 제스처 내내 게이트가 열린 채로 남는다.**
   막의 포즈 게이트(그 종이의 시점에서만)를 제스처 떨림 방지용 동결 포즈로 판정했더니,
   궤도 제스처 **내내** 「아직 그 시점」으로 남아 ① 막이 도는 장면 위에 계속 곱해지고
   (3-d 위반) ② 그 drawFilms가 궤도 매 프레임 돌았다 — **31.6ms/프레임**(400획 궤도가
   2.0→32.8ms). 잡은 것은 비용 원장(cost20)의 나란한 두 칸이었고, 원인은 draw의 두 몫
   (막/위 획)을 가르는 표식(filmCost)이 첫 실행에 냈다 — films 31.6 · above 0.0.
   ⚠ 수리 전 값들의 원장 파일은 **없다** — 재실행이 같은 이름을 덮었다(#71 ㉠ 어김의
   기록. 명령 출력 원문은 web2/NOTES ⑩에 인용돼 있다 — 전/후 비교가 필요한 수리는 수리
   전 판을 `*_before.json`으로 옮기고 재실행한다).
   → **동결 참조는 표시의 떨림을 막는 자리(불투명도·파선)에는 옳고, 상태 게이트에는
   틀리다** — 게이트는 살아 있는 값으로 판정한다(연속으로 움직이는 값은 경계에서
   왕복하지 않으므로 떨림 걱정도 없다). ⚠ 짝: 비용을 재는 회차였기에 걸렸다 —
   새 프레임 경로를 더한 회차는 «그 경로가 안 도는 국면»의 프레임 값을 한 칸 낸다.

## #74 — 2026-08-28 (web2-23 · 보호 조항의 팔이 **다른 이유로** 통과하고 있었다)

### #74. **보호 조항의 팔이 «다른 이유로» 통과하고 있었다 — 임계가 무엇을 가르는지를 값으로 본다**

> ⚙️ **이 줄은 web2-25가 붙였다.** `selfcheck.py`의 `scan_pitfall_citations`가 이 형식
> (`### #NN. **제목**`)을 **등록부**로 읽는데 web2-23이 그것을 안 달아, #74를 인용한 원장이
> **「없는 번호」로 오검**됐다. 고칠 곳은 검사가 아니라 **기록하는 쪽**이다(#38 · #19의 요지).

web2-23(은선)에서 같은 뿌리의 둘이 났다: **팔이 초록인데 재는 것이 그것이 아니었다.**
둘 다 «임계가 무엇을 가르는지»를 값으로 확인하고 나서야 갈렸다(D-3·#69 ㉣의 새 형태).

㉠ **정확히 같은 평면에서는 부등식이 어느 쪽으로도 안 기운다 — 그래서 「같은 평면
   조항」의 반증이 발화하지 않는다.** 지시가 못 박은 반증(「조항을 빼면 벽 모서리가
   자기 벽에 가려 사라지는가」)을 정면 벽 장면으로 돌렸더니 **조항을 빼도 결과가 같았다**.
   까닭 둘: ① 선분이 면과 정확히 같은 평면이면 조각의 깊이와 면의 깊이가 **정확히 같다**
   (fp 오차 1e-13 대역) — 여유를 0으로 줄여도 안 기운다 ② 정면 벽은 광선이 면에 거의
   수직이라 어긋남이 깊이로 안 증폭된다. 조항이 실제로 일하는 자리는 **비스듬히 보는
   «거의 평면»**이다: 면의 깊이 `t = (d − n·o)/(n·rd)`에서 `n·rd`가 작아질수록 평면
   계수의 작은 어긋남이 `1/(n·rd)`배로 커진다(실측: 옆 벽 `n·rd ≈ 0.108` → 어긋남
   0.026~0.064가 깊이 0.28로 = 허용 0.095의 세 배).
   → **보호 조항의 반증은 «그 조항이 없으면 실제로 깨지는 장면»을 따로 세운다.** 그리고
   왜 갈리는지를 **부등식 둘로 못 박는다**(두 끝이 평면에서 tol 안 · 그런데 깊이 차가
   tol 밖) — 안 그러면 장면을 손댈 때 팔의 뜻이 조용히 바뀐다.
   ⚠ 짝: **면 다각형의 «경계 위» 점은 포함 판정이 «밖»으로 읽는다** — 그래서 「벽 자신의
   모서리가 살아남는다」 팔은 조항이 없어도 통과한다(그 팔 하나만으로는 실패 불가능한
   격자다). 조항을 실제로 지나는 것은 **면의 안을 가로지르는 같은 평면 선**이고, 팔은
   그것을 따로 넣어야 무언가를 잰다.

㉡ **합성된 화면에서 절대 밝기 임계를 쓰면 «종이 결»이 잉크로 세어지고, 그 오염은
   dpr에 따라 갈린다.** 밑그림 픽셀 팔이 「밝기 200 아래면 잉크」로 판정했는데,
   막(#film)에는 섬유 결이 곱해져 있고 그 결은 색조의 0.82배(≈176)라 **결 자체가
   임계 아래**였다. dpr1에서는 통과하고 **dpr2에서만 빨갰다**(device px가 촘촘해져 결의
   최저치가 더 자주 잡힌다).
   → **합성 위의 픽셀 팔은 그 실행에서 «빈 자리의 바닥값»을 재고 그것을 기준으로
   읽는다**(절대 임계 ⛔). 판정도 절대값이 아니라 **둘의 차**로 한다(F와 H를 가르는 것이
   물음이면 F·H 두 줄과 빈 줄 셋을 같은 실행에서 읽는다). ⚠ 짝: #73 ㉠(합성의 상태는
   문면이 아니라 실측이 정한다)의 **판독** 쪽 반쪽이고, D-C3(dpr 1에서만 확인하지 않는다)이
   이것을 잡았다 — dpr 하나만 돌렸으면 초록으로 배포됐다.

㉢ **가리는 단계를 겹 스택에 끼우면 «그 아래를 읽던 게이트»가 먼저 깨진다 — 그리고
   그때 의심할 것은 팔이 아니라 제품이다.** web2-23 2부 초판이 밑그림 겹의 **rect 전체**를
   종이색으로 덮었다(「치환」이 서려면 원래 획을 지워야 한다는 것은 옳았다). 그것이
   web2-20 3부의 **게이트 ⑧**(「세 장을 겹쳐도 아래 획이 읽힌다」)을 대비 0.0002로 깼다 —
   그 종이 안의 **대기 획과 아래 겹의 획까지** 함께 사라졌기 때문이다.
   ⚠ 이것은 **#71 ㉤과 방향이 반대다**: ㉤은 「겹을 옮기면 팔이 «사라졌다»로 **오독**하니
   읽는 자리를 고쳐라」이고, 여기서는 **팔이 옳았고 제품이 틀렸다**. ㉤의 처방을 그대로
   따랐다면 게이트를 고쳐 초판을 배포했을 것이다. 두 경우를 가르는 물음은 하나다 —
   **「그 팔이 지키던 사람의 요구가 지금도 유효한가」**(여기서는 「종이를 겹쳐도 아래가
   비친다」가 종이의 정의였다). 유효하면 팔이 옳다.
   → ㉠ **가리는 단계를 새로 끼우는 항목은 착수 시에 그 아래를 읽던 게이트 팔의 목록을
   먼저 만든다**(여기서는 paper.spec의 ⑧⑨). ㉡ **범위를 좁혀 둘 다 세운다** — 답은
   「전부 덮기」가 아니라 **자기가 대체하는 선 자리만 도려내기**였다(치환도 서고 ⑧도 산다).
   ㉢ 착수 표에 번호를 적어 두는 것만으로는 안 막혔다(그 회차가 #71 ㉤을 «직접 해당»으로
   적고도 어겼다) — **막은 것은 전량 e2e다.** 표는 읽기이고 팔은 실행이다.

## #75 — 2026-08-28 (web2-25 · 「되는데 안 보인다」와 저장 형식의 좁힘)

### #75. **만드는 자리와 보이는 자리가 다른 술어를 읽으면 «추가는 되는데 안 보인다»가 난다**

㉠ **사람의 첫 문장이 그 형태였다** — 「옐/트가 **추가는 되지만 화면에 보이지 않는 문제**」.
   원인은 겹이 **활성 종이**에 붙는데 막은 **`atSheetPose`**(지금 포즈가 그 종이의 시점인가)
   에서만 뜨는 것이었다: 궤도로 돌려본 시점은 아직 어느 종이의 시점도 아니므로 얹은 겹이
   보이지 않는다. **두 자리가 같은 물음을 다른 술어로 읽고 있었다.**
   → 답은 새 비교를 짓는 것이 아니라 **표시 게이트의 술어를 그대로 판정에 쓰는 것**이다
   (`freezePoseForLayer`가 `atSheetPose`를 읽는다). 그러면 「얹었는데 안 보인다」가
   **구성상 불가능**해진다 — 갈릴 자리가 없기 때문이다.
   ⚠ **지시가 준 판정 방법은 안 섰다**(D-4): 「`setPose`가 늘 새 객체를 넣으니 객체
   동일성을 쓰라」였는데 `addSheet`가 포즈를 **복사**해 넣으므로 굳힌 직후에도 거짓이고,
   그러면 누를 때마다 종이가 는다. **팔 ②(「같은 시점이면 종이가 안 는다」)가 그 자리에서
   깨진다** — 사람이 준 기전은 확인 대상이지 결론이 아니다.
   → 일반형: **「A를 만들었는데 A가 안 보인다」류는 «만드는 조건»과 «보이는 조건»의 술어를
   나란히 적어 보는 것으로 시작한다.** 둘이 다르면 그것이 원인이고, 수리는 **하나로 합치는
   것**이지 한쪽을 흉내내는 것이 아니다.

㉡ **저장 형식을 줄이는 수리는 «불변식이 걸린 좌표»를 못 건드린다 — 그리고 그 자리는 애초에
   얻을 것도 없다.** 파일 크기 5부가 좌표를 소수 1자리로 반올림했는데, 초판이 **확정 끝점
   `a`·`b`까지** 깎아 `own3d.test` 넷이 빨개졌다: 자립 3D의 **잉크 심판**(「그 3D를 지금
   카메라로 다시 사영하면 획의 끝점에 떨어진다」 · `OWN3_TOL_PX` **0.01px**)이 그 자리에
   걸려 있고 0.1 단위 이동은 최대 0.05px라 **왕복에서 불변식이 깨진다**. 같은 회차에서
   밑그림 마디를 깎았다가 web2-23의 **왕복 동일성**(`underlay.test` ④)도 깼다.
   → **되돌린 사유는 두 팔이고**, 표는 그 결정이 값싸다는 것을 보였다: 구성 요소별
   바이트에서 큰 몫은 `raw`(73.6%) 하나이고 끝점+메타는 2.3%다. → ① **좌표를 깎기 전에
   «그 좌표에 걸린 불변식»을 먼저 센다**(무엇이 그 값의 정확한 일치를 요구하는가).
   ② **표가 지목하지 않은 자리는 애초에 건드리지 않는다** — 지시가 「먼저 재고 나서
   고친다」라 한 이유가 이것이다.
   ⚠⚠ **다만 표의 몫은 픽스처의 성질이다**(D-5): 밑그림 0.1%는 **마디 8개** 장면의 값이고
   최악 밑그림(마디 609)이면 20% 대역이다. 「얻을 것이 없다」를 **일반 규칙으로 승격하지
   않는다** — 되돌린 근거로 서는 것은 «규약이 깨진다» 쪽이고, 몫은 **그 픽스처 안에서만**
   그렇다. 표를 근거로 쓸 때는 **그 표가 덮는 대역을 함께 적는다.**

㉢ **«그림이 안 바뀐다»를 절대 0으로 요구하면 임계를 헐겁게 잡는 반대편 잘못으로 간다 —
   기준선을 그 실행에서 만든다.** 반올림이 그림을 바꾸는지 재려고 「그리기 직후 화면 ↔
   복원한 화면」을 견줬더니 0.2%가 달랐다. **그런데 그 둘은 그리는 경로가 다르다**
   (생 draft ↔ 문서에서 다시 그리기) — 그 차가 반올림 몫인지 재그리기 몫인지 그 팔은 못
   가른다(D-1: 표식 먼저). → **양쪽을 다 복원 화면으로 두고 저장 형식만 갈랐다**(반증 손잡이
   `diag.saveRound`). 그리고 판정을 **세 값의 비교**로 세웠다: 반올림 몫 0.02~0.05% ↔
   **재그리기 몫** 0.18~0.32% ↔ 잡음 바닥 **0.000%**. 반올림이 «복원할 때마다 이미 나는
   차»보다 작으면 「안 바뀐다」가 값으로 선다.
   ⚠ 이것은 **#74 ㉡의 확장**이다 — 그쪽은 「절대 밝기 임계 대신 그 실행의 바닥값」이었고
   여기서는 「절대 비율 대신 **그 실행의 다른 차**」다. 안티에일리어싱·질감처럼 **재현이
   비트 단위로 같지 않은 화면**에서는 언제나 이 형태가 필요하다.

㉣ **UI의 «형태»를 갈면 그 형태를 뜻으로 읽던 팔이 깨진다 — 그때도 물음은 #74 ㉢이다.**
   겹의 형태를 「겹쳐 놓은 탭 더미」에서 「접으면 요약·펼치면 목록」으로 갈았더니 팔 셋이
   빨개졌다: ① 자식 탭 상자가 서로 겹친다(«가산적»을 형태로 재던 팔) ② 종이 「+」 아이콘이
   Phosphor `plus`다 ③ 종이 「+」를 누른 뒤의 합성 화면 밝기(**셔터의 번쩍임**이 +7을 냈다).
   → 물음은 하나다 — **「그 팔이 지키던 사람의 요구가 지금도 유효한가.」** 셋 다 유효했고
   **대상·자리·시점만** 갈렸다: ①은 「가산적임이 형태로 갈린다」를 **요약의 수**로 옮겨
   다시 썼고 ②는 `camera`로 옮기되 **겹의 plus는 그대로 단언**해 둘이 갈렸다는 사실 자체를
   팔에 남겼고 ③은 **번쩍임이 가라앉기를 기다린 뒤** 읽게 했다.
   ⚠ **UI를 갈면 먼저 그 형태를 읽는 팔의 목록을 만든다**(#74 ㉢ ㉠의 UI판 —
   `grep`으로 옛 선택자(`.lpaper`)를 훑는 것이 그 목록이다).

### #76. **사람이 준 원인 후보가 넷 중 셋 틀렸다. 그리고 «지금 값»을 올리면 그 값을 «다른 뜻으로 읽던» 팔이 깨진다** (web2-26)

실기기 구두 확인 30항목에서 나온 결함 여섯을 받은 회차다. 지시가 각 항목에 **원인 후보**를
달아 줬고, D-1(표식 먼저)·D-4(사람이 준 근거도 확인 대상)를 그대로 돌렸더니 **넷 중 셋이
틀렸다.** 셋 다 「없는 것을 있다고 본」 형태다.

| 항목 | 지시의 후보 | 측정이 낸 것 |
|---|---|---|
| 26-1 선따기 오염 | 「`own3`/lift가 **층과 무관한 전역 맵**에 쓴다」 | 전역 맵은 **없다**(3D 쪽은 이미 갈려 있었다). 오염은 **지우개 조각**이 `layer`를 안 물려받은 것 — 그리고 지시가 준 픽스처(긋기만)로는 **재현이 안 됐다** |
| 26-3 연장선 | 「점 물림과 선 물림이 같은 반경을 쓴다」 → `ext`·`perp` **둘 다** 넓혀라 | 반경 진단은 맞았다. 그러나 `perp`는 **선 후보가 아니다** — 발이 선분 **안으로 클램프**돼 점처럼 국소화돼 있고, 넓히자 깨끗한 세로획의 끝이 **15px 밀리고 축을 잃었다** |
| 26-5 한 손가락 | 「현행 = 한 손가락 그리기, 두 손가락 이동」 | 손가락은 **한 번도 그린 적이 없다**(touch 갈래가 `beginDraft` 앞에서 반환). 현행은 「한 손가락 궤도」이고, 증상의 자리는 **`orbitBy`가 첫 줄에서 반환하는 선언 단계**였다 |
| 26-6 필압 | 「농도와 굵기가 **한 계수를 공유**할 가능성이 크다」 | **공유가 아니라 부재**였다 — 압력은 굵기에만 실리고 농도는 등급 상수다. 「하한선이 너무 높다」의 정체가 그것이다 |

㉠ **「A가 B와 얽혀 있다」는 후보는 «A가 B에 아예 없다»와 구별되지 않는다 — 표식이 그 둘을
   가른다.** 26-6이 그 형태다: 「한 계수를 공유한다」와 「한쪽에 아예 안 실린다」는 증상
   (「세게 그어도 안 진해진다」)이 **같다.** 후보를 읽고 계수를 찾으러 가면 못 찾고 헤매고,
   **그 경로에 표식을 심으면**(각 함수가 압력을 무엇으로 바꾸는지 나란히 찍어 보면) 첫 실행에
   갈린다. 26-1도 같다 — 「전역 맵이 있다」와 「층을 안 물려받는다」는 화면에서 같아 보인다.
   → **후보가 «있는 것의 성질»을 말하면 먼저 그것이 있는지부터 찍는다.**

㉡ **지시가 준 픽스처로 재현이 안 되면 «못 했다»고 적고, 그 항목의 «실제 몸짓»을 다시
   읽는다.** 26-1의 지시 픽스처(종이 3획 → 겹에서 따라 긋기)는 **깨끗하게 통과했다**(종이 3).
   재현은 DEVICE-CHECK **E2의 문면**(「관통선 + 선따기 → 확대해서 **바깥 토막 지우기**」)에서
   났다 — 지우개가 픽스처에 없었던 것이다. **증상 보고의 원문이 픽스처의 정본**이고
   지시의 요약본이 아니다(D-2의 실행 규칙).

㉢ **「같은 자로 재던 둘을 가른다」는 수리는 «둘 중 하나가 실은 첫째 범주»일 수 있다 —
   가르기 전에 각각의 정체를 다시 묻는다.** 26-3에서 `ext`와 `perp`는 「선」으로 묶여 있었다.
   갈라 보면 `ext`는 **획득식이고 무한히 뻗은 직선**이라 띠를 넓혀도 모호해질 상대가 없고,
   `perp`는 **상시이고 선분 안의 한 점**이라 넓히면 축 스냅을 이긴다. 판별 물음 둘:
   **① 그 후보의 자리가 한 축으로만 정해지는가(무한선) 아니면 두 축으로 정해지는가(점)?
   ② 그것이 사람의 «집었다»는 몸짓 뒤에만 사는가, 늘 켜져 있는가?** 둘째 물음이 특히
   중요하다 — 획득식이면 넓혀도 «허공 끌림»이 안 돌아온다.

㉣ **⚠⚠ 「지금 값」을 올리면 그 값을 «다른 뜻으로 읽던» 팔이 깨진다 — #74 ㉡의 확대판이고,
   이번엔 다섯이 한꺼번에 났다.** 26-2가 종이 결의 진폭을 지각 대역으로 올리자
   (막 sd 0.13~1.36 → 3.9~4.3) **결을 잉크로 세던 팔이 다섯** 빨개졌다. 다섯 다 사람의
   요구는 유효했고(#74 ㉢의 물음), 갈린 것은 **읽는 자리와 기준**이었다:

   | 무엇을 읽었나 | 왜 깨졌나 | 새 기준 |
   |---|---|---|
   | **절대 밝기 임계**(150) | 세 장 겹친 막의 빈 자리가 133까지 내려왔다 | 그 실행의 **빈 줄 바닥값과의 차** |
   | **못 박힌 좌표 대역**(60~200) | 타일 크기가 dpr을 따르자 그 띠가 이음매를 가로질렀다 | 타일 크기의 **비율**(0.23~0.78) |
   | **전체 평균 기준의 «에너지»** | 결이 세지자 **섬유가 없는 빈 띠도 평균에서 멀어졌다**(부호가 뒤집혔다) | 휘도 **98분위**(종이색) 기준의 «어두워진 몫» |
   | **36px 떨어진 «곁»** | 두 상자가 결의 **다른 대목**을 표본해 신호(~2계조)를 삼켰다 | 획 **바로 위·아래 8px**의 평균 |
   | **빈 줄의 `min`을 «종이»로** | 그 값은 실은 **그 줄에서 가장 어두운 섬유**다 | 「있는가」는 **중앙값**(종이) · 「없는가」는 `min`(결) |

   → 일반형: **질감·잡음의 세기를 바꾸는 수리는 «신호 대 배경»을 쓰는 모든 팔의 기준을
   동시에 바꾼다.** 착수 전에 `grep`으로 그 척도를 쓰는 자리 목록을 만들고, 각각에
   **「이 팔이 묻는 것이 «종이보다 어두운가»인가 «결보다 어두운가»인가」**를 적는다.
   **물음이 둘이면 기준도 둘이어야 한다** — 하나로 묶으면 어느 쪽이든 3~4계조 차로 깨진다.

㉤ **시간 임계를 내리는 수리는 «시간으로 나눈 다른 양»을 같이 느슨하게 만든다.**
   26-4의 머무름 문은 「4px 상자 안에서 600ms」였고, 그 실효 표류 **속도** 문턱이
   `상자 ÷ 시간`이다. 시간만 350ms로 내리면 문턱이 6.7 → **11.4 px/s로 넓어진다** —
   「천천히 그린 곡선이 직선화된다」가 **더** 잘 나는 방향이다. 답은 **상자를 시간에
   비례시키는 것**이고(어느 임계에서나 6.7 px/s로 고정) 그래야 「시간만 내리지 않았다」가
   값으로 선다. ⚠ 짝: 그 자리에 **지수평활**을 쓰면 안 된다 — 멈춘 뒤 문턱까지 내려오는 데
   `τ·ln(그리던 속도/문턱)`이 들어(실측 600ms 대역) **얻으려던 시간을 통째로 먹는다**.
   이동창을 쓴다.

㉥ **독립 성분의 바닥을 뺄 때는 «뺄셈»이 아니라 «제곱근»이다.** #74 ㉡의 「절대 임계 대신
   그 실행의 바닥값」은 맞고, **빼는 산술**이 틀리기 쉽다: 두 성분이 독립이면 **분산이
   더해지므로** `sqrt(신호² − 바닥²)`이다. 26-2에서 뺄셈으로 하면 바닥이 큰 조건
   (dpr1 — 0.162)만 부당하게 깎여 dpr 비가 1.108 대신 1.155로 부풀었고, 게이트
   (1.0 ± 0.15)를 **경계에서 빨갛게** 만들었다.

㉦ **화면 값을 dpr 사이에서 견주려면 «평균»이 아니라 «어두운 쪽 분위수»다.** 26-6의
   획 진하기를 상자 평균으로 재니 dpr2에서 차가 6.4 → **1.6으로 줄었다** — 획이 1.4px라
   같은 CSS 상자에 픽셀이 네 배면 획의 몫이 그만큼 희석된다. 「획이 얼마나 진한가」는
   **획의 심**이 답이고 5분위가 그것을 집는다. (㉣의 표 셋째 줄과 같은 계열: **무엇을 묻는지
   정하면 통계가 따라온다.**)

### #77. **「모드를 더한다」는 그 자리를 쓰던 «다른 경로»를 죽인다. 그리고 값이 스스로를 정의하면 그 값의 «검증»은 항등이다** (web2-27~29)

㉠ **한 손잡이에 뜻을 하나 더 얹으면 옛 뜻이 조용히 죽는다.** web2-29 1단계가 리본의 치수
   단추에 「모드 켜기」를 얹었더니 **web2-10의 키패드·음성 경로가 통째로 죽었다** — 그 경로는
   「선을 긋고 패널에서 숫자를 넣는다」인데 패널을 여는 순간 그리기가 손글씨로 갈렸다
   (실측: 2500이 들어갈 자리에 75). 전량 e2e가 잡았다.
   → **새 모드는 새 자리에서 들어간다.** 그리고 화면에서 **「패널이 열렸다」와 「모드가
   켜졌다」가 갈려야** 한다(선택 표시를 어느 단추가 지는가가 그 판정이다).
   ⚠ 짝: 같은 회차의 **28-1**도 같은 형태였다 — 「명령을 누르면 접는다」를 **비우기**에
   얹었더니 그 버튼 곁에 뜨는 **확인의 앵커가 사라졌다**. 답도 같다: 뜻이 하나 더 필요하면
   **표시를 하나 더 만든다**(`data-fold="late"`) — 코드가 추측하게 두지 않는다.

㉡ **값이 스스로를 정의하면 「적힌 값 ↔ 잰 값」류의 검증은 구성상 항등이다** — 지시가
   그런 검증을 요구해도 **발화 조건이 없으면 만들지 않는다**. web2-29의 `Stroke.dim`은
   주석이 아니라 **그 획의 길이**이고 리프팅이 그 값으로 길이를 다시 세운다. 그러므로
   「잰 값」은 언제나 「적힌 값」이다 — 실측 **9000 ↔ 9000.000 · 비 1.000000**.
   초판은 문턱 상수(0.2)까지 만들었다가 **걷었다**. §5.1 자기참조 유형 3의 실례다.
   → **판정을 만들기 전에 「이 두 값이 서로 독립인가」를 묻는다.** 그리고 그 0이 「척도가
   죽었다」가 아님은 **분해능**으로 보인다(값을 4000으로 바꾸면 잰 값이 9000 → 4000).

㉢ **「A가 이미 있는지」부터 세면 회차의 크기가 절반이 된다.** web2-29 1단계는 커 보였는데
   세어 보니 인식·분절·확정 전 표시·값 적용·단위 파싱이 **전부 이미 있었고** 없는 것은
   둘뿐이었다(종이 위에서 쓰는 길 · **치수선**). 그리고 그 둘 중 하나가 **놀라움**이었다 —
   치수는 그때까지 화면에 **한 번도 안 그려졌다**. 착수 표에 «이미 있는 것» 칸을 두면
   그 놀라움이 착수 시점에 나온다.

㉣ **지시의 전제가 낡을 수 있다 — 인용된 수는 그 자리에서 다시 잰다.** web2-27 3번의
   「옐로 100획이 488KB」는 **web2-25 이전 값**이었고 실측은 **238,456 B(48.9%)**로
   게이트가 이미 서 있었다. 회차의 일이 「줄이기」에서 「지시가 준 상한을 지키기」로
   바뀌었다(솎기 임계가 선폭의 0.36배라 상한 밖이었다 — 고치자 파일이 **늘었다**).
   → 지시가 수를 인용하면 **원장을 그 자리에서 다시 읽는다**(§5의 인용 규약과 같은 방향).

㉤ **「없는 것은 갈래다」 — `null`을 돌려주는 함수를 보면 «고칠 것»이 아니라 «옆에 둘 것»을
   찾는다.** web2-27 1번의 `pointOnGround`는 지평선 위에서 `null`을 주는데 그것은 **옳다**
   (위로 가는 광선은 바닥과 안 만난다). 없던 것은 **위 갈래**였고 답은 `pointOnCeiling`을
   **따로** 두는 것이었다 — 바닥 접지는 다른 데서도 쓰이고 거기서 `null`이 사라지면 안 된다.
   ⚠ 그리고 **새 갈래의 범위는 팔이 두 번 좁혔다**: 걸침 거부를 세로선까지 넓히자 «눈앞의
   기둥»이 죽었고, 천장을 되살림 패스에도 넣자 팔 열둘이 깨졌다. **새 갈래는 지시가 못 박은
   자리에만 넣는다.**

㉥ **팔이 «처음» 빨개졌다고 그 회차가 원인인 것은 아니다 — 이미 있던 경주가 드러난 것일 수
   있다.** `roundsave ③`의 「잡음 바닥」이 29-1에서 0.067%가 났는데, 그 값이 **반올림 몫과
   정확히 같았다**(0.067%) — 그것이 오염의 증거였다: 복원 직후의 자동 저장이 저장소를
   덮어써 **두 복원이 다른 문서를 열고 있었다**. 29-1은 로드 타이밍을 조금 밀었을 뿐이다.
   → **두 수가 정확히 같으면 둘이 같은 것을 재고 있는지 의심한다.** 수리는 임계가 아니라
   **같은 바이트를 다시 넣는 것**이다(경주를 없앤다 — 손잡이를 다시 박는 것으로는 dpr2에서
   0.035%가 남았다).

### #78. (2026-08-29 · web2-30) — **헤드리스 획득률이 올랐는데 손에서 안 걸리면 «후보 경쟁»을 의심한다. 픽스처의 획 수를 봐라**

web2-26 3번이 연장선의 획득 띠를 넓혀 헤드리스 획득률을 **40 → 100/110**으로 올렸다.
**실제 도면에서는 거의 안 걸렸다.** 원인은 값이 아니라 **층위**였다:

- 픽스처는 획이 몇 개뿐인 **깨끗한 장면**이다. 실제 작도는 획이 수십 개고 **포인터 근처에
  끝점·중점·교점 후보가 늘 하나쯤 있다** — 실측: 획 41개 장면에서 **점 후보가 이기는 칸
  85/299(28.4%)**.
- `OSNAP_ORDER`에서 점이 `ext`보다 앞이므로 그 칸에서는 **띠를 아무리 넓혀도 못 이긴다.**

**규칙 셋.**
㉠ **「헤드리스에서는 오르는데 손에서 안 걸린다」를 보면 임계가 아니라 «누가 이기는가»를 센다.**
   그 자리에서 **다른 후보가 이기는 비율**을 먼저 재라 — 그 수가 곧 「띠를 넓혀도 소용없다」의
   증명이다.
㉡ **픽스처의 «획 수»가 축이다**(#71 ㉢의 구체판). 후보 경쟁을 재는 팔은 **실사용 밀도**에서
   재야 한다. 지시가 「40개 이상」을 못 박은 것이 그 뜻이다.
㉢ **어떤 것은 후보가 아니라 «선언된 구속»이다.** 사람이 의도적으로 집는 것(연장선·축 잠금·
   방향 잠금)은 **가까움으로 이기는 목록에 넣는 순간 진다.** 판별 물음: **「이것은 늘 켜져
   있는가, 사람이 집은 뒤에만 사는가」** — 뒤엣것이면 후보가 아니라 구속이고, 적용은
   «경쟁»이 아니라 **투영**이다(선례: AutoCAD object snap tracking · Rhino 방향 잠금).

⚠ 짝: **구속으로 옮기면 그 후보를 읽던 팔이 전부 뒤집힌다** — 「획득하면 ext가 난다」가
「선언해도 ext는 안 난다」가 된다. 뒤집힌 반증에는 **분해능 단언**을 붙여라(「선언이 실제로
섰다」를 먼저 확인하지 않으면 0은 아무 뜻이 없다).
⚠⚠ 그리고 **발화 조건을 잃은 반증은 은퇴시킨다**(#77 ㉡) — 억지로 살리면 항등이 된다.

### #79. (2026-08-29 · web2-30 후속 · web2-32) — **화면의 한 모서리는 한 가지만 쓴다. 겹치면 자리를 나누고 z로 풀려 하지 마라**

web2-30의 전량 e2e가 잡은 제품 결함이 이 형태였다: 설정 서랍·진단 패널·자립 깃발이 **같은
우하단**을 쓰면서 **열린 깃발이 「진단」 단추를 삼켰다.**

- **z-index로는 못 푼다.** 세로바처럼 **자기 쌓임 맥락(stacking context)을 만드는 요소**가
  있으면 그 안의 z는 바깥으로 넘어가지 못한다. 맥락째 올리면 이번에는 **다른 규칙이 뒤집힌다**
  (「알림이 리본 위」가 그 자리였다).
- **답은 자리 다툼을 없애는 것이다.** 30-3의 답은 「진단」을 **명령으로** 바꿔 누르면 서랍이
  접히게 한 것이었다 — 두 가지가 같은 칸을 동시에 차지할 국면 자체를 없앤다.
- 규칙: **한 모서리 = 한 가지.** 새 것을 그 모서리에 두려면 **자리를 나누거나**(다른 모서리 ·
  같은 줄의 다른 칸) **하나를 접는다.** 「z를 올려서 해결」은 그 순간만 참이다.

⚠ **판별 물음**: 「이 둘이 **동시에** 열릴 수 있는가」. 그렇다면 자리를 나눈다.
⚠ 짝: **이런 자리 다툼은 단위 팔이 못 잡는다** — 화면 합성의 사실이라 **전량 e2e**가 잡았다
(#74 ㉢의 사촌: 그때도 막은 것은 착수 표가 아니라 전량 e2e였다).


### #80. (2026-08-29 · web2-32) — **복원한 판은 이미 저장소의 것이다 · 그리고 «내 회귀인가»는 기준 트리에서 같은 팔을 돌려 가른다**

`roundsave.spec ③`이 전량에서만 무작위로 빨개졌다. 두 가지가 한 자리에 있었다.

**㉠ 제품**: 초기화가 리스너를 울리므로 **부팅 직후 400ms 뒤에 «방금 읽은 것을 도로 쓰는»
저장**이 예약된다. 그 창 안에 저장소를 비우면(사람이 지우거나 팔이 `localStorage.clear()`)
**비운 뒤에 옛 문서가 되살아난다** — 실측 **50ms**. 자동 저장은 «바뀐 것»을 남기는 일이므로
**판본 게이트**(`savedVersion`)가 답이다. ⚠ 부팅 직후 `clearTimeout` 하나로는 **안 된다** —
초기화가 리스너를 **여러 번** 울려 그 뒤에 다시 예약된다(그것도 실측으로 갈렸다).

**㉡ 진단**: 전량에서만 나는 실패를 보면 **«내 것인가»부터 가른다** — 갈래의 소스를
기준 커밋으로 되돌려(`git checkout <base> -- src`) **같은 팔·같은 추적**을 돌린다.
그 한 번이 «회귀 사냥»과 «선재 결함 수리»를 가른다. 여기서는 기준 트리도 실패했고,
그래서 이 회차의 수리가 **정당해졌다**(안 그랬으면 남의 결함을 내 것으로 오귀속했다).
⚠ **경주형 실패는 «한 번 통과»가 무죄의 증거가 아니다** — 기준 트리에서 2회 중 1회 실패했고
갈래 트리에서는 3회 중 3회 실패했다(같은 경주라도 트리마다 승률이 다르다). 그러므로
**기준 트리를 최소 2회** 돌리고, 그래도 안 나면 «못 갈랐다»고 적고 수리를 미룬다.
⚠ 짝: **부하도 후보다** — 같은 실행에서 `tsc`·`vitest`를 함께 돌리면 픽셀·타이밍 팔이
흔들린다. 전량을 돌리는 동안에는 **아무것도 같이 돌리지 않는다**(그리고 dev 서버가 디스크를
읽으므로 **실행 중에 소스를 고치면 그 실행은 무효다**).

### #81. (2026-08-30 · web2-32 · **확대 2026-09-01**) — **상한 없는 대기를 만들지 마라 — 배포든 시험이든 산출물이든 · 그리고 배포 확인을 «화면 긁기»로만 하지 마라**

> ⚠⚠ **이 항목의 범위는 «배포 대기»가 아니라 «모든 대기»다**(2026-09-01 사람 지시로 넓혔다 — 아래 「#81 확대」). 아래 본문은 그 규칙이 **처음 난 자리**(배포)의 기록이고, 규칙 ㉡은 처음부터 「모든 대기」라고 적혀 있었는데 **새로 만드는 대기에 적용되지 않았다.**

`deploy-check`가 화면(`sw.js`)의 빌드 식별자만 긁었고, 그것을 무한 반복하는 대기를 걸었다.
**세 시간을 기다렸다.** 그동안 실제로 일어난 일:

```
그 커밋(76c1e5f)의 Pages 실행 = build 실패(npm test) → deploy **skipped**
→ 화면은 마지막 성공분(5627083)을 영원히 낸다 → 조건이 영원히 거짓
```

**세 갈래를 이 순서로 가른다**(사람이 못 박은 진단 순서):
① **권위 있는 곳부터** — 화면이 아니라 **API**다. `actions/runs?head_sha=…`로 그 커밋의
   실행이 **있는가 · 무엇으로 끝났는가**를 본다. 없거나 실패면 **대기는 영원히 안 끝난다.**
② **캐시를 우회해 실제 배포물을 받는다** — `?t=<epoch>` + `Cache-Control: no-cache`.
   우회한 값과 안 한 값이 **다르면** 원인은 CDN이고, **같으면** CDN이 아니다(여기서는 같았다).
③ **비교식을 본다** — 화면은 7자 단축 해시인데 `git rev-parse HEAD`는 40자다. 그대로
   `===`로 견주면 조건이 **영원히 거짓**이다. (여기서는 `startsWith`라 정상이었다 — 그래서
   이 갈래는 **배제**됐고, 배제도 진단이다.)

**규칙 둘.**
㉠ **화면 긁기는 «퍼졌는가»만 말한다 — «나갈 수 있는가»는 API가 말한다.** 판정자는 둘 다 본다.
   (`web2/tools/deploy-check.mjs`가 이제 ①'로 그 실행 상태를 먼저 낸다 — 실패면 그 자리에서
   「기다려도 안 나간다」로 끝낸다.)
㉡ **상한 없는 대기를 만들지 않는다.** 모든 대기에 **상한**과 **상한에 걸렸을 때 볼 목록**을
   함께 적는다(Pages는 정상이면 1~3분이다 — 15분이면 그것은 대기가 아니라 **멈춘 것**이다).
⚠ 짝: **CI를 막은 것이 «이 회차의 결함»이 아닐 수 있다** — 여기서는 web2-23의 타이밍 팔이었고
**원인이 둘이었다**(하나를 고치니 다음이 나왔다):
㉠ **예열 순서** — `cost23`의 첫 칸이 예열을 못 받아 부풀었다(CI: 50획 **9.91** · 100획 4.3 ·
   200획 6.57 · 400획 9.69 — 열의 첫 칸만 어긋났다). **격자를 한 번 돌고 버리는** 예열로 고쳤다.
㉡ **«비»는 고정 몫에 물린다** — 러너는 굽기 한 번에 **3~4ms 고정 몫**이 있다(이 컨테이너의
   최소 칸 0.15ms ↔ 러너 대역 3~4ms). 그래서 「40면이 5면의 **두 배**」가 여기서는 6.2배인데
   러너에서는 **1.98배**로 내려가 0.02 차이로 빨개진다. 고정 몫은 **차를 빼면 사라지므로**
   같은 주장을 **차**로 다시 썼다(「면 5→40의 값 > 획 50→400의 값」 — 5.25 > 0.78 ↔ 4.81 > 1.4).
**둘 다 임계를 안 낮췄다** — 재는 순서와 묻는 형태를 고쳤다(#14의 「재는 것을 재는지 본다」).

### #81 확대 (2026-09-01 — 사람 지시) — **문면은 「모든 대기」였는데 새로 만드는 대기에 안 걸렸다**

> ⛳⛳ **등재 번호는 #95다. 이 절은 그 규칙의 «근거»이지 두 번째 집이 아니다.**
> 같은 규칙을 **두 세션이 각각** 적었다(2026-08-31~09-01): 다른 세션이 **#95**로
> 등재해 「최근 다섯」 표·이력 표·각주에 넣었고, 이 세션은 사람 지시대로
> **#81의 근거로** 적었다. **번호를 둘로 두지 않는다**(#54 — 출처가 둘이면 갈린다):
> **규칙의 집은 #95이고, 이 절은 #81 쪽에서 그 규칙이 자란 자리와 사례 둘을 든다.**
> #95가 이 절을 가리키고 이 절이 #95를 가리킨다 — **사례는 합쳐서 여섯이다**
> (#95의 넷 + 여기 둘). 규칙 문면의 정본은 #95의 셋이다:
> **(1) 상한 · (2) 상한에서 무엇을 볼지 · (3) 대상이 아직 살아 있는지.**

> **세션이 끝날 때 백그라운드에 도는 대기가 있으면 그 자체가 결함이다.**
> **마감 보고에 「도는 백그라운드 없음」을 값으로 적어라.**

#81 ㉡이 처음부터 「**모든** 대기에 상한과 «상한에 걸렸을 때 볼 목록»을 함께 적는다」였다.
그런데 그 뒤로 만든 대기들이 **그 규칙을 안 지났다** — 규칙이 «배포 확인» 이야기 안에
들어 있어서 **배포 대기의 규칙으로 읽혔기** 때문이다. 그래서 제목과 범위를 넓힌다.

**근거 둘**(2026-09-01 사람이 잡아서 보고했다):

```
㉠ web2-39 마감 뒤의 배포 대기가 **12시간 51분** 돌았다
   `until node tools/deploy-check.mjs | grep -q '결과: **배포됨**'; do sleep 45; done`
   상한 없음 · 「상한에 걸리면 무엇을 볼지」 없음 · 사람이 끊었다.
   ⚠ 그 사이 배포는 **이미 끝나 있었다**(main 2a15097 · 화면 식별자 2a15097) —
     조건이 «내 HEAD»에 묶여 있었고 그 HEAD는 진작 옛것이 됐다.
     즉 #81 ①의 「조건이 영원히 거짓」이 **다른 이유로** 재발했다.

㉡ 같은 세션의 개발 서버가 **14시간 37분** 떠 있었다(localhost:5301 · web2)
   그 자체가 대기는 아니지만 **같은 뿌리**다 — 끝을 안 정하고 켠 것이 세션을 넘겼다.
   ⚠ 이쪽은 **#70**(오래 떠 있는 개발 서버가 옛 빌드를 낸다)로도 걸린다. 두 항목이 만난다.
```

⚠ **셈이 안 맞는 자리를 그대로 적는다**(D-4 — 사람이 준 수도 확인 대상이다): 지시는
「지금까지 상한 없는 대기가 **여섯** 번 — 배포 대기 **셋**과 시험·산출물 대기 **넷**」이라고
적었는데 셋 + 넷 = **일곱**이다. 어느 쪽이 맞는지 이 항목이 판정하지 않는다.
**세어서 확인한 것만** 적으면 이렇다:

```
#81 본문의 그 대기(web2-32)                          3시간      배포
web2-39 세션의 전량 e2e 로그 대기(`until grep …`)     끝났다     산출물
web2-39 세션의 배포 대기 ①(`until deploy-check …`)    끝났다     배포
web2-39 세션의 배포 대기 ②(같은 형태)                12시간 51분 배포   ← 사람이 끊었다
```

**세 번 다 같은 형태이고 세 번 다 상한이 없었다.** 「끝났다」로 적힌 둘이 위험하지 않았던
것이 아니라 **운이 좋았던 것**이다 — 조건이 참이 될 이유가 사라지면 셋 다 12시간이 된다.

**규칙 셋**(㉠㉡은 위 본문 그대로이고 여기 ㉢㉣㉤을 더한다).

㉢ **대기의 범위는 «배포»가 아니다.** 배포 · CI · 시험 · 산출물 · 파일 · 포트 —
   **끝을 남이 정하는 것을 기다리는 모든 자리**가 이 항목이다.
㉣ **상한을 «시간»으로 적고 «상한에 걸리면 볼 것»을 함께 적는다.** 상한이 없으면 그것은
   대기가 아니라 **잠금**이다. 정상 대역의 3~5배가 상한의 눈금이다
   (Pages 1~3분 → 15분 · 전량 e2e 40분 → 2시간).
   ⚠⚠ **#95가 여기에 셋째를 더했다 — «대상이 아직 살아 있는가»**. 그쪽 사례 둘이 그 축이다:
   `pgrep -f <패턴>`이 **자기 명령줄을 세고**(대기가 자기를 찾는다), 파수(`EXIT=`)를 **일하는
   쪽이 로그에 안 써서** 조건이 영원히 거짓이었다. 이 절의 12시간 51분짜리도 **같은 축**이다 —
   대상(Pages 실행)은 살아 있었는데 **비교 대상인 내 HEAD가 죽어 있었다**(낡았다).
   즉 「살아 있는가」는 **기다리는 쪽·일하는 쪽·비교 대상** 셋 다에 묻는다.
㉤ ⛳ **세션이 끝날 때 도는 백그라운드가 있으면 그 자체가 결함이다.**
   마감 보고에 **「도는 백그라운드 없음」을 값으로 적는다** — 「없다고 생각한다」가 아니라
   **센 값**이다(#89의 형태: 「초록」이 「무엇이 초록인가」 없이는 정보가 아닌 것과 같다).
   개발 서버도 여기 든다(㉡ · #70).

**판별 물음 둘**: ① 내가 기다리는 것의 **끝을 내가 정하는가** ② 그 조건이 **영원히 거짓이
될 수 있는가**(실행이 죽는다 · 내 HEAD가 낡는다 · 파일이 안 나온다) — ①아니오 ②예 이면
**상한 없는 대기 ⛔**.

**반증 조건**: `python selfcheck.py`의 `scan_unbounded_wait`가 ㉤을 지킨다 —
마감 블록에 그 줄이 없으면 플래그다. 실제로 떼서 확인했다(그 줄을 지우면 빨개진다).
⚠ **그 검사가 못 보는 것**(#26): 세션 안에서 즉석으로 만든 대기는 저장소에 없으므로
**안 보인다.** 그것을 지키는 것은 검사가 아니라 **마감 보고의 그 줄**이다.

### #76. (2026-08-29 · web2-26) — **사람이 준 원인 후보가 넷 중 셋 틀렸다 · 그리고 «지금 값»을 올리면 그 값을 «다른 뜻으로 읽던» 팔이 깨진다**

**사람이 준 원인 후보가 넷 중 셋 틀렸다 · 그리고 «지금 값»을 올리면 그 값을 «다른 뜻으로 읽던» 팔이 깨진다**(web2-26) — ㉠ **「A가 B와 얽혀 있다」는 후보는 «A가 B에 아예 없다»와 화면에서 구별되지 않는다**(필압: 「농도·굵기가 한 계수를 공유」 → **공유가 아니라 부재**. 층 오염: 「전역 맵」 → 전역 맵은 없고 **지우개 조각이 층을 안 물려받았다**) → 후보가 «있는 것의 성질»을 말하면 **먼저 그것이 있는지부터 찍는다** ㉡ **지시가 준 픽스처로 재현이 안 되면 «못 했다»고 적고 항목의 «실제 몸짓»을 다시 읽는다** — 26-1은 지시 픽스처로 깨끗이 통과했고 재현은 원본 확인표 문면(「확대해서 **바깥 토막 지우기**」)에서 났다 ㉢ **「같은 자로 재던 둘을 가른다」는 수리는 둘 중 하나가 실은 첫째 범주일 수 있다** — `perp`는 선분 **안으로 클램프된 점**이라 띠를 넓히자 확정 좌표가 **15px 밀렸다**. 판별 물음 둘: **자리가 한 축인가 두 축인가 · 사람이 «집었다» 뒤에만 사는가 늘 켜져 있는가** ㉣ ⚠⚠ **「지금 값」을 올리면 그 값을 다른 뜻으로 읽던 팔이 깨진다** — 결의 진폭을 지각 대역으로 올리자 **다섯**이 빨개졌다(절대 밝기 150 · 못 박힌 좌표 대역 60~200 · 전체 평균 기준 에너지 · 36px 떨어진 «곁» · 빈 줄의 `min`을 «종이»로). 다섯 다 요구는 유효했고 **읽는 자리와 기준**만 갈렸다 → **질감의 세기를 바꾸는 수리는 «신호 대 배경»을 쓰는 팔 전부의 기준을 동시에 바꾼다. 팔마다 「종이보다 어두운가」인지 「결보다 어두운가」인지 적는다 — 물음이 둘이면 기준도 둘이다** ㉤ **시간 임계를 내리면 «시간으로 나눈 다른 양»이 같이 느슨해진다** — 머무름 상자의 실효 속도 문턱 `상자÷시간`이 600→350ms에서 6.7 → 11.4 px/s로 **넓어졌다**. 답은 **상자를 시간에 비례시키는 것**. ⚠ 그 자리에 지수평활 ⛔(멈춘 뒤 내려오는 데 `τ·ln(...)` — 얻으려던 시간을 먹는다) ㉥ **독립 성분의 바닥은 «뺄셈»이 아니라 `sqrt(신호²−바닥²)`** — 분산이 더해진다(뺄셈이 dpr 비를 1.108 → 1.155로 부풀려 게이트를 경계에서 깨뜨렸다) ㉦ **dpr 사이에서 획의 진하기는 «평균»이 아니라 «어두운 쪽 분위수»** — 1.4px 획은 dpr2 상자 평균에서 네 배 희석된다(차 6.4 → 1.6)

### #77. (2026-08-29 · web2-27~29) — **「모드를 더한다」는 그 자리를 쓰던 «다른 경로»를 죽인다 · 값이 스스로를 정의하면 그 «검증»은 항등이다**

**「모드를 더한다」는 그 자리를 쓰던 «다른 경로»를 죽인다 · 값이 스스로를 정의하면 그 «검증»은 항등이다**(web2-27~29) — ㉠ 한 손잡이에 뜻을 하나 더 얹으면 **옛 뜻이 조용히 죽는다**(치수 단추에 모드를 얹자 web2-10 키패드 경로가 죽었다 — 2500 자리에 75). **새 모드는 새 자리에서 들어간다** · 화면에서 「패널이 열렸다」와 「모드가 켜졌다」가 갈려야 한다. 짝: 28-1이 비우기에 접힘을 얹어 **확인의 앵커가 사라졌다** — 답은 둘 다 «표시를 하나 더 만든다»(`data-fold="late"`) ㉡ **값이 스스로를 정의하면 「적힌 값 ↔ 잰 값」 검증은 구성상 항등이다** — `Stroke.dim`이 길이를 정하므로 9000 ↔ 9000.000 · 비 1.000000. 지시가 요구해도 **발화 조건이 없으면 안 만든다**(문턱 상수까지 만들었다가 걷었다 — §5.1 자기참조 유형 3). 그 0이 «척도가 죽었다»가 아님은 분해능으로 보인다 ㉢ **「이미 있는지」부터 세면 회차가 절반이 된다** — 29-1은 인식·분절·표시·적용·파싱이 전부 있었고 없는 것이 둘이었다. 그중 하나가 **놀라움**(치수가 화면에 한 번도 안 그려졌다) ㉣ **지시의 전제가 낡을 수 있다** — 「488KB」는 web2-25 이전 값이고 실측 238,456 B(48.9%)로 게이트가 이미 서 있었다. **지시가 수를 인용하면 원장을 그 자리에서 다시 읽는다** ㉤ **`null`을 보면 «고칠 것»이 아니라 «옆에 둘 것»을 찾는다** — `pointOnGround`의 null은 옳고 없던 것은 위 갈래였다(`pointOnCeiling`을 따로). ⚠ 새 갈래의 범위는 팔이 **두 번** 좁혔다(걸침 거부를 세로선까지 넓히자 눈앞의 기둥이 죽었고, 천장을 되살림 패스에 넣자 팔 열둘이 깨졌다) ㉥ **팔이 «처음» 빨개졌다고 그 회차가 원인인 것은 아니다** — `roundsave` 잡음 바닥이 **반올림 몫과 정확히 같은 값**(0.067%)이었고 그것이 오염의 증거였다. **두 수가 정확히 같으면 둘이 같은 것을 재는지 의심한다**

⚠⚠ **㉡의 후속(web2-32 5·6·7)**: 「값이 스스로를 정의하면 그 검증은 항등이다」는 **기능을
없애라는 뜻이 아니었다.** 29-2는 그 항등을 보고 어긋남 표시를 걷었는데(AS-C107),
**원인이 하나가 아니라 둘의 곱이었다**(2026-08-30 1차 리뷰어가 초판의 「자 단독」 주장을
원장으로 반박했다 — 이 후속 절도 그때 같이 고쳤다):

```
㉮ 표본이 하나면  →  어느 자로 재도 1이다      (30-6이 잡은 것)
㉯ 자가 «적용 뒤»면 →  표본이 둘이어도 1이다     (web2-32가 잡은 것)
둘 다 고쳐야      →  둘째 치수에서 1.5로 갈린다
```

29-2의 장면은 ㉮라 **두 자가 같은 답을 냈고 그래서 가를 수 없었다** — 「자를 잘못 댔다」만
적으면 다음 사람이 ㉮에서 다시 걸린다. **판별 물음도 둘이다**: 「표본이 하나인가」 **그리고**
「이 자가 무엇을 보장하는가 — 그 보장을 안 지나는 다른 자가 있는가」.
근거는 `scale32_web2.json`의 `skew_table`(두 열이 나란하다)과 `skew_first_dim`(올바른 자로
재도 치수가 하나면 1)이다.

⚠ **이 함정은 «잡은 세션 안에서» 두 번 더 났다** — 같은 회차의 32-6이 ① 정체 왕복 오차
(구성상 0)에 임계를 걸었고 ② 「참값 대조」로 **축척의 분모 획을 재서 적은 값이 그대로
나오는 것**을 썼다. 둘 다 리뷰어가 잡았다. **항등은 «내가 지금 잡고 있는 그 형태»로도
다시 온다** — 새 게이트를 세울 때마다 그 물음을 다시 한다.

### #82. (2026-08-30 · web2-32 5·6·7) — **고정 몫이 있는 환경에서 성능 주장을 «비»로 쓰지 마라. «차»로 써라**

**주장**: 「40면이 5면의 **두 배**」처럼 성능을 **비**로 적으면, 측정 대상이 작을수록
그 비가 **환경의 고정 몫에 잡아먹힌다**. 같은 주장을 **차**로 적으면 고정 몫이 상쇄된다.

**근거(실측)**: web2-23의 `cost23` 팔이 그렇게 죽었다. 이 컨테이너에서는 한 칸의 최소 시간이
**0.15 ms**인데 CI 러너는 굽기 한 번에 **3~4 ms 고정 몫**이 있다. 그래서 같은 코드가

```
이 컨테이너:  40면/5면 = 6.2배        (고정 몫이 신호에 비해 작다)
CI 러너   :  40면/5면 = 1.98배       (고정 몫 3~4 ms가 분자·분모 양쪽에 실려 비를 1로 끌어당긴다)
```

가 되어 임계 2.0에 **0.02 차이로** 빨개졌고, 그것이 **배포를 막았다**(#81과 짝).
고쳐서 **차**로 다시 쓰자 두 환경이 같은 방향을 냈다:
「면 5→40의 **값** > 획 50→400의 **값**」 — 5.25 > 0.78(컨테이너) ↔ 4.81 > 1.4(러너).

**왜 차는 안 물리나**: 측정값이 `고정몫 + 신호`이면
`비 = (c+a)/(c+b)`는 c가 클수록 1로 간다. `차 = (c+a)−(c+b) = a−b`에서 **c가 지워진다.**

**규칙**:
- 타이밍 팔의 단언은 **차**로 쓴다. 비로 써야 한다면 **고정 몫을 먼저 빼고** 쓴다
  (그러려면 «빈 일»의 시간을 같은 실행에서 재야 한다 — 안 재면 그 비는 못 믿는다).
- 임계에 걸렸을 때 **임계를 낮추는 것이 답이 아니다** — 묻는 형태가 틀린 것이다(#14의 계열).
- 이 함정은 **앞으로 만드는 모든 타이밍 팔에서 다시 만난다**(사람이 못 박았다). 새 팔을
  세울 때 「이 수는 비인가 차인가 · 고정 몫이 얼마인가」를 먼저 적는다.
- ⚠ **비를 «차»로 바꾸는 것이 이 함정의 전부가 아니다** — 아래 두 대가를 같이 본다.

**⚠⚠ 대가 둘(1차 리뷰어 [11]이 이 항목의 수치로 잡았다 — 등재와 같은 세션에)**

㉠ **고정 몫 3~4 ms는 «역산값»이지 측정된 «빈 일»이 아니다.** 위 네 수로 모델이 닫히긴 한다
   (컨테이너 5면 1.01 ms · 40면 6.26 ms / 러너 5면 4.91 ms · 40면 9.72 ms → c ≈ 3.9 ms).
   그런데 그것은 **면 축 하나에서 «두 환경의 차»로 푼 값**이다 — 이 항목이 스스로 건 조건
   (「빈 일을 같은 실행에서 재라」)을 **이 항목의 근거가 안 지켰다.**
㉡ **같은 실행의 «획 축»은 그 모델을 깬다.** 고정 몫이 차에서 지워진다면 두 축의
   러너/컨테이너 **신호비**가 같아야 하는데:

```
면 축   러너 4.81 ÷ 컨테이너 5.25 = 0.92
획 축   러너 1.4  ÷ 컨테이너 0.78 = 1.79      ← 두 배 차이. 「c만 다르다」가 아니다
```

   즉 러너는 **고정 몫만 더 있는 것이 아니라 신호 자체가 축마다 다르게 늘어난다**(획 축은
   메모리·GC 몫이 다를 것이다 — **안 쟀다**). 그래서 차로 옮긴 뒤에도 획 축의 여유는
   6.7배 → **3.4배**로 줄었다. **차는 «비보다 낫다»이지 «환경 무관»이 아니다.**

→ **이 항목을 인용할 때 ㉠㉡을 같이 인용한다.** 다음에 이 자리에서 할 일은 **빈 일을 같은
   실행에서 직접 재는 것**이다(그러면 c를 빼고 비를 쓸 수도 있고, ㉡의 잔차도 드러난다).

**⚠ 범위 — 「비를 쓰지 마라」가 아니다**(2차 리뷰어 [14]). 이 항목이 말하는 것은
**고정 몫이 있는 양**(시간·굽기 횟수처럼 «한 번 치르는 값»이 섞이는 측정)이다.
**고정 몫이 없는 상대량은 비가 옳다** — 같은 회차의 어긋남 자(`DIM_SKEW_RATIO` = 적은 값 ÷
잰 값. ⤴ **web2-34 7번이 대칭 자 `DIM_SKEW_FOLD`로 바꿨는데 «상대량»이라는 이 요지는 그대로다**)가 그 예다: 거기서 절대 mm를 쓰면 **짧은 선이 통째로 조용해진다**(100 mm의 40%가
40 mm라 치수 스냅 한 칸 밑이다). 판별 물음: **「이 측정에 «크기와 무관하게 늘 더해지는
값»이 있는가」** — 있으면 차, 없으면 비다.

### #83. (2026-08-30 · web2-35) — **「새 특징을 더했더니 올랐다」는 «그 자리에 무엇이든 하나 더 둔 것»과 구별되지 않는다. 그리고 팔마다 자가 다르면 한 문턱으로 나란히 세우지 마라**

web2-35는 손글씨 인식에 **궤적**(획순·방향·전환·획수·붓 뗀 자리·공중 경로)을 더했고
합성 표본에서 **506 → 770/880**이 나왔다. 그 수를 「궤적의 승리」로 적으면 32-4의 오진을
그대로 반복하는 것이었다 — 지시가 걸어 둔 「궤적 단독 이득을 따로 재라」가 그것을 막았다.

**㉠ 값이 크게 오르면 먼저 «같은 자리에 가장 싼 대안»을 넣어 본다.**
여기서 그 대안은 **이미 저장소에 있었다** — `digits.ts`의 $P 점군(web2-08). **같은 원형
표**를 쓰고 **순서·방향만 안 본다**. 그것을 같은 자리에 붙이니 **695**가 나왔다.

```
506  래스터 두 시야(기준선)
695  + $P만        ← 세 번째 시야를 «아무거나» 붙였을 때의 값.  +189
725  + $P만 @실린 문턱 0.10  ← 오답 3(불변식 밖)
707  + 궤적만      ← 전량으로는 $P@0.095를 12칸 이기지만 **그 12칸은 씨 변동폭 안이다**(㉦)
770  + 둘 다(채택) ← 둘이 서로 다른 자형을 살리므로 이어 붙는다(+45 · +63)
```

**올린 것의 대부분(+189)은 «궤적»이 아니라 «시야를 하나 더 둔 것»이었다.**
⚠⚠ 그리고 **「궤적이 그 대안보다 낫다」는 결론이 서지 않는다** — 아래 ㉦를 보라.
→ **새 기제의 이득은 «기제 없는 같은 구조»와 견줘야 이득이다.** 위약 팔이 없으면
그 수는 «구조의 이득»이지 «기제의 이득»이 아니다.
⚠⚠ **그리고 위약이 «이미 있는 것»과 같은 계열이면 반만 갈린다.** 여기서 세운 위약은
「같은 구조에서 궤적 항만 끈 판」이었는데, 그것은 이미 있던 $P와 **같은 계열**(같은 원형
표 · 순서·방향을 안 보는 모양 거리)이었다. 그래서 답이 나온 물음은 「이득이 **그 기제의
축**에서 오는가」이고, 「**아무 새 시야나** 얹어도 같은가」는 **안 갈렸다** —
**위약을 세울 때 «그것이 기존 것과 다른 축인가»를 먼저 적는다.**

**㉡ ⚠⚠ 팔마다 거리의 «자»가 다르면 한 문턱으로 나란히 세우지 마라**(#76 ㉣의 재발).
초판이 궤적과 $P를 **같은 값 0.11**로 세워 「$P가 725로 더 낫다」를 냈다. 그 725는
**오답 3**이었고 이 회차의 불변식(오답 ≤ 2)을 **어긴 값**이라 애초에 견줄 자격이 없었다.
팔마다 문턱을 훑어 **«불변식을 지키는 최대 맞음»**을 그 팔의 값으로 쓰니 707 대 695로
**부호가 뒤집혔다.** → **서로 다른 자로 잰 두 수를 견줄 때는 «같은 눈금»이 아니라
«같은 제약»으로 맞춘다.**

⚠⚠ **그리고 이 규칙을 세운 회차가 같은 회차 안에서 다시 어겼다**(2차 리뷰어가 「세션 내
재발」로 적었다). 팔 표에서는 지켰는데 **서술에서 어겼다** — $P를 **제 최선 문턱 0.095**로
둔 자형별 차이표(궤적만 50 · $P만 38)를 **런타임($P 0.10)**의 총계 770 옆에 세우고
「그것이 770의 근거다」라고 적었다. 그 표는 구제된 264칸 중 **239칸만 설명한다**(25칸이
떠 있었고 아무도 안 셌다). 런타임 자로 다시 내니 **45 / 63 / 156 / 108**이고 합이 맞는다.
→ ㉡은 「**문턱**을 같게 두지 마라」로만 읽히기 쉽다. **정확히는 «동작점이 다른 두 표를
같은 문단에 두지 마라»다** — 표마다 어느 문턱에서 잰 것인지 **수 옆에** 적는다.
**검산 하나가 그것을 잡는다**: 분해표의 각 칸 합이 총계와 맞는가.

**㉢ 원형·템플릿을 이 회차에서 지으면 그 회차의 평가가 무의미하다.**
궤적 원형을 평가 픽스처(11자형)를 보고 지었으면 「궤적이 올렸다」가 아니라 「픽스처를
외웠다」가 된다. 그래서 원형의 출처를 **이전 회차의 것**(web2-08의 $P 템플릿 표)으로
못 박고, 픽스처를 **좌표 비교로** 둘로 갈라 냈다 — 표본 안 4자형(320칸, 원형과 같은 획) ·
**표본 밖 7자형(560칸)**. 인용은 표본 밖으로 한다(230 → 450). 그리고 **세리프 원형을
런타임에 넣지 않았다** — 원형을 짓고 그 원형으로 채점할 수 없기 때문이다.
⚠⚠ **그런데 그 가르기는 «원형 좌표»만 가린다.** 이 회차가 **평가 픽스처를 보고 지은 것**은
따로 있었다 — 되짚기 항과 획 병합 규칙을 「4·닫힌·1획」을 본 뒤에 세웠다(D-1대로 자형별
표를 먼저 폈으니 필연이다). **특징 설계의 출처도 좌표 출처와 같이 적는다.** 실측으로 그 몫을
보이면 더 낫다 — 병합을 끄면 잃는 칸이 정확히 그 자형 80칸 하나였다.

**㉣ 무엇을 더할지는 «실패의 구조»가 정한다 — 평균이 아니라 분해표를 먼저 편다.**
32-4 원장의 `per_form_box`가 낸 것은 **「틀림 2 · 거부 372」**였다. 클래스끼리 헷갈리는
자리가 사실상 없으므로 **후보 재순위기는 할 일이 없고** 필요한 것은 **구제자**였다.
궤적의 관용적 용법(재순위)을 그대로 가져왔으면 아무것도 안 올랐다.
→ **「무엇이 약한가」를 묻기 전에 「어떻게 실패하는가」를 센다**(오답인가 거부인가).

**㉤ 반사실 팔의 주장은 «그 층이 지키는 축»에서 세운다.**
초판은 「궤적만으로 판정하면 기준선보다 못하다」를 **맞음**으로 적었는데, 맞음은 오히려
**늘었다**(506 → 585). 터지는 것은 **오답**이었다(2 → **295**). 단언이 틀린 축을 재고
있었다. → 비용이 비대칭이면(#61) 반사실도 **그 비대칭 쪽**에서 단언한다.

**㉥ 곁가지 — `PITFALLS.md` 제목 줄의 형식이 곧 등록부다.**
#76·#77이 `## #76 — …`로 적혀 있어 `selfcheck`의 인용 검사(`### #NN.` 또는 `NN. **`만
읽는다)가 그 둘을 인용한 원장을 **「없는 번호」로 오검**하고 있었다(`glyph32_web2.json`의
#77이 그 자국이다). 그 검사의 주석이 이미 「고칠 곳은 검사가 아니라 기록하는 쪽」이라고
못 박아 두었다 — **제목 줄만** 형식에 맞추면 사라진다.
⚠⚠ **그리고 그것이 #76·#77만의 일이 아니었다**: `### #NN (날짜) — ` 형식을 쓰는 **#78~#82
전부**가 등록부 밖이었고(번호 뒤에 **마침표가 없다**), 그래서 #78·#80·#81을 인용한 원장 셋이
「없는 번호」로 잡히고 있었다. 다섯 줄에 마침표 하나씩을 넣어 전부 사라졌다.
**새 항목은 `### #NN.`으로 적는다 — 번호 뒤 마침표가 등록부의 열쇠다.**

**㉦ ⚠⚠ 이득이 «오답 한 칸»에 걸려 있으면 씨 변동폭부터 잰다**(#14의 재발 · 2차 리뷰어).
「궤적이 $P보다 12칸 낫다」는 $P가 문턱 0.10에서 **오답 3**이라 불법이 되는 데 통째로
걸려 있었다. 씨 다섯을 따로 내니 그 차가 **[-1, 0, 7, 4, 2]**(폭 8 · 둘은 0 이하)였고
$P의 오답도 씨별 **[0, 0, 1, 0, 2]**로 **2를 넘는 씨가 하나도 없었다** — 「불법」이
**다섯 씨를 합쳤을 때만** 나오는 판정이었다. 그 결론을 뺐다.
→ **머리끼리 견주는 값(A가 B보다 나은가)은 씨에 약하고, 사슬 안의 한계 이득(B가 있어도
A가 더 얹는가)은 씨에 강했다**(여기서 45 · 씨별 폭 2). 같은 표본에서 둘 중 **뒤엣것만**
주장한다.
⚠⚠ **그 «강함»을 일반 규칙으로 읽지 마라 — 씨가 무엇을 바꾸는지 먼저 봐라**(2차 리뷰어).
여기서 씨는 **흔들기의 난수 실현만** 바꾸고 자형·상자 구성은 **다섯 씨에 똑같이** 들어 있다.
그리고 그 45는 **자형 하나에 통째로** 몰려 있다. 그러므로 폭 2는 「효과가 일반적이다」가
아니라 **「그 한 자형이 흔들기에 강하다」**이고, 머리끼리 견주는 값이 흔들린 것은 서로 다른
두 자형 효과가 **거의 상쇄되는 차**여서다. **씨가 표본의 «구성»을 안 바꾸면 씨 변동폭은
구성에 대한 강건함을 아무것도 말하지 않는다.**

**㉧ 「표본 밖이 더 올랐다」는 남은 여유로 정규화해야 한다**(#11). 표본 안은 여유가 44뿐이라
**다 먹어도 44**이고 표본 밖은 330이다 — 원수 비교(220 > 44)는 **통과가 예정된 게이트**다.
여유 대비로는 표본 «안»이 100%로 더 높다. **외우기를 배제하는 증거로 그 비교를 쓸 수 없다** —
남는 주장은 「원형에 없는 필체에서도 크게 오른다」(41.1% → 80.4%) 하나다.

**㉨ 앞에 거름을 두면 뒤 팔의 상대 순위가 거저 통과한다**(#20). 궤적을 $P 앞에 두면
$P의 오답이 3 → 2로 내려간다. 그것을 **「궤적의 값」으로 적으면 안 된다** — #20이 이름
붙인 그 모양이다. 사실로만 기록한다.

**㉩ 새 게이트에도 방금 쓴 자를 대라 — 폐기한 기준이 새 조항으로 되살아난다.**
㉦로 「12칸/880은 씨 변동폭 안이라 결론이 못 된다」를 세운 **같은 대응**이, 새 게이트로
「표본 밖 정답률 **8할 초과**」를 걸었다 — 450/560 = 80.36%로 **여유가 2칸**이고 씨 분해가
없었다(실제 씨별 최저 76.8%로 그 선 아래다). **걷어낸 자리만 보고 새로 세운 자리를 안 봤다.**
→ 게이트를 고칠 때는 **새 조항에 방금 쓴 자를 그대로 대 본다.**

### #87. (2026-08-30 · web2-34 6번 후속) — **«겹친다»와 «가린다»는 다른 물음이다 · 쌓임 맥락 안의 z-index는 밖을 못 넘는다**

> ⚠ **이 절은 표(「최근 다섯」)에서 빠졌다**(2026-08-31 · web2-39 §0㉡이 #92를 더하면서). **본문은 그대로 살아 있고 자식이 둘이다** — **#91**(「안 바뀌었는가」 ≠ 「안 썼는가」) · **#92**(재는 양이 결과와 연결돼 있는가). 셋을 묶는 한 문장: **재기 전에 무엇을 재는지를 정하라.**

34-6이 R5(한 모서리는 한 가지만)를 위 띠에서 갈랐다고 적었는데, 리뷰어가 세로바 쪽을
물어 재 보니 **통 여섯 전부**가 치수 기둥과 겹쳤고 둘은 34-6이 고친 것보다 컸다
(`#etray` **127×243** · `#snap-pop` **94×309** ↔ 고친 표시 팝업 158×103).

**㉠ 겹침을 상자로만 재면 절반이다.** 「겹치는가」(상자 교집합)와 「가리는가」
(`elementFromPoint`가 누구를 내는가)는 **다른 물음**이고, 사람이 겪는 것은 뒤쪽이다.
34-6 초판의 팔은 앞쪽만 쟀다. 뒤쪽을 재자 **넷이 통째로 리본 뒤로 깔려 있었다**:
연필통 (1077,314) → `pad-keys` · 촉통 → `dimpanel` · 크기통 → 리본의 div · 롤통 → 리본의 label.
연필통·촉통은 **web2-30 이래**, 크기통은 **34-3 이래**다 — 아무 팔도 안 잡고 있었다.

**㉡ 쌓임 맥락 안의 z는 밖을 못 넘는다**(#79의 기제를 값으로). `#sidebar`가
`position:fixed + z-index`라 맥락을 만들고, 그 안의 `#tray { z-index: 12 }`가 맥락 밖의
`#dimpanel`(z 11)을 못 넘는다. **선언은 처음부터 옳았고 맥락이 그것을 삼켰다.**
그러므로 답은 z를 올리는 것도 자리를 나누는 것도 아니라 **맥락에서 꺼내는 것**이다 —
`position: fixed`로 자리를 JS가 잡는 요소는 DOM에서 어디 있든 화면 자리가 같으므로
**한 픽셀도 안 움직이고** 순서만 선다. 판별: 같은 자리의 다른 팝오버가 **맥락 밖**에
있으면서 잘 뜨고 있으면(`#snap-pop`) 그것이 정답 형태다.

**㉢ 규칙의 «범위»는 새 사례가 정한다.** R5는 「겹치면 자리를 나눠라」였는데, 그것은
**둘 다 오래 떠 있는** 사례(#79의 우하단 넷)에서 나온 문면이다. **잠깐 얹히는 것**(통·
팝오버)에 그대로 대면 답이 없다 — 통은 제 단추 옆에 떠야 하고 기둥은 세로로 길어
**반드시 겹친다**. 규칙을 무르지 말고 **판별 물음을 하나 더 붙인다**: ① 동시에 열릴 수
있는가 ② **둘 다 오래 떠 있는가.** ①만 예면 잰 것은 「자리」가 아니라 「순서」다.

**㉣ 무엇을 옮기면 그 자리를 «크기»로 예약하던 이웃이 깨진다.** 위 띠에 서랍 둘을 넣어
폭이 112 → **156**으로 늘자(왼쪽 끝 918 → **874**) 종이 띠의 예약
`calc(100vw - 300px)`(= 900)이 모자라 **탭 띠가 위 띠를 파고들었다** — 전량 e2e가 잡았다
(`paperbar.spec`에서 「+」가 안 눌린다). 예약이 **상수**로 적혀 있으면 그 상수는
옮긴 쪽과 **말없이 묶여 있다** — 변수로 빼고 그 띠의 실제 폭에서 잡는다.

⚠ **㉣는 2026-08-30에 자기 번호를 받았다 — `### #88.`이 정본이다.** 여기 남긴 것은
그 사례가 이 항목에서 **나온 자리**이기 때문이고, 규칙으로 인용할 때는 #88을 인용한다.

**㉤ ㉠을 한 문장으로 — 「상자 교집합은 «겹치는가»에 답하고 질문은 «가리는가»였다.」**
#71이 **재는 «조건»이 틀린** 경우(같은 자로 다른 조건에서 쟀다)라면, 이것은
**재는 «대상»이 틀린** 경우다 — 조건을 아무리 맞춰도 상자 교집합은 끝내 다른 물음에
답한다. `elementFromPoint`가 실제 물음에 답한다. 판별: **팔이 내는 수를 사람이 겪는
문장으로 옮겨 읽어라** — 「158×103 겹친다」는 사람이 겪는 문장이 아니고
「연필통을 누르면 `pad-keys`가 눌린다」가 그것이다. 옮겨지지 않으면 자가 틀린 것이다.

### 표 갱신 이력 — #76 ~ #88 (2026-08-30 · web2-32 5·6·7에서 소급 기재)

⚠ **1차 리뷰어 [17]이 이 구멍을 잡았다** — 이력 절의 마지막 기재가 2026-08-28(#75)이었고
#76~#82의 갱신 줄이 하나도 없었다. 표에 없는 항목이 **왜** 내려갔는지 기록이 없으면
「빠뜨린 것」과 「내린 것」이 구별되지 않는다.

| 언제 | 표에 든 것 | 내려간 것 | 비고 |
|---|---|---|---|
| 2026-08-29 web2-26 | #76 | (당시 기재 없음) | — |
| 2026-08-29 web2-27~29 | #77 | (당시 기재 없음) | — |
| 2026-08-29 web2-30 | #78 | (당시 기재 없음) | — |
| 2026-08-29 web2-30 후속 | #79 | **#78** | ⚠ 소급 판정: #78은 **`DEFERRED.md`의 32-3 행이 아직 인용한다** — 본문 절은 살아 있다 |
| 2026-08-29 web2-32 | #80 | (미상) | — |
| 2026-08-30 web2-32 | #81 | (미상) | — |
| 2026-08-30 web2-32 5·6·7 | **#82** | **#76** | 본문 절을 이 회차가 **세웠다**(그전에는 표 행에만 있었다) |
| 2026-08-30 web2-34 3번 | **#83** | **#77** | 두 갈래가 **같은 #82를 썼다** — web2-35의 것을 **#83**으로 옮겨 달았다(본문·표·인용 전부). #77 본문 절은 그대로 있다 |
| 2026-08-30 web2-34 8번 | **#86** | **#81** | 표를 「최근 다섯」으로 유지하며 #81을 뺐다 — **본문 절(#81 배포 확인)은 그대로**다 |
| 2026-08-30 web2-34 6번 후속 | **#87** | **#82** | 리뷰어 [1]이 「세로바 쪽은 봤는가」를 물어 나온 항목이다. 표를 「최근 다섯」으로 유지하며 #82를 뺐다 — **본문 절은 그대로**다 |
| 2026-08-31 web2-31 저장 회차 | **#88** | **#83** | **#87 ㉣를 자기 번호로 승격**했다(사람이 지시에 못 박은 등재). 같은 회차가 #87에 **㉤**를 더했다(«겹친다» ↔ «가린다»를 #71과 대비). 표를 「최근 다섯」으로 유지하며 #83을 뺐다 — **본문 절은 그대로**다 |
| 2026-08-31 web2-37 §2 | **#89** | **#84** | 사람이 지시에 못 박은 등재(「초록의 범위를 적어라」). 표를 「최근 다섯」으로 유지하며 #84를 뺐다 — **본문 절은 그대로**다 |
| 2026-08-31 web2-37 §1 | **#90** | **#85** | 사람이 지시에 못 박은 등재(원장 쓰기 관문). 표를 「최근 다섯」으로 유지하며 #85를 뺐다 — **본문 절은 그대로**다 |
| _(위 두 줄은 **web2-41이 소급해 채웠다**)_ | | | 37이 자기 행을 안 넣어 표와 각주가 갈려 있었다(2차 리뷰어가 잡았다). 소급 판정이 아니라 **표의 완결성**이다 — 각주는 처음부터 그 둘을 적고 있었다 |
| 2026-08-31 web2-41 §3 | **#91** | **#86** | 사람이 지시에 못 박은 등재(#90 ㉡을 자기 번호로 승격 — 「이것이 안 변했다」를 주장하는 **모든** 팔에 걸리므로 관문 하나의 사정으로 두지 않는다). 표를 「최근 다섯」으로 유지하며 **#86**을 뺐다 — **본문 절은 그대로**다. ⚠ 초판이 #87을 뺐다가 되돌렸다(1차 리뷰어 [16]): 관례는 **가장 오래된 것**을 빼는 것이고, #87은 #91의 **계열 부모**라 착수 시 읽는 표에서 사라지면 안 된다 |
| 2026-08-31 web2-39 §0㉡ | **#92** | **#87** | 사람이 지시에 못 박은 등재(「재는 양이 결과와 연결돼 있는지 물어라」). **#71·#87의 형제이고 #91과 짝**이다. 표를 「최근 다섯」으로 유지하며 #87을 뺐다 — **본문 절은 그대로**다(그 절이 #91의 부모다) |
| 2026-08-31 web2-40 | **#93** | **#88** | 사람 지시가 아니라 **이 회차가 자기 게이트에서 걸려** 나온 항목이다(`grain26.spec`이 dpr3에서만 멎었다). 표를 「최근 다섯」으로 유지하며 #88을 뺐다 — **본문 절은 그대로**다 |
| 2026-08-31 web2-40 | **#94** | **#89** | 같은 회차가 **실측으로** 잡았다(원장 관문이 playwright 런타임에서 통째로 새고 있었다). #90·#91의 자식이다. 표를 「최근 다섯」으로 유지하며 #89를 뺐다 — **본문 절은 그대로**다 |
| 2026-08-31 web2-40 후속 | **#95** | **#90** | 사람이 지시에 못 박은 등재(**#81 ㉡을 시험·산출물 대기까지 넓힌다**). 사람이 여섯 시간 도는 대기 넷을 보고 냈다. 표를 「최근 다섯」으로 유지하며 #90을 뺐다 — **본문 절은 그대로**다(#94가 그 자식이라 계보는 #95 → #94 → #90으로 이어진다) |

⚠⚠ **같은 회차가 등록부 구멍도 고쳤다**: 항목 제목이 `### #NN (…)` 형식이면
`selfcheck.py`의 `scan_pitfall_citations` 등록부 정규식(`### #NN.`)을 벗어나 **#76~#81이
통째로 «없는 번호»**로 잡히고 있었다(원장 다섯이 죽은 참조였다). 점 하나를 넣어 8건 → 3건
(남은 셋은 색 코드 `#101014`·`#2471` 오검). **새 항목은 반드시 `### #NN.` 형식으로 쓴다.**

### #84. (2026-08-30 · web2-34 3번) — **「견본이 1:1인가」를 속성으로 재면 통과한다 · 그리고 반증은 대역의 «양 끝»에서 재라 — 틀린 식도 한쪽 끝에서는 맞는다**

㉠ **속성 ≠ 화면.** 30-2가 세운 「견본 == 그은 선」은 **속성끼리** 견줬다(줄 안 rect의
`height` 속성 ↔ `app.nib`). 그 자에는 **CSS가 얹는 배수가 안 보인다** — 이 저장소에만도
`.trow`·`.nrow`의 `--ui-scale`(1.5)과 `.tool.on svg{scale(1.14)}`가 걸려 있어서,
같은 속성으로도 화면의 크기는 **1.5배·1.14배**가 된다. 34-3의 팔은 `getBoundingClientRect`로
**렌더 px**를 재고 **svg 배수 1.000**까지 따로 단언한다(실측 견본 지름 10.00 · 24.00 ·
54.00 · 120.00 px = 2r 그대로). ⚠ 그래서 크기 줄에는 `.tool`을 **안 붙였다** — 고른 줄만
1.14배가 되면 그 줄에서 1:1이 깨진다. 「선택 표시」는 크기가 아니라 옅은 바탕으로 준다.

㉡ **반증을 한 칸에서 하면 «반증이 안 된다»고 잘못 판정한다.** 옛 «비율만» 식
(`r = 4.5 + (v−4)/56×12`)을 되돌리면 큰 칸 셋은 실제의 **51.8% · 34.9% · 27.5%**로 빨개지는데
**가장 작은 칸은 0.57 px 차라 통과한다**(94.3% · 선폭 1 안). 대역의 한쪽 끝에서 틀린 식이
우연히 맞기 때문이다 — **반증도 대역 전체에서 돌린다**(D-5의 반증 판 · #12의 사촌).
같은 이유로 게이트는 **네 칸 전부**를 요구한다.

㉢ **끌개를 지우면 그 요소를 «자리»로 재던 팔과 원장이 같이 죽는다**(#75 ㉣의 사례 추가):
`#thick` 하나를 지우자 `ui30`(display 검사) · `sidebar.spec`(막대 자리 실측 + 원장 필드
`thick_right_x`) · `zones ⑥`(전체 화면 숨김 목록) · `icons ②`(무채색 제외 목록) 넷이 걸렸다.
답은 임계를 무는 것이 아니라 **그 팔이 무엇을 묻고 있었는지 다시 적는 것**이고
(「막대가 세로바 왼쪽인가」 → 「**펼침이** 세로바 왼쪽인가」), 원장은 **재실행**한다(#47).

### #85. (2026-08-30 · web2-34 4번) — **전역 캡처로 «접는» 규칙을 세우면 «접힌다»를 재는 팔은 삼키는 판에서도 초록이다 — 두 축을 한 칸에서 같이 재라**

R7(「열린 통은 바깥의 무엇을 눌러도 접힌다」)은 `window`의 `pointerdown`을 **캡처 단계**에서
듣는 규칙이다. 그런 규칙에는 **틀리는 방향이 둘**인데 팔은 대개 하나만 잰다:

```
① 안 접힌다        ← 「접혔나」를 재는 팔이 잡는다
② 접히지만 그 누름이 통째로 죽는다  ← 같은 팔이 **초록으로 통과한다**
```

②는 `stopPropagation`/`preventDefault`를 한 줄 넣으면 바로 나는 형태이고(#77 ㉠의
계열), 이 앱에서는 **캔버스의 획·탭이 전부 죽는다**. 실측(팔이 «틀린 판»을 실제로 만들어
낸 값 — `diag.boxAwayModeForTest`):

| 판 | 안 접힘 | 잴 수 있는 칸 중 삼켜짐 |
|---|---|---|
| 제자리 | 0/10 | 0/9 |
| `away` 없음 | **9**/10 | 0/9 |
| **삼키는 판** | **0**/10 | **8/8** |

**삼키는 판에서 「접힌다」는 열 칸 모두 초록이다.** 그러므로 전수 팔은 한 칸에서
**「접혔나」와 「그 누름의 제 일이 살았나」를 둘 다** 내야 한다(획 수·지운 수·도구·픽셀).

㉠ **바깥의 대역을 «단추»로만 잡지 마라.** 단추만 눌러 보면 ②가 안 보인다 — 죽는 것은
**캔버스**다. 팔을 단추(①)와 캔버스(②)로 가르는 것이 그 대역이다.

㉡ **예외는 «패널»이 아니라 «구간»에 걸릴 수 있다.** 지시가 준 대상 목록을 그대로 걸었더니
설정 서랍의 **필압 보정 절차**가 죽었다(그 절차의 문면이 「캔버스에 두 획을 그으세요」이고
다음 지시·그만두기가 **그 패널 안**이다 — 30-7이 옮겨 둔 것이 그것뿐이다). 답은 서랍을
통째로 빼는 것이 아니라 **그 구간에만** 예외를 거는 것이다(`pinned: () => app.pressCalib !== null`).
판정 문면은 하나다: **「바깥을 눌러야 그 패널의 일이 되는가」**(`#dimpanel`이 그 원본).

㉢ **여닫이 단추를 통의 «안»에 넣어라.** 안 넣으면 같은 단추를 다시 눌러 닫으려 할 때
`pointerdown`이 먼저 접고 `click`이 도로 여는 **깜빡임**이 된다. 그리고 **팔에서도**
여닫이라 「눌렀으니 열렸겠지」로 쓰면 앞 칸이 남긴 상태에 따라 그 누름이 «닫기»가 된다 —
**열렸는지를 보고 누른다**(이 회차의 ④가 실제로 그렇게 걸렸다).

㉣ **«전부에 걸었다»의 반례는 대개 저장소 안에 이미 있다.** 이 회차가 만든 것은 규약이
아니라 **이사**였다 — 같은 `away` 핸들러가 `paperbar`·`layerbar`·`notice` 셋에 복제돼
있었고 나머지 여덟 통에만 없었다(#54). 새 규칙을 세우기 전에 **`grep`으로 그 규약이 이미
몇 벌인지부터 센다.**

### #86. (2026-08-30 · web2-34 8번) — **파생값의 위약을 «값»으로만 재면 통과하는 칸이 있다 — 값 축과 «정체» 축을 따로 재라. 그리고 위약이 안 갈리는 축에서 돌린 팔은 아무것도 안 잰 것이다**

「잰 값은 저장하지 않는다 — 저장하는 것은 **어느 두 점을 재는가**다」(AS-C120)를 실측으로
세우려면 **좌표를 담은 위약 판**을 나란히 돌려야 한다(#83 ㉠). 그런데 그 위약을 재는
자가 둘이고 **둘이 서로 다른 것을 잰다**:

```
값 축   — 승격 뒤 위약이 내는 mm이 정체 판과 갈리는가
정체 축 — 위약이 담은 «그 점»이 아직 도면 위의 같은 점인가(작도 시점 화면 px)
```

실측(`measure34_web2.json` · 구도 넷 × 재기 셋 = 12칸 · P1→P2 승격):

| 축 | 갈린 칸 | 크기 |
|---|---|---|
| 값(mm의 fold) | **10/12** | 1.2516 ~ **4.9483**배 (mm 차 385 ~ **12127**) |
| 점(드리프트 px) | **12/12**(끝점 24/24) | **34.84 ~ 1005.47** px (문 `TAP_MAX_PX` 2) |

**값 축만 재는 팔은 두 칸에서 위약을 초록으로 통과시킨다** — 축척 기준 획과 «같은
화면평행 세로»만 지나는 재기는 길이 **비**가 f에 안 반응해 mm가 승격에 불변이다
(fold 정확히 1.000000). 그 칸에서도 점은 54 px·1005 px 어긋나 있다. #85가 「접힘」과
「제 일」을 갈랐던 것과 같은 형태이고, 여기서는 **「값이 맞나」와 「같은 점을 가리키나」**다.

㉠ ⚠⚠ **위약이 «안 갈리는 축»에서 돌린 팔은 아무것도 안 잰다.** web2-32는 같은 위약을
**축척 두 배**로 돌렸는데 거기서는 정체 판도 위약 판도 **정확히 두 배**가 된다(12/12 fold
1.000000 — 이 원장의 `scale_control`). 그래서 그 팔은 초록이었고 AS-C120은 **⏳ 논증**으로
남았다. 좌표 저장의 실패는 **재리프팅(카메라 재확정)에서만** 드러난다 — CLAUDE.md §1이
「카메라가 재조정되면 전부 처음부터 다시 올린다」이므로 위약을 재는 **축은 그 자리 하나**다.

㉡ **「같은 두 점을 가리킨다」를 «잰 값이 같다»로 확인하지 마라.** 값이 같은 것은 정체가
유지된 것의 **결과**이고, 그 함의는 한쪽으로만 간다. 직접 재는 것은 문서에 남은 **획 id·t**와,
승격 후 3D 점을 되짚어 나오는 **같은 획·같은 t**다(`identifyPoint`가 다른 획을 고를 수 있다 —
그것이 이 열의 반증 조건이다).

㉢ **지시가 «P2 → P3»이라고 적어도 저장소에 그 승격이 없으면 있는 것을 잰다**(D-4).
web2의 `fSource`는 `none | default | two-vp` 셋뿐이라 ~~실재하는 승격은 **P1→P2** 하나다~~
(f가 임의 게이지에서 f²=|u₁||u₂|로 확정되고 주점이 깊이 VP에서 W/2로 옮겨간다).
web2-13 2부가 같은 갈림을 이미 적었다 — **먼저 `grep`으로 그 상태 집합을 세고** 문면과
다르면 원장에 그 갈림을 남긴다.

⚠⚠ **「실재하는 승격은 P1→P2 하나다」는 web2-31 2번의 측정이 반증했다**(2026-08-31 ·
web2-31 마감 [6] — #42 ⑥의 형태로 지우지 않고 붙인다). 틀린 자리는 **`fSource`를 승격의
술어로 쓴 것**이다: 차수는 소실점 **개수**이고(이론서 2.3) `fSource`는 f의 출처일 뿐이라,
소실점이 **2 → 3**이 되는 자리에서 `fSource`도 `camSig`도 안 움직인다. 그래서
「fSource 집합이 셋뿐」을 세고 「승격이 하나뿐」이라고 결론지은 것이 **잘못된 셈**이다.

실측(`lens31_web2.json` · `gate5_promotion_resets.reachability.routes.third_vp`):
소실점 **2 → 3** · f `3286.335345` 불변 · `camSig` `two-vp|3286.335345` → 같은 값 ·
그런데 **차수가 올라 보기 렌즈가 버려진다**(`viewF` 2배 → null). 여섯 길 중 **그리기로
도달하는 유일한 승격**이 그것이다. 그리고 `CLAUDE.md` 지시 1(D-L53)이 **처음부터**
「남는 승격은 **P2 → P3** 하나다 · P1(가로선 확정)은 불가역이다」라고 적어 두었다 —
문면이 저장소보다 앞서 있었고 **측정이 문면 쪽을 가리켰다**.

**남는 규칙은 그대로다**: 문면이 이름 붙인 승격이 저장소에 없으면 **있는 것을 잰다**.
바뀐 것은 «무엇이 있는가»의 답이고, 그 답을 낸 것은 **상태 집합의 `grep`이 아니라
경로를 여섯 길로 실제로 걸어 본 것**이다(#35). 상태 이름을 세는 것으로 「승격이 없다」를
결론짓지 마라 — **승격의 술어를 먼저 확인하라**(여기서는 `fSource`가 아니라 소실점 개수).

### #88. (2026-08-31 · web2-31 저장 회차에서 등재 — 사례는 web2-34 6번 후속의 #87 ㉣) — **예약값을 상수로 두지 마라 · 예약하는 «대상»에서 유도하라**

**증상.** 위 띠에 서랍 둘을 넣어 폭이 112 → **156**으로 늘자(왼쪽 끝 918 → **874**)
종이 띠의 예약 `calc(100vw - 300px)`(= 900)이 모자라 **탭 띠가 위 띠를 파고들었다**
(`paperbar.spec`에서 「+」가 안 눌린다 — **전량 e2e가** 잡았다. 단위 팔은 못 잡는다).

**기제.** `300px`은 「위 띠가 그때 쓰던 폭」을 손으로 옮겨 적은 수다. 적는 순간
**옮긴 쪽과 말없이 묶인다** — 코드에 그 결합을 가리키는 것이 하나도 없으므로,
위 띠를 넓힌 사람은 자기가 종이 띠를 깨뜨렸다는 것을 **알 방법이 없다**.
이것은 #87 ㉣의 사례를 그 자체로 세운 것이고, #87(자리·순서)과는 다른 축이다:
**#87은 «누가 위에 오는가», #88은 «누가 얼마를 차지한다고 믿는가»**다.

**규칙.** 예약은 **예약하는 대상에서 유도한다.** 그 띠의 실제 폭을 변수로 내보내고
(`--topbar-reserve`) 예약하는 쪽이 그것을 읽는다. 그러면 옮긴 쪽이 변수를 갱신하고
읽는 쪽이 따라오므로 결합이 **코드에 보인다**.

**판별 물음** — 어떤 수를 상수로 적으려 할 때:
① 이 수는 **다른 요소의 현재 크기**인가(그 요소가 자라면 틀리는가)
② 그 요소를 키우는 사람이 **여기를 볼 이유가 있는가**
①이 예이고 ②가 아니오면 상수로 두면 안 된다 — 유도하거나, 최소한 변수로 뺀다.

⚠ **적용 범위는 CSS 밖으로도 간다.** 「몇 px 남는가」·「몇 개까지 들어가는가」·
「이 배열의 길이만큼 자리를 비운다」가 전부 같은 형태다. 34-6의 여유 계산
(25 → 105px)도 그 예다.

⚠⚠ **두 번째 사례군 — «팔이 다른 요소의 좌표·치수를 옮겨 적는 형태»**(2026-08-31 ·
web2-31이 낸 재발 둘). 등재 당시의 사례는 CSS 예약 하나였는데, 실제로 재발한 둘은
**제품이 아니라 팔**이 남의 수를 들고 있던 것이다:

| 자리 | 팔이 든 상수 | 실제 출처 | 고친 방법 |
|---|---|---|---|
| `e2e/flow.spec.ts` | 큐브 자리 **(1090, 60)** | `app.cubeLayout` | 대상에서 읽는다 |
| `e2e/ui34.spec.ts` | 각인 창 폭 **6.6** | 창 `rect`의 `getBBox().width` | 대상에서 읽는다 |
| `e2e/zones.spec.ts` | 펜 각인 창 폭 **8.8** | 창 `rect`의 `getBBox().width` | 대상에서 읽는다 |

팔이 든 상수는 **더 오래 안 걸린다**. 제품 쪽 상수는 화면이 깨지면 사람이 보지만,
팔의 상수는 «초록이므로 아무도 안 본다» — 창을 좁힌 사람이 팔에 안 걸리고, 팔은
**옛 폭에 대해** 계속 통과 도장을 찍는다. 판별 물음 ①②는 팔에도 그대로 걸린다.

⚠ **상수를 대상에서 유도하게 고치는 것은 «임계를 무는 것»이 아니다** — 문의 **값**이
아니라 **출처**를 바꾸는 것이고, 값이 그대로여도 이제는 대상이 움직이면 따라 움직인다.
그 수리의 반증은 **대상을 바꿔 보고 문이 따라오는지 보는 것**이다(D-3).

⚠⚠ **팔은 «넘쳤는가»가 아니라 «예약이 실제 폭과 같은가»를 재라.** 넘침만 재면
여유가 우연히 남는 화면 크기에서 초록이 된다(#71 — 재는 조건이 값의 절반이다).

### #88 확대 (2026-08-31 · web2-37 §2 — 사람 지시)

> 예약값·좌표를 상수로 박지 마라. **화면뿐 아니라 검사 팔에도 있다.**
> 팔이 상수를 들고 있으면, **화면을 옳게 고쳐도 팔이 빨개지고 사람은 화면을 의심한다.**

31-3이 `flow.spec`의 `(1090,60)`에서 겪은 것이 그 형태다 — **#88을 등재한 바로 그 라운드에서
팔 안에서 터졌다.** 규칙을 세운 회차가 그 규칙을 자기 팔에서 어긴 것이므로, 판별 물음을
**팔에도 그대로** 건다: ① 이 수가 **다른 것의 현재 값**인가 ② 그 다른 것을 바꾸는 사람이
**여기를 볼 이유가 있는가** — ①예 ②아니오면 상수 ⛔, **대상에서 읽어라**.

⚠ **팔 안의 상수가 더 오래 안 걸린다** — 초록이라 아무도 안 본다. 그리고 걸리는 날에는
**엉뚱한 곳을 의심하게 만든다**(「화면이 깨졌나?」). 그것이 화면의 상수보다 나쁜 점이다.

_이 회차(web2-37)의 사례 셋 — 전부 대상에서 읽게 고쳤다: `turn31`이 획 수를 **돌리기 전**에
세던 것(37-4가 대기 획을 버리므로 세는 자리가 «돌린 뒤»다) · `orbitradius`가 걷을 op 수를
손으로 적는 대신 **스택 깊이에서 읽는** 것 · `xint37`의 축 방향 끝점을 좌표로 적지 않고
**소실점에서 유도**하는 것._

### #89. (2026-08-31 · web2-37 §2 — 사람 지시) — **초록의 범위를 적어라**

> **「초록」은 「무엇이 초록인가」 없이는 정보가 아니다.**
> CI가 도는 것과 사람이 도는 것이 다르면, **배포 판정은 CI가 도는 것만 보증한다.**

사례: `pages.yml`이 도는 e2e는 `static_deploy.spec.ts` **하나뿐**인데(typecheck·`npm test`·build는
돈다) 여러 회차가 「Pages 초록 = 배포 확인」으로 읽었다. 그 사이 **`ui34 ④`가 34-3 이래
빨간 채로 배포돼 왔고 아무 회차도 못 봤다** — 전량을 사람이 손으로 돌린 회차(web2-31 마감)에서야
드러났다.

- **마감 보고에 「무엇이 초록인가」를 적는다**: CI가 돈 것(타입·단위·빌드·`static_deploy`)과
  사람이 돈 것(전량 e2e · dpr1+dpr2 · 몇 분)이 **다른 줄**이어야 한다.
- 「전량은 사람이 도는 세션에서만 돈다」가 사실이면 그 사실이 **보고에 있어야** 한다.
  없으면 다음 회차가 또 초록을 전량으로 읽는다.
- ⚠ 이 조항은 #58(배포까지가 한 항목이다)의 **짝**이다: #58은 「저장소 안의 초록이 배포가
  아니다」이고 #89는 「그 초록이 무엇의 초록인지 적어라」다.
- ⚠⚠ **같은 형태가 원장에도 있다**(#90) — 「원장이 갱신됐다」도 「무엇이 그것을 썼는가」
  없이는 정보가 아니다.

### #90. (2026-08-31 · web2-37 §1 — 사람 지시) — **규칙을 파일마다 적으면 새 파일이 그것을 안 적는 것이 기본값이 된다 · 그리고 «덮어썼다»는 해시로 안 잡힌다**

원장은 `LEDGER=1` 단독 실행에서만 쓰기로 web2-22가 정했고, 그 뒤 하네스마다
`if (process.env.LEDGER === '1')`을 **손으로 옮겨 적어** 왔다. 결과(2026-08-31 실측):

```
npm test (LEDGER 없음)        → 원장 24개를 덮어썼다
web2-31 전량 e2e (두 실행 모두) → 원장 아홉을 덮어썼다
```

㉠ **문면이 흩어져 있으면 «안 옮겨 적는 것»이 기본값이다.** 새 하네스를 쓰는 사람은 그
문면을 볼 이유가 없다 — 자기 파일에는 아직 없기 때문이다. 답은 **관문을 한 자리에 두는
것**이다(`web2/tools/ledgercore.ts`): 그러면 새 하네스는 아무것도 안 해도 막힌다.
같은 길을 `web/tools/testSplit.mjs`가 이미 걸었다(「새 하네스는 자동으로 분류되고 목록을
손으로 안 고친다」).

㉡ ⚠⚠ **해시 대조로는 이 결함이 안 잡힌다.** 덮어쓴 24개의 sha256이 **전수 같았다** —
하네스가 결정론적이라 같은 내용이 다시 쓰였을 뿐이다. 규칙은 「같은 값을 쓰라」가 아니라
**「쓰지 마라」**이므로 판정자는 **mtime**과 **막은 횟수**다. 「해시가 같으니 안 썼다」는
**추론이 한 단계 빠진 문장**이다(#5의 형태 — 보장을 측정으로 읽는 것).

㉢ **런타임이 둘이면 배선도 둘이다.** vitest에서는 원숭이 패치가 **안 듣는다**(`node:fs`의
ESM 파사드가 이미 만들어진 뒤라 CJS exports를 갈아 끼워도 이름 가져오기가 옛 것을 본다 —
실측 `named same as cjs? false`). 그쪽 답은 `test.alias`이고, playwright는 스펙이 babel로
CJS가 되므로 패치가 듣는다. **한 배선으로 둘을 덮었다고 믿지 마라 — 각각 재라.**

㉣ 반증 조건: 관문을 떼면 `selfcheck.py`의 `scan_ledger_guard`가 빨개진다(실제로 떼서 확인).

### #91. (2026-08-31 · web2-41 §3 — 사람 지시) — **불변을 «해시»로 재면 «같은 값으로 덮어쓰기»를 못 잡는다**

> 「안 바뀌었는가」와 「안 썼는가」는 **다른 물음**이다.

#87 계열(**재는 대상이 틀렸다**)에 붙는다. #90 ㉡이 이 사실을 발견한 자리이고, 여기 **규칙으로
따로 세우는** 이유는 그것이 원장 관문 하나의 사정이 아니기 때문이다 — 「이것이 안 변했다」를
주장하는 **모든** 팔에 걸린다.

37 회차 §1의 원장 관문은 처음에 게이트가 **「해시가 같으면 통과」**였다. 그런데 `npm test`가
원장 **24개**를 덮어쓰고 있었고 그 24개의 sha256이 **전수 같았다** — 하네스가 결정론이라
같은 내용이 다시 쓰였을 뿐이다. 즉 **규칙이 깨진 채로 게이트가 초록이었다.**

- **규칙이 「쓰지 마라」이면 판정자는 «쓰기 사건»이어야 한다** — `mtime`(그리고 관문이
  스스로 센 **막은 횟수**). 내용은 결과이지 사건이 아니다.
- 판별 물음 둘: ① 내가 금지한 것이 **상태**인가 **행위**인가 ② 그 행위가 **상태를 안 바꾸고도**
  일어날 수 있는가 — ①행위 ②예 이면 해시·값 대조 ⛔.
- 같은 형태가 어디에 또 있나: 「저장 안 했다」를 파일 내용으로 · 「네트워크 안 탔다」를 응답
  내용으로 · 「그리지 않았다」를 픽셀로 · 「재계산 안 했다」를 결과값으로. 전부 **결정론이면
  통과한다**.
- ⚠ 뒤집으면 「해시가 다르다」는 여전히 쓸 만하다 — **한쪽 방향만 정보를 준다**(#5의 형태:
  보장을 측정으로 읽지 마라). 그 비대칭을 팔의 문면에 적는다.
- 반증 조건: 관문을 떼면 `selfcheck.py`의 `scan_ledger_guard`가 빨개진다(#90 ㉣ — 실제로 떼서 확인).

⚠ **이 항목을 등재하며 #89·#90·#91의 제목 줄을 `##` → `###`로 고쳤다.** `selfcheck.py`의
`scan_pitfall_citations`가 등록부로 읽는 형식이 `N. **제목**` 또는 `### #N.` 둘뿐이라,
web2-37이 `##`로 적은 #89·#90이 **조용히 «없는 번호»였다**(그 둘을 인용하는 원장이 없어서
안 걸리다가, 이 항목이 인용하자 죽은 참조로 떴다). #38의 문면대로 **고칠 곳은 검사가 아니라
기록하는 쪽**이다 — 정규식을 넓히지 않았다.

### #92. (2026-08-31 · web2-39 §0㉡ — 사람 지시) — **재는 양이 결과와 연결돼 있는지 물어라**

> 「A를 골랐는가 B를 골랐는가」가 **결과를 바꾸지 않으면** 그 오답률은 0%든 100%든 뜻이 없다.
> **차이를 세기 전에 그 차이가 무엇을 바꾸는지를 먼저 재라.**

#71(**재는 조건**이 틀렸다) · #87(**재는 대상**이 틀렸다)의 형제이고 **#91과 짝**이다
(#91은 「안 바뀌었는가」 ≠ 「안 썼는가」 — 이 항목은 「달랐는가」 ≠ 「달라졌는가」).

web2-37이 붐비는 장면에서 **「고른 교차가 의도한 것을 빗나감 13/16 = 0.8125」**를 쟀고,
web2-41 지시문이 그것을 「무작위(0.733)에 붙어 나왔다」로 읽어 규칙을 통째로 바꿨다.
web2-41이 실제로 재 보니 **그 픽스처의 세로 여덟은 밑이 한 지면선 위**라 어느 후보를 골라도
**같은 3D 선**이 나온다 — 잘못 놓인 선 **0/16**. 즉 **0.8125는 결과가 같은 선택들 사이의
차이**였고, 그 수로는 규칙의 좋고 나쁨을 못 가른다(AS-C140 · `xint41_web2.json`의 `crowded_flat`).
41 지시문이 근거로 덧붙인 「후보 선들이 평행이고 측면 오프셋만 다른데 손떨림이 그보다 크다」도
같은 자리에서 틀렸다 — 0.58은 **선들이 벌어진 폭**이 아니라 **교차점들이 한 선 위에서 떨어진
거리**였고, 축이 고정된 채 점을 그 선 위로 미끄러뜨려도 선은 안 바뀐다.

- **판별 물음 둘**: ① 내가 세는 것이 **선택의 이름표**인가 **결과의 자리**인가
  ② 이름표를 바꿨을 때 **자리가 실제로 움직이는가**(같은 픽스처에서 값으로 재라).
  ①이름표 ②아니오면 그 비율은 **아무것도 안 잰다**.
- **먼저 재는 값이 있다** — 「이 픽스처에서 두 답이 우연히 같은가」. 41이 쓴 자가
  `candSpread / geomSizeOf`(결과 선들의 벌어짐 ÷ 장면 크기)다. 그것이 0이면 이름표
  오답률은 **버린다**.
- **규칙이 맞아도 근거는 틀릴 수 있다** — 41-1·41-2의 코드는 그대로 서 있다(깊이가
  실제로 갈리는 판에서 37의 규칙이 15/15를 잘못 놓는다). 고친 것은 **문서뿐**이다.
  ⚠ 그러므로 이 항목은 「그 회차를 되돌려라」가 아니라 **「근거를 결과 축에서 다시
  대라」**다(#5의 형태: 보장을 측정으로 읽지 마라).
- 같은 형태가 어디에 또 있나: 「어느 스냅이 이겼는가」를 세면서 붙은 **점**은 같은 자리 ·
  「어느 축으로 분류됐는가」를 세면서 놓인 **선**은 같음 · 「어느 후보가 1등인가」를 세면서
  1·2등의 **출력이 같음**. 전부 **모집단을 좁히면(정체가 갈리는 칸만) 되살아난다**.
- **반증 조건**: 이 규칙이 아무것도 안 막는 조건은 「이름표 축과 자리 축이 늘 같이 움직이는
  픽스처」다 — 41이 그 판을 **따로 지어** 둘이 갈리는 것을 실증했다(붐빔② · 벌어짐
  0.349~0.583 · 잘못 놓임 15/15). **판을 따로 안 지으면 이 물음 자체가 안 선다**(#86의 규율).

### #93. (2026-08-31 · web2-40 — 이 회차가 자기 게이트에서 걸렸다) — **e2e의 몸짓은 «의도»가 아니라 «시간»으로 읽힌다 — 기기가 느려지면 같은 코드가 다른 몸짓이 된다**

> 팔이 적은 것은 「선을 긋는다」인데 브라우저가 받는 것은 **누르고 있던 시간**이다.
> 제품에 «누르고 있으면 다른 뜻»인 진입이 하나라도 생기면, **느린 기기에서 그 팔은 다른
> 것을 시험한다.** 그리고 그것은 **그 기기에서만** 빨개지므로 회귀로 안 읽힌다.

web2-40이 `grain26.spec`을 돌리자 **dpr3에서만** 멎었다. 표식을 먼저 심었더니(D-1) 화면
알림이 **「글씨 — 이 선의 치수를 쓴다」**였다 — 두 번째 작도 획이 통째로 사라지고
`#layer-add`가 「소실점 작도가 끝나야 얹을 수 있다」로 남아 `page.click`이 영원히 기다렸다.

- **기제**: web2-39가 「선을 **꾹 눌러** 글씨를 시작한다」(`writeHoldMs` 450 ms)를 넣었다.
  그 팔의 둘째 획은 시작점이 첫 획 **위**라 `writeTargetAt`이 그것을 잡는다. dpr3에서는
  3600×2400 캔버스라 `mouse.down` 다음 첫 `mouse.move`까지 **337 ms**가 걸렸고(실측),
  프레임이 느려질수록 그 값이 문턱에 붙는다. **제품 결함이 아니다** — 느린 기기에서
  「누르고 끌기」가 실제로 그렇게 보인다. 결함은 **픽스처가 그 진입을 밟고 있던 것**이다.
- **판별 물음 둘**: ① 이 몸짓에 **머무름·누름을 세는 문**이 걸리는가
  ② 그 문의 시간이 **한 프레임 몇 개**인가(느린 칸에서 프레임 시간을 실제로 재라).
  ①예 ②「두어 개」면 그 팔은 기기 속도에 묶여 있다.
- **고치는 자리는 팔이지 문턱이 아니다** — 시작점을 그 문이 잡는 반경(여기서는
  `osnap.radius × 2` ≈ 16 px) **밖**으로 옮겼다(60 px). 문턱을 낮추면 제품이 갈린다.
- ⚠ **dpr마다 돌려 봐야 보인다**(D-C3의 시간 판). dpr1·dpr2는 멀쩡했다 — dpr3만 느렸다.
- **반증 조건**: 시작점을 도로 선 위에 두면 dpr3에서 **다시** 그 알림이 뜬다(실제로 그렇게 났다).

### #94. (2026-08-31 · web2-40 — 이 회차가 실측으로 잡았다) — **관문의 «문면»과 «판정부»를 검사하면서 «그 런타임에서 실제로 막히는가»는 아무도 안 물었다**

> 금지를 **행위**로 걸었으면(#91) 검사도 **그 행위가 그 런타임에서 일어나는가**를 봐야 한다.
> 배선 줄이 있는가(문면) · 판정 함수가 옳은가(단위)는 **둘 다 통과하면서** 샐 수 있다.

web2-37이 세운 원장 쓰기 관문(#90)에 검사가 둘 있었다 — `selfcheck.py`의
`scan_ledger_guard`(배선 **문면**)와 `test/ledgergate.test.ts`(**vitest 런타임**의 판정부).
web2-40이 재 보니 **playwright 런타임에서는 통째로 새고 있었다**:

```
LEDGER 없이 grain40.spec 실행 → 원장 mtime 1788172101 → 1788172242   (썼다)
스펙 안 탐침: `named === cjs?` false · `ledgerBlocked.length` 0        (안 막았다)
```

- **기제**: playwright 배선은 「스펙이 babel로 CJS가 되어 `fs.writeFileSync`를 **속성
  접근**으로 부른다」를 전제로 CJS exports를 갈아 끼운다. **playwright 1.62에서 그 전제가
  거짓이다** — 스펙이 ESM으로 돌고 `import { writeFileSync } from 'node:fs'`는 **이미
  만들어진 ESM 파사드의 스냅샷**을 본다. 즉 **도구가 올라가면서 관문이 조용히 죽었다.**
- ⚠⚠ **앞 회차가 못 본 까닭이 #91 그대로다**: 판정을 `git status stage0/out`(파일 **내용**)으로
  했는데 하네스가 결정론이라 **같은 내용이 다시 쓰였다**. #91을 세운 바로 다음 회차가
  그 함정에 걸렸다 — 규칙을 적는 것과 **판정자를 바꾸는 것**은 다른 일이다.
- **고치는 방향은 «패치»가 아니라 «부르는 자리»다**: 원숭이 패치는 import 방식·번들러·
  런타임에 묶인다. `tools/ledgerfs.ts`가 **호출 시점에** 판정하고 스펙은 `node:fs` 대신
  그것을 가져온다(바뀌는 것은 import 줄 하나). 그러면 새 스펙이 «안 적는 것»이 기본값이
  되는 #90 ㉠도 그대로 막힌다.
- **판별 물음 둘**: ① 이 검사가 보는 것이 **문면·판정부**인가 **행위**인가
  ② 그 행위를 **그 런타임에서** 실제로 일으켜 봤는가. ①문면 ②아니오면 그 검사는
  **도구가 바뀌는 날 조용히 죽는다**.
- **반증 조건**: `e2e/ledgergate.spec.ts`가 매 실행 그것을 낸다 — LEDGER 없이는 탐침 파일이
  **안 생기고** 기존 원장의 **mtime이 안 움직이며**(판정자는 mtime이다 — #91),
  `LEDGER=1`에서는 **써진다**(늘 막으면 원장을 갱신할 길이 없다는 뜻이라 그것도 결함이다).

### #95. (2026-08-31 · web2-40 후속 — 사람이 여섯 시간 도는 대기 넷을 보고 지시했다) — **#81 ㉡을 시험·산출물 대기까지 넓힌다: 기다리는 쪽과 일하는 쪽은 별개다 — 일이 죽으면 기다림은 영원히 산다**

> **상한 없는 대기를 만들지 마라. 배포뿐 아니라 시험·산출물 대기도 같다.**
> 모든 대기는 셋을 함께 가진다: **(1) 상한 · (2) 상한에 걸렸을 때 무엇을 볼지 ·
> (3) 기다리는 대상이 아직 살아 있는지.**

⛳ **이 규칙을 두 세션이 각각 적었다** — 다른 세션이 사람 지시로 **#81 확대**(위 「#81 확대」
절)에 같은 것을 적었고 사례 둘을 더 든다: **배포 대기 12시간 51분**(비교 대상인 내 HEAD가
낡아 조건이 영원히 거짓 — 위 (3)의 «살아 있는가»가 **비교 대상**에도 걸리는 사례) ·
**개발 서버 14시간 37분**(#70과 만난다). **번호는 여기 #95 하나이고** 그 절은 근거 쪽이다.
**사례는 합쳐서 여섯**(여기 넷 + 그쪽 둘)이다.
⛳⛳ 그 세션이 **셀프체크도 붙였다** — `selfcheck.py`의 `scan_unbounded_wait`:
① 가장 최근 마감 블록이 **「도는 백그라운드 없음」**을 값으로 적었는가(**세션이 끝날 때 도는
대기가 있으면 그 자체가 결함이다**) ② 저장소 도구에 상한 없는 대기 고리가 있는가.
⚠ **세션 안에서 즉석으로 만든 대기는 저장소에 없어 안 보인다** — 그것을 지키는 것은 검사가
아니라 마감 보고의 그 한 줄이다.

#81 ㉡이 그 규칙을 **배포 대기**에 세웠다. 이 회차가 **같은 결함을 시험 대기에서** 냈다 —
web2-40이 배포까지 끝난 뒤에도 대기 넷이 **최장 6시간 24분** 돌고 있었다. 넷이 기다리던
일은 **전부 몇 분 만에 끝나 있었다.** 기제가 둘이고 **둘 다 (3)의 실패**다:

**㉠ `pgrep -f <패턴>`이 «자기 자신»을 센다.** 대기 셸의 명령줄에 그 패턴이 그대로 들어 있으므로
`until ! pgrep -f "playwright test e2e/slide40"`는 **자기를 찾아** 조건이 영원히 거짓이다.
실측(죽이기 직전):

```
playwright test e2e/slide40 -> 2260 6394      ← 2260이 그 대기 자신 · 6394는 «진단하는 나»
playwright test e2e/turn31  -> 3289 6394
playwright test e2e/grain26 -> 31201 6394
```

실제 playwright 프로세스는 **0개**였다. 「일이 살아 있는가」를 묻는 검사가 **묻는 행위 자체**를
일로 세고 있었다 — #94의 형태다(검사가 보는 것이 **행위**여야 하는데 **문자열**을 봤다).
→ 고치는 법: `pgrep -f`로 남의 일을 세지 마라. **PID를 붙들거나**(`$!` · `wait`),
   패턴을 자기 명령줄에 안 남는 것으로 잡거나, `pgrep -f` 결과에서 **자기 PID(`$$`)를 뺀다**.

**㉡ 파수(sentinel)를 일하는 쪽이 안 쓴다.** `until grep -q "^EXIT=" /tmp/g26c.log`로 기다렸는데
일하는 쪽은 `... > /tmp/g26c.log 2>&1; echo EXIT=$?`였다 — **`echo`가 표준출력으로 갔고 로그
파일에는 안 들어갔다.** 파일은 멀쩡히 완성돼 있었고(`2 passed (2.2m)`) 파수만 없었다.
→ 고치는 법: 파수를 **같은 리다이렉션 안에서** 쓴다(`; echo EXIT=$? >> 로그`). 그리고
   **파수가 없어도 끝을 알 수 있는 둘째 조건**을 함께 건다(상한 · 프로세스 종료).

**판별 물음 셋** (대기를 적기 전에 답한다):
① 이 조건이 **거짓으로 굳는 경우**가 있는가 — 그 경우 무엇이 그것을 알려 주는가
② 조건이 세는 것이 **일하는 쪽의 상태**인가, 아니면 **내가 만든 문자열**인가
③ 상한에 걸렸을 때 **무엇을 볼지**가 그 자리에 적혀 있는가

**형태 규칙**: `until [ -s <파일> ]`·`until ! pgrep …` 같은 **무한 루프를 새로 만들지 않는다.**
써야 하면 **반복 횟수 상한**과, 상한에 걸렸을 때 **그 일의 상태를 찍는 절**을 같이 넣는다:

```sh
for i in $(seq 1 60); do        # 상한 = 60 × 10s = 10분
  grep -q "^EXIT=" "$LOG" && break
  sleep 10
done
grep -q "^EXIT=" "$LOG" || {    # 상한에 걸렸다 — «무엇을 볼지»가 여기 있다
  echo "상한 초과 — 그 일의 상태:"; ps -o pid=,etime=,cmd= -p "$JOB_PID" 2>/dev/null || echo "  그 일은 이미 없다"
  tail -5 "$LOG"
}
```

⚠ **대가는 시간이 아니라 «판단»이다** — 이 회차에서 그 넷이 붙들고 있던 결과는 **전부 다른
길(작업 알림)로 이미 도착했고** 회차의 결론에 기댄 곳이 없다. 그러나 대기가 살아 있는 동안
사람은 「아직 도는 일이 있다」고 읽는다. **끝난 일을 끝났다고 못 말하는 것**이 이 결함의 값이다.
⚠⚠ 짝: **#81 ㉡은 그대로 살아 있다**(배포 대기의 상한과 볼 목록). 이 항목은 그 규칙을
**시험·산출물 대기**로 넓히고 **(3) «대상이 살아 있는가»를 셋째 항으로 더한 것**이다 —
#81이 다룬 「배포는 죽었는데 화면만 본다」가 여기서는 「일은 죽었는데 패턴만 본다」로 나온다.

## ⚡ 항목 착수 전에 이것부터 읽는다 — **최근 다섯**

⚠⚠ **이 절은 파일의 마지막이다.** 그 자리에 둔 이유가 17차 후속의 사례다 —
이 파일이 계속 길어지므로 **`tail`로 읽는 것이 자연스러운 유인**이 있고, 실제로 17차가
`tail -80`으로 읽어 **상단에 있던 이 표를 지나쳤다**(그 80줄이 우연히 #49~#53이라 결과만
같았다 — PITFALLS #55). 유인을 이기려 하지 말고 **읽는 자리에 표를 둔다**(A-3: 단순한 쪽).

```
tail -40 PITFALLS.md     # 착수 시 이 표를 읽는 명령
```

📐 **표를 읽고 나면 `CLAUDE.md` §2의 「D. 진단 규약」 다섯을 그 항목에 적용한다**
(2026-08-21 · web2-03 지시 0). 이 표는 «과거에 무엇을 틀렸나»이고 D는 «지금 어떻게
진단하나»다 — 둘이 짝이다. D는 CLAUDE.md의 **착수 명령 바로 아래**에 있다:
① 표식 먼저(후보부터 보지 않는다) ② 재현 먼저(못 하면 안 고친다) ③ 새 검사에는 반증 조건
④ 사람이 준 근거도 확인 대상 ⑤ 픽스처가 실사용 대역을 덮는가.

⚠ **새 항목은 이 절 «앞»에 잇는다** — 뒤에 이으면 표가 다시 위로 밀려 같은 결함이 난다.
`python selfcheck.py`의 `scan_pitfalls_table_last`가 그 자리를 지킨다(사람이 안 세도 걸린다).

| # | 한 줄 |
|---|---|
| **#95** | **상한 없는 대기를 만들지 마라 — 배포뿐 아니라 시험·산출물 대기도 같다. 기다리는 쪽과 일하는 쪽은 별개이고, 일이 죽으면 기다림은 영원히 산다**(web2-40 후속 · **#81 ㉡의 확장**) — web2-40이 배포까지 끝난 뒤에도 대기 넷이 **최장 6시간 24분** 돌고 있었고 그 넷이 기다리던 일은 **전부 몇 분 만에 끝나 있었다**. 기제 둘, 둘 다 「대상이 살아 있는가」의 실패다: ㉠ **`pgrep -f <패턴>`이 자기 자신을 센다** — 대기 셸의 명령줄에 그 패턴이 들어 있어 `until ! pgrep -f "playwright test e2e/slide40"`가 **자기를 찾는다**(실측: 그 패턴에 걸린 PID가 대기 자신과 진단하는 셸뿐 · 실제 playwright는 0개) ㉡ **파수를 일하는 쪽이 안 쓴다** — `echo EXIT=$?`가 표준출력으로 가고 로그에는 안 들어가 `until grep -q "^EXIT="`가 영원히 거짓(파일은 멀쩡히 완성돼 있었다). **모든 대기는 셋을 함께 가진다: (1) 상한 (2) 상한에 걸렸을 때 무엇을 볼지 (3) 대상이 아직 살아 있는지.** 형태 규칙: `until [ -s <파일> ]`·`until ! pgrep …` **무한 루프 ⛔** — 쓰려면 **반복 횟수 상한**과 **상한에서 그 일의 상태를 찍는 절**을 같이 넣는다. ⚠ 대가는 시간이 아니라 **판단**이다: 끝난 일을 끝났다고 못 말하게 된다 |
| **#94** | **관문의 «문면»과 «판정부»를 검사하면서 «그 런타임에서 실제로 막히는가»는 아무도 안 물었다 — 금지가 «행위»면 검사도 그 행위를 그 런타임에서 일으켜 봐야 한다**(web2-40 · #90·#91의 자식) — web2-37의 원장 쓰기 관문에 검사가 둘 있었는데(`scan_ledger_guard` = 배선 **문면** · `test/ledgergate.test.ts` = **vitest** 판정부) **playwright 런타임에서는 통째로 새고 있었다**: LEDGER 없이 `grain40.spec`을 돌리자 원장 mtime이 **1788172101 → 1788172242**로 움직였고 스펙 안 탐침이 `named === cjs?` **false** · 막은 횟수 **0**을 냈다. 기제는 **도구가 올라간 것**이다 — 관문은 「스펙이 CJS라 속성 접근으로 부른다」를 전제로 CJS exports를 갈아 끼우는데 playwright 1.62의 스펙은 ESM이라 `import { writeFileSync }`가 **이미 만들어진 파사드의 스냅샷**을 본다. ⚠⚠ **앞 회차가 못 본 까닭이 #91 그대로다**: 판정을 `git status`(**내용**)로 했는데 결정론 하네스가 **같은 값을 다시 썼다** — #91을 세운 다음 회차가 그 함정에 걸렸다. 고치는 방향은 패치가 아니라 **부르는 자리**(`tools/ledgerfs.ts`가 호출 시점에 판정 · 스펙은 import 줄 하나만 바꾼다). 판별: ① 검사가 보는 것이 **문면·판정부**인가 **행위**인가 ② 그 행위를 **그 런타임에서** 일으켜 봤는가 — ①문면 ②아니오면 그 검사는 **도구가 바뀌는 날 조용히 죽는다** |
| **#93** | **e2e의 몸짓은 «의도»가 아니라 «시간»으로 읽힌다 — 기기가 느려지면 같은 코드가 다른 몸짓이 된다**(web2-40 · D-C3의 시간 판) — `grain26.spec`이 **dpr3에서만** 멎었다. 표식을 먼저 심으니(D-1) 화면 알림이 **「글씨 — 이 선의 치수를 쓴다」**였다: web2-39의 누름 진입(`writeHoldMs` 450 ms)에 팔의 둘째 획이 걸린 것이다(시작점이 첫 획 **위**라 `writeTargetAt`이 잡는다 · dpr3에서 3600×2400 캔버스라 `mouse.down` 다음 첫 `mouse.move`까지 **337 ms**). **제품 결함이 아니다** — 느린 기기에서 「누르고 끌기」가 실제로 그렇게 보인다. 결함은 **픽스처가 그 진입을 밟고 있던 것**이고 고치는 자리는 **팔이지 문턱이 아니다**(시작점을 잡히는 반경 16 px 밖 60 px로). 판별: ① 이 몸짓에 **머무름·누름을 세는 문**이 걸리는가 ② 그 문의 시간이 **한 프레임 몇 개**인가 — ①예 ②「두어 개」면 그 팔은 기기 속도에 묶여 있다 |
| **#92** | **재는 양이 결과와 연결돼 있는지 물어라 — 「A를 골랐는가 B를 골랐는가」가 결과를 안 바꾸면 그 오답률은 0%든 100%든 뜻이 없다**(web2-39 §0㉡ · #71 «재는 조건» · #87 «재는 대상»의 형제 · **#91과 짝**) — 37이 붐비는 장면에서 「고른 교차가 의도한 것을 빗나감 **13/16**」을 쟀고 41 지시문이 그것을 「무작위 0.733에 붙었다」로 읽어 규칙을 통째로 바꿨다. 41이 재 보니 그 픽스처의 세로 여덟은 밑이 **한 지면선 위**라 어느 후보를 골라도 **같은 3D 선**이 나온다 — 잘못 놓인 선 **0/16**(AS-C140). 즉 그 비는 **결과가 같은 선택들 사이의 차이**였다. 41이 근거로 덧붙인 「후보 선들이 **평행**이고 측면 오프셋만 다른데 손떨림이 그보다 크다」도 같은 자리에서 틀렸다 — 0.58은 **선들이 벌어진 폭**이 아니라 **교차점들이 한 선 위에서 떨어진 거리**다. 판별: ① 세는 것이 **선택의 이름표**인가 **결과의 자리**인가 ② 이름표를 바꾸면 **자리가 움직이는가** — ①이름표 ②아니오면 그 비는 아무것도 안 잰다. **차이를 세기 전에 그 차이가 무엇을 바꾸는지를 먼저 재라**(41의 자는 `candSpread / geomSizeOf`). ⚠ **규칙이 맞아도 근거는 틀릴 수 있다** — 41-1·41-2의 코드는 그대로 서 있고 고친 것은 문서뿐이다 |
| **#91** | **불변을 «해시»로 재면 「같은 값으로 덮어쓰기」를 못 잡는다 — 「안 바뀌었는가」와 「안 썼는가」는 다른 물음이다**(web2-41 §3 · #87 계열 «재는 대상이 틀렸다») — 37 §1의 원장 관문 게이트가 「해시가 같으면 통과」였는데, 덮어쓴 원장 **24개의 sha256이 전수 같았다**(결정론 하네스라 같은 내용이 다시 쓰였다). **규칙이 깨진 채로 초록이었다.** 규칙이 「쓰지 마라」이면 판정자는 **쓰기 사건**(`mtime` · 관문이 센 «막은 횟수»)이다. 판별: ① 금지한 것이 **상태**인가 **행위**인가 ② 그 행위가 **상태를 안 바꾸고도** 일어나는가 — ①행위 ②예면 해시 대조 ⛔. 같은 형태: 「저장 안 했다」를 파일 내용으로 · 「안 그렸다」를 픽셀로 · 「재계산 안 했다」를 결과값으로. ⚠ 비대칭이다 — 「해시가 **다르다**」는 여전히 정보다(#5) |
| 📌 **#42** | **상시 행이다**(밀려나지 않는다 — 위 정정). **착수 표의 "해당 없음"은 그 번호의 본문을 다시 읽고 나서만 적고, 완료 시 자기 원장이 인용한 번호와 대조한다.** ⑥ **원장을 재실행했으면 그 원장을 인용한 문서를 전부 찾아 값부터 다시 읽는다** · ⑦ 완료 대조는 `selfcheck.json`의 `pitfall_citations` · ⑧ 전부 재실행한 항목은 합집합을 착수 표로 쓰지 않는다 · ⑨ 한 원장의 낡음을 발견하면 그 커밋이 건드린 원장 전부를 훑는다 · ⑩ **새 규칙을 세운 항목은 그 규칙을 자기 다음 항목 착수 표에 옮겨 적는다** · ⑪ 공집합도 위반이다. **조항 전문은 본문에서 읽는다** |

_(#76·#77·#79·#80·**#81**·#82·#83·#84·#85·**#86**·**#87**·**#88**·**#89**·**#90**은 본문에 그대로 있다 — 「최근 **다섯**」을 지키려고 #82를 더하면서 #76을, #83을 더하면서 #77을, #84를 더하면서 #79를, #85를 더하면서 #80을, #86을 더하면서 #81을, #87을 더하면서 #82를, #88을 더하면서 #83을, #89를 더하면서 #84를 · #90을 더하면서 #85를, #91을 더하면서 #86을, #92를 더하면서 #87을, #93을 더하면서 #88을 · #94를 더하면서 #89를 · **#95를 더하면서 #90을** 표에서 뺐다. 2차 리뷰어의 낮음 ① · web2-35 병합 · web2-34 3번·4번·8번·6번 후속 · web2-31 저장 회차 · web2-37 §1·§2 · web2-41 §3 · web2-39 §0㉡ · web2-40(둘) · **web2-40 후속**.)_

⚠ **#79(R5 — 한 모서리는 한 가지만)는 이 회차가 계속 쓰고 있다**: 표에서 빠졌을 뿐이고
34-6이 그 항목 자체를 다룬다. 표는 «최근»이지 «중요»가 아니다.

**자동**으로 표시된 것은 `python selfcheck.py`가 **일부만** 잡는다.
⚠ **항목을 통째로 읽기 대상에서 빼지 않는다**(리뷰어 지적) — 아래 **안 덮는 범위**는
여전히 사람이 본다. 자동은 "그 줄만 안 봐도 된다"는 뜻이지 "그 규칙을 잊어도 된다"가 아니다.
