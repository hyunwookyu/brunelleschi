# progress.md — 진행 기록

현행 계획서는 [`docs/line_plan.md`](docs/line_plan.md)(코드명 L, 2026-08-15 전환).
각 단계의 **선택 근거와 기각 사유**를 적는다(CLAUDE.md A-3).
반복해서 걸린 것은 [`PITFALLS.md`](PITFALLS.md)에 따로 있다.

---

## L-A 게이트 — **사전 등록** (측정 전에 박는다)

계획서 §9의 L-A 게이트는 "확정되고, 화면이 안 튀고, 연결된 획이 전부 놓인다"로만 적혀
있어 사후에 수치를 정할 여지가 있었다(리뷰어 지적, PITFALLS #26).
**아래는 `lift_gate.json`을 처음 돌리기 전에 정한 값이다.**

### 조건 (동작점을 하나 고르지 않는다, PITFALLS #12)

- **구도 5개**: 3점 ×3(요 35°/피치 15°, 요 20°/피치 25°, 요 50°/피치 8°) · 2점 ×1(피치 0°) ·
  1점 ×1(요 0°·피치 0°). _"구도가 하나뿐"이 S·G 전체에 걸렸던 한계다._
- **끝점 겨냥 오차 5수준**: 0 / 0.005 / 0.01 / **0.03 / 0.05**.
  뒤의 둘이 **유일한 실획 자릿수**(AS-C2의 0.028~0.051)이고 S·G는 그 구간을 한 번도 안 쟀다.
- 잉크 등급 2(precise·medium) · 시드 6. **결과는 오차 수준별로 갈라 낸다.**

### 분모 (PITFALLS #11 — 행마다 다르면 같은 양의 비교가 아니다)

| 지표 | 분자 | 분모 |
|---|---|---|
| 확정률 | 소실점 ≥2 검출 + `recoverCamera.ok`인 장면 | 전체 장면 |
| 오배정률 | 지지 집합에서 다수결 축과 다른 획 | **소실점에 배정된 획** (배정 안 된 획 수를 함께 낸다) |
| 배치율 | 3D 세그먼트가 나온 획 | **가장 큰 연결 성분의 획** (모집단 12도 함께 낸다) |
| 조용히 틀린 배치 | 형태 오차 > **0.2** 상자 대각 | **놓인 획** |
| 가짜 교점률 | 참 3D에서 점을 공유하지 않는 교점 | 채택된 교점 전부 |

### 통과 기준

1. **확정** — 확정률 **≥ 0.90** (끝점 오차 0.01 이하). 소실점 3개 검출률은 함께 내되
   **판정에 쓰지 않는다** — 2개만 잡히는 것은 실패가 아니라 **2점으로 읽고 나중에 승격**하는
   설계다(§6).
2. **배치** — 배치율 **≥ 0.95** (끝점 오차 0.01 이하), 조용히 틀린 배치 **≤ 0.10**.
   _비교 기준: G-A.1 첫 시점 배치율 0.887 / 조용히 틀림 **0.278**, S 0.846 / 0.057._
3. **전환 무변화** — `segGap` 최대 **< 0.5px**.
   ⚠ **이것은 게이트가 아니라 불변식이다.** 각 점이 자기 광선 위에 있으므로 되쏘면 원본으로
   돌아온다 — **설계 보장**이고 selfcheck 자기참조 유형 3이다(PITFALLS #5).
   계획서 §5.3이 "튀면 카메라가 틀린 것"이라 한 것은 **성립하지 않는다**: f 오차는 전역 배율
   편향이라(AS-C4·이론서 16.4) 같은 카메라 재투영에서 상쇄된다.
   → **대체 지표: 교점 잔차.** 참 카메라에서 중앙 잔차 ≤ 0.05이고,
   **f를 ±20% 틀리게 주면 최소 2배로 커져야 한다.** 안 커지면 그 지표도 보장이므로 폐기한다.
4. **미배치가 화면에 남는다** — 놓지 않은 획이 `unplaced`에 이유와 함께 남는다(단위 테스트).

**통과 못 하면 멈추고 보고한다**(유일한 중단 조건).

---

> **2026-08-15 L 전환에서 새로 시작했다.** S·G의 기록(3286줄)은
> `docs/archive/progress_SG.md`에 있다.

---

## L-A.1 — 정리 (문서)

`line_plan.md`를 `docs/line_plan.md`로 편입했다.
`docs/sketch3d_plan.md`·`docs/segment_plan.md`를 `docs/archive/`로 옮기고 머리말에 폐기 사유를 적었다.
`progress`·`DECISIONS`·`DEFERRED`·`assumptions`를 `docs/archive/*_SG.md`로 옮기고
유효 항목만 새 파일에 옮겨 적었다(D-L1).

**옮겨 적은 것**: assumptions 이월 10건(AS-C1~C10) · DECISIONS 이월 6건(D-C1~C6) ·
DEFERRED는 대상이 남아 있는 것만.
**버린 것**: 배치 계층·프리핸드 보존에 딸린 전제와 결정 전부.

## L-A.2 — 소실점 후보 검출 (§5.1)

`web/src/s3d/vpDetect.ts` · `web/test/vp_detect.test.ts`(11).
쌍마다 교점을 씨앗으로 두고 지지를 세어 최다부터 떼어 낸 뒤, 전부 찾고 나서 다시 배정한다.

**계획서 문장 그대로는 작동하지 않았다.** 세 번 고쳤고 셋 다 측정이 시켰다.

| 무엇이 틀렸나 | 관측 | 어떻게 고쳤나 |
|---|---|---|
| "산포 임계는 화면 대각 비율" | 먼 소실점에서 발산한다. 획 길이 대비도 같은 방향 — 잉크 노이즈에서 **참 소실점의 부적합도 0.061이 임계 0.06을 넘어 검출이 통째로 실패** | **각도**로 걸었다(D-L2). 2.5°, 스윕으로 정함 |
| "선 3개가 한 점에서 만나면 확정" | **상자의 꼭짓점이 그 조건을 완벽히 만족한다.** 첫 실행에서 검출된 "소실점" 셋이 전부 꼭짓점이었다 | **도달거리** 조건(D-L3). 소실점은 획 끝에서 획 길이 1배 이상 밖에 있어야 한다 |
| 지지를 개수로 센다 | 지평선 근처 모서리가 두 수평 소실점을 다 지나므로 **엉뚱한 중간점이 지지 6**을 모은다(참 소실점은 4). 소실점이 둘만 잡혔다 | **가중합** `Σ(1−φ/tol)` + 전부 찾은 뒤 **재배정**(D-L4) |

**측정**(구도 하나 · 상자 하나 · 끝점 오차 0~0.01 × 시드 10 = 40장면):

| `support_deg` | 3점 검출 | 2점만 | 오배정 | f 오차 중앙 | p90 |
|---|---|---|---|---|---|
| 2.0 | 34/40 | 6 | 12/399 | 0.127 | 0.298 |
| **2.5** | **37/40** | 3 | **13/423** | 0.105 | 0.267 |
| 3.0 | 35/40 | 5 | 16/434 | 0.090 | 0.267 |
| 4.0 | 38/40 | 2 | 16/456 | 0.090 | 0.288 |

**최적화 대상은 검출 개수가 아니다.** 소실점이 둘만 잡히는 것은 실패가 아니라
**2점 투시로 읽고 나중에 승격하는 것**이 설계다(§6). 그래서 오배정을 본다 —
1.5~6°에서 3.0~3.5%로 거의 평탄하고 2.5°가 둘 다 낫다.

~~**f 오차 ~9%는 알고리즘의 몫이 아니다**(D-L5). 참 축 라벨을 주고 최소제곱 가중만 바꿔도
중앙값이 0.094에서 안 움직인다.~~ **철회했다** — 구도 하나에서 재서 획 길이가 비슷했기 때문이고,
구도를 다섯으로 늘리니 중앙값이 0.0921 → 0.0793으로 움직인다(D-L5).

~~**기각**: 최소제곱 길이 가중(p90만 12% 개선, 중앙 불변 — A-3).~~ **뒤집어 채택했다**(D-L5).
검출 단계의 직교 조건(카메라가 f²<0으로 이미 검증한다. 두 군데서 판정하면 어긋날 때 원인을 못 가른다).

**정정**: 아래 L-A.4에서 이 표를 다시 냈다. **이 절의 수치는 구도 하나(요 35°·피치 15°)에서
나온 것이고 구도를 다섯으로 늘리면 결론이 달라진다.** 원장은 `vp_detect.json@1b6175a4`이며
그쪽 값을 쓴다. 이 표는 지운다 — 그리고 **왜 틀렸는지**를 L-A.4에 적었다.

⚠ **원장 STALE이 생겼다**(그때 21건이라 적었으나 **20건**이었다 — 아래 L-A.4의 정정 절). `VP_TOL`을 `constants.ts`에 등록(D-C4)해서 상수 해시가
`1671e540` → `6c7f9be8`로 바뀌었고, S·G 시절 산출물 전부가 STALE로 표시된다.
차이는 `VP_TOL: None → …` 하나뿐이므로 **그 측정들이 실제로 낡은 것은 아니지만**,
검사를 약화시키지 않는다(경고 18). 해당 산출물은 폐기 대상 코드가 낸 것이므로
**L-A.5에서 `stage0/out/archive_SG/`로 옮긴다**(`archive_W/`·`archive_pre_W/` 선례).

---

## L-A.4 — 게이트 측정과 **판정: 실패**

원장: `stage0/out/vp_detect.json@1b6175a4` · `stage0/out/lift_gate.json@1b6175a4`.
하네스 `web/test/lift_gate.test.ts`. 조건·분모·통과 기준은 위 사전 등록 절.

### 사전 등록 대비 판정

| # | 기준 | 관측 | |
|---|---|---|---|
| 1 | 확정률 ≥ 0.90 (끝점 오차 ≤0.01) | **기준 구도 36/36 = 1.00** · 5구도 합산 0.90 / 0.917 / 0.85 | ◐ |
| 2 | 배치율 ≥ 0.95 (분모=최대 성분) | **1.000 — 그런데 이 분모는 판정을 공짜로 통과시킨다**(PITFALLS #11). 모집단 12 기준으로는 0.62~0.83 | ✗ |
| 3 | 조용히 틀린 배치 ≤ 0.10 | **참 카메라·기준 구도에서만 34/430 = 0.079.** 검출 카메라에서 **230/359 = 0.641** | ✗ |
| 4 | 미배치가 화면에 남는다 | `unplaced`에 이유와 함께 남는다(단위 테스트) | ✓ |
| 5 | 전환 무변화 | `segGap` 최대 **0.0** — 그리고 **f를 ±20% 틀리게 줘도 0.0이다**(사전 등록한 반례 검사) | 보장 확인 |

**게이트 실패.** 계획서가 정한 유일한 중단 조건에 걸렸다.

### 무엇이 성립하고 무엇이 안 되는가 — 원인이 하나로 좁혀졌다

기준 동작점(3점 요35°·피치15°, 끝점 오차 ≤0.01, 36장면 432획):

| 카메라 | 배치(of 12) | 조용히 틀림 | 형태 오차 중앙 |
|---|---|---|---|
| **참 카메라** | 0.9954 | **34/430 = 0.079** | **0.026** |
| 검출 카메라 | 0.831 | 230/359 = 0.641 | 0.271 |

**올리기(§5.2)는 맞다.** 참 카메라를 주면 조용히 틀린 배치가 **0.079**로, S의 0.057과 같은
대역이면서 **배치율은 0.846 → 0.995**다(G는 0.887 / 0.278). 정확한 투영에서는 오차가 1e-6이고
동차계의 최소 고유값이 −3.6e-17이다 — **앵커도 씨앗도 없이 참 상자가 배율까지 복원된다.**

**무너뜨리는 것은 카메라다.** 그리고 f가 아니라 **축 방향**이다:

- 축 방향 오차(검출 축 ↔ 가장 가까운 참 축, 번호 규약 무관) 중앙 **4.18°** · p90 **11.75°**
- f 상대오차 중앙 0.0762
- 그런데 **f만 10% 틀리게 주면 형태 오차 중앙이 0.098 → 0.115뿐이다**(`f_bias`).
  즉 **f 크기가 지배항이 아니다** — 소실점 위치 오차가 3D 축 프레임을 4° 틀어 놓는 것이다.
  `axisDirection = unit([vp − P, f])`이므로 먼 소실점의 100px 오차가 곧 2~3°다.

전 구도·전 잡음(5구도 × 5잡음 × 2등급 × 6시드):

| 끝점 오차 | 참 카메라 조용히 틀림 | 검출 카메라 | 검출 배치(of 12) |
|---|---|---|---|
| 0 | 183/717 = 0.255 | 392/571 = 0.687 | 0.793 |
| 0.005 | 198/716 = 0.277 | 442/559 = 0.791 | 0.776 |
| 0.01 | 226/702 = 0.322 | 332/449 = 0.739 | 0.624 |
| **0.03**(실획 자릿수) | 301/584 = 0.515 | 220/240 = 0.917 | 0.333 |
| **0.05**(실획 자릿수) | 329/520 = 0.633 | 146/154 = 0.948 | 0.214 |

**실획 자릿수(AS-C2의 0.028~0.051)에서는 참 카메라로도 절반이 틀린다.**
S·G가 그 구간을 한 번도 재지 않았으므로 이것은 **L이 새로 드러낸 값이지 L이 만든 문제가 아니다.**

### 시도해 기각된 것 셋

| 시도 | 결과 | 판정 |
|---|---|---|
| **카메라 일괄 적합**(회전 3 + f 1을 획 전부로, `cameraFit.ts`) | 기준 구도 f 0.0762 → **0.0827(악화)**. 다른 구도는 0.114 → 0.383, 0.186 → 0.394, 1점은 상한 2.0까지 달아났다 | **기각**(D-L6). 코드는 남기고 배선하지 않는다 |
| **교점 잔차를 카메라 검증 지표로** | f 0.8~1.2배에서 중앙 0.0285 / 0.0296 / 0.0286 / 0.0275 / **0.0262** — **안 움직인다.** 같은 구간에서 형태 오차는 0.185 → 0.098 → 0.140으로 움직인다 | **폐기**. 입사 관계는 사영 불변이므로 **원리적으로** f를 못 잡는다 |
| **전환 픽셀 차이**(계획서 §5.3) | `segGap` 최대가 모든 f 배율에서 **0.0** | **보장이지 측정이 아니다**(PITFALLS #5) |

### 채택한 것 셋 (측정이 시켰다)

| 채택 | 근거 |
|---|---|
| **도달거리 조건** `min_reach = 1.0`(D-L8) | 스윕에서 양방향 최적: 0으로 내리면 꼭짓점 오검출로 오배정 245/852, 4로 올리면 **1점 구도의 참 소실점이 죽는다**(3점 검출 0/24). 1.0에서 먼 구도 3점 검출 50/96 · 1점 오배정 8/180 |
| **국소화 가능성** `min_spread_mult = 1.5`(D-L7) | 참 소실점이 유한인 다발 **199/221 = 0.90**을 살리고 무한원인 다발 **33/67 = 0.49**를 거른다. ⚠ **깨끗한 분리가 아니다** — 산포 분포가 겹친다(유한 최소 0.43° · 무한 최대 17.1°) |
| **길이² 가중 최소제곱**(D-L5) | f 상대오차 중앙 균등 0.0921 → `len` 0.0860 → **`len²` 0.0793**. 초판이 "차이 없다"로 읽은 것은 구도 하나에서 재서 획 길이가 비슷했기 때문이다(길이비 2.03~4.44) |

### 다음 세션이 고를 수 있는 것 — 사람이 정한다

1. **소실점 정밀도를 올린다(미시도).** 지배항이 축 방향 4°이고 그것은 소실점 위치에서 온다.
   상자 하나(축당 4획)로는 지렛대가 짧다. 획이 더 쌓이면 나아지는지가 미측정이다 —
   **획 수를 늘려 축 방향 오차 곡선을 재는 것**이 가장 싼 다음 측정이다.
2. **카메라를 사용자가 확정한다.** 옛 S 접근의 UI(축1/축2/축3 소실점 찍기)가 **그대로 살아 있다.**
   그러면 §5.1·§5.2가 "초기 2D → 카메라 확정"이라는 고유 항목 하나를 잃고 SketchUp 선 도구만 남는다.
   대신 형태는 참 카메라 대역(0.079)으로 간다.
3. **L-B로 넘어가고 초기 확정의 부정확을 받아들인다.** 실시간 스냅은 **시작점이 확정된 뒤**의
   이야기이므로 초기 구조가 조금 틀어져도 그 뒤 획들은 그 구조에 정확히 붙는다.
   대가는 첫 구조가 돌려 보면 기울어 있는 것이고, L-C 승격이 그것을 다시 푼다.
4. **실획 표본을 받고 다시 잰다.** 지금 판정은 Quick,Draw 유도 합성 잉크 위에 있고
   AS-C10은 그 모델이 **양방향으로 틀렸다**고 이미 적었다. 대상 사용자는 숙련자다.

### 원장 STALE — 정정

L-A.2에 "STALE 21건"이라 적었는데 **당시에도 20건**이었고(`selfcheck.json` `n_stale`),
전체 테스트를 다시 돌린 지금은 **1건**이다(`e2e.json` — Playwright가 내는 것이라 이번에 안 돌았다).
나머지는 각자의 테스트가 재실행되며 현재 스냅샷으로 다시 써졌다.
→ **`archive_SG/`로 옮길 필요가 없다.** 리뷰어가 우려한 `real_ink.json`·`bend_share.json`·
`one_stroke_one_axis.json`은 그대로 `stage0/out/`에 있고 자기 테스트가 살아 있다.
L-A.5에서 옮길 대상은 **폐기하는 테스트가 내던 것뿐**이다.

---

## L-A.5d + L-A.6 — 축 방향 오차 곡선과 획 수 곡선

원장: `stage0/out/camera_gate.json@1b6175a4`. 하네스 `web/test/camera_gate.test.ts`.
**착수 시 `PITFALLS.md`를 읽었다.** 걸리는 번호: #2(대리값) · #8(꼬리) · #11(분모) ·
#12(동작점) · #13(절단값) · #14(분자/분모) · #25(원장) · #26(사전 등록).

### ① 임계는 **0.5°와 1° 사이**다 (§5.5 b, 그리고 §5.4의 대리 측정)

기준 구도(3점 요35°·피치15°, 끝점 오차 ≤0.01, 431획). **목표 각도**를 주고 축을 독립으로
돌린 뒤 `recoverCamera`로 다시 푼 값이므로 **실측 축 오차는 목표의 약 2배**로 나온다
(수심이 f를 다시 내면서 축이 더 움직인다). 판정은 **실측 쪽**으로 읽는다.

| 준 각도 | 실측 축 오차 | 조용히 틀림 (>0.2) | (>0.1) | (>0.5) | 형태 오차 중앙 | f 오차 |
|---|---|---|---|---|---|---|
| 0 | 0 | **29/431 = 0.067** | 71/431 | 3/431 | 0.030 | 0 |
| 0.25° | 0.48° | **30/431 = 0.070** | 90/431 | 3/431 | 0.061 | 0.008 |
| **0.5°** | **0.96°** | **36/431 = 0.084** | 236/431 | 2/431 | 0.109 | 0.016 |
| 1° | 1.92° | 218/431 = 0.506 | 372/431 | 2/431 | 0.205 | 0.033 |
| 2° | 3.81° | 366/431 = 0.849 | 427/431 | 65/431 | 0.399 | 0.075 |
| 4° | 7.96° | 409/431 = 0.949 | 431/431 | 356/431 | 0.770 | 0.139 |

**참 카메라 대역(0.079)에 드는 것은 실측 축 오차 ≈ 1° 이하다.** 1.9°에서 0.506으로 무너진다.
_절단값을 하나 고르지 않았다(#13) — 0.1 절단으로 보면 0.5°에서 이미 0.55로 나쁘다.
즉 **"쓸 만하다"의 기준이 0.2 절단이라는 것이 판정에 들어 있다.**_

**화면 픽셀로 환산하면** (f=1000, 축 오차 1° 기준): 소실점이 화면 밖 800px이면 **29px**,
1400px이면 **52px**, 2200px이면 **102px** 안으로 맞춰야 한다. 소실점이 멀수록 관대해진다.
**드래그로 도달 가능한 자릿수다** — 다만 화면 밖 소실점은 직접 못 끌므로 가이드 선이 필요하다(§5.4).

⚠ **이것은 대리값이다**(PITFALLS #2). 재는 것은 "축 오차가 θ면 형태가 얼마나 틀리는가"이고
**"사람이 θ에 도달한다"는 측정하지 않았다**(AS-L9, 표본 0).

### ② 획을 늘려도 자동 검출은 임계에 못 간다 — **AS-L10 반증**

| 등분 k | 축당 획 | 총 획 | 검출 축 오차 중앙 / p90 | 검출 배치(of all) | 검출 조용히 틀림 |
|---|---|---|---|---|---|
| 1 | 4 | 12 | **7.40° / 35.0°** | 0.569 | 1666/2049 = 0.813 |
| 2 | 18 | 54 | **10.04° / 32.7°** | 0.373 | 4919/6035 = 0.815 |
| 3 | 48 | 144 | **10.09° / 33.1°** | 0.263 | 1816/2045 = 0.888 |
| 4 | 100 | 300 | **4.53° / 42.7°** | 0.128 | 539/692 = 0.779 |

**단조 감소하지 않는다.** 7.4 → 10.0 → 10.1 → 4.5이고 **임계 1°에서 4배 이상 떨어져 있다.**
배치율은 오히려 급락한다(0.57 → 0.13) — 내부 격자선이 짧아 `min_len_ratio`에 걸리고
미분류가 늘기 때문이다. 같은 조건의 **참 카메라 대조**는 반대로 간다(배치 0.88 → 0.997).

⚠ **조건이 k에 따라 다르다**(#11·#12). k≥3에서 구도 5→3, 시드 6→3/1, 잡음 5→3 수준으로
줄였고 `seed_pairs_max`가 씨앗 쌍을 건너뛴다. **줄인 사실을 원장에 적었다.**
그래도 4배 이상의 격차는 조건 차이로 설명되지 않는다.

**→ 자동 확정 경로를 두지 않는다**(§5.5 c의 조건이 성립하지 않는다).
검출은 **초안**으로만 쓰고 사용자 조정을 **항상** 요구한다.

### 기각·채택

| | |
|---|---|
| **기각** | 자동 확정 제안 경로(§5.5 c). 획 수로 임계에 도달하지 못한다 |
| **채택** | `seed_pairs_max = 6000` — 획 300개에서 검출이 `O(n³)`으로 멈췄다. 결정론적 보폭으로 씨앗 쌍을 건너뛴다. 씨앗은 출발점일 뿐이고 값은 최소제곱이 준다 |

## L-A.5abc — 소실점 초안·조정 UI (§5.4)

`web/src/s3d/vpDraft.ts` · `web/test/vp_draft.test.ts`(8) · `ConstraintAccumulator.setLines` ·
`InkCanvas`의 끌기 모드 · `main.ts` 배선(`소실점 추정` 버튼 · `소실점 조정` 도구 · 확정=`카메라 잠금`).
**착수 시 `PITFALLS.md`를 읽었다.** 걸리는 번호: #17(측정 경로/앱 경로 분리) · #19 · #21(dpr) · #22(새 탭).

**점이 아니라 선이다.** 소실점은 대개 화면 밖이라 끌 수 없다 —
fSpy·SketchUp Match Photo의 선례를 따라(A-3) **축마다 가이드 선 두 개**를 두고 끝점 넷을 끈다.
누산기가 이미 `vp_line`을 받으므로 새 기전이 아니고, **초기값을 검출이 채우는 것**이 새로운 부분이다.

**dpr 규약은 `canvasFrame` 단일 출처를 탄다**(D-C3) — 끌기 좌표를 바깥에서 `clientX`로 읽지 않고
`InkCanvas`에 `dragMode`/`onDrag`를 더해 `local(e)`을 쓰게 했다. 그 버그는 dpr 1에서 안 보인다.

### 브라우저 확인에서 잡은 것 둘

**① 캔버스 크기가 또 굳었다 — AS-C7 세 번째.** `panel.acc.imgSize`가 `[1, 24]`인 채로
캔버스는 490×672였다. `ResizeObserver`는 이 환경에서 초기 콜백을 안 주고(S-4에서 확인),
`requestAnimationFrame`은 **숨은 탭에서 완전히 멈춘다**(구 V-n: 600ms에 0프레임).
브라우저 창이 뒤에서 열리면 둘 다 발화하지 않는다. 그 크기가 화각·무한원 판정의 기준이라
**소실점이 하나로 읽혔다.**

→ 관찰자를 하나 더 거는 대신 **쓰는 자리에서 스스로 고친다**: `refresh()`가 매번
`sizeStale()`을 보고 어긋나 있으면 `fit()`한다. 어떤 관찰자가 발화하든 안 하든 회복된다.
`setTimeout(0)`과 `visibilitychange`도 함께 걸었다(타이머는 숨은 탭에서도 돈다).
고친 뒤 `[490, 672]`로 정상.

**② 내 브라우저 픽스처가 틀렸다** — "소실점 쪽으로 t만큼" 방식으로 상자를 만들었더니
`AB − B = 0.86(A − O)`가 되어 **화면에서 평행한 아핀 상자**가 나왔다(수렴이 0, 산포 0.0°).
검출이 그것을 무한원으로 판정한 것은 **맞는 동작**이었다.
→ 측정 하네스와 **같은 픽스처**(`test/scene3d.ts`)를 브라우저에서 그대로 import해 쓴다.
그러면 앱 경로와 측정 경로가 같은 입력을 본다(PITFALLS #17).

### 확인된 동작 (브라우저, 490×672, dpr 1)

- 참 소실점 `[1280,148] [−262,148] [245,2948]`의 상자 12획 →
  검출 `[1178,168] [−290,135]` + 수직 다발은 무한원(산포 3.3° < 3.75°) → **2점 투시로 읽는다**
  (설계대로다 — 나중에 승격한다)
- `소실점 추정` → 축 2개 확정, 가이드 4개
- `소실점 조정`에서 핸들을 24px 끌면 소실점이 실제로 움직인다

⚠ **민감하다.** 가이드 둘이 거의 나란해지면 교점이 멀리 날아간다 — 24px 끌었더니
소실점이 `−360` → `−37639`로 갔다. **그래서 실시간 피드백이 필수다**(사용자가 그리드를 본다).
가이드를 길게 잡는 것(`min_guide_ratio` 0.12)이 완화책이고, 값은 실사용에서 재정한다.

### `liftAll`을 앱에 배선했다 — 그리고 하나가 더 빠져 있었다

`main.ts`의 첫 시점 배치를 `settle`/`reprojectAll`에서 **`liftFirstView`**(→ `liftAll`)로 바꿨다.
`pts3d = [a, b]` 두 점으로 쓰므로 튜브 렌더·저장·내보내기가 그대로 동작한다.

**빠진 것: 확정 순간의 축 재분류.** 배선 직후 브라우저에서 **배치가 0**이었다 —
L에서는 **획이 카메라보다 먼저** 그려지므로 그릴 때의 축 판정이 전부 `free`이고,
`liftAll`은 미분류를 놓지 않는다(D-C2). §5.2의 첫 단계("확정 순간 → 모든 획을 축별로 분류")를
글자 그대로 빠뜨린 것이다. `liftFirstView`가 확정 카메라로 다시 분류하도록 고쳤고,
**사용자가 직접 고른 축(`userAxis`)은 덮어쓰지 않는다.**

고친 뒤 브라우저에서 **2점 확정 12/12 · 3점 확정 12/12 배치**.
전 경로(그리기 → 추정 → 조정 → 확정 → 3D)가 앱에서 돈다.

### 남은 것 — 다음 세션

- **§5.4 게이트를 실사용에서 확인**: 사람이 가이드를 끌어 참 카메라 대역(조용히 틀림 0.079)에
  실제로 도달하는가. 대리 측정은 "축 오차 ≈1° 이하면 든다"까지만 말한다(AS-L9).
- 옛 배치 계층(`settle`·`placeInto`·`appPlace.FIRST_VIEW_OPTS`)이 **더는 첫 시점에서 안 불린다**.
  돌린 시점 경로(`VIEW_OPTS`)는 아직 쓴다 — L-B의 뷰포트 통합에서 함께 정리한다.

---

## L-A.7 + 리뷰어 대응 (L-A.5/L-A.6에 18건, 높음 7)

**착수 시 `PITFALLS.md`를 읽었다.** L-A.7에 걸리는 번호:
**#24(단위 변경은 아무것도 고치지 않는다 — D-L2에서 걸린 자리)** · #3(지표가 정의의 귀결) ·
#6(대조군) · #8(꼬리) · #13(절단값) · #25(원장) · #26(사전 등록).

### ⚠ 임계를 **≈1° → ≈0.5°**로 정정한다 (리뷰어 [1][2][3])

세 지적이 같은 방향으로 작용했고 전부 맞다.

**[1] 대역 기준값이 다른 하네스의 값이었다.** `lift_gate`의 0.0791은 **검출 오배정이 섞인**
값이고, `camera_gate`는 **참 축 라벨**을 준다. 같은 하네스 안의 무오차 행은 **29/431 = 0.0673**이다.
그것을 대역으로 쓰면:

| 준 각도 | **실측 축 오차** | 조용히 틀림(0.2) | 대역 대비 | 형태 오차 중앙 | **등록 기준(0.026) 대비** |
|---|---|---|---|---|---|
| 0 | 0 | 29/431 = 0.0673 | 1.00배 | 0.0296 | 1.00배 |
| 0.25° | **0.48°** | 30/431 = 0.0696 | **1.03배** | 0.0613 | 2.07배 |
| 0.5° | **0.96°** | 36/431 = 0.0835 | **1.24배** | 0.1085 | 3.67배 |
| 1° | 1.92° | 218/431 = 0.5058 | 7.52배 | 0.2050 | 6.93배 |
| 2° | 3.81° | 366/431 = 0.8492 | 12.6배 | 0.3985 | 13.5배 |

**[3] 판정 지표를 사유 없이 바꿨다.** §5.5가 사전 등록한 기준은 **형태 오차 0.026**인데
판정은 조용히 틀림 0.079로 했다. 등록 기준으로 읽으면 **실측 0.48°에서 이미 2.07배**다.
→ 등록 기준을 살린다. **두 지표가 같은 곳을 가리키지 않으므로 둘 다 적는다**:
- 조용히 틀림(0.2 절단) 기준: 실측 **0.96°**까지 대역의 1.24배 — 낭떠러지는 1.9°
- 형태 오차 중앙 기준(**등록된 것**): 실측 **0.48°**에서 이미 2.07배

**→ 임계는 실측 축 오차 ≈0.5°다.** 초판의 "≈1°"는 대역을 다른 하네스에서 가져오고
등록 지표를 바꿔서 나온 값이었다.

**[2] 화면 픽셀 환산이 준 각도 단위였다.** 29/52/102px는 **준 1°**(실측 1.9°, 이미 무너지는 곳)다.
실측 기준으로 고치면 — 소실점이 화면 밖 800/1400/2200px일 때
**실측 1.0° → 14/26/51px, 실측 0.5° → 7/13/26px.**

### [5] 핸들 예산 — **소실점 여유가 아니라 핸들 여유를 재야 했다**

리뷰어가 정확히 짚었다: 소실점 좌표의 여유(px)는 **핸들의 여유**가 아니다.
그래서 직접 쟀다(`vp_homog.json@1b6175a4`의 `by_guide_length`).
**축 오차 0.5°를 다 쓰기까지 핸들이 움직여도 되는 px**(최악 방향):

| 가이드 길이 | 각차 3° | 5° | 10° | 20° |
|---|---|---|---|---|
| 100px | 0.20 | 0.28 | 0.46 | 0.72 |
| **140px**(초판 기본) | **0.29** | **0.40** | **0.65** | **1.01** |
| 300px | 0.63 | 0.88 | 1.40 | 2.16 |
| **525px**(고친 기본) | ~1.1 | ~1.5 | ~2.5 | ~3.8 |
| 600px | 1.28 | 1.76 | 2.82 | 4.33 |

**초판 `min_guide_ratio` 0.12(=140px)는 서브픽셀 예산이었다** — 사람이 맞출 수 없다.
0.45(≈525px)로 올렸다. **그래도 1~4px이고 넉넉하지 않다.**
지배 인자는 **핸들 간격**이다(소실점이 두 핸들을 잇는 직선으로 정해지므로 각도 정밀도가 간격에 비례).

**이것이 §5.4의 실제 한계이고, AS-L9의 답이 아직 아니다** — 사람이 1~4px 안에 놓는지는 미측정이다.

**그리고 캔버스가 또 하나의 한계다.** 0.45로 올린 뒤 브라우저(490×672, 대각 832)에서 실제
가이드 길이는 **304~358px**이었다 — 요구한 374px이 **캔버스 여백에 잘린다**.
즉 **작은 캔버스에서는 핸들 예산을 늘릴 방법이 없다.** L-B 뷰포트 통합에서 다시 본다.

### [6] §5.4의 판정 수단이 §5.3이 폐기한 것과 같은 유형인가 — **아니다, 다만 부분적이다**

리뷰어의 물음이 옳다. 갈라 적는다.
- §5.3·§6.2가 폐기된 것은 **올린 3D를 되쏘아 검사하는 것**이 항등이기 때문이다(출력의 자기검사).
- §5.4에서 사용자가 보는 것은 **소실점 위치를 정하는 입력**이고, 3점에서는 소실점 셋이
  카메라를 **완전히** 정한다(수심 6.3, 자유도 0). 그러므로 입력을 눈으로 맞추는 것은 항등이 아니다.
- **그러나 사용자는 증폭을 볼 수 없다.** 수심이 f를 내면서 축이 더 움직여 실측 오차가
  준 각도의 약 2배가 된다(위 표). 눈으로 맞춘 정밀도가 그대로 축 정밀도가 아니다.
- 그리고 §5.3이 적은 대로 **카메라 오류는 돌려 봐야 보인다** — §5.4의 판정은 돌리기 전이다.

→ §5.4는 "입력을 맞추는 수단"이지 "출력을 검증하는 수단"이 아니다. 계획서를 그렇게 고쳤다.

### [7] §6.3 격상 논거가 틀렸다 — **철회한다**

"승격은 다른 카메라라 화면이 튄다"고 적었는데, §6.1이 **새 카메라로 처음부터 다시 올린다**이므로
각 점은 다시 **새 카메라의 자기 광선 위**에 놓인다. **첫 시점 화면은 또 항등이다** —
§5.3이 폐기된 것과 같은 이유다. 움직이는 것은 3D와 그리드·미배치 집합이지 이미 그린 획이 아니다.
→ 계획서 §6.3에서 "화면이 튄다"를 뺐다. **되돌리기는 여전히 필요하지만 근거가 다르다**:
사용자가 **돌려 보고** 판단한다.

### [4] 새 게이트에 측정 대상이 없었다

게이트를 문자 그대로 읽으면 `deg_0` 행(참 카메라를 그대로 준 행)이 곧 대역이라 **자기 항등**이다.
그리고 0.079/0.0673은 **구도 하나**의 값이고, 5구도 합산 참 카메라 값은
**1197/3185 = 0.376 · 배치 0.885**로 5배 다르다.
→ 게이트를 다시 등록한다(아래 "L-A.5 게이트 재등록").

### 나머지 지적 대응

| # | 지적 | 대응 |
|---|---|---|
| [8] | 획 수 곡선의 비단조가 표본 차이일 수 있다 | **비단조 주장을 뺀다.** n과 시드·구도를 표에 넣었다(k3은 n=93·시드3·구도3, k4는 **n=18·시드1**). 남는 주장은 "임계에서 4배 이상 멀다"뿐이다 |
| [9] | 배치율 급락의 원인 귀속이 틀렸다 | 맞다. `min_len`에 걸리는 것은 k4에서 8%(276/300)뿐이고, `camera_ok`가 258/300 → **6/18**이다. **큰 몫은 카메라 실패다**(PITFALLS #11) |
| [10] | "획을 늘렸다"의 조작이 등록과 다르다 | 맞다. 늘린 것은 **1/k 길이의 내부 격자선**이라 지렛대가 오히려 짧아진다. AS-L10의 ❌에 **"내부 격자선을 늘린 경우"** 조건을 붙였다. 결론(자동 확정 없음)은 안 바뀐다 |
| [11] | 오라클 대조를 배치율만 인용했다 | 오라클 조용히 틀림은 k1 0.376 → k2 0.478 → k3 0.413 → k4 0.406으로 **전부 k1보다 나쁘다**. 함께 적는다 |
| [12] | "이미 놓인 기하가 따라 움직인다"가 첫 확정에서는 성립 안 함 | 맞다. 첫 확정 전에는 놓인 기하가 없고 **그리드뿐**이다. 계획서를 고쳤다 |
| [13] | 브라우저 보고가 배치 12/12뿐 | 맞다 — 그 분모는 L-A.4가 이미 "공짜 통과"라 적은 것이다. 형태 오차를 안 냈다. **DEFERRED로 올린다**(브라우저에서 참값 대조를 하려면 하네스를 앱에 넣어야 한다) |
| [14] | 해시 `@2fd74e1c`가 디스크에 없는 실행 | 13곳을 `@2fd74e1c`으로 고쳤다(`grep`으로 확인) |
| [15] | AS-C7이 "두 번"으로 남아 있다 | 세 번째와 대응(자가 치유)으로 갱신했다. PITFALLS #22도 |
| [16] | 자가 치유가 재발을 관측 불가로 만든다 | 맞다(PITFALLS #7·#19). `sizeStale()` 발동 횟수를 세어 진단에 낸다 |
| [17] | 10.04/10.09를 두 점으로 제시 | 유효 자릿수 2로 줄이고 n을 병기했다 |
| [18] | D 번호 교차 참조 어긋남 · 철회된 문장 잔존 | 고쳤다 |

### L-A.5 게이트 재등록 (측정 전에 박는다)

**판정 대상**: 사람이 가이드를 끌어 도달하는 **실측 축 방향 오차**.
**기준값**: 같은 하네스(`camera_gate`)의 무오차 행 — 조용히 틀림 **0.0673** · 형태 오차 중앙 **0.0296**.
**통과**: 실측 축 오차가 **0.5° 이하**여서 두 지표가 각각 **1.1배 · 2.1배 이내**.
**구도**: 기준 구도 하나가 아니라 **5구도 전부**를 낸다. 기준 구도 값만으로 통과를 주장하지 않는다.
**측정 수단**: Playwright로 핸들을 끌어 실측 축 오차를 재는 종단 시나리오(L-D)
— 자동화가 어려우면 **실사용 대기**로 DEFERRED에 남긴다.

### L-A.7 — 동차(각도) 파라미터화

`vpHomog.ts` · `vp_homog.test.ts`(7) · `vp_homog_measure.test.ts` → `vp_homog.json`.

**무엇이 고쳐지고 무엇이 안 고쳐지는지 먼저 갈랐다**(PITFALLS #24 — D-L2에서 걸린 자리).

| | |
|---|---|
| **고쳐진다** | 표현의 발산. `v = ℓ₁ × ℓ₂`는 나눗셈이 없고 나란하면 `v₂ = 0`(무한원, 이론서 2.2). 각차 60°→0.5°에서 **위치는 0.64px → 8115px(12,600배)** 튀는데 **동차 각도는 0.031° → 0.41°(13배)**로 유계다 |
| **안 고쳐진다** | 카메라의 발산. 같은 구간에서 **f 상대 변화 0.0043 → 0.113(26배)**, **축 방향 0.115° → 1.10°/px**. 재매개화는 같은 기하를 다른 좌표로 적은 것뿐이다 |

⚠ **각도가 유계인 것은 정의의 귀결이다**(PITFALLS #3). 그것을 성과로 읽지 않는다.
동차 표현이 실제로 주는 것은 **값이 유한하게 유지되어 스냅을 연속적으로 걸 수 있다**는 것뿐이다.

**무한원 스냅**(§5.4 c): 각차 < **3.0°**면 무한원으로 보고 그 축을 비운다 —
소실점 개수가 줄어 1·2점 분기로 떨어진다(이론서 5.3). 각차 3°에서 0.39°/px이고
0.5° 예산의 78%라 그 아래는 조작이 불가능하다. 화면에 사유와 각차를 함께 알린다.

**반례 하나를 코드에서 잡았다**: 처음에 "각차가 0에 가까우면 퇴화"로 적었는데 틀렸다 —
서로 다른 두 평행선의 외적은 0이 아니고 그 점이 **정상적인 무한원 소실점**이다.
각차로 퇴화를 판정하면 무한원을 퇴화로 버린다. 퇴화는 **외적 크기**로 판정한다.

---

## 계획 개정 2 편입 (2026-08-15, 사람 지시)

`line_plan_r2.md`(저장소 루트, 본체가 만들지 않은 문서)를 `docs/line_plan.md`로 삼고
이전 판을 `docs/archive/line_plan_r1.md`로 보존했다. HANDOFF가 "사람이 지시하기 전에는
현행이 아니다"로 남겨 뒀던 건이고 이번 세션 첫 지시가 그 지시다.

**개정 넷**: ① 제품 정의 — 투시도와 입체가 **동등하고 동시에** 보인다. 3D는 부가 기능이 아니고
초보자용 가드레일을 두지 않는다(§1). ② **미배치는 실패가 아니라 대기 상태**이고
승격이 **연쇄**한다(§9.1). ③ **뷰 시스템** — 뷰가 2D 획을 소유하고, 승격되면 소속이 없어진다(§9.2).
④ **UI 재작성**을 §10으로 등록 — `web/src/ui/`만 교체하고 `canvasFrame`·잉크 캡처·씬/워커·
`lift.ts`·`vpDetect.ts`·카메라 수학·저장/내보내기는 그대로 쓴다.

**⚠ 원문을 그대로 넣지 않은 자리가 셋이다** — 개정 2는 L-A.7 **이전에** 쓰였고
그 사이에 정정된 수치를 담고 있었다. 원문을 그대로 두면 다음 세션이 정정 전 값을 다시 인용한다
(PITFALLS #1 — "문서의 수치가 원장보다 뒤처진다").

| 자리 | 원문 | 고친 값 | 근거 |
|---|---|---|---|
| §5.2 임계 | 축 오차 **약 1°**, 대역 **0.079** | **0.5°**, 대역 **0.0673 / 0.0296 둘 다** | 0.079는 `lift_gate`에서 온 **오배정 섞인** 값(#27). 지표를 하나만 쓰지 않는다(#28) |
| §5.2 여유 | 화면 환산 **29~102px** | **핸들 예산 0.29~4px** | 소실점 좌표 여유는 **사용자가 만지는 양이 아니다**(#29) |
| §12 위험 | "축 오차 1° 안" · "드래그 범위" | 0.5° · 핸들 예산 | 같음 |

§11의 L-A.7도 **완료로 표시하고 실제 결과로 바꿨다** — 등록된 항목은 "각도 파라미터화"였는데
한 것은 **동차 파라미터화**이고, 등록된 기대("발산이 없어진다")는 **절반만 맞았다**
(표현은 유계, **카메라는 여전히 발산**). 계획서에 기대만 남기면 반증이 지워진다.

CLAUDE.md의 제품 정의와 A-3의 미배치 문장도 개정 2에 맞췄다.
**개정 1은 폐기가 아니라 대체다** — 개정 2가 §2~§4·§8을 "개정 1 유지"로 참조하므로
그 절들의 원문은 아카이브 쪽에 있다. 아카이브 머리말에 그렇게 적었다(D-L1의 예외가 아니라,
D-L1이 금지하는 것은 **판단 근거로 인용**하는 것이다).

---


## L-B.1 — 단일 뷰포트 (계획서 §10.3)

_이 절은 **리뷰어 지적 15건 대응 후**의 판이다. 초판의 결론 하나가 뒤집혔다 —
`ink_gap`을 "유일한 실측"으로 적었는데 **그것도 정의의 귀결**이었다. 아래 [1] 참조._

### 착수 시 PITFALLS 대조

읽었다. 걸리는 번호로 **#5(자기참조 유형 3)** · #17 · #6 · #21 · #22 · #25 · #26을 적었다.
리뷰어가 **적지 않았는데 걸린 것 여섯**을 더 짚었다: #1(문서가 원장보다 뒤처짐) ·
#2·#3(자기확인·정의의 귀결) · #11(분모) · #12(동작점 하나) · #19(검사 약화) · #24.

### 무엇을 만들었나

```
src/s3d/sceneCam.ts   three 카메라 ← 확정 투시 카메라 (주점이 중심이 아닌 경우 포함)
src/ui/doc.ts         문서 모델 — seg3d === null 이 곧 2D 레이어(§9.1)
src/ui/camState.ts    가이드 → 카메라 (옛 main.ts의 applyGuides/placeCtx를 떼어낸 것)
src/ui/stage.ts       단일 뷰포트 — three 캔버스 아래, 잉크 캔버스 위
src/mainL.ts + l.html 새 UI 엔트리. **옛 index.html은 L-B 게이트까지 안 지운다**(A-4)
e2e/stage.spec.ts     브라우저 확인을 원장으로 (`stage_browser.json`)
```

**어려운 부분은 주점 하나였다.** three의 `PerspectiveCamera`는 화각과 종횡비만 받으므로
주점이 늘 화면 중심이다. `setViewOffset`으로 푼다 — 더 큰 프레임 `W×H`의 중심을 주점에 두고
캔버스를 그 부분창으로 본다(`W = 2·max(px, w−px)`). 주점이 중심이면 `W=w, offset=0`이 되어
**평범한 카메라로 되돌아간다**. `viewport.resize()`가 `aspect = w/h`로 덮어쓰면 주점이
중심으로 돌아가므로 `projectionHook`을 넣었다(D-L9).


### 이 항목에는 **측정이 없다** — 보장 둘뿐이다 (리뷰어 1회차 [1], PITFALLS #5)

`stage0/out/stage_cam.json@1b6175a4`. **등록 5구도**(3점×3 · 2점 · 1점) × **3등급** ×
**등록 시드 6** × **잡음 4수준**.

| | 값 | 무엇인가 |
|---|---|---|
| `seg_gap` | **0** | **보장** — `liftAll`이 각 점을 자기 광선에 놓으니 되쏘면 그 점이다 |
| `three_vs_project_gap` | **5.0e-13**(15행 중 최댓값) | **보장** — three의 실제 투영행렬을 통과시킨다 |
| `chord_dev` | 중앙 0.52 / 0.78 / 1.05px (precise/medium/coarse) | **이것도 보장이다** ↓ |

**초판은 `chord_dev`(옛 `ink_gap`)를 "사용자가 실제로 보는 변화"라는 실측으로 적었다.**
리뷰어가 뒤집었고, **대조군에 양성 채널을 붙여** 확인했다(기준 구도, medium, 시드 1, 잡음 0.01):

| 섭동 | `chord_dev` 중앙 | **양성 채널**: 3D 세그먼트 길이 중앙 |
|---|---|---|
| f × 0.8 | 1.5437 | **1.0364** |
| f × 1.0 | 1.5437 | 0.8667 |
| f × 1.2 | 1.5437 | **0.7383** |
| 소실점0 +12px | 1.5437 | 0.8677 |
| 소실점0 +40px | 1.5437 | 0.8698 |

**섭동은 계산에 닿았고**(세그먼트 길이가 1.04 → 0.74로 움직인다) **`chord_dev`만 소수 넷째
자리까지 불변**이다. 2회차 [1]이 "무변화 대조군은 섭동이 도달하지 않은 것과 구별이 안 된다"고
지적했고, 양성 채널이 그 구별을 만든다. 축 방향 섭동도 함께 넣었다 — **지배항은 f가 아니라
축 방향**이기 때문이다(AS-L8). f만 쓰면 이미 무효로 기록된 손잡이를 다시 잡는 것이다.

`seg_gap = 0`이므로 재투영 세그먼트는 언제나 대표 직선이고, 값을 정하는 것은 **잉크 모델의
활과 획 길이**뿐이다. **`lf_bow` 비 1:1.77:2.97에 중앙 비는 1:1.49:2.00** — "정비례"는 아니고
**등급마다 놓인 획 수가 달라 구도 구성이 다르다**(#11). AS-C10대로 **실획 현실을 주장하지 않는다.**

**임계를 걸지 않는다**(셋 다 보장, D-L11). `test/scene_cam.test.ts`(14건)가 불변식으로 잠그고
반례 셋을 넣었다 — 주점 뒤집기를 빼면 50px 이상, y 부호를 안 뒤집으면 100px 이상, 카메라 뒤는 `null`.

### 새로 드러난 관측 — **참 카메라에서도 coarse 배치가 무너진다** (리뷰어 2회차)

`chord_dev`의 분모를 적다가 나왔다. **참 카메라**를 준 배치율(등록 시드 6 × 잡음 4, 분모 1440):

| 등급 | 배치 | 비율 |
|---|---|---|
| precise | 908/1440 | 0.63 |
| medium | 868/1440 | 0.60 |
| **coarse** | **640/1440** | **0.44** |

`coarse`는 **`lift_gate`·`camera_gate`가 한 번도 안 잰 등급**이다(둘 다 precise·medium만 쓴다).
**카메라가 완벽해도 잉크가 거칠면 배치가 0.44로 내려간다.**
⚠ **`lift_gate`의 배치율과 나란히 읽지 않는다**(#27) — 분모(전체 획 대 최대 연결 성분)와
잡음 축 조합이 다르다. 원장에 `skew_note`로 적었다.

### 초판이 틀렸던 자리 (리뷰어 1회차 [2][3][10][11] · 2회차 [3])

| # | 무엇 | 고침 |
|---|---|---|
| 1-[3] | **"5구도 전부"가 등록 집합이 아니었다** — 3점 계열 다섯을 골라 놓고 그렇게 적었다. `setViewOffset`·무한원 소실점이 가장 다르게 도는 **2점·1점이 빠져** 있었다 | 등록 집합으로 바꿨다 |
| 2-[3] | **시드·잡음도 등록 집합이 아니었다** — 시드 4711 하나·잡음 0.01 하나에서 소수 넷째 자리를 인용했다(#14) | 등록 시드 6 × 잡음 4로 바꿨다. 대조군은 **한 동작점**임을 원장에 적었다(#12) |
| 1-[10] | **`three_vs_project_gap`이 정확히 0**이었다 — 우리가 다시 쓴 같은 산식과 비교했으니 **보장조차 아니고 항등**이었다 | `THREE.PerspectiveCamera`의 실제 투영행렬을 통과시킨다(5.0e-13). 반례로 `toBeGreaterThan(0)` |
| 1-[2] | `lift_gate.json`의 **같은 이름 `ink_gap_px`**는 장면당(n=54), 여기는 점 단위다 | 이름을 `chord_dev`로 갈랐다 |
| 1-[11] | **조건 블록이 없었다** | `conditions`를 넣고 등급 셋을 낸다 |

### 브라우저 확인을 원장으로 옮겼다 (리뷰어 1회차 [8], PITFALLS #25)

초판은 브라우저 수치가 **`progress.md`에만** 있었다 — L-A.2의 스윕 표가 죽은 것과 같은 자리다.
Playwright(`e2e/stage.spec.ts`)로 자동화해 `stage0/out/stage_browser.json`에 남긴다.
**1280×675 · 상자 하나 12획 · medium · 잡음 0.006**이고 가이드를 **참 소실점으로** 세운다.

| 확인 | 결과 |
|---|---|
| 확정 | 12/12 3D · f 1000.0px(참 1000) · 주점 (640.0, 337.5)(참값 일치) |
| 배선 항등 | **1.3e-13 px**(24점 최댓값) |
| 주점 비중심 경로 | 주점 (577.6, 422.4) · `offsetX` **124.7** → **1.3e-13** |
| 프레임 대조군 | CSS 프레임 1.3e-13 · **백버퍼 프레임 969.8px**(같은 24점 최댓값) |
| 창 축소 1280 → 820 | `offsetX` 124.7 → **0으로 다시 계산**. `SIZE_HEAL.count = 3` |
| 픽셀로 확인 | 3D 선분 중점 **12/12** 칠해짐 · **대조군** 빈 모서리 **0** |
| 궤도 | 카메라가 풀리고 `OrbitControls` 동작 · 2D 층 **0픽셀** · GL 2383px |
| 확정 시점으로 | 다시 물리고 2D 층 복귀 |

**⚠ 2회차 [8][9]: 이 대조군은 dpr 검사가 아니다.** 초판은 "dpr 대조군"이라 적었는데
되살린 것은 **측정 쪽 비교 프레임**이지 앱의 `canvasFrame` 규약이 아니다. 게다가 조건이
`deviceScaleFactor 1`이고 dpr 2는 **페이지 안에서 흉내낸 것**이라 브라우저 합성 경로를 안 탄다 —
**PITFALLS #21이 기록한 실패 자리가 정확히 그 조건이다.** 앱의 dpr 규약을 잠그는 것은
`e2e/coords.spec.ts`(dpr 1·2·3 프로젝트)이고, 이 스펙의 프레임 대조군은
"비교가 무엇을 해도 통과하지는 않는다"만 보인다. 원장에 `what_this_is_not`으로 적었다.

**이 확인이 말하지 않는 것**을 원장에 적었다: 배치 정확도 · 사람이 도달하는 축 오차
(**참 소실점을 그대로 넣었다**) · 실획 · 등급/구도(**동작점 하나**).

### DEFERRED의 핸들 예산 — **부분 해소**다 (리뷰어 2회차 [13])

초판은 "해소 · 약 4배"라고 적었다. **둘 다 틀렸다.**

- **[13-a] 잘림이 없어지지 않았다.** 새 뷰포트의 가이드는 **607.6 / 618.5 / 1233.3 / 1254.6 /
  1256.8 / 1327.7px**인데 `min_guide_ratio` 0.45는 **651px**을 요구한다 — **여섯 중 둘이 미달**이다.
  잘린 비율이 0.81 → 0.93으로 줄었을 뿐이다
- **[13-b] 배수는 가장 짧은 가이드로 읽는다.** **카메라 정확도는 가장 나쁜 축이 정한다.**
  짧은 쪽(≈600px) 1.28~4.33px 대 옛 한계(≈300px) 0.63~2.16px = **약 2배**다
- **[13-c] 그 예산도 "핸들 하나만 움직인다"는 가정** 위에 있다. `vp_homog`의 섭동은 단일
  핸들이고 사람은 **가이드 6개 = 핸들 12개**를 만진다 — **개별 핸들 예산이지 사용자가
  도달해야 할 정밀도가 아니다.** 그 구분을 §5.2·§12·AS-L9·DEFERRED에 적었다

| 가이드 길이 | 핸들 예산(축 0.5°, 각차 3~20°) |
|---|---|
| 140px (옛 기본) | 0.29 ~ 1.01px — 서브픽셀 |
| 300px (옛 브라우저 한계) | 0.63 ~ 2.16px |
| **600px (새 뷰포트의 짧은 가이드)** | **1.28 ~ 4.33px** |
| 1250px (긴 가이드) | 2.68 ~ 9.03px |

`vp_homog.json`에 **1250px 행을 더해 실측했다**(외삽 아님).

### selfcheck 강화 — 초판의 해석이 두 번 틀렸다 (리뷰어 1회차 [4] · 2회차 [4])

`near-zero` 규칙이 `0 < abs(val) < 1e-10`이라 **1e-11은 걸리고 0은 안 걸렸다.**
오차류 이름이면서 값이 정확히 0이면 플래그한다.

- **1회차 정정**: "389건 중 나머지는 옛 산출물의 정확한 0"이 틀렸다. 규칙이 **전체 경로**로
  매칭해 `…_err.n = 0`(**표본 0**)이 380건 중 **367건**이었다. `leaf` 매칭으로 고쳤다
- **2회차 정정**: 그 뒤 적은 "**옛 산출물에는 정확히 0인 오차가 없었다**"도 **틀렸다.**
  `lift_gate.json`의 `seg_gap_px`는 min~max가 전부 0이다. 새 규칙이 못 보는 이유는
  **꼬리(min·p10·p90·max) 제외 규약** 때문이고, 그래서 **`p90=0`이고 `max>0`인 분포형 오차는
  두 규칙 어디에도 안 걸린다** — 이 규칙이 겨냥한 결함의 주된 기록 형식이 바로 그것이다.
  **DEFERRED에 올린다**(꼬리 제외 규약을 건드리면 플래그가 폭증한 전례가 있다: 89 → 345)
- **"강화만 한다"도 부정확하다.** `low` → `leaf`는 **범위 축소**다. 축소로 사라진 것은
  전부 `.n`(표본 수)이고 오차값은 하나도 없지만, 그 사실을 함께 적어야 한다(#19)
- `"_dev"` → `"dev_"` — `service_worker_in_dev`가 오차로 잡혔다(오탐)
- 반례 7건: `tests/test_selfcheck_exact_zero.py`

### 문서 정정 (리뷰어 1회차 [6][7][12][14][15] · 2회차)

계획서 §5.4의 "0.995 / 0.079 / 0.026"을 뺐고(**§5.2만 고치고 여기는 안 고쳤었다**),
`camera_gate.json:target`의 폐기 대역 0.0791을 `registered_band` 0.0673/0.0296으로 바꿨다
(**문서만 고치면 다음 세션이 원장에서 다시 인용한다**). §5.2·§13의 "각도 파라미터화가 발산을
없앤다"를 표현/카메라로 갈랐고 §13의 항목을 **반증됨**으로 옮겼다. §5.4↔§5.2 교차 참조를
원장 `plan` 필드까지 고쳤다. 2회차로 **`HANDOFF.md` 4곳**(0.079·0.995·≈1°·PITFALLS 26개),
**AS-L2·AS-L8의 폐기 대역**, **`vp_homog.json`의 "0.12 × 대각 = 140px"**(상수는 0.45다),
**§12의 "0.29~4px"**를 고쳤다. `DECISIONS.md`에 D-L9~D-L11.

### 남긴 것 (2회차 중 대응 안 한 것 — CLAUDE.md §4대로 DEFERRED)

- **분포형 오차의 정확한 0을 selfcheck가 못 본다**(2회차 [4]). 꼬리 제외 규약과 충돌한다
- **핸들 예산의 다중 핸들 판**(2회차 [13-c]). 핸들 12개가 독립으로 어긋날 때의 축 오차는 미측정
- **L-B.1의 사전 등록 절이 없다**(2회차 #26). L-B 게이트는 §11에 있으나 소단계 등록은 없다.
  다만 이 항목의 산출은 **전부 보장**이라 통과/실패를 가릴 임계가 애초에 없다

### 검증

vitest **233/233**(39파일) · tsc 0 · pytest **44/44** · playwright **9/9**(새 스펙 1) ·
selfcheck 1451 · **STALE 0** · 상수 해시 `2fd74e1c` 불변.

---

## L-B.2 — 가이드 조정 UI와 실시간 피드백 (계획서 §5.2)

_리뷰어 13건 대응 후의 판이다. **초판은 이 절을 `web/progress.md`에 썼다** — 쉘 작업 디렉토리가
`web/`이었기 때문이고, 그러면 **다음 세션이 이 항목을 못 본다**(HANDOFF가 루트 `progress.md`를
가리킨다). 지웠고 여기가 유일한 원장이다._

### 착수 시 PITFALLS 대조

읽었다. 걸리는 번호: **#29(여유는 사용자가 만지는 양으로)** · **#17(측정 경로와 앱 경로)** ·
#3(정의의 귀결) · #6(대조군) · #10(뺄셈 금지) · #11(분모) · #12(동작점 하나) · #25 · #26.
리뷰어가 **적지 않았는데 걸린 것**을 더 짚었다: #1 · #5 · #7 · #8 · #14 · #16 · #22 · #27.

**#29가 이 항목의 전부다.** L-A.7이 잰 "핸들 1px이 축을 몇 도 움직이는가"는 **원장에만 있었다.**
자동 판정이 원리적으로 불가능해 사용자의 눈이 유일한 판정 수단인데(§5.3)
**눈은 자기 손이 얼마나 정밀해야 하는지를 못 본다.** 그 값을 화면으로 옮긴다.

### 무엇을 만들었나

```
src/s3d/vpSensitivity.ts   핸들 1px → 축 방향 몇 도. **앱과 하네스가 같이 쓴다**(#17)
src/s3d/vpDraft.ts (추가)  guideLineAt / moveGuideBy / extendGuide — 선 자체를 잡는다
src/mainL.ts (추가)        축별 예산 표시 · 안 올라가는 획의 사유 · `가이드 늘리기`
```

`SENS_TOL`을 `test/constants.ts`에 넣었다(D-C4). 상수 해시 `1a6f104b` → **`2fd74e1c`**로
바뀌어 전 산출물을 다시 돌렸고 문서 인용 20곳과 **`vpDraft.ts`의 주석**을 고쳤다 —
그 주석은 구 해시와 함께 **철회된 "축 오차 1°"·"29~102px"**을 들고 있었다(PITFALLS #1·#29).

### 결과 — `stage0/out/guide_budget.json@1b6175a4`

**⚠ 사전 등록의 통과선 둘이 반증 불가였다**(리뷰어 [3][4]).
① "늘리면 예산이 는다"는 지렛대가 길어지면 deg/px가 단조 감소하므로 정의의 귀결이고,
② "카메라가 안 바뀐다"는 같은 직선 위에서 늘리므로 `v = ℓ₁ × ℓ₂`의 **항등**이다.
**대조군이 아니었다** — L-B.1의 `chord_dev`에서 배운 것과 같은 형태다(#5·#6).
등록의 실질은 "느는가"가 아니라 **"얼마나, 그리고 필요한 값에 닿는가"**뿐이다.

| | 값 |
|---|---|
| 카메라가 서는 경우 | **31/90** |
| 이득 비 | 최소 1.00 · 중앙 **1.3** · 최대 6.8 (n=31) |
| 요구 길이 미달 가이드 | **185/320 → 114/320** (직접 센 수, #10) |

**구도별 최소 예산**(늘린 뒤). 축에 적용한 "가장 나쁜 것이 정한다"를 **구도에도 적용한다**(#8·#16):

| 구도 | n | 최소 | 최대 | **1px 미만** |
|---|---|---|---|---|
| 3pt_yaw20_pitch25 | 3 | 3.32 | 5.58 | 0 |
| 3pt_yaw50_pitch8 | 8 | **0.73** | 5.07 | 3 |
| 3pt_yaw35_pitch15 | 13 | **0.69** | 6.18 | 2 |
| 2pt_yaw35_pitch0 | 4 | **0.23** | 2.92 | 1 |
| **1pt_yaw0_pitch0** | 3 | **0.12** | 0.96 | **3/3** |

**1점 구도는 늘린 뒤에도 세 시드 전부가 서브픽셀**이고 31셀 중 9셀이 1px 미만이다.
AS-L9에 적혀 있던 **1.3~4.3px은 참 소실점을 넣은 낙관값**이었다 — 검출 초안 실측은 이쪽이다.

### 리뷰어가 뒤집은 것 둘

**① 남는 미달은 그림 탓이 아니라 캔버스 탓이다.** 늘린 뒤 길이는 **언제나 그 방향의 캔버스 안
현**이다. 1280×675에서 가장 짧은 현(수직)은 **588.18px**(675 − 여백 2×43.4)이고
요구는 **651.2px**이라 **수직에 가까운 가이드는 어떤 그림에서도 미달**이다 —
미달 152개 중 **26개가 정확히 그 588 대역**이다. `DEFERRED.md`의 옛 원인 귀속
("길이를 그림이 정한다")을 뒤집어 적었다. `min_guide_ratio`를 캔버스 **짧은 변** 기준으로
다시 정하는 것이 남았다(DEFERRED).

**② 31/90을 "§5.1의 확인"으로 읽으면 안 된다.** `vp_detect.json@1b6175a4`는 **같은 잡음**에서
확정률 **0.883**이다. 0.88 → 0.34를 만든 것은 검출이 아니라 **소실점을 가이드 선 두 개로
줄이는 초안 변환**이다. 사유 카운터를 넣었다(#7 — 추측하지 말고 센다):

| 사유 | 수 |
|---|---|
| 가이드 4개 미만 | 21 |
| 축 둘 미만 | 8 |
| **`recoverCamera` 실패** | **30** |
| 성립 | 31 |

**지배항은 `recover_failed` 30이다.** 가이드는 있는데 카메라가 안 선다 — 어느 단계에서
무엇을 잃는지는 `vpDraft`의 지지선 선택을 봐야 하고 L-B.2의 범위가 아니다(DEFERRED).

### 브라우저 (Playwright, `stage_browser.json`의 `l_b2`)

| 확인 | 결과 |
|---|---|
| 검출 초안만 | 가이드 5개(236 / 379 / 589 / 591 / 1206px) · **카메라 안 섬** · 민감도 **안 뜸** |
| 참 소실점을 넣은 뒤 | 축별 예산 **2.5 / 2.3 / 2.2px** (0.20 / 0.22 / 0.23°/px) |
| 선 끌기 | 방향 **불변** · 소실점이 (2118.5, 69.6) → (2214.6, 46.5)로 미끄러진다 |

**민감도가 안 뜨는 것이 옳은 동작이다** — 카메라가 안 서면 **없는 숫자를 지어내지 않는다**(A-3).

⚠ **초판은 "축3이 가장 짧고 예산도 가장 좁다 — 표가 그렇게 나온다"고 적었는데 틀렸다**
(리뷰어). 축1(1233px)이 축0(1226px)보다 길면서 예산은 좁다. **길이 순서와 예산 순서가
어긋난다** — 예산을 정하는 것은 길이만이 아니라 **각차와 함께**다. `vp_homog`의 길이 스윕과
이 세 행을 나란히 읽지 않는다(#27): 그 표는 각차를 고정한 것이고 여기는 각차가 축마다 다르다.

⚠ **`vp_homog`의 1250px 행(2.68~9.03)과 브라우저의 2.2~2.5px도 그래서 못 겹친다.**
정의는 같은 양이지만 **(길이, 각차) 쌍**으로만 비교된다.

### 종단에서 잡은 결함 하나

`sens`를 처음에는 `소실점 추정`과 끌기 종료에서만 갱신했다. 그러면 가이드가 다른 경로로
바뀔 때 **낡은 값이 남는다** — Playwright가 실제로 걸었다. 갱신을 **`refresh()` 한 곳으로**
모았다(끄는 중에는 건너뛴다). AS-C7의 자가 치유와 같은 형태다.

### 반례 테스트와 그 한계

`test/vp_sensitivity.test.ts`(10건). ① 짧게 하면 예산이 준다 ② 각차를 좁히면 준다
③ 예산 × 민감도 = 0.5° ④ 카메라가 안 서면 `null` ⑤ `shortestGuidePx`가 짧은 쪽
⑥ `extendGuide`는 f·소실점 불변 ⑦ `moveGuideBy`는 방향 유지·소실점 이동 ⑧ `guideLineAt` 반경.

⚠ **리뷰어가 판별력을 좁혔다**: ③과 ⑥은 **항등**이고, ①②는 수치 미분의 단조성이라
**"입력을 아예 안 읽는" 배선 오류만** 잡는다. **값 자체를 독립으로 확인한 항목이 없다.**
그리고 `vp_homog_measure`가 이제 같은 `vpSensitivity.ts`를 쓰므로 두 산출물의 일치는
**교차검증이 아니라 같은 코드**다 — #17을 지킨 대가다. 해석해나 `probe_px` 변경 대조는 DEFERRED.

### 유효 자릿수

셀 넷이 n=0~1이고 31 중 13(42%)이 한 구도다. AS-C9대로 **유효 자릿수 2**로 적었다
(1.3배 · 0.12px). 최대 6.8배는 **n=1 셀**에서 나온 값이라 대표값으로 쓰지 않는다.

### 검증

vitest **244/244**(41파일) · tsc 0 · pytest 44/44 · playwright **10/10** ·
selfcheck 1451 · **STALE 0** · 상수 해시 **`2fd74e1c`**.

---

## L-B.3 — 스냅 (계획서 §3)

### 착수 시 PITFALLS 대조

읽었다(**#30을 새로 추가한 직후**다 — 사람 지시로 이 항목 착수 전에 넣었다).
걸리는 번호:

- **#2·#4(자기확인·모델이 문제를 만들지 못한다)** — **이 항목의 전부다.** 합성 잉크의 끝점은
  `endJitter = 0`이면 참 모서리에 **못으로 박혀 있다**(`scene3d.ts` 머리말이 그렇게 적었다).
  그 상태로 스냅 성공률을 재면 **1.0이 나오는데 그것은 스냅의 성질이 아니라 픽스처의 성질**이다.
  → `endJitter`를 반드시 스윕한다.
- **#12·#13(동작점 하나·절단값)** — 스냅 반경을 **하나 고르지 않는다.** 스윕해서 전부 낸다.
- **#9·#10·#11(무엇이 섞였나·뺄셈 금지·분모)** — 성공률을 **맞음/틀림/안 붙음** 셋으로 가른다.
  셋을 각각 직접 세고 분모는 시작점 총수다.
- **#6·#30(대조군·양성 채널)** — "스냅이 배치를 회복한다"는 주장에는 **스냅 없는 대조군**과
  **섭동이 도달했음을 보이는 양성 채널**이 함께 있어야 한다.
- **#17(측정 경로와 앱 경로)** — `snap.ts` 하나를 앱과 하네스가 같이 쓴다.
- **#14(시드 변동폭)** — 시드 6 · 5구도. 비율보다 **분자/분모**로 적는다.
- **#25·#26(원장·사전 등록)** — `stage0/out/snap.json`. 판정 규칙을 **측정 전에** 아래에 박는다.
- **#16(병목이 옮겨간 것)** — 스냅이 배치율을 올리면서 **조용히 틀린 배치**를 늘리는지 함께 본다.
- **#8(꼬리)** · **#1(원장 재확인)** · **#19(설명이 검사를 끄지 않게)** · **#22(캔버스 굳음)**.

### 사전 등록 (측정 전에 박는다, #26)

| | |
|---|---|
| 스윕 축 | 반경비 {0.008, 0.012, 0.018, 0.025, 0.035, 0.05} × 등급 3 × `endJitter` {0, 0.01, 0.03, 0.05} × 시드 6 × 5구도 |
| 판정 | 시작점이 **그 획이 겨냥한 참 모서리 끝점**에 붙으면 맞음 · **다른 참 대상**에 붙으면 **틀림** · 아무 데도 안 붙으면 미스 |
| 반경 선택 규칙 | ① **맞음 − 틀림**이 최대인 반경, ② **틀림 ≤ 0.1 × 맞음**을 만족하는 최대 반경. **둘이 다르면 둘 다 적는다**(#28) |
| 대조군 | 스냅 끔 · 지면(`on_face`) 제외 — `on_face`는 화면 거리가 **정의상 0**이라 넣으면 성공률이 공짜로 1이 된다 |
| 반증 조건 | `endJitter = 0`에서만 좋고 0.03·0.05에서 무너지면 그것은 픽스처를 잰 것이다(#2) |

### 무엇을 만들었나

```
src/s3d/snap.ts       끝점·중점·교차점·수선 발·선 위·면 위 + 우선순위·반경. **앱과 하네스가 같이 쓴다**(#17)
src/s3d/liveLine.ts   앵커 + 축 → 커서 광선의 최근접점으로 끝점 3D 결정 (§3 마지막 문단·§7)
src/capture/inkCanvas.ts (추가)  `onHover` — 누르기 전에 표식을 보인다(좌표는 `canvasFrame` 단일 출처)
src/mainL.ts (추가)   스냅 표식·상태 표시·시작점 확정 경로
```

`SNAP_TOL`·`LIVE_TOL`을 `test/constants.ts`에 넣었다(D-C4). 상수 해시가 바뀌어 전 산출물을 다시 돌렸다.
⚠ **초판은 여기와 아래 절 제목에 `dbcf3a11`을 적었는데 그 실행은 디스크에 없다**(리뷰어 2회차 [3]) —
이후 `DRAFT_TOL`에 둘이 더 들어가 최종 해시는 **`2fd74e1c`**이다. **인용은 최종 해시 하나로 통일한다.**

**교차점은 3D 교차만 쓴다.** 화면 교차는 대개 가림이고 `lift.ts`가 이미 그 자리에서
걸렸다(상자 하나에서 27개 중 3개). Rhino의 `Int`도 실제 교차이므로 **선례가 같은 쪽**이다.

**`on_face`의 대상은 지면 하나다**(면 생성이 범위 밖, DEFERRED). 그리고 그 화면 거리는
**정의상 0**이라(커서 광선이 그 평면과 만나는 점이므로) 우선순위 최하위로 두고
**측정에서는 끈다** — 넣으면 성공률이 공짜로 1이 된다(#3).

### 스냅이 화면 연산인 이유 — 새로 설계한 것이 없다

올라간 기하는 되쏘면 **정확히 그 화면 점**으로 돌아온다(`lift.ts`의 `segGap = 0` 보장).
그래서 **"3D 대상에 붙인다"와 "그 대상의 상으로 `pts2d[0]`을 옮긴다"가 같은 연산**이다.
솔버를 고칠 필요가 없었다.

⚠ **그 귀결로 `start_reprojection_gap_px`는 항등이다**(#5) — 배선 검사이지 정확도가 아니다.
e2e 산출물 이름에 `_IDENTITY`를 붙였다.

### 결과 — `stage0/out/snap.json@1b6175a4`

**① 사전 등록한 스윕이 경계에서 끝났다.** 등록한 두 규칙이 **둘 다 0.05(스윕의 끝)**를
골랐다 — 최적이 밖에 있었다는 뜻이고 그 표로는 "0.05가 좋다"가 아니라 **"더 키워도
좋아진다"**까지만 말할 수 있다. 정확히 #13의 상태다. **규칙은 그대로 두고 스윕만 넓혔다**
(0.07 / 0.10 / 0.14 / 0.20 / 0.28 추가).

**② 이 잉크 모델에서는 등급이 시작점 겨냥 오차를 거의 안 바꾼다.** 중앙 **7.6 / 7.6 / 8.0px**(유효 자릿수 2, AS-C9)
(precise / medium / coarse). `renderInk`의 활이 **양끝에서 정확히 0**이라 `lf_bow`가
시작점에 안 닿는다 — 정하는 것은 `endJitter`다(중앙 **0.13 / 4.7 / 14.7 / 23.8px**).
→ **`coarse` 배치가 무너지는 원인을 시작점 겨냥에서 찾으면 안 된다.**

**③ 넓힌 스윕에서도 등록한 두 규칙이 실패했다.** A는 반경을 키우는 **비용이 픽스처에 없어서**
계속 커지고(0.20 = 캔버스 대각의 1/5), B는 대상을 촘촘하게 하면 **어떤 반경도 만족하지 못한다.**
그래서 규칙 C(**포화점** — 미스 ≤ 1%이고 다음 칸의 이득 ≤ 1%인 가장 작은 반경)를
**사후에 추가하고 사유를 원장에 적었다**(#28).

**④ 대상 밀도가 결론을 바꾼다** — 이것이 이 항목에서 가장 중요한 발견이다.
`boxLattice`로 대상을 늘려 같은 스윕을 다시 돌렸다(등급 medium · 시드 2 · 잡음 3 · 5구도):

| | 획 | 참 꼭짓점 간격 중앙 | 규칙 A | 규칙 B | **규칙 C** |
|---|---|---|---|---|---|
| k1(상자) | 12 | 121.2px | 0.14 | 0.28 | 0.10 |
| k2 | 54 | 40.5px | 0.14 | **없음** | 0.07 |
| **k3** | 144 | **21.3px** | 0.10 | **없음** | **0.05** |

k3에서 **틀린 붙음이 반경과 무관하게 15076/38880에서 포화한다**(전부 `endpoint` — 이웃
꼭짓점을 문 것이다). 겨냥 오차 중앙 14.7px이 꼭짓점 간격 21.3px의 절반을 넘기 때문이다.
→ **한계를 정하는 것은 반경이 아니라 (겨냥 오차 / 대상 간격)의 비**다. 스냅으로 못 고치는 몫이 있다.

**`SNAP_TOL.radius_ratio = 0.05`**(960×672에서 58.6px)로 정했다 — **가장 촘촘한 k3의
포화점**이다. 성긴 쪽에서 고르면 촘촘해질 때 못 쓴다. 선례(SketchUp/Rhino의 10~15px)보다
훨씬 크고, 이유는 **손 획의 겨냥 오차 자체가 크기 때문**이다.

**⑤ 무엇에 붙었나**(#7). 반경 0.05에서 끝점 4170 · 선 위 60 · 중점 34 · 미스 56(분모 4320).
틀린 것의 종류가 반경에 따라 **성질이 바뀐다**(#16): 작은 반경에서는 `on_edge`
(모서리를 못 찾아 옆 선 몸통에 붙음)가 817/836인데, 큰 반경에서는 `endpoint`
(**다른 꼭짓점**을 뭄)가 21/21이 된다. 병목이 없어진 것이 아니라 옮겨간 것이다.

### 브라우저 (Playwright, `stage_browser.json`의 `l_b3`)

**실제 포인터로 긋는다**(`page.mouse`) — 꼭짓점에서 13·12px 어긋나게 시작해서
같은 모서리 방향으로 긋는다. 결과: 표식 `끝점`(17.2px) · `snapStart.ofId = s6` ·
축 0으로 확정 · 배치됨. **대조군**으로 빈 자리(캔버스 모서리)에서 그으면
붙지 않고 **2D로 대기**한다(§9.1 — 실패가 아니다).

### 반례 테스트 (`test/snap.test.ts`, 11건)

L-B.2에서 "항등과 단조성뿐"이라고 지적받은 자리를 피해 **값을 독립으로 확인하는 항목**을 넣었다:
① 우선순위가 거리를 이긴다(중점 위에 커서를 둬도 끝점이 이긴다. 반경을 좁히면 중점이 이겨
**순위가 갈랐는지 반경이 갈랐는지 구분된다**) ② **중점은 3D 중점의 상이지 화면 중점이 아니다**
(둘이 20px 넘게 갈리는 것을 먼저 확인하고 — #4 — 화면 중점에서는 후보가 **안 나오는** 것까지 본다)
③ **화면에서만 겹치는 두 선분은 교차점을 안 낸다**(두 선분이 같은 화면 점을 지난다는 것을
먼저 확인하고, `on_edge`는 **둘 다 나오는** 것으로 "후보가 없어서"가 아님을 가른다)
④ 3D에서 만나면 몸통 교차라도 낸다 ⑤ 수선 발의 **직교성을 독립 계산으로** 확인
⑥ 공유 모서리는 끝점 후보 하나 ⑦ 반경 밖이면 `null` ⑧ 카메라 뒤는 후보 없음
⑨ 지면은 `ground`가 있어야 나오고 최하위 ⑩ `geomScale`이 배율 10배에서 같은 판정
⑪ 우선순위 목록이 계획서 §3 그대로.

### 리뷰어 12건 대응 (1회차)

**[1] 등록한 대조군을 안 돌렸다 — 가장 큰 지적.** 사전 등록 표에 "대조군: 스냅 끔"을 적어 놓고
`snap.json`에 `control` 키가 하나도 없었다. 계획서 §11 L-B 게이트의 **두 번째 항목**
("스냅된 시작점에서의 배치 정확도")도 측정 대상이 아니었다. → `placement_from_snapped_start`를
넣었다. **앱과 같은 경로**다(시작점을 대상의 상으로 옮김 → `nearestAxisOnScreen` → `segmentFromAnchor`).
"스냅 끔"은 **설계상 0을 놓는다**(앵커가 없다) — 측정이 아니라 사실이므로 팔로 돌리지 않고 원장에 적었다.

| 반경 | 배치 | 스냅 실패 | 축 실패 | 형태 오차 중앙 | **조용히 틀림 0.2** |
|---|---|---|---|---|---|
| 0.025 | 3168/4320 | 295 | 857 | 0.019 | **532/3168** |
| **0.05** | **3403/4320** | 56 | 861 | 0.020 | **569/3403** |
| 0.10 | 3459/4320 | 1 | 860 | 0.022 | **686/3459** |

**놓인 것의 17%가 조용히 틀린다**(cut 0.2). 그리고 cut 0.5가 504/569이라 **틀린 것은 크게 틀린다.**
축 실패 861이 스냅 실패보다 훨씬 크다 — L-B.4가 볼 자리다.

**[2] 반경 선택의 논거가 데이터와 반대였다.** "성긴 쪽에서 고르면 촘촘해질 때 못 쓴다"고 적었는데
세 밀도 **전부에서 0.10이 맞음↑·틀림↓·미스↓**였다. **이 픽스처에 반경을 키우는 비용이 없었기
때문**이다. → 비용을 **측정에 넣었다**: `unintended_snap`(기존 기하와 무관한 자리에서 시작하려 할 때
스냅이 끼어드는 비율, 캔버스 균등 난수 24점/픽스처).

| k3 | 맞음 | 끼어듦 |
|---|---|---|
| 0.025 | 2764/4320 | 1440/6480 |
| **0.05** | 2963 (**+199**) | 1854 (**+414**) |
| 0.10 | 2990 (**+27**) | 2772 (**+918**) |

**0.10은 얻는 것이 거의 없고 잃는 것이 크다.** 0.05를 그대로 두되 **근거를 바꿔 적었다**.

**[3] 규칙 C의 절단 감도가 없었다.** 0.5% / 1% / 2%를 다 낸다. k3에서 **0.05 / 0.05 / 0.035**,
k1·k2에서 0.10~0.05. **절단 2%면 0.035**라는 사실을 원장에 적었다.

**[4] 밀도 표가 등급 1·시드 2였다** — 그런데 **앱 상수가 그 표에서 나왔다**. 등록 조건
(등급 3 · 시드 6)으로 다시 돌렸다. 분모가 k1 3240 / k2 14580 / k3 38880으로 늘었고
**규칙 C의 답은 안 바뀌었다**(k3 0.05).

**[5] k1의 겨냥 오차를 k3의 간격과 나란히 놓았다**(#27). 밀도별로 직접 쟀다 —
**k1 12.63 · k2 12.01 · k3 12.19px**(중앙). 거의 같다(`drawEdges`의 `endJitter`가 **획이 아니라
그림 전체 대각**에 비례하기 때문이다). 그래서 비 논거는 **같은 밀도 안에서** 성립한다:
k3에서 겨냥 오차 중앙 12.2px 대 꼭짓점 간격 절반 10.65px.

**[6] "등급이 안 바꾼다"는 잉크 모델의 성질일 수 있다.** 맞다. 끝점에 닿는 유일한 성분인
`hf_sigma`가 등급 간 **0.0310 / 0.0336 / 0.0314 — 8%밖에 안 다르다**. 결론의 사정거리를
**"이 잉크 모델에서는"**으로 좁혀 적었고, 유효 자릿수도 2로 고쳤다(**7.6 / 7.6 / 8.0px**).

**[7] `@addaeb9d` 29곳** → `@2fd74e1c`로 고쳤다(`grep` 0건 확인). HANDOFF의 "PITFALLS 29개"도 30개로.

**[8] `assumptions.md` AS-L4·AS-C2**를 이번 관측으로 갱신하고 `DECISIONS.md`에 **D-L12~D-L14**를 넣었다.

**[9] `lift_grade.json`이 progress에 없었다** → 아래 L-B.3(a) 절.

**[10] `snapped_kind_counts`가 결론 집합을 안 썼다** — 못으로 박힌 `end_jitter = 0`의 1080셀이
섞여 있었다. `rows_end_jitter_gt_0`를 나란히 넣었다.

**[11] 앱 캔버스 px 병기 · `l_b3`의 `size_heal_count`** 추가. 앱(1280×675)에서 반경은 **72px**이고
하네스(960×672)에서는 58.6px이다 — 선례와 비교할 때 어느 쪽인지 밝힌다.

**[12] (a) 판정 허용오차가 사전 등록에 없었다** → 1/200·1/100·1/50 스윕. 반경 0.05에서
맞음 3858 / 3870 / 3938(분모 4320)로 **거의 안 움직인다** — 결론을 정하는 값이 아니다.
**(b) 승격에서 스냅이 조용히 풀린다.** 옮겨 놓은 `pts2d[0]`은 **옛 카메라가 만든 대상의 상**이라
새 카메라에서는 그 대상의 상이 다른 자리다. `snapStart.ofId`를 다시 풀어야 한다 —
`applySnapToStart`에 경고를 달고 **DEFERRED(L-C)**에 올렸다.

---

## L-B.3(a) — 참 카메라에서 `coarse` 배치가 0.44인 원인 (사람 지시)

### 착수 시 PITFALLS 대조

걸리는 번호: **#10(뺄셈 금지 — 각 팔을 직접 돌린다)** · **#16(병목 이동)** · #1(같은 양이 두 숫자) ·
#7(사유 카운터) · #8(꼬리) · #11(분모) · #12·#13(동작점·절단) · #25 · #27(하네스 간 비교) · #30(양성 채널).

**#1이 이 항목에서 실제로 걸렸다** — 형태 오차를 재려고 `camera_gate.test.ts`의 지역 함수를
복사할 뻔했다. 계획서 §5.4가 "0.026 대 0.0296"으로 **같은 양을 두 숫자로** 돌린 것이 그 사고다.
`perStrokeError`를 `test/scene3d.ts`로 옮겨 **하나만 남겼다**.

### 방법 — 단계를 오라클로 대체한다

`liftAll`은 세 단계다. 각 단계를 오라클로 바꾼 팔을 **직접 돌린다**(뺄셈 아님, #10).

### 재현 확인 — 다른 것을 재고 있지 않은가

| | precise | medium | coarse |
|---|---|---|---|
| `stage_cam.json` | 0.6306 | 0.6028 | 0.4444 |
| **여기** | **908/1440 = 0.6306** | **868/1440 = 0.6028** | **640/1440 = 0.4444** |

**정확히 재현한다.** 구도·시드 산식·skew·잡음 축을 `stage_cam`과 같게 맞췄기 때문이다(#27).

### 답 — **축 판정이 지배항이다**

| 팔 | precise | medium | coarse |
|---|---|---|---|
| `judged` (실제) | 908/1440 | 868/1440 | **640/1440** |
| **`oracle_axis`** | **1257/1440** | **1266/1440** | **1187/1440** |
| `oracle_axis` + `touch_ratio` 0.09 | 1440/1440 | — | 1440/1440 |

**축을 오라클로 주면 `coarse`가 0.44 → 0.82로 오르고 등급 격차가 0.19 → 0.05로 거의 사라진다.**
미배치 사유(`liftAll`이 이미 돌려주고 있었는데 아무도 안 셌다):

| 사유 | precise | medium | coarse |
|---|---|---|---|
| **축이 미분류다** | 316 | 367 | **533** |
| 구조에 이어지지 않았다 | 189 | 194 | 237 |
| 어느 획과도 교점이 없다 | 27 | 11 | 30 |

**오라클 축에서는 `축이 미분류다`가 0이 되고 `구조에 이어지지 않았다`만 남는다**(183/174/253).
그것은 접합 반경이 정하고, 0.09로 키우면 **1440/1440**이 놓인다.

**조건수는 "못 푸는" 문제가 아니라 "틀리게 푸는" 문제다.** `교점 제약이 형태를 정하지 못한다`가
**0건**이다 — `solveNull`은 언제나 답을 낸다. 대신 형태 오차 중앙이 어느 팔에서도 **0.10~0.18**이고
놓인 것의 **cut 0.2 조용히 틀림이 34~39%**다.

⚠ **고칠 때마다 조용히 틀림이 함께 는다**(#16): 오라클 축 기본 반경 496/1257(39%) →
오라클 축 + 0.09 **954/1440(66%)**. **배치율만 보면 반경을 키우는 것이 이득으로 보이는데 아니다.**

### ⚠ 스냅은 이 0.44를 못 고친다

스냅 대상은 **3D 레이어**인데(§9.1) **초기 확정 순간에는 3D가 아직 없다** — 모든 획이 2D 대기이고
대상이 0이다. 스냅이 닿는 것은 **확정 뒤에 그리는 획**뿐이다.
0.44에 닿는 것은 **(1) 확정 시점의 축 판정(L-B.4) (2) 승격 연쇄(L-B.7)**다.
`DEFERRED.md`·`HANDOFF.md`가 "L-B.3 뒤에 다시 잰다"로 적어 둔 전제를 **뒤집어 적었다**.

### 동작점

잡음이 지배적으로 크다 — `coarse` 배치가 잡음 0에서 234/360인데 **0.05에서 70/360**이다.
구도별로는 1점이 형태 오차 최악(중앙 0.18~0.33). **동작점 하나로 말하지 않는다**(#12·#8).

---

## L-B.3(b) — 초안 변환이 확정률 0.88을 0.34로 떨어뜨리는 이유 (사람 지시)

### 착수 시 PITFALLS 대조

걸리는 번호: **#16(확정률만 보면 안 된다 — 서기만 하고 틀리면 더 나쁘다)** · **#30(양성 채널)** ·
#6(대조군) · #7(카운터) · #10·#11(직접 센 집합·분모 합 검사) · #24(재매개화는 아무것도 안 고친다) ·
#25 · #27(같은 조건).

### 가설과 그 시험

`draftFromDetection`이 소실점마다 **가장 긴 둘**을 고르는데, 소실점은 **두 선의 교점**이므로
조건을 정하는 것은 길이만이 아니라 **각차**다. 각차가 좁으면 L-A.7의 **무한원 스냅**(3°)이 그 축을
비우고 카메라가 안 선다. **이것이 사람 지시의 "각도 파라미터화 전환과 관련 있는가"의 정확한 형태다.**

### 답 — 관련 있지만 지배항은 아니다

| 팔 | 카메라 성립 | 무한원 스냅된 축 | 가이드 쌍 각차 중앙 | **축 오차 중앙** | f 상대오차 |
|---|---|---|---|---|---|
| `longest` + 스냅 3°(현행) | **31/90** | 42 | 7.18° | 12.35° | 0.30 |
| `longest` + 스냅 **끔** | 40/90 | 0 | 7.18° | 13.80° | 0.32 |
| `widest` + 스냅 3° | **44/90** | **0** | 14.24° | 9.96° | 0.29 |
| **`len_x_sep` + 스냅 3°** | **43/90** | **0** | 13.94° | **9.05°** | **0.24** |
| **중립 시작**(그림 무시) | **90/90** | 90 | — | **13.83°** | 0.41 |

**31/90을 정확히 재현한다.** 그리고:

- **무한원 스냅은 9/90을 설명한다**(31 → 40). 관련은 있으나 지배항이 아니다
- **지배항은 지지선 선택이다.** `longest`의 가이드 쌍 각차는 **최소 0.03°**다 — 가장 긴 둘이
  하필 나란한 경우다. `widest`·`len_x_sep`은 최소 3.78°·3.36°이고 **무한원 스냅에 아예 안 걸린다**
- ⚠ **확정률만 보면 안 된다**(#16). **어느 팔에서도 축 오차 중앙이 9~14°로 임계 0.5°의 18~28배다.**
  초안 카메라는 **어차피 쓸 수 없다** — §5.1이 이미 그렇게 적었다. 초안의 가치는 **끌기 시작 위치**다
- **그래서 중립 시작과 비교했다**(사람 지시): 중립은 카메라가 **언제나 서지만**(90/90)
  축 오차 13.83° · f 0.41로 **초안(9.05° · 0.24)보다 나쁘다.** **초안이 중립보다 낫다.**

### 채택 — `len_x_sep` + **빈 축만 중립으로 채운다**

`widest`가 확정률 1건 높지만 `len_x_sep`이 **축 오차·f 둘 다 낫다**. 길이도 함께 보므로
핸들 예산에도 유리하다. `DRAFT_TOL.support_pick = "len_x_sep"`.

그리고 **`guides_lt_4` 21/90은 어느 팔에서도 안 줄었다** — 검출이 축을 못 채운 것이다.
그 축은 화면에 **손잡이가 아예 없어 사용자가 끌 것이 없다.** 조정 UI의 전제가 깨지므로
`DRAFT_TOL.fill_missing_axes = true`로 **빈 축만** 중립 위치로 채운다(있는 축은 안 건드린다 —
초안이 중립보다 낫기 때문이다).

### 그 결과 `guide_budget`가 바뀌었다 — **31/90 → 58/90**

| | 옛값(`longest`, 채우기 없음) | **새값(`len_x_sep` + 채우기)** |
|---|---|---|
| 카메라 성립 | 31/90 | **58/90** |
| 이득 비 중앙 | 1.3 | 1.30 |
| 요구 길이 미달 | 185/320 | **347/540** → **249/540** |

**구도별 최소 핸들 예산**(늘린 뒤):

| 구도 | n | 최소 | 최대 | 1px 미만 |
|---|---|---|---|---|
| 3pt_yaw35_pitch15 | 13 | 0.95 | 7.52 | 1 |
| 3pt_yaw20_pitch25 | 14 | **0.63** | 3.25 | 4 |
| 3pt_yaw50_pitch8 | 17 | 0.80 | 7.23 | 4 |
| 2pt_yaw35_pitch0 | 8 | **0.465** | 2.86 | 2 |
| 1pt_yaw0_pitch0 | 6 | **0.547** | 1.09 | 4 |

⚠ **행 간 비교가 같은 양의 비교가 아니다**(#11). n이 3~4에서 6~17로 늘었으므로
**최소값이 내려간 행은 나빠진 것이 아니라 표본이 넓어진 것**이다(3pt_yaw35가 3.32 → 0.95인 것이 그 예).
서브픽셀 셀 비율은 9/31(29%) → **15/58(26%)**로 거의 같다.
**1점 구도가 서브픽셀이던 문제는 0.12 → 0.547px로 4.5배 나아졌지만 여전히 1px 미만이다.**

---

## L-B.4 — 축 판정과 실시간 색, 미리보기 선 (계획서 §4)

### 착수 시 PITFALLS 대조

읽었다. 걸리는 번호:

- **#16(병목이 옮겨간 것)** — **이 항목의 전부다.** L-B.3(a)가 "축을 오라클로 주면 0.44 → 0.82"를
  냈지만 **같은 팔에서 조용히 틀림이 39%**다. 임계를 풀어 배치를 늘리면 조용히 틀림이 함께 는다.
  **배치율만 보면 반대로 간다.**
- **#7(추측하지 말고 센다)** — `classifyStroke`가 이미 `reason`을 돌려준다
  (`too_short`·`multi_axis`·`too_bent`·`no_axis_matches`·`ambiguous`·`no_camera`).
  **533이 무엇인지 세지 않고 임계를 만지지 않는다.**
- **#12·#13(동작점·절단)** — 임계를 하나 고르지 않는다. 스윕하고 절단 셋을 다 낸다.
- **#20(거름은 선택 뒤에)** — `angle_max_deg` 같은 거부권을 **선택 전에** 걸면 경쟁자가 죽어
  오배정이 **늘었다**(79% → 90%). 순서를 바꾸지 않는다.
- **#6·#30(대조군·양성 채널)** — "실시간 색이 오배정을 줄인다"는 **사용자 행동 의존**이라
  직접 측정이 불가능하다(AS-L5). **간접 지표를 측정으로 적지 않는다.**
- **#3(정의의 귀결)** — 임계를 풀면 `free`가 주는 것은 **정의상 참**이다. 재는 것은 그 다음이다.
- #8(꼬리) · #11(분모) · #14(시드) · #17(단일 출처) · #25 · #26(사전 등록) · #27.

### 사전 등록 (측정 전에 박는다, #26)

| | |
|---|---|
| 스윕 축 | ① 확정 시점: `vp_dist_ratio` {0.03,0.06,0.10,0.16,0.22} × `bend_max` {0.12,0.18,0.30} × `ambiguity_margin` {1.2,1.5,2.0} ② 실시간: `LIVE_TOL.axis_deg` {4,8,12,18,25,35} |
| 판정 | 배치율(모집단 분모) · **조용히 틀림 cut 0.1/0.2/0.5**(놓인 것 분모) · 축 오배정(참 축 대비) · `free` 사유 히스토그램 |
| 채택 규칙 | **조용히 틀림(cut 0.2)이 기준 조건보다 늘지 않는 선에서** 배치율이 최대인 임계. 늘면서 배치가 느는 것은 **채택하지 않는다**(#16) |
| 대조군 | 기준 조건(`AXIS_TOL` 현행값) · 오라클 축 |
| 반증 조건 | 배치율이 늘어도 조용히 틀림이 함께 늘면 그 임계는 **개선이 아니다** |

### 무엇을 만들었나

```
src/capture/inkCanvas.ts (추가)  `onLive` — 그리는 중의 점열. 오면 **전체 다시 그리기**로 바뀐다
                                  (증분 그리기는 안 지우므로 미리보기가 커서를 못 따라온다)
src/s3d/liveLine.ts (기존)        `nearestAxisOnScreen` · `segmentFromAnchor`
src/mainL.ts (추가)               `resolveLive` — **실시간과 확정이 같은 함수를 쓴다**
                                  축 색 미리보기 선 · 앵커 표식 · 상태 표시
test/axis_live.test.ts            임계 스윕과 실시간 판정 → `stage0/out/axis_live.json`
```

**미리보기와 확정이 어긋날 여지가 구조적으로 없다** — 둘 다 `resolveLive(앵커, 시작점, 끝점)`
하나를 부른다. ⚠ 그러므로 §11 게이트의 "일치 0"은 **측정이 아니라 항등에 가깝다**(#5).
브라우저에서 실제로 0이 나오지만 그것이 재는 것은 **정확도가 아니라 배선**이다.

### 결과 — `stage0/out/axis_live.json@1b6175a4`

**① 533이 무엇인지 셌다**(#7). 기본 조건의 `free` 사유:

| 사유 | precise | medium | coarse |
|---|---|---|---|
| `no_axis_matches` | 204 | 215 | 198 |
| `ambiguous` | 92 | 84 | 62 |
| **`multi_axis`** | **0** | 50 | **254** |
| `too_short` | 20 | 18 | 19 |

**`multi_axis`가 `coarse`의 등급 특이 항이다** — 거친 잉크의 떨림이 **없는 코너를 만들어 낸다**
(`maxTurn`이 잡음에 발화한다). precise에서는 0이다.

**② 확정 시점 판정은 임계로 못 고친다.** 45개 조합 어디에서도 배치와 조용히 틀림이
**거의 고정된 비율로 맞바뀐다**:

| `vp_dist_ratio` | 배치 | 축 오배정 | 미분류 | **조용히 틀림 0.2** |
|---|---|---|---|---|
| 0.03 | 2259/4320 | 309 | 1393 | **0.305** |
| **0.06**(기본) | 2416/4320 | 372 | 1216 | **0.362** |
| 0.10 | 2524/4320 | 429 | 1054 | 0.384 |
| 0.16 | 2584/4320 | 462 | 965 | 0.398 |
| 0.22 | 2632/4320 | 502 | 895 | 0.416 |

⚠ **세 열의 합이 4320이 아니다** — 기준 행에서 2416 + 372 + 1216 = 4004이고
**축은 배정됐으나 못 놓인 688**이 빠져 있다(`lift_grade`의 비축 미배치 사유 합과 일치한다).

사전 등록한 규칙(조용히 틀림이 기준 이하이면서 배치 최대)이 고르는 것은
`vp0.1_bend0.18_amb2`이고 이득이 **+26/4320**이다 — **시드 변동폭 안이다**(#14).
**`AXIS_TOL`을 바꾸지 않는다**(A-3: 측정이 아무 방향도 안 가리킨다).

**③ 방향 급변 검출도 같은 맞바꿈이다 — 그런데 대조군이 뜻밖의 것을 말했다.**
`turn_deg` 45° → 60°에서 배치 2416 → 2585이지만 조용히 틀림 0.362 → 0.387이다.
그리고 **진짜 코너가 있는 획을 이어 그린 대조군**(#4 — 픽스처가 벌을 줄 수 있어야 한다)에서
거부율이 `turn_deg` 90°(사실상 끈 것)에서도 **2018/2160 = 93%**다.
→ **진짜 코너를 걸러내는 것은 `multi_axis`가 아니라 다른 규칙들**(굽음·애매·무매칭)이고,
`multi_axis`는 **거친 잉크에만 발화하는 부작용 쪽에 가깝다.**
그래도 조용히 틀림이 늘어 등록 규칙을 통과하지 못하므로 **끄지 않는다.**

**④ 고치는 것은 임계가 아니라 앵커다 — 이것이 이 항목의 답이다.**

| | 배치 | 축 오배정 | 형태 오차 중앙 | **조용히 틀림 0.2** |
|---|---|---|---|---|
| 확정 시점(`classifyStroke`) | **2416/4320 = 0.56** | 372 | **0.1222** | **36.2%** |
| **실시간(앵커 있음, 12°)** | **3403/4320 = 0.79** | 442 | **0.0198** | **16.7%** |

⚠ **이 표는 절단 0.2만 본다 — 절단 0.5에서 방향이 뒤집힌다**(아래 리뷰어 대응 [3·4]).
⚠ **차이는 앵커 말고 둘이 더 있다** — **배치의 정의**(실시간은 축이 정해지면 반드시 놓인다)와
**거부 규칙 집합**(실시간에는 `ambiguous`·`multi_axis`·`too_bent`가 없다).
⚠ **조건 차를 함께 읽는다**(#27): 실시간 팔은 **참 3D 기하가 이미 있다**고 가정한다
(스냅과 같은 상한 조건). 초기 확정에는 그것이 없다. 그러므로 이 비교는
**"앵커가 있으면 다른 문제가 된다"**를 말하지 **"초기 확정을 고쳤다"**를 말하지 않는다.
**초기 확정의 몫은 L-B.7 승격 연쇄로 넘어간다.**

**⑤ `LIVE_TOL.axis_deg = 12`를 유지한다.** 각차 4 → 35°에서 배치 2554 → 3728이고
조용히 틀림 0.102 → 0.219다. 무릎이 12~18°이고(12 → 18은 +99 배치에 +0.8pp)
등록 규칙은 기준 위에서 아무것도 안 고른다. **사유 없이 바꾸지 않는다**(#28).
참고로 **앵커에서 본 최근접 축과의 각차 분포**는 중앙 **2.29°** · p90 **53.6°**로 꼬리가 두껍다 —
임계로 가를 수 없는 몫이 그 꼬리다.

### 브라우저 (Playwright, `stage_browser.json`의 `l_b4`)

끌고 가는 **도중에** 상태창이 `그리는 중 — 시작 끝점 · 축1(…°)`을 낸다.
끌기 마지막 위치의 미리보기 3D와 뗀 뒤의 확정 3D가 **정확히 같다**(gap 0) —
⚠ **항등이다**(#5). 같은 함수를 같은 입력으로 부른다.

### 검증

vitest **260/260**(46파일) · tsc 0 · pytest 44/44 · playwright **12/12** ·
selfcheck **STALE 0** · 상수 해시 `2fd74e1c`(신규 상수 없음).

---

## L-B.5 — 축 고정 수정자 (계획서 §4)

### 착수 시 PITFALLS 대조

읽었다. 걸리는 번호: **#17(측정 경로와 앱 경로)** — 고정은 `resolveLive` **안**에 들어가야 한다.
바깥에서 축을 덮으면 미리보기와 확정이 갈린다(§11 게이트의 "일치 0"이 깨진다).
**#3(정의의 귀결)** — "고정하면 그 축으로 놓인다"는 **정의상 참**이라 성공률을 재는 것이 뜻이 없다.
잴 수 있는 것은 **고정이 무엇을 바꾸는가**뿐이다. #7(사유 카운터) · #12 · #25 · #26 · #6(대조군).

**측정 항목이 원리적으로 없다.** 계획서 §13이 AS-L5("실시간 색이 오배정을 줄인다")를
**사용자 행동 의존 — 간접 측정만**으로 이미 분류했고 축 고정은 그보다 더 직접적으로 사용자 조작이다.
그래서 이 항목은 **선례를 그대로 따르고 배선만 확인한다**(A-3). 지어낸 지표를 원장에 넣지 않는다.

### 선례 (SketchUp — 그대로 따른다)

| 조작 | 뜻 |
|---|---|
| `Shift` (누르는 중) | **지금 추론된 축을 잠근다.** 추론이 흔들릴 때 강제한다 |
| `←` / `↑` / `→` | 각각 **축1 / 축3(수직) / 축2**를 잠근다. 다시 누르면 푼다 |
| `Esc` | 잠금 해제 |

SketchUp의 화살표 배정(왼쪽=녹색축·위=파랑축·오른쪽=빨강축)을 **화면 배치 그대로** 옮긴다 —
사용자가 배울 것이 없다.

---

## L-B.3 리뷰어 2회차 — 18건 대응

**이 회차의 지적은 거의 전부 하나의 원인이다: 본문 수치를 쓴 뒤 하네스를 다시 돌렸고,
그 뒤 본문을 안 고쳤다.** 정확히 PITFALLS #1이다. 원장을 다시 열어 **전부 다시 적었다.**

### 코드·하네스를 고친 것 (높음 5건)

**[6] `placement_from_snapped_start`가 못박힌 셀을 섞고 있었다.** `snapped_kind_counts`에는
`end_jitter > 0` 판을 넣어 놓고 **여기는 빠뜨렸다** — 그런데 **L-B 게이트의 두 번째 항목이 이 표다.**
증거는 원장 안에 있었다(`shape_err.p10 = 0`). 다시 세었다:

| 반경 | 배치(`end_jitter > 0`) | 스냅 실패 | 축 실패 | **조용히 틀림 0.2** |
|---|---|---|---|---|
| 0.025 | 2268/3240 | 295 | 677 | 437/2268 = **19.3%** |
| **0.05** | **2503/3240** | 56 | 681 | **474/2503 = 18.9%** |
| 0.10 | 2559/3240 | 1 | 680 | 555/2559 = 21.7% |

**17% → 19%**로 고쳤고 축 실패도 861 → **681**이다.

**[7] `lift_grade`의 결론 범위가 자기 표와 안 맞았다.** "형태 오차 0.10~0.18 · 조용히 틀림 34~39%"라
적었는데 실제는 **0.0875~0.2692 · 29~67%**다. **범위를 표에서 직접 계산해 넣도록** 하네스를 고쳤다
(손으로 적으면 또 어긋난다). 그리고 **접합 반경을 키울수록 형태 오차가 단조 증가하는 것**을
함께 냈다 — `oracle_touch_*`(precise)에서 **0.1131 → 0.1388 → 0.1824 → 0.2031 → 0.2339 → 0.2593**.
트레이드오프의 크기를 절반 이하로 보고하고 있었다.

**[8] 오라클 축 팔이 교점 집합도 함께 바꾼다 — 단계 분리가 깨진다.** 축 라벨이 바뀌면
`frameOf`가 서는 획이 달라지고 `findJoints`가 **같은 축 쌍을 제외**하므로 후보 쌍 자체가 달라진다.
참 교점 회수가 **precise 1182 → 1764 · coarse 666 → 1500(2.25배)**다.
→ **"지배항은 축 판정"을 "축 판정과 그 하류인 교점 회수"로 좁혀 적었다.**
둘의 몫을 가르려면 축 라벨만 오라클로 주고 교점 집합은 judged 것으로 고정하는 팔이 필요하고,
그것은 `liftAll` 내부를 갈라야 한다(DEFERRED).

**[9] 출하 구성의 수치가 어느 문서에도 없었고, f는 오히려 나빠졌다.** 채택한 것은
`len_x_sep` + **채우기**인데 근거로 든 것은 전부 **채우기 없는 팔**의 값이었다.

| 팔 | 카메라 | 축 오차 중앙 | f 상대오차 |
|---|---|---|---|
| `longest`(기준선) | 31/90 | 12.35° | **0.3007** |
| `len_x_sep`(채우기 없음) | 43/90 | 9.05° | 0.2432 |
| **`len_x_sep_filled`(출하)** | **58/90** | **8.95°** | **0.3152** |

**출하 구성의 f는 기준선보다 나쁘다.** 채운 축이 f 산출에 들어가기 때문이다.
D-L16의 "f 0.30 → 0.24"를 **지지선만 바꾼 팔의 성질**로 한정해 적고,
**채택 근거는 확정률과 축 오차이지 f가 아니다**를 명시했다.

**[10] D-L17의 "스냅된 축 42 → 0"이 출하 구성에서 성립하지 않는다.** 출하 구성은 **58**이다 —
채우기가 넣는 중립 수직 둘이 **정확히 나란해서**(각차 0°) 무한원으로 판정되기 때문이다.
**그것이 옳은 동작이다**(화면 평행 축에는 소실점이 없다, 이론서 2.2). 즉 스냅이 비우는 것은
검출 실패가 아니라 **채운 기본값**이고, 유지 근거는 원래대로 "좁은 각차에서 조작 불가"다.

**[11] 채우기가 "없는 숫자를 지어내는" 자리를 만들었다 — 코드를 고쳤다.** 채우기 뒤에는
**초안만으로 카메라가 서고 민감도 숫자가 뜨는데** 그중 일부 축은 그림이 아니라 기본값이다.
`Guide.filled`를 넣어 ① **흐린 점선**으로 그리고 ② 예산 표시에 `채움`을 붙이고
③ **경고 줄**("축 N은 검출이 아니라 기본 위치로 채운 것입니다 — 반드시 맞춰야 합니다")을 낸다.
그리고 그 성질이 뒤집혔으므로 `l_b2`의 낡은 `note`(#19 — 설명이 필드와 반대를 말한다)도 고쳤다.

### 수치를 다시 적은 것 (중간 9건 · 낮음 4건)

| # | 잘못 | 원장 |
|---|---|---|
| [1] | 종류 분포 "끝점 4170 · 선 위 60 · 중점 34 · 미스 56(4320)" | **끝점 2850 · 선 위 204 · 중점 130 · 미스 56(3240)**(`end_jitter > 0`) |
| [1] | 틀린 종류 "on_edge 817/836 · endpoint 21/21" | **0.008에서 on_edge 893/912 · 0.280에서 endpoint 378/381** |
| [2] | k3 포화 "1330/4320" (다섯 문서) | **15076/38880 = 39%** |
| [3] | `@dbcf3a11` 인용 두 곳 | 그 실행은 **없다**. 최종 해시 `2fd74e1c` |
| [4] | 밀도 규칙 표 | k1 {A 0.20, C 0.07} · k2 {A 0.14, C 0.07} · k3 {A 0.14, C 0.05}. **k1의 C가 0.10 → 0.07로 움직였다** |
| [4] | "규칙 C의 답은 안 바뀌었다" | **k3에만 해당한다** |
| [5] | AS-L4의 "맞음 3072/3240" | **2826/3240 = 0.87** |
| [12] | 절 제목 "확정률 **0.88**을 0.34로" | 그 0.88은 `vp_detect`의 값이라 **이 하네스에 겹쳐 읽으면 안 된다**(#27). 이 하네스의 기준선은 **검출 유한 소실점 ≥2가 75/90**이고 **상한은 참 소실점으로도 72/90**이다 |
| [13] | `guide_budget`의 두 분모 | `guides_below_required` 540(=90×6) 대 `extend_ceiling` **348**(=58×6, 카메라가 서는 경우만). DEFERRED의 "152 중 26"을 **164 중 98**로 고쳤다 |
| [14] | "표본이 넓어져서 최소값이 내려갔다" | **두 실행은 `support_pick`도 바뀌었다** — 원인을 안 갈랐다. 그렇게 고쳐 적었다 |
| [15] | `len_x_sep` 채택에 시드 변동폭 없음 | 차이가 43 대 44(1건/90) · 9.1° 대 10.0°다. **시드 변동폭 안일 수 있다**고 약화해 적었다 |
| [16] | "`l_b3`에 `size_heal_count` 추가" | 실제로는 안 들어갔다. 넣었다 |
| [17] | selfcheck가 `f_rel_err` 전량 동일(0.4076)을 못 잡았다 | DEFERRED 항목의 범위를 **"분포 전체가 한 값"이 0이 아닐 때**까지 넓혀 적었다 |
| [18] | 본문 ②가 여전히 7.55/7.56/7.97 | **7.6 / 7.6 / 8.0**(AS-C9) |

### 남긴 것 (2회차 중 대응 안 한 것 — CLAUDE.md §4대로 DEFERRED)

- **[8]의 단계 분리** — 축 라벨만 오라클로 주고 교점 집합을 고정하는 팔. `liftAll` 내부를 갈라야 한다
- **[15]의 시드 분해** — `draft_recover`에 시드별 값을 내려면 셀이 6배가 된다. 채택 근거를 약화해 적는 것으로 갈음했다
- **[17] selfcheck의 "분포 전체가 한 값"** — L-B.1이 올린 항목의 범위를 넓혔을 뿐 규칙은 아직 없다

---

## L-B.5 — 결과

**측정 항목이 원리적으로 없다**(착수 대조에 그렇게 적었다). 만든 것과 확인한 것만 적는다.

`resolveLive` **안**에 고정을 넣었다 — 바깥에서 축을 덮으면 미리보기와 확정이 갈린다(#17).
`Shift`는 **처음 추론된 축**을 잡아 뗄 때까지 유지하고(SketchUp과 같다), 화살표는 토글,
`Esc`는 해제다. 고정이 바뀌면 **그리는 중이라도** `relive()`가 미리보기를 다시 푼다 —
안 하면 화면이 낡는다(AS-C7과 같은 형태).

고정으로 놓인 획은 **`userAxis = true`**다 — §6.1의 "사용자 지정 스냅만 유지를 시도한다"와
`solveInto`의 재분류 건너뛰기가 그 표시를 읽는다.

**브라우저**(`stage_browser.json`의 `l_b5`): 어느 축과도 안 맞는 방향으로 그으면
**2D로 대기**하고, 같은 획을 축 고정으로 다시 그으면 **그 축으로 놓이고 `userAxis`가 선다**.
끌기 도중 상태창이 `축 고정`을 낸다.

⚠ 이 확인이 말하지 않는 것: **고정이 오배정을 줄이는가**(AS-L5 — 사용자 행동 의존, 직접 측정 불가).

---

## L-B.4 리뷰어 1회차 — 12건 대응

### 높음 둘 — **결론이 바뀐다**

**[3·4] ④의 결론이 절단 0.2 하나에 서 있었고, 절단 0.5에서 방향이 뒤집힌다.**
사전 등록은 "cut 0.1/0.2/0.5"였는데 **결론은 0.2만 인용했다**(#13을 내가 어겼다).

| | 배치 | 형태 오차 중앙 | p90 | max | **cut 0.1** | **cut 0.2** | **cut 0.5** |
|---|---|---|---|---|---|---|---|
| 확정 시점 | 2416/4320 | **0.1222** | 0.4468 | 3.35 | 1360 (56%) | 874 (**36.2%**) | 190 (**7.9%**) |
| 실시간(12°) | 3403/4320 | 0.0198 | **0.7158** | **13.33** | 781 (23%) | 569 (**16.7%**) | 504 (**14.8%**) |

**절단에 따라 갈린다 — 작은 틀림은 줄고 큰 틀림은 는다.** 실시간은 cut 0.5에서 비율 1.9배 ·
절대수 2.7배 나쁘고, 성격도 반대다(확정 시점은 cut0.5/cut0.2 = 22%인데 실시간은 **89%** —
**실시간에서 틀린 것은 거의 다 크게 틀린다**). ④를 그렇게 다시 적는다:
**"앵커가 있으면 다른 문제가 된다"**는 유지하되 **"앵커가 더 낫다"는 절단 0.2에서만 참이다.**

_초판이 확정 시점의 형태 오차를 "—"로 비운 것도 고쳤다 — 있는 값(0.1222)을 안 적으면
유리한 쪽만 보인다(#8)._
_그리고 실시간 `shape_err`의 `min = p10 = 0`은 **못박힌 `end_jitter = 0` 표본의 설계 보장**이다
(앵커 = 참 꼭짓점 · 참 축이면 재구성이 항등이다)._

**[4] 사전 등록한 규칙이 완화 방향만 볼 수 있었다.** 목적이 "조용히 틀림 ≤ 기준 **이면서
배치 최대**"인데 맞바꿈이 45조합 전부에서 단조라 **조이는 후보는 구조적으로 못 고른다.**
그런데 이 프로젝트의 정책은 A-3·§9.1이다 — **미배치는 실패가 아니라 대기이고 승격이 연쇄한다.**
그 정책에서 미배치의 비용은 **미뤄지는** 것이고 조용히 틀림은 **영구**다.
**규칙을 바꾸지 않고 반대 방향을 하나 더 냈다**(#28):

| | 배치 | 축 오배정 | 형태 오차 중앙 | cut 0.2 | **cut 0.5** |
|---|---|---|---|---|---|
| 기준(`vp0.06_bend0.18_amb1.5`) | 2416/4320 | 372 | 0.1222 | 0.362 | 190 |
| **조이기(`vp0.03_bend0.18_amb2`)** | **2179/4320**(−237) | **278**(−94) | **0.0887** | **0.279** | **116**(−39%) |

**측정은 방향을 가리키고 있었다 — 조이는 쪽이다.** 초판의 "아무 방향도 안 가리킨다"는
**목적함수가 만든 결론**이었다.

⚠ **그래도 지금 채택하지 않는다.** 이 정책이 성립하려면 **승격 연쇄가 실제로 회수해야 하는데
그 기전이 아직 없다**(L-B.7). 회수율을 재기 전에 공유 상수를 바꾸면 "미배치는 대기다"가
**검증되지 않은 전제**가 된다. **L-B.7 뒤에 다시 본다**(DEFERRED · D-L18).

### 중간·낮음 열

**[7] "+26이 시드 변동폭 안이다"를 실제로 쟀다.** 기준 조건의 시드별 배치는
**387 / 396 / 397 / 406 / 412 / 418**이고 폭이 **31**이다. 이득 26 < 폭 31 — 주장이 유지된다.
초판은 **재지 않고** 그렇게 적었다(#14는 "작아 보이면 시드 탓"이 아니다).

**[4·6] `turn_detection` 대조군의 해석이 틀렸다 — 세어 보니 반대였다.**
"진짜 코너를 걸러내는 것은 `multi_axis`가 아니라 다른 규칙들"이라고 **세지 않고 지목했다**(#7).
사유를 세니 기본 조건에서 **`multi_axis` 1991 / `no_axis_matches` 60 / `ambiguous` 3**이다 —
**`multi_axis`가 지배 규칙이 맞다.** 그러므로 그것을 푸는 것은 **진짜 비용이 있다.**
⚠ 다만 대조군의 동적 범위가 5.8pp(92.4~98.2%)로 **천장에 붙어 있어** 기능 붕괴 자체는
검출하지 못한다 — `turn90`에서도 `multi_axis` 1736이 남는다(투시에서 직각 코너의
겉보기 방향 변화가 90°를 넘기 때문이다). 그 한계를 원장에 적었다.

**[4·11] 두 팔의 "배치"가 같은 사건이 아니다.** 실시간 팔은 **축이 정해지면 반드시 놓인다**
(2961 + 442 = 3403 = 배치, 구성상 항등). 확정 팔은 판정 **더하기** 해 성립을 요구한다
(2732 + 372 = 3104인데 배치 2416). 그리고 실시간 팔의 거부 규칙은 `angle_over`·`no_snap` **둘뿐**이고
`ambiguous`·`multi_axis`·`too_bent`가 아예 없다. **두 팔의 차이는 앵커 말고 둘이 더 있다** —
**배치의 정의**와 **거부 규칙 집합**.

**[4] "무릎이 12~18°"가 한계증분과 안 맞는다.** 새 배치 1건당 새 조용히 틀림(cut 0.2)은
4→8 35% · 8→12 40% · 12→18 45% · **18→25 89%** · 25→35 92%다 — **급변은 18과 25 사이**다.
초판의 "+99에 +0.8pp"는 **희석된 평균**이었다(#15). 그리고 cut 0.5로 읽으면 **4°가 뚜렷이 낫다**
(9.0% 대 12°의 14.8%). **`LIVE_TOL.axis_deg = 12`를 유지하는 근거를 "무릎"에서 바꿔 적는다** —
등록 규칙이 기준값만 고를 수 있는 구조였으므로 이 항목은 **잠정값을 재확인했을 뿐**이고,
cut 0.5 기준의 재검토는 L-B.7과 함께 본다(DEFERRED).

**[1] ② 표의 세 열이 4320에 안 맞았다.** 2416 + 1216 = 3632이고 **688이 안 보였다** —
**축은 배정됐으나 못 놓인 것**이다(`lift_grade`의 비축 사유 합과 정확히 일치한다).

**[1] DEFERRED 두 항목 · [2] AS-C3 · [1] DECISIONS(D-L18·D-L19) · [5] 계획서 §11 게이트 문장**을
갱신했다. 특히 **"미리보기와 확정의 일치(0)"는 항등이므로 게이트 항목이 아니다** — 계획서를 고쳤다.
안 고치면 다음 세션이 원문을 다시 시도한다(#23).

---

## L-B.7 — 승격 연쇄 (계획서 §9.1)

### 순서를 바꾼 사유

계획서 §11의 순서는 L-B.6(뷰 시스템) → L-B.7이다. **B.7을 먼저 한다.**
근거는 A-3("측정이 가리키는 방향을 우선한다")이다 — 지금 열려 있는 결정 셋이
**전부 "승격이 실제로 회수하는가"에 달려 있다**:

| 열린 것 | 어디 |
|---|---|
| 축 판정 임계를 조이는 쪽 | D-L18 · L-B.4 [4] |
| 참 카메라 `coarse` 0.44의 남은 몫 | L-B.3(a) · DEFERRED |
| 스냅으로 못 고치는 몫 | D-L15 |

B.6(뷰 시스템)은 **그 셋 중 어느 것에도 안 닿는다**(UI와 소유 관계다).
셋을 미해결로 둔 채 UI를 먼저 만들면 **판단 근거 없이 상수를 들고 다음 단계로 간다.**

### 착수 시 PITFALLS 대조

- **#16(병목 이동)** — "승격이 회수한다"가 참이어도 **회수된 것이 조용히 틀리면** 아무것도 안 고친 것이다.
  회수율과 **회수된 것의 형태 오차**를 반드시 함께 낸다.
- **#3(정의의 귀결)** — 미배치를 다시 풀면 일부가 놓이는 것은 거의 **정의상 참**이다.
  재는 것은 "놓이는가"가 아니라 **"얼마나, 그리고 맞게"**다.
- **#6·#30(대조군·양성 채널)** — 승격 **없이** 같은 획을 다시 푼 팔이 있어야 한다.
- **#11(분모)** — 회수율의 분모는 **그 조건에서 실제로 미배치인 획**이다. 조건마다 다르므로 함께 적는다.
- **#10(뺄셈 금지)** · **#12·#13(동작점·절단)** · **#7(사유 카운터)** · #25 · #26 · #27.

### 사전 등록 (측정 전에 박는다, #26)

| | |
|---|---|
| 판정 | ① **회수율** = 승격으로 새로 놓인 획 / 그 조건의 미배치 획 ② **회수된 것의 조용히 틀림** cut 0.1/0.2/0.5 ③ 승격이 **기존 기하를 안 움직이는가**(움직이면 그것은 승격이 아니라 재계산이다) |
| 팔 | 기준 `AXIS_TOL` · **조이기**(`vp0.03_bend0.18_amb2`) — D-L18이 유력하다고 본 그것 |
| 대조군 | 승격 없이 같은 미배치 집합을 다시 푼 것(= 아무 일도 안 일어나야 한다) |
| 채택 규칙 | **조이기가 옳으려면**: 조이기의 (배치 + 회수) 합이 기준의 (배치 + 회수) 이상이면서 **조용히 틀림이 낮아야** 한다. 둘 중 하나라도 안 되면 D-L18은 기각이다 |
| 반증 조건 | 회수율이 낮거나, 회수된 것이 기준보다 더 틀리면 **"미배치는 대기다"가 이 구현에서는 거짓**이다 |

### ⚠ 이 절의 형태 오차는 `axis_live`와 **다른 정의**다 (리뷰어 [1])

같은 2416획인데 `axis_live.json`은 중앙 **0.1222** · cut 0.2 **874(0.3618)**이고
`promote.json`은 중앙 **0.1533** · cut 0.2 **1023(0.4234)**이다. **분모도 표본도 같다.**
갈리는 것은 **정규화**다 — `axis_live`는 `scene3d.perStrokeError`(전역 최적 배율로 맞춘 뒤)를
쓰고 `promote`는 `segErr`(배율 보정 없이 직접)를 쓴다.

**같은 양이 두 숫자로 돌아다니는 것이 PITFALLS #1이고, `perStrokeError`를 공용으로 옮긴 것이
바로 그 사고를 막으려던 것이었는데 이 항목에서 다시 만들었다.** D-L21의 "0.426 → 0.385"와
D-L18의 "0.3618 → 0.2786"이 **같은 두 팔의 같은 절단인데 다른 값**인 이유가 이것이다.
**두 원장의 형태 오차를 나란히 읽지 않는다**(#27). 통일은 DEFERRED에 올렸다.

### 결과 — `stage0/out/promote.json@1b6175a4`

**① 계획서 §9.1의 기전은 이 구현에서 아무것도 회수하지 못한다.**
"§5.4의 일괄 풀이를 재사용한다"를 그대로 만들어(대기 획 + 확정 획을 함께 풀고 확정 획이
정해 주는 배율로 새로 놓인 것만 채택) 돌렸더니 **세 팔 모두 회수 0**이다.

| 팔 | 확정 | 대기 | **일괄 재풀이 회수** |
|---|---|---|---|
| 기준 | 2416/4320 | 1904 | **0/1904** |
| 조이기 | 2179/4320 | 2141 | **0/2141** |
| 늘리기 | 2766/4320 | 1554 | **0/1554** |

사유를 세니 `축이 미분류다` **1116** · `구조에 이어지지 않았다` **620** ·
`no_committed_geometry` **168**이고 합이 **1904 — 대기 전부**다(기준 팔).

⚠ **초판은 이 합을 안 적었고 그래서 결론을 넓게 썼다**(리뷰어 [2][3]). 합이 분모와 같다는 것은
**대기 획이 솔버에 한 획도 안 닿았다**는 뜻이다 — `A s = 0`이 후보를 받은 획은 **0개**다.
그러므로 말할 수 있는 것은 **"기전이 회수를 못 한다"가 아니라 "이 구현에서는 거름이
대기 집합을 전부 비워 기전이 실행되지 않았다"**까지다. **계획서에서 지웠던 문장을 되돌렸다.**

그리고 **`축이 미분류다`는 획의 성질이 아니다** — 같은 1904획에서 앵커 경로는 720획에 축을
붙였고 그 거부 사유에 `축이 미분류다`가 **0건**이다. 확정 판정(`classifyStroke`)과 실시간
판정(`nearestAxisOnScreen`)이 **다른 규칙**이기 때문이고, L-B.4 [4·11]이 이미 그렇게 적었다.
**축 라벨을 주는 경로를 앞에 두면 일괄 기전이 실행될 수 있다 — 안 쟀다**(DEFERRED).

**② 실제로 회수하는 것은 앵커다.** 대기 획의 **시작점이 확정된 기하에 붙으면**
§3·§7의 실시간 경로가 그 획을 놓는다. **720/1904 = 37.8%**를 회수한다.
→ **승격은 "다시 푸는 것"이 아니라 "앵커가 생기는 것"이다.** 계획서 §9.1을 그렇게 고쳤다(#23).

**③ ⚠ 그런데 회수된 것이 확정된 것보다 낫지 않다**(#16).

| | n | cut 0.1 | **cut 0.2** | **cut 0.5** | 중앙 | **p90** | **max** |
|---|---|---|---|---|---|---|---|
> ⚠⚠ **이 절(L-B.7)의 형태 오차·조용히 틀림은 전부 폐기된 정의다**(게이지를 안 없앤 `segErr`) —
> **아래 표뿐 아니라 그 아래 본문의 서술도 그렇다.** L-B.M이 D-L22로 고쳤고 결론이 바뀌었다.
> 특히 **"중앙은 그대로 두고"(현재 정의에서 거짓 — 0.235 대 0.1222)** ·
> **"p90 1.22배 · max 1.94배"(현재 2.12배 · 2.62배)** ·
> **"0.426 → 0.385"(현재 0.4037 → 0.329)**를 그대로 인용하지 않는다.
> 여기 남겨 두는 이유는 **무엇이 어떻게 틀렸는지**가 근거이기 때문이다.
> **살아 있는 수치는 이 절 아래 L-B.M의 재측정 표와 `promote.json`이다.**

| 확정(기준) | 2416 | 62.7% | **42.3%** | **20.9%** | **0.1533** | 0.738 | 17.64 |
| **앵커 회수** | 720 | 63.8% | **43.6%** | **23.5%** | 0.157 | **0.9034** | **34.30** |

⚠ **초판은 확정 행의 중앙을 "—"로 비우고 꼬리를 안 냈다**(리뷰어 [7][8], #8) —
**L-B.4에서 같은 지적을 받고 고친 자리가 재발했다.** 꼬리를 넣으면 문장이 달라진다:
**중앙·절단은 그대로인데 p90이 1.22배 · max가 1.94배다** — **큰 틀림 쪽은 나빠진다.**

**승격은 개수를 늘리고, 중앙은 그대로 두고, 꼬리는 나쁘게 한다.** 그러므로 §9.1의 "미배치는 대기다"는
**개수 회수에 대해서만 참**이고 **"나중에 더 정확히 놓인다"는 뜻이 아니다.**

**④ 왜 나머지 63%는 안 올라가나**(#7 — 세었다). 기준 팔에서
`no_snap` **813** · `angle_over` 371이다. **대기 획의 시작점이 확정 기하 근처에 아예 없는 것**이
지배항이고, 그럴 수밖에 없다 — **그 획들이 대기인 이유가 바로 안 이어졌기 때문**이다.

**⑤ D-L18 기각**(등록 규칙대로). 조이기의 (배치 + 회수)가 **2976**으로 기준 **3136**보다 작다 —
회수율이 두 팔에서 **37.8% 대 37.2%로 거의 같아** 차이를 못 메운다.

⚠ **다만 조이기는 조용히 틀림을 낮춘다**: cut 0.2 **0.426 → 0.385** · cut 0.5 **0.215 → 0.182**.
**등록 규칙이 개수를 우선하도록 짜여 있어서 기각된 것**이고, 품질을 우선하는 목적함수에서는
여전히 조이기가 이긴다. **규칙을 사후에 바꾸지 않는다**(#28) — 기각으로 적고 그 사실을 함께 적는다.

**⑥ 대조군과 불변식.** 승격 없이 대기 집합만 다시 풀면 **207/1904(10.9%)**가 놓인다 —
그것들은 **대기 획끼리만 이어진 덩어리**라 배율이 정해지지 않는다. `promote`가 그것을
`구조에 이어지지 않았다`로 거른다. 그리고 **승격이 기존 기하를 안 움직인다**(`committed_drift`
최대 **0**, 세 팔 전부) — 덮어쓰면 0이 아니게 되므로 **되살릴 수 있는 검사**다.

### 앱

획이 놓일 때마다 · 확정 직후에 `promoteChain`이 돈다. **연쇄한다** — 이번에 놓인 것이
다음 획의 대상이 되므로 더 안 늘 때까지 반복한다(최대 8회). 상태창이
`승격 연쇄로 N획이 더 올라갔습니다`를 낸다.

---

## L-B.M — 지표 정의 통일 (사람 지시, 2026-08-16)

**착수 시 `PITFALLS.md`를 읽었다.** 이 항목에 걸리는 번호:
**#1**(문서가 원장보다 뒤처진다 — 이 항목이 그 기계화다) · **#3**(지표가 정의의 귀결) ·
**#5**(양성 채널) · **#7**(추측 말고 카운터) · **#11**(분모가 전부인가) · **#12**(동작점 하나 금지) ·
**#13**(절단값) · **#14**(분자/분모) · **#17**(단일 출처) · **#25**(원장 밖 측정) ·
**#26**(사후에 기준을 정하지 않는다) · **#28**(판정 지표를 사유 없이 바꾸지 않는다) ·
**#30**(양성 채널을 함께 둔다) · **#31**(탐색 공간) · **#32**(미실행 ≠ 반증).

### 지시 세 가지에 대한 대응

**a. 정의를 한 곳으로 모았다** — `web/test/metrics.ts`가 유일한 집이다.
`fitGauge` · `segShapeError` · `perStrokeError(Map)` · `axisDirErrors` · `silentWrong` · `SILENT_CUTS`.
`scene3d.ts`에서 옮겨 왔고 **하위 호환 재수출을 두지 않았다** — 두 경로가 생기면 다시 갈린다.

**b. 정의 해시를 산출물에 넣었다** — `metric_defs: metricsSnapshot()`.
30개 하네스 + e2e 2개 전부. `selfcheck.scan_stale_metrics`가 `stage0/out/metrics.json`과
대조해 **STALE(정의)**를 낸다. 반례 8건이 `tests/test_selfcheck_stale_metrics.py`에 있다.

⚠ **첫 판이 틀렸고 고쳤다.** 해시를 `Function.prototype.toString()`으로 냈더니
**vitest(esbuild)와 Playwright의 TS 변환이 같은 코드에 다른 문자열을 내서**
두 러너의 산출물이 서로 STALE로 떴다(`0ef2a685` 대 `ead43fb8`). 그것은 **다시 돌려도
안 지워지는 거짓 양성**이다. **소스 파일 텍스트**(주석·공백 제외)로 바꿨다.
그다음에도 한 번 더 갈렸다 — `require`로 `node:fs`를 받으면 **ESM 러너에서 폴백**으로
떨어진다(`source: "fallback"`). 정적 import로 고쳤고, selfcheck가 `source != "file"`을 짚는다.

**c. 정의가 갈린 지표를 전수 확인했다.** 결과는 넷이다.

| 지표 | 사본 | 값이 갈렸나 |
|---|---|---|
| 형태 오차 | `scene3d.perStrokeError` · `lift_gate` 지역 · `lift.test.shapeError` · **`promote.segErr`** | **`segErr`만 갈렸다** — 게이지를 안 없앴다. 앞의 셋은 식이 같았다 |
| 축 방향 오차 | `camera_gate` · `draft_recover` | **바이트 동일**. 갈리진 않았으나 갈릴 자리였다 |
| 조용히 틀림 절단 | 하네스마다 인라인 | 절단값 0.1·0.2·0.5는 같았다 → `SILENT_CUTS`로 묶었다 |
| `rate(k,n)` | 10개 파일 | 전부 동일. 지표가 아니라 서식이라 그대로 둔다 |

넷 다 `metrics.ts`로 모았다.

### 어느 쪽이 맞는 양인가 — 게이지 (D-L22)

`liftAll`이 깊이를 `gauge_height`로 정규화하므로 **절대 배율은 자유도**다.
안 없애고 참값과 빼면 그 차이는 **형태 오차가 아니라 게이지 차이**다.
그래서 게이지를 **인자로 강제**했고 셋을 가른다:

```
fit(기본)   잰 집합 자체에서 최소제곱 적합      한 번에 푸는 확정 집합
고정값       다른 집합에서 적합한 배율을 받는다   승격 — 회수 획은 이미 확정된 틀에 붙는다
1            배율 자유도가 없다고 본다            틀리다. 옛 segErr가 이것이었다
```

승격에서 `fit`을 쓰면 **회수 획만 따로 배율을 다시 맞추는 것**이라
"이미 확정된 틀에 붙는다"는 성질이 지워진다. 그래서 **확정 집합의 게이지를 고정으로 넘긴다.**
확정이 0이라 게이지가 안 서는 장면은 **형태 오차 분모에서 뺀다**(#11) —
기준 팔 14 · 조이기 25 · 늘리기 4장면이다.

### 다시 재니 결론 둘이 바뀌었다

| | 옛 `segErr`(게이지 1) | **고정 게이지**(D-L22) |
|---|---|---|
| 확정 2416 중앙 | 0.1533 | **0.1222** — `axis_live.json`과 **일치한다**(그것이 [1]의 목적이었다) |
| 확정 cut 0.2 | 42.3% | **874/2416 = 36.2%** |
| 앵커 회수 720 중앙 | 0.157 | **0.235** |
| 앵커 회수 cut 0.2 | 43.6% | **392/720 = 54.4%** |
| 판정 | "회수가 확정과 **같다**" | **"회수가 확정보다 뚜렷이 나쁘다"** (1.5배) |

**D-L21(D-L18 기각)은 유지된다** — 조이기의 (배치+회수)가 2976 대 기준 3136으로 여전히 작다.
⚠ 다만 **초판의 서술을 고쳤다**(리뷰어 [10]): 등록 규칙은 "개수 우선"이 아니라
**두 조건의 연언**이었다. 단조 맞바꿈 아래서 통과하려면 회수가 그 단조성을 깨야 했고 안 깼다 —
**충족 가능하되 좁은 규칙**이다. 조이기의 조용히 틀림 이득은 0.4037 → **0.329**로 오히려 커졌다.

## L-B.7 리뷰어 잔여 대응 — DEFERRED **12행 중 11행을 닫았다**(리뷰어 [15]가 계수를 잡았다)

| # | 무엇 | 결과 |
|---|---|---|
| **[1]** | 형태 오차 정의가 둘 | **닫힘.** 위와 같다(D-L22·D-L23) |
| **[2][3]** | 축을 준 뒤 일괄 기전 재측정 | **닫힘 — 그리고 계획서가 살아났다.** 오라클 축에서 입력 1904 · **솔버가 놓은 것 1154** · 채택 **1154(60.6%)**로 앵커(720)보다 **많다.** 초판의 0은 **미실행**이었다(#32). ⚠ 오라클은 상한이고 실제 축 라벨로는 24/966이다. **D-L24** |
| **[4]** | 연쇄 자체가 미측정 | **닫힘(원장 측).** 2회차 **192** · 3회차 **25** · 4회차 **1** = 추가 **218**, 상한 8회 도달 **0장면** — **수렴한다.** 합계 3354/4320. 불변식으로 못 박았다(`chainByRound[2] > 0`). **브라우저 확인은 L-B.6 뒤에 남는다** |
| **[5]** | 대조군 207획을 안 쟀다 | **닫힘.** 확정 게이지로 중앙 **0.1985** · cut 0.2 **103/207**, 자기 게이지로 **0.0994** · **64/207**. "배율이 정해지지 않는다"가 **논증에서 측정으로** 바뀌었고 그 크기는 중앙 2배다 |
| **[6]** | `committed_drift = 0`에 양성 채널이 없다 | **닫힘.** 확정 기하를 1.01배로 덮어쓴 반례 팔에서 중앙 **0.043**(n=324)이다 — 되살릴 수 있는 검사이지 설계 보장이 아니다. 불변식 `min(driftPositive) > 0` |
| **[10]** | 등록 규칙 서술이 규칙의 형태와 다르다 | **닫힘.** 위와 같다 |
| **[11]** | 회수율을 등급·잡음으로 안 갈랐다 | **닫힘 — 그리고 37.8%는 안 유지된다.** 등급 precise 201/532 · medium 245/572 · coarse 274/800(0.34~0.43)은 비슷한데 **잡음은 jit0 162/227(0.71) → jit0.05 163/781(0.21)로 3.4배** 벌어진다. **잡음이 커지면 확정도 줄고 회수율도 줄어 양쪽에서 잃는다** |
| **[12]** | 앵커 팔의 조건이 원장에 없다 | **닫힘.** `conditions.anchor_arm`에 스냅 대상(**확정 기하** — `snap.json`·`axis_live.json`은 **참 3D**를 썼다) · `axis_deg` · `radius_ratio` · 연쇄 상한을 적었다. **그래서 이 원장의 앵커 수치를 그 둘과 직접 비교하지 않는다** |
| **[13]** | 회수 720이 판정 조건의 개수일 수 있다 | **인정하고 원장에 적었다.** 거부 사유가 `no_snap` 813 · `angle_over` 371 둘뿐이고 합이 1904라 **"축이 정해지면 반드시 놓인다"가 구성상 항등**이다. 회수율은 "축 판정 + 스냅의 통과율"이지 "배치가 맞는가"가 아니고, 맞는가는 조용히 틀림이 답한다 |
| [14] | `loosen` 팔이 사전 등록에 없다 | **미해소** — DEFERRED 유지 |
| **[15]** | §9.1이 `assumptions.md`에 없다 | **닫힘. AS-L11**로 등록했다 |
| **[16]** | §9.1 개정문에 참 카메라 조건이 없다 | **닫힘.** 계획서 §9.1 머리에 조건 블록을 넣었다 |

### 검증

vitest **274/274**(48파일, +13 `metrics.test.ts`) · tsc 0 · pytest **52/52**(+8) ·
playwright **13/13** · selfcheck **STALE 0 · STALE(정의) 0** ·
상수 해시 `2fd74e1c` · **정의 해시 `3df57ac7`**.

---

## L-B.6 — 뷰 시스템 UI 배선 (계획서 §9.2~§9.4) — **착수**

**착수 시 `PITFALLS.md`를 읽었다.** 이 항목에 걸리는 번호:

- **#17**(측정 경로와 앱 경로가 갈라진다) — 뷰 자세 변환(우리 규약 ↔ three)을 `stage.ts` 한 곳에만
  둔다. `viewport.unpin`이 이미 같은 뒤집기를 쓰고 있어 **두 곳이 될 자리**였다.
- **#22 / AS-C7**(캔버스 크기 굳음) — 뷰 전환은 카메라를 갈아 끼우므로 크기 재계산 경로를 탄다.
  확인은 **창을 앞에 두고** 한다.
- **#18**(써 놓고 안 읽는다) — `SView.pose`·`seq`가 `doc.ts`에 있는데 **아무도 안 읽고 있었다**.
  이 항목이 그것을 읽는 자리다. `selfcheck.scan_unread_fields`가 대상이다.
- **#5**(자기참조) — "저장한 자세로 돌아가면 같은 화면이다"는 **왕복 지표**다.
  `pose()`와 `setPose()`가 서로의 역이므로 그 왕복 오차는 **설계 보장이지 측정이 아니다**.
  임계를 걸지 않고, 대신 **틀린 자세를 넣으면 값이 움직이는지**를 반례로 건다.
- **#23**(계획서와 실제가 어긋나면 실제를 따른다) — §9.3의 "새 각도에서 그리면 뷰가 생긴다"는
  **궤도 후 그리기(L-B.8)가 열려야 도달 가능하다**. 생성 경로는 여기서 만들고
  **도달은 L-B.8에서 확인한다.** 그 사실을 여기 적는다.
- **#26**(게이트 수치를 사후에 정하지 않는다) — 이 항목은 배선이라 수치 게이트가 없다.
  판정은 **Playwright에서 실제로 되는가**이고 그 목록을 먼저 적는다:
  ① 뷰 목록이 뜬다 ② 다른 뷰로 갔다가 돌아오면 **같은 화면**이다
  ③ 뷰를 지우면 그 안의 대기 획이 함께 사라지고 **승격된 획은 남는다**
  ④ 확정 뷰는 못 지운다 ⑤ 다른 뷰의 대기 획은 숨는다(개수가 화면에 나온다)

## L-B.M 리뷰어 17건 대응 — **주장 하나가 실측으로 깎였다**

리뷰어가 **높음 5 · 중간 7 · 낮음 5**를 냈다. 그중 **측정으로 답할 수 있는 것을 먼저 돌렸고,
그 결과 이 세션이 한 시간 전에 적은 결론 하나가 틀렸다는 것이 확인됐다.**

### [1] 게이지 추정기가 격차의 대부분이었다 — **가장 중요한 지적이고 맞다**

지적: 확정은 `fit`(자기 집합 최소제곱), 회수는 `고정`(확정 집합 배율)이라 **추정기가 다른데**
그 차이를 배치의 성질로 읽었다. 최소제곱 적합은 정의상 오차를 낮추므로 **편향의 방향이
정해져 있다**(#31 — 이 설계는 "회수가 확정보다 낫다"는 격자점을 낼 수 없다).
같은 원장이 대조군 207획에서 그 크기를 이미 쟀다(중앙 2.0배).

**돌렸다.** 회수 720을 **자기 게이지**로도 재고, 확정을 **회수의 게이지**로도 쟀다.

| 집합 | 추정기 | 중앙 | cut 0.2 |
|---|---|---|---|
| 회수 720 | 고정(확정 집합) | 0.235 | **392/720 = 0.544** |
| 회수 720 | **자기** | 0.1405 | **286/720 = 0.397** |
| 확정 2416 | 자기(= `fit`) | 0.1222 | **874/2416 = 0.362** |
| 확정 1979 | **회수의 게이지** | 0.1743 | **929/1979 = 0.469** |

**같은 종류의 추정기끼리 견주면 격차는 1.10배이지 1.51배가 아니다.**
> ⚠ **2회차 리뷰어가 이 1.10배도 깎았다** — 시드별로 0.865~1.150이라 **폭 안**이다.
> 아래 "리뷰어 2회차" 절을 본다. **살아 있는 서술은 그쪽이다.**
반대 방향(확정을 회수 게이지로)도 나빠지므로 **추정기 효과가 양방향으로 확인된다.**

**고친 문장**: "회수가 확정보다 1.5배 나쁘다" → "형태 격차는 1.10배이고, 나머지 ≈15pp는
확정된 틀과 배율이 안 맞는 몫이다." ⚠ **2회차에서 이것도 고쳤다** — 1.10배가 시드 폭 안이라
**형태 격차는 결론이 아니고 배율 몫만 실재한다.**

### [2] 구도를 안 갈랐다 — **1점 구도는 회수한 것의 96.8%가 조용히 틀린다**

`base_by_composition`이 원장에 이미 있었는데 결론에 안 넣었다. 회수의 조용히 틀림(cut 0.2)이
3pt_yaw35_pitch15 **52/163** · 3pt_yaw20_pitch25 **49/137** · 3pt_yaw50_pitch8 **142/242** ·
2pt **59/85** · **1pt 90/93 = 0.968**이다. 회수율도 0.21~0.61(2.9배)로 **잡음축(3.4배)에 버금간다.**
**"미배치는 대기다"를 구도와 무관하게 적지 않는다** — AS-L11·§9.1·D-L22에 넣었다.

### [3] 오라클 일괄의 품질을 안 적었다 — **착수에 등록한 #16을 스스로 어겼다**

원장에 `675/1154 = 0.585`가 있었는데 모든 문서가 **개수(1154 · 60.6%)만** 인용했다.
축을 맞춘 앵커(406/804 = 0.505)보다도 확정(0.362)보다도 나쁘다.
**정확히 적으면 "축을 주면 더 많이, 더 틀리게 놓는다"이고, §9.1이 살아난 것은 개수 기전으로서다.**

### [4] "일괄 1154 > 앵커 720"은 기전 비교가 아니었다 — **대조군을 돌렸다**

720은 실제 축 라벨이고 1154는 오라클이라 **기전 차이와 축 정보 차이가 섞여 있었다.**
앵커 팔에도 같은 오라클 축을 준 대조군: **804/1904**. 일괄이 그보다 **1.44배** 많고,
**804 대 720이 축 정보의 몫**이다. 판정(일괄이 더 많다)은 유지되고 **배수가 1.60 → 1.44로 줄었다.**

### [5] 동작점이 하나였다 — **스윕을 돌렸고 판정은 유지된다**

거부 사유가 `angle_over`·`no_snap` 둘뿐이라 회수율은 두 상수의 통과율이다.

| `axis_deg`·반경 | 회수 | 조용히 틀림 0.2 |
|---|---|---|
| 4 · 0.05 | 479/1904 (0.252) | 0.484 |
| **12 · 0.05**(기준) | **720/1904 (0.378)** | **0.544** |
| 12 · 0.02 | 516/1904 (0.271) | 0.552 |
| 20 · 0.05 | 808/1904 (0.424) | 0.559 |
| 12 · 0.10 | 818/1904 (0.430) | 0.575 |
| 20 · 0.10 | 934/1904 (0.491) | 0.594 |

**여기서도 개수와 조용히 틀림이 단조로 맞바뀐다**(L-B.4가 확정 시점에서 본 것과 같은 형태).
가장 헐거운 점(934)도 오라클 일괄 1154에 못 미치므로 **[4]의 판정이 동작점을 바꿔도 유지된다.**

### [6][12][15] 계수·분모·정의

- **[6]** 회수가 720(1회차)과 938(연쇄 포함) 둘로 쓰였다 → 인용마다 **어느 쪽인지 적는다.**
  잡음 배율도 1회차 3.4배 / 연쇄 포함 **2.5배**로 갈리므로 둘 다 낸다.
- **[12]** `rounds_used.n` 338과 장면 360의 차이는 **대기가 0인 22장면**이다.
  `chain.rounds_denominator`에 카운터로 남겼다.
- **[15]** "12건 중 9건"이 계수 오류였다 — **12행 중 11행**이다. 세 문서를 고쳤다.

### [10][11] 낡은 수치

- **[10]** `progress.md`의 L-B.7 절 ⚠ 범위가 표만 덮고 있었다 → **절 전체로 넓혔고**
  본문의 세 문장(중앙 그대로 · p90 1.22배 · 0.426→0.385)을 **개별로 짚었다**.
- **[11]** `DEFERRED.md`의 `0.4885` → 원장은 **0.4493**이다. 고쳤다.

### [13][14][17] 표기

- **[13]** `promoted == solved_pending`은 항등이 아니라 **연결 거름이 0건이었던 것**이다.
  `promoted_equals_solved` 주석으로 원장에 적었다.
- **[7]** 연쇄 수렴이 **픽스처의 성질일 수 있다** — 장면당 12획이다. `chain.fixture_note`에
  적고 **DEFERRED**로 올렸다(상자 둘 이상 픽스처가 필요하다).
- **[8][9][16]** 게이지를 기계가 읽는 필드로 안 남긴 것 · 인용 규약이 정의 판을 못 가르는 것 ·
  시드 변동폭 → **DEFERRED**. 셋 다 설계에 반례가 더 든다.

### 이 대응이 바꾼 것

**바뀐 결론 하나**: 승격의 품질 격차가 1.5배가 아니다(2회차에서 **배율 몫만 실재**로 다시 좁혔다).
**강해진 경고 둘**: 1점 구도 0.968 · 오라클 일괄이 더 많이·더 틀리게 놓는다.
**유지된 판정 둘**: D-L21(D-L18 기각) · D-L24(일괄이 축만 주면 앵커보다 많다, 배수 1.44).

## L-B.M 리뷰어 2회차 17건 — **같은 주장을 두 번째로 깎았다**

1회차 대응에서 "형태 격차 1.10배"라 적은 것을 리뷰어가 셋으로 갈랐다:
**([3]) 그 1.10은 형태 오차의 비가 아니라 cut 0.2 비율의 비다 · ([5]) 절단 하나만 냈다 ·
([8]) 시드 변동폭을 안 쟀다.** 셋 다 돌렸고 **결론이 또 바뀌었다.**

### [8] 시드 폭이 그 배수를 삼켰다 — **1.10배는 결론이 아니다**

| 시드 | 회수 자기게이지 | 확정 | 배수 |
|---|---|---|---|
| 1 | 0.4417 | 0.3855 | **1.146** |
| 2 | 0.4560 | 0.4239 | 1.076 |
| 3 | 0.4080 | 0.4149 | **0.983** |
| 4 | 0.3217 | 0.3158 | 1.019 |
| 5 | 0.3243 | 0.3750 | **0.865** |
| 6 | 0.4194 | 0.3647 | 1.150 |

**배수가 0.865~1.150으로 1을 가로지른다** — PITFALLS #14대로 **그 결론은 없다.**
같은 시드 표에서 **고정 게이지** 배수는 1.20~1.57로 **여섯 전부 1보다 크다.**

**최종 서술**: **승격이 더 내는 오차는 형태가 아니라 배율이다.**
형태만 보면 회수와 확정이 **구분되지 않고**(시드 폭 안), 차이는 전부
**확정된 틀과 배율이 안 맞는 데서** 온다. 앱이 회수 획에 배율을 다시 안 맞추므로
**그 배율 어긋남이 사용자가 보는 오차다**(cut 0.2 0.544 대 0.362).

### [3] 배수를 하나로 적지 않는다 — 꼬리는 자기 게이지에서도 나쁘다

| 양 | 회수(자기) | 확정(자기) | 배수 |
|---|---|---|---|
| cut 0.2 비율 | 0.397 | 0.362 | 1.10 **(시드 폭 안)** |
| 형태 오차 중앙 | 0.1405 | 0.1222 | 1.15 |
| 형태 오차 **p90** | **0.9144** | **0.4468** | **2.05** |

**셋을 다 적는다.** "형태 격차 N배"라는 한 수를 쓰지 않는다(#8).

### [5] 절단 셋을 다 냈다

자기 게이지 회수: cut 0.1 **431/720** · 0.2 **286/720** · 0.5 **142/720**.
확정: 1360/2416 · 874/2416 · 190/2416. **cut 0.5에서 배수가 2.5배로 가장 크다** —
꼬리 쪽이 나쁘다는 [3]과 같은 방향이다.

### [1] 오라클 팔의 품질 비교 — **다리를 놓으니 한 문장이 뒤집혔다**

셋을 **전부 자기 게이지**로: 일괄 **558/1154 = 0.484** · 앵커 **269/804 = 0.335** ·
확정 **874/2416 = 0.362**.

- "축을 주면 더 많이, 더 틀리게 놓는다"는 **일괄에 대해 유지된다**(0.484 > 0.362).
- ⚠ **"축을 맞춘 앵커도 확정보다 나쁘다"는 틀렸다** — 자기 게이지에서 **0.335로 확정보다 낫다.**
  초판의 0.505는 **고정 게이지**였고 확정 0.362는 `fit`이었다. **고쳤다.**

### [2] 동작점 강건성을 오라클 축에서 다시 봤다

1회차 스윕은 **실 라벨** 앵커였고 그것을 **오라클** 일괄과 견줬다 — [4]가 막 제거한 교란이다.
축을 맞춘 스윕: 반경 0.02 **566** · 0.05 **804** · 0.10 **968**.
가장 헐거운 점도 **968 < 1154**이므로 D-L24는 유지된다.
⚠ **오라클 축에서는 `axis_deg`가 아무 효과가 없다**(각도 시험을 건너뛴다) —
그 스윕이 실제로 흔드는 것은 **스냅 반경 하나**다. 그 사실을 원장에 적었다.

### [6] 부분집합 효과와 추정기 효과를 갈랐다

확정 1979 부분집합을 **자기 게이지**로 재면 743/1979 = **0.375**(전체 2416은 0.362).
같은 부분집합을 **회수 게이지**로 재면 929/1979 = **0.469**.
즉 **부분집합 교체는 1.3pp, 추정기는 9.4pp**다 — 양방향 대조가 성립한다.
⚠ **분모가 안 맞는다**(리뷰어 3회차 [8]): 2416 − 1979 = **437**인데 카운터는 **173**이다.
카운터는 **회수가 0이라 부분집합에 아예 안 들어간 장면의 확정 획**만 센다 —
나머지 **264획**은 `kFix`가 없어(확정 0인 장면) 그 앞 단계에서 빠진 것이고 **따로 안 셌다**.
**DEFERRED에 남긴다** — 두 사유를 갈라 세려면 카운터를 하나 더 놓아야 한다(#10·#11).

### [7][14] 구도로 갈랐다

| 구도 | 확정 cut 0.2 | 회수 cut 0.2 | 연쇄 추가 |
|---|---|---|---|
| 3pt_yaw35_pitch15 | 82/412 (0.199) | 52/163 (0.319) | 59 |
| 3pt_yaw20_pitch25 | 105/393 (0.267) | 49/137 (0.358) | 68 |
| 3pt_yaw50_pitch8 | 169/469 (0.360) | 142/242 (0.587) | 75 |
| 2pt_yaw35_pitch0 | 196/456 (0.430) | 59/85 (0.694) | 16 |
| **1pt_yaw0_pitch0** | **268/422 (0.635)** | **90/93 (0.968)** | **0** |

**1점 구도는 확정도 나쁘고 회수는 거의 전부 틀리며 연쇄가 아예 없다.**
"승격은 연쇄한다"는 **3점 구도의 현상**이다.

### [9][10][15] L-B.6 종단 확인의 구멍 셋

- **[9] 등록 판정 ③의 후반이 공허 통과였다** — 지울 뷰에 승격된 획이 **0개**였다.
  그 뷰에 승격 획 하나를 만들어 넣고 다시 돌린다. 이제 `lifted_in_deleted = 1`이고
  삭제 뒤에도 남는 것을 본다.
- **[15] "숨는다"를 픽셀로 봤다** — 다만 **절대 픽셀 수가 아니라 넣기 전후의 차이**다
  (그 상자 안에 그리드·가이드가 있다). `delta_other_view = 0`이고
  **양성 채널**로 같은 획을 이 뷰 소유로 넣으면 `delta_same_view_control > 0`이다.
- **[10] `SIZE_HEAL.count`와 캔버스 크기를 `l_b6`에 넣었다**(#22 / AS-C7).

### [11][12][13] 기록

- **[11]** 1회차의 [14](`loosen` 미등록)와 [17](0.1222 일치는 보장이다)에 대응이 없었다.
  [14]는 **DEFERRED에 남아 있다**(그대로 유지). [17]은 맞는 지적이라 아래에 적는다:
  **`axis_live` 0.1222와 `promote` 0.1222의 일치는 구성상 보장이지 검증이 아니다** —
  같은 함수·같은 게이지·같은 2416획이다. 그것이 [1]의 **목표**였지 결과의 정당성이 아니다.
- **[12]** 계획서 §11 항목 7이 철회된 문장("일괄 재풀이는 0을 회수한다")을 들고 있었다. 고쳤다.
- **[13]** `HANDOFF.md` 머리의 "9건 닫힘"이 남아 있었다. 고쳤다.

---

## L-B.6 — 뷰 시스템 UI 배선 **완료**

### 한 것

`stage.ts`에 **`pose()` / `setPose()`** — 규약 변환(우리 y 아래·z 안쪽 ↔ three y 위·z 앞)을
**한 곳에만** 둔다(#17). `viewport.unpin`이 이미 같은 뒤집기를 쓰고 있어 두 곳이 될 자리였다.

`l.html`에 뷰 목록 패널, `mainL.ts`에 `switchView` · `viewForDrawing` · `renderViews` ·
삭제 배선. `window.S2S`에 `views` · `currentView` · `switchView` · `pose` · `orbitTo` ·
`viewForDrawing`.

### 사전에 적은 판정 다섯 — 결과

| | 결과 |
|---|---|
| ① 뷰 목록이 뜬다 | ✅ `rows` = 뷰 수 |
| ② 다른 뷰로 갔다가 돌아오면 같은 화면 | ✅ 자세 왕복 `< 1e-9`. ⚠ **보장이라 임계를 안 건다**(#5) — 틀린 자세 대조군이 0.7(세계 좌표 위치 성분, 상자 대각 ≈2)로 움직인다 |
| ③ 지우면 대기 획은 함께, 승격 획은 남는다 | ✅ 총 **15 → 13**(대기 둘만), 승격 **13** 유지. **지울 뷰 안의 승격 획 1개(`lifted_in_deleted`)가 남는 것**까지 본다(리뷰어 [9] 대응) |
| ④ 확정 뷰는 못 지운다 | ✅ 버튼 `disabled` |
| ⑤ 다른 뷰의 대기 획은 숨는다 | ✅ 상태창 표시 + **픽셀 차이 0**(양성 채널 > 0) |

되돌리기가 뷰 삭제도 되돌린다(스냅샷이 뷰를 복사한다).

### 버그 하나를 종단 확인이 잡았다

**`OrbitControls.update()`가 카메라를 자기 `target`으로 다시 겨눈다.** 자세만 넣고
`target`을 안 옮기면 방향이 덮여 **왕복이 안 맞고**, 그러면 `samePose`가 어긋나
**뷰로 돌아가 그릴 때마다 뷰가 하나씩 늘어난다.** L-B.8 스펙의 ④가 그것을 잡았다.
고친 방향은 `target`을 **그 자세의 시선 위**에 두는 것이다 — `update()`가 항등이 된다.
굴림은 `OrbitControls`가 위를 +Y로 고정하므로 원래 표현할 수 없다(자세도 거기서 왔다).

---

## L-B.8 — 궤도 후 계속 그리기 (계획서 §7 · §9.3) **완료**

**착수 시 `PITFALLS.md`를 읽었다.** 걸리는 번호:
**#17**(측정 경로/앱 경로 — 스냅·실시간을 **한 줄도 안 고치고** 좌표만 옮긴다) ·
**#5**(왕복 항등 — `pose`/`setPose`가 서로의 역이라 보장이다) ·
**#23**(§9.3이 도달 가능해진다 — L-B.6에서 못 하던 것) ·
**#22 / AS-C7**(자유 시점의 `f`가 캔버스 높이에서 오므로 크기 굳음에 직접 걸린다) ·
**#3**("돌린 뒤에도 그려진다"는 배선이 되면 거의 정의상 참이다 — 재는 것은 **3D가 제자리인가**다).

### 어떻게 했나 — **기하를 시점 좌표로 옮겨 넣는다**

스냅과 실시간 판정은 **화면 연산**이라 `project(principal, f)`가 카메라를 원점에 둔 것으로
가정한다. 그래서 `snap.ts`·`liveLine.ts`를 고치는 대신 **경계에서 좌표를 바꾼다**:

```
Frame { ctx, toV, fromV, dirV, pinned }
  확정 시점 : ctx = cam.ctx(),  toV/fromV = 항등
  돌린 시점 : ctx = viewPlaceCtx(pose, 세계 축, 캔버스, 45°),  toV/fromV = 자세 변환
```

**세계 축 방향은 첫 카메라가 정한 것 그대로다** — 바뀌는 것은 소실점뿐이다(`viewCamera.ts`).
`snapStart.at`과 `seg3d`는 **세계 좌표로 적는다**. 시점 좌표로 적으면 뷰를 바꿀 때마다
같은 획이 다른 점을 가리킨다.

### 결과 (`stage_browser.json`의 `l_b8`)

| | 결과 |
|---|---|
| 돌린 시점에서 스냅이 돈다 | ✅ 대상 12선, 떠 있는 커서에 표식 |
| 그리면 **새 뷰가 생기고** 소유한다 | ✅ 뷰 1 → 2, `viewRef = currentView` |
| 그 획이 **3D로 놓인다** | ✅ `placed` · 축 번호가 잡힌다 |
| 확정 시점으로 돌아가도 **제자리** | ✅ `seg3d`가 **비트 단위로 안 바뀐다**(세계 좌표다) |
| 같은 자세면 **뷰가 안 늘어난다** | ✅ `viewForDrawing`이 기존 뷰를 찾는다 |

### 안 여는 것 둘 — 사유를 적는다

- **돌린 시점의 지면 스냅은 끈다.** `groundFrame`은 소실점에서 지면을 세우는데 그 소실점은
  시점마다 다르다. **없는 것을 지어내지 않는다**(A-3).
- **돌린 시점의 2D 대기 획을 일괄 풀이에 안 넣는다.** `liftAll`은 **확정 카메라의 소실점**을
  쓰므로 다른 화면 좌표를 그 솔버에 넣는 것은 틀린 계산이다. 그 획은 그 뷰의 2D로 남고
  **앵커가 생기면 그때 올라간다**(§9.1 — 정확히 그 정책의 자리다).

### selfcheck 플래그 넷 — 원인 확인

`l_b8.drawn_in_orbit.axis = 0` · `mid_drag.axis = 0` · `views[*].pending = 0`이
"카운터 0"으로 걸렸다. **셋 다 카운터가 아니다** — 앞의 둘은 **축 번호 0**(가로축)이고
뒤는 **그 뷰에 대기 획이 없다**는 뜻이다(그린 획이 그 자리에서 놓였다). 오탐이다.

---

## 축 판정 절단 곡선 (사람 지시 a·b·c) — ⚠ **아래 초판은 철회됐다**

> ⚠⚠ **이 절의 (a)(b)(c) 결론은 리뷰어가 셋으로 갈라 무너뜨렸다.**
> **살아 있는 서술은 이 절 다음의 "절단 곡선 — 철회와 재측정"이다.**
> 여기 남겨 두는 이유는 **무엇이 어떻게 틀렸는지**가 근거이기 때문이다.


지시: **절단값을 단일 동작점으로 두지 말고 곡선으로 잰다. 앵커 유무 × 절단 0.2/0.35/0.5/0.7에서
배치율과 조용히 틀림을 함께 본다. 교차점이 어디인지 특정한다.**

**착수 시 `PITFALLS.md`를 읽었다.** 걸리는 번호: **#12**(동작점 하나) · **#13**(절단값) ·
**#15**(개수만/비율만 보면 반대로 간다) · **#11**(분모) · **#1**(정의가 하나인가).

### 먼저 — **이 원장 안에 정의 갈림이 하나 더 있었다**

`axis_live`의 **확정 팔**은 `perStrokeError`(적합 게이지)인데 **실시간 팔**은
`Math.max(...)/diag`를 **인라인**으로 적고 있었다. L-B.M의 전수 확인이 **복사된 함수만 찾고
인라인 식을 놓쳤다** — 그것도 정의 갈림이다. 지금은 `metrics.segShapeError`를 부른다.

⚠ **다만 두 팔의 게이지가 다른 것은 옳다**: 실시간 팔의 앵커는 **참 기하 위의 점**이라
결과가 이미 참 좌표계에 있고 배율 자유도가 없다(게이지 1). 확정 팔은 `liftAll`이라 자유도가 있다.
**확정 쪽이 더 관대한 추정기**이므로 "실시간이 낫다"는 결론은 **보수적**이다.

### (a) 교차점 — **절단 0.375**

| 절단 | 확정(앵커 없음) 배치 2416/4320 | 실시간(앵커) 배치 3403/4320 |
|---|---|---|
| 0.1 | 1360/2416 = 0.563 | **781/3403 = 0.230** |
| 0.2 | 874/2416 = 0.362 | **568/3403 = 0.167** |
| 0.35 | 424/2416 = 0.176 | **525/3403 = 0.154** |
| **0.375** | — | **← 교차** |
| 0.5 | **190/2416 = 0.079** | 503/3403 = 0.148 |
| 0.7 | **90/2416 = 0.037** | 369/3403 = 0.108 |

**교차는 0.375**이고 등급별로 precise 0.35 · medium 0.40 · coarse 0.395다 — **거의 같다.**

### (b) 두 조건 모두에서 성립하는 설정 — **있다. 앵커다. 등급별 분기가 필요 없다**

비율만 보면 교차가 있지만 **비율은 분모가 다르다**(2416 대 3403). 배치와 정확도를 한 수로
묶은 `right_over_all`(놓였고 맞은 것 / **전체 4320**)로 보면:

| 절단 | 확정 | 앵커 | 앵커 승 |
|---|---|---|---|
| 0.2 | 0.357 | **0.656** | ✅ |
| 0.35 | 0.461 | **0.666** | ✅ |
| 0.5 | 0.515 | **0.671** | ✅ |
| 0.7 | 0.538 | **0.702** | ✅ |
| 1.0 | 0.549 | **0.748** | ✅ |

**모든 절단 · 모든 등급(15칸 전부)에서 앵커가 이긴다.** 앵커가 **41% 더 놓기 때문**이다.

**따라서 "앵커는 큰 틀림을 늘린다"는 조건부 비율의 성질이고, 같은 획 수에서 맞게 놓이는
개수는 앵커가 언제나 더 많다.** 초판(HANDOFF ⑨)이 "절단 0.5에서 뒤집힌다"고 읽은 것은
**비율만 보고 분모를 안 본 것**이다 — #15의 반대 방향이다.

### (c) DEFERRED로 넘길 것이 없다

두 조건 모두에서 성립하는 설정이 있으므로 **등급별 분기를 두지 않는다**(A-3: 가장 단순한 것).
⚠ 남는 한계 둘은 적는다: **두 팔의 추정기가 다르다**(확정 쪽이 관대하므로 결론은 보수적) ·
**실시간 팔은 참 3D 기하를 대상으로 준 상한**이다(`snap.json`과 같은 조건).

---

## L-C.1 — 차수 승격: 검출 · 전체 재계산 · 뷰 카메라 갱신 (계획서 §6.1) — **착수**

**착수 시 `PITFALLS.md`를 읽었다.** 이 항목에 걸리는 번호:

- **#5 / #30**(자기참조 · 양성 채널) — §6.2가 이미 반증됐다(AS-L6): 재투영 잔차는 **사영 불변**이라
  승격 전후에 **둘 다 0**이다. 그러므로 **"승격이 잘 됐는가"를 재투영으로 재지 않는다.**
  이 항목이 낼 수 있는 것은 **참값 대조**(합성 장면) 하나뿐이다.
- **#16 / #15**(개수만 세면 반대로 간다) — 승격은 **소실점이 하나 더 생기는 것**이라
  배치가 늘 가능성이 높다. **늘어난 배치가 맞는지**를 함께 안 내면 아무것도 안 잰 것이다.
- **#1**(정의) — 형태 오차는 `metrics.ts` 하나이고, 승격 전후는 **카메라가 다르므로 게이지도
  다시 적합해야 한다**. 전과 후를 **같은 게이지로 재지 않는다**(그 자체가 갈림이다).
- **#11**(분모) — 승격 전 배치 집합과 승격 후 배치 집합은 **다른 집합**이다.
  "좋아졌다"를 두 집합의 비율로 적으면 안 되고 **같은 획에서 전후를 짝지어** 본다.
- **#13**(절단) · **#12**(동작점) · **#14**(시드 폭) — 표제 수치에 전부 건다.
- **#23**(계획과 실제) — §6.1의 "사용자 지정 스냅만 유지를 시도한다"가 **성립하는지 먼저 본다**.
  `DEFERRED`의 "차수 승격에서 스냅이 조용히 풀린다"가 정확히 그 자리다.
- **#26**(사전 등록) — 아래에 판정을 **측정 전에** 박는다.

### 사전 등록 — **측정 전에 박는다**

```
승격이 옳으려면:  같은 획 집합에서 (배치 + 회수)가 승격 전 이상이면서
                  조용히 틀림(cut 0.2)이 승격 전보다 **늘지 않아야** 한다
둘 중 하나라도 안 되면 승격은 자동으로 걸지 않는다 — §6.2대로 사용자 확인으로 간다
```

그리고 **스냅 유지**는 별도로 센다: `snapStart`가 있는 획 중 새 카메라에서 **그 대상의 상으로
다시 옮겨진 것**의 비율과, **다시 안 옮기면 어긋나는 거리**(px). 후자가 0이면
"조용히 풀린다"는 걱정 자체가 없는 것이다 — **양성 채널이 먼저다**(#30).

---

## 절단 곡선 — **철회와 재측정** (리뷰어 3회차 [1][2][3][4])

**착수 시 `PITFALLS.md`를 읽었다.** 이 재측정에 걸리는 번호(초판이 빠뜨린 것을 포함한다):
**#2**(측정이 자기확인) · **#3**(지표가 정의의 귀결) · **#8**(꼬리) · **#9**(무엇이 섞였나) ·
**#15**(개수만/비율만) · **#26**(사후에 지표를 만들지 않는다) · **#28**(판정 지표 변경) ·
**#31**(탐색 공간을 목적함수가 좁혔나) · **#14**(시드 폭).
⚠ **초판의 착수 목록에 #3·#26·#28·#31이 없었다** — 그 항목이 한 일이 **새 판정 지표를
만든 것**인데 정확히 그 자리들이다.

### [1] 앵커 팔 분모에 **못박힌 `end_jitter = 0`이 25% 섞여 있었다**

`axis_live`의 실시간 팔 `shape_err.p10 = 0`이 그 서명이다. `snap.json`이 그것을
**"끝점이 참 모서리에 못으로 박힌 셀"**로 규정해 놓았고 **L-B.3 2회차가 이미 한 번 고쳤다.**
확정 팔은 솔버를 거치므로 0이 안 나온다 — **앵커에만 못이 들어와 공짜로 이겼다.**

**`end_jitter > 0`으로 잘라 다시 냈다**(`cut_curve.arms_end_jitter_positive`):

| | 확정(앵커 없음) | 실시간(앵커) |
|---|---|---|
| 배치 | 1563/3240 = **0.482** | 2503/3240 = **0.772** |
| 형태 오차 중앙 | 0.1528 | 0.0340 |
| p90 / max | 0.5231 / **3.35** | 0.7268 / **13.33** |
| 교차(비율) | — | **절단 0.40** |

### [2] **틀린 개수는 교차한다** — "개수에는 교차가 없다"는 거짓이었다

`right_over_all`은 **맞은 개수**만 본다. **틀린 개수**(전체 분모)를 함께 내면:

| 절단 | 확정 `wrong/all` | 앵커 `wrong/all` | |
|---|---|---|---|
| 0.2 | 0.1985 | **0.1460** | 앵커 승 |
| 0.35 | 0.0988 | 0.1327 | **확정 승** |
| 0.5 | 0.0528 | 0.1259 | **확정 승(2.4배)** |
| 0.7 | 0.0256 | 0.0938 | **확정 승(3.7배)** |

**초판이 "비율만 보고 분모를 안 본 것"이라며 철회했던 서술이 실은 맞았다** —
`DEFERRED`의 "앵커는 작은 틀림을 줄이고 **큰 틀림을 늘린다**"가 원장과 맞는다.

### [3] `right_over_all`은 λ = 1을 **암묵적으로 고른 지표**였다

그 지표는 **미배치와 조용히 틀림에 똑같이 0점**을 준다. 그런데 같은 원장의
`policy_objective`가 **"미배치의 비용은 미뤄지는 것이고 조용히 틀림은 영구다"**라고 적어 놨다.
그리고 그 지표는 **교차가 나온 뒤에 만들어졌다**(#26·#28).

**비용비 λ를 흔들었다**(점수 = 맞은 수 − λ × 틀린 수, 전체 분모):

| 절단 | λ=1 | λ=2 | λ=3 | λ=5 | λ=10 |
|---|---|---|---|---|---|
| 0.2 | 앵커 | 앵커 | 앵커 | 앵커 | 앵커 |
| 0.5 | 앵커 | 앵커 | **확정** | 확정 | 확정 |
| 0.7 | 앵커 | 앵커 | 앵커 | **확정** | 확정 |

**작은 절단에서는 앵커가 어떤 비용비에서도 이기고, 큰 절단에서는 비용비가 정한다.**
A-3("조용히 틀린 배치를 만들지 않는다")은 **λ를 크게 보는 쪽**이므로
**큰 틀림 기준으로는 앵커가 진다.** 초판의 "모든 절단에서 앵커가 이긴다"를 **철회한다.**

### [4] "등급별 분기 불필요"도 철회한다 — **등급이 나올 수 없는 팔이었다**

앵커 팔의 배치율은 등급 셋에서 **0.7889 / 0.7861 / 0.7882**로 사실상 같고
조용히 틀림도 0.1743 / 0.1617 / 0.1648이다. 확정 팔은 0.6306 / 0.6028 / **0.4444**로 격차 0.19다.
**앵커 팔이 등급에 둔감한 것은 대상이 참 3D(오라클)이기 때문**이고
거부 사유가 둘뿐이라 "축이 정해지면 반드시 놓인다"가 구성상 항등이다.
**등급 무관은 관측이 아니라 설계의 산물이다**(#31 — 이 프로젝트가 이번에 추가한 번호다).

**진짜 물음(팔 *안에서* 등급별로 `LIVE_TOL.axis_deg`를 달리 둘 것인가)은 안 쟀다** —
`live_time.rows`가 등급 합산이다. **DEFERRED에 남긴다.**

### 사람이 물은 (a)(b)(c)에 대한 **최종 답**

- **(a) 교차점**: `end_jitter > 0`에서 비율 교차는 **절단 0.40**, 틀린 개수 교차는 **≈0.3**.
  ⚠ 시드 폭을 안 쟀으므로 **유효 2자리로 "0.3~0.4 부근"**으로 읽는다(AS-C9).
- **(b) 두 조건 모두에서 성립하는 설정**: **비용비에 달렸다.** λ ≤ 2면 앵커가 모든 절단에서
  이기고, λ ≥ 3이면 큰 절단에서 확정이 이긴다. **λ는 측정 밖에서 와야 하고 그 근거가 없다**
  (사용자가 조용히 틀린 배치를 얼마나 싫어하는지 표본 0).
- **(c)**: 그러므로 **DEFERRED에 남긴다**(지시 c의 조건이 성립했다). 등급별 분기는
  **팔 안에서 다시 재야** 판단할 수 있고 그것도 DEFERRED다.

---

## L-C.1 — 차수 승격 **완료**

### 한 것

`src/s3d/promoteOrder.ts` — **전부 다시 푼다**(§6.1). 두 번 돈다:
1차는 스냅을 무시하고 올려 **대상의 새 3D**를 얻고, 2차에서 `snapStart.ofId`를 그 대상의
**새 상**으로 다시 옮긴 뒤 다시 푼다. 앱 배선은 `차수 승격` · `소실점 다시` 두 버튼이고
되돌리기는 기존 `실행취소`가 그대로 받는다(스냅샷이 문서 하나다).

### 사전 등록한 규칙의 판정 — **자동으로 걸지 않는다**

```
승격이 옳으려면: 배치가 안 줄면서 조용히 틀림(cut 0.2)이 안 늘어야 한다
```

| | 승격 전 | 승격 후 |
|---|---|---|
| 짝지은 배치(같은 획) | only_before **570** | only_after **402** → **−168** |
| 형태 오차 중앙 | 0.1259 | **0.0913** |
| p90 | 1.1301 | **0.5395** |
| cut 0.2 | 186/552 | **172/552** |
| cut 0.5 | 90/552 | **60/552** |
| 획별 | — | 개선 **356** · 악화 **194** · 동일 2 |

**품질은 나아지는데 배치가 준다** — 소실점이 셋이 되면 축 판정이 더 까다로워진다.
**등록한 두 조건 중 배치가 깨졌으므로 자동으로 안 건다.** §6.2(사용자가 보고 되돌린다)가 답이다. ⚠ **그 근거는 D-L26이 대체했다**(리뷰어 2회차) — D-L25 아래에서 배치 감소는 더 이상 기각 사유가 아니고, 판정을 세우는 것은 **§6.2**(무결론이면 자동화의 근거가 없다)다.
시드 여섯 전부에서 조용히 틀림 비가 **0.80~1.00**이라(1을 안 넘는다) 품질 개선은 시드 폭 밖이다.

### 양성 채널 둘

- **가짜 승격 대조군**(#6·#30): 참 2점 장면에 **없는 세 번째 소실점**을 넣으면
  악화 **200** 대 개선 **96**, cut 0.2 **119 → 180**. **그러므로 위의 개선은 측정이다.**
- **차수가 실제로 바뀌었다**(#32): `order_changed.all_promoted = true`(전부 2 → 3).
  안 바뀌었으면 그 실행은 승격에 대한 정보가 0이다.

### DEFERRED 항목 하나가 실측으로 닫혔다

**"차수 승격에서 스냅이 조용히 풀린다"** — 다시 안 옮겼다면 어긋났을 거리가
중앙 **0.595px**인데 **p90 311px · max 639px**다. **걱정이 실재한다**(양성 채널이 먼저다, #30).
`promoteOrder`가 **1381/1676**(참 승격 216장면만. ⚠ **초판은 1908/2391이라 적었는데 그것은 가짜 승격 대조군 72장면이 섞인 값이다** — L-C.2 리뷰어 [5], #9)를 다시 옮긴다. ⚠ **295개(17.6%)는 대상이 새 카메라에서 안 놓여 못 살렸고**,
끝점·중점 말고는 매개변수가 안 정해져 **`a`쪽 끝점으로 근사**한다(DEFERRED).

### 브라우저 확인 (`stage_browser.json`의 `l_c1`)

2점으로 확정(3D 10획) → 수직 가이드 둘 추가 → `차수 승격` →
**차수 2 → 3 · 3D 10 → 11 · 스냅 2/2 재고정 · 기하가 실제로 움직인다** →
`실행취소`가 **비트 단위로 복원한다**. §5.3의 "전환 무변화"는 **초기 확정에 한한 이야기**이고
승격은 화면이 움직이는 것이 정상이다(§6.2).

---

## λ 결정 — 사람이 정했다 (2026-08-16)

`DEFERRED.md`가 "λ는 측정 밖에서 와야 한다"고 남겨 둔 항목을 사람이 닫았다.
**λ = 3 · 절단 0.5**를 사전 등록 목적함수로 박는다 → **D-L25**.

근거(사람 지시 그대로): 미배치는 2D 레이어로 가고 **승격 대기**가 된다(비용이 유예된다).
틀린 배치는 **조용히 남고 그 위에 구조가 쌓인다**(비용이 영구이고 복리다). 비용이 대칭이 아니다.
A-3("애매하면 놓지 않는다 / 놓지 않되 버리지 않는다")이 같은 판단이다.

⚠⚠ **초판은 여기에 "절단 곡선이 그것을 수치로 확인했다"와 "λ=3을 하한으로 읽는다"를 적었다 —
둘 다 철회한다**(리뷰어 1회차 [2][3] · 2회차). 그 칸(cut 0.5 · λ=3)의 여유는 **0.0025**이고
n = 3240에서 **8획어치**다. **시드별로 다시 재니 부호가 3 대 3으로 갈리고 폭이 0.2166**이다
(`axis_live.json@1b6175a4`의 `seed_spread_at_dl25_point`) — 합산 여유의 **87배**다.
"λ≥5에서는 뚜렷이 이긴다"도 같은 표의 같은 열이므로 함께 무너진다. **그러므로 "하한"이라는 읽기도 없다.**
⚠ 절단 0.5도 측정이 고른 것이 아니다(#13) — λ=3에서 확정이 이기는 것은 **cut 0.5~0.6뿐**이고
cut 0.4·0.7·1.0에서는 앵커가 이긴다. 0.5는 `metrics.ts`의 **등록 절단 셋 중 큰 것**일 뿐이다.
⚠ 두 팔의 **추정기도 다르다**(확정=적합 게이지 · 앵커=게이지 1)라 "확정이 낫다"는 할인해서 읽는다.
**채택 근거는 측정이 아니라 정책 하나뿐이고** 그 사실을 D-L25에 적었다.

⚠ **그렇다면 그 정책의 사실 전제도 대조해야 한다**(2회차 리뷰어). 전제는 "미배치는 유예된다"이고
그것이 **AS-L11**인데 그 항목의 현재 상태는 ⚠️ **"개수는 성립·연쇄는 수렴(픽스처 한정).
품질은 반증. 1점 구도에서 거의 전부 틀린다"**다 — **유예는 절반만 참**이고 회수율은 동작점 하나에서
37.8%(연쇄 포함 49.3%)다. **그래도 λ ≥ 3을 유지한다**: 회수가 절반이어도 틀린 배치는 회수 확률이
0이고 그 위에 구조가 쌓인다. ⚠ **다만 1점 구도에서는 그 논증이 안 선다** — 연쇄가 0이면
유예가 아니라 **영구 미배치**다. **구도별 λ는 미측정이고 DEFERRED다.**

**옛 등록 규칙을 대체한다.** "조용히 틀림 ≤ 기준이면서 배치 최대"는 **완화 방향만 볼 수 있었다**(#31).
새 규칙은 한 수라 양방향이다. 사후 변경이 아니다(#28) — 근거가 측정 밖에서 왔고 그것을 적었다.

### 그것이 D-L21을 되열었다 — **아직 안 뒤집는다**

D-L25 목적함수로 `promote.json@1b6175a4`의 세 팔을 다시 채점하면 **조이기가 이긴다**:

| 절단 0.5 · λ=3 | 놓임 | 조용히 틀림 | 맞음 | 점수 /4320 |
|---|---|---|---|---|
| 기준 | 3136 | 394 | 2742 | (2742 − 1182)/4320 = **0.361** |
| 조이기 | 2976 | 334 | 2642 | (2642 − 1002)/4320 = **0.380** |
| 늘리기 | 3343 | 595 | 2748 | (2748 − 1785)/4320 = **0.223** |

절단 0.2에서도 조이기가 이긴다(−0.218 대 −0.446). 교차는 **λ=1과 λ=2 사이**다
(λ=1 절단 0.5: 기준 0.5435 대 조이기 0.5343 — 기준이 이긴다).

⚠ **뒤집지 않는 이유는 시드 폭이다**(#14). `promote.json`에 시드별 분해가 없고
차이 **80/4320**은 L-B.4가 실측한 배치 시드 폭 **31**과 같은 자릿수다.
뒤집으면 `AXIS_TOL`이 바뀌어 그것에 의존한 산출물이 전부 STALE이 된다(D-C4).
**L-D 종단 검증에서 함께 본다** — 상수 변경을 흡수할 자리가 거기다. `DEFERRED.md`에 올렸다.

## PITFALLS #33·#34 추가 (사람 지시)

- **#33 전수 확인이 함수만 찾지 않았는지 본다** — L-B.M의 정의 통일 점검이 인라인 식 하나를
  놓쳤고 절단 곡선에서 발견됐다. **문법 형태로 찾는 점검은 같은 계산의 다른 표현을 놓친다.**
  값 대조를 함께 한다. 정의 해시(D-L23)도 같은 사각지대다
- **#34 고친 결함이 같은 자리에 재발하지 않았는지 본다** — `end_jitter = 0` 혼입이
  L-B.3 2회차에서 고쳐졌고 절단 곡선에서 재발했다. **고칠 때 그 조건을 쓰는 다른 호출부를 함께 본다**

---

## L-C.2 — 되돌리기 UI (§6.2) **착수 → 완료**

### PITFALLS 대조 (착수 시 — 이 항목에 걸리는 번호)

| # | 왜 걸리나 | 어떻게 막나 |
|---|---|---|
| **#7** 추측 말고 카운터 | "승격에서 무엇을 잃었나"를 추정으로 쓰면 안 된다 | 잃은 획·못 살린 스냅을 **id 집합으로 직접** 모은다 |
| **#10** 뺄셈으로 만들지 않는다 | 못 살린 스냅을 `had − reanchored`로 세고 있었다 | `promoteOrder`가 `lost_ids`를 **직접 모은다** |
| **#5·자기참조 3** | 스냅샷을 되돌리고 스냅샷과 비교하면 **설계 보장**이지 측정이 아니다 | 원장에 **보장이라고 적고 임계를 안 건다**. **양성 채널**(#30)로 비교가 눈뜬 것임을 보인다 |
| **#17** 앱 경로와 측정 경로 | 요약·차이 계산이 UI 안에 있으면 하네스가 다른 것을 잰다 | `promoteDiff.ts` 순수 함수 하나. 앱과 하네스가 같이 쓴다 |
| **#19** 설명이 검사를 끈다 | 주석으로 "카메라는 안 되돌린다"고 적으면 그 문장이 근거가 된다 | 되돌린다. 안 되는 것은 **표시**한다 |
| **#21·#22** dpr·크기 굳음 | 새 캔버스 표식을 그린다 | CSS 픽셀 규약 유지 · `drawBelowInk` 안에서 그린다 |
| **#23** 계획과 실제 | §6.2가 "되돌리기가 주 수단"인데 **지금 `실행취소`는 카메라를 안 되돌린다** | 그것이 이 항목의 결함이다 — 고친다 |
| **#26** 게이트 수치는 측정 전에 | 사전 등록 절을 먼저 쓴다 | 아래 |
| **#34**(새) 같은 자리 재발 | 승격이 `pts2d[0]`을 옮기므로 되돌리기가 그것도 되돌려야 한다 | 왕복에서 `pts2d`까지 비트 대조 |

### 사전 등록 — 판정 (측정 전에 박는다, #26)

1. **되돌리기는 왕복에서 문서와 카메라를 **전부** 복원한다**(설계 보장 — 임계 없음).
   양성 채널: **다른 상태의 스냅샷**으로 되돌리면 대조가 **틀렸다고 말해야** 한다.
   ⚠ **완료 시점에 이 문장을 좁혔다**(리뷰어 [9]) — 이 구성이 확인할 수 있는 것은
   **"스냅샷이 담은 필드 전부"**이지 "앱 상태 전부"가 아니다. 담기지 않은 필드의 목록과
   사유를 원장에 적는 것으로 대신한다.
2. **잃은 것이 0이 아니다** — 승격 픽스처에서 `dropped`·`snap_lost`가 실제로 생긴다
   (0이면 이 UI는 아무것도 안 보이는 것이고 그 사실을 적어야 한다).
3. **`diffPlacement`의 반례 테스트** — 인위로 하나를 떨어뜨리면 `dropped`에 그 id가 정확히 하나 는다.

### 한 것

**① 되돌리기가 카메라까지 되돌린다 — 이것이 결함이었다.**
`실행취소`가 `snapshotDoc` 하나만 되돌렸다. 그리기·지우기에서는 충분했다(카메라가 안 움직인다).
**승격은 다르다** — 되돌리면 기하는 옛 해로 가는데 소실점은 새 것으로 남아
§6.1이 금지한 **좌표계가 섞인 상태**가 정확히 생긴다. ⚠ **화면은 그럴듯하다**(3D는 어차피
그려지고 잉크는 제자리다) — 그래서 아무도 못 본다. `src/ui/appSnap.ts`가 문서·가이드·
`locked`·`lockedOrder`·승격 요약을 함께 담고, **앱과 하네스가 같은 함수를 부른다**(#17).

**② 차수를 명시한 되돌리기**(§6.2가 요구한 것). `2점으로 되돌리기` 버튼이 도구 막대에 뜬다.
⚠ **표식을 뜨는 시점이 `차수 승격`을 누를 때가 아니다** — 그때는 사용자가 이미 가이드를 더
세워 놓아 카메라가 **새 차수**다. 그 상태를 "2점"이라 부르면 되돌려도 소실점이 셋이다.
뜨는 자리는 **`확정`과 `소실점 다시`** 둘이고 후자가 **카메라를 만지기 직전**이다.
지금 차수의 표식은 안 낸다(있는 자리로 가는 버튼은 아무 일도 안 한다).

**③ 잃은 것을 화면에 표시한다**(사람 지시). 상태 패널 맨 위에 승격 요약이 뜨고,
캔버스에 **내려간 획 = 주황 점선 · 끊긴 스냅 = 빨간 ⊘**를 그린다.
집합은 **id로 직접 모은다**(#10) — 개수만으로는 어느 획인지 못 그린다.
`promoteOrder`에 `lost_ids`·`reanchored_ids`를 넣었고 `diffPlacement`가 전후를 짝짓는다.

**④ 재연결은 버튼이다 — 자동으로 안 한다.** 옛 대상이 새 카메라에서 안 놓였으므로
재연결은 **다른 대상에 붙는 것**이고, 소리 없이 하면 A-3("조용히 틀린 배치를 만들지 않는다")을
정면으로 어긴다. **D-L25(λ = 3)가 같은 방향**이다 — 미배치의 비용이 틀린 배치의 1/3이다.
붙일 곳이 없으면 표시된 채 남는다.

**⑤ 곁가지 — 상태 줄의 별표가 그대로 나오고 있었다.** 안내문 대부분이 마크다운으로 적혀
있는데 상태 줄은 HTML을 그린다. `소실점 다시`·`가이드 늘리기`·`확정`의 문장이 전부 그랬다.
L-C.2의 요약이 **강조에 기대는 화면**이라 여기서 드러났다. 고친 자리는 **쓰는 곳 하나**(`md()`)다.

### 사전 등록한 판정 — 셋 다 통과 (`order_undo.json@1b6175a4`)

**판정 ② 잃은 것이 0이 아니다.** 216장면에서 **내려간 획 570 · 올라간 획 402**
(placed_delta **−168** — `order_promote.json`과 같은 수다.
⚠ **그것을 #33(값 대조)이라 부른 것은 과장이고 고쳤다**(리뷰어 [6]) — 같은 픽스처·같은 산식이라
그냥 결정성이다. 여기서 실제로 확인되는 것은 **#17**이다: 짝짓기를 **앱이 부르는 `diffPlacement`**로
하는데도 하네스가 직접 짝지은 값과 같다 — 앱 경로와 측정 경로가 안 갈렸다).
장면당 내려감 중앙 **2** · p90 **5** · max **10**.
**176/216 장면에서 무언가 내려온다** — 이 UI는 실제로 보일 것이 있다.
스냅은 **1676개 중 295개(17.6%)가 끊기고** 94/216 장면에서 생긴다.
⚠ `order_promote.json`의 스냅 수(2391 / 1908 / 483)와 다른 것이 정상이다 — 그쪽은 **가짜 승격
대조군 72장면까지 합산**한 값이고 여기는 참 3점 216장면만이다(#9·#11). 검산: 2391 − 1676 = 715 ·
1908 − 1381 = 527 · 483 − 295 = 188이고 527 + 188 = 715다.
⚠ **그 혼입값이 `HANDOFF.md`·`DEFERRED.md`·이 문서에 참 승격의 수로 인용돼 있었고 고쳤다**
(리뷰어 [5], #1: 재측정하면 인용 문서를 전부 찾아 고친다). `order_promote.json`의
`snap_reanchor`에도 **범위를 적었다**(#11 — 한 원장 안에서 배치는 216장면, 스냅은 288장면이다).

**판정 ① 되돌리기 왕복.** ⚠ **설계 보장이지 측정이 아니다**(자기참조 유형 3) — 스냅샷을
되돌리고 그 스냅샷과 비교하는 것이므로 **임계를 안 걸었다**. 값은 **양성 채널**에 있다:
같은 대조를 승격 **후** 상태에 걸면 **14자리를 이름으로 짚는다**
(`lockedOrder` · `guides.length` · `strokes[i].pts2d[0]` · `strokes[i].seg3d`).
⚠ **`pts2d`를 본다**(#34) — 승격이 스냅된 시작점을 옮기므로(L-C.1이 p90 **311px**로 실측)
그것을 안 보면 "되돌렸다"가 **시작점만 새 자리에 남은 상태**를 통과시킨다.

⚠ **초판은 "전부 복원한다"고 적었는데 그것은 이 구성으로 확인할 수 없는 주장이다**(리뷰어 [9]).
**스냅샷이 아예 안 담은 필드는 `snapDiff`도 못 보므로 양성 채널도 못 본다** — L-C.1이 실제로
잡은 결함이 정확히 그 형태였다. 그래서 **담는 것과 안 담는 것을 원장에 나눠 적었다**(`covers`):
담는 것 일곱, **가이드에서 파생되는 것 여섯**(`vps`·`principal`·`f`·`fSource`·`order`·`acc` —
`apply()`가 다시 만든다. 브라우저에서 `fSource`가 3점 '수심' → 2점 '두 소실점'으로 돌아오는 것을
확인했다), **되돌릴 대상이 아닌 것 다섯**(`tool`·`axisLock`·`hoverSnap`·`live`·`note` —
그리는 중의 상태다, A-3).

⚠ **왕복 횟수도 적었다**(리뷰어 [10]) — **n = 8**이고 어기는 방식을 바꿔 가며 돈다
(가이드 이동·가이드 추가·`seg3d` 제거·`seg3d` 이동·`pts2d`·`snapStart`·`locked`·전부).
매번 **어긴 것이 대조에 잡히는지 먼저 보고**(전부 잡힌다) 되돌린다(전부 복원).

**판정 ③ 반례 테스트.** `test/promoteDiff.test.ts` 8건 — 하나를 떨어뜨리면 그 id가 정확히
하나 잡히고, 한쪽에만 있는 id도 조용히 안 버린다(#11), 요약 문장이 잃은 것을 안 빠뜨린다.
`snapDiff`의 양성 채널도 여기서 자리별로 확인한다.

⚠ **selfcheck가 `set_vs_counter_mismatch = 0`을 "집계 미작동 의심"으로 플래그한다.**
그것이 옳은 의심이라 **같은 술어에 어긋난 쌍을 넣어 발화를 보였다**
(`set_vs_counter_probe_fires: true`, 검사 횟수 **432**).

### 브라우저 확인 (`stage_browser.json`의 `l_c2`)

⚠ **첫 픽스처는 아무것도 안 잃었다**(`dropped 0 · snapLost 0`) — 그 상태로 통과시키면
**표시 경로를 한 줄도 안 지난 공허 통과**다(리뷰어가 L-B.6 [9]에서 같은 형태를 잡았다).
그래서 **잃는 픽스처를 따로 만들었다**(medium · jitter 0.04 · seed 4242):

| | |
|---|---|
| 내려간 획 · 끊긴 스냅 | **6** · **4**(재고정 1/5) |
| 패널 문구 | 둘 다 나온다 |
| **캔버스 표식** | 주황·빨강 **픽셀로 확인**(dpr 배율을 곱해 읽는다, #21) |
| 재연결 버튼 | **3/4가 다른 대상에 붙었다**(1개는 붙을 곳이 없다) |
| 되돌리기 | 재연결을 되돌리면 **표시가 다시 나온다** |
| 일반 `실행취소` | `locked` false · `lockedOrder` **2** · 3D 10획 복원 |
| `2점으로 되돌리기` | 그 사이 그린 획까지 한 번에 · `pts2d[0]`까지 **비트 단위 복원** |

⚠ **`실행취소` 한 번이 가는 자리를 정확히 적는다**: **승격 직전**이고 그것은
"`소실점 다시`로 셋째 축까지 세웠지만 아직 승격은 안 한" 상태다(앱이 명시적으로 지원한다).
그러므로 그 자리에서 `S.order()`는 **3이 맞다** — 가이드가 그대로이기 때문이다.
되돌아와야 하는 것은 `locked`·`lockedOrder`·기하 셋이고 **초판은 그 셋을 안 되돌렸다.**

### 이 항목이 말하지 않는 것

- **사용자가 그 표시를 보고 옳게 판단하는가** — 표본 0(AS-C1). 표시가 있다는 것과
  그것이 판단을 낫게 한다는 것은 다르다
- **재연결이 붙이는 자리가 옳은가** — 그것은 스냅의 문제이고 `snap.json`이 잰다.
  여기서 잰 것은 **끊긴 것이 보이고 되돌릴 수 있는가**뿐이다
- **돌린 뷰에서의 표시** — `drawPromoteLoss`는 확정 시점에서만 그린다(`pts2d`가 그 화면
  좌표이기 때문이다). 돌린 뷰에서 승격하면 표시가 없다 → `DEFERRED`
- **여러 차수 표식이 동시에 있을 때**(1점·2점·3점) — 픽스처가 2 → 3 하나뿐이다 → `DEFERRED`

### 리뷰어 1회차 12건 — 대응

**높음 넷은 전부 재측정으로 답했고 결론 둘이 바뀌었다.**

**[1] `web/progress.md`를 만들었다** — CLAUDE.md가 명시로 금지한 그 자리다(`⚠ progress.md를 web/에서
만들지 않는다`). 쉘 작업 디렉토리가 `web/`일 때 `cat >>`를 해서 생겼고, **λ 결정·PITFALLS 추가·
L-C.2 착수 기록 전체**가 거기로 갔다. 루트로 합치고 `web/progress.md`를 지웠다.
완료 본문이 L-C.1 헤더 아래에 헤더 없이 붙어 있던 것도 고쳤다(`## L-C.2 — 되돌리기 UI (§6.2)`).

**[2] D-L25의 "절단 곡선이 수치로 확인했다"를 철회했다 — 재측정이 리뷰어를 지지했다.**
그 칸(cut 0.5 · λ=3)의 여유는 **0.0025**(n=3240에서 8획어치)인데 **시드 폭을 안 쟀다**.
`axis_live`에 시드별 λ 점수를 넣어 다시 재니:

| 시드 | 1 | 2 | 3 | 4 | 5 | 6 |
|---|---|---|---|---|---|---|
| 확정 − 앵커 | **+0.133** | −0.030 | **+0.035** | **+0.032** | −0.083 | −0.072 |

**부호가 3 대 3으로 갈리고 폭이 0.2166**이다 — 합산 여유의 **87배**다.
**그 칸은 D-L25의 근거가 못 된다.** D-L25는 **정책만으로 서고** 그렇게 적었다.
같은 세션이 D-L21 재채점은 시드 폭 때문에 보류하면서 이 수는 폭을 안 보고 박았다 — 그 비대칭이 지적의 핵심이었고 맞다.

**[3] 절단 0.5의 근거도 적었다**(#13). λ=3에서 확정이 이기는 것은 **cut 0.5~0.6뿐**이고
cut 0.4·0.7·1.0에서는 앵커가 이긴다. 0.5는 `metrics.ts`의 **등록 절단 셋 중 큰 것**이고
그 이상의 근거가 없다 — D-L25에 그렇게 적었다.

**[4] L-C.1의 등록 규칙도 D-L25로 다시 채점했다 → D-L26.** 대체 범위를 D-L18 하나로 본 것이
틀렸다. 다시 채점하려면 **한쪽만 놓인 획의 형태 오차**가 필요한데 초판 하네스가 `both_placed`에서만
쟀다. 넣어서 재니 **합산으로는 승격이 이긴다**(절단 0.5·λ=3에서 0.1443 → **0.1705**, +0.0262).
⚠ **그런데 시드별 부호가 또 3 대 3이다**(+0.079/+0.113/+0.069 대 −0.046/−0.025/−0.032, 폭 **0.1597**).
**D-L25 목적함수는 승격에 대해 아무 말도 안 한다.** 남는 것은 A-3이고 그것은 **자동으로 안 거는 쪽**이다 —
**판정은 같고 근거가 달라졌다.** 옛 근거("배치가 −168로 준다")는 D-L25 아래에서 더 이상 기각 사유가 아니다.

**[5] `483/2391`이 가짜 승격 대조군 혼입값이었다**(#9·#11·#1). 참 승격만의 값은 **1381/1676**이고
못 살린 것은 **295(17.6%)**다. `HANDOFF.md` 두 곳 · `DEFERRED.md` · `progress.md`를 고쳤고,
`order_promote.json`의 `snap_reanchor`에 **범위**를 적었다(한 원장 안에서 배치 216장면 · 스냅 288장면).

**[6] `−168` 일치를 #33이라 부른 것은 과장이다** — 같은 픽스처·같은 산식의 결정성이다. **#17**로 고쳤다.

**[7] 브라우저 손실 픽스처의 출처를 원장에 적었다**(#25·#12). 18조합을 훑어 셋 중 하나를 골랐고,
**모집단의 p90 자리**(끊김 0.80 대 모집단 0.176)이며 **격자 밖**(jitter 0.04 · seed 4242)이라는 것을
`loss_fixture_provenance`에 적었다. 결론이 아니라 **코드 경로를 지나려고** 고른 것이다.

**[8] "유일한 판정 수단"을 한정했다** — AS-L6이 반증한 것은 **재투영 잔차 하나**이고
`tiltDeg`는 같은 문서가 "실제로 움직인다"고 적었다. 그 시험이 **L-C.3**이다.

**[9] "전부 복원한다"를 "스냅샷이 담은 필드 전부"로 좁히고 `covers`를 적었다**(위 판정 ① 참조).

**[10] 왕복 횟수 n = 8**을 넣었다. **[11]** 중복된 `moved_by_promotion`을 지웠고 `names`를 전부 냈다.
**[12]** `HANDOFF.md`는 세션 끝에 갱신한다.

### 리뷰어가 확인해 준 것

D-L25가 인용한 λ 표 값 여섯 · D-L21 재채점의 세 산식(0.361 / 0.380 / 0.223)과 λ 교차 ·
`order_undo.json`의 구도별 합 · 상수 해시 `2fd74e1c` · 정의 해시 `3df57ac7`이 전부 원장과 맞는다.

### 리뷰어 2회차 14건 — 대응

**높음 셋 중 둘은 문서 정정, 하나는 논증이 틀렸다.**

**[높음 ①] 철회한 문장이 `progress.md`·`DEFERRED.md`에 남아 있었다** — **#33이 기록한 그 사각지대다.**
숫자(`483/2391`)는 `grep`으로 전수 정정했는데 **산문 주장**("절단 곡선이 수치로 확인했다",
"λ=3을 하한으로 읽는다")은 안 찾았다. **문법 형태로 찾는 점검은 같은 주장의 다른 표현을 놓친다** —
#33이 정확히 그 말이고 이번에 그대로 걸렸다. 셋 다 고쳤다.
"λ≥5에서는 뚜렷이 이긴다"도 **같은 표의 같은 열**이므로 함께 무너진다 — 그래서 "하한"이라는 읽기도 없다.

**[높음 ②] D-L25가 유일하게 기대는 정책 전제가 AS-L11에서 절반만 참이다.**
전제는 "미배치는 유예된다"인데 AS-L11의 상태가 ⚠️ **"품질은 반증. 1점 구도에서 거의 전부 틀린다"**이고
**1점 구도는 연쇄가 0**이다. **λ ≥ 3은 유지한다** — 회수가 절반이어도 틀린 배치는 회수 확률이 0이고
그 위에 구조가 쌓인다. ⚠ **그러나 1점 구도에서는 그 논증이 안 선다**(연쇄 0 = 영구 미배치).
**구도별 λ를 DEFERRED에 새 행으로 올렸다.**

**[높음 ③] D-L26의 "A-3이 남는다"가 틀렸다 — 같은 원장이 반대 방향이다.**
승격은 **모든 절단에서 조용히 틀림을 줄인다**(cut 0.1 628 → 485 · 0.2 379 → 308 · 0.5 187 → 128).
A-3의 두 조항은 **둘 다 승격 쪽**을 가리킨다. 판정을 세우는 것은 **§6.2**다 —
"사용자가 즉시 보고 되돌린다"이고 **무결론이면 자동화의 근거가 없으므로 현상유지(수동)**다.
그리고 승격은 화면이 실제로 움직이는 조작이라 근거 없이 자동으로 걸면 **사용자가 안 시킨 큰 변화**다.
**판정은 그대로, 논증을 갈아 끼웠다.**

**[중간] 양성 채널을 넣었다**(#30) — `dl25_rescore`에 **가짜 승격 대조군 재채점**이 없었다.
넣으니 등록 동작점에서 **−0.3414(−295획)**이고 **절단 여섯 전부에서 벌한다**.
즉 **목적함수는 눈이 떠 있고**, 참 승격에서 결론이 없는 것은 **효과가 시드 폭 안**이기 때문이다.
이것이 없었으면 "합산으로 승격이 이긴다"가 목적함수의 편향인지 승격의 성질인지 안 갈렸다.

**[중간] 절단을 여섯으로 늘리고 여유를 획수로도 냈다**(#13·#14). `CUTS = [0.1, 0.2, 0.35, 0.5, 0.7, 1]`.
λ=3에서 **참 승격은 여섯 전부에서 이기고**(여유 68~404획) 가짜 승격은 여섯 전부에서 진다.
⚠ **λ=2·절단 0.5 칸의 여유는 9획**이고 같은 세션이 8획짜리 칸을 철회했다 — **같은 기준이면 그 칸도 못 쓴다**고 적었다.

**[중간] 게이지 적합 모집단이 다르다**(1122 대 954). D-L22가 같은 종류에서 **부분집합 효과를 1.3pp**로
실측해 놓았고 여기 합산 여유는 **2.6pp**다 — **이미 알려진 편향의 2배 안**이다. caveat에 적고 DEFERRED.

**[중간] `covers.held`가 `Stroke` 여덟 중 넷만 적었다** — 열거가 새면 대응도 샌다.
전부 분류했고 **`held_but_not_compared`**를 새로 갈랐다(`id`·`userAxis`·`viewRef`·`color`·`width`는
**담기지만 `snapDiff`가 안 본다** — 복원은 되고 *복원 실패를 잡는 능력*이 없다).
`Stroke.layer`는 `SStroke`에 아예 없다(#18이 잡은 그 필드이고 L 전환에서 빠졌다).

**[중간] `derived_not_held`의 브라우저 근거가 한 경로뿐이다** — `revert_by_order`만 가이드를 바꾼다.
일반 `실행취소`는 이 픽스처에서 가이드가 6 → 6이라 파생 복원을 안 시험한다. 그 사실을 적었고,
`acc`가 **가이드만 받는다**는 것도 적었다(`byAxis(this.guides)`가 유일한 출처 — 확정 뒤 획은 소실점을 안 바꾼다).

**[중간] `HANDOFF.md`의 "다음에 할 것"이 L-C.2였다** — 반쪽 갱신이 미갱신보다 나쁘다. L-C.3부터로 고쳤다.
**[중간] "배치가 깨졌으므로"** 두 자리(`progress.md`·`HANDOFF.md`)에 **D-L26이 근거를 대체했다**를 달았다.

**[낮음] `skew 0.37`을 `off_grid`에 넣었다**(하네스는 0.12 — 3배다. 훑기에서 흔들지 않은 고정값이다).
**[낮음] `trials`의 개별 1 대 `all` 8**을 설명했다 — `all`은 `locked`(1) + `guides.length`(1) +
**획 여섯의 `seg3d.presence`**(6)다.
**[낮음] `web/progress.md` 대조** — 그 파일은 **커밋된 적이 없어** git에 없다. 다만 합칠 때
파일 **전체를 통째로** 루트에 넣었으므로(부분 발췌가 아니다) 누락이 구성상 없다.

### 2회차에서 대응 못 한 것 → `DEFERRED`

- **`seed_spread`의 정확히 1.00**(L-C.1 표제) — 한 시드에서 개선이 0이다. "품질 개선은 시드 폭 밖"이
  경계에 닿아 있다. 다시 재려면 그 시드의 전후 개수를 갈라 세야 한다
- **게이지 적합 모집단 차이의 몫**(2.6pp 중 얼마가 부분집합 효과인가)
- **`held_but_not_compared` 다섯을 대조에 넣기** — 지금 경로가 안 건드리므로 넣을 이유가 없었다.
  건드리게 되면 그때 넣는다
- **#26 범위**: D-L25로 한 재채점은 전부 **이미 수집된 데이터**이므로 사전 등록이 아니라 사후 재해석이다.
  그래서 **판정을 뒤집는 데 안 썼다**(둘 다 기존 판정 유지). 그 사실을 D-L25에 적었다

---

## L-C.3 — 대체 자동 신호 (§6.2) **착수**

### PITFALLS 대조 (착수 시)

| # | 왜 걸리나 | 어떻게 막나 |
|---|---|---|
| **#30** 사영 불변부터 본다 | 이 항목이 존재하는 이유가 **AS-L6이 §6.2의 재투영 잔차를 사영 불변으로 반증**한 것이다. 후보 둘도 같은 함정일 수 있다 | **f를 ±20% 틀리게 주고 값이 움직이는지** 먼저 본다(`camera_gate`의 양성 채널 방식). 안 움직이면 **거기서 끝난다** |
| **#5** 자기참조 유형 3 | f² 편차는 **주점을 수심으로 잡은 뒤** 그 수심이 보장하는 양을 재는 것일 수 있다 | 판별법: **틀린 카메라를 넣어 움직이는지.** 안 움직이면 측정이 아니다 |
| **#26** 게이트 수치는 측정 전에 | "신호가 쓸 만하다"의 기준을 **측정 전에** 박는다 | 아래 사전 등록 |
| **#13** 절단값이 결론을 정한다 | 신호의 임계를 하나만 고르면 그 결론은 없다 | ROC처럼 **여러 동작점**을 낸다 |
| **#14** 시드 폭 | L-C.2에서 **두 번 걸렸다**(D-L25·D-L26) | 표제 수치는 **시드별로 먼저** 낸다 |
| **#15** 개수만 세면 반대로 간다 | "옳은 승격을 통과시킨다"만 보면 **전부 통과시키는 신호**가 이긴다 | **가짜 승격 대조군**을 함께 낸다(양·음 둘 다) |
| **#11** 분모 | 신호가 안 나오는 경우(카메라가 안 서는 장면)를 분모에서 빼면 성공률이 부푼다 | 산출 불가를 **따로 센다** |

### 사전 등록 — 판정 (측정 전에 박는다, #26)

**신호가 쓸 만하려면 셋을 다 만족해야 한다.**

1. **사영 불변이 아니다** — f를 ±20% 틀리게 주면 값이 움직인다(양성 채널, #30).
   **이것을 못 넘으면 나머지를 안 잰다.**
2. **자기참조가 아니다** — 틀린 카메라(가짜 소실점)를 넣으면 값이 움직인다(#5).
3. **참 승격과 가짜 승격을 가른다** — 어떤 동작점에서든 **참 승격 통과율 − 가짜 승격 통과율 ≥ 0.5**.
   못 가르면 §6.2대로 **사용자 확인으로 간다**(계획서가 이미 그렇게 적었다).

⚠ 셋 다 만족해도 **자동으로 걸지는 않는다** — D-L26이 승격 자체에 결론이 없다고 적었다.
신호의 쓸모는 **"되돌리기를 권하는 것"**이지 "자동으로 승격하는 것"이 아니다.

### 한 것 — **후보 둘 다 안 선다. §6.2의 대비책으로 간다**(`order_signal.json@1b6175a4`)

**후보 ② 소실점 삼각형의 f² 편차 — ❌ 자기참조 유형 3, 정보량 0**(규칙 ②에서 죽는다).

`fFromThreeVps`가 주점 `P`를 **삼각형의 수심**으로 잡는다. 그런데
`(V₁−P)·(V₂−P) = (V₂−P)·(V₃−P) = (V₁−P)·(V₃−P)`는 **수심의 항등식**이다.
그러므로 세 f²의 편차는 **어떤 삼각형에서도 0**이다 — 추측이 아니라 셌다(#7):
참 소실점 셋 · **크게 틀린 VP3 셋**(+400, +900) · **가짜 승격** · **둔각**(f²<0이라 카메라가 안 선다) ·
임의 삼각형까지 **아홉 전부 최대 2.6e-16**이다.

계획서 §6.2가 인용한 "이론서 6.3의 자유도 0 조건"은 **맞다**. 틀린 것은 그것을 잴 수 있다는 전제다 —
**주점을 수심으로 잡는 순간 그 자유도가 소진된다.** 잴 수 있으려면 주점이 **독립으로** 주어져야 하고
(2점 경로의 이미지 중심 가정처럼), 3점 경로에서는 주점이 소실점에서 나온다.
**AS-L6과 같은 유형의 오류이고, 계획서가 지시한 "먼저 확인한다"가 그것을 잡았다.**

**후보 ① 획별 `tiltDeg` — 규칙 ①은 통과, 규칙 ③에서 죽는다.**

⚠ **리뷰어 두 번을 거치며 팔이 둘 → 셋이 되고 게이지가 바뀌었다. 결론의 이유가 뒤집혔다.**

| 팔 | 무엇 | 장면 | Δtilt 중앙 | **자기 게이지** 형태 오차 | **고정 게이지** |
|---|---|---|---|---|---|
| **참 승격** | 참 3점, 참 VP3 | 216(신호 없음 20) | +0.312 | 0.0912 | 0.0912 |
| **가짜 승격** | 참 **2점**, 없는 VP3 | 72(3) | +0.393 | 0.2537 | **3.1196** |
| **틀린 VP3** | 참 3점, VP3 **(+400,+900)px** | 216(21) | +0.167 | 0.1079 | **0.2599** |
| _구도 대조군_ | 같은 2pt 장면을 **옳게 2점으로** | 69 | 0.1435 | — |

**규칙 ①(사영 불변인가) — 아니다, 통과.** f×0.8/1.0/1.2에서 장면 중앙 **1.414 / 1.316 / 1.228**.
섭동한 f는 **축 판정·`liftAll`·`frameOf` 셋 전부**를 먹인다.

**규칙 ③(오승격을 가르는가) — 못 가른다.** 양방향을 다 훑어도(초판은 한 방향만 봤다)
`wrong_vp3` 구도별 **0.118 / 0.074 / 0.242** · `spurious` 합산 **0.1979**다.
시드 부호도 갈린다(여섯 중 둘).

### ⚠⚠ 2회차가 초판의 이유 두 개를 뒤집었다

**① "등록 기준 0.5가 도달 불가능했다" — 철회.** **도달 가능하다.**
**참값을 고정 게이지로 재면 판별력이 `spurious` 0.9694 · `wrong_vp3` 0.5446**으로 둘 다 0.5를 넘는다.

**② "`wrong_vp3`는 애초에 가를 것이 거의 없다" — 철회.** **가를 것이 많다.**
그 팔의 **VP3 축 각오차가 중앙 5.59°**(2.97~9.84°)로 **AS-L9 임계 0.5°의 11배**이고,
고정 게이지 형태 오차가 0.0912 → **0.2599**(2.85배)다.

**뒤집은 것은 게이지다.** 초판이 쓴 자기 게이지는 **최소제곱이 전역 배율을 적합해 없앤 뒤**의 수이고,
**AS-L11이 "앱이 배율을 다시 안 맞추므로 그 배율 어긋남이 사용자가 보는 오차다"라고 적어 둔
바로 그 성분을 지운 것**이다. `spurious`가 0.2537 → **3.1196**으로 12배 벌어지는 것이 그 증거다.

**구도 효과도 갈랐다.** 같은 2pt 장면을 **옳게 2점으로 푼** 대조군이 자기 게이지 **0.1435**라
`spurious`의 0.2537 중 일부는 구도였다. **그러나 고정 게이지에서는 3.1196 대 0.0912라
구도로 설명되지 않는다.**

**그러므로 판정의 형태가 바뀐다:** `tiltDeg`가 실패한 것은 **잴 것이 없어서가 아니라
이 신호가 못 보기 때문**이다. 이유는 **f 오차와 축 오차를 한 수에 섞기 때문**이다 —
f가 클수록 tilt가 작아지는 단조 관계라 두 원인이 겹친다.

### 판정 — **사용자 확인으로 간다**(§6.2가 이미 적은 두 갈래 중 하나)

계획서 §6.2: "대체 자동 신호 후보 둘을 시험하되, 사영 불변이 아닌 양인지 먼저 확인한다.
**아니면 되돌리기만 쓴다.**" 시험했고 안 섰다. **그 되돌리기가 L-C.2다** —
차수를 명시한 되돌리기와 승격이 잃은 것의 표시. **우회가 아니라 계획서의 갈래다.**

⚠ **다만 "잴 것이 없어서"가 아니다.** 고정 게이지 참값으로는 오승격 두 종류가 **둘 다 0.5를 넘게
갈린다**(0.9694 · 0.5446). **관측 가능한 신호를 아직 못 찾은 것**이고, 그 여지가 크다는 것이
이 항목이 남긴 가장 쓸모 있는 사실이다. `DEFERRED`에 그렇게 적었다.

### 이 항목이 말하지 않는 것

- **다른 신호가 없다** — 시험한 것은 계획서가 적은 후보 **둘**이다.
  고정 게이지 참값이 **0.9694 · 0.5446**까지 가르므로 **메울 여지가 크다.**
  ⚠ 그 수를 **"상한"이라 부르지 않는다** — 한 대리 참값의 성능이지 도달 가능성의 증명이 아니다.
  그 사이를 메우는 **관측 가능한** 양이 있는지는 미측정이고 `DEFERRED`에 후보 둘을 적었다
- **사람이 잘 판단한다** — 자동 신호가 없다는 것과 사람이 잘 한다는 것은 다른 주장이다(표본 0, AS-C1)
- **앱 조건** — 여기는 참 소실점을 넣고 뺀다. 앱에서는 사용자가 맞춘 소실점이라 그 오차가 더해진다

### selfcheck가 같은 것을 잡았다

`order_signal.json`의 f² 편차 아홉 행 전부에 **`near-zero(<1e-10) → 무노이즈/자기참조 검증 의심`**이
붙었다. **그 플래그가 곧 이 항목의 결론이다** — 규칙이 의심으로 잡은 것을 측정이 확인했다.
원장에 **"자기참조 유형 3이고 임계를 걸지 않는다"**고 적었다(CLAUDE.md §5.1의 처리 그대로).
검증 현황: vitest **297/297**(53파일) · tsc 0 · pytest 52/52 · playwright 17/17 · STALE 0.

### 리뷰어 1회차 14건 — 대응

**높음 둘이 결론을 바꿨다. 둘 다 재측정으로 답했다.**

**[높음 1] 구도 교락** — 참 팔은 3pt 구도 셋, 가짜 팔은 2pt 구도 하나였다.
층화해 보니 **가짜 팔은 3pt 층에서 아예 비어 있어 구도 안에서는 잴 수조차 없다.**
즉 합산 0.1979는 **구도 간 비교**이고 구도 효과가 섞여 있었다.

**[높음 2] 오승격의 두 종류 중 하나만 쟀다** — 후보 ②에는 `wrong_vp3` 대조 행을 만들어 놓고
후보 ①에는 안 썼다. **세 번째 팔을 넣었다**(참 3점 장면 + VP3를 (+400, +900)px 옮김).
참 팔과 **같은 구도 셋**이라 교락이 없고, 거기서 **구도별 판별력이 0.015 · 0.074 · 0**이다.
⚠ 그리고 새 사실이 나왔다: **그 팔은 참값으로 갈라도 0.1265**다 — 형태 오차가 **1.18배**라
**애초에 가를 것이 거의 없다.** 그 오승격은 실제로 별로 안 나쁘다.

**[중간 3] "오라클이 상한"을 철회했다** — 그것은 **형태 오차 중앙 임계 규칙 한 가족의 격자
최댓값**이지 판별의 상한이 아니다. 따라서 **"등록 기준 0.5가 도달 불가능했다"도 증명되지 않았다.**
판정을 ①②③(기준과 무관한 근거)에 다시 세웠다.

**[중간 4] f 섭동이 파이프라인을 관통하는가** — 관통한다. `ctx.f` 하나가 **축 판정·`liftAll`·
`frameOf` 셋 전부**를 먹인다. 원장에 적었다.

**[중간 5·6] 층화를 넣었다** — 구도 · 잡음 4수준 · 등급 3 · **시드 3을 뺀 판**.
시드 3을 빼면 `vs_spurious`가 0.1979 → **0.1393**으로 준다(그 시드가 합산을 지배했다).

**[중간 7] `no_signal` 성격** — 셀별 분포(`no_signal_by`)를 남겼고 탈락 비율 차(9.3% / 4.2% / 9.7%)를
원장에 적었다. **결론이 탈락 방향에 안 달렸다** — 탈락이 전부 한쪽으로 갔다고 가정해도 0.5에 못 닿는다.

**[낮음 9·10·11·14] 표기** — AS-L6에 **단위**를 명시했고(2.06/3.49는 획별·잡음별, 1.414/1.316/1.228은
장면 중앙·합산), `wrong_vp3` 오프셋을 원장에 넣었고, **이론서 6.4가 이미 항등식으로 적어 두었다**는
사실을 §6.2 주석과 원장에 넣었다(#23의 사례 — 인접 절을 읽었으면 측정 전에 기각할 수 있었다).

**[낮음 12] DEFERRED에 L-C.3 절**을 만들고 **PITFALLS #35·#36**을 추가했다.

### ⚠ 이 대응이 버그 하나를 드러냈다

층화를 넣자 `bestOf`가 **빈 팔에 `Math.max(1, 0)`**을 써서 없는 비율을 내고 있었다 —
가짜 팔이 없는 3점 층에 **−0.03**, 참 팔이 없는 2점 층에 **판별력 1**.
`null`로 고쳤고 **PITFALLS #36**으로 올렸다.
_그 사이 초판 대응문에 적었던 "구도 안에서 판별력이 음수다"는 **그 버그의 산물이고 철회한다.**_

**[낮음 8] Δtilt의 공통 성분** — 두 팔 모두 Δtilt 중앙이 양수(+0.31 / +0.39 / +0.17)인 것은
**2점 → 3점이라는 공통 구조 변화**로 설명될 수 있다는 지적. 그것을 따로 안 갈랐다 →
**`wrong_vp3` 팔이 그 물음에 부분적으로 답한다**(같은 구조 변화에서 판별력 0에 붙는다).
완전히 가르려면 무잡음·참 카메라의 Δtilt를 기준선으로 빼야 하고 그것은 DEFERRED.

### 리뷰어 2회차 13건 — 대응

**높음 셋이 결론의 이유를 뒤집었다. 셋 다 재측정으로 답했고 초판 문장 둘을 철회했다.**

**[높음 1] `wrong_vp3`의 축 각오차가 원장에 없었다** — 픽셀만 적으니 AS-L9(임계 0.5°)·
AS-L8("먼 소실점 100px이 2~3°")과 **대조가 안 됐다**. 도로 냈다: **중앙 5.59°**(2.97~9.84°) —
**임계의 11배**다. **"그 오승격은 별로 안 나쁘다"는 틀렸다.**

**[높음 2] 형태 오차가 자기 게이지였다** — AS-L11이 "**앱이 배율을 다시 안 맞추므로 그 배율
어긋남이 사용자가 보는 오차다**"라고 적어 둔 성분을 최소제곱이 지우고 있었다.
고정 게이지로 다시 내니 `spurious` 0.2537 → **3.1196**(12배) · `wrong_vp3` 0.1079 → **0.2599**다.
**오라클도 다시 냈다: 0.4979 → 0.9694 · 0.1265 → 0.5446.** 둘 다 등록 기준 0.5를 넘는다.

**[높음 3] 오라클에도 같은 구도 교락이 있었다** — 참 팔 3pt 셋 대 가짜 팔 2pt 하나.
**같은 2pt 장면을 옳게 2점으로 푼 대조군**을 넣었다: 자기 게이지 **0.1435**라 0.2537 중 일부는
구도였다. ⚠ **그러나 고정 게이지에서는 3.1196 대 0.0912라 구도로 설명되지 않는다.**

**철회 둘:**
1. **"등록 기준 0.5가 도달 불가능했다"** → **도달 가능하다**(0.9694 · 0.5446).
2. **"`wrong_vp3`는 애초에 가를 것이 거의 없다"** → **가를 것이 많다**.

**판정의 형태가 바뀌었다**: `tiltDeg`가 실패한 것은 **잴 것이 없어서가 아니라 이 신호가 못 보기
때문**이다. 그것이 이 항목이 남긴 가장 쓸모 있는 사실이고 `DEFERRED`에 후보 둘을 적었다.

**[중간 4] `bestOf`가 한 방향만 훑고 있었다**(#31). `wrong_vp3`의 Δtilt는 참 팔보다 **작아서**
자연스러운 방향이 반대였다 — 한 방향만 보면 반대 방향 신호가 전부 ≤0으로 깔린다.
양방향으로 고치니 구도별이 0.015 · 0.074 · 0 → **0.118 · 0.074 · 0.242**(`dir: lt`)다.
**0.5에는 여전히 못 닿지만 "0에 붙는다"는 서술은 틀렸다.**

**[중간 5·6·7·10·11·13] 표기·필드** — `registered_bar_above_ceiling`·`arms_are_separable`·
`what_this_does_not_say`의 "상한" 표현을 note와 맞췄고, `progress.md`의 낡은 **0.4684**를 고쳤고,
**세 팔 전부에 시드별 Δtilt 중앙**을 넣었고, 층화 행에 `dir`을 넣었다.

**[중간 8] PITFALLS #35를 좁혔다** — "오라클도 못 넘으면 기준을 다시 박는다"가 **#26의 반대편 문**을
연다는 지적이 맞다. **"적기만 한다. 기준을 낮추지 않는다"**로 고쳤고,
**"오라클의 지표를 먼저 의심한다"**를 이번 사례(자기 게이지)와 함께 넣었다.

**[중간 9] DEFERRED가 "남은 것은 전부 참값이 필요하다"고 너무 넓게 닫았다** —
원장 자신이 **주점을 독립으로 고정한 f² 편차**를 명시했었다. 새 행으로 올렸다(AS-C5 미검증 단서와 함께).
`tiltDeg`의 f 성분을 빼는 것도 행으로 올렸다.

**[낮음 12] `HANDOFF.md`** — 세션 끝에 갱신한다(아래).

---

## L-D.1 — 사용자 지정·삭제 경로 (§9.5) **착수**

### PITFALLS 대조 (착수 시)

| # | 왜 걸리나 | 어떻게 막나 |
|---|---|---|
| **#17** 앱 경로/측정 경로 | 선택·삭제·축 지정이 UI 안에만 있으면 하네스가 못 부른다 | `S2S`에 손잡이를 내고 Playwright가 **앱 경로 그대로** 부른다 |
| **#21·#22** dpr·크기 굳음 | 획을 **클릭으로 고른다** — 좌표 변환이 걸린다 | `canvasFrame` 단일 출처를 탄다(`onDrag`/`onHover`와 같은 규약) |
| **#23** 계획과 실제 | §9.5는 두 문장뿐이다("면 위에 있다 수준의 지정 경로와 지우는 경로") | **선례를 따른다**(A-3): SketchUp의 지우개와 화살표 축 지정. 새로 설계하지 않는다 |
| **#19** 설명이 검사를 끈다 | "면 지정은 범위 밖"이라 주석에 적으면 그 문장이 근거가 된다 | 면 생성이 범위 밖인 것은 `DEFERRED`에 이미 있다 — **축 지정으로 대신하고 그 차이를 적는다** |
| **#7** 추측 말고 카운터 | "지정하면 올라간다"를 추정으로 쓰면 안 된다 | 지정 후 **실제로 놓였는지 세어** 화면과 원장에 낸다 |
| **#26** 사전 등록 | 아래 |

### 사전 등록 — 판정 (측정 전에 박는다, #26)

1. **삭제가 문서에서만 지우지 않는다** — 씬·스냅 후보·뷰 소유가 함께 정리된다. 되돌릴 수 있다.
2. **축 지정이 실제로 획을 올린다** — 대기 획에 축을 주면 **놓이는 개수가 는다**(0이면 이 경로는 쓸모가 없고 그 사실을 적는다).
3. **지정한 축은 재분류가 안 덮는다** — 차수 승격을 걸어도 유지된다(`userAxis`, §6.1).

### 한 것 — 고르기·삭제는 선다. **축 지정은 안 올린다**(사전 등록 ②의 답이 0이다)

`src/ui/pick.ts`(순수, `PICK_TOL` 등록) + `고치기` 도구. 클릭으로 고르고
**← 축1 · → 축2 · ↑ 축3**(그리는 중의 축 고정과 **같은 배정**, A-3) · **Delete** 삭제 · **Esc** 해제.

**브라우저 확인**(`stage_browser.json`의 `l_d1`, 상자 12모서리 + **면 대각선 둘**):

| 사전 등록 | 결과 |
|---|---|
| ① 삭제가 문서에서만 안 지운다 | **통과** — 획 14 → 13, **스냅 대상 12 → 11**(함께 줄었다), 선택 해제 |
| ①(되돌리기) | **통과** — `실행취소`가 14로 복원하고 그 id가 돌아온다 |
| ② 축 지정이 획을 올린다 | ❌ **0/2다.** `userAxis`는 서고 축도 지정한 값인데 **안 놓인다** |
| ③ 지정한 축을 승격이 안 덮는다 | `userAxis: true`가 선다(`promoteOrder`의 `relabel`이 그것을 건너뛴다) |

**② 가 0인 원인을 특정했다 — 추측이 아니다**(#7):

1. **일괄 풀이는 앵커가 없다.** `solveInto`가 **대기 집합끼리만** 푼다 —
   대각선 둘만 남은 집합은 서로를 구속하지 못한다. L-B.7이 이미 같은 것을 쟀다(일괄 **0/1904**).
2. **앵커 경로는 사용자 축을 안 본다.** `promoteChain` → `resolveLive`가 **축을 다시 추론**하고,
   면 대각선은 어느 축과도 `LIVE_TOL.axis_deg`(12°) 안에 안 들어와 거부한다.
   **`userAxis`가 그 경로에 전달되지 않는다.**

**고치는 방향은 정해져 있다**(D-L19): `resolveLive`는 **이미 축 고정을 받는다**(Shift가 쓰는 그 인자).
사용자 지정 축을 그 인자로 넘기면 된다. **바깥에서 덮으면 안 된다** — D-L19가 그것을 금지했다.
⚠ **이번 세션에서 안 했다** — 컨텍스트가 남지 않았고, 그 변경은 §7의 실시간 경로를 건드리므로
**측정(오배정이 함께 느는지, #16)이 붙어야 한다.** `DEFERRED`에 올렸다.

⚠ **면 지정이 아니라 축 지정이다.** 면 생성이 범위 밖이라(DEFERRED) 줄 수 있는 것은 방향뿐이고,
면 지정은 획을 **평면에 가두는** 다른 조작이다. §9.5의 문장을 그대로 만족시키지는 못한다.

### ⚠ 상수 해시가 움직였다 — `46c028d1` → `2fd74e1c`

`PICK_TOL`을 `SHARED_CONSTANTS`에 등록해서다(A-4·D-C4: "새 임계는 `test/constants.ts`에 넣는다.
빠지면 STALE이 안 잡힌다"). **L-A.2가 `VP_TOL`을 등록하며 20건을 STALE로 만들고 받아들인 것과 같다** —
D-L10이 "해시가 바뀌어 산출물이 STALE이 된다"를 등록 거부 근거로 쓰지 말라고 못 박았다.

**값은 안 바뀌었다**(기존 임계는 그대로이고 하나가 늘었을 뿐이다). 이번 실행에서 하네스를
전부 다시 돌렸으므로 **STALE 0**이고, 문서의 인용 **62곳**을 새 해시로 고쳤다(#1·§5 인용 규약).

검증 현황: vitest **307/307**(54파일) · tsc 0 · pytest 52/52 · playwright **18/18** ·
**STALE 0** · 상수 해시 **`2fd74e1c`** · 정의 해시 `3df57ac7`.

### 리뷰어 1회차 11건 — **결론을 철회한다**

**높음 넷 중 둘이 이 항목의 결론을 뒤집는다. 컨텍스트가 남지 않아 재측정은 다음 세션이다.**

**[높음 2] "0/2는 결함이다"를 철회한다 — 살아 있는 원장이 반대를 적고 있다.**
`face_diagonal.json@1b6175a4`가 **바로 이 픽스처**(면 대각선)에 대해 이미 적었다:
**"축으로 배정된 사선이 놓인 것은 회수가 아니라 조용히 틀린 배치다. 갈라 세지 않으면
회수율이 부풀려진다 — 처음에 그렇게 셌다가 고쳤다."**
수치는 회수된 사선 중앙 **0.0216** 대 축 오배정 중앙 **0.4483**(p90 0.9532) — **20배**다.
즉 **면 대각선에 축을 주고 놓는 것은 이 저장소가 이미 기각한 배치**이고,
**0/2는 결함이 아니라 옳은 동작일 수 있다.**
⚠ **그러면 `DEFERRED`가 적은 수정 방향(사용자 축을 `resolveLive`에 넘긴다)도 위험하다** —
이 픽스처에서 2/2가 놓이는데 그 둘이 **0.4483 자리**일 수 있다.
그리고 `face_diagonal`의 접근은 **"면은 축 둘이 편다 + 앵커가 위치를 정한다"**로
미분류 사선의 **84.16%**를 놓는다 — **§9.5가 원한 것은 축 지정이 아니라 그쪽**일 수 있다.
**사전 등록 ②의 기준("놓이는 개수가 는다")에 옳음 게이지가 없었다**(#9·#15).

**[높음 1] "일괄 0/1904"를 근거로 인용한 것을 철회한다** — 이 저장소가 **미실행**이라고
이미 철회한 수다(#32, D-L24). 같은 원장에 **승격 없이 대기 집합만 다시 풀면 207/1904(10.9%)**가
있다. **원인 ①("일괄은 앵커 없이 못 푼다")은 근거를 잃었다** — 남는 사실은
**이 픽스처의 대기 집합이 2획뿐**이라는 표본 크기이고, 그러면 원인은 기전이 아니라 픽스처다.

**[높음 3] 사전 등록 ③은 미판정이다.** "차수 승격을 걸어도 유지된다"를 등록해 놓고
**승격을 한 번도 안 걸었다** — `userAxis: true`만 보고 `promoteOrder`의 코드를 읽어 통과 처리했다.
**코드 읽기는 카운터가 아니다**(#7). 원장 `l_d1`에 승격 필드가 하나도 없다. **미판정으로 되돌린다.**

**[높음 4] #35를 안 지켰다 — 바로 앞 항목이 만든 규칙이다.** 등록 ②에
**"무엇이 이 기준을 넘을 수 있는가"**(오라클)가 없다. 그래서 **0/2가 축 지정의 성질인지
픽스처의 성질인지 갈리지 않는다.** L-C.3이 정확히 그 형태로 실패했고 그래서 #35를 만들었다.

**[중간 5·6·9·10] 원장이 얇다** — 씬·뷰 소유 필드가 없는데 "통과"라 적었고
(`snap_targets` 12→11은 `lifted` 12와 같은 수라 **독립 증거가 아닐 수 있다**),
등급·시드·jitter·dpr·`SIZE_HEAL.count`가 없고(#12·#14·#21·#22), n=2이고, Esc 경로가 미측정이고,
축 번호 규약(0-기반)이 원장에 없다.

**[중간 7] 해시 훑기가 `.md`만 덮었다**(#33 재발) — 산출물 JSON과 소스 주석에 `@46c028d1`이
셋 남아 있었다. **고쳤다.** ⚠ **"62곳"의 분모도 확인이 안 된다**(.md 일치 줄은 61이다).

**[중간 8] `PICK_TOL = 0.0145`의 근거가 없다** — 선례(SketchUp 10~15px)에서 온 **잠정값**이고
**측정이 정하지 않았다.** `SNAP_TOL`은 `unintended_snap`으로 정했고 그 근거가 적혀 있다(D-L14).
**그렇게 적는다**(아래 DECISIONS).

**[낮음 11] §9.5를 안 고쳤다**(#23) — 고친다(아래).

## L-D.2 저장·내보내기 + 배포 (2026-08-16)

착수 시 `PITFALLS.md` 최근 다섯을 읽었다. 걸린 것: **#34**(v1 `store.ts`가 같은 IndexedDB를
쓰므로 **키를 갈랐다** — `current` 대 `current2`), **#18**(`saveNote`를 화면이 읽게 했다),
**#32**(인앱 브라우저가 서비스 워커를 막아서 **Playwright로 다시 쟀다** — 미실행을 반증으로
처리하지 않는다).

### 1. 저장·복원 v2 — 뷰가 함께 간다

`src/ui/docStore.ts`. v1은 `Stroke[]` 하나였고 **뷰가 없었다**. L의 문서는 뷰가 2D 획의
**소유자**이므로(§9.1) 뷰를 안 담으면 **2D 레이어가 통째로 사라진다** — `viewRef`가 가리킬
곳이 없어 어느 시점에서 그린 것인지 잃는다.

담는 것: 뷰 목록(자세) · 획 전부(`pts2d` 원본 · `seg3d` · 축 · 사용자 축 · 스냅 출처) ·
카메라 제약 **입력 그대로** · 현재 뷰 · 차수 · id 시퀀스.
`test/doc_store.test.ts`(7)가 잠근다 — 왕복 항등, **id 이어받기 반례**(안 이어받으면 새 획이
불러온 획과 겹친다), **없는 뷰를 가리키는 획**은 확정 뷰로 데려온다(조용히 안 사라진다),
v1 문서 거절.

### 2. 내보내기 — 뷰 이름이 파일에 남는다

`exportGeom`이 `Stroke` 대신 **최소 모양**(`ExportLine`)을 받는다 — 옛 UI와 L이 **같은 함수**를
쓴다(#17). OBJ는 `# axis=… view=…` 주석, glTF는 노드 이름 `s3@확정 뷰`.
**3D로 올라간 획만** 나가고 2D 레이어는 개수를 알린다(조용히 안 줄인다).
자체 JSON은 **2D 레이어까지 담는 유일한 경로**다.

### 3. 정적 배포 — 서버가 필요 없다

`base: "./"`(상대 경로)로 빌드하므로 **하위 경로든 루트든** 열린다. `.github/workflows/pages.yml`이
빌드 전에 `tsc`·`vitest`를 돌린다. `l.html`에 manifest·apple-touch-icon을 붙였고 `mainL`이
`./sw.js`를 **상대 경로로** 등록한다(Pages 하위 경로에서 `/sw.js`는 남의 자리다).

**확인**(`e2e/static_deploy.spec.ts`): 빌드본을 `/SKETCH2SPACE/` 아래에 올려
① 열리고(절대 경로 자산 0) ② 서비스 워커 scope가 그 하위 경로이고
③ **오프라인 새로고침에서도 뜬다**. 셋 다 통과.
_인앱 브라우저에서는 서비스 워커 등록이 막혀 ②·③을 못 쟀다 — 그것을 "안 된다"로 읽지 않고
러너를 바꿔 다시 쟀다(#32)._

### 4. 원격 접속

README 1.5절에 `cloudflared tunnel --url https://localhost:5173`을 적었다 —
같은 망이 아니어도 되고 HTTPS가 자동이라 자체 서명 문제가 사라진다.
LAN + 자체 서명은 **폴백**으로 남긴다.

### 검증
vitest **314/314**(신규 `doc_store` 7) · Playwright 정적 배포 1/1 · pytest 58/58 · tsc·빌드 통과.
selfcheck **1552건 · STALE(상수) 0 · STALE(정의) 0**(정의 해시 `a45078a5`).
⚠ 초판은 "STALE 0"이라 적었는데 그때 **STALE(정의) 1건**이 살아 있었다(#1이 자기 세션에서 재발했다) —
`rate()`를 `metrics.ts`에 넣어 정의 해시가 움직였고 `e2e.json`이 낡았다. **다시 돌려서 0으로 만들었다.**

## PITFALLS 작동 문제 (2026-08-16, 사람 지시) — 착수 기록

착수 시 읽은 것: 목록 전체(당시 36개). 이 항목이 만든 규칙에 스스로 걸린 자리:
**#33**(#36 자동화를 `Math.max(1,n)` **문법 하나**로 만들었다 — 같은 세션에서 재발) ·
**#1**(`rate()` 추가로 정의 해시가 움직였는데 "STALE 0"이라 적었다) ·
**#35**(검사를 만들었지만 **덮는 원장이 0건**이다).

한 것: #33·#35·#36을 자동 검사로(반례 6건) · 최근 다섯 상단 · 자동 표에 **덮는/안 덮는 범위** ·
리뷰어에 세션 내 재발 점검 · #37 신설.

## §9.5 재검토 (2026-08-16, 사람 지시) — 착수 기록

착수 시 읽은 것: 최근 다섯. 걸리는 번호: **#9·#15**(개수만 세지 않는다) · **#2**(대리값) ·
**#12·#14**(동작점·시드 폭) · **#35**(오라클).

**새로 재지 않았다**(지시). `face_diagonal.json@1b6175a4`만으로 D-L28을 세웠고,
리뷰어 지적으로 **결론을 사선으로 좁혔다** — 그 원장의 모집단은 면 대각선 120개뿐이고
축이 미분류라 대기 중인 획(1116)에 대해서는 아무 말도 안 한다.
`corner_err_ratio`를 "형태 오차"라 부른 것도 고쳤다(D-L22의 지표와 다른 양이다, #28).
**남은 것**: #35의 오라클이 없다(그 원장은 §9.5 조건이 아니라 자동 경로의 것이다).

## L-D.3 착수 기록 (2026-08-16) — **이전 세션이 안 적었다**

⚠ **`progress.md`에 L-D.3 절이 없었다**(리뷰어 [12]). 커밋 `3362dec`가 착수했는데
CLAUDE.md §2의 "걸리는 번호를 적는 것이 읽었다는 증거"가 빠졌다. 소급해서 적는다 —
**소급이라는 사실을 함께 적는다**(#28: 사후에 기록을 만들지 않는다. 이것은 판정이 아니라 누락 보완이다).

걸리는 번호: **#17**(측정 경로와 앱 경로 · 덮는 범위를 함께 적는다) ·
**#25**(브라우저 확인을 원장에 남긴다) · **#26**(판정을 측정 전에 박는다) ·
**#30**(양성 채널 — 통과가 무엇을 배제했는지) · **#22**(캔버스 크기 굳음, 창을 앞에 두고).

## 자동 검사가 덮는 대상 수를 낸다 (2026-08-16, 사람 지시) — 착수 기록

착수 시 읽은 것: 최근 다섯. 걸리는 번호: **#32**(미실행을 반증으로 처리하지 않는다 —
이 항목 자체가 그것이다) · **#19**(검사를 약화시키는 방향으로 고치지 않는다) ·
**#35**(등록한 기준의 도달 가능성) · **#33**(문법 형태로만 찾는 점검의 사각지대).

### 문제

**#35 검사가 0건이었고 그것이 "깨끗함"으로 읽혔다.** 반례 테스트가 검사 자체의 동작은
확인했지만, **원장 37개 중 `gate`라는 이름의 블록을 가진 것이 하나도 없어** 덮는 대상이 0이었다.
이것이 #32의 상황이다 — 0건이 깨끗함인지 안 돎인지 안 갈린다.

### 고친 쪽 — **검사가 아니라 기록하는 쪽**

검사의 이름 목록을 넓혀 0을 지우는 것은 **#19**(설명이 검사를 끄는 것과 같은 방향)이므로
반대로 갔다. `web/test/gate.ts`의 `gate()`가 `registered`·`reachability` **둘을 강제**하고
(비면 던진다), 여섯 하네스가 그 이름으로 적는다:

| 원장 | 등록 기준 | 도달 가능성 |
|---|---|---|
| `camera_gate` | L-A.7 재등록 — 축 오차 0.5°에서 1.1배·2.1배, 5구도 전부 | **지표는 도달 가능**(같은 하네스 `deg_0`가 정의상 1.0배). ⚠ **사람이 0.5°에 닿는지는 표본 0**(AS-L9) |
| `lift_gate` | L-A 게이트(초판) | 같은 하네스 `true_camera_oracle` 팔. ⚠ 그 값(0.0791)을 대역으로 쓰지 않는다(#27). **은퇴** |
| `order_signal` | 규칙 셋의 연언, 판별력 ≥ 0.5 | **오라클이 넘는다** — 고정 게이지 0.9694 · 0.5446. 자기 게이지 초판(0.4979)은 지표의 산물 |
| `guide_budget` | 최소 예산 증가 + f 불변 | 둘 다 구성상 확인 가능. ⚠ 잔존 미달은 **캔버스 높이**가 상한(588.18 < 651.2) |
| `preview_gap` | 접합 이동 중앙 0.05 · p90 0.15 (D-S6) | ⚠ **구조적으로 넘기 쉽다** — 큰 어긋남은 이동량이 아니라 미배치로 샌다 |
| `segment_gate` | G-A.1 옛 값 대비 | 같은 판정 계층의 옛 값. **폐기된 계획(G)** |

### 덮는 대상 수를 출력한다

`selfcheck._cover()`가 검사마다 `{scan, unit, targets, hits}`를 `selfcheck.json`의
`coverage`에 남기고 콘솔에 찍는다. **`targets`가 0이면 그 자체가 플래그로 뜬다.**

| 검사 | 단위 | 덮는 수 |
|---|---|---|
| `scan_sweep_coverage`(#33) | 훑기 | **3** |
| `scan_gate_reachability`(#35) | 게이트 블록 | **0 → 6** |
| `scan_zero_denominator`(#36) | 하네스 파일 | **36**(플래그 12) |

⚠ **0이 아니라고 전수인 것은 아니다** — 게이트 발견은 **이름**으로 하므로 다른 이름으로 적힌
게이트는 여전히 안 보인다. #33이 규정한 그 사각지대다. PITFALLS 표에 적었다.

### 검증
pytest **63/63**(신규 6: 세 검사의 커버리지 보고와 0 플래그) ·
vitest 신규 `gate.test.ts` 3건(빈 `reachability`를 막는지 — 반례) · tsc 0 ·
selfcheck **1552건 · STALE(상수) 0 · STALE(정의) 0** · 상수 `2fd74e1c` · 정의 `a45078a5`.

## L-D.0 — 롤 0 제약(지평선 수평 고정), **게이트 실패** (2026-08-16, 사람 지시)

착수 시 읽은 것: 최근 다섯 + 이론서(#37). 걸리는 번호:
**#37**(이론서를 먼저 봤다 — 3.1 지평선 · 5.3 자유도 회계 · 6.3 수심 · 2.2 무한원) ·
**#26**(통과 기준을 측정 전에 박았다) · **#35**(오라클 팔) · **#30**(양성 채널 = 롤 대조군) ·
**#32**(제약이 실제로 발동했는지 카운터) · **#12·#13·#14**(동작점·절단·시드) · **#8**(꼬리).

### 이론

이론서 3.1 — 수평 방향의 소실점은 한 직선(지평선) 위에 있다. 롤 0이면 그 직선이 **화면 수평**이다.
§5.3 자유도 회계에서 자세가 3(pan·tilt·roll) → 2가 된다. 귀결 셋:

1. 수평 축 소실점 둘은 **같은 y**를 갖는다(= 지평선 높이 `h`)
2. `h`가 확정되면 **두 번째 소실점은 획 하나로 정해진다** — 연장해 `y = h`와 만나는 점
3. 세 번째(수직) 소실점은 수심 조건(6.3)으로 **직선 하나로 좁혀진다**:
   `V₁V₂`가 수평이므로 `V₃.x = P.x`이고, `(P − V₁)·(V₃ − V₂) = 0`에서
   `y₃ = h − (Pₓ−x₁)(Pₓ−x₂)/(P_y−h)`. `P_y = h`(피치 0)면 무한원 — 2점 투시다.

**③은 참 카메라에서 오차 0으로 성립한다**(`horizon.json@1b6175a4`의 `orthocenter_identity`,
5구도 중 유한 축 셋인 구도 전부). ⚠ **그것은 항등이고 정보량이 0이다**(#5) — 식의 부호와
유도를 잠그는 것 말고는 아무 말도 안 한다. 임계를 걸지 않는다.

### 사전 등록 (측정 전에 박았다, #26)

> L-A.6과 **같은 조건**에서 축 방향 오차 중앙이 **약 1° 아래**로 내려가면 자동 확정 경로를
> 되살린다. 기준선은 같은 하네스의 `detected_baseline`이고 L-A.6 값은
> **7.40 / 10.04 / 10.09 / 4.53°**(축당 4 / 18 / 48 / 100획).
> **도달 가능성**(#35): 같은 하네스의 `orthocenter_identity`가 제약 자체는 정확함을 보인다 —
> 남는 문제는 입력(검출 소실점)이고 그것이 도달하는지를 이 측정이 답한다.

### 결과 — **게이트 실패. 그리고 배치가 붕괴한다.**

축 방향 오차 **중앙**(도). 팔 넷: 기준 / 롤0 사영 / 오라클 축 라벨 / 제약 적합.

| 축당 획 | 기준 | 롤0 | 오라클 라벨 | 제약 적합 |
|---|---|---|---|---|
| 4 | 7.4048 | **6.1460** | **4.4599** | 5.7339 |
| 18 | 10.0437 | **8.0473** | **4.9690** | 9.2961 |
| 48 | 10.0922 | 9.4668 | 9.2513 | 8.7764 |
| 100 | 4.5290 | 4.6094 | 8.4495 | 8.5438 |

**임계 ≈1°에 어느 팔도 못 간다.** 가장 좋은 칸(오라클 라벨 · 축당 4획)이 **4.46°로 4.5배**다.
⚠ 꼬리는 거의 안 움직인다(p90 35 → 36 → 12.5 → 37.7) — 개선은 중앙에만 있다(#8).

**그리고 배치가 무너진다** — 놓인 획 수:

| 축당 획 | 기준 | 롤0 | 오라클 | 제약 적합 |
|---|---|---|---|---|
| 4 | 2049 | 792 | 504 | 848 |
| 18 | 6035 | 522 | 912 | 730 |
| 48 | 2045 | 9 | 6 | **3** |
| 100 | 692 | 0 | 0 | **0** |

조용히 틀림도 오른다(cut 0.2: 0.8131 → 0.9242 · 0.8151 → 0.8908). **분모가 다르므로**
비율로 읽지 않는다(#11) — **개수로 읽어도 같은 방향**이다.

### 배치 붕괴가 사영의 성질인지 제약의 성질인지 — **갈랐다**(#32)

첫 판은 **사후 사영**이었다(나온 소실점의 y를 나중에 맞춘다). 그러면 소실점이 **자기 지지선에서
벗어나고** `liftAll`의 정합성 검사가 거절한다 — 그 붕괴는 제약이 아니라 사영의 성질일 수 있다.
그래서 **제약을 적합 안에 넣은 팔**을 함께 냈다(`fitHorizonPair`: 미지수 `(x₁, x₂, h)` 셋을
길이² 가중 최소제곱 한 번으로 푼다, D-L5와 같은 가중).

**안 살아난다** — 848 / 730 / 3 / 0. 소실점 이동량 중앙이 **78~98px**이고, 즉 검출이 낸 두
"수평" 소실점의 y가 실제로 그만큼 어긋나 있다. 롤 0을 강제하면 그 어긋남이 소실점을 끌고 간다.
**붕괴는 제약의 성질이다.**

### 양성 채널 — 종이를 기울이면 대가가 있다(#30)

참 축도 같은 각으로 돌려 **기준계를 맞췄다**. ⚠⚠ **초판은 안 돌렸고 그래서 이 대조군이
통째로 무효였다** — `roll_zero` 팔이 지평선을 수평으로 되돌리며 사실상 롤을 되감았고 그것이
"기울여도 좋아진다"로 읽혔다. 재던 것이 품질이 아니라 **기준계 불일치**였다(#35의 ⚠⚠ · #37).
고친 뒤:

| 롤 | 기준 | 롤0 |
|---|---|---|
| 0° | 6.0933 | **4.4693** |
| 2° | 6.0933 | 5.0614 |
| 5° | 6.0933 | 5.9275 |
| 10° | 6.0933 | **10.0103**(기준보다 나쁘다) |

기준선이 롤과 무관하게 6.0933으로 **고정**된 것이 기준계가 맞았다는 표시다.
**손익분기는 5° 근처**이고 10°에서는 제약이 해를 끼친다.

### 판정 — **자동 확정 경로를 되살리지 않는다. 코드는 남긴다.**

게이트가 요구한 1°에 못 갔고, 게다가 **배치가 붕괴한다.** 사용자 확정을 유지한다(§5.4).
`horizon.ts`는 **앱에 배선하지 않는다** — D-L6(카메라 일괄 적합)과 같은 처리다:
측정 근거를 재현할 수 있어야 하므로 코드는 남기고 경로는 안 연다.

⚠ **"초안 품질이 올랐다"고 적지 않는다.** 사람 지시는 안 내려가면 그것을 기록하라고 했는데,
축 방향 오차가 **초안 품질의 지표가 아니다** — 초안이 하는 일은 **끌기 시작 위치**를 주는 것이고
그 지표는 `draft_recover`의 확정률과 축 오차다(D-L16·D-L17). **그 하네스로 안 쟀다.**
쟀다고 적으면 #2(대리값의 성질을 재는 것)이다. `DEFERRED.md`에 트리거를 달았다.

### 검증
vitest 신규 `horizon.test.ts` 4건(반례 · 항등 · 곡선 · 양성 채널) · tsc 0 ·
새 상수 `HORIZON_TOL`을 `test/constants.ts`에 등록했다(D-C4) — **상수 해시가 움직인다.**

## L-D.3 — **사전 등록: 판정** (측정 전에 박는다, #26)

리뷰어 [11]이 짚었다 — L-D.1에는 사전 등록 절이 있었는데 **L-D.2·L-D.3에는 없었고**
그런데 결과가 "셋 다 통과"라 적혀 있었다. 무엇을 통과라 부를지가 측정 뒤에 정해진 것이다.
계획서 §11의 L-D에도 게이트가 없다(L-B와 대비). **남은 항목의 기준을 여기 박는다.**

| # | 무엇 | 통과 | **무엇이 이것을 넘을 수 있는가**(#35) |
|---|---|---|---|
| 1 | **상자 둘 이상 픽스처** | `setup()`이 상자 **둘**을 세우고, 둘 사이에 스냅으로 이어진 획이 하나 이상 있다. 그 획이 놓인다 | 지금 픽스처가 상자 하나이므로 **구성상 도달 가능**하다 — 못 하면 배선 결함이다 |
| 2 | **L-B 게이트 3번(승격 연쇄 여러 회)** | `promoteChain`이 **2회 이상** 돌고 각 회차의 (입력 대기 수 · 회수 수)가 원장에 남는다. 8회 상한에 안 닿는다 | 하네스가 이미 4회차까지 봤다(1회 720 → 2회 192 → 3회 25 → 4회 1, `promote.json@1b6175a4`). ⚠ **장면당 12획이라 깊이 4가 픽스처 상한에 가깝다** — 상자 둘이 그 상한을 올린다. **회수 수는 하네스와 직접 비교하지 않는다**(조건이 다르다) |
| 3 | **차수 표식 다중** | `orderMarks`가 2점·3점을 **함께** 들고 있을 때 되돌리기가 **가장 최근 차수로** 간다. 2회 되돌리면 2회 전으로 간다 | `order_undo.json@1b6175a4`이 표식 하나짜리를 이미 통과시켰다. 다중은 같은 자료구조의 목록 길이 문제이므로 **구성상 도달 가능**하다 |
| 4 | **지연** | 상자 둘(24획 이상)에서 획 하나 지연 중앙이 **개발 서버 기준 10ms 미만** | `archive_W/e2e.json`(아카이브)이 12획에서 중앙 1.8ms·최대 3.1ms다. 획 수가 두 배여도 획별 비용은 O(놓인 획 수)이므로 자릿수 안에 든다 |
| 5 | **스크린샷 회귀** | 같은 픽스처에서 두 번 돌린 스크린샷의 픽셀 차이가 **0**이다(결정론) | 잉크가 결정론적 LCG이고 자세가 고정이므로 **구성상 도달 가능**하다. ⚠ 폰트·안티에일리어싱이 기계마다 다르므로 **기계 간 비교는 안 한다** |

**기각한 기준**: "배치율이 얼마 이상" — 이 항목은 **배선 확인**이지 정확도 측정이 아니다.
정확도는 합성 하네스가 재고(`promote`·`axis_live`), 앱에서 참값 대조를 하는 것은
측정 경로와 앱 경로를 섞는 일이다(#17, `DEFERRED.md`에 이미 있다).

⚠ **D-L21 재개는 이 항목에서 안 한다.** `DEFERRED.md`가 트리거를 달았다 —
`promote`·`axis_live`의 `Math.max(1, n)` 53곳이 층화 비율인지 표시용 보호인지 **먼저 갈라야**
한다. 그 둘이 D-L21의 근거 원장이다(리뷰어 [3]).

## L-D.0 리뷰어 대응 — **결론 하나를 철회한다** (2026-08-16)

리뷰어 14건(높음 4). **높음 넷이 전부 새 측정을 요구했고 넷 다 쟀다.** 결과:

### ⓵ **"붕괴는 제약의 성질이다"를 철회한다**(리뷰어 [4][5], #30)

위약 대조군을 넣었다 — 소실점을 **같은 크기로 임의 방향** 옮긴다. 놓인 획 수:

| 축당 획 | 기준 | 롤0 | **위약(임의 방향)** | 제약 적합 |
|---|---|---|---|---|
| 4 | 2049 | 792 | **832** | 848 |
| 18 | 6035 | 522 | **802** | 730 |
| 48 | 2045 | 9 | **14** | 3 |
| 100 | 692 | 0 | **0** | 0 |

**위약도 똑같이 붕괴한다.** 그러므로 붕괴는 **롤 0의 성질이 아니라** *검출 소실점을 그
지지선에서 그만큼 떼어 놓는 것 자체*의 성질이다 — **방향과 무관하다.**
⚠ **그래도 롤 0에 대한 함의는 남는다**: 롤 0을 걸려면 **반드시 그만큼 옮겨야** 하기 때문이다
(검출 지평선이 실제로 그만큼 기울어 있다). 다만 그것은 "롤 0이 나쁘다"가 아니라
**"검출 해를 사후에 교정하는 어떤 방법도 이 대가를 낸다"**이다.
**`PITFALLS #39`의 근거 문장을 고쳤다** — 적합 팔이 아니라 **위약 팔**이 그 자리였다.

### ⓶ Δy는 78~98px이 아니라 **24~53px이다**(리뷰어 [5])

78~98은 **적합 팔의 2D 이동량**이고 Δx를 포함한다. y 성분만 내니
사영 팔 **47.9 / 32.9 / 42.1 / 53.2px**, 적합 팔 **27.9 / 24.1 / 30.6 / 38.0px**다.
"두 수평 소실점의 y가 78~98px 어긋나 있다"는 **성분 혼동이었고 고쳤다.**

### ⓷ 무한원 축 구도가 붕괴를 만드는 것이 **아니다**(리뷰어 [2])

`no_synth` 팔(수직축 합성을 끈다 — 2pt·1pt에서는 소실점이 **없는 것이 옳다**, 이론서 2.2)의
`vertical_synthesized`가 **0**인데 **배치 수가 사영 팔과 완전히 같다**(792 / 522 / 9 / 0).
구도별로 갈라도 붕괴는 **모든 구도에서** 나고 **3점 구도가 가장 심하다**
(3pt_yaw35_pitch15 424 → 56 · 3pt_yaw20_pitch25 344 → 11 대 1pt 398 → 291).
합산이 2pt·1pt에 끌려간 것이 아니다.

### ⓸ `HORIZON_TOL` 스윕은 **공허했다**(리뷰어 [1], #31)

아홉 격자점(`min_slope_deg` 2/3.75/7 × `min_vp_sep_ratio` 0.10/0.15/0.30)이
**완전히 같은 수**를 낸다. 이유는 `applyRollZero`가 두 임계를 **거의 안 부르기 때문**이다 —
`min_slope_deg`는 `vpOnHorizon`(획 하나 + 지평선) 전용이라 사영 경로가 안 쓰고,
`min_vp_sep_ratio`는 **수직축 합성 분기**에서만 걸리는데 ⓷대로 그 분기가 배치를 안 바꾼다.
**무결론이 아니라 설계의 귀결이고**(#31이 요구한 그 확인) 원장에 그렇게 적었다.
즉 리뷰어 [1]의 물음("임계를 바꾸면 붕괴가 남는가")의 답은 **"이 두 임계는 결론에 안 닿는다"**다.

### ⓹ `reachability`를 고쳤다(리뷰어 [3], **#35 세션 내 재발**)

초판이 `orthocenter_identity`(항등, 정보량 0)를 그 자리에 적었다. **같은 세션이 #35 검사를
고쳐 놓고 그 필드를 항등으로 채운 것**이고, 자동 검사는 필드가 안 비었으므로 통과했다 —
PITFALLS 표가 "안 잡는 것: 오라클을 실제로 돌렸는가"로 명시한 바로 그 자리다.
**`oracle_axis_labels_roll_zero`(4.4599 / 4.9690 / 9.2513 / 8.4495°)로 다시 적었고**,
**이 하네스에서 검출 기반 팔이 1°에 닿은 적이 한 번도 없다**를 명시했다.
그러므로 #35대로: **기준을 못 넘은 것이 신호의 성질인지 기준의 성질인지 이 하네스만으로는
안 갈린다.** ⚠ 기준은 안 낮춘다.

### ⓺ 등록 임계가 정정 전 값이었다(리뷰어 [8])

박은 값이 ≈1°인데 AS-L9·CLAUDE.md §2의 현행 임계는 **0.5°**다. **사후에 안 고친다**(#26·#28) —
등록문은 그대로 두고 그 사실을 원장에 적었다. **방향이 한쪽이라 결론은 안 바뀐다**:
0.5°로 읽으면 실패가 더 크다.

### ⓻ 문장 하나를 고쳤다(리뷰어 [9])

"개수로 읽어도 같은 방향"의 개수가 불명이었고, **조용히 틀린 개수는 오히려 준다**
(k_1 1666 → 732 · k_2 4919 → 465). 같은 방향이 되는 것은 **옳게 놓인 개수**다
(k_1 383 → 60 · k_2 1116 → 57). 그렇게 고쳤다.

### 남은 판정 — **게이트 실패는 그대로다**

축 오차가 임계에 못 가는 것(6.15° · 오라클 4.46° 대 0.5~1°)은 위 어느 것도 안 바꾼다.
**D-L32의 판정은 유지되고 근거 하나가 좁아졌다** — "붕괴는 제약의 성질"이 아니라
"검출 해를 사후 교정하는 것 일반의 대가"다.

⚠ **표에 표본을 병기했다**(리뷰어 [10]): k_3는 시드 3·n=93, k_4는 **시드 1·n=18**이다.
**방향이 뒤집히는 칸이 정확히 k_4**이고 붕괴 헤드라인 "692 → 0"도 그 행이다.

### 검증
vitest `horizon.test.ts` **6건**(반례 · 항등 · 곡선 · **구도별 층화** · **HORIZON_TOL 스윕** · 양성 채널).

## 자동 검사의 자기무력화 (2026-08-16, 사람 지시) — 착수 기록

착수 시 읽은 것: 최근 다섯. 걸리는 번호: **#38**(덮는 대상 수 — 이 항목이 그 다음 단계다) ·
**#35**(도달 가능성) · **#19**(검사를 약화시키는 방향으로 고치지 않는다) · **#5**(항등) ·
**#33**(값 대조) · **#32**(0건이 깨끗함인지 안 돎인지).

### 문제 — **검사를 만들면서 그 검사를 무력화했다**

#35를 자동 검사로 옮긴 **바로 그 세션이 같은 세션 안에서** `reachability`를 정보량 0인
항등("참 카메라에서 오차 0")으로 채웠다. 필드가 비지 않았으므로 통과했다.
**#38의 다음 단계다**: 덮는 수가 0이 아니어도 **값이 자명하면 같은 상태**다.

### (b) 기존 자동 검사 넷 점검 — **둘이 같은 상태였다**

| 게이트 | 초판 `reachability` | 판정 |
|---|---|---|
| `camera_gate` | "`deg_0` 행이 **정의상** 1.0배·1.0배" | ❌ **자명하다** — 그 행이 **대역의 출처 자체**다 |
| `guide_budget` | "**구성상 확인 가능하다** — 수치 미분이라 결정론적" | ❌ **자명하다** — 측정이 잘 정의됐다는 말이지 기준을 넘을 수 있다는 말이 아니다 |
| `horizon` | (이미 고쳤다) 오라클 팔 4.4599° | ✅ |
| `lift_gate` | `true_camera_oracle` 팔 | ✅ |
| `order_signal` | 오라클 고정 게이지 0.9694 | ✅ |
| `preview_gap` | "구조적으로 넘기 쉽다" | ⚠ **오라클 팔이 없다** — 이제 `reachability_absent`로 명시한다 |
| `segment_gate` | 옛 값(다른 하네스) | ✅ |

**`camera_gate`를 고치면서 드러난 것**(이 항목의 부산물이지만 중요하다):
자명하지 않은 팔은 `deg_0.25` 행이고 **그것만 통과선을 넘는다**
(형태 0.0613 ≤ 0.0622 · 조용히 틀림 0.0696 ≤ 0.0740).
**등록 지점인 `deg_0.5`는 둘 다 넘는다**(0.1085 > 0.0622 · 0.0835 > 0.0740).
즉 **실효 요구는 등록한 0.5°가 아니라 ≈0.25°이고 두 배 엄격하다.**
⚠ **기준을 안 고친다**(#26) — 적기만 한다. `camera_gate.json@1b6175a4`의 `gate.status`에 있다.

다른 셋(#33 훑기 3 · 인용 95 · #36 하네스 38)은 값이 자명하지 않았다.
다만 **같은 형태의 구멍이 둘 있었고 막았다**:
· 훑기 패턴이 **한 번도 안 맞아도** 조용했다(파일은 훑었는데 대조한 것이 0).
· **없는 원장을 인용하면** 조용히 건너뛰어 값 대조를 한 번도 안 지났다.

### (c) 검사에 넣었다

`gate()`가 **`reachability_value` + `reachability_source`**(원장 안 `/` 경로)를 강제하고,
오라클이 없으면 **`reachability_absent`**에 그 사실을 명시하게 한다.
`selfcheck.scan_gate_reachability`가 셋을 본다:
**① 산문뿐인가 ② 값이 정확히 0·1인가(항등 의심) ③ 적은 값이 원장의 그 경로와 맞는가**(#33 값 대조).

⚠ **경로 구분자가 `/`다** — 점을 쓰면 `deg_0.25`처럼 **키 이름에 점이 든 것**을 못 가른다.
이 저장소의 스윕 키가 실제로 그렇다(`deg_0.25` · `jitter_0.005` · `r2px.45`).

⚠ **아직 자동이 못 보는 것**: 0·1이 아니면서도 자명한 값(다른 형태의 항등)과
`registered`가 실제 상수를 가리키는지. PITFALLS 표의 "안 잡는 것"에 적었다.

### 검증
pytest **71/71**(신규 8: 산문뿐 · 항등값 · 값 불일치 · 경로 실패 · 정상 침묵 · 부재 명시 ·
훑기 공허 · 없는 원장 인용) · vitest `gate.test.ts` **7건** · 게이트 일곱 전부 값 대조 통과.

## L-D.3 종단 검증 — **상자 둘, 사전 등록 다섯 전부 통과** (2026-08-16)

착수 기록은 위 "L-D.3 착수 기록"과 "L-D.3 — 사전 등록: 판정"에 있다.
원장: `stage_browser.json@1b6175a4`의 **`l_d3`**.

| # | 사전 등록 | 결과 | 판정 |
|---|---|---|---|
| 1 | 상자 둘 · 두 상자를 잇는 배치가 실제로 일어난다 | 획 **24**(12×2), 확정에서 **24/24 배치** | ✅ |
| 2 | 연쇄가 **2회 이상** 돌고 회차별 기록이 남는다. 상한 8 미도달 | **3회**(대기 8 → 놓임 2 · 대기 6 → 1 · 대기 5 → 0). `hit_cap` false | ✅ |
| 3 | `orderMarks`가 2·3점을 **함께** 들고, 최근 차수로 되돌아간다 | 표식 **[2, 3]** · `revertToOrder(3)`이 3점 상태를 그대로 복원(가이드 6 · 배치 20) | ✅ |
| 4 | 획 하나 지연 중앙 **10ms 미만**(개발 서버) | **중앙 2.1ms · 최대 2.5ms**(n=8) | ✅ |
| 5 | 같은 픽스처를 두 번 세우면 픽셀이 **같다** | `identical: true`(430,344 bytes) | ✅ |

### 이 항목이 잡은 것 셋

**① 상자 둘이 연쇄를 실제로 길게 만든다.** 상자 하나 픽스처에서는 확인이 1회분이었고
`promote.json`의 하네스가 "장면당 12획이라 깊이 4가 픽스처 상한에 가깝다"고 적어 두었다.
24획에서 **연쇄가 3회 돈다** — 브라우저에서 여러 회를 본 것은 이번이 처음이다(L-B 게이트 3번).
⚠ **회차별로 남긴다**(#32) — 합계만 내면 "여러 회 돌았는가"가 안 보인다.
그래서 `promoteChain`에 `chainTrace`를 넣고 `S2S.chainTrace()`로 낸다.
⚠ **상한 8에 닿았는지도 남긴다** — 닿았다면 수렴이 아니라 잘린 것이다.

**② 지연 측정의 초판이 앱이 아니라 CDP 왕복을 재고 있었다.**
`page.mouse.*`를 `Date.now()`로 감쌌더니 중앙이 30ms대였고 등록한 10ms를 넘었다.
`flow.spec.ts`가 **이미 옳은 방식**을 쓰고 있었다 — 페이지 안에서 `pointerup` **디스패치가
반환할 때까지**를 `performance.now()`로 잰다. 고치니 **2.1ms**다(#17: 같은 것을 두 곳에서
다르게 재고 있었다). ⚠ `e2e.json`의 1.8ms는 **옛 UI**라 같은 구간이 아니다 — 견주지 않는다.

**③ 스크린샷 회귀가 스펙 하나만 돌리면 통과하고 전체를 돌리면 실패했다.**
`clear()`만 하고 다시 세웠더니 **앞 절이 남긴 상태**(`cam.locked`·`lockedOrder`·자세)가
두 판에서 달라 픽셀이 갈렸다. 매번 새로고침해 출발점을 같게 만들었다.
⚠ **한 스펙만 돌려 통과한 것을 통과로 읽으면 안 된다** — 전체 순서에서 다시 본다.

### 기대가 틀린 자리 둘 — **코드가 맞았다**(A-3)

**⓵ `revertToOrder(2)` 뒤에 다시 승격해도 3점이 안 된다.** 되돌리기가 **가이드까지**
되돌리므로 세 번째 축이 없어지기 때문이다. 초판 테스트는 3이 될 것으로 기대했는데
**§6.1이 요구하는 동작이 지금 것**이다(둘 중 하나만 되돌리면 좌표계가 섞인다).
그 사실을 못박는 확인으로 바꿨다 — `order` 2 유지 · `guides` 4 유지.

**⓶ 표식은 둘인데 버튼은 하나다.** `lockedOrder`의 표식은 일부러 안 낸다
("있는 자리로 되돌아가는 버튼은 아무 일도 안 한다"). 초판은 버튼 둘을 기대했다.
표식 3은 **더 그린 뒤** 돌아올 자리로 살아 있고 `revertToOrder(3)`이 그것을 쓴다.

### 안 한 것

**AS-L9 시나리오는 못 넣었다** — 사람이 가이드를 끄는 것이라 자동화가 안 된다.
**DEFERRED**로 보낸다(HANDOFF가 이미 그 조건을 적어 두었다).
**D-L21 재개도 여기서 안 한다** — `DEFERRED.md`의 트리거대로 `promote`·`axis_live`의
`Math.max(1, n)` 53곳을 먼저 갈라야 한다.

### 검증
Playwright **22/22**(네 스펙) · vitest 327/327 · pytest 71/71 · tsc 0 ·
selfcheck STALE(상수) 0 · STALE(정의) 0.

## #40 리뷰어 대응 — **내 판정 하나가 프레임 혼동이었다. 철회한다** (2026-08-16)

리뷰어 11건(높음 4). **넷 다 내 쪽이 틀렸다.**

### ⓵ **높음: `deg_N`은 준 각도이지 실측이 아니다 — "실효 요구가 두 배" 철회**

등록문은 "**실측** 축 방향 오차 0.5° 이하"인데 원장 키 `deg_N`은 **소실점에 준 각도**다
(`what`이 그렇게 적어 두었다). 같은 원장의 실측 중앙:

| 행 | 준 각 | **실측 축 오차 중앙** | 형태 | 조용히 틀림 |
|---|---|---|---|---|
| `deg_0.25` | 0.25° | **0.4838°** | 0.0613 | 0.0696 |
| `deg_0.5` | 0.5° | **0.9641°** | 0.1085 | 0.0835 |

**실측 0.5°에 대응하는 행은 `deg_0.25`이고 그 행은 통과한다**(0.0613 ≤ 0.0622 · 0.0696 ≤ 0.0740).
그러므로 "등록 지점 `deg_0.5`가 못 넘는다 → 실효 요구가 등록값의 두 배로 엄격하다"는 **틀렸다.**
그것은 **준→실측 약 2배 증폭**이고 L-A.7이 이미 적어 둔 것이다(#24: 단위·프레임 혼동).
`camera_gate.json`의 `gate.status` · `progress.md` · `PITFALLS #40` 세 곳을 고쳤다.

⚠ **CLAUDE.md §2가 이 게이트 실패를 유일한 중단 조건으로 정해 두었다** — 내가 "등록 지점이
못 넘는다"고 적고도 중단·보고를 안 한 것이 그 자체로 결함이었다. **실제로는 통과이므로
중단 사유가 없다.**

### ⓶ 높음: 대역과 통과 요구의 **모집단이 다르다**(미해소, 기록만)

등록문은 "**5구도 전부**를 낸다(기준 구도 하나로 통과를 주장하지 않는다)"인데
대역(0.0296 / 0.0673)은 **`baseline_3pt_jitter_le_0_01` 단일 구도 부분집합**의 값이다.
5구도 합산으로 읽으면 축 오차가 **0인 행조차** 못 넘는다(0.1237 > 0.0622 · 0.3758 > 0.0740).
**두 모집단이 같지 않고 원장에 구도별 분해가 없다**(#11·#27).
**기준을 안 고친다**(#26) — `gate.status`에 적었다. **여유도 얇다**(#14): 형태 1.4% · 조용히 틀림 **획 2개**.

### ⓷ 높음: **이 항목 자체가 같은 함정에 세 번 더 빠졌다**

| 게이트 | 내가 적은 값 | 무엇이 틀렸나 | 고침 |
|---|---|---|---|
| `segment_gate` | 0.8463(`old/first/placed`) | **판정의 기준선 그 자체다** — `camera_gate`가 "정의상 1.0배"로 ❌ 받은 것과 같은 구조인데 **절대값이라 "0·1" 규칙을 피해 갔다** | `reachability_absent` |
| `guide_budget` | 588.18px | **다른 지표다** — 기준은 "예산이 늘어난다"이고 이것은 길이이며, 게다가 **다른 요구의 도달 불가**다 | `reachability_absent` |
| `lift_gate` | 0.2552 | **등록 지표가 아니다**(등록은 확정·무변화·전부 놓임). 등록 지표의 오라클은 배치율 **1.000**인데 그 1은 **분모가 최대 연결 성분**이라 나오는 구성의 귀결이다(#27) | `reachability_absent` |

**값이 있다고 검증된 것이 아니다.** PITFALLS #40의 "안 잡는 것"에 **여섯**을 적었다.

### ⓸ 중간: **면제를 통과로 셌다**

`preview_gap`은 `reachability_absent`라 **값 대조를 지난 것이 아니라 면제된 것**인데
PITFALLS·HANDOFF가 "**일곱 전부** 값 대조 통과"라 적었다. #32의 형태다.
`_cover`가 **검증과 면제를 갈라 센다** — 지금 **값 대조 3**(`camera_gate`·`horizon`·`order_signal`) ·
**면제 4**다. ⚠ **면제는 통과가 아니다** — 그 게이트의 도달 가능성은 **미상**이고 근거로 안 쓴다.

### ⓹ 중간: 검증 문구가 원장보다 뒤처졌다(#1) — 이 항목의 주제가 그것이다

`selfcheck.json`이 1751건 · 인용 93인데 문서는 1601 · 95였다. 고쳤다.

## 옛 UI 삭제 — **시도했고 되돌렸다** (2026-08-16)

A-4대로 L-D.3 게이트를 통과시킨 뒤 지웠다(`index.html` · `main.ts` · `cameraPanel.ts` ·
`e2e/flow.spec.ts`). 빌드·`stage.spec`·`static_deploy`는 통과했는데 **둘이 걸렸다**:

1. **`e2e/coords.spec.ts`가 옛 UI를 몬다** — `window.s2s`(소문자)와 `#tools button[data-t="draw"]`는
   **옛 UI의 손잡이·마크업**이다. 새 UI는 `window.S2S`이고 도구 막대가 다르다.
   그 스펙은 **D-C3(dpr 좌표 규약)을 dpr 1·2·3에서 잠그는 유일한 자리**이고(#21),
   포팅은 이 항목의 범위를 넘는다.
2. **`test/wiring.test.ts`가 `src/main.ts`를 읽는다** — 앱이 옵션 객체를 직접 안 적는지 보는
   배선 확인이다(#17). `mainL.ts`로 옮기려면 그 검사가 무엇을 덮는지 다시 정의해야 한다.

**되돌렸다.** A-4가 "테스트 깨진 채 다음 항목에 가지 않는다"이고, 옛 UI 삭제는
그 둘을 새 UI로 옮긴 **뒤에** 하는 일이다. 되돌리며 `e2e.json` 인용도 원복했다.

**다음 세션이 할 일(순서대로)**:
① `coords.spec.ts`를 `l.html` + `window.S2S`로 포팅한다 — **버그를 되살려 실제로 잡는지
   확인한다**(#21: 첫 판은 배경 층 덕에 통과하고 있었다).
② `wiring.test.ts`의 `main.ts` 참조를 `mainL.ts`로 옮기고 **덮는 범위를 다시 적는다**(#17).
③ 그다음 `index.html` · `main.ts` · `cameraPanel.ts` · `flow.spec.ts`를 지운다.
④ `e2e.json`은 만드는 스펙이 없어지므로 `archive_W/`로 옮기고 **`@해시` 인용을 뗀다** —
   다시 못 돌리는 원장에 현재형 인용을 두면 STALE이 영구로 뜬다. 인용 5곳이 대상이고
   **새로 만든 #40 검사가 그것을 즉시 잡는다**(이번에 실제로 잡았다).
⑤ `vite.config`의 입력·`sw.js` 폴백·루트 진입점(리다이렉트 한 장)을 함께 옮긴다.

## 옛 UI 삭제 — **완료** (2026-08-16)

착수 시 읽은 것: 최근 다섯. 걸리는 번호: **#21**(dpr 규약 — 버그를 되살려 실제로 잡는지 확인) ·
**#17**(측정 경로와 앱 경로 · 덮는 범위를 함께 적는다) · **#19**(검사를 약화시키지 않는다) ·
**#30**(양성 채널) · **#41**(판정과 행동).

### 먼저 옮긴 것 둘 — **지우기 전에 잠금이 새 경로에서 성립해야 한다**(A-4)

**① `e2e/coords.spec.ts` → 새 UI.** D-C3(dpr 좌표 규약)을 dpr 1·2·3에서 잠그는 유일한 자리다.
갈린 것 넷: 손잡이 `window.s2s` → **`window.S2S`** · 획 목록 `s2s.strokes` → **`S2S.doc().strokes`** ·
진단 `s2s.diag()` → **`S2S.diag()`**(`mainL.ts`에 `frameDiag`를 새로 넣었다) ·
도구 막대 `#tools [data-t]` → **`#bar [data-act]`**.
⚠ **`view_vs_overlay`의 뜻이 달라졌다** — 단일 뷰포트라 견주는 것이 **잉크 CSS 상자 ↔ three
렌더러 뷰 크기**다(옛 UI는 2D·3D 캔버스 둘이었다). 이름은 유지했다(그 스펙이 그 이름으로 잠근다).

⚠⚠ **`[회귀]` 팔을 새로 넣었다**(#21·#30). 원래 단언은 `inkAtScaled === 0`("×dpr 자리에 잉크가
없다")인데 **탐침이 아무것도 못 보는 상태여도 0이 나온다** — 첫 판이 실제로 그렇게 통과했다.
그래서 **×dpr 자리에 일부러 칠하고 같은 탐침이 잡는지** 본다(dpr 2·3에서 `before` 0 → `after` > 0).
못 잡으면 그 단언은 아무것도 배제하지 않는다.

**② `test/wiring.test.ts`의 `main.ts` 참조.** 원래 잡던 결함(옛 UI가 `settle`을 옵션 없이 불러
측정해 둔 경로가 앱에서 안 켜져 있었다)은 **대상이 사라졌다** — 새 UI는 그 계층을 아예 안 쓴다.
**지우지 않고 방향을 뒤집었다**: 이제 **새 UI가 폐기된 배치 계층으로 돌아가지 않았는가**를 본다
(`mainL.ts`에 `settle(` 호출이 없다 + `liftAll`·`placeLive`·`solveInto`가 실제로 있다 —
뒤의 것이 없으면 앞의 단언이 공허하다, #32).
⚠ **검사를 약화시키지 않았다**(#19) — 원래 검사가 지키던 것(**측정 경로 = 앱 경로**)은
`e2e/stage.spec.ts`가 `window.S2S`로 앱 함수를 그대로 부르는 것으로 옮겨졌다. 원장에 적었다.

### 지운 것

`web/index.html` · `web/src/main.ts` · `web/src/ui/cameraPanel.ts` · `web/e2e/flow.spec.ts`.
`vite.config.ts` 입력이 `l.html` 하나가 됐고 `sw.js` 폴백도 `l.html`로 옮겼다.
**`public/index.html`은 리다이렉트 한 장**이다(UI가 아니다) — 루트로 들어온 사람을 보낸다.
`stage0/out/e2e.json` → **`archive_W/`**. 만드는 스펙이 없어져 **다시 못 돌리므로** 뺐다 —
남기면 상수가 움직일 때마다 STALE로 뜨는데 고칠 방법이 없다. 머리말에 그 사실을 박고
인용 5곳에서 **`@해시`를 뗐다**. ⚠ **#40 검사가 그 인용을 즉시 잡았다**(`없는 원장을 인용한다`).

### ⓵ 삭제가 드러낸 것 — **오프라인 양성 채널의 팔 하나가 무효였다. 철회한다.**

빌드가 바뀌자(`index.html`이 진입점에서 빠져 청크 구성이 달라졌다) `static_deploy`의
양성 채널이 뒤집혔다. 원인을 세어 보니(#7) 캐시 목록에 **`sketch3d-<build>`와
`sketch3d-broken`이 함께** 있었다 — **앱(`mainL.ts`)이 페이지를 열 때마다 자기 서비스
워커를 다시 등록하므로**, 별도로 등록한 "망가진 워커"와 **같은 scope에 둘이 얹혔다.**
⚠ **"어느 쪽이 그 새로고침을 제어했는가"는 안 봤다**(리뷰어) — 관측한 것은 **캐시 이름 둘의
공존**이고 "경쟁했다"는 거기서 온 **추론**이다. `controller.scriptURL`을 안 찍었다.
**철회는 그래도 정당하다** — 교락 가능성만으로 그 팔의 결론은 못 쓴다.

그러므로 **초판의 팔 A(사전 캐시 목록에서 `.js`를 뺀 별도 워커)는 교락된 팔이고**,
거기서 읽은 **"A가 살았다 → ③의 통과는 사전 캐시 목록의 효력이 아니다"를 철회한다.**

**팔 하나로 줄였다**(A-3): 앱의 워커를 그대로 두고 **그 캐시에서 `.js`만 지운다**.
지우기 전 문서·앱 둘 다 살고(`doc` true · `app` true), 지운 뒤 **문서는 살고 앱은 죽는다**
(`doc` true · `app` false). 그것이 ③이 실제로 배제하는 것이다.
⚠ **사전 캐시 목록만의 몫은 이 설계로 안 갈린다** — 앱의 등록을 끌 수 없기 때문이다. **미측정**이다.
⚠ `reload` 거부(`ERR_INTERNET_DISCONNECTED`)를 **잡아서 관측으로 만든다** — 안 잡으면
"문서도 못 왔다"가 테스트 오류로 뜨고 그것이 사고로 읽힌다.

### 검증
vitest **327/327**(57파일) · Playwright **22 통과 · 1 건너뜀 / 23**(coords 9 · stage 12 · static_deploy 2). ⚠ **건너뛴 하나는 `coords.spec`의 `[양성 채널] dpr 1`이고 의도된 것이다** — dpr 1에서는 ×dpr 자리가 제자리라 **가를 것이 없다**(`test.skip`에 그 사유가 있다). 빌드본 없음으로 인한 건너뜀이 아니다(#32) ·
pytest 71/71 · tsc·빌드 통과 · selfcheck STALE(상수) 0 · STALE(정의) 0.

## 옛 UI 삭제 — 리뷰어 대응 (2026-08-16)

리뷰어 16건(높음 4). **높음 넷 중 셋이 내 쪽이 틀렸다.**

### ⓵ 높음 [4] — **`[회귀]` 팔은 #21이 아니다. 재분류했다.**

`inkAtScaled === 0`의 양성 채널로 **×dpr 자리에 직접 칠하는** 팔을 넣고 "#21(버그를 되살려
실제로 잡는지)"이라 적었다. **틀렸다** — 캔버스에 직접 칠하는 것은 **앱의 좌표 변환 경로를
안 지난다.** 그 팔이 배제하는 것은 "**탐침이 눈이 멀었다**" 하나이고 그것은 **#30**이다.
이름을 `[양성 채널]`로 바꾸고, **"앱이 dpr을 두 번 적용해도 이 스펙이 실패한다"는 미확인**임을
스펙 머리말·`coords.json`·`DEFERRED.md`에 적었다.
⚠ **그러면 "옛 UI 삭제의 전제(잠금이 새 경로에서 성립한다)"가 그만큼 약하다.** 그렇게 적었다.

### ⓶ 높음 [5] — **"원래 보호가 `stage.spec`으로 옮겨졌다"를 철회한다.**

`wiring.test.ts`의 방향을 뒤집으면서 "원래 지키던 것(측정 경로 = 앱 경로)은 `stage.spec`이
`window.S2S`로 앱 함수를 그대로 부르는 것으로 옮겨졌다"고 적었다. **어긋난다** —
`stage_browser.json`이 스스로 "배치 정확도는 안 잰다, **측정 경로와 앱 경로를 섞는 일이다**"라고
적는다. 앱 함수를 부르는 것은 **앱 경로를 지나는 것**이지 *하네스와 앱이 같은 출처를 쓰는지*의
확인이 아니다. **그러므로 지금 그 확인은 없다** — D-L35에 적고 `DEFERRED.md`에 트리거를 달았다.

### ⓷ 높음 [1] — **#40의 규칙 ③을 바로 다음 항목이 어겼다**(세션 내 재발)

`l_d3`을 다시 돌리고 `reload_ms`가 1156 → 1175로 바뀌었는데 문서 넷을 안 고쳤다.
**자동이 못 잡은 이유도 #40이 스스로 적어 둔 사각지대다** — 인용 해시는 맞고 **값만** 다르며,
`l_d3`은 `gate()` 이름 밖이라 값 대조 대상이 아니었다.

**두 가지로 고쳤다**:
1. **`l_d3`과 `coords`의 사전 등록을 `gate()`로 옮겼다** — 게이트 블록이 **7 → 9**가 됐고
   이제 자동 대조를 지난다.
2. ⚠⚠ **밀리초 수치를 문서에서 뺐다.** 같은 기계에서도 2.1↔2.2ms · 1152↔1175ms로 흔들린다 —
   **벽시계 값을 산문에 인용하면 문서가 곧 낡는다.** 판정은 "**10ms 미만**"이라는 자릿수이고
   값은 원장에 있다. 그렇게 적는 것이 이 유형의 유일한 해법이다.

### ⓸ 높음 [2][3] — **이미 L-D.4에서 고쳤다**(리뷰어가 그 전 스냅숏을 봤다)

README의 "종단 검증 (S-10)"은 **L-D.3 기준으로 다시 썼고**, v1 저장 서술도
"**열 수단이 없어졌다**"로 고쳤다(D-L29의 조건을 못 지킨 채 지운 것을 명시했고
`DEFERRED.md`에 "읽는 스크립트를 한 번 짜면 된다"로 남겼다).

### 나머지

| # | 지적 | 대응 |
|---|---|---|
| [6] | `view_vs_overlay`의 덮는 범위가 줄었는지 미기재 | `coords.json`의 `what_this_does_not_say`에 적었다 — **옛 실패 모드(두 캔버스 배율 불일치)는 대상이 사라졌다**(캔버스가 하나다). `DEFERRED.md`에도 |
| [7] | coords 측정이 원장 밖(#25) | **`coords.json` 신설** — dpr별 어긋남·`stretch`·`view_vs_overlay`·탐침 대조군 |
| [8] | "1 skip"의 정체 | **`coords`의 `[양성 채널] dpr 1`이고 의도된 것**이다(dpr 1은 ×dpr 자리가 제자리라 가를 것이 없다). 빌드본 없음이 아니다 |
| [9] | 워커 경쟁이 추론 | **관측한 것은 캐시 이름 둘의 공존**이고 `controller.scriptURL`은 안 찍었다고 적었다. **철회는 그래도 정당하다**(교락 가능성만으로 그 팔은 못 쓴다) |
| [10] | 아카이브 원장이 현재형 근거로 쓰인다 | §2.4에 "**다시 못 잰다 · 회귀 감시용이 아니다**"를 박고 §0에도 "(아카이브)"를 붙였다 |
| [11] | 저장 왕복 항등의 과대 주장 | 덮는 필드를 원장 목록대로 적고 **`pts2d` 전체는 안 본다**를 명시. ⚠ **`pts3d`라는 필드는 지금 없다**(`seg3d`다) |
| [13] | #41의 사건 지목 | **둘로 갈랐다** — ①은 `camera_gate`(중단 조건 그 자체), ②는 `horizon`(항목이 스스로 등록한 게이트라 **어긋남이 아니다**). **같은 말을 쓰지 말라**는 규칙을 더했다 |
| [14] | 결정 둘이 DECISIONS에 없다 | **D-L35**(wiring 방향 전환) · **D-L36**(양성 채널 축소와 철회) |
| [15] | `l_d3`이 게이트 이름 밖 | 위 ⓷대로 옮겼다. 게이트 **7 → 9** |
| [16] | 미측정이 `what_this_does_not_say`에 없다 | 올렸다 |

### 검증
vitest 327/327 · Playwright **22 통과 · 1 건너뜀 / 23** · pytest 71/71 · tsc·빌드 통과 ·
selfcheck **1757건 · STALE 0/0** · 게이트 **9**(값 대조 3 · 면제 6).

## CI 실패 — 외부 데이터 의존을 등록처로 묶었다 (2026-08-16, 사람 지시)

착수 시 읽은 것: 최근 다섯. 걸리는 번호: **#32**(0건을 통과로 세지 않는다 — 이 항목의 핵심) ·
**#33**(전수 확인이 문법으로만 찾지 않았는지) · **#25**(원장) · **#19**(검사를 약화시키지 않는다) ·
**#38**(덮는 대상 수).

### 무엇이 걸렸나

GitHub Actions의 Pages 배포가 막혔다:
```
test/one_stroke_one_axis.test.ts:109  expected 0 to be greater than 500
test/bend_share.test.ts:185           expected 0 to be greater than 100
```
둘 다 `data/quickdraw/*.ndjson`을 읽는데 **그 파일은 `.gitignore`에 있다**(낱말당 수십 MB,
Google CC BY 4.0 데이터라 저장소에 복제 안 한다). **CI에 없다.**

⚠ **실패 메시지가 원인을 안 가리켰다.** `loadQuickDraw`가 파일이 없으면 **조용히 `[]`를 내서**,
"표본 0"이 아니라 "0이 500보다 크지 않다"로 나왔다. **조용한 빈 결과가 진단을 가린다.**

### (a) 건너뛰되 **통과로 세지 않는다**

`web/test/dataDeps.ts`를 등록처로 만들었다. 셋을 지킨다:

1. **건너뛴다 — 통과가 아니다.** `it.skipIf(NO_DATA)`이고 **사유가 테스트 이름에 붙는다**.
   vitest가 `skipped`로 보고한다.
2. **원장을 안 건드린다.** 데이터가 없을 때 빈 산출물을 쓰면 **저장소에 커밋된 지난 측정이
   덮인다** — 그러면 CI 실행이 로컬 측정을 지운다. 건너뛰면 원장은 그대로고,
   상수가 어긋나면 selfcheck의 STALE이 잡는다(원래 그 검사의 일이다).
3. **`S2S_REQUIRE_DATA=1`이면 건너뛰지 않고 실패한다.** 로컬·야간이 그 모드다 —
   **"있어야 하는 자리에서 조용히 건너뛰는 것"을 막는 자리**가 그것이다.

**기전 자체에 반례를 걸었다**(`test/data_deps.test.ts` 6건) — 없을 때 사유를 내는가 ·
빈 디렉토리를 없는 것으로 보는가(`README.md`·점 파일만 있으면 **비어 있는 것**이다) ·
등록 안 된 이름은 던지는가 · `REQUIRE_DATA`에서 실패로 바뀌는가.

**실제로 확인했다**: `data/quickdraw`를 잠시 치우고 전체를 돌렸다 —
**330 통과 · 2 건너뜀**이고 `stage0/out`의 원장은 **하나도 안 바뀌었다**.
`S2S_REQUIRE_DATA=1`에서는 같은 조건이 **던진다**.

### (b) 전수 확인 — **셋이고 그중 하나가 같은 함정에 있었다**

훑은 것: `web/test/**.ts` · `web/e2e/**.ts`의 `existsSync`·`readFileSync`·`readdirSync`.

| 의존 | 쓰는 곳 | 상태 |
|---|---|---|
| `data/quickdraw` | `one_stroke_one_axis` · `bend_share` | **고쳤다**(위) |
| `sessions/` | `real_ink` | ⚠ **같은 함정에 있었다** — 표본이 없으면 `expect(true).toBe(true)`로 **통과 처리**했다. `ctx.skip()`으로 바꿨다 |
| `web/dist` | `static_deploy.spec` | 이미 `test.skip`에 사유가 있었다(#32를 그때 적었다) |

`camera_ref.json`은 **저장소 안에 커밋돼 있으므로** 대상이 아니다.

⚠⚠ **첫 판의 전수 확인이 둘을 놓쳤다**(#33). `readFileSync`를 **직접 부르는 파일**만 셌는데
`bend_share`·`one_stroke_one_axis`는 `loadQuickDraw`를 거쳐서 그 호출이 그 파일에 없다.
**공허성 단언이 그것을 잡았다** — "거른 결과가 넷이어야 한다"고 박아 두니 둘만 나왔다.
거름을 셋으로 넓혔다(직접 읽기 · **로더 import** · 건너뛰기 기전 사용).
**그 단언이 없었으면 빈 전수 확인이 통과했다.**

`sweeps.json`에 **`external_data` 훑기**를 등록했다(`web/test`·`web/e2e`의 읽기 호출, 7건 일치).
⚠ 그 훑기는 **문법**만 본다 — 값 대조는 위 테스트가 한다.

### (c) 배포와 측정을 갈랐다

`pages.yml`을 job 둘로 나눴다:

- **`build`(배포 경로)** — `tsc` · `npm run build` · **`static_deploy.spec.ts`만**.
  그 스펙이 배포와 직접 관련된 유일한 e2e다(하위 경로·서비스 워커 scope·오프라인).
  ⚠ `dist/`가 있어야 도므로 **빌드 다음에** 둔다.
- **`measure`** — `npx vitest run` 전체. **`continue-on-error: true`**라 배포를 안 막는다.
  측정 실패는 배포 가능성과 무관하고, 여기서 막으면 문서 한 줄 고치는 push마다 배포가 멎는다.
  ⚠ **그래도 빨간 X는 뜬다** — 조용히 지나가는 것이 아니다.
  건너뛴 것을 **작업 요약(`$GITHUB_STEP_SUMMARY`)에 적는다**.

⚠ **"빠르게 하려고 뺀다"가 아니라 "배포와 무관한 것을 뺀다"가 기준이다.**
A-4(깨진 채로 다음으로 가지 않는다)는 그대로다 — 배포 job도 여전히 타입과 배포 확인을 돈다.

### 부수

`actions/checkout@v4` · `actions/setup-node@v4` → **`@v5`**(Node 20 대상 경고).
`upload-pages-artifact@v3`·`deploy-pages@v4`는 현행 최신이라 그대로 둔다.

### 검증
vitest **332 통과 · 1 건너뜀**(58파일. 건너뜀은 `real_ink` — `sessions/`가 비었다) ·
Playwright 22 통과 · 1 건너뜀 · pytest 71/71 · tsc·빌드 통과 ·
selfcheck STALE(상수) 0 · STALE(정의) 0 · 훑기 **4**.
**데이터를 치운 상태**에서도 330 통과 · 2 건너뜀이고 원장 불변.

### 배포 확인 (2026-08-16)

푸시 뒤 Actions **세 job 전부 초록**(`build` · `deploy` · `measure`).
배포본: <https://hyunwookyu.github.io/brunelleschi/>

브라우저로 확인한 것 — ⚠ **원장 밖 관측이다**(#25. `static_deploy.spec.ts`는 여전히
**로컬 정적 서버**를 잰다):

- `/brunelleschi/`와 `/brunelleschi/l.html` 둘 다 **200** (루트 리다이렉트가 듣는다)
- `window.S2S`가 서고 도구 막대가 뜬다(`그리기`·`가이드 조정`·`궤도`·`고치기` …)
- 서비스 워커가 **`https://hyunwookyu.github.io/brunelleschi/`를 scope로** 잡고 페이지를 제어한다
- **콘솔 오류 0**
- `<title>`이 `Brunelleschi — 투시 선 도구`(이름 반영이 배포본까지 갔다)

⚠ **README의 "아직 한 번도 실행되지 않았다"를 사실로 바꿨다** — 그 문장은 **#32의 거울상**을
피하려고 적어 둔 것이었고("적힌 것과 도는 것은 다르다"), 이제 돌았으므로 그렇게 적는다.

⚠⚠ **`web/progress.md`를 또 만들었다**(2026-08-16, 이 커밋 직전). 쉘 작업 디렉토리가
`web/`인 상태에서 `cat >> progress.md`를 했다 — **HANDOFF가 그 자리를 경고하고 있었는데도
같은 실수를 반복했다.** 지웠고, 재발을 막는 검사를 selfcheck에 넣었다(#34: 한 자리에서
고친 것은 그 자리에서만 고쳐진다 — 이번엔 **기계가 보게** 한다).

---

## 규칙 기반 소실점 확정 — 전면 교체 (2026-08-16, 사람 지시)

### 착수 시 `PITFALLS.md`를 읽었다 — 이 항목에 걸리는 번호

| # | 이 항목의 어디에 걸리나 |
|---|---|
| **#5** | 자기참조 유형 3. **유도된 V₃는 측정이 아니라 항등**이고(이론서 6.4), **무잡음 입력의 축 오차 0도 항등**이다(교점이 정의상 참 소실점). 둘 다 표제에서 뺐다 |
| **#11** | 분모. **카메라가 안 서는 실행도 센다** — 선 실행만 재면 규칙이 공짜로 이긴다. ⚠ **그래도 두 팔의 분모가 다르다**(아래) |
| **#12** | 동작점 하나 금지 — 잡음 5수준 × 등급 2 |
| **#14** | 시드 여섯. 비율보다 분자/분모 |
| **#23** | 계획서(여기서는 지시문) 문장이 기하적으로 성립하는가 — 규칙 c의 "선 하나면 된다"를 실제로 확인했다(성립한다) |
| **#26** | 게이트를 측정 전에 박았다(`rule_camera.json`의 `gate.registered`) |
| **#27** | **대역을 다른 하네스에서 안 가져온다** — 검출 팔을 **같은 픽스처·같은 실행**에서 함께 돌렸다 |
| **#30** | 양성 채널 — 규칙이 **안 받는 것**을 시험이 함께 확인한다(애매·나란한 선·미분류) |
| **#33** | 값 대조 — 새 임계 `RULE_TOL`을 `test/constants.ts`에 등록했다 |
| **#35 · #40** | 도달 가능성. 오라클이 **항등이라 없고**, 대신 `rule_wide_pair` 팔을 두었다 |
| **#37** | 이론서를 먼저 봤다 — 2.2(무한원) · 3.1(지평선) · 6.3(수심) · **6.4(수심 항등식 — d가 유도인 이유)** |
| **#41** | **이 게이트는 항목이 스스로 등록한 것이고 CLAUDE.md §2의 중단 조건이 아니다.** 같은 말로 안 부른다 |

### 무엇을 바꿨나

**옛 경로를 통째로 뺐다** — 검출 초안(`draftFromDetection`) · 가이드 여섯 · 핸들 · 민감도 ·
`가이드 조정`/`소실점 추정`/`가이드 늘리기` 버튼. 프리셋도 가이드도 없다.

**새 경로는 `s3d/vpRules.ts` 하나다.** 그은 선이 곧 제약이다:

```
a. 화면 가로·세로 선  → 그 축 자체(무한원, 이론서 2.2). 계산할 것이 없다
b. 깊이선 두 개       → 교점이 첫 소실점. **그 소실점이 지평선을 정의한다**
c. 깊이선 하나 더     → 그 선 × 지평선 = 두 번째 소실점. **세 개가 아니라 하나다**
d. 세 번째(수직)      → 수심 조건으로 **유도**(6.3). 그리는 것이 아니다
```

**유일한 판단은 깊이인가 화면 가로세로인가**이고 임계 둘(`screen_axis_deg` 4° ·
`depth_min_deg` 8°) 사이는 **애매**로 두어 **묻는다.** 묻는 동안 규칙은 안 움직인다.

`horizon.ts`를 **그 순서로 다시 썼다**(사람 지시). 폐기된 사후 사영(`applyRollZero` ·
`fitHorizonPair`, D-L32)은 `horizonRollZero.ts`로 **옮겼고 지우지 않았다** —
`horizon.test.ts`가 그 판정의 근거를 계속 재현한다.

**곁가지로 1점 투시가 처음으로 선다.** 두 가지가 막고 있었다:
① **f가 없었다** — 소실점 하나면 자유도 1(f)이 남는데 `lensF`를 아무도 안 세웠다.
`recoverCamera`가 `ok:false`를 내고 `ctx()`가 `null`이 되어 **무엇을 그려도 대기**였다.
기본 렌즈 35mm 환산을 설정으로 넣고 화면에 **출처를 낸다**(측정이 아니다).
② **무한원 축의 방향을 넘길 자리가 없었다** — `PlaceCtx.axisDirs`를 신설했다.
화면 가로 축은 `(1,0,0)`, 세로는 `(0,1,0)`이다(c=0).

### 직선 렌더 (지시 2)

계획서 §1.1이 "획이 직선 세그먼트가 되고 손떨림은 버린다"인데 **렌더 층 셋이 그 결정을
안 따르고 있었다.** 셋 다 고쳤다:

| 자리 | 전 | 후 |
|---|---|---|
| `inkCanvas` 진행 중 획 | 점열 폴리라인(증분) | **시작점 → 커서 직선**(전체 재그리기) |
| `inkCanvas` 확정 획 | 점열 폴리라인 | 시작점 → 끝점 직선 |
| `mainL.drawPending`·승격 손실 | 점열 폴리라인 | `drawStraight` |
| `strokeView` 튜브 중심선 | `pts3d` 전부 | **양 끝 둘** |

`pts2d`·`pts3d`는 **그대로 보존한다** — 되돌아갈 때 이 자리들만 되돌리면 된다.
⚠ **3D 레이어는 이미 직선이었다**(`stage.ts`가 `LineSegments`를 쓴다). 보이던 손 획은
**2D 잉크 층**이었다. `strokeView`/`tube`는 지금 앱에 배선돼 있지 않다(테스트만 부른다).

### 측정 — `stage0/out/rule_camera.json`

L-A.6과 같은 다섯 구도 · 잡음 5수준 · 등급 2 · 시드 6. **검출 팔을 같은 실행에서 함께 돌렸다**(#27).

표제는 **`headline_no_identity`**다 — 잡음 0 행과 `1pt_yaw0_pitch0` 구도를 뺐다(둘 다 항등, 아래).

| 팔 | 축 오차 중앙(°) | p90 | 카메라 섬 | 잰 축 수 | 물음/실행 |
|---|---|---|---|---|---|
| `rule_drawn` (축을 번갈아 긋는다) | **32.57** | 44.66 | 92/192 | 204 | 2.53 |
| `rule_grouped` (축별로 몰아 긋는다) | **6.93** | 26.39 | 157/192 | 464 | 2.62 |
| `rule_wide_pair` (각차가 큰 둘을 먼저) | **6.93** | 29.78 | 147/192 | 435 | 2.62 |
| `detect` (같은 획, 4선 최소제곱) | **9.17** | 35.00 | 159/192 | 321 | — |

**사전 등록 게이트: 통과**(6.93 < 9.17). ⚠ **그러나 이 통과를 결론으로 읽지 않는다** — 넷:

1. **순서가 지배한다.** 축별로 몰아 그으면 6.93°, 번갈아 그으면 **32.57°**다.
   규칙 b가 "**처음** 두 깊이선의 교점"이라 그 둘이 다른 축이면 엉뚱한 점이 나온다.
   `grouped`·`wide_pair`는 **참 축 라벨을 쓰는 순서 오라클**이다 — 사람이 그 순서로
   긋는다는 가정(AS-6과 같은 자리)이고 **표본이 0이다**.
2. **두 팔의 분모가 다르다**(#11). 규칙 팔은 축 464개, 검출 팔은 321개를 잰다 —
   규칙은 차수 중앙 3, 검출은 2다. **같은 양의 비교가 아니다.**
3. **어느 팔도 임계 0.5°에 못 간다.** 4~10° 대역 안에서 자리를 바꾼 것뿐이다.
   자동 확정이 여전히 없다는 사실(AS-L10)은 안 바뀌었다.
4. **1.5배 차이는 시드 밴드 밖인지 확인하지 않았다**(#14). 지금은 시드를 합산한 하나의 중앙값이다.

**항등 둘을 갈라 냈다**(#5):
- **잡음 0 행** — 참 투영을 넣으면 두 깊이선의 교점이 **정의상 참 소실점**이다.
- **`1pt_yaw0_pitch0` 구도** — 유일한 유한 소실점이 **주점 자리**이고, 소실점이 하나면
  주점이 그 소실점이 된다(이론서 5.3). 그러면 축 방향이 **f와 무관하게 광축 (0,0,1)**이고
  그 구도의 참 깊이축과 같아서 **어떤 오차를 넣어도 0이 나온다.** 실측이 `deg_median = 0`으로
  그것을 보였다 — 발견이 아니라 **정의의 귀결**이다.

**측정이 지시문을 하나 정정했다**(#23·A-3). "오차의 출처가 그은 선의 정확도뿐"이 아니다 —
**두 선의 각차**가 함께 정한다(`first_pair_sep_deg_median` 9.6~11.9°).
나란한 두 선의 교점은 조건수가 죽는다. 그래서 `min_sep_deg` 3°를 두고, 그보다 나란하면
**소실점을 안 만들고 기다린다.**

### 측정이 잡은 결함 하나 — 고쳤다

초판은 지지선 판별을 **지평선 위 x 거리**(`same_vp_ratio`)로 했다. 소실점이 멀수록 같은
각도 오차가 큰 x 차이로 나오므로 **첫 축을 향해 그은 지지선이 새 소실점으로 오인되고**,
진짜 두 번째 축의 선은 "슬롯이 찼다"고 거절됐다 — 조용히 틀리는 종류다.
**각으로 재는 것으로 바꿨다**(`axis.ts`의 부적합도 = 수직거리 ÷ 길이. #17: 새 임계를 안 만든다.
#24: 단위를 고치는 것이지 임계를 무르는 것이 아니다). `same_vp_ratio`를 지웠다.

---

## 축 스냅 · 지평선 고정 (2026-08-16, 사람 지시)

### 착수 시 `PITFALLS.md` 최근 다섯을 읽었다 — 걸리는 번호

| # | 이 항목의 어디에 걸리나 |
|---|---|
| **#41** | **A-4가 "깨진 채로 가지 말라"고 했고 지난 항목이 안 지켰다.** 이번엔 **먼저 닫는다** — 못 닫으면 격리하고 `DEFERRED.md`에 남긴 뒤 진행한다(사람 지시가 그 갈래를 열어 뒀다) |
| **#40** | 새 게이트의 도달 가능성을 **항등으로 채우지 않는다.** ⚠ 축 스냅은 **확정 후 선의 오차를 0으로 만드는 기전**이라 "축 오차가 준다"가 **설계 보장**일 수 있다 — 재는 것이 **첫 한두 선의 오차**인지 확인한다 |
| **#39** | 제약을 걸 때 **위약 팔**을 함께 돌린다. 지평선 고정은 제약이므로 "같은 크기의 무의미한 제약"과 갈라야 한다 |
| **#38** | 자동 검사·측정이 **덮는 대상 수**를 함께 낸다(모호 판정이 **한 번도 발동 안 하면** 그 팔은 공허하다) |
| **#37** | 새 지표·기전 전에 이론서를 본다 — 지평선(3.1) · 수심(6.3) · 무한원(2.2) · 자유도(5.3) |

### A-4 먼저 — 깨진 종단 확인 둘을 닫았다

**원인은 셋이고 전부 하네스 쪽이었다.** "비결정"으로 보였던 것은 **실패 지점이 두 곳**이라
실행마다 먼저 걸리는 줄이 달랐던 것이다(#7: 추측하지 말고 센다 — 어느 `expect`가 실패했는지
찍어 보니 갈렸다).

| 자리 | 무엇이었나 |
|---|---|
| `단일 뷰포트` ① | **주점을 옮기는 손잡이를 잘못 골랐다.** 롤 0에서 `V₁V₂`가 수평이면 수심의 x가 `V₃.x`로 정해지므로(이론서 6.3) **수평 소실점을 밀어도 주점 x가 안 움직인다**(실측 0). 게다가 `threeIntrinsics`의 `offsetX = W/2 − pₓ`이고 `W = 2·max(pₓ, w−pₓ)`라 **주점이 중심보다 오른쪽이면 `offsetX`가 정확히 0**이다 — 오른쪽으로 밀었다가 걸렸다. **왼쪽으로, 수직 소실점을** 민다 |
| `단일 뷰포트` ② | **민 카메라를 안 되돌렸다.** 그 아래 검사들은 **확정 카메라로 푼 기하**를 되쏘므로 카메라만 옮겨 두면 상이 어긋난다 — 칠해진 선분이 밀기 크기에 비례해 줄었다(**−140에서 0/12 · −30에서 7/12 · −6에서 8/12 · −2에서 11/12**). 옛 판은 가이드 핸들을 26px 밀어 소실점이 거의 안 움직였고 그래서 안 드러났다. **끝나면 되돌린다** |
| `되돌리기 UI` | **픽스처가 더는 스냅을 안 잃는다** — 규칙 카메라에서는 승격 뒤에도 대상이 전부 놓여 **6/6이 새 상으로 옮겨진다**. 옛 판은 `snap_lost > 0`을 무조건 요구했다. **임계를 낮춰 통과시키지 않았다**(그러면 #32의 "미실행을 통과로 읽는 것"이다) — **회계 항등**(`옮긴 것 + 끊긴 것 = 있던 것`)과 **기전이 돌았는가**(`snap_had > 0`)를 대신 요구하고, 끊긴 것이 0이면 표시 검사 넷이 **실행되지 않았다는 사실을 원장에 적는다** |

⚠ **`vd.DRAFT_TOL.min_guide_ratio` 인용도 함께 죽었다** — 가이드가 없으니 "요구 길이"도 없는
양이다. `null`로 적고 사유를 남겼다(#29의 숙제였다는 것이 근거다).

**Playwright 21 통과 · 2 건너뜀 / 23.**

### 축 스냅 · 지평선 고정 — 구현과 측정

**구현**
- `s3d/axisSnap.ts` 신설 — 후보 계산(`axisCandidates`) + 선택(`chooseAxis`). 각도로 거르지 않고
  **언제나 어느 축으로 간다**. 끝점은 커서 광선과 축 직선의 **최근접점**(= 화면 수직 투영).
- **모호 구간**은 1·2등 각차가 `ambiguous_deg`(6°) 안일 때이고 **커서에 가까운 쪽**이 이긴다.
  좌우 규칙 없음 · 긋는 방향 무관 · 화면에는 **고른 것만** · 상태 줄에 `모호` 표시.
- 토글(`축 스냅` 버튼 · **F8** — 라이노 직교 모드) 기본 켬 · 수정자(**Shift**, 그 획만 자유).
  ⚠ **Shift의 뜻이 바뀌었다** — 옛 "추론 축 잠금"은 축 스냅이 늘 도니 할 일이 없어졌다.
- **지평선이 처음부터 있다**. `RuleState.horizon`이 `number`가 되고 기본값은 화면 중앙(피치 0).
  깊이선 **하나**로 소실점이 선다(옛 판은 둘). `waiting`과 `min_sep_deg`가 없어졌다.
- 지평선을 **언제나 옅게 그린다**(`drawHorizon`) — 카메라가 서기 전에도.

**⚠ 곁가지 귀결**: 기본 지평선이 화면 중앙 = 피치 0이므로 **수심 유도가 `null`**이고
(이론서 2.2 — 피치 0에서 수직축은 화면 평행) **초기 스케치는 1점·2점뿐이다.**
3점은 궤도로 지평선이 중앙을 벗어난 뒤다. `QUESTIONS.md`에 올렸다.

**측정 — `stage0/out/axis_snap.json`** (다섯 팔 × 두 순서 · 5구도 × 6시드 × 등급 2 × 잡음 4)

| 팔 (번갈아 긋기) | 축 오차 중앙 | 배치 | 형태 중앙 | 조용히 틀림(0.2) | 스냅한 획 |
|---|---|---|---|---|---|
| 스냅 켬 · 지평선 켬 | 9.96° | 1167/2880 | 0.696 | **0.967** | 991 |
| 스냅 끔 · 지평선 켬 | 9.96° | 1167/2880 | **0.441** | **0.811** | 0 |
| 스냅 켬 · 지평선 끔 | 12.91° | 1617/2880 | 0.690 | 0.920 | 715 |
| 스냅 끔 · 지평선 끔 | 12.91° | 1617/2880 | 0.564 | 0.868 | 0 |
| **위약**(틀린 지평선) | 32.48° | 358/2880 | 1.681 | 1.000 | 308 |

#### ⛔ 사전 등록 게이트 **실패** — 예상이 틀렸고 측정을 따른다(A-3)

**등록**: 축 스냅 켬이 ① 배치율이 안 줄고 ② 조용히 틀림이 안 늘고 ③ 순서 격차가 준다.
**결과**: ①은 같고(1167/2880 동일 — 축 스냅은 배치 여부를 안 바꾼다), **②가 깨졌고**
(0.811 → 0.967) **③도 깨졌다**(0 → 5.52°. ⚠ 지평선 끔 팔의 격차 0은 그 팔이
**참 축 라벨 오라클**이라 순서와 무관해진 것이고, 지평선 팔과 같은 양이 아니다).

**왜 그런가 — 축 스냅은 그리기 오차를 없애고 카메라 오차를 증폭한다.**
이 픽스처의 카메라 축 오차가 중앙 **9.96~15.47°**다. 축 스냅은 획을 **그 축에 정확히** 붙이므로
각 획이 카메라 오차를 **고스란히** 물려받는다. 스냅이 없으면 획마다 다른 방향이 들어와
`liftAll`의 전역 해에서 **일부 상쇄**된다. 즉 **축 스냅의 이득은 카메라가 정확할 때 나온다** —
지금 카메라는 임계(0.5°)의 **20~30배**다.

⚠ **축 오차는 팔 사이에서 변하지 않는다**(9.9569로 동일). 사람 지시 4의 예상
("확정 후 선의 오차가 0이 되므로 축 오차가 크게 준다")은 **이 하네스에서 반증됐다** —
소실점은 확정 후 잠기므로(CLAUDE.md §1) **뒤에 그은 선이 카메라에 기여하지 않는다.**
축 스냅이 바꾸는 것은 **배치의 형태**이지 카메라가 아니다. AS-L16으로 적었다.

**위약 팔이 제 몫을 했다**(#39): 지평선을 **틀린 높이**(+0.18·H)에 고정하면 축 오차가
32~35°, 배치가 1/3로 무너진다. 즉 **"지평선이 있다"가 아니라 "지평선이 맞다"가 이득의 출처**다.

**모호 판정**은 확정 뒤 991획 중 **253회 발동(21.7%)**하고 **정확도 0.047**이다.
임계 스윕(0·3·6·12·24°)에서 발동은 0 → 1161로 늘지만 **배치·조용히 틀림은 거의 안 움직인다** —
⚠ **이 픽스처에서는 모호 판정이 결과를 안 바꾼다.** 정확도가 낮은 것은 **카메라 축이
이미 10~15° 틀려서 "참 축"과 슬롯의 대응 자체가 흔들리기 때문**으로 보이나 **안 갈랐다**.

#### 하네스가 잡은 버그 둘 — **#38이 잡았다**

1. **앵커의 화면 좌표를 획의 `pts2d[0]`으로 줬다.** 앵커(3D)와 기준점(2D)이 어긋나
   각차가 엉망이 됐고 **모호 판정이 97% 발동**했다. 되쏜 좌표로 고치니 21.7%가 됐다.
2. **확정 전 획까지 다시 스냅했다.** 앱에서 축 스냅은 **확정 뒤**에만 돈다 —
   카메라를 세운 획까지 덮어써서 형태 오차가 0.696 대신 0.741이었다.
   **덮는 대상 수(`snapped_strokes`)를 낸 것이 둘 다 잡았다.**

---

## 병합 · 게이트 해석 · 3점 투시 · Shift 통일 (2026-08-16, 사람 지시)

### 착수 시 `PITFALLS.md` 최근 다섯을 읽었다 — 걸리는 번호

| # | 이 항목의 어디에 걸리나 |
|---|---|
| **#41** | **게이트가 실패했는데 멈추지 않는다 — 그것이 중단 조건이 아니기 때문이다.** CLAUDE.md §2의 중단 조건은 **L-A.7 재등록 게이트**(사람이 가이드를 끌어 0.5°)이고 축 스냅 게이트는 그것이 아니다. 사람이 "기록만 하고 넘어간다"로 판정과 행동을 맞췄다 — **어긋난 채 진행하지 않는다** |
| **#40** | 3점 투시 측정에 **도달 가능성**을 함께 적는다. 참 지평선을 넣는 팔은 **오라클**이고 사람이 그 자리에 지평선을 놓는지는 **표본 0**이다(AS-L9와 같은 자리). 오라클을 "상한"이라 부르지 않는다(#35) |
| **#39** | 지평선을 옮기는 것은 **제약을 바꾸는 것**이다 → **위약 팔**(같은 크기 **틀린 방향** 이동)을 함께 낸다. `axis_snap.json`의 위약이 그 자리였고 이번에도 그 형식을 쓴다 |
| **#38** | 새 측정은 **덮는 대상 수**(구도 수 · 실행 수 · 실제로 3점이 된 실행 수)를 함께 낸다. 0이면 그 팔은 공허하다 |
| **#37** | 새 기전을 만들기 전에 이론서를 본다 — **지평선 = 피치**(3.1) · **수심 유도**(6.3) · **무한원**(2.2) · **자유도 회계**(5.3). 지평선 드래그는 새 이론이 아니라 **3.1의 역방향 조작**이다 |

### 1. main 병합과 배포 확인 (사람 승인)

`claude/vanishing-point-rules-overhaul-qxrqga`를 **fast-forward**로 main에 넣었다
(main이 뒤처져 있지 않았다 — 병합 커밋이 없다). 커밋 여섯: 규칙 전면 교체 · 배선 마무리 ·
A-4 종단 확인 · 착수 기록 · 축 스냅/지평선 · 배포 확인.

| 확인 | 결과 |
|---|---|
| Actions `build` | ✅ 초록 |
| Actions `deploy` | ✅ 초록 — **브랜치에서는 막혀 있던 job이다**(main 제한). 병합으로 처음 돌았다 |
| Actions `measure` | 실행 중(같은 실행) |
| 사이트 갱신 | ✅ `assets/l-j0Lx4sLb.js`에 **`축 스냅` · `라이노 직교 모드` · `지평선`** 문자열이 있다 — 배포본이 이번 빌드다 |

⚠ **이 확인은 원장 밖 관측이다**(HANDOFF의 기존 경고와 같다) — `static_deploy.spec.ts`는
여전히 로컬 정적 서버를 잰다.

### 2. 게이트 실패 해석 — 기록만 하고 넘어간다 (사람 판정)

**원인 진단이 승인됐다**: 축 스냅은 획을 축에 정확히 붙이므로 **카메라 오차를 증폭**하고,
이 픽스처의 카메라가 임계의 20~30배라 그 항이 지배한다. 즉 **축 스냅이 나쁜 게 아니라
카메라가 나쁘다.** 위약 팔(32.48°)이 "지평선이 **맞을 때만** 이득"을 가른 것도 승인됐다.

**설계를 비틀지 않는다 — 켬 기본 유지**(D-L42 그대로). 대신 `QUESTIONS.md` **f**에
실획 측정 항목을 등록했다: **"카메라 오차가 임계의 몇 배 이하일 때 축 스냅이 이득으로
돌아서는가."** 지금 아는 점은 **20~30배에서 켬이 나쁘다** 하나뿐이고, 부호가 바뀌는 배수는
미측정이다. ⚠ **합성 섭동으로 배수 축을 만들어도 섭동 0 셀은 공허하다**(#5 — 참 소실점에서
축 스냅은 정의상 참 축에 붙는다).

### 3. 3점 투시 — **지평선을 사용자가 끈다**(D-L45, QUESTIONS g의 답)

**고른 것**: 지평선 드래그(선택지 b). **기각**: 기본 피치 상수를 0이 아닌 값으로 두는 것(a) —
상수 하나라 더 단순해 보이지만 **어느 값도 임의**이고 앙각/부감을 사용자가 못 고른다.
드래그는 그 상수를 사용자가 주는 것이라 **(a)를 포함한다**. 종이 위 투시에서 지평선은
**가장 먼저 긋는 선**이므로 선례도 그쪽이다(A-3).

**새 자유도가 아니다** — `RuleState.horizon`이 이미 값 하나였다. 바뀐 것은 **누가 그 값을
주는가**뿐이고, **소실점이 서면 잠긴다**(`horizonAdjustable`). 잠그는 이유는 D-L32다:
소실점은 지평선 **위에서** 정해지므로 지평선만 옮기면 자기 지지선에서 떨어진다.
반경은 `PICK_TOL`을 그대로 쓴다(#17 — 새 임계를 안 만들었고 **상수 해시가 안 움직였다**).

#### 측정 — `stage0/out/horizon_pitch.json` (5구도 × 6시드 × 등급 2 × 잡음 5 × 순서 2)

표제는 **피치≠0 세 구도 · 잡음 걸린 행**(잡음 0은 항등이라 뺐다, #5).

| 팔(`grouped`) | 차수 3인 실행 | 축 오차 중앙 | p90 | 카메라 선 실행 |
|---|---|---|---|---|
| 중앙 지평선(지금까지) | **0/144** | 25.2358° | 38.9013 | 110/144 |
| **참 지평선**(오라클) | **141/144** | **2.1133°** | 10.3922 | 106/144 |
| 위약(참에서 반대 방향 같은 크기) | 141/144 | 35.0618° | 46.193 | **35/144** |

⚠⚠ **아래 초판 판정은 리뷰어 지적으로 개정됐다 — 3-R 절이 최종이다.** 원문을 남긴다:
사전 등록 셋 전부 통과: ① 3점이 실제로 난다(중앙 팔은 0) ② 참 팔이 중앙보다 낫다
③ **위약이 중앙보다 나쁘다** — 이득의 출처가 "옮겼다"가 아니라 **"맞다"**임을 가른다(#39).
`axis_snap.json`의 위약과 **같은 방향의 결론**이다.

⚠ **참 팔은 오라클이다**(#35) — 사람이 지평선을 그만큼 맞추는지는 **표본 0**이고(AS-L9)
상한이라 부르지 않는다. ⚠ **중앙 팔의 25.24°를 `rule_camera.json`의 6.93°와 견주지 않는다** —
여기 모집단은 피치≠0 구도만이라 중앙 지평선이 **구성상 268px 틀려 있다**(#11).

**selfcheck 의심 플래그의 정체를 원장 안에 적었다**(`what_the_zeros_mean`):
`center|*`의 `horizon_offset_px_median = 0`은 **구성상 0**(덮는 대상 표시, #38),
`center|*`의 `order3_rate = 0`은 **관측 그 자체**, 2pt·1pt 층의 offset 0은
**세 팔이 한 팔이 되는 층**(덮는 대상 없음)이다.

#### 브라우저가 결함 하나를 잡았다 — 고쳤고 회귀 팔을 달았다

`resize`를 "기본값일 때만 중앙으로"에서 **비례 이동**으로 바꿨는데, **레이아웃 전 캔버스
높이가 1px**인 순간이 있어서 그 상태의 `setHorizon(200)`이 **1로 잘리고** 그 1이 비례로
늘어나 **지평선이 화면 바닥(675)에 붙었다.** 실제 앱 화면에서 값으로 확인했다.
고친 규칙: **옛 높이가 2px 이하면 비례가 아니라 기본값(중앙)으로 다시 잡는다.**
`horizon_drag.test.ts`의 `[회귀]` 항목이 그 버그를 되살려 실제로 잡는다(#21).

#### 화면 안내를 한 곳으로 모았다

`ruleStatus()`의 지평선 줄이 **옛 규칙 문구**("첫 소실점이 정했습니다")를 그대로 내고 있었다 —
D-L43에서 이미 틀린 말이었다. 지금은 그 줄 하나가 **끌 수 있는지 · 구도가 무엇인지**를 함께
내고, `renderStatus`는 따로 안 낸다(두 곳에서 내면 문구가 갈린다).

**검증**: vitest **65파일 381 통과 · 1 건너뜀** · tsc·빌드 통과 · Playwright **21 통과 · 2 건너뜀** ·
selfcheck **STALE(상수) 0 · STALE(정의) 0 · 인용 90건 플래그 0 · 게이트 블록 12개 플래그 0** ·
브라우저 실측(3점이 초기 두 획으로 섰다: 소실점 (1150,215)·(150,215)·유도 (640,2017), f=481). ⚠ **이 브라우저 관측은 원장 밖이다**(#25 · 리뷰어 [11]) — `stage0/out/`에 그 수가 없다. 근거로 쓰는 것은 원장(`horizon_pitch.json@1b6175a4`)이고 이것은 **배선이 실제로 돈다는 확인**이다.

### 4. `Shift` 의미 통일 — **문서와 화면과 코드가 같은 말을 한다**(D-L44)

옛 뜻("추론된 축을 잠근다")이 **세 곳에 남아 있었다**:

| 자리 | 무엇이 남아 있었나 |
|---|---|
| `mainL.ts`의 `axisLock: "infer"` · `shiftHeld` | **죽은 코드다** — 키보드가 더는 `"infer"`를 안 넣는다. `onLive`의 "처음 추론된 축을 잡아 둔다" 블록도 도달 불가였다 |
| 상태 줄 | `축 고정 Shift` · `(… Shift 추론 축 · Esc 해제)`라고 **적고 있었다** — 실제로는 안 되는 조작이다 |
| `README.md` | 축 스냅·`Shift`·`F8`·화살표가 **아예 없었다**(축 스냅 항목이 README를 안 고쳤다) |

지웠고, 상태 줄은 `축 고정 축N (← → ↑ · 같은 키 다시 · Esc)`로 바꿨고,
README에 **조작 표**를 넣었다(지평선 손잡이 · `F8` · `Shift` · 화살표 · 도구 셋, 각각 선례와 함께).

⚠ **`Shift`는 이제 반대 방향이다** — 잠그는 키가 아니라 **푸는** 키다(그 획만 자유).

**검증**: vitest **65파일 382 통과 · 1 건너뜀** · tsc·빌드 통과 · Playwright **21 통과 · 2 건너뜀** ·
pytest **73/73** · selfcheck 플래그 1856건(STALE 0 · 인용 0 · 게이트 0 · 분모 12건은 기존 `Math.max(1,n)`).

⚠ **절차 하나를 어겼다**: 앞 항목에서 `progress.md`를 적을 때 셸 작업 디렉토리가 `web/`이라
**`web/progress.md`가 또 생겼다**(바로 앞 커밋 d6450f1이 그것을 기계가 보게 만든 항목이다).
selfcheck는 **그 파일을 정확히 잡는다**(시험해서 확인했다: 플래그 1건) — 못 잡은 것이 아니라
**내가 커밋 전에 selfcheck를 다시 안 돌린 것**이다. 규칙: **`progress.md`를 적은 뒤 커밋 전에
selfcheck를 한 번 더 돌린다.**

---

## 오스냅 (2026-08-16, 사람 지시)

### 착수 시 `PITFALLS.md` 최근 다섯을 읽었다 — 걸리는 번호

| # | 이 항목의 어디에 걸리나 |
|---|---|
| **#41** | 이 항목은 게이트를 등록하되 **중단 조건이 아니다**(중단 조건은 L-A.7 하나다). 판정과 행동을 어긋나게 두지 않는다 |
| **#40** | 새 게이트의 도달 가능성을 **항등으로 채우지 않는다.** ⚠ **양 끝 스냅은 조심해야 한다** — 합성 잉크의 끝점이 **참 모서리에 못박혀** 있으면(#2가 실제로 걸렸던 자리) "양 끝을 스냅하면 정확하다"가 **설계 보장**이 된다. 겨냥 오차를 실제로 넣은 행으로만 표제를 낸다 |
| **#39** | 오스냅이 **축 스냅을 이긴다**는 규칙을 넣으므로 **대조 팔**이 필요하다 — 같은 획을 축 스냅만으로 놓은 팔과 견준다 |
| **#38** | **덮는 대상 수를 낸다** — 양 끝 스냅이 **몇 번 실제로 발동했는지**. 0이면 그 팔은 공허하다 |
| **#37** | 새 기전 전에 이론서·선례를 본다. 오스냅 목록·우선순위·표시는 **Rhino/SketchUp 그대로**이고(A-3) 새로 설계하지 않는다. 두 점이 3D를 정하는 것은 §3의 "시작점이 확정되면 커서가 끝점을 정한다"의 **양쪽 판**이다 |

**추가로 걸리는 것**: **#11**(분모 — 대각선 비율의 분모가 "축 밖 획 전부"인지) ·
**#3**(지면 스냅은 화면 거리가 정의상 0이라 성공률에서 뺀다 — `snap.json`이 이미 그 규약이다) ·
**#12**(겨냥 오차 동작점을 하나로 고르지 않는다) · **#34**(같은 결함이 다른 호출부에 남았는지 —
`snapStart`를 고칠 때 `snapEnd`도 같이 본다).

### 3-R. 지평선 항목 리뷰어 1회차 — **15건(높음 4). 넷 다 내 쪽이 틀렸다**

⚠ **사전 등록 절이 이 문서에 없었다**(#26 · 리뷰어 [8]). 게이트 문안은 하네스 소스
(`web/test/horizon_pitch.test.ts`의 `REGISTERED`)에 **첫 실행 전에** 박혀 커밋 `d0347f7`에 들어갔지만,
`progress.md`에 절을 안 뒀다 — L-D.2·L-D.3에서 같은 지적을 받은 자리이고 **재발이다**(#34).
아래가 그 절이고, **개정된 등록**이다(초판 등록의 결함 셋을 리뷰어가 짚었다).

#### 사전 등록 (개정 — 리뷰어 대응 후 재측정)

**두 순서(`grouped`·`drawn`) 각각에서** 참 지평선 팔이:
① 차수 3인 실행이 난다(중앙 팔은 0) ② 축 오차 중앙(**수평 축 둘만 자른 값 포함**)이 중앙 팔보다 작다
③ **참 ↔ 위약**(중앙에서 같은 크기·반대 방향)에서 참이 낫다. **구도별로도 판정해 함께 적는다.**

| # | 지적 | 대응 |
|---|---|---|
| **[1]** | **도달 가능성 값이 판정 대상 그 자체였다**(`reachability_value 2.1133 = result.deg_true`) | **맞다.** 참 지평선 팔은 오라클이자 판정 대상이라 그 값은 자명하다 — #40이 `segment_gate`에서 고친 것과 **같은 구조**다. `reachability_absent`로 바꾸고, 실제로 물어야 할 도달 가능성(**사람이 몇 px 안에 놓는가**)은 표본 0임을 적었다. 대신 **허용 오차 곡선**을 냈다 |
| **[2]** | **위약은 참에서 `2d`이지 `d`가 아니다** — "위약 대 중앙"은 거리 단조성으로도 설명된다 | **맞다.** 서술이 틀렸다(설계는 옳았다). 판정 짝을 **참 ↔ 위약**(중앙에서 같은 크기, 반대 방향)으로 바로잡았고 원장·D-L45·AS-L19의 문구를 고쳤다 |
| **[3]** | 표제가 **`grouped` 하나**의 값인데 등록 모집단에 **순서 축이 없었다** | **맞다.** 두 순서를 등록하고 **둘 다 판정**한다(#28). 인용도 둘 다 적는다 — `grouped` 25.24 → 2.11, `drawn` 11.12 → 2.57 |
| **[4]** | 지평선 오차 축이 **{0, d, 2d} 셋뿐**이라 "몇 px까지 맞으면 이득인가"가 없다 | **맞다.** `±10·±30·±60·±120px` 스윕을 넣었다. **이 항목에서 가장 중요한 새 결과가 여기서 나왔다**(아래) |
| **[5]** | 팔마다 축 집합이 다르다(참 팔엔 유도된 세 번째가 섞인다) | **맞다.** `deg_median_h2`(수평 축 둘만)를 함께 내고 게이트가 그것도 본다. AS-L19의 "같은 획·같은 실행" 표현을 뺐다 |
| **[6]** | 구도별로는 위약 조건이 **3 중 2**에서만 성립했다 | **맞다.** 게이트가 **구도별 판정**을 함께 낸다. 짝을 바로잡은 뒤(**[2]**) **세 구도 전부 통과**한다 |
| **[7]** | `order3 141`은 카메라가 섰다는 뜻이 아니고 **위약도 141**이다 | **맞다.** ①이 "중앙이 아니다"만 가른다는 사실을 등록 문안에 박고, `camera_ok`를 표제·인용에 함께 낸다 |
| [9] | **스칼라 지평선 = 롤 0**이 제품 경로에 들어갔는데 QUESTIONS d·e는 "제약 없음"이라 적혀 있다 | **맞다.** d·e에 그 사실을 적었다. ⚠ 이것은 D-L43이 이미 만든 상태이고 D-L45가 드러낸 것이다 |
| [10] | "중앙 = 피치 0"은 **AS-C5**(주점 = 이미지 중심) 위에 있다 | **맞다.** D-L45와 AS-L19에 적었다 |
| [11] | 브라우저 실측이 **원장 밖**이다 | **맞다.** 그 문장에 "원장 밖 관측" 표시를 달았다(§1과 같은 처리) |
| [12] | 인용에 `@해시`가 없다 | **맞다.** 세 문서에 `@1b6175a4`를 붙였다 |
| [13] | "새 자유도가 아니다"는 자료구조 얘기다 | **갈라 적었다** — 자료구조는 안 바뀌고, **고정 상수가 사용자 입력이 된 것**은 맞다 |
| [14] | PITFALLS의 게이트 덮는 수 표가 낡았다(7) | **고쳤다** — **12**(값 대조 4 · 면제 8) |
| [15] | 세 팔 전부 **물음에 참 축으로 답한 오라클** 위의 값이다 | **맞다.** 원장 `ask_policy`에 "모든 팔이 같은 정책이고 어느 팔도 실사용 성능이 아니다"를 박고 인용 표에 각주를 달았다 |

#### 재측정 결과 — **허용 오차 곡선이 이 항목의 답을 바꿨다**

`horizon_pitch.json@1b6175a4`, 표제는 피치≠0 세 구도 · 잡음 걸린 행.

| 순서 | 중앙 지평선 | 참 지평선 | 위약 | 차수 3(참/중앙/위약) | 카메라 선 실행(참/중앙/위약) |
|---|---|---|---|---|---|
| `grouped` | 25.2358° | **2.1133°** | 35.0618° | 141 / **0** / 141 | 106 / 110 / **35** |
| `drawn` | 11.1205° | **2.5697°** | 40.8094° | 136 / **0** / 134 | 138 / 137 / **58** |

수평 축 둘만 자른 값도 같은 방향이다(`grouped` 25.2358 → **2.5848**, `drawn` 11.1205 → **3.1030**).
**등록 조건 다섯이 두 순서 · 세 구도 전부에서 통과한다.**

**허용 오차 곡선**(참에서 ±px, `grouped` / `drawn`의 축 오차 중앙):

| 오차 | −120 | −60 | −30 | −10 | **0** | +10 | +30 | +60 | +120 |
|---|---|---|---|---|---|---|---|---|---|
| `grouped` | 3.3527 | 2.7788 | 2.4588 | 2.1988 | **2.1133** | 2.1234 | 2.3011 | 2.7744 | 4.7413 |
| `drawn` | 3.2009 | 2.9383 | 2.6764 | 2.5901 | **2.5697** | 2.5711 | 2.6385 | 2.8741 | 4.7282 |

**±120px까지 전부 중앙 팔보다 낫다**(중앙 팔은 25.24 / 11.12). 그리고 **중앙 팔 자체가
참에서 중앙 268px 떨어진 팔**이므로, 창의 바깥 끝은 **120px과 268px 사이 어딘가**이고
**이 스윕은 그 끝을 못 찾았다**(안 쟀다 — 넓히면 찾을 수 있다).

⚠⚠ **이것이 소실점 핸들과 다른 점이다.** `guide_budget.json`이 잰 소실점 핸들 예산은
**0.12~6.8px(1점 구도는 서브픽셀)**이었다 — 사람이 도달할 수 없는 자릿수다.
**지평선은 최소 ±120px의 창**을 가진다. **자유도 하나를 사람 손이 닿는 단위로 옮긴 것**이고
(#29가 요구한 환산이 그것이다), 그것이 D-L45를 채택하는 진짜 근거다.
⚠ **사람이 그 창에 드는지는 여전히 표본 0이다**(AS-L9).

### 5. 오스냅 — **양 끝이 붙으면 축 없이 놓인다**(D-L46)

**선례를 그대로 따랐다**(A-3). 대상·우선순위·표시가 전부 라이노이고 새로 설계한 것이 없다.

| 무엇 | 어떻게 |
|---|---|
| 대상 | 정점 · 끝점 · 중점 · 교차점 · 수선 발 · 근처점(선 위) · 면 위 점 |
| 우선순위 | `정점 > 끝점 > 중점 > 교차점 > 수선 발 > 선 위 > 면 위` |
| **정점** | 이 자료구조에 **메쉬가 없다** — 없는 것을 지어내지 않고 **여러 획이 만나는 끝점**에 그 이름을 줬다. **위치는 끝점과 같으므로 붙는 자리가 안 바뀐다**(표시만 갈린다) |
| 표시 | 종류별 **아이콘**(네모·채운 네모·마름모·×·⊥·원·삼각) + 색 + **툴팁**(무엇을 정하는지) |
| **양 끝 스냅** | 끝점도 스냅하면 **두 점이 선분을 정한다** — 축이 필요 없다. 미리보기·확정·승격 연쇄가 **같은 함수**를 부른다(#17) |
| 축 스냅과의 우선순위 | **오스냅이 이긴다**(라이노에서 오스냅이 직교 모드를 덮는다) |
| 축 라벨 | 두 점 배치는 **축을 안 붙인다**(`free`) — 방향이 맞아 보여도 추정하지 않는다(A-3) |
| 승격 | 양 끝을 다시 풀어 **둘 다 살아나면 두 점으로 놓고 하나라도 못 살리면 안 놓는다**(반쪽은 조용히 틀린 자리다) |
| 저장 | `Stroke.snapEnd` 신설. 옛 저장본은 `null`로 복원된다 |

#### 측정 — `stage0/out/osnap.json@1b6175a4` (5구도 × 6시드 × 등급 2 × 잡음 4, 대각선 480개)

| 지표 | 값 |
|---|---|
| 시작점 붙음 | **476/480** |
| **양 끝 붙음** | **459/480** (첫 판 474 — 근처점을 뺀 뒤 값이다) |
| 시작점만 붙음(2D 대기) | 17 |
| 오붙음(절단 0.2) | **5/459** — 잡음 0.05 행에 몰려 있다 |
| 오붙음(절단 0.1 / 0.5) | 7/459 / **0/459** |
| **축 경로의 대각선 배치** | **0** — 정의의 귀결이다(대각선은 어느 축도 아니다) |

⚠⚠ **형태 오차 중앙 0은 측정이 아니라 보장이다**(#5·#2) — 대상이 **참 3D**라 옳은 꼭짓점에
붙으면 오차가 **정의상 0**이다. 그러므로 이 지표는 **크기가 아니라 이산**(0 또는 큰 값)이고
**읽을 값은 오붙음률**이다. 원장의 `what_the_zeros_mean`에 그렇게 적었다.
⚠ **상한이다** — 대상이 참 3D이고 카메라 오차가 안 섞였다. 앱에서는 대상이 **놓인 3D 레이어**이고
그것은 카메라 오차를 물고 있다.
⚠ **지면 스냅은 성공률에서 뺐다**(#3) — 화면 거리가 정의상 0이라 넣으면 성공률이 공짜로 1이 된다.

#### 사람 지시 중 **안 한 것**

- **"지평선 근처점 스냅이 이미 있으므로 그 경로를 확장한다"** — 그 문장을 **지평선 손잡이의
  근접 판정**(D-L45에서 만든 `horizonGrab`)으로 읽었다. 오스냅 후보에 **지평선을 대상으로
  넣지는 않았다** — 지평선은 3D 대상이 아니라 **화면 직선**이라 붙여도 3D가 안 정해진다.
  다르게 읽어야 하면 다음 세션이 고친다.

#### 종단 확인이 설계 하나를 정정했다 — **끝점 스냅은 "정확한 대상"만 쓴다**(라이노 기본값)

첫 판은 끝점도 **모든 종류**에 붙였다. 그러자 **모서리를 따라 그은 획이 전부 두 점 배치가 됐다** —
`on_edge`(근처점)가 선 근처 어디서나 걸리기 때문이다. **라이노에서 Near가 기본 꺼짐인 이유가
그것이고**(A-3: 선례를 따른다) 그대로 따랐다: 끝점 스냅은 **정점·끝점·중점·교차점·수선 발**만 쓴다.
시작점 스냅은 종전대로 전부 쓴다(그 경로의 측정 `snap.json`이 그 목록 위에 있다).
하네스도 같은 규약으로 고쳤다(#17) — 양 끝 붙음이 **474 → 459/480**으로 줄고 오붙음은 5/459다.

#### 종단 확인이 사실 하나를 드러냈다 — **같은 꼭짓점이 여러 점이다**

모서리를 따라 그은 획의 두 앵커가 **서로 다른 획의 3D**에서 왔고, 그 둘을 이은 방향이
**축과 6.3° 어긋났다**(실측). 우리 기하는 획마다 따로 올라오므로 "같은 꼭짓점"이 획마다
다른 점이다 — `staticCandidates`의 병합 반경(`merge_ratio` 0.002·G) 밖이다.

- **그래서 두 점 배치의 축 라벨은 종종 미분류다.** 라벨을 억지로 붙이지 않는다(A-3).
- **`osnap.json`은 이 문제를 못 본다** — 대상을 **참 3D**로 주기 때문이다(상한).
  실제 앱의 대상은 **놓인 3D 레이어**이고 그것은 이 불일치를 물고 있다.
- 세 종단 확인의 단언을 **새 계약으로 갱신했다**(옛 단언은 축 경로의 계약이었다):
  `스냅` 스펙은 이제 **두 점 배치의 정의**(3D가 두 앵커와 같은가)를 검사하고,
  `실시간 축 판정`은 "양 끝 스냅"도 판정으로 인정하며, `궤도 후 그리기`는
  **축이든 두 점이든 놓였으면 통과**하되 **둘 다 아니면 실패**다.
- ⚠ **`DEFERRED.md` 항목으로 올린다**: 꼭짓점 불일치를 줄이는 것(공유 꼭짓점 병합)은
  기하 계층의 문제이고 이 항목의 범위가 아니다. **트리거**: 두 점 배치의 축 미분류 비가
  실사용에서 거슬리면.

#### ⛔ 브라우저 실측이 **정점 스냅이 공허함**을 보였다(#38)

앱 기하(상자 하나 12획)에서 **끝점 후보가 24개**다 — **하나도 안 합쳐진다.**
"같은 꼭짓점"인 두 끝점 사이 거리가 기하 크기 대비 **최소 0.0305 · 중앙 0.0653 · 최대 0.1182**이고
병합 반경 `merge_ratio`는 **0.002**다 — **15~33배 밖**이다.
그래서 **`vertex` 후보가 0개**다: 정점 스냅은 **단위 시험에서만 뜨고 실제 기하에서는 한 번도 안 뜬다.**

- ⚠ **덮는 대상이 0인 기능이다**(#38). 지웠다가 되살리기보다 **표시로 남기고 사실을 적는다** —
  기하가 한 점을 갖게 되면(아래 DEFERRED) 그때부터 뜬다.
- **병합 반경을 넓히지 않는다**: 공유 상수라 하류 원장이 전부 STALE이 되고(D-C4),
  넓히면 **다른 꼭짓점끼리 합쳐지는 위험**이 생긴다(0.0305는 이웃 간격과 같은 자릿수다).
  범위를 넓히지 않는다(A-3).
- 이 값이 **6.3° 어긋남의 출처**다 — 같은 꼭짓점이 획마다 다른 점이기 때문이다.
- ⚠ **원장 밖 관측이다**(#25) — 브라우저 콘솔에서 앱 경로(`staticCandidates`)를 직접 불렀다.
  같은 값을 원장에 남기려면 그 하네스가 **앱의 3D 레이어**를 대상으로 써야 하는데
  `osnap.json`은 **참 3D**를 쓴다(그것이 이 사실을 못 본 이유다).

### 5-R. 오스냅 리뷰어 1회차 — **15건(높음 5). 다섯 중 넷이 내 쪽이 틀렸다**

| # | 지적 | 대응 |
|---|---|---|
| **[1]** | **게이트가 실패할 수 없게 등록됐다** — ③이 구성상 빈 팔을 상대로 하고, 통과선 0.5의 출처가 없다 | **맞다.** 통과선을 **0.19**로 다시 박고 **출처를 적었다**(`snap.json`의 시작점 스냅 배치 조용히 틀림 **474/2503**, 같은 절단·같은 합성 잉크). **실패시키는 관측**도 한 줄로 적었다(100 중 20 이상이 절단 0.2 밖). ⚠ 프레임이 완전히 같지는 않다(#27)고 함께 적었다 |
| **[3]** | **"축 경로로는 0개 = 정의"가 `face_diagonal.json`(19/120)과 어긋나고 실행 카운터가 없다** | **맞고, 실제로 틀렸다.** 축 판정을 **실제로 돌렸다**: `axis_calls` **480** · `axis_assigned` **59(0.1229)**. **0이 아니다** — 대각선의 **12.3%가 축으로 배정된다**(그 원장의 결론대로 그것은 회수가 아니라 **조용히 틀린 배치**다). "정의의 귀결"이라는 주장을 **철회한다**(#32) |
| **[5]** | **꺾인 획이 두 점 배치로 조용히 놓인다** — 축 경로가 걸러 주던 것이 우회된다 | **맞다. 코드를 고쳤다**: 양 끝이 붙어도 `representative(pts).bend > AXIS_TOL.bend_max`면 **놓지 않는다**(2D 대기). 임계는 축 경로가 쓰던 것 그대로다(#17). ⚠ **이 픽스처에서 `too_bent`는 0**이다 — 합성 대각선에 그 종류가 없어 **덮는 대상이 0이고**(#38) 가드의 효력은 **미측정**이다 |
| **[4]** | **6.3°를 "라벨 미분류" 문제로 옮겨 적었다** — 실제로는 놓인 선분의 방향 오차다 | **맞다.** 두 점 배치의 라벨은 **설계상 언제나 미분류가 아니다**(축과 나란하면 라벨을 붙인다) — 그래서 "종종 미분류"도 틀린 서술이었다. `DEFERRED.md`의 트리거를 **"두 점 배치 선분의 축 대비 방향 오차"**로 바꿨다 |
| **[2]** | **종단 확인 단언 셋이 전부 완화 방향이고 하나는 항등이다** | **맞다**(#19). 다만 그 셋은 **계약이 바뀌어서** 바꾼 것이고, 약화를 메우는 것은 **새 검사**여야 한다 → `DEFERRED.md`에 올렸다: **양 끝 스냅이 대상의 실제 3D와 맞는지**(앵커 기록이 아니라 대상 기하와 대조)를 잠그는 팔. ⚠ **이번 항목에서 안 했다** |
| [6][11][12][14] | 원장 안 낡은 수 "5/474" · PITFALLS 커버리지 표 · 정점 0회가 D-L46·README에 없음 · `result`의 보장값 | **전부 고쳤다**(각각 "잡음 0.05 행에서 5/106" · 표를 원장값으로 · D-L46과 README에 "지금 기하에서는 안 뜬다" · `shape_median_GUARANTEE`로 이름을 바꿔 임계가 아님을 표시) |
| [7][8][9][10][13][15] | 구도별 판정 없음 · 표제가 잡음 혼합 · "라이노 기본값 그대로"가 실제와 다름 · 상한 사유 불충분 · 근처점 제거의 대가 미기록 · 1.0 플래그 원인 미기록 | **전부 맞다.** 아래에 **구도별·잡음별 수치를 적고**, "선례에서 가져온 것과 바꾼 것"을 갈라 적는다. **[13]은 안 쟀다** — 첫 판의 오붙음을 기록하지 않았고 재실행으로 복원하지 않았다(`DEFERRED.md`) |

#### 재측정 — `osnap.json@1b6175a4`

| 지표 | 값 |
|---|---|
| 시작점 붙음 / 양 끝 붙음 | **476/480** · **459/480** |
| **축 경로**(같은 대각선) | 판정 **480회** 호출 · 축 배정 **59(0.1229)** — **0이 아니다**(초판 주장 철회) |
| 오붙음(절단 0.2) | **5/459**, 그리고 **전부 잡음 0.05 행**이다(0.005·0.01·0.03 행은 **0/118·0/118·0/117**, 0.05 행은 **5/106**) |
| 구도별 양 끝 붙음 | 0.9896 · **0.7917**(3pt_yaw20_pitch25) · 1 · 1 · 1 — **못 붙은 17 중 16이 그 한 구도**다 |
| 꺾인 획 거절 | **0** — 이 픽스처에 그 종류가 없다(가드의 효력 **미측정**, #38) |

⚠ **실획 자릿수(0.028~0.051)의 동작점은 표제가 아니다**(#12) — 그 대역은 잡음 0.03·0.05 행이고
거기서 양 끝 붙음이 **117/120 · 106/120**, 오붙음이 **0/117 · 5/106**이다.
⚠ **구도 하나(3pt_yaw20_pitch25)가 나쁘다** — 그리고 그 구도는 못 붙은 획이 오붙음 **분모에서도
빠지므로** 오붙음이 좋아 보인다(생존 편의, #11).

#### 선례에서 **가져온 것**과 이 자료구조 때문에 **바꾼 것** (리뷰어 [9])

| 가져온 것 | 바꾼 것(그리고 왜) |
|---|---|
| 대상 목록 · 우선순위 · 종류별 표식 | **정점 = 여러 획이 만나는 끝점** — 라이노의 Vertex는 메쉬 정점인데 **이 기하에는 메쉬가 없다** |
| 오스냅이 직교 모드를 이긴다 | **근처점을 끝점에서만 뺐다** — 라이노의 Near는 **전역 토글**이고 "첫 점은 켜고 둘째 점은 끈다"는 규약은 **선례에 없다**. 시작점 경로의 측정(`snap.json`)이 근처점을 포함한 목록 위에 있어 그쪽을 안 건드린 것이다 |

---

## 2026-08-17 — 아이패드 배포본 시험 뒤 사람 지시 A~K

**착수 시 `PITFALLS.md` 대조**(CLAUDE.md §2 — 읽었다는 증거를 여기 적는다).

### A. 축 스냅 결함 — 걸리는 번호

| # | 이 항목에서 걸리는 자리 |
|---|---|
| **#17** | 새 임계를 만들지 않는다 — 화면 직교 스냅의 각 임계는 `RULE_TOL.screen_axis_deg`(4°)를 **그대로 쓴다**. ⚠ **최소 길이(A-5)는 새 임계다** — 대체할 기존 값이 없어 신설하고 `test/constants.ts`에 등록한다 |
| **#5** | "축 스냅이 되게 했다"는 **설계 보장**이다 — 스냅이 켜지면 그 방향으로 놓이는 것은 정의다. 재는 것은 **카메라 축 오차**와 **조용히 틀린 배치**다 |
| **#7** | 화면에 "왜 안 걸리는지"를 낸다 — 추측하지 말고 상태를 그대로 |
| **#12** | 동작점 하나로 결론내지 않는다 — 스냅 표를 **1·2·3점 전부**에서 테스트로 잠근다 |
| **#32** | 표 테스트가 **공허하지 않은지** 확인한다(덮는 대상 수 = 각 상태에서 실제로 후보가 몇 개 나오는가를 함께 단언한다) |
| **#38** | 자동 검사는 덮는 대상 수를 함께 낸다 |
| **#41** | 중단 조건(L-A 게이트)에 이 항목은 안 걸린다 — 축 스냅 게이트는 이미 실패로 기록됐고(D-L42) 사람이 "그래도 켬"으로 판정했다 |

**해당 없음**: #14(시드) · #16(분모) · #33(훑기) · #40(게이트 도달 가능성 — 이 항목은 새 게이트를 등록하지 않는다)
