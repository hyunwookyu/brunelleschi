# progress.md — 진행 기록

현행 계획서는 [`docs/line_plan.md`](docs/line_plan.md)(코드명 L, 2026-08-15 전환).
각 단계의 **선택 근거와 기각 사유**를 적는다(CLAUDE.md A-3).
반복해서 걸린 것은 [`PITFALLS.md`](PITFALLS.md)에 따로 있다.

---

## L-A 게이트 — **사전 등록** (측정 전에 박는다)

계획서 §9의 L-A 게이트는 "확정되고, 화면이 안 튀고, 연결된 획이 전부 놓인다"로만 적혀
있어 사후에 수치를 정할 여지가 있었다(리뷰어 지적, PITFALLS #26).
**아래는 `lift_gate.json`을 처음 돌리기 전에 정한 값이다.**

### 조건 (동작점을 하나 고르지 않는다, PITFALLS #12)

- **구도 5개**: 3점 ×3(요 35°/피치 15°, 요 20°/피치 25°, 요 50°/피치 8°) · 2점 ×1(피치 0°) ·
  1점 ×1(요 0°·피치 0°). _"구도가 하나뿐"이 S·G 전체에 걸렸던 한계다._
- **끝점 겨냥 오차 5수준**: 0 / 0.005 / 0.01 / **0.03 / 0.05**.
  뒤의 둘이 **유일한 실획 자릿수**(AS-C2의 0.028~0.051)이고 S·G는 그 구간을 한 번도 안 쟀다.
- 잉크 등급 2(precise·medium) · 시드 6. **결과는 오차 수준별로 갈라 낸다.**

### 분모 (PITFALLS #11 — 행마다 다르면 같은 양의 비교가 아니다)

| 지표 | 분자 | 분모 |
|---|---|---|
| 확정률 | 소실점 ≥2 검출 + `recoverCamera.ok`인 장면 | 전체 장면 |
| 오배정률 | 지지 집합에서 다수결 축과 다른 획 | **소실점에 배정된 획** (배정 안 된 획 수를 함께 낸다) |
| 배치율 | 3D 세그먼트가 나온 획 | **가장 큰 연결 성분의 획** (모집단 12도 함께 낸다) |
| 조용히 틀린 배치 | 형태 오차 > **0.2** 상자 대각 | **놓인 획** |
| 가짜 교점률 | 참 3D에서 점을 공유하지 않는 교점 | 채택된 교점 전부 |

### 통과 기준

1. **확정** — 확정률 **≥ 0.90** (끝점 오차 0.01 이하). 소실점 3개 검출률은 함께 내되
   **판정에 쓰지 않는다** — 2개만 잡히는 것은 실패가 아니라 **2점으로 읽고 나중에 승격**하는
   설계다(§6).
2. **배치** — 배치율 **≥ 0.95** (끝점 오차 0.01 이하), 조용히 틀린 배치 **≤ 0.10**.
   _비교 기준: G-A.1 첫 시점 배치율 0.887 / 조용히 틀림 **0.278**, S 0.846 / 0.057._
3. **전환 무변화** — `segGap` 최대 **< 0.5px**.
   ⚠ **이것은 게이트가 아니라 불변식이다.** 각 점이 자기 광선 위에 있으므로 되쏘면 원본으로
   돌아온다 — **설계 보장**이고 selfcheck 자기참조 유형 3이다(PITFALLS #5).
   계획서 §5.3이 "튀면 카메라가 틀린 것"이라 한 것은 **성립하지 않는다**: f 오차는 전역 배율
   편향이라(AS-C4·이론서 16.4) 같은 카메라 재투영에서 상쇄된다.
   → **대체 지표: 교점 잔차.** 참 카메라에서 중앙 잔차 ≤ 0.05이고,
   **f를 ±20% 틀리게 주면 최소 2배로 커져야 한다.** 안 커지면 그 지표도 보장이므로 폐기한다.
4. **미배치가 화면에 남는다** — 놓지 않은 획이 `unplaced`에 이유와 함께 남는다(단위 테스트).

**통과 못 하면 멈추고 보고한다**(유일한 중단 조건).

---

> **2026-08-15 L 전환에서 새로 시작했다.** S·G의 기록(3286줄)은
> `docs/archive/progress_SG.md`에 있다.

---

## L-A.1 — 정리 (문서)

`line_plan.md`를 `docs/line_plan.md`로 편입했다.
`docs/sketch3d_plan.md`·`docs/segment_plan.md`를 `docs/archive/`로 옮기고 머리말에 폐기 사유를 적었다.
`progress`·`DECISIONS`·`DEFERRED`·`assumptions`를 `docs/archive/*_SG.md`로 옮기고
유효 항목만 새 파일에 옮겨 적었다(D-L1).

**옮겨 적은 것**: assumptions 이월 10건(AS-C1~C10) · DECISIONS 이월 6건(D-C1~C6) ·
DEFERRED는 대상이 남아 있는 것만.
**버린 것**: 배치 계층·프리핸드 보존에 딸린 전제와 결정 전부.

## L-A.2 — 소실점 후보 검출 (§5.1)

`web/src/s3d/vpDetect.ts` · `web/test/vp_detect.test.ts`(11).
쌍마다 교점을 씨앗으로 두고 지지를 세어 최다부터 떼어 낸 뒤, 전부 찾고 나서 다시 배정한다.

**계획서 문장 그대로는 작동하지 않았다.** 세 번 고쳤고 셋 다 측정이 시켰다.

| 무엇이 틀렸나 | 관측 | 어떻게 고쳤나 |
|---|---|---|
| "산포 임계는 화면 대각 비율" | 먼 소실점에서 발산한다. 획 길이 대비도 같은 방향 — 잉크 노이즈에서 **참 소실점의 부적합도 0.061이 임계 0.06을 넘어 검출이 통째로 실패** | **각도**로 걸었다(D-L2). 2.5°, 스윕으로 정함 |
| "선 3개가 한 점에서 만나면 확정" | **상자의 꼭짓점이 그 조건을 완벽히 만족한다.** 첫 실행에서 검출된 "소실점" 셋이 전부 꼭짓점이었다 | **도달거리** 조건(D-L3). 소실점은 획 끝에서 획 길이 1배 이상 밖에 있어야 한다 |
| 지지를 개수로 센다 | 지평선 근처 모서리가 두 수평 소실점을 다 지나므로 **엉뚱한 중간점이 지지 6**을 모은다(참 소실점은 4). 소실점이 둘만 잡혔다 | **가중합** `Σ(1−φ/tol)` + 전부 찾은 뒤 **재배정**(D-L4) |

**측정**(구도 하나 · 상자 하나 · 끝점 오차 0~0.01 × 시드 10 = 40장면):

| `support_deg` | 3점 검출 | 2점만 | 오배정 | f 오차 중앙 | p90 |
|---|---|---|---|---|---|
| 2.0 | 34/40 | 6 | 12/399 | 0.127 | 0.298 |
| **2.5** | **37/40** | 3 | **13/423** | 0.105 | 0.267 |
| 3.0 | 35/40 | 5 | 16/434 | 0.090 | 0.267 |
| 4.0 | 38/40 | 2 | 16/456 | 0.090 | 0.288 |

**최적화 대상은 검출 개수가 아니다.** 소실점이 둘만 잡히는 것은 실패가 아니라
**2점 투시로 읽고 나중에 승격하는 것**이 설계다(§6). 그래서 오배정을 본다 —
1.5~6°에서 3.0~3.5%로 거의 평탄하고 2.5°가 둘 다 낫다.

~~**f 오차 ~9%는 알고리즘의 몫이 아니다**(D-L5). 참 축 라벨을 주고 최소제곱 가중만 바꿔도
중앙값이 0.094에서 안 움직인다.~~ **철회했다** — 구도 하나에서 재서 획 길이가 비슷했기 때문이고,
구도를 다섯으로 늘리니 중앙값이 0.0921 → 0.0793으로 움직인다(D-L5).

~~**기각**: 최소제곱 길이 가중(p90만 12% 개선, 중앙 불변 — A-3).~~ **뒤집어 채택했다**(D-L5).
검출 단계의 직교 조건(카메라가 f²<0으로 이미 검증한다. 두 군데서 판정하면 어긋날 때 원인을 못 가른다).

**정정**: 아래 L-A.4에서 이 표를 다시 냈다. **이 절의 수치는 구도 하나(요 35°·피치 15°)에서
나온 것이고 구도를 다섯으로 늘리면 결론이 달라진다.** 원장은 `vp_detect.json@46c028d1`이며
그쪽 값을 쓴다. 이 표는 지운다 — 그리고 **왜 틀렸는지**를 L-A.4에 적었다.

⚠ **원장 STALE이 생겼다**(그때 21건이라 적었으나 **20건**이었다 — 아래 L-A.4의 정정 절). `VP_TOL`을 `constants.ts`에 등록(D-C4)해서 상수 해시가
`1671e540` → `6c7f9be8`로 바뀌었고, S·G 시절 산출물 전부가 STALE로 표시된다.
차이는 `VP_TOL: None → …` 하나뿐이므로 **그 측정들이 실제로 낡은 것은 아니지만**,
검사를 약화시키지 않는다(경고 18). 해당 산출물은 폐기 대상 코드가 낸 것이므로
**L-A.5에서 `stage0/out/archive_SG/`로 옮긴다**(`archive_W/`·`archive_pre_W/` 선례).

---

## L-A.4 — 게이트 측정과 **판정: 실패**

원장: `stage0/out/vp_detect.json@46c028d1` · `stage0/out/lift_gate.json@46c028d1`.
하네스 `web/test/lift_gate.test.ts`. 조건·분모·통과 기준은 위 사전 등록 절.

### 사전 등록 대비 판정

| # | 기준 | 관측 | |
|---|---|---|---|
| 1 | 확정률 ≥ 0.90 (끝점 오차 ≤0.01) | **기준 구도 36/36 = 1.00** · 5구도 합산 0.90 / 0.917 / 0.85 | ◐ |
| 2 | 배치율 ≥ 0.95 (분모=최대 성분) | **1.000 — 그런데 이 분모는 판정을 공짜로 통과시킨다**(PITFALLS #11). 모집단 12 기준으로는 0.62~0.83 | ✗ |
| 3 | 조용히 틀린 배치 ≤ 0.10 | **참 카메라·기준 구도에서만 34/430 = 0.079.** 검출 카메라에서 **230/359 = 0.641** | ✗ |
| 4 | 미배치가 화면에 남는다 | `unplaced`에 이유와 함께 남는다(단위 테스트) | ✓ |
| 5 | 전환 무변화 | `segGap` 최대 **0.0** — 그리고 **f를 ±20% 틀리게 줘도 0.0이다**(사전 등록한 반례 검사) | 보장 확인 |

**게이트 실패.** 계획서가 정한 유일한 중단 조건에 걸렸다.

### 무엇이 성립하고 무엇이 안 되는가 — 원인이 하나로 좁혀졌다

기준 동작점(3점 요35°·피치15°, 끝점 오차 ≤0.01, 36장면 432획):

| 카메라 | 배치(of 12) | 조용히 틀림 | 형태 오차 중앙 |
|---|---|---|---|
| **참 카메라** | 0.9954 | **34/430 = 0.079** | **0.026** |
| 검출 카메라 | 0.831 | 230/359 = 0.641 | 0.271 |

**올리기(§5.2)는 맞다.** 참 카메라를 주면 조용히 틀린 배치가 **0.079**로, S의 0.057과 같은
대역이면서 **배치율은 0.846 → 0.995**다(G는 0.887 / 0.278). 정확한 투영에서는 오차가 1e-6이고
동차계의 최소 고유값이 −3.6e-17이다 — **앵커도 씨앗도 없이 참 상자가 배율까지 복원된다.**

**무너뜨리는 것은 카메라다.** 그리고 f가 아니라 **축 방향**이다:

- 축 방향 오차(검출 축 ↔ 가장 가까운 참 축, 번호 규약 무관) 중앙 **4.18°** · p90 **11.75°**
- f 상대오차 중앙 0.0762
- 그런데 **f만 10% 틀리게 주면 형태 오차 중앙이 0.098 → 0.115뿐이다**(`f_bias`).
  즉 **f 크기가 지배항이 아니다** — 소실점 위치 오차가 3D 축 프레임을 4° 틀어 놓는 것이다.
  `axisDirection = unit([vp − P, f])`이므로 먼 소실점의 100px 오차가 곧 2~3°다.

전 구도·전 잡음(5구도 × 5잡음 × 2등급 × 6시드):

| 끝점 오차 | 참 카메라 조용히 틀림 | 검출 카메라 | 검출 배치(of 12) |
|---|---|---|---|
| 0 | 183/717 = 0.255 | 392/571 = 0.687 | 0.793 |
| 0.005 | 198/716 = 0.277 | 442/559 = 0.791 | 0.776 |
| 0.01 | 226/702 = 0.322 | 332/449 = 0.739 | 0.624 |
| **0.03**(실획 자릿수) | 301/584 = 0.515 | 220/240 = 0.917 | 0.333 |
| **0.05**(실획 자릿수) | 329/520 = 0.633 | 146/154 = 0.948 | 0.214 |

**실획 자릿수(AS-C2의 0.028~0.051)에서는 참 카메라로도 절반이 틀린다.**
S·G가 그 구간을 한 번도 재지 않았으므로 이것은 **L이 새로 드러낸 값이지 L이 만든 문제가 아니다.**

### 시도해 기각된 것 셋

| 시도 | 결과 | 판정 |
|---|---|---|
| **카메라 일괄 적합**(회전 3 + f 1을 획 전부로, `cameraFit.ts`) | 기준 구도 f 0.0762 → **0.0827(악화)**. 다른 구도는 0.114 → 0.383, 0.186 → 0.394, 1점은 상한 2.0까지 달아났다 | **기각**(D-L6). 코드는 남기고 배선하지 않는다 |
| **교점 잔차를 카메라 검증 지표로** | f 0.8~1.2배에서 중앙 0.0285 / 0.0296 / 0.0286 / 0.0275 / **0.0262** — **안 움직인다.** 같은 구간에서 형태 오차는 0.185 → 0.098 → 0.140으로 움직인다 | **폐기**. 입사 관계는 사영 불변이므로 **원리적으로** f를 못 잡는다 |
| **전환 픽셀 차이**(계획서 §5.3) | `segGap` 최대가 모든 f 배율에서 **0.0** | **보장이지 측정이 아니다**(PITFALLS #5) |

### 채택한 것 셋 (측정이 시켰다)

| 채택 | 근거 |
|---|---|
| **도달거리 조건** `min_reach = 1.0`(D-L8) | 스윕에서 양방향 최적: 0으로 내리면 꼭짓점 오검출로 오배정 245/852, 4로 올리면 **1점 구도의 참 소실점이 죽는다**(3점 검출 0/24). 1.0에서 먼 구도 3점 검출 50/96 · 1점 오배정 8/180 |
| **국소화 가능성** `min_spread_mult = 1.5`(D-L7) | 참 소실점이 유한인 다발 **199/221 = 0.90**을 살리고 무한원인 다발 **33/67 = 0.49**를 거른다. ⚠ **깨끗한 분리가 아니다** — 산포 분포가 겹친다(유한 최소 0.43° · 무한 최대 17.1°) |
| **길이² 가중 최소제곱**(D-L5) | f 상대오차 중앙 균등 0.0921 → `len` 0.0860 → **`len²` 0.0793**. 초판이 "차이 없다"로 읽은 것은 구도 하나에서 재서 획 길이가 비슷했기 때문이다(길이비 2.03~4.44) |

### 다음 세션이 고를 수 있는 것 — 사람이 정한다

1. **소실점 정밀도를 올린다(미시도).** 지배항이 축 방향 4°이고 그것은 소실점 위치에서 온다.
   상자 하나(축당 4획)로는 지렛대가 짧다. 획이 더 쌓이면 나아지는지가 미측정이다 —
   **획 수를 늘려 축 방향 오차 곡선을 재는 것**이 가장 싼 다음 측정이다.
2. **카메라를 사용자가 확정한다.** 옛 S 접근의 UI(축1/축2/축3 소실점 찍기)가 **그대로 살아 있다.**
   그러면 §5.1·§5.2가 "초기 2D → 카메라 확정"이라는 고유 항목 하나를 잃고 SketchUp 선 도구만 남는다.
   대신 형태는 참 카메라 대역(0.079)으로 간다.
3. **L-B로 넘어가고 초기 확정의 부정확을 받아들인다.** 실시간 스냅은 **시작점이 확정된 뒤**의
   이야기이므로 초기 구조가 조금 틀어져도 그 뒤 획들은 그 구조에 정확히 붙는다.
   대가는 첫 구조가 돌려 보면 기울어 있는 것이고, L-C 승격이 그것을 다시 푼다.
4. **실획 표본을 받고 다시 잰다.** 지금 판정은 Quick,Draw 유도 합성 잉크 위에 있고
   AS-C10은 그 모델이 **양방향으로 틀렸다**고 이미 적었다. 대상 사용자는 숙련자다.

### 원장 STALE — 정정

L-A.2에 "STALE 21건"이라 적었는데 **당시에도 20건**이었고(`selfcheck.json` `n_stale`),
전체 테스트를 다시 돌린 지금은 **1건**이다(`e2e.json` — Playwright가 내는 것이라 이번에 안 돌았다).
나머지는 각자의 테스트가 재실행되며 현재 스냅샷으로 다시 써졌다.
→ **`archive_SG/`로 옮길 필요가 없다.** 리뷰어가 우려한 `real_ink.json`·`bend_share.json`·
`one_stroke_one_axis.json`은 그대로 `stage0/out/`에 있고 자기 테스트가 살아 있다.
L-A.5에서 옮길 대상은 **폐기하는 테스트가 내던 것뿐**이다.

---

## L-A.5d + L-A.6 — 축 방향 오차 곡선과 획 수 곡선

원장: `stage0/out/camera_gate.json@46c028d1`. 하네스 `web/test/camera_gate.test.ts`.
**착수 시 `PITFALLS.md`를 읽었다.** 걸리는 번호: #2(대리값) · #8(꼬리) · #11(분모) ·
#12(동작점) · #13(절단값) · #14(분자/분모) · #25(원장) · #26(사전 등록).

### ① 임계는 **0.5°와 1° 사이**다 (§5.5 b, 그리고 §5.4의 대리 측정)

기준 구도(3점 요35°·피치15°, 끝점 오차 ≤0.01, 431획). **목표 각도**를 주고 축을 독립으로
돌린 뒤 `recoverCamera`로 다시 푼 값이므로 **실측 축 오차는 목표의 약 2배**로 나온다
(수심이 f를 다시 내면서 축이 더 움직인다). 판정은 **실측 쪽**으로 읽는다.

| 준 각도 | 실측 축 오차 | 조용히 틀림 (>0.2) | (>0.1) | (>0.5) | 형태 오차 중앙 | f 오차 |
|---|---|---|---|---|---|---|
| 0 | 0 | **29/431 = 0.067** | 71/431 | 3/431 | 0.030 | 0 |
| 0.25° | 0.48° | **30/431 = 0.070** | 90/431 | 3/431 | 0.061 | 0.008 |
| **0.5°** | **0.96°** | **36/431 = 0.084** | 236/431 | 2/431 | 0.109 | 0.016 |
| 1° | 1.92° | 218/431 = 0.506 | 372/431 | 2/431 | 0.205 | 0.033 |
| 2° | 3.81° | 366/431 = 0.849 | 427/431 | 65/431 | 0.399 | 0.075 |
| 4° | 7.96° | 409/431 = 0.949 | 431/431 | 356/431 | 0.770 | 0.139 |

**참 카메라 대역(0.079)에 드는 것은 실측 축 오차 ≈ 1° 이하다.** 1.9°에서 0.506으로 무너진다.
_절단값을 하나 고르지 않았다(#13) — 0.1 절단으로 보면 0.5°에서 이미 0.55로 나쁘다.
즉 **"쓸 만하다"의 기준이 0.2 절단이라는 것이 판정에 들어 있다.**_

**화면 픽셀로 환산하면** (f=1000, 축 오차 1° 기준): 소실점이 화면 밖 800px이면 **29px**,
1400px이면 **52px**, 2200px이면 **102px** 안으로 맞춰야 한다. 소실점이 멀수록 관대해진다.
**드래그로 도달 가능한 자릿수다** — 다만 화면 밖 소실점은 직접 못 끌므로 가이드 선이 필요하다(§5.4).

⚠ **이것은 대리값이다**(PITFALLS #2). 재는 것은 "축 오차가 θ면 형태가 얼마나 틀리는가"이고
**"사람이 θ에 도달한다"는 측정하지 않았다**(AS-L9, 표본 0).

### ② 획을 늘려도 자동 검출은 임계에 못 간다 — **AS-L10 반증**

| 등분 k | 축당 획 | 총 획 | 검출 축 오차 중앙 / p90 | 검출 배치(of all) | 검출 조용히 틀림 |
|---|---|---|---|---|---|
| 1 | 4 | 12 | **7.40° / 35.0°** | 0.569 | 1666/2049 = 0.813 |
| 2 | 18 | 54 | **10.04° / 32.7°** | 0.373 | 4919/6035 = 0.815 |
| 3 | 48 | 144 | **10.09° / 33.1°** | 0.263 | 1816/2045 = 0.888 |
| 4 | 100 | 300 | **4.53° / 42.7°** | 0.128 | 539/692 = 0.779 |

**단조 감소하지 않는다.** 7.4 → 10.0 → 10.1 → 4.5이고 **임계 1°에서 4배 이상 떨어져 있다.**
배치율은 오히려 급락한다(0.57 → 0.13) — 내부 격자선이 짧아 `min_len_ratio`에 걸리고
미분류가 늘기 때문이다. 같은 조건의 **참 카메라 대조**는 반대로 간다(배치 0.88 → 0.997).

⚠ **조건이 k에 따라 다르다**(#11·#12). k≥3에서 구도 5→3, 시드 6→3/1, 잡음 5→3 수준으로
줄였고 `seed_pairs_max`가 씨앗 쌍을 건너뛴다. **줄인 사실을 원장에 적었다.**
그래도 4배 이상의 격차는 조건 차이로 설명되지 않는다.

**→ 자동 확정 경로를 두지 않는다**(§5.5 c의 조건이 성립하지 않는다).
검출은 **초안**으로만 쓰고 사용자 조정을 **항상** 요구한다.

### 기각·채택

| | |
|---|---|
| **기각** | 자동 확정 제안 경로(§5.5 c). 획 수로 임계에 도달하지 못한다 |
| **채택** | `seed_pairs_max = 6000` — 획 300개에서 검출이 `O(n³)`으로 멈췄다. 결정론적 보폭으로 씨앗 쌍을 건너뛴다. 씨앗은 출발점일 뿐이고 값은 최소제곱이 준다 |

## L-A.5abc — 소실점 초안·조정 UI (§5.4)

`web/src/s3d/vpDraft.ts` · `web/test/vp_draft.test.ts`(8) · `ConstraintAccumulator.setLines` ·
`InkCanvas`의 끌기 모드 · `main.ts` 배선(`소실점 추정` 버튼 · `소실점 조정` 도구 · 확정=`카메라 잠금`).
**착수 시 `PITFALLS.md`를 읽었다.** 걸리는 번호: #17(측정 경로/앱 경로 분리) · #19 · #21(dpr) · #22(새 탭).

**점이 아니라 선이다.** 소실점은 대개 화면 밖이라 끌 수 없다 —
fSpy·SketchUp Match Photo의 선례를 따라(A-3) **축마다 가이드 선 두 개**를 두고 끝점 넷을 끈다.
누산기가 이미 `vp_line`을 받으므로 새 기전이 아니고, **초기값을 검출이 채우는 것**이 새로운 부분이다.

**dpr 규약은 `canvasFrame` 단일 출처를 탄다**(D-C3) — 끌기 좌표를 바깥에서 `clientX`로 읽지 않고
`InkCanvas`에 `dragMode`/`onDrag`를 더해 `local(e)`을 쓰게 했다. 그 버그는 dpr 1에서 안 보인다.

### 브라우저 확인에서 잡은 것 둘

**① 캔버스 크기가 또 굳었다 — AS-C7 세 번째.** `panel.acc.imgSize`가 `[1, 24]`인 채로
캔버스는 490×672였다. `ResizeObserver`는 이 환경에서 초기 콜백을 안 주고(S-4에서 확인),
`requestAnimationFrame`은 **숨은 탭에서 완전히 멈춘다**(구 V-n: 600ms에 0프레임).
브라우저 창이 뒤에서 열리면 둘 다 발화하지 않는다. 그 크기가 화각·무한원 판정의 기준이라
**소실점이 하나로 읽혔다.**

→ 관찰자를 하나 더 거는 대신 **쓰는 자리에서 스스로 고친다**: `refresh()`가 매번
`sizeStale()`을 보고 어긋나 있으면 `fit()`한다. 어떤 관찰자가 발화하든 안 하든 회복된다.
`setTimeout(0)`과 `visibilitychange`도 함께 걸었다(타이머는 숨은 탭에서도 돈다).
고친 뒤 `[490, 672]`로 정상.

**② 내 브라우저 픽스처가 틀렸다** — "소실점 쪽으로 t만큼" 방식으로 상자를 만들었더니
`AB − B = 0.86(A − O)`가 되어 **화면에서 평행한 아핀 상자**가 나왔다(수렴이 0, 산포 0.0°).
검출이 그것을 무한원으로 판정한 것은 **맞는 동작**이었다.
→ 측정 하네스와 **같은 픽스처**(`test/scene3d.ts`)를 브라우저에서 그대로 import해 쓴다.
그러면 앱 경로와 측정 경로가 같은 입력을 본다(PITFALLS #17).

### 확인된 동작 (브라우저, 490×672, dpr 1)

- 참 소실점 `[1280,148] [−262,148] [245,2948]`의 상자 12획 →
  검출 `[1178,168] [−290,135]` + 수직 다발은 무한원(산포 3.3° < 3.75°) → **2점 투시로 읽는다**
  (설계대로다 — 나중에 승격한다)
- `소실점 추정` → 축 2개 확정, 가이드 4개
- `소실점 조정`에서 핸들을 24px 끌면 소실점이 실제로 움직인다

⚠ **민감하다.** 가이드 둘이 거의 나란해지면 교점이 멀리 날아간다 — 24px 끌었더니
소실점이 `−360` → `−37639`로 갔다. **그래서 실시간 피드백이 필수다**(사용자가 그리드를 본다).
가이드를 길게 잡는 것(`min_guide_ratio` 0.12)이 완화책이고, 값은 실사용에서 재정한다.

### `liftAll`을 앱에 배선했다 — 그리고 하나가 더 빠져 있었다

`main.ts`의 첫 시점 배치를 `settle`/`reprojectAll`에서 **`liftFirstView`**(→ `liftAll`)로 바꿨다.
`pts3d = [a, b]` 두 점으로 쓰므로 튜브 렌더·저장·내보내기가 그대로 동작한다.

**빠진 것: 확정 순간의 축 재분류.** 배선 직후 브라우저에서 **배치가 0**이었다 —
L에서는 **획이 카메라보다 먼저** 그려지므로 그릴 때의 축 판정이 전부 `free`이고,
`liftAll`은 미분류를 놓지 않는다(D-C2). §5.2의 첫 단계("확정 순간 → 모든 획을 축별로 분류")를
글자 그대로 빠뜨린 것이다. `liftFirstView`가 확정 카메라로 다시 분류하도록 고쳤고,
**사용자가 직접 고른 축(`userAxis`)은 덮어쓰지 않는다.**

고친 뒤 브라우저에서 **2점 확정 12/12 · 3점 확정 12/12 배치**.
전 경로(그리기 → 추정 → 조정 → 확정 → 3D)가 앱에서 돈다.

### 남은 것 — 다음 세션

- **§5.4 게이트를 실사용에서 확인**: 사람이 가이드를 끌어 참 카메라 대역(조용히 틀림 0.079)에
  실제로 도달하는가. 대리 측정은 "축 오차 ≈1° 이하면 든다"까지만 말한다(AS-L9).
- 옛 배치 계층(`settle`·`placeInto`·`appPlace.FIRST_VIEW_OPTS`)이 **더는 첫 시점에서 안 불린다**.
  돌린 시점 경로(`VIEW_OPTS`)는 아직 쓴다 — L-B의 뷰포트 통합에서 함께 정리한다.

---

## L-A.7 + 리뷰어 대응 (L-A.5/L-A.6에 18건, 높음 7)

**착수 시 `PITFALLS.md`를 읽었다.** L-A.7에 걸리는 번호:
**#24(단위 변경은 아무것도 고치지 않는다 — D-L2에서 걸린 자리)** · #3(지표가 정의의 귀결) ·
#6(대조군) · #8(꼬리) · #13(절단값) · #25(원장) · #26(사전 등록).

### ⚠ 임계를 **≈1° → ≈0.5°**로 정정한다 (리뷰어 [1][2][3])

세 지적이 같은 방향으로 작용했고 전부 맞다.

**[1] 대역 기준값이 다른 하네스의 값이었다.** `lift_gate`의 0.0791은 **검출 오배정이 섞인**
값이고, `camera_gate`는 **참 축 라벨**을 준다. 같은 하네스 안의 무오차 행은 **29/431 = 0.0673**이다.
그것을 대역으로 쓰면:

| 준 각도 | **실측 축 오차** | 조용히 틀림(0.2) | 대역 대비 | 형태 오차 중앙 | **등록 기준(0.026) 대비** |
|---|---|---|---|---|---|
| 0 | 0 | 29/431 = 0.0673 | 1.00배 | 0.0296 | 1.00배 |
| 0.25° | **0.48°** | 30/431 = 0.0696 | **1.03배** | 0.0613 | 2.07배 |
| 0.5° | **0.96°** | 36/431 = 0.0835 | **1.24배** | 0.1085 | 3.67배 |
| 1° | 1.92° | 218/431 = 0.5058 | 7.52배 | 0.2050 | 6.93배 |
| 2° | 3.81° | 366/431 = 0.8492 | 12.6배 | 0.3985 | 13.5배 |

**[3] 판정 지표를 사유 없이 바꿨다.** §5.5가 사전 등록한 기준은 **형태 오차 0.026**인데
판정은 조용히 틀림 0.079로 했다. 등록 기준으로 읽으면 **실측 0.48°에서 이미 2.07배**다.
→ 등록 기준을 살린다. **두 지표가 같은 곳을 가리키지 않으므로 둘 다 적는다**:
- 조용히 틀림(0.2 절단) 기준: 실측 **0.96°**까지 대역의 1.24배 — 낭떠러지는 1.9°
- 형태 오차 중앙 기준(**등록된 것**): 실측 **0.48°**에서 이미 2.07배

**→ 임계는 실측 축 오차 ≈0.5°다.** 초판의 "≈1°"는 대역을 다른 하네스에서 가져오고
등록 지표를 바꿔서 나온 값이었다.

**[2] 화면 픽셀 환산이 준 각도 단위였다.** 29/52/102px는 **준 1°**(실측 1.9°, 이미 무너지는 곳)다.
실측 기준으로 고치면 — 소실점이 화면 밖 800/1400/2200px일 때
**실측 1.0° → 14/26/51px, 실측 0.5° → 7/13/26px.**

### [5] 핸들 예산 — **소실점 여유가 아니라 핸들 여유를 재야 했다**

리뷰어가 정확히 짚었다: 소실점 좌표의 여유(px)는 **핸들의 여유**가 아니다.
그래서 직접 쟀다(`vp_homog.json@46c028d1`의 `by_guide_length`).
**축 오차 0.5°를 다 쓰기까지 핸들이 움직여도 되는 px**(최악 방향):

| 가이드 길이 | 각차 3° | 5° | 10° | 20° |
|---|---|---|---|---|
| 100px | 0.20 | 0.28 | 0.46 | 0.72 |
| **140px**(초판 기본) | **0.29** | **0.40** | **0.65** | **1.01** |
| 300px | 0.63 | 0.88 | 1.40 | 2.16 |
| **525px**(고친 기본) | ~1.1 | ~1.5 | ~2.5 | ~3.8 |
| 600px | 1.28 | 1.76 | 2.82 | 4.33 |

**초판 `min_guide_ratio` 0.12(=140px)는 서브픽셀 예산이었다** — 사람이 맞출 수 없다.
0.45(≈525px)로 올렸다. **그래도 1~4px이고 넉넉하지 않다.**
지배 인자는 **핸들 간격**이다(소실점이 두 핸들을 잇는 직선으로 정해지므로 각도 정밀도가 간격에 비례).

**이것이 §5.4의 실제 한계이고, AS-L9의 답이 아직 아니다** — 사람이 1~4px 안에 놓는지는 미측정이다.

**그리고 캔버스가 또 하나의 한계다.** 0.45로 올린 뒤 브라우저(490×672, 대각 832)에서 실제
가이드 길이는 **304~358px**이었다 — 요구한 374px이 **캔버스 여백에 잘린다**.
즉 **작은 캔버스에서는 핸들 예산을 늘릴 방법이 없다.** L-B 뷰포트 통합에서 다시 본다.

### [6] §5.4의 판정 수단이 §5.3이 폐기한 것과 같은 유형인가 — **아니다, 다만 부분적이다**

리뷰어의 물음이 옳다. 갈라 적는다.
- §5.3·§6.2가 폐기된 것은 **올린 3D를 되쏘아 검사하는 것**이 항등이기 때문이다(출력의 자기검사).
- §5.4에서 사용자가 보는 것은 **소실점 위치를 정하는 입력**이고, 3점에서는 소실점 셋이
  카메라를 **완전히** 정한다(수심 6.3, 자유도 0). 그러므로 입력을 눈으로 맞추는 것은 항등이 아니다.
- **그러나 사용자는 증폭을 볼 수 없다.** 수심이 f를 내면서 축이 더 움직여 실측 오차가
  준 각도의 약 2배가 된다(위 표). 눈으로 맞춘 정밀도가 그대로 축 정밀도가 아니다.
- 그리고 §5.3이 적은 대로 **카메라 오류는 돌려 봐야 보인다** — §5.4의 판정은 돌리기 전이다.

→ §5.4는 "입력을 맞추는 수단"이지 "출력을 검증하는 수단"이 아니다. 계획서를 그렇게 고쳤다.

### [7] §6.3 격상 논거가 틀렸다 — **철회한다**

"승격은 다른 카메라라 화면이 튄다"고 적었는데, §6.1이 **새 카메라로 처음부터 다시 올린다**이므로
각 점은 다시 **새 카메라의 자기 광선 위**에 놓인다. **첫 시점 화면은 또 항등이다** —
§5.3이 폐기된 것과 같은 이유다. 움직이는 것은 3D와 그리드·미배치 집합이지 이미 그린 획이 아니다.
→ 계획서 §6.3에서 "화면이 튄다"를 뺐다. **되돌리기는 여전히 필요하지만 근거가 다르다**:
사용자가 **돌려 보고** 판단한다.

### [4] 새 게이트에 측정 대상이 없었다

게이트를 문자 그대로 읽으면 `deg_0` 행(참 카메라를 그대로 준 행)이 곧 대역이라 **자기 항등**이다.
그리고 0.079/0.0673은 **구도 하나**의 값이고, 5구도 합산 참 카메라 값은
**1197/3185 = 0.376 · 배치 0.885**로 5배 다르다.
→ 게이트를 다시 등록한다(아래 "L-A.5 게이트 재등록").

### 나머지 지적 대응

| # | 지적 | 대응 |
|---|---|---|
| [8] | 획 수 곡선의 비단조가 표본 차이일 수 있다 | **비단조 주장을 뺀다.** n과 시드·구도를 표에 넣었다(k3은 n=93·시드3·구도3, k4는 **n=18·시드1**). 남는 주장은 "임계에서 4배 이상 멀다"뿐이다 |
| [9] | 배치율 급락의 원인 귀속이 틀렸다 | 맞다. `min_len`에 걸리는 것은 k4에서 8%(276/300)뿐이고, `camera_ok`가 258/300 → **6/18**이다. **큰 몫은 카메라 실패다**(PITFALLS #11) |
| [10] | "획을 늘렸다"의 조작이 등록과 다르다 | 맞다. 늘린 것은 **1/k 길이의 내부 격자선**이라 지렛대가 오히려 짧아진다. AS-L10의 ❌에 **"내부 격자선을 늘린 경우"** 조건을 붙였다. 결론(자동 확정 없음)은 안 바뀐다 |
| [11] | 오라클 대조를 배치율만 인용했다 | 오라클 조용히 틀림은 k1 0.376 → k2 0.478 → k3 0.413 → k4 0.406으로 **전부 k1보다 나쁘다**. 함께 적는다 |
| [12] | "이미 놓인 기하가 따라 움직인다"가 첫 확정에서는 성립 안 함 | 맞다. 첫 확정 전에는 놓인 기하가 없고 **그리드뿐**이다. 계획서를 고쳤다 |
| [13] | 브라우저 보고가 배치 12/12뿐 | 맞다 — 그 분모는 L-A.4가 이미 "공짜 통과"라 적은 것이다. 형태 오차를 안 냈다. **DEFERRED로 올린다**(브라우저에서 참값 대조를 하려면 하네스를 앱에 넣어야 한다) |
| [14] | 해시 `@46c028d1`가 디스크에 없는 실행 | 13곳을 `@46c028d1`으로 고쳤다(`grep`으로 확인) |
| [15] | AS-C7이 "두 번"으로 남아 있다 | 세 번째와 대응(자가 치유)으로 갱신했다. PITFALLS #22도 |
| [16] | 자가 치유가 재발을 관측 불가로 만든다 | 맞다(PITFALLS #7·#19). `sizeStale()` 발동 횟수를 세어 진단에 낸다 |
| [17] | 10.04/10.09를 두 점으로 제시 | 유효 자릿수 2로 줄이고 n을 병기했다 |
| [18] | D 번호 교차 참조 어긋남 · 철회된 문장 잔존 | 고쳤다 |

### L-A.5 게이트 재등록 (측정 전에 박는다)

**판정 대상**: 사람이 가이드를 끌어 도달하는 **실측 축 방향 오차**.
**기준값**: 같은 하네스(`camera_gate`)의 무오차 행 — 조용히 틀림 **0.0673** · 형태 오차 중앙 **0.0296**.
**통과**: 실측 축 오차가 **0.5° 이하**여서 두 지표가 각각 **1.1배 · 2.1배 이내**.
**구도**: 기준 구도 하나가 아니라 **5구도 전부**를 낸다. 기준 구도 값만으로 통과를 주장하지 않는다.
**측정 수단**: Playwright로 핸들을 끌어 실측 축 오차를 재는 종단 시나리오(L-D)
— 자동화가 어려우면 **실사용 대기**로 DEFERRED에 남긴다.

### L-A.7 — 동차(각도) 파라미터화

`vpHomog.ts` · `vp_homog.test.ts`(7) · `vp_homog_measure.test.ts` → `vp_homog.json`.

**무엇이 고쳐지고 무엇이 안 고쳐지는지 먼저 갈랐다**(PITFALLS #24 — D-L2에서 걸린 자리).

| | |
|---|---|
| **고쳐진다** | 표현의 발산. `v = ℓ₁ × ℓ₂`는 나눗셈이 없고 나란하면 `v₂ = 0`(무한원, 이론서 2.2). 각차 60°→0.5°에서 **위치는 0.64px → 8115px(12,600배)** 튀는데 **동차 각도는 0.031° → 0.41°(13배)**로 유계다 |
| **안 고쳐진다** | 카메라의 발산. 같은 구간에서 **f 상대 변화 0.0043 → 0.113(26배)**, **축 방향 0.115° → 1.10°/px**. 재매개화는 같은 기하를 다른 좌표로 적은 것뿐이다 |

⚠ **각도가 유계인 것은 정의의 귀결이다**(PITFALLS #3). 그것을 성과로 읽지 않는다.
동차 표현이 실제로 주는 것은 **값이 유한하게 유지되어 스냅을 연속적으로 걸 수 있다**는 것뿐이다.

**무한원 스냅**(§5.4 c): 각차 < **3.0°**면 무한원으로 보고 그 축을 비운다 —
소실점 개수가 줄어 1·2점 분기로 떨어진다(이론서 5.3). 각차 3°에서 0.39°/px이고
0.5° 예산의 78%라 그 아래는 조작이 불가능하다. 화면에 사유와 각차를 함께 알린다.

**반례 하나를 코드에서 잡았다**: 처음에 "각차가 0에 가까우면 퇴화"로 적었는데 틀렸다 —
서로 다른 두 평행선의 외적은 0이 아니고 그 점이 **정상적인 무한원 소실점**이다.
각차로 퇴화를 판정하면 무한원을 퇴화로 버린다. 퇴화는 **외적 크기**로 판정한다.

---

## 계획 개정 2 편입 (2026-08-15, 사람 지시)

`line_plan_r2.md`(저장소 루트, 본체가 만들지 않은 문서)를 `docs/line_plan.md`로 삼고
이전 판을 `docs/archive/line_plan_r1.md`로 보존했다. HANDOFF가 "사람이 지시하기 전에는
현행이 아니다"로 남겨 뒀던 건이고 이번 세션 첫 지시가 그 지시다.

**개정 넷**: ① 제품 정의 — 투시도와 입체가 **동등하고 동시에** 보인다. 3D는 부가 기능이 아니고
초보자용 가드레일을 두지 않는다(§1). ② **미배치는 실패가 아니라 대기 상태**이고
승격이 **연쇄**한다(§9.1). ③ **뷰 시스템** — 뷰가 2D 획을 소유하고, 승격되면 소속이 없어진다(§9.2).
④ **UI 재작성**을 §10으로 등록 — `web/src/ui/`만 교체하고 `canvasFrame`·잉크 캡처·씬/워커·
`lift.ts`·`vpDetect.ts`·카메라 수학·저장/내보내기는 그대로 쓴다.

**⚠ 원문을 그대로 넣지 않은 자리가 셋이다** — 개정 2는 L-A.7 **이전에** 쓰였고
그 사이에 정정된 수치를 담고 있었다. 원문을 그대로 두면 다음 세션이 정정 전 값을 다시 인용한다
(PITFALLS #1 — "문서의 수치가 원장보다 뒤처진다").

| 자리 | 원문 | 고친 값 | 근거 |
|---|---|---|---|
| §5.2 임계 | 축 오차 **약 1°**, 대역 **0.079** | **0.5°**, 대역 **0.0673 / 0.0296 둘 다** | 0.079는 `lift_gate`에서 온 **오배정 섞인** 값(#27). 지표를 하나만 쓰지 않는다(#28) |
| §5.2 여유 | 화면 환산 **29~102px** | **핸들 예산 0.29~4px** | 소실점 좌표 여유는 **사용자가 만지는 양이 아니다**(#29) |
| §12 위험 | "축 오차 1° 안" · "드래그 범위" | 0.5° · 핸들 예산 | 같음 |

§11의 L-A.7도 **완료로 표시하고 실제 결과로 바꿨다** — 등록된 항목은 "각도 파라미터화"였는데
한 것은 **동차 파라미터화**이고, 등록된 기대("발산이 없어진다")는 **절반만 맞았다**
(표현은 유계, **카메라는 여전히 발산**). 계획서에 기대만 남기면 반증이 지워진다.

CLAUDE.md의 제품 정의와 A-3의 미배치 문장도 개정 2에 맞췄다.
**개정 1은 폐기가 아니라 대체다** — 개정 2가 §2~§4·§8을 "개정 1 유지"로 참조하므로
그 절들의 원문은 아카이브 쪽에 있다. 아카이브 머리말에 그렇게 적었다(D-L1의 예외가 아니라,
D-L1이 금지하는 것은 **판단 근거로 인용**하는 것이다).

---


## L-B.1 — 단일 뷰포트 (계획서 §10.3)

_이 절은 **리뷰어 지적 15건 대응 후**의 판이다. 초판의 결론 하나가 뒤집혔다 —
`ink_gap`을 "유일한 실측"으로 적었는데 **그것도 정의의 귀결**이었다. 아래 [1] 참조._

### 착수 시 PITFALLS 대조

읽었다. 걸리는 번호로 **#5(자기참조 유형 3)** · #17 · #6 · #21 · #22 · #25 · #26을 적었다.
리뷰어가 **적지 않았는데 걸린 것 여섯**을 더 짚었다: #1(문서가 원장보다 뒤처짐) ·
#2·#3(자기확인·정의의 귀결) · #11(분모) · #12(동작점 하나) · #19(검사 약화) · #24.

### 무엇을 만들었나

```
src/s3d/sceneCam.ts   three 카메라 ← 확정 투시 카메라 (주점이 중심이 아닌 경우 포함)
src/ui/doc.ts         문서 모델 — seg3d === null 이 곧 2D 레이어(§9.1)
src/ui/camState.ts    가이드 → 카메라 (옛 main.ts의 applyGuides/placeCtx를 떼어낸 것)
src/ui/stage.ts       단일 뷰포트 — three 캔버스 아래, 잉크 캔버스 위
src/mainL.ts + l.html 새 UI 엔트리. **옛 index.html은 L-B 게이트까지 안 지운다**(A-4)
e2e/stage.spec.ts     브라우저 확인을 원장으로 (`stage_browser.json`)
```

**어려운 부분은 주점 하나였다.** three의 `PerspectiveCamera`는 화각과 종횡비만 받으므로
주점이 늘 화면 중심이다. `setViewOffset`으로 푼다 — 더 큰 프레임 `W×H`의 중심을 주점에 두고
캔버스를 그 부분창으로 본다(`W = 2·max(px, w−px)`). 주점이 중심이면 `W=w, offset=0`이 되어
**평범한 카메라로 되돌아간다**. `viewport.resize()`가 `aspect = w/h`로 덮어쓰면 주점이
중심으로 돌아가므로 `projectionHook`을 넣었다(D-L9).


### 이 항목에는 **측정이 없다** — 보장 둘뿐이다 (리뷰어 1회차 [1], PITFALLS #5)

`stage0/out/stage_cam.json@46c028d1`. **등록 5구도**(3점×3 · 2점 · 1점) × **3등급** ×
**등록 시드 6** × **잡음 4수준**.

| | 값 | 무엇인가 |
|---|---|---|
| `seg_gap` | **0** | **보장** — `liftAll`이 각 점을 자기 광선에 놓으니 되쏘면 그 점이다 |
| `three_vs_project_gap` | **5.0e-13**(15행 중 최댓값) | **보장** — three의 실제 투영행렬을 통과시킨다 |
| `chord_dev` | 중앙 0.52 / 0.78 / 1.05px (precise/medium/coarse) | **이것도 보장이다** ↓ |

**초판은 `chord_dev`(옛 `ink_gap`)를 "사용자가 실제로 보는 변화"라는 실측으로 적었다.**
리뷰어가 뒤집었고, **대조군에 양성 채널을 붙여** 확인했다(기준 구도, medium, 시드 1, 잡음 0.01):

| 섭동 | `chord_dev` 중앙 | **양성 채널**: 3D 세그먼트 길이 중앙 |
|---|---|---|
| f × 0.8 | 1.5437 | **1.0364** |
| f × 1.0 | 1.5437 | 0.8667 |
| f × 1.2 | 1.5437 | **0.7383** |
| 소실점0 +12px | 1.5437 | 0.8677 |
| 소실점0 +40px | 1.5437 | 0.8698 |

**섭동은 계산에 닿았고**(세그먼트 길이가 1.04 → 0.74로 움직인다) **`chord_dev`만 소수 넷째
자리까지 불변**이다. 2회차 [1]이 "무변화 대조군은 섭동이 도달하지 않은 것과 구별이 안 된다"고
지적했고, 양성 채널이 그 구별을 만든다. 축 방향 섭동도 함께 넣었다 — **지배항은 f가 아니라
축 방향**이기 때문이다(AS-L8). f만 쓰면 이미 무효로 기록된 손잡이를 다시 잡는 것이다.

`seg_gap = 0`이므로 재투영 세그먼트는 언제나 대표 직선이고, 값을 정하는 것은 **잉크 모델의
활과 획 길이**뿐이다. **`lf_bow` 비 1:1.77:2.97에 중앙 비는 1:1.49:2.00** — "정비례"는 아니고
**등급마다 놓인 획 수가 달라 구도 구성이 다르다**(#11). AS-C10대로 **실획 현실을 주장하지 않는다.**

**임계를 걸지 않는다**(셋 다 보장, D-L11). `test/scene_cam.test.ts`(14건)가 불변식으로 잠그고
반례 셋을 넣었다 — 주점 뒤집기를 빼면 50px 이상, y 부호를 안 뒤집으면 100px 이상, 카메라 뒤는 `null`.

### 새로 드러난 관측 — **참 카메라에서도 coarse 배치가 무너진다** (리뷰어 2회차)

`chord_dev`의 분모를 적다가 나왔다. **참 카메라**를 준 배치율(등록 시드 6 × 잡음 4, 분모 1440):

| 등급 | 배치 | 비율 |
|---|---|---|
| precise | 908/1440 | 0.63 |
| medium | 868/1440 | 0.60 |
| **coarse** | **640/1440** | **0.44** |

`coarse`는 **`lift_gate`·`camera_gate`가 한 번도 안 잰 등급**이다(둘 다 precise·medium만 쓴다).
**카메라가 완벽해도 잉크가 거칠면 배치가 0.44로 내려간다.**
⚠ **`lift_gate`의 배치율과 나란히 읽지 않는다**(#27) — 분모(전체 획 대 최대 연결 성분)와
잡음 축 조합이 다르다. 원장에 `skew_note`로 적었다.

### 초판이 틀렸던 자리 (리뷰어 1회차 [2][3][10][11] · 2회차 [3])

| # | 무엇 | 고침 |
|---|---|---|
| 1-[3] | **"5구도 전부"가 등록 집합이 아니었다** — 3점 계열 다섯을 골라 놓고 그렇게 적었다. `setViewOffset`·무한원 소실점이 가장 다르게 도는 **2점·1점이 빠져** 있었다 | 등록 집합으로 바꿨다 |
| 2-[3] | **시드·잡음도 등록 집합이 아니었다** — 시드 4711 하나·잡음 0.01 하나에서 소수 넷째 자리를 인용했다(#14) | 등록 시드 6 × 잡음 4로 바꿨다. 대조군은 **한 동작점**임을 원장에 적었다(#12) |
| 1-[10] | **`three_vs_project_gap`이 정확히 0**이었다 — 우리가 다시 쓴 같은 산식과 비교했으니 **보장조차 아니고 항등**이었다 | `THREE.PerspectiveCamera`의 실제 투영행렬을 통과시킨다(5.0e-13). 반례로 `toBeGreaterThan(0)` |
| 1-[2] | `lift_gate.json`의 **같은 이름 `ink_gap_px`**는 장면당(n=54), 여기는 점 단위다 | 이름을 `chord_dev`로 갈랐다 |
| 1-[11] | **조건 블록이 없었다** | `conditions`를 넣고 등급 셋을 낸다 |

### 브라우저 확인을 원장으로 옮겼다 (리뷰어 1회차 [8], PITFALLS #25)

초판은 브라우저 수치가 **`progress.md`에만** 있었다 — L-A.2의 스윕 표가 죽은 것과 같은 자리다.
Playwright(`e2e/stage.spec.ts`)로 자동화해 `stage0/out/stage_browser.json`에 남긴다.
**1280×675 · 상자 하나 12획 · medium · 잡음 0.006**이고 가이드를 **참 소실점으로** 세운다.

| 확인 | 결과 |
|---|---|
| 확정 | 12/12 3D · f 1000.0px(참 1000) · 주점 (640.0, 337.5)(참값 일치) |
| 배선 항등 | **1.3e-13 px**(24점 최댓값) |
| 주점 비중심 경로 | 주점 (577.6, 422.4) · `offsetX` **124.7** → **1.3e-13** |
| 프레임 대조군 | CSS 프레임 1.3e-13 · **백버퍼 프레임 969.8px**(같은 24점 최댓값) |
| 창 축소 1280 → 820 | `offsetX` 124.7 → **0으로 다시 계산**. `SIZE_HEAL.count = 3` |
| 픽셀로 확인 | 3D 선분 중점 **12/12** 칠해짐 · **대조군** 빈 모서리 **0** |
| 궤도 | 카메라가 풀리고 `OrbitControls` 동작 · 2D 층 **0픽셀** · GL 2383px |
| 확정 시점으로 | 다시 물리고 2D 층 복귀 |

**⚠ 2회차 [8][9]: 이 대조군은 dpr 검사가 아니다.** 초판은 "dpr 대조군"이라 적었는데
되살린 것은 **측정 쪽 비교 프레임**이지 앱의 `canvasFrame` 규약이 아니다. 게다가 조건이
`deviceScaleFactor 1`이고 dpr 2는 **페이지 안에서 흉내낸 것**이라 브라우저 합성 경로를 안 탄다 —
**PITFALLS #21이 기록한 실패 자리가 정확히 그 조건이다.** 앱의 dpr 규약을 잠그는 것은
`e2e/coords.spec.ts`(dpr 1·2·3 프로젝트)이고, 이 스펙의 프레임 대조군은
"비교가 무엇을 해도 통과하지는 않는다"만 보인다. 원장에 `what_this_is_not`으로 적었다.

**이 확인이 말하지 않는 것**을 원장에 적었다: 배치 정확도 · 사람이 도달하는 축 오차
(**참 소실점을 그대로 넣었다**) · 실획 · 등급/구도(**동작점 하나**).

### DEFERRED의 핸들 예산 — **부분 해소**다 (리뷰어 2회차 [13])

초판은 "해소 · 약 4배"라고 적었다. **둘 다 틀렸다.**

- **[13-a] 잘림이 없어지지 않았다.** 새 뷰포트의 가이드는 **607.6 / 618.5 / 1233.3 / 1254.6 /
  1256.8 / 1327.7px**인데 `min_guide_ratio` 0.45는 **651px**을 요구한다 — **여섯 중 둘이 미달**이다.
  잘린 비율이 0.81 → 0.93으로 줄었을 뿐이다
- **[13-b] 배수는 가장 짧은 가이드로 읽는다.** **카메라 정확도는 가장 나쁜 축이 정한다.**
  짧은 쪽(≈600px) 1.28~4.33px 대 옛 한계(≈300px) 0.63~2.16px = **약 2배**다
- **[13-c] 그 예산도 "핸들 하나만 움직인다"는 가정** 위에 있다. `vp_homog`의 섭동은 단일
  핸들이고 사람은 **가이드 6개 = 핸들 12개**를 만진다 — **개별 핸들 예산이지 사용자가
  도달해야 할 정밀도가 아니다.** 그 구분을 §5.2·§12·AS-L9·DEFERRED에 적었다

| 가이드 길이 | 핸들 예산(축 0.5°, 각차 3~20°) |
|---|---|
| 140px (옛 기본) | 0.29 ~ 1.01px — 서브픽셀 |
| 300px (옛 브라우저 한계) | 0.63 ~ 2.16px |
| **600px (새 뷰포트의 짧은 가이드)** | **1.28 ~ 4.33px** |
| 1250px (긴 가이드) | 2.68 ~ 9.03px |

`vp_homog.json`에 **1250px 행을 더해 실측했다**(외삽 아님).

### selfcheck 강화 — 초판의 해석이 두 번 틀렸다 (리뷰어 1회차 [4] · 2회차 [4])

`near-zero` 규칙이 `0 < abs(val) < 1e-10`이라 **1e-11은 걸리고 0은 안 걸렸다.**
오차류 이름이면서 값이 정확히 0이면 플래그한다.

- **1회차 정정**: "389건 중 나머지는 옛 산출물의 정확한 0"이 틀렸다. 규칙이 **전체 경로**로
  매칭해 `…_err.n = 0`(**표본 0**)이 380건 중 **367건**이었다. `leaf` 매칭으로 고쳤다
- **2회차 정정**: 그 뒤 적은 "**옛 산출물에는 정확히 0인 오차가 없었다**"도 **틀렸다.**
  `lift_gate.json`의 `seg_gap_px`는 min~max가 전부 0이다. 새 규칙이 못 보는 이유는
  **꼬리(min·p10·p90·max) 제외 규약** 때문이고, 그래서 **`p90=0`이고 `max>0`인 분포형 오차는
  두 규칙 어디에도 안 걸린다** — 이 규칙이 겨냥한 결함의 주된 기록 형식이 바로 그것이다.
  **DEFERRED에 올린다**(꼬리 제외 규약을 건드리면 플래그가 폭증한 전례가 있다: 89 → 345)
- **"강화만 한다"도 부정확하다.** `low` → `leaf`는 **범위 축소**다. 축소로 사라진 것은
  전부 `.n`(표본 수)이고 오차값은 하나도 없지만, 그 사실을 함께 적어야 한다(#19)
- `"_dev"` → `"dev_"` — `service_worker_in_dev`가 오차로 잡혔다(오탐)
- 반례 7건: `tests/test_selfcheck_exact_zero.py`

### 문서 정정 (리뷰어 1회차 [6][7][12][14][15] · 2회차)

계획서 §5.4의 "0.995 / 0.079 / 0.026"을 뺐고(**§5.2만 고치고 여기는 안 고쳤었다**),
`camera_gate.json:target`의 폐기 대역 0.0791을 `registered_band` 0.0673/0.0296으로 바꿨다
(**문서만 고치면 다음 세션이 원장에서 다시 인용한다**). §5.2·§13의 "각도 파라미터화가 발산을
없앤다"를 표현/카메라로 갈랐고 §13의 항목을 **반증됨**으로 옮겼다. §5.4↔§5.2 교차 참조를
원장 `plan` 필드까지 고쳤다. 2회차로 **`HANDOFF.md` 4곳**(0.079·0.995·≈1°·PITFALLS 26개),
**AS-L2·AS-L8의 폐기 대역**, **`vp_homog.json`의 "0.12 × 대각 = 140px"**(상수는 0.45다),
**§12의 "0.29~4px"**를 고쳤다. `DECISIONS.md`에 D-L9~D-L11.

### 남긴 것 (2회차 중 대응 안 한 것 — CLAUDE.md §4대로 DEFERRED)

- **분포형 오차의 정확한 0을 selfcheck가 못 본다**(2회차 [4]). 꼬리 제외 규약과 충돌한다
- **핸들 예산의 다중 핸들 판**(2회차 [13-c]). 핸들 12개가 독립으로 어긋날 때의 축 오차는 미측정
- **L-B.1의 사전 등록 절이 없다**(2회차 #26). L-B 게이트는 §11에 있으나 소단계 등록은 없다.
  다만 이 항목의 산출은 **전부 보장**이라 통과/실패를 가릴 임계가 애초에 없다

### 검증

vitest **233/233**(39파일) · tsc 0 · pytest **44/44** · playwright **9/9**(새 스펙 1) ·
selfcheck 1451 · **STALE 0** · 상수 해시 `46c028d1` 불변.

---

## L-B.2 — 가이드 조정 UI와 실시간 피드백 (계획서 §5.2)

_리뷰어 13건 대응 후의 판이다. **초판은 이 절을 `web/progress.md`에 썼다** — 쉘 작업 디렉토리가
`web/`이었기 때문이고, 그러면 **다음 세션이 이 항목을 못 본다**(HANDOFF가 루트 `progress.md`를
가리킨다). 지웠고 여기가 유일한 원장이다._

### 착수 시 PITFALLS 대조

읽었다. 걸리는 번호: **#29(여유는 사용자가 만지는 양으로)** · **#17(측정 경로와 앱 경로)** ·
#3(정의의 귀결) · #6(대조군) · #10(뺄셈 금지) · #11(분모) · #12(동작점 하나) · #25 · #26.
리뷰어가 **적지 않았는데 걸린 것**을 더 짚었다: #1 · #5 · #7 · #8 · #14 · #16 · #22 · #27.

**#29가 이 항목의 전부다.** L-A.7이 잰 "핸들 1px이 축을 몇 도 움직이는가"는 **원장에만 있었다.**
자동 판정이 원리적으로 불가능해 사용자의 눈이 유일한 판정 수단인데(§5.3)
**눈은 자기 손이 얼마나 정밀해야 하는지를 못 본다.** 그 값을 화면으로 옮긴다.

### 무엇을 만들었나

```
src/s3d/vpSensitivity.ts   핸들 1px → 축 방향 몇 도. **앱과 하네스가 같이 쓴다**(#17)
src/s3d/vpDraft.ts (추가)  guideLineAt / moveGuideBy / extendGuide — 선 자체를 잡는다
src/mainL.ts (추가)        축별 예산 표시 · 안 올라가는 획의 사유 · `가이드 늘리기`
```

`SENS_TOL`을 `test/constants.ts`에 넣었다(D-C4). 상수 해시 `1a6f104b` → **`46c028d1`**로
바뀌어 전 산출물을 다시 돌렸고 문서 인용 20곳과 **`vpDraft.ts`의 주석**을 고쳤다 —
그 주석은 구 해시와 함께 **철회된 "축 오차 1°"·"29~102px"**을 들고 있었다(PITFALLS #1·#29).

### 결과 — `stage0/out/guide_budget.json@46c028d1`

**⚠ 사전 등록의 통과선 둘이 반증 불가였다**(리뷰어 [3][4]).
① "늘리면 예산이 는다"는 지렛대가 길어지면 deg/px가 단조 감소하므로 정의의 귀결이고,
② "카메라가 안 바뀐다"는 같은 직선 위에서 늘리므로 `v = ℓ₁ × ℓ₂`의 **항등**이다.
**대조군이 아니었다** — L-B.1의 `chord_dev`에서 배운 것과 같은 형태다(#5·#6).
등록의 실질은 "느는가"가 아니라 **"얼마나, 그리고 필요한 값에 닿는가"**뿐이다.

| | 값 |
|---|---|
| 카메라가 서는 경우 | **31/90** |
| 이득 비 | 최소 1.00 · 중앙 **1.3** · 최대 6.8 (n=31) |
| 요구 길이 미달 가이드 | **185/320 → 114/320** (직접 센 수, #10) |

**구도별 최소 예산**(늘린 뒤). 축에 적용한 "가장 나쁜 것이 정한다"를 **구도에도 적용한다**(#8·#16):

| 구도 | n | 최소 | 최대 | **1px 미만** |
|---|---|---|---|---|
| 3pt_yaw20_pitch25 | 3 | 3.32 | 5.58 | 0 |
| 3pt_yaw50_pitch8 | 8 | **0.73** | 5.07 | 3 |
| 3pt_yaw35_pitch15 | 13 | **0.69** | 6.18 | 2 |
| 2pt_yaw35_pitch0 | 4 | **0.23** | 2.92 | 1 |
| **1pt_yaw0_pitch0** | 3 | **0.12** | 0.96 | **3/3** |

**1점 구도는 늘린 뒤에도 세 시드 전부가 서브픽셀**이고 31셀 중 9셀이 1px 미만이다.
AS-L9에 적혀 있던 **1.3~4.3px은 참 소실점을 넣은 낙관값**이었다 — 검출 초안 실측은 이쪽이다.

### 리뷰어가 뒤집은 것 둘

**① 남는 미달은 그림 탓이 아니라 캔버스 탓이다.** 늘린 뒤 길이는 **언제나 그 방향의 캔버스 안
현**이다. 1280×675에서 가장 짧은 현(수직)은 **588.18px**(675 − 여백 2×43.4)이고
요구는 **651.2px**이라 **수직에 가까운 가이드는 어떤 그림에서도 미달**이다 —
미달 152개 중 **26개가 정확히 그 588 대역**이다. `DEFERRED.md`의 옛 원인 귀속
("길이를 그림이 정한다")을 뒤집어 적었다. `min_guide_ratio`를 캔버스 **짧은 변** 기준으로
다시 정하는 것이 남았다(DEFERRED).

**② 31/90을 "§5.1의 확인"으로 읽으면 안 된다.** `vp_detect.json@46c028d1`는 **같은 잡음**에서
확정률 **0.883**이다. 0.88 → 0.34를 만든 것은 검출이 아니라 **소실점을 가이드 선 두 개로
줄이는 초안 변환**이다. 사유 카운터를 넣었다(#7 — 추측하지 말고 센다):

| 사유 | 수 |
|---|---|
| 가이드 4개 미만 | 21 |
| 축 둘 미만 | 8 |
| **`recoverCamera` 실패** | **30** |
| 성립 | 31 |

**지배항은 `recover_failed` 30이다.** 가이드는 있는데 카메라가 안 선다 — 어느 단계에서
무엇을 잃는지는 `vpDraft`의 지지선 선택을 봐야 하고 L-B.2의 범위가 아니다(DEFERRED).

### 브라우저 (Playwright, `stage_browser.json`의 `l_b2`)

| 확인 | 결과 |
|---|---|
| 검출 초안만 | 가이드 5개(236 / 379 / 589 / 591 / 1206px) · **카메라 안 섬** · 민감도 **안 뜸** |
| 참 소실점을 넣은 뒤 | 축별 예산 **2.5 / 2.3 / 2.2px** (0.20 / 0.22 / 0.23°/px) |
| 선 끌기 | 방향 **불변** · 소실점이 (2118.5, 69.6) → (2214.6, 46.5)로 미끄러진다 |

**민감도가 안 뜨는 것이 옳은 동작이다** — 카메라가 안 서면 **없는 숫자를 지어내지 않는다**(A-3).

⚠ **초판은 "축3이 가장 짧고 예산도 가장 좁다 — 표가 그렇게 나온다"고 적었는데 틀렸다**
(리뷰어). 축1(1233px)이 축0(1226px)보다 길면서 예산은 좁다. **길이 순서와 예산 순서가
어긋난다** — 예산을 정하는 것은 길이만이 아니라 **각차와 함께**다. `vp_homog`의 길이 스윕과
이 세 행을 나란히 읽지 않는다(#27): 그 표는 각차를 고정한 것이고 여기는 각차가 축마다 다르다.

⚠ **`vp_homog`의 1250px 행(2.68~9.03)과 브라우저의 2.2~2.5px도 그래서 못 겹친다.**
정의는 같은 양이지만 **(길이, 각차) 쌍**으로만 비교된다.

### 종단에서 잡은 결함 하나

`sens`를 처음에는 `소실점 추정`과 끌기 종료에서만 갱신했다. 그러면 가이드가 다른 경로로
바뀔 때 **낡은 값이 남는다** — Playwright가 실제로 걸었다. 갱신을 **`refresh()` 한 곳으로**
모았다(끄는 중에는 건너뛴다). AS-C7의 자가 치유와 같은 형태다.

### 반례 테스트와 그 한계

`test/vp_sensitivity.test.ts`(10건). ① 짧게 하면 예산이 준다 ② 각차를 좁히면 준다
③ 예산 × 민감도 = 0.5° ④ 카메라가 안 서면 `null` ⑤ `shortestGuidePx`가 짧은 쪽
⑥ `extendGuide`는 f·소실점 불변 ⑦ `moveGuideBy`는 방향 유지·소실점 이동 ⑧ `guideLineAt` 반경.

⚠ **리뷰어가 판별력을 좁혔다**: ③과 ⑥은 **항등**이고, ①②는 수치 미분의 단조성이라
**"입력을 아예 안 읽는" 배선 오류만** 잡는다. **값 자체를 독립으로 확인한 항목이 없다.**
그리고 `vp_homog_measure`가 이제 같은 `vpSensitivity.ts`를 쓰므로 두 산출물의 일치는
**교차검증이 아니라 같은 코드**다 — #17을 지킨 대가다. 해석해나 `probe_px` 변경 대조는 DEFERRED.

### 유효 자릿수

셀 넷이 n=0~1이고 31 중 13(42%)이 한 구도다. AS-C9대로 **유효 자릿수 2**로 적었다
(1.3배 · 0.12px). 최대 6.8배는 **n=1 셀**에서 나온 값이라 대표값으로 쓰지 않는다.

### 검증

vitest **244/244**(41파일) · tsc 0 · pytest 44/44 · playwright **10/10** ·
selfcheck 1451 · **STALE 0** · 상수 해시 **`46c028d1`**.

---

## L-B.3 — 스냅 (계획서 §3)

### 착수 시 PITFALLS 대조

읽었다(**#30을 새로 추가한 직후**다 — 사람 지시로 이 항목 착수 전에 넣었다).
걸리는 번호:

- **#2·#4(자기확인·모델이 문제를 만들지 못한다)** — **이 항목의 전부다.** 합성 잉크의 끝점은
  `endJitter = 0`이면 참 모서리에 **못으로 박혀 있다**(`scene3d.ts` 머리말이 그렇게 적었다).
  그 상태로 스냅 성공률을 재면 **1.0이 나오는데 그것은 스냅의 성질이 아니라 픽스처의 성질**이다.
  → `endJitter`를 반드시 스윕한다.
- **#12·#13(동작점 하나·절단값)** — 스냅 반경을 **하나 고르지 않는다.** 스윕해서 전부 낸다.
- **#9·#10·#11(무엇이 섞였나·뺄셈 금지·분모)** — 성공률을 **맞음/틀림/안 붙음** 셋으로 가른다.
  셋을 각각 직접 세고 분모는 시작점 총수다.
- **#6·#30(대조군·양성 채널)** — "스냅이 배치를 회복한다"는 주장에는 **스냅 없는 대조군**과
  **섭동이 도달했음을 보이는 양성 채널**이 함께 있어야 한다.
- **#17(측정 경로와 앱 경로)** — `snap.ts` 하나를 앱과 하네스가 같이 쓴다.
- **#14(시드 변동폭)** — 시드 6 · 5구도. 비율보다 **분자/분모**로 적는다.
- **#25·#26(원장·사전 등록)** — `stage0/out/snap.json`. 판정 규칙을 **측정 전에** 아래에 박는다.
- **#16(병목이 옮겨간 것)** — 스냅이 배치율을 올리면서 **조용히 틀린 배치**를 늘리는지 함께 본다.
- **#8(꼬리)** · **#1(원장 재확인)** · **#19(설명이 검사를 끄지 않게)** · **#22(캔버스 굳음)**.

### 사전 등록 (측정 전에 박는다, #26)

| | |
|---|---|
| 스윕 축 | 반경비 {0.008, 0.012, 0.018, 0.025, 0.035, 0.05} × 등급 3 × `endJitter` {0, 0.01, 0.03, 0.05} × 시드 6 × 5구도 |
| 판정 | 시작점이 **그 획이 겨냥한 참 모서리 끝점**에 붙으면 맞음 · **다른 참 대상**에 붙으면 **틀림** · 아무 데도 안 붙으면 미스 |
| 반경 선택 규칙 | ① **맞음 − 틀림**이 최대인 반경, ② **틀림 ≤ 0.1 × 맞음**을 만족하는 최대 반경. **둘이 다르면 둘 다 적는다**(#28) |
| 대조군 | 스냅 끔 · 지면(`on_face`) 제외 — `on_face`는 화면 거리가 **정의상 0**이라 넣으면 성공률이 공짜로 1이 된다 |
| 반증 조건 | `endJitter = 0`에서만 좋고 0.03·0.05에서 무너지면 그것은 픽스처를 잰 것이다(#2) |

### 무엇을 만들었나

```
src/s3d/snap.ts       끝점·중점·교차점·수선 발·선 위·면 위 + 우선순위·반경. **앱과 하네스가 같이 쓴다**(#17)
src/s3d/liveLine.ts   앵커 + 축 → 커서 광선의 최근접점으로 끝점 3D 결정 (§3 마지막 문단·§7)
src/capture/inkCanvas.ts (추가)  `onHover` — 누르기 전에 표식을 보인다(좌표는 `canvasFrame` 단일 출처)
src/mainL.ts (추가)   스냅 표식·상태 표시·시작점 확정 경로
```

`SNAP_TOL`·`LIVE_TOL`을 `test/constants.ts`에 넣었다(D-C4). 상수 해시 `46c028d1` → **`dbcf3a11`**.

**교차점은 3D 교차만 쓴다.** 화면 교차는 대개 가림이고 `lift.ts`가 이미 그 자리에서
걸렸다(상자 하나에서 27개 중 3개). Rhino의 `Int`도 실제 교차이므로 **선례가 같은 쪽**이다.

**`on_face`의 대상은 지면 하나다**(면 생성이 범위 밖, DEFERRED). 그리고 그 화면 거리는
**정의상 0**이라(커서 광선이 그 평면과 만나는 점이므로) 우선순위 최하위로 두고
**측정에서는 끈다** — 넣으면 성공률이 공짜로 1이 된다(#3).

### 스냅이 화면 연산인 이유 — 새로 설계한 것이 없다

올라간 기하는 되쏘면 **정확히 그 화면 점**으로 돌아온다(`lift.ts`의 `segGap = 0` 보장).
그래서 **"3D 대상에 붙인다"와 "그 대상의 상으로 `pts2d[0]`을 옮긴다"가 같은 연산**이다.
솔버를 고칠 필요가 없었다.

⚠ **그 귀결로 `start_reprojection_gap_px`는 항등이다**(#5) — 배선 검사이지 정확도가 아니다.
e2e 산출물 이름에 `_IDENTITY`를 붙였다.

### 결과 — `stage0/out/snap.json@dbcf3a11`

**① 사전 등록한 스윕이 경계에서 끝났다.** 등록한 두 규칙이 **둘 다 0.05(스윕의 끝)**를
골랐다 — 최적이 밖에 있었다는 뜻이고 그 표로는 "0.05가 좋다"가 아니라 **"더 키워도
좋아진다"**까지만 말할 수 있다. 정확히 #13의 상태다. **규칙은 그대로 두고 스윕만 넓혔다**
(0.07 / 0.10 / 0.14 / 0.20 / 0.28 추가).

**② 등급은 시작점 겨냥 오차를 거의 안 바꾼다.** 중앙 **7.55 / 7.56 / 7.97px**
(precise / medium / coarse). `renderInk`의 활이 **양끝에서 정확히 0**이라 `lf_bow`가
시작점에 안 닿는다 — 정하는 것은 `endJitter`다(중앙 **0.13 / 4.7 / 14.7 / 23.8px**).
→ **`coarse` 배치가 무너지는 원인을 시작점 겨냥에서 찾으면 안 된다.**

**③ 넓힌 스윕에서도 등록한 두 규칙이 실패했다.** A는 반경을 키우는 **비용이 픽스처에 없어서**
계속 커지고(0.20 = 캔버스 대각의 1/5), B는 대상을 촘촘하게 하면 **어떤 반경도 만족하지 못한다.**
그래서 규칙 C(**포화점** — 미스 ≤ 1%이고 다음 칸의 이득 ≤ 1%인 가장 작은 반경)를
**사후에 추가하고 사유를 원장에 적었다**(#28).

**④ 대상 밀도가 결론을 바꾼다** — 이것이 이 항목에서 가장 중요한 발견이다.
`boxLattice`로 대상을 늘려 같은 스윕을 다시 돌렸다(등급 medium · 시드 2 · 잡음 3 · 5구도):

| | 획 | 참 꼭짓점 간격 중앙 | 규칙 A | 규칙 B | **규칙 C** |
|---|---|---|---|---|---|
| k1(상자) | 12 | 121.2px | 0.14 | 0.28 | 0.10 |
| k2 | 54 | 40.5px | 0.14 | **없음** | 0.07 |
| **k3** | 144 | **21.3px** | 0.10 | **없음** | **0.05** |

k3에서 **틀린 붙음이 반경과 무관하게 1330/4320에서 포화한다**(전부 `endpoint` — 이웃
꼭짓점을 문 것이다). 겨냥 오차 중앙 14.7px이 꼭짓점 간격 21.3px의 절반을 넘기 때문이다.
→ **한계를 정하는 것은 반경이 아니라 (겨냥 오차 / 대상 간격)의 비**다. 스냅으로 못 고치는 몫이 있다.

**`SNAP_TOL.radius_ratio = 0.05`**(960×672에서 58.6px)로 정했다 — **가장 촘촘한 k3의
포화점**이다. 성긴 쪽에서 고르면 촘촘해질 때 못 쓴다. 선례(SketchUp/Rhino의 10~15px)보다
훨씬 크고, 이유는 **손 획의 겨냥 오차 자체가 크기 때문**이다.

**⑤ 무엇에 붙었나**(#7). 반경 0.05에서 끝점 4170 · 선 위 60 · 중점 34 · 미스 56(분모 4320).
틀린 것의 종류가 반경에 따라 **성질이 바뀐다**(#16): 작은 반경에서는 `on_edge`
(모서리를 못 찾아 옆 선 몸통에 붙음)가 817/836인데, 큰 반경에서는 `endpoint`
(**다른 꼭짓점**을 뭄)가 21/21이 된다. 병목이 없어진 것이 아니라 옮겨간 것이다.

### 브라우저 (Playwright, `stage_browser.json`의 `l_b3`)

**실제 포인터로 긋는다**(`page.mouse`) — 꼭짓점에서 13·12px 어긋나게 시작해서
같은 모서리 방향으로 긋는다. 결과: 표식 `끝점`(17.2px) · `snapStart.ofId = s6` ·
축 0으로 확정 · 배치됨. **대조군**으로 빈 자리(캔버스 모서리)에서 그으면
붙지 않고 **2D로 대기**한다(§9.1 — 실패가 아니다).

### 반례 테스트 (`test/snap.test.ts`, 11건)

L-B.2에서 "항등과 단조성뿐"이라고 지적받은 자리를 피해 **값을 독립으로 확인하는 항목**을 넣었다:
① 우선순위가 거리를 이긴다(중점 위에 커서를 둬도 끝점이 이긴다. 반경을 좁히면 중점이 이겨
**순위가 갈랐는지 반경이 갈랐는지 구분된다**) ② **중점은 3D 중점의 상이지 화면 중점이 아니다**
(둘이 20px 넘게 갈리는 것을 먼저 확인하고 — #4 — 화면 중점에서는 후보가 **안 나오는** 것까지 본다)
③ **화면에서만 겹치는 두 선분은 교차점을 안 낸다**(두 선분이 같은 화면 점을 지난다는 것을
먼저 확인하고, `on_edge`는 **둘 다 나오는** 것으로 "후보가 없어서"가 아님을 가른다)
④ 3D에서 만나면 몸통 교차라도 낸다 ⑤ 수선 발의 **직교성을 독립 계산으로** 확인
⑥ 공유 모서리는 끝점 후보 하나 ⑦ 반경 밖이면 `null` ⑧ 카메라 뒤는 후보 없음
⑨ 지면은 `ground`가 있어야 나오고 최하위 ⑩ `geomScale`이 배율 10배에서 같은 판정
⑪ 우선순위 목록이 계획서 §3 그대로.

### 리뷰어 12건 대응 (1회차)

**[1] 등록한 대조군을 안 돌렸다 — 가장 큰 지적.** 사전 등록 표에 "대조군: 스냅 끔"을 적어 놓고
`snap.json`에 `control` 키가 하나도 없었다. 계획서 §11 L-B 게이트의 **두 번째 항목**
("스냅된 시작점에서의 배치 정확도")도 측정 대상이 아니었다. → `placement_from_snapped_start`를
넣었다. **앱과 같은 경로**다(시작점을 대상의 상으로 옮김 → `nearestAxisOnScreen` → `segmentFromAnchor`).
"스냅 끔"은 **설계상 0을 놓는다**(앵커가 없다) — 측정이 아니라 사실이므로 팔로 돌리지 않고 원장에 적었다.

| 반경 | 배치 | 스냅 실패 | 축 실패 | 형태 오차 중앙 | **조용히 틀림 0.2** |
|---|---|---|---|---|---|
| 0.025 | 3168/4320 | 295 | 857 | 0.019 | **532/3168** |
| **0.05** | **3403/4320** | 56 | 861 | 0.020 | **569/3403** |
| 0.10 | 3459/4320 | 1 | 860 | 0.022 | **686/3459** |

**놓인 것의 17%가 조용히 틀린다**(cut 0.2). 그리고 cut 0.5가 504/569이라 **틀린 것은 크게 틀린다.**
축 실패 861이 스냅 실패보다 훨씬 크다 — L-B.4가 볼 자리다.

**[2] 반경 선택의 논거가 데이터와 반대였다.** "성긴 쪽에서 고르면 촘촘해질 때 못 쓴다"고 적었는데
세 밀도 **전부에서 0.10이 맞음↑·틀림↓·미스↓**였다. **이 픽스처에 반경을 키우는 비용이 없었기
때문**이다. → 비용을 **측정에 넣었다**: `unintended_snap`(기존 기하와 무관한 자리에서 시작하려 할 때
스냅이 끼어드는 비율, 캔버스 균등 난수 24점/픽스처).

| k3 | 맞음 | 끼어듦 |
|---|---|---|
| 0.025 | 2764/4320 | 1440/6480 |
| **0.05** | 2963 (**+199**) | 1854 (**+414**) |
| 0.10 | 2990 (**+27**) | 2772 (**+918**) |

**0.10은 얻는 것이 거의 없고 잃는 것이 크다.** 0.05를 그대로 두되 **근거를 바꿔 적었다**.

**[3] 규칙 C의 절단 감도가 없었다.** 0.5% / 1% / 2%를 다 낸다. k3에서 **0.05 / 0.05 / 0.035**,
k1·k2에서 0.10~0.05. **절단 2%면 0.035**라는 사실을 원장에 적었다.

**[4] 밀도 표가 등급 1·시드 2였다** — 그런데 **앱 상수가 그 표에서 나왔다**. 등록 조건
(등급 3 · 시드 6)으로 다시 돌렸다. 분모가 k1 3240 / k2 14580 / k3 38880으로 늘었고
**규칙 C의 답은 안 바뀌었다**(k3 0.05).

**[5] k1의 겨냥 오차를 k3의 간격과 나란히 놓았다**(#27). 밀도별로 직접 쟀다 —
**k1 12.63 · k2 12.01 · k3 12.19px**(중앙). 거의 같다(`drawEdges`의 `endJitter`가 **획이 아니라
그림 전체 대각**에 비례하기 때문이다). 그래서 비 논거는 **같은 밀도 안에서** 성립한다:
k3에서 겨냥 오차 중앙 12.2px 대 꼭짓점 간격 절반 10.65px.

**[6] "등급이 안 바꾼다"는 잉크 모델의 성질일 수 있다.** 맞다. 끝점에 닿는 유일한 성분인
`hf_sigma`가 등급 간 **0.0310 / 0.0336 / 0.0314 — 8%밖에 안 다르다**. 결론의 사정거리를
**"이 잉크 모델에서는"**으로 좁혀 적었고, 유효 자릿수도 2로 고쳤다(**7.6 / 7.6 / 8.0px**).

**[7] `@addaeb9d` 29곳** → `@46c028d1`로 고쳤다(`grep` 0건 확인). HANDOFF의 "PITFALLS 29개"도 30개로.

**[8] `assumptions.md` AS-L4·AS-C2**를 이번 관측으로 갱신하고 `DECISIONS.md`에 **D-L12~D-L14**를 넣었다.

**[9] `lift_grade.json`이 progress에 없었다** → 아래 L-B.3(a) 절.

**[10] `snapped_kind_counts`가 결론 집합을 안 썼다** — 못으로 박힌 `end_jitter = 0`의 1080셀이
섞여 있었다. `rows_end_jitter_gt_0`를 나란히 넣었다.

**[11] 앱 캔버스 px 병기 · `l_b3`의 `size_heal_count`** 추가. 앱(1280×675)에서 반경은 **72px**이고
하네스(960×672)에서는 58.6px이다 — 선례와 비교할 때 어느 쪽인지 밝힌다.

**[12] (a) 판정 허용오차가 사전 등록에 없었다** → 1/200·1/100·1/50 스윕. 반경 0.05에서
맞음 3858 / 3870 / 3938(분모 4320)로 **거의 안 움직인다** — 결론을 정하는 값이 아니다.
**(b) 승격에서 스냅이 조용히 풀린다.** 옮겨 놓은 `pts2d[0]`은 **옛 카메라가 만든 대상의 상**이라
새 카메라에서는 그 대상의 상이 다른 자리다. `snapStart.ofId`를 다시 풀어야 한다 —
`applySnapToStart`에 경고를 달고 **DEFERRED(L-C)**에 올렸다.

---

## L-B.3(a) — 참 카메라에서 `coarse` 배치가 0.44인 원인 (사람 지시)

### 착수 시 PITFALLS 대조

걸리는 번호: **#10(뺄셈 금지 — 각 팔을 직접 돌린다)** · **#16(병목 이동)** · #1(같은 양이 두 숫자) ·
#7(사유 카운터) · #8(꼬리) · #11(분모) · #12·#13(동작점·절단) · #25 · #27(하네스 간 비교) · #30(양성 채널).

**#1이 이 항목에서 실제로 걸렸다** — 형태 오차를 재려고 `camera_gate.test.ts`의 지역 함수를
복사할 뻔했다. 계획서 §5.4가 "0.026 대 0.0296"으로 **같은 양을 두 숫자로** 돌린 것이 그 사고다.
`perStrokeError`를 `test/scene3d.ts`로 옮겨 **하나만 남겼다**.

### 방법 — 단계를 오라클로 대체한다

`liftAll`은 세 단계다. 각 단계를 오라클로 바꾼 팔을 **직접 돌린다**(뺄셈 아님, #10).

### 재현 확인 — 다른 것을 재고 있지 않은가

| | precise | medium | coarse |
|---|---|---|---|
| `stage_cam.json` | 0.6306 | 0.6028 | 0.4444 |
| **여기** | **908/1440 = 0.6306** | **868/1440 = 0.6028** | **640/1440 = 0.4444** |

**정확히 재현한다.** 구도·시드 산식·skew·잡음 축을 `stage_cam`과 같게 맞췄기 때문이다(#27).

### 답 — **축 판정이 지배항이다**

| 팔 | precise | medium | coarse |
|---|---|---|---|
| `judged` (실제) | 908/1440 | 868/1440 | **640/1440** |
| **`oracle_axis`** | **1257/1440** | **1266/1440** | **1187/1440** |
| `oracle_axis` + `touch_ratio` 0.09 | 1440/1440 | — | 1440/1440 |

**축을 오라클로 주면 `coarse`가 0.44 → 0.82로 오르고 등급 격차가 0.19 → 0.05로 거의 사라진다.**
미배치 사유(`liftAll`이 이미 돌려주고 있었는데 아무도 안 셌다):

| 사유 | precise | medium | coarse |
|---|---|---|---|
| **축이 미분류다** | 316 | 367 | **533** |
| 구조에 이어지지 않았다 | 189 | 194 | 237 |
| 어느 획과도 교점이 없다 | 27 | 11 | 30 |

**오라클 축에서는 `축이 미분류다`가 0이 되고 `구조에 이어지지 않았다`만 남는다**(183/174/253).
그것은 접합 반경이 정하고, 0.09로 키우면 **1440/1440**이 놓인다.

**조건수는 "못 푸는" 문제가 아니라 "틀리게 푸는" 문제다.** `교점 제약이 형태를 정하지 못한다`가
**0건**이다 — `solveNull`은 언제나 답을 낸다. 대신 형태 오차 중앙이 어느 팔에서도 **0.10~0.18**이고
놓인 것의 **cut 0.2 조용히 틀림이 34~39%**다.

⚠ **고칠 때마다 조용히 틀림이 함께 는다**(#16): 오라클 축 기본 반경 496/1257(39%) →
오라클 축 + 0.09 **954/1440(66%)**. **배치율만 보면 반경을 키우는 것이 이득으로 보이는데 아니다.**

### ⚠ 스냅은 이 0.44를 못 고친다

스냅 대상은 **3D 레이어**인데(§9.1) **초기 확정 순간에는 3D가 아직 없다** — 모든 획이 2D 대기이고
대상이 0이다. 스냅이 닿는 것은 **확정 뒤에 그리는 획**뿐이다.
0.44에 닿는 것은 **(1) 확정 시점의 축 판정(L-B.4) (2) 승격 연쇄(L-B.7)**다.
`DEFERRED.md`·`HANDOFF.md`가 "L-B.3 뒤에 다시 잰다"로 적어 둔 전제를 **뒤집어 적었다**.

### 동작점

잡음이 지배적으로 크다 — `coarse` 배치가 잡음 0에서 234/360인데 **0.05에서 70/360**이다.
구도별로는 1점이 형태 오차 최악(중앙 0.18~0.33). **동작점 하나로 말하지 않는다**(#12·#8).

---

## L-B.3(b) — 초안 변환이 확정률 0.88을 0.34로 떨어뜨리는 이유 (사람 지시)

### 착수 시 PITFALLS 대조

걸리는 번호: **#16(확정률만 보면 안 된다 — 서기만 하고 틀리면 더 나쁘다)** · **#30(양성 채널)** ·
#6(대조군) · #7(카운터) · #10·#11(직접 센 집합·분모 합 검사) · #24(재매개화는 아무것도 안 고친다) ·
#25 · #27(같은 조건).

### 가설과 그 시험

`draftFromDetection`이 소실점마다 **가장 긴 둘**을 고르는데, 소실점은 **두 선의 교점**이므로
조건을 정하는 것은 길이만이 아니라 **각차**다. 각차가 좁으면 L-A.7의 **무한원 스냅**(3°)이 그 축을
비우고 카메라가 안 선다. **이것이 사람 지시의 "각도 파라미터화 전환과 관련 있는가"의 정확한 형태다.**

### 답 — 관련 있지만 지배항은 아니다

| 팔 | 카메라 성립 | 무한원 스냅된 축 | 가이드 쌍 각차 중앙 | **축 오차 중앙** | f 상대오차 |
|---|---|---|---|---|---|
| `longest` + 스냅 3°(현행) | **31/90** | 42 | 7.18° | 12.35° | 0.30 |
| `longest` + 스냅 **끔** | 40/90 | 0 | 7.18° | 13.80° | 0.32 |
| `widest` + 스냅 3° | **44/90** | **0** | 14.24° | 9.96° | 0.29 |
| **`len_x_sep` + 스냅 3°** | **43/90** | **0** | 13.94° | **9.05°** | **0.24** |
| **중립 시작**(그림 무시) | **90/90** | 90 | — | **13.83°** | 0.41 |

**31/90을 정확히 재현한다.** 그리고:

- **무한원 스냅은 9/90을 설명한다**(31 → 40). 관련은 있으나 지배항이 아니다
- **지배항은 지지선 선택이다.** `longest`의 가이드 쌍 각차는 **최소 0.03°**다 — 가장 긴 둘이
  하필 나란한 경우다. `widest`·`len_x_sep`은 최소 3.78°·3.36°이고 **무한원 스냅에 아예 안 걸린다**
- ⚠ **확정률만 보면 안 된다**(#16). **어느 팔에서도 축 오차 중앙이 9~14°로 임계 0.5°의 18~28배다.**
  초안 카메라는 **어차피 쓸 수 없다** — §5.1이 이미 그렇게 적었다. 초안의 가치는 **끌기 시작 위치**다
- **그래서 중립 시작과 비교했다**(사람 지시): 중립은 카메라가 **언제나 서지만**(90/90)
  축 오차 13.83° · f 0.41로 **초안(9.05° · 0.24)보다 나쁘다.** **초안이 중립보다 낫다.**

### 채택 — `len_x_sep` + **빈 축만 중립으로 채운다**

`widest`가 확정률 1건 높지만 `len_x_sep`이 **축 오차·f 둘 다 낫다**. 길이도 함께 보므로
핸들 예산에도 유리하다. `DRAFT_TOL.support_pick = "len_x_sep"`.

그리고 **`guides_lt_4` 21/90은 어느 팔에서도 안 줄었다** — 검출이 축을 못 채운 것이다.
그 축은 화면에 **손잡이가 아예 없어 사용자가 끌 것이 없다.** 조정 UI의 전제가 깨지므로
`DRAFT_TOL.fill_missing_axes = true`로 **빈 축만** 중립 위치로 채운다(있는 축은 안 건드린다 —
초안이 중립보다 낫기 때문이다).

### 그 결과 `guide_budget`가 바뀌었다 — **31/90 → 58/90**

| | 옛값(`longest`, 채우기 없음) | **새값(`len_x_sep` + 채우기)** |
|---|---|---|
| 카메라 성립 | 31/90 | **58/90** |
| 이득 비 중앙 | 1.3 | 1.30 |
| 요구 길이 미달 | 185/320 | **347/540** → **249/540** |

**구도별 최소 핸들 예산**(늘린 뒤):

| 구도 | n | 최소 | 최대 | 1px 미만 |
|---|---|---|---|---|
| 3pt_yaw35_pitch15 | 13 | 0.95 | 7.52 | 1 |
| 3pt_yaw20_pitch25 | 14 | **0.63** | 3.25 | 4 |
| 3pt_yaw50_pitch8 | 17 | 0.80 | 7.23 | 4 |
| 2pt_yaw35_pitch0 | 8 | **0.465** | 2.86 | 2 |
| 1pt_yaw0_pitch0 | 6 | **0.547** | 1.09 | 4 |

⚠ **행 간 비교가 같은 양의 비교가 아니다**(#11). n이 3~4에서 6~17로 늘었으므로
**최소값이 내려간 행은 나빠진 것이 아니라 표본이 넓어진 것**이다(3pt_yaw35가 3.32 → 0.95인 것이 그 예).
서브픽셀 셀 비율은 9/31(29%) → **15/58(26%)**로 거의 같다.
**1점 구도가 서브픽셀이던 문제는 0.12 → 0.547px로 4.5배 나아졌지만 여전히 1px 미만이다.**

---

## L-B.4 — 축 판정과 실시간 색, 미리보기 선 (계획서 §4)

### 착수 시 PITFALLS 대조

읽었다. 걸리는 번호:

- **#16(병목이 옮겨간 것)** — **이 항목의 전부다.** L-B.3(a)가 "축을 오라클로 주면 0.44 → 0.82"를
  냈지만 **같은 팔에서 조용히 틀림이 39%**다. 임계를 풀어 배치를 늘리면 조용히 틀림이 함께 는다.
  **배치율만 보면 반대로 간다.**
- **#7(추측하지 말고 센다)** — `classifyStroke`가 이미 `reason`을 돌려준다
  (`too_short`·`multi_axis`·`too_bent`·`no_axis_matches`·`ambiguous`·`no_camera`).
  **533이 무엇인지 세지 않고 임계를 만지지 않는다.**
- **#12·#13(동작점·절단)** — 임계를 하나 고르지 않는다. 스윕하고 절단 셋을 다 낸다.
- **#20(거름은 선택 뒤에)** — `angle_max_deg` 같은 거부권을 **선택 전에** 걸면 경쟁자가 죽어
  오배정이 **늘었다**(79% → 90%). 순서를 바꾸지 않는다.
- **#6·#30(대조군·양성 채널)** — "실시간 색이 오배정을 줄인다"는 **사용자 행동 의존**이라
  직접 측정이 불가능하다(AS-L5). **간접 지표를 측정으로 적지 않는다.**
- **#3(정의의 귀결)** — 임계를 풀면 `free`가 주는 것은 **정의상 참**이다. 재는 것은 그 다음이다.
- #8(꼬리) · #11(분모) · #14(시드) · #17(단일 출처) · #25 · #26(사전 등록) · #27.

### 사전 등록 (측정 전에 박는다, #26)

| | |
|---|---|
| 스윕 축 | ① 확정 시점: `vp_dist_ratio` {0.03,0.06,0.10,0.16,0.22} × `bend_max` {0.12,0.18,0.30} × `ambiguity_margin` {1.2,1.5,2.0} ② 실시간: `LIVE_TOL.axis_deg` {4,8,12,18,25,35} |
| 판정 | 배치율(모집단 분모) · **조용히 틀림 cut 0.1/0.2/0.5**(놓인 것 분모) · 축 오배정(참 축 대비) · `free` 사유 히스토그램 |
| 채택 규칙 | **조용히 틀림(cut 0.2)이 기준 조건보다 늘지 않는 선에서** 배치율이 최대인 임계. 늘면서 배치가 느는 것은 **채택하지 않는다**(#16) |
| 대조군 | 기준 조건(`AXIS_TOL` 현행값) · 오라클 축 |
| 반증 조건 | 배치율이 늘어도 조용히 틀림이 함께 늘면 그 임계는 **개선이 아니다** |

### 무엇을 만들었나

```
src/capture/inkCanvas.ts (추가)  `onLive` — 그리는 중의 점열. 오면 **전체 다시 그리기**로 바뀐다
                                  (증분 그리기는 안 지우므로 미리보기가 커서를 못 따라온다)
src/s3d/liveLine.ts (기존)        `nearestAxisOnScreen` · `segmentFromAnchor`
src/mainL.ts (추가)               `resolveLive` — **실시간과 확정이 같은 함수를 쓴다**
                                  축 색 미리보기 선 · 앵커 표식 · 상태 표시
test/axis_live.test.ts            임계 스윕과 실시간 판정 → `stage0/out/axis_live.json`
```

**미리보기와 확정이 어긋날 여지가 구조적으로 없다** — 둘 다 `resolveLive(앵커, 시작점, 끝점)`
하나를 부른다. ⚠ 그러므로 §11 게이트의 "일치 0"은 **측정이 아니라 항등에 가깝다**(#5).
브라우저에서 실제로 0이 나오지만 그것이 재는 것은 **정확도가 아니라 배선**이다.

### 결과 — `stage0/out/axis_live.json@46c028d1`

**① 533이 무엇인지 셌다**(#7). 기본 조건의 `free` 사유:

| 사유 | precise | medium | coarse |
|---|---|---|---|
| `no_axis_matches` | 204 | 215 | 198 |
| `ambiguous` | 92 | 84 | 62 |
| **`multi_axis`** | **0** | 50 | **254** |
| `too_short` | 20 | 18 | 19 |

**`multi_axis`가 `coarse`의 등급 특이 항이다** — 거친 잉크의 떨림이 **없는 코너를 만들어 낸다**
(`maxTurn`이 잡음에 발화한다). precise에서는 0이다.

**② 확정 시점 판정은 임계로 못 고친다.** 45개 조합 어디에서도 배치와 조용히 틀림이
**거의 고정된 비율로 맞바뀐다**:

| `vp_dist_ratio` | 배치 | 축 오배정 | 미분류 | **조용히 틀림 0.2** |
|---|---|---|---|---|
| 0.03 | 2259/4320 | 309 | 1393 | **0.305** |
| **0.06**(기본) | 2416/4320 | 372 | 1216 | **0.362** |
| 0.10 | 2524/4320 | 429 | 1054 | 0.384 |
| 0.16 | 2584/4320 | 462 | 965 | 0.398 |
| 0.22 | 2632/4320 | 502 | 895 | 0.416 |

사전 등록한 규칙(조용히 틀림이 기준 이하이면서 배치 최대)이 고르는 것은
`vp0.1_bend0.18_amb2`이고 이득이 **+26/4320**이다 — **시드 변동폭 안이다**(#14).
**`AXIS_TOL`을 바꾸지 않는다**(A-3: 측정이 아무 방향도 안 가리킨다).

**③ 방향 급변 검출도 같은 맞바꿈이다 — 그런데 대조군이 뜻밖의 것을 말했다.**
`turn_deg` 45° → 60°에서 배치 2416 → 2585이지만 조용히 틀림 0.362 → 0.387이다.
그리고 **진짜 코너가 있는 획을 이어 그린 대조군**(#4 — 픽스처가 벌을 줄 수 있어야 한다)에서
거부율이 `turn_deg` 90°(사실상 끈 것)에서도 **2018/2160 = 93%**다.
→ **진짜 코너를 걸러내는 것은 `multi_axis`가 아니라 다른 규칙들**(굽음·애매·무매칭)이고,
`multi_axis`는 **거친 잉크에만 발화하는 부작용 쪽에 가깝다.**
그래도 조용히 틀림이 늘어 등록 규칙을 통과하지 못하므로 **끄지 않는다.**

**④ 고치는 것은 임계가 아니라 앵커다 — 이것이 이 항목의 답이다.**

| | 배치 | 축 오배정 | 형태 오차 중앙 | **조용히 틀림 0.2** |
|---|---|---|---|---|
| 확정 시점(`classifyStroke`) | **2416/4320 = 0.56** | 372 | — | **36.2%** |
| **실시간(앵커 있음, 12°)** | **3403/4320 = 0.79** | 442 | **0.0198** | **16.7%** |

⚠ **조건 차를 함께 읽는다**(#27): 실시간 팔은 **참 3D 기하가 이미 있다**고 가정한다
(스냅과 같은 상한 조건). 초기 확정에는 그것이 없다. 그러므로 이 비교는
**"앵커가 있으면 다른 문제가 된다"**를 말하지 **"초기 확정을 고쳤다"**를 말하지 않는다.
**초기 확정의 몫은 L-B.7 승격 연쇄로 넘어간다.**

**⑤ `LIVE_TOL.axis_deg = 12`를 유지한다.** 각차 4 → 35°에서 배치 2554 → 3728이고
조용히 틀림 0.102 → 0.219다. 무릎이 12~18°이고(12 → 18은 +99 배치에 +0.8pp)
등록 규칙은 기준 위에서 아무것도 안 고른다. **사유 없이 바꾸지 않는다**(#28).
참고로 **앵커에서 본 최근접 축과의 각차 분포**는 중앙 **2.29°** · p90 **53.6°**로 꼬리가 두껍다 —
임계로 가를 수 없는 몫이 그 꼬리다.

### 브라우저 (Playwright, `stage_browser.json`의 `l_b4`)

끌고 가는 **도중에** 상태창이 `그리는 중 — 시작 끝점 · 축1(…°)`을 낸다.
끌기 마지막 위치의 미리보기 3D와 뗀 뒤의 확정 3D가 **정확히 같다**(gap 0) —
⚠ **항등이다**(#5). 같은 함수를 같은 입력으로 부른다.

### 검증

vitest **260/260**(46파일) · tsc 0 · pytest 44/44 · playwright **12/12** ·
selfcheck **STALE 0** · 상수 해시 `46c028d1`(신규 상수 없음).
