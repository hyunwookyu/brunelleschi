접근을 전환한다. segment_plan.md를 저장소에 편입하고
docs/segment_plan.md 로 두어라.

docs/sketch3d_plan.md의 §4(획 → 3D)와 §5(자료구조)를 대체한다.
§3(카메라 확정)과 §1(제품 정의)은 유지한다.

════════════════════════════════════════
전환 요지
════════════════════════════════════════

완주 후 판정이 "첫 시점에서 그리는 데는 겨우 선다, 돌려서 그리는 데는
서지 않는다"였다. 원인이 둘이고 둘 다 구조적이다.

1. 뷰포트를 분리했다.
   2D 캔버스와 3D 뷰를 나눠 왕복시켜 "첫 시점"과 "돌린 시점"이 별개
   좌표계가 됐다. 돌린 시점 사선 오배정 79%(놓인 90건 전부),
   시점 교차 시 후보 0, pts2d 좌표계 혼동이 전부 여기서 나왔다.

2. 획을 그대로 정합하려 했다.
   손떨림이 있는 곡선을 3D에 놓으려니 끝점 겨냥 오차가 그대로 배치
   오차가 됐다. 배치율 0.112~0.846, 사선 오배정 15.8~79%,
   고치기 상한 모형에서도 21~35% 오류.

**원칙은 유지된다.** 보이는 것은 여전히 손 획이고 스냅 직선은 화면에
나오지 않는 내부 기하다. 손떨림은 그대로 남고, 그 획이 어느 3D 선에
속하는지만 직선이 정한다.
기존 원칙("방향만 판정하고 형태는 건드리지 않는다")의 확장이다 —
방향뿐 아니라 위치도 직선이 정하고, 형태는 여전히 획이 정한다.

════════════════════════════════════════
G-A. 기하 계층 교체 (2세션 규모)
════════════════════════════════════════

전부 순수 함수라 UI 없이 테스트된다. 서로 의존하므로 나누지 않는다.

1. 기존 배치 계층 제거 (segment_plan.md §7.2)
   앵커 체인, 정합성 검사, 깊이 타당성, 고치기 후보 탐색,
   돌린 시점 전용 임계, 소실점 투표 갱신.
   **측정 결과는 progress.md에 보존한다.** 코드만 제거한다.

2. Segment 자료구조 (§1)
   3D 위치는 L(t) + offset(t)로 재구성된다.
   세그먼트가 기하이고 오프셋이 표현이다.

3. 획 → 세그먼트 매달기 (§2)
   획이 직선의 길이를 정한다. 직선을 먼저 만들고 획을 맞추는 것이
   아니므로 "획이 직선보다 길거나 짧은" 문제가 없다.
   떨림 평면은 현재 facing 방식 그대로 (§2.1).

4. 3D 교차 접합 (§3)
   끝점이 정확할 필요가 없다. 못 미치거나 지나쳐도 교차점이 계산된다.
   지나침이 오히려 정상이고 손으로 그릴 때 자연스럽다.
   교차 임계는 측정으로 정한다. 관대해도 되지만 크면 무관한 것이 붙는다.

5. 씨앗 앵커 (§3.1)
   첫 획만 앵커가 필요하고 나머지는 교차로 전파된다.
   연결된 것이 하나만 있어도 놓인다.

6. **중간 판정** — 아래 필수

════════════════════════════════════════
G-A.1 중간 판정 (G-A의 마지막 항목, 필수)
════════════════════════════════════════

같은 합성 조건(끝점 오차 1%, 고각 18°, 구도 하나)에서 기존 수치와
대조한다.

  배치율 첫 시점        0.846 → 상승해야 함
  배치율 돌린 시점 45°   0.419 → 상승해야 함
  사선 오배정 첫 시점    0.158 → 유지 이하
  조용히 틀린 배치       0.057 / 0.655 → 하락해야 함

**개선이 안 나오면 G-B로 가기 전에 멈추고 보고한다.**
접근이 틀렸다는 뜻이므로 1.5세션을 아낀다.

이것이 이번 전환의 유일한 중단 조건이다.

════════════════════════════════════════
G-B. 화면 통합 (1.5세션 규모)
════════════════════════════════════════

1. 2D/3D 겹침 (§4.1)
   초기에 두 층이 같은 자리에 겹쳐 있고 사용자는 모른다.
   전환이 한 번뿐이고 그 뒤로 뷰포트가 하나다.
   "첫 시점 / 돌린 시점" 구분 자체가 없어진다.

2. 전환 조건과 무변화 (§4.2, §4.3)
   3점은 서로 다른 축 3개가 각각 2획 이상.
   2점·1점은 축 + 렌즈 슬라이더.
   **카메라가 맞았다면 전환 순간 화면이 똑같아야 한다.**
   전환 전후 픽셀 차이를 지표로 삼는다. 튀면 카메라가 틀린 것이다.

3. 소실점 잠금과 재설정 (§4.4)
   전환 후 갱신 자체를 하지 않는다. 기존 소프트 락보다 강하다.
   "점을 찍거나 선을 그을 때마다 소실점이 튀는" 현재 문제가 해소된다.
   재설정 버튼 하나 — 2D로 돌아가고 획은 남는다.

4. 실시간 축 미리보기 (§5)
   그리는 도중 축 색이 실시간으로 바뀐다.
   사용자가 그으면서 보고 각도를 틀어 고친다.
   **오배정 15.8%와 79%를 동시에 줄이는 핵심 장치다.**
   2D 증거로 구분 불가한 경우(D-S22)도 사용자가 보면 안다.
   축 강제 해제 수정자(Shift 등)도 함께.

전환이 매끄러운지는 미리보기가 돌아야 확인되므로 함께 간다.

════════════════════════════════════════
G-C. 마무리 (1세션 규모)
════════════════════════════════════════

1. 자유 세그먼트 (§6)
   축 → 면 위 사선 → 자유 세그먼트 순으로 시도.
   양 끝이 기존 세그먼트에 붙어야 성립.
2. 미배치 처리 — 화면에 남기고 지정 경로
3. 지표 재정의 (§7.3)
4. 종단 검증 (Playwright)
5. 문서 갱신 (README, PROTOTYPE, DECISIONS, DEFERRED)

════════════════════════════════════════
진행 규칙 — 무중단 자율 진행
════════════════════════════════════════

G-A → G-A.1 → G-B → G-C까지 멈추지 않고 완주한다.
중간 보고 없음. 확인 요청 없음. 시간 무관.
컨텍스트가 소진될 때까지 최대한 많이 진행한다.

각 항목 완료 시:
  테스트·selfcheck 실행 → 리뷰어 호출 → 지적 대응 → 커밋 → 다음

A-1 세션 인계
  컨텍스트가 차면 HANDOFF.md를 갱신하고 종료한다.
  다음 세션이 그것만 읽고 이어갈 수 있어야 한다.
  현재 단계, 다음 할 일(파일·함수 수준), 잠정 결정과 근거,
  미해소 지적, 최근 커밋 해시, 읽을 문서 순서.

A-2 막혔을 때
  재시도 3회 → 우회(더 단순한 구현) → 격리 → 다음 항목.
  중단하는 유일한 경우는 G-A.1이다.

A-3 판단 규칙
  측정 우선. 예상과 어긋나면 측정을 따르고 assumptions에 반증 기록.
  선택지가 여럿이면 가장 단순한 것. 복잡한 대안은 DEFERRED.md.
  범위를 넓히지 않는다.
  애매하면 놓지 않는다. 조용히 틀린 배치를 만들지 말 것.
  놓지 않되 버리지 않는다 — 미배치는 화면에 남긴다.

A-4 품질 하한
  테스트 깨진 채 다음 항목으로 가지 않는다.
  새 기능마다 테스트. 반례 포함.
  각 항목 끝에서 브라우저로 열리는 상태여야 한다.
  참조 저장소(notebysebas/sketchbook-v8) 코드를 복사하지 않는다.
  새 임계는 constants.ts에 넣는다. 안 넣으면 STALE이 안 잡힌다.
  **dpr 1에서만 확인하지 않는다** (반복 유형 20번).
  **회귀 테스트는 버그를 되살려 실제로 잡는지 확인한다.**

A-5 완주 조건
  G-C가 끝나면 README.md, PROTOTYPE.md, DEFERRED.md,
  DECISIONS.md, HANDOFF.md(완료)를 갱신하고 보고한다.

════════════════════════════════════════
실획 — 들어오면 최우선
════════════════════════════════════════

sessions/에 획이 들어오면 다른 작업보다 먼저 측정한다.
없으면 기다리지 않고 계속 진행한다.

이번 접근이 끝점 오차에 덜 민감하므로 AS-12 의존이 줄지만,
축 판정(AS-6)은 그대로 실획에 달려 있다.
