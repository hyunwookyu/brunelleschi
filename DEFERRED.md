# DEFERRED.md — 우회·단순화·미룬 항목

A-2가 우회를 허용한 자리와, 계획서가 지정했으나 지금 하지 않는 것.
**사실과 원래 목표를 함께 적는다** — 나중에 되돌릴 수 있어야 한다.

> **2026-08-15 L 전환에서 새로 시작했다.** 이전 항목은 `docs/archive/DEFERRED_SG.md`.

---

## 2026-08-18 8차 항목 0 — 원격 갈래 병합이 남긴 것

| 미룬 것 | 왜 | 되살릴 조건 |
|---|---|---|
| **`basic_flow`의 `lifted` 기대가 3 → 4로 늘었다** — 되돌리지 않고 **적어 두고 넘어갔다** | 병합된 원격 갈래의 **미앵커 배치**(`placeUnanchored`·`planeAnchor`, D-L77)가 앵커 없는 넷째 획을 **지면에 임의 깊이**로 놓는다(실측: `snapStart: null` · `seg3d` y가 앞 셋과 같은 지면 −0.295 · z 3.499~6.995). 이 갈래의 옛 동작(3)이 **8차 지시 항목 6이 요구하는 것**이고 원격의 동작(4)이 **지금 배포돼 있는 것**이다. 병합에서 배포 동작을 임의로 되돌리면 항목 6 이전에 사용자가 보는 것이 또 한 번 바뀐다 | **항목 6-5**(`on_face` 임의 스냅 제거)가 지운다. 그때 이 수는 **3으로 돌아가야 하고**, 돌아가지 않으면 항목 6이 미이행이다 — 그 줄이 **양성 채널**이다(#30) |
| **`elevation_flow.json`의 `gate.reachability_value`가 `[1]`이다**(#40 · `scan_gate_reachability` 플래그 1건) | 원격 갈래가 신설한 게이트이고 도달 가능성 값이 **정확히 1**이라 #40이 잡는다 — 그 자리의 1은 대개 측정이 아니라 **구성의 귀결**이다. 병합 시점에 그 하네스의 팔을 다시 설계하는 것은 항목 0의 범위 밖이라 **플래그를 남긴 채 등재한다**(끄지 않았다 — #19) | `elevation_flow`를 다시 여는 항목에서 **1이 아닌 팔**(음성 대조의 실제 값)로 바꾼다. **항목 6**이 입면 흐름의 배치 규약을 바꾸므로 그때가 자연스러운 자리다 |
| **원격 갈래가 인용한 원장 해시 13곳이 낡았다**(`@509615a1` → 현재 `54346ad1`) | 원격 갈래는 `web/test/constants.ts`를 안 건드렸으므로 그 갈래의 해시는 **분기점(`63e4831`)의 값**이다. 병합으로 이 갈래의 상수가 이겼고 원장을 다시 돌렸으므로 그 인용들은 **값부터 다시 읽어야 한다**(해시만 고치면 #42 ③ 그 자체다) | 항목 0에서 **원장을 다시 읽어 값과 해시를 함께 고쳤다**(아래 항목 0 절). 남은 것은 `n_stale` 은퇴 하네스 셋뿐이다 |

---

## L 전환이 명시적으로 미룬 것

| 항목 | 원래 목표 | 지금 | 사유 |
|---|---|---|---|
| **프리핸드 보존** | CLAUDE.md 핵심 원칙 "획을 버리지 않는다" | **획이 직선 세그먼트가 되고 손떨림은 버린다** | 계획서 §1.1. 미루면 떨림 평면 선택·굽은 획 미배치·획 파라미터화·미리보기와 확정의 차이가 **전부 없어진다**. 남는 것은 시작점과 끝점 둘뿐이고 그게 SketchUp이 다루는 것과 같다. **`pts2d`를 계속 보존하므로 구조가 막지 않는다** — 세그먼트가 확정된 뒤 원본 획을 그 위에 얹는 **표현 계층**으로 되돌아온다 |
| 튜브 메쉬의 곡선 보존 | 획 그대로를 3D 튜브로 | 직선 렌더 | 위와 같은 이유. `tube.ts`는 남기고 입력만 두 점이 된다 |
| 면 생성(§6) | 획 루프 → 면 | 범위 밖 | L-A~L-D에 없다. AS-4("면 없이 형태가 읽힌다")의 근거가 좁다는 것은 기록돼 있다 |
| 곡선 획 처리 | AS-C8 (실획 26.5%) | 현(弦)으로 접힌다 | 프리핸드와 함께 돌아온다 |

## 실획 표본에 걸린 것 (표본이 0이다)

| 항목 | 무엇이 필요한가 | 왜 지금 못 하나 |
|---|---|---|
| **스냅 반경** | 손으로 그은 시작점이 겨냥에서 몇 px 어긋나는가 | Quick,Draw `closure_gap` 환산 0.028~0.051이 유일한 자릿수인데 그것은 *자기 시작점*으로 돌아오는 오차이지 *다른 획의 끝점*을 겨냥하는 오차가 아니다. **상한이라는 근거가 없다**(하한일 수도). L-B는 합성으로 스윕만 하고 값은 잠정으로 둔다 |
| AS-C1(표본 대표성) | 스타일러스·숙련자 획 | 수집 경로가 없다. `sessionExport.ts`가 남아 있으므로 붙일 자리는 있다 |
| AS-C5(2점 주점 가정) | 실제 손 스케치에서 주점이 이미지 중심인가 | 미측정. f 전체가 이 가정 위에 있다 |

## S·G에서 넘어온 미해소 (대상이 남아 있는 것만)

| 항목 | 무엇이 문제인가 | 어디서 |
|---|---|---|
| **구도가 하나뿐이다** | 모든 배치 측정이 요 35°·피치 15° 3점, 직육면체 하나 위에 있다. "구도가 결과를 바꾼다"고 말하면서 구도는 하나만 쟀다 | L-A 측정 하네스가 **구도를 여러 개** 만든다(1점·2점·3점이 승격으로 자연히 생긴다) |
| `scan_unread_fields`가 문자열 리터럴 안의 필드명을 읽기로 센다 | selfcheck의 알려진 한계. `tests/test_selfcheck_scan.py`가 못 박아 뒀다 | 그대로 |
| 저장 슬롯이 하나다 | S-9 잔여 | **판단했다(L-D.2, 2026-08-16): 하나로 둔다.** 자동 저장 하나 + `JSON` 내보내기/불러오기가 여러 벌을 만드는 경로다(파일이 슬롯이다). 슬롯 UI를 따로 두면 저장소 관리 화면이 필요하고 그것은 §9의 범위 밖이다(A-3: 범위를 넓히지 않는다). ⚠ **README가 "슬롯 하나"를 그대로 적고 있고 그것이 맞다** |
| iPad 실측 지연이 없다 | 지연 수치가 전부 데스크톱·개발 서버 | L-D 종단 검증 |

## L-A.7이 남긴 것

| 항목 | 원래 목표 | 지금 | 사유 |
|---|---|---|---|
| **브라우저 확인의 형태 오차** | 앱에서 참값 대조 | 배치 개수만 본다 | 그 분모는 L-A.4가 이미 "공짜 통과"라 적었다(리뷰어 [13]). 참값 대조를 앱에서 하려면 측정 하네스를 앱에 넣어야 한다 — 측정 경로와 앱 경로를 섞는 일이라(PITFALLS #17) L-D 종단 검증에서 Playwright로 한다 |
| **AS-L9 사람 도달 가능성** | 사람이 축 오차 0.5°(핸들 1~4px)에 드는가 | 미측정 | 대리값만 있다. L-D 종단 시나리오로 재거나, 자동화가 어려우면 **실사용 대기**다 |
| **작은 캔버스의 핸들 예산** | **부분 해소(L-B.1)** | 단일 뷰포트에서 가이드 **607~1328px** — 그러나 **여섯 중 둘이 요구치 미달**이다 | 옛 좌우 분할(490×672)에서는 304~358px이 한계였다. 단일 뷰포트(1280×675)에서 `min_guide_ratio` 0.45가 요구하는 **651px**에 대해 실제 길이는 607.6 / 618.5 / 1233.3 / 1254.6 / 1256.8 / 1327.7px다 — **짧은 둘은 여전히 잘린다**(잘린 비율 0.81 → 0.93으로 줄었을 뿐이다). **개선 배수는 가장 짧은 가이드로 읽는다**: 600px 행 1.28~4.33px 대 옛 300px 행 0.63~2.16px = **약 2배**(가장 긴 것으로 읽으면 4배가 나오는데, **카메라 정확도는 가장 나쁜 축이 정한다**). ⚠ 그 예산도 **핸들 하나만 움직인다는 가정** 위에 있다(`vp_homog`의 섭동은 단일 핸들이다). 사람은 핸들 12개를 만진다. `stage_browser.json` · `vp_homog.json@54346ad1` |
| **지렛대가 긴 획으로 획 수 늘리기** | AS-L10을 완전히 닫기 | 내부 격자선만 시험했다 | 늘린 획이 `1/k` 길이라 지렛대가 오히려 짧아졌다(리뷰어 [10]). 상자 여러 개·긴 모서리 경로는 **막히지 않았다**. 결론(자동 확정 없음)은 안 바뀌지만 반증의 범위가 좁다 |

## L-B.1에서 미룬 것

| 항목 | 무엇 | 왜 미루나 |
|---|---|---|
| **분포형 오차의 정확한 0을 selfcheck가 못 본다** | `lift_gate.json:seg_gap_px`는 min~max가 전부 0인데 새 "정확히 0" 규칙이 안 잡는다 — **꼬리(min·p10·p90·max) 제외 규약** 때문이다. `p90=0`이고 `max>0`인 분포는 두 규칙 어디에도 안 걸린다 | 꼬리 제외 규약을 건드리면 플래그가 폭증한 전례가 있다(**89 → 345**). "분포 블록 안에서 median이 아닌 자리가 0" 같은 좁은 규칙이 필요하고, 그 설계에 반례가 더 든다 |
| **핸들 예산의 다중 핸들 판** | `vp_homog`의 섭동은 **핸들 하나**를 옮긴다. 사람은 **가이드 6개 = 핸들 12개**를 만진다. 넷이 독립으로 어긋나도 축 오차 0.5°가 유지되는가 | 지금 수치(짧은 가이드 1.28~4.33px)는 **개별 핸들 예산**이고, 그 사실을 §5.2·§12·AS-L9·이 문서에 적었다. 다중 핸들 판은 AS-L9의 실사용 측정과 함께 보는 것이 맞다 |
| **`min_guide_ratio` 0.45가 여전히 잘린다** | 새 뷰포트(1280×675)에서도 요구 651px에 **가이드 여섯 중 둘(607.6·618.5)이 미달**이다 | **⚠ 원인 귀속이 틀렸다**(L-B.2가 정정). 늘리기를 넣은 뒤 길이는 **언제나 그 방향의 캔버스 안 현**이고 그림이 정하지 않는다. 캔버스 1280×675에서 가장 짧은 현(수직)은 **588.18px**(높이 675 − 여백 2×43.4)이라 **요구 651px에 원리적으로 못 닿는다** — 수직에 가까운 가이드는 **어떤 그림에서도** 미달이다. 미달 **164개 중 98개**가 정확히 그 588px 대역이다(`extend_ceiling`, 분모는 **카메라가 서는 58장면 × 6 = 348**이다 — `guides_below_required`의 540과 **다른 모집단**이다). 고치려면 `min_guide_ratio`를 캔버스 짧은 변 기준으로 다시 정하거나 세로를 키워야 한다 |
| ~~**참 카메라에서도 `coarse` 배치가 0.44**~~ **해소(원인 특정) 2026-08-16** | `lift_grade.json@54346ad1`이 분해했다: **지배항은 축 판정**이다(미배치 사유 `축이 미분류다` coarse 533). 오라클 축이면 0.44 → 0.82이고 **등급 격차가 0.19 → 0.05로 거의 사라진다**. 그다음이 접합 반경(오라클 축 + `touch_ratio` 0.09면 1440/1440). **조건수는 "못 푸는" 문제가 아니라 "틀리게 푸는" 문제**다(`solveNull` 실패 0건, 형태 오차 중앙 0.10~0.18) | ⚠ **"L-B.3 스냅 뒤에 다시 잰다"는 전제가 틀렸다.** 스냅 대상은 3D 레이어인데 **초기 확정 순간에는 3D가 없다** — 스냅이 이 값에 닿지 않는다. 남은 일은 **L-B.4(확정 시점 축 판정)**과 **L-B.7(승격 연쇄)**이고, 고칠 때마다 조용히 틀림이 함께 느는 것을 봐야 한다(#16) |

## L-B.2에서 미룬 것

| 항목 | 무엇 | 왜 미루나 |
|---|---|---|
| **`min_guide_ratio` 0.45가 세로 가이드에 도달 불가** | 캔버스 1280×675에서 수직 현의 상한이 588.18px인데 요구는 651.2px다 | 임계를 **캔버스 짧은 변 기준**으로 다시 정하는 것이 맞아 보이나, 그 값을 정하는 근거가 `vp_homog`의 길이 스윕뿐이고 **길이만으로는 예산이 안 정해진다**(각차가 함께 정한다 — L-B.2 브라우저에서 1233px 축이 1226px 축보다 예산이 좁았다). 각차·길이 2차원 스윕이 필요하다 |
| ~~**초안이 카메라를 못 세우는 30/90이 `recover_failed`다**~~ **해소 2026-08-16** | `draft_recover.json@509615a1`이 특정했다: 지배항은 **지지선 선택**(가장 긴 둘이 하필 나란해 각차 최소 **0.03°**)이고, 무한원 스냅은 9/90만 설명한다. `len_x_sep` + 빈 축 중립 채우기로 **31/90 → 58/90**(D-L16). ⚠ **확정률만 보면 안 된다**(#16) — 어느 팔에서도 **축 오차 중앙 9~14°**로 임계 0.5°의 18~28배다. 초안 카메라는 어차피 못 쓰고 가치는 **끌기 시작 위치**에 있다. 중립 시작(13.83°)보다는 낫다는 것이 확인됐다 | — |
| **민감도 표시가 사용자의 조정을 실제로 돕는가** | 값을 화면에 냈으나 효과는 미측정 | 지표는 정의의 귀결이고(#3) "보여 주면 나아진다"는 별개의 주장이다. AS-L9의 실사용 측정과 함께 본다 |

## L-B.3에서 미룬 것

| 항목 | 무엇 | 왜 미루나 |
|---|---|---|
| **차수 승격에서 스냅이 조용히 풀린다** | 스냅은 `pts2d[0]`을 **대상의 상**으로 옮기는데, 그 상은 **그 카메라에서만** 그 대상이다. §6.1은 승격 시 `pts2d`를 보존해 전부 처음부터 다시 올리므로, 새 카메라에서는 옮겨 놓은 점이 대상의 상이 **아니다**. `snapStart.ofId`를 새 카메라로 **다시 풀지 않으면** 스냅이 소리 없이 풀린다 | **L-C(차수 승격)의 일이다.** `applySnapToStart`에 경고를 달아 두었다. 고치는 방향은 정해져 있다 — `snapStart`가 이미 `kind`·`ofId`를 들고 있으므로 승격 시 그 대상의 **새 상**으로 다시 옮기면 된다. 다만 대상 자체가 승격에서 다시 풀리므로 **순서**(어느 획을 먼저 올리는가)가 문제가 되고 그것이 L-C의 범위다 |
| **스냅으로 못 고치는 몫**(D-L15) | 겨냥 오차 / 대상 간격의 비가 1/2를 넘으면 **어떤 반경으로도** 이웃 꼭짓점을 문다. k3에서 15076/38880이다 | L-B.4·L-B.5·L-B.7을 지났는데 **아무것도 이 비를 안 바꾼다** — 축 판정도 고정도 승격도 **시작점이 어디에 붙는지**를 안 바꾼다. 남은 후보는 **이어 그리기 문맥**(직전 획의 끝점을 다음 획의 시작으로 우선)이고 그것은 계획서에 없다. **실획 표본이 오면 그때 본다** |
| **놓인 것의 19%가 조용히 틀린다** | 반경 0.05에서 배치 **2503/3240**(`end_jitter > 0`), cut 0.2 조용히 틀림 **474/2503** | **L-B.4가 답했다: 임계로는 못 고친다**(45조합에서 단조 맞바꿈). ⚠ **그리고 절단 0.5에서는 실시간이 확정 시점보다 나쁘다**(14.8% 대 7.9%) — 앵커는 작은 틀림을 줄이고 **큰 틀림을 늘린다**. 남은 몫은 **L-B.7 승격 연쇄**다 |
| **`LIVE_TOL.axis_deg = 12`의 근거가 약하다** | L-B.4가 4~35°를 쟀다. ⚠ **등록 규칙이 기준값만 고를 수 있는 구조**였으므로 이 항목은 **잠정값을 재확인했을 뿐**이다. 한계증분으로 보면 급변은 **18과 25 사이**이고(초판의 '무릎 12~18'은 희석된 평균이었다), **cut 0.5로 읽으면 4°가 뚜렷이 낫다**(9.0% 대 14.8%) | 절단을 무엇으로 읽을지가 **승격 연쇄의 회수율에 달렸다** — 큰 틀림을 줄이려 조이면 미배치가 늘고, 그것이 회수되는지가 L-B.7이다. **그 뒤에 다시 본다** |
| **`on_face`의 대상이 지면 하나다** | 면 생성이 범위 밖이라 다른 면이 없다. 그리고 지면 스냅의 화면 거리는 **정의상 0**이라 성공률 측정에 못 섞는다 | 면 생성(§6)이 L-A~L-D 범위 밖이다. 면이 생기면 `SnapCtx`에 넣기만 하면 된다 — 구조는 막지 않는다 |
| **`min_guide_ratio` 0.45 재정의**(L-B.2에서 이월) | 캔버스 1280×675에서 수직 현의 상한이 588.18px인데 요구는 651.2px다 | 여전히 미해소다. L-B.3(b)의 채우기가 **가이드 수**를 늘렸을 뿐 길이 상한은 캔버스가 정한다(요구 미달 347/540 → 249/540). 각차·길이 2차원 스윕이 필요하다는 판단은 그대로다 |

## L-B.7에서 미룬 것

| 항목 | 무엇 | 왜 미루나 |
|---|---|---|
| **L-B.6(뷰 시스템)이 남았다** | 계획서 §11의 순서는 B.6 → B.7인데 **B.7을 먼저 했다**(사유는 `progress.md`의 L-B.7 절: 열린 결정 셋이 전부 회수율에 달려 있었다) | `doc.ts`에 자료구조(`SView`·`deleteView`·`pending(viewId)`)는 이미 있고 **UI 배선만 남았다**. 다음 세션의 첫 항목이다 |
| **승격이 품질을 개선하지 않는다** | 회수된 720의 조용히 틀림이 cut 0.2에서 **392/720 = 0.544** 대 확정 **874/2416 = 0.362**다. ⚠ **격차의 대부분은 게이지 추정기다** — 자기 게이지로 재면 286/720 = 0.397이라 **같은 추정기끼리는 1.10배**다(리뷰어 [1]). 남는 ≈15pp가 **확정된 틀과 배율이 안 맞는 몫**이다. ⚠ **1점 구도는 90/93 = 0.968이다**(리뷰어 [2]) | 승격은 **개수 회수**의 기전이지 정확도의 기전이 아니다. 정확도는 카메라(축 오차)가 정하고 그것은 AS-L9(사람이 0.5°에 드는가)에 달려 있다 — **표본 0이다** |
| **오라클 축이 아니면 일괄 기전이 안 돈다** | 축을 주면 **1154/1904(60.6%)**를 회수하는데(D-L24), 연쇄가 실제로 붙인 축 라벨로는 **24/966**뿐이다. 그 사이가 곧 축 판정의 몫이다. ⚠ **그리고 일괄은 더 많이·더 틀리게 놓는다**(675/1154 = 0.585) | **축 판정을 고치는 것은 L-B 범위 밖이다** — L-B.4가 임계로는 못 고친다고 쟀고(45조합 단조 맞바꿈) 남은 후보는 카메라 정확도(AS-L9, 표본 0)다 |
| **`no_snap` 813이 승격의 상한이다** | 대기 획의 시작점이 확정 기하 근처에 **아예 없다**. 그 획들이 대기인 이유가 바로 안 이어졌기 때문이라 **구조적**이다 | 반경을 키우면 `unintended_snap`이 는다(D-L14). 다른 정보(이어 그리기 문맥)가 필요하고 계획서에 없다 |
| **품질 우선 목적함수에서는 조이기가 이긴다** | D-L18은 등록 규칙(**두 조건의 연언** — 개수 우선이 아니다, 리뷰어 [10])대로 **기각**했으나, 조이기가 조용히 틀림을 cut 0.2 **0.4037 → 0.329**로 낮춘다(고정 게이지) | **규칙을 사후에 바꾸지 않는다**(#28). 목적함수를 바꾸려면 **바꾸는 사유가 측정 밖에서** 와야 하고(예: 사용자가 조용히 틀린 배치를 얼마나 싫어하는가) 그것은 표본이 없다 |

| **연쇄 수렴이 픽스처의 성질일 수 있다**(리뷰어 [7]) | 장면당 획이 **12개**(상자 하나)라 연쇄 깊이 4는 그래프가 정하는 상한에 가깝다. §9.1의 "떨어진 덩어리도 연결되는 순간 전부 올라간다"는 **두 번째 덩어리가 없어 이 픽스처로 확인 불가**다 | 상자 둘 이상 픽스처가 필요하고 그것은 `scene3d`에 없다. 만들면 검출·확정 경로의 조건도 함께 바뀌므로 **L-B 게이트 재측정과 함께** 해야 한다 |
| **게이지 선택이 기계가 읽는 필드로 안 남는다**(리뷰어 [8]) | 정의 해시는 `metrics.ts` 소스만 덮는다. 어느 하네스가 `fit` 대신 `1`을 넘겨도 해시는 그대로다 — **이번 사고의 원인이 바로 그 인자였다** | 산출물에 `gauge: "fit"|"fixed"|"one"` 필드를 넣고 selfcheck가 대조하는 것이 맞다. 다만 하네스마다 게이지가 집합별로 다를 수 있어(확정 fit + 회수 fixed) **필드 하나로는 안 된다** — 설계에 반례가 더 든다 |
| **인용 규약이 정의 판을 못 가른다**(리뷰어 [9]) | `promote.json@54346ad1`이 옛 `segErr` 판과 새 판을 **동시에 가리킨다**(상수 해시가 같았기 때문이다) | 규약을 `파일.json@상수+정의`로 넓히는 것이 맞아 보이나, 기존 인용 수백 곳을 한꺼번에 고쳐야 한다. **다음 원장 갱신에서 새 인용부터 적용한다** |
| **층별 수치에 시드 변동폭이 없다**(리뷰어 [16]) | 등급 셋의 차이(0.34~0.43)를 "안정"이라 적었는데 시드 폭을 안 쟀다 | 시드별 분해를 내려면 셀이 6배로 쪼개져 셀당 표본이 40 아래로 간다. **폭을 재려면 시드를 늘려야 하고** 그것은 하네스 실행 시간 문제다 |

## L-B.7 리뷰어가 남긴 것 — **12행 중 11행이 닫혔다**(2026-08-16, L-B.M)

닫힌 것은 `progress.md`의 **L-B.7 리뷰어 잔여 대응** 표에 결과와 함께 있다: **[1] [2][3] [4](원장 측) [5] [6] [10] [11] [12] [13] [15] [16]** — 표의 12행 중 11행이다.
아래에는 **남은 것만** 둔다. **L-B.M 자신에 대한 리뷰어 17건은 위 표들에 흡수했다.**

| # | 항목 | 무엇 |
|---|---|---|
| [14] | **`loosen` 팔이 사전 등록에 없다** | 등록은 기준·조이기 둘인데 원장은 셋이다. 그리고 늘리기의 앵커 결과(3343/4320 · cut 0.2 **0.4493**)가 본문에 없다 |

## L-B.M 리뷰어 2회차가 남긴 것 (17건 중 대응 못 한 것)

| # | 항목 | 무엇 |
|---|---|---|
| [11]의 [14] | **`loosen` 팔이 사전 등록에 없다**(1회차 [14]에서 이월) | 등록은 기준·조이기 둘인데 원장은 셋이다. 판정에 안 쓰므로 결론은 안 바뀌지만 **등록과 실행이 어긋난 자리**다. 지우면 늘리기 방향의 정보가 없어지고, 등록을 사후에 고치면 #26을 어긴다 — **다음 항목의 사전 등록에 셋을 다 적는다** |
| [16] | **시드 폭이 층별 수치에는 여전히 없다** | 표제 수치 둘(자기 게이지 배수 · 오라클 배수)은 이번에 쟀다. **층별**(등급·잡음·구도)은 셀당 표본이 85~800이라 시드 여섯으로 쪼개면 셀이 14~133이 된다 — **폭을 재려면 시드를 늘려야 하고** 그것은 하네스 실행 시간 문제다 |
| — | **배율을 확정 틀에 맞추는 기전이 없다** | 승격이 더 내는 오차가 **전부 배율**이라는 것이 이번에 확인됐다(형태는 시드 폭 안). 앵커 경로는 시작점만 확정 기하에 붙이고 **끝점의 깊이는 축 방향과 커서가 정한다** — 그 둘이 확정 틀의 배율을 안 본다 | 고치려면 회수 획의 배율을 확정 집합에 맞추는 단계가 필요한데, 그것은 **앵커 하나로 정해지지 않는다**(끝점이 자유롭다). 두 점이 붙는 경우에만 가능하고 그것은 다른 기전이다 |
| — | **1점 구도에서 승격이 무너진다** | 회수한 93획 중 **90획이 조용히 틀리고 연쇄가 0**이다. 확정도 268/422로 나쁘다 | 구도별 분기를 두려면 **어느 구도인지 앱이 알아야** 하는데 그것은 `Camera.order`로 있다. 다만 **1점 구도에서 아예 승격을 끄는 것**이 옳은지는 사용자 표본이 없다(미배치가 늘고 그것이 §9.1의 대기다) |
| — | **연쇄의 여러 회 반복을 브라우저에서 안 봤다** | 원장은 하네스에서 4회까지 봤고 `l_b8`은 앱에서 **1회 연쇄**만 본다 | 앱의 `promoteChain`은 8회 상한으로 돌지만 **여러 회가 실제로 도는 픽스처**를 브라우저에 넣으려면 상자 둘 이상이 필요하다(같은 이유로 [7]이 미뤄졌다) |

## 절단 곡선이 남긴 것 (리뷰어 3회차)

| 항목 | 무엇 | 왜 미루나 |
|---|---|---|
| ~~**앵커/확정의 우열이 비용비 λ에 달렸다**~~ **닫힘 2026-08-16 — 사람이 λ를 정했다(D-L25)** | **λ = 3 · 절단 0.5**를 사전 등록 목적함수로 박았다. 근거는 비용의 비대칭이다 — **미배치는 유예되고**(§9.1 대기) **틀린 배치는 영구이며 그 위에 구조가 쌓인다**. ⚠⚠ **그 칸에 측정 지지가 없다**(리뷰어 2회차): 여유 **0.0025**(8획)에 대해 **시드 부호가 3 대 3으로 갈리고 폭이 0.2166**이다(`axis_live.json@54346ad1`의 `seed_spread_at_dl25_point`). "λ≥5에서는 뚜렷하다"·"λ=3은 하한이다"도 같은 열이라 함께 무너졌고 **철회했다**. **D-L25는 정책만으로 선다.** ⚠ 그 정책의 전제(미배치는 유예)도 **AS-L11에서 절반만 참**이다 — **1점 구도는 연쇄가 0이라 영구 미배치**이고 **구도별 λ는 미측정이다**(아래 새 행) | — |
| **등급별 임계 분기를 팔 *안에서* 안 쟀다** | 낸 것은 팔 **사이**의 교차점이고, 앵커 팔은 오라클 대상이라 **등급 효과가 구성상 안 나온다**(#31). 진짜 물음은 "`LIVE_TOL.axis_deg`의 최적점이 등급마다 다른가"다 | `live_time.rows`가 등급 합산이라 쪼개야 하고, 쪼개면 셀이 작아져 시드 폭이 결론을 삼킬 수 있다(#14). **실획 등급을 모르는 채로 분기를 두는 것 자체가 근거가 없다**(AS-C1) |
| **⚠ λ = 3이 D-L21(축 판정 임계)을 되열었다 — 아직 안 뒤집었다** | D-L25의 목적함수로 `promote.json@54346ad1`의 세 팔을 다시 채점하면 **조이기가 이긴다**. 절단 0.5·λ=3에서 (놓였고 맞은 것 − 3×조용히 틀린 것)/4320이 기준 **(2742 − 3×394)/4320 = 1560/4320 = 0.361** · 조이기 **(2642 − 3×334)/4320 = 1640/4320 = 0.380** · 늘리기 **963/4320 = 0.223**이다. 절단 0.2에서도 조이기가 이긴다(−0.218 대 −0.446). 교차는 **λ=1과 λ=2 사이**다(λ=1: 기준 0.5435 대 조이기 0.5343으로 기준이 이긴다) | **시드 폭을 안 쟀다**(#14). `promote.json`에 시드별 분해가 없고 차이 80/4320은 L-B.4가 실측한 배치 시드 폭 **31**과 같은 자릿수다. 뒤집으려면 **하네스에 시드별 출력을 넣고 다시 돌려야** 하고, 뒤집으면 `AXIS_TOL` 상수가 바뀌어 그것에 의존한 산출물이 전부 STALE이 된다(D-C4). **L-D 종단 검증에서 함께 본다** — 그때가 상수 변경을 흡수할 유일한 자리다 |
| **`end_jitter = 0` 못박힘이 다른 원장에도 있는지 전수 확인 안 함** | `axis_live`의 실시간 팔에서 재발했고 `snap.json`이 서명(`p10 = 0`)을 규정해 놨다. **selfcheck가 이것을 못 본다** — 꼬리(min·p10) 제외 규약의 사각지대이고 DEFERRED에 이미 등록된 항목이다 | 규약을 건드리면 플래그가 폭증한 전례가 있다(89 → 345). "분포 블록에서 `p10 = 0`이고 `median > 0`" 같은 좁은 규칙이 필요하고 그 설계에 반례가 더 든다 |
| **부분집합 264획의 사유가 안 셌다** | 확정 2416 − 1979 = 437인데 카운터는 173(회수 0인 장면)이다. 나머지 264는 `kFix`가 없는 장면(확정 0)에서 앞 단계에 빠진 것이고 **따로 안 셌다** | 두 사유를 갈라 세려면 카운터를 하나 더 놓아야 한다(#10 — 뺄셈으로 만들지 않는다). 부분집합 효과 1.3pp의 해석에 영향이 있다 |
| **인라인 지표식 전수 확인** | `axis_live`에서 인라인 식 하나가 나왔다. **정의 해시로 못 잡힌다**(하네스가 `metrics.ts`를 아예 안 부르면 해시가 그대로다) | 30개 하네스의 모든 산술식을 사람이 읽어야 한다. 기계로 하려면 "오차류 이름의 지역 변수에 `/diag`가 들어간 식"을 정적 탐지해야 하고 그것은 `selfcheck`의 새 규칙이다 |

## L-C.1이 남긴 것

| 항목 | 무엇 | 왜 미루나 |
|---|---|---|
| **승격이 배치를 줄인다(−168)** | 소실점이 셋이 되면 `classifyStroke`가 더 까다로워진다 — 후보가 늘어 `ambiguity`가 더 자주 걸린다 | 임계를 승격 뒤에만 풀면 **차수마다 다른 임계**가 되고 그것은 L-B.4가 "임계로는 못 고친다"고 쟀던 자리다. 그리고 **미배치는 대기**이므로(§9.1) 줄어든 배치가 앵커로 회수될 수 있다 — 그것을 안 쟀다 |
| **`snapStart`가 매개변수를 안 들고 있다** | 승격 때 옛 스냅 점이 대상 위 어디였는지 되돌려야 하는데, **끝점·중점 말고는 한 대상만으로 안 정해진다**(교차점·수선 발·선 위는 두 선분의 관계다). `a`쪽 끝점으로 근사한다 | `snap.ts`의 `SnapCand`에 매개변수를 넣는 자료구조 변경이고, 교차점은 **두 대상의 매개변수 쌍**이라 필드 하나로 안 된다. 근사의 대가는 `drift_px`에 섞여 있고 **갈라 안 쟀다** |
| **못 살린 스냅 295개**(`order_undo.json@54346ad1`. ⚠ **초판의 483은 가짜 승격 대조군이 섞인 값이다** — #9) | 대상이 새 카메라에서 안 놓이면 그 스냅은 옛 상 그대로 남는다 — **조용히 틀린 시작점**이다 | 대상이 안 놓이는 이유가 축 판정이고(위 항목과 같은 뿌리) 그것이 L-B.4의 미해소다. **그 획을 2D로 되돌리는 것**이 A-3에 맞을 수 있으나 §9.1의 대기와 구분이 필요하다 |
| **승격 뒤 뷰 카메라 갱신을 참값으로 안 쟀다** | `promoteOrderNow`가 뷰의 눈 위치를 기하 배율로 같이 옮긴다. 브라우저 확인은 있으나 **"상대적 시점이 유지되는가"를 참값으로 안 쟀다** | 뷰 자세의 참값이 없다 — 합성 장면에 "사용자가 보던 각도"라는 참값이 없기 때문이다. L-D 종단 시나리오에서 **화면 대조**로 볼 수는 있다 |

## L-C.2가 남긴 것

| 항목 | 무엇 | 왜 미루나 |
|---|---|---|
| **돌린 뷰에서는 승격 손실이 안 보인다** | `drawPromoteLoss`가 `stage.isPinned`일 때만 그린다 — `pts2d`는 **확정 카메라의 화면 좌표**라 돌린 뷰에 그리면 화면에 붙어 따라다니는 유령이 된다(`drawBelowInk` 머리말과 같은 이유) | 돌린 뷰에서 보이려면 **내려간 획의 옛 3D**를 들고 그 시점으로 되쏘아야 하는데, 내려간 획은 정의상 3D가 없다. 승격 **직전**의 3D를 따로 들고 다녀야 하고 그것은 자료구조 변경이다. 표시가 필요한 순간은 승격 직후이고 그때는 확정 시점이므로(`promoteOrderNow`가 `pinTo`한다) **지금 손실은 없다** |
| **차수 표식이 여럿일 때를 안 봤다** | 픽스처가 2 → 3 하나뿐이다. 1 → 2 → 3으로 두 번 승격하면 표식이 둘이 되고, `revertToOrder(1)` 뒤에 3점 표식이 남아 있는 것이 옳은지 안 정했다 | 두 번 승격하는 브라우저 픽스처가 필요하고 그것은 **상자 둘 이상**이 있어야 한다(L-B 게이트 3번·[7]과 같은 이유). **L-D 종단 검증에서 그 픽스처를 만들 때 함께 본다** |
| **끊긴 스냅의 재연결이 옳은 자리에 붙는지 안 쟀다** | `relinkLostSnaps`가 `snapAt`을 그대로 부르므로 **다른 대상에 붙는다**. 브라우저에서 3/4가 붙었지만 **참값 대조가 없다** | 참값 대조를 하려면 "그 획이 원래 붙어야 했던 3D 점"이 있어야 하는데, 대상이 새 카메라에서 **안 놓였기 때문에** 끊긴 것이라 그 점이 없다. 사용자가 누르는 명시 조작이고 되돌릴 수 있으므로 A-3을 안 어긴다 |
| **`md()`가 상태 줄 전체에 걸린다** | `**x**` → `<b>x</b>` 치환을 조립된 HTML에 한 번 건다. 넣는 문장이 전부 `mainL.ts` 안이라 지금은 안전하지만, **사용자 입력이 상태 줄에 들어오는 순간 위험해진다**(뷰 이름 같은 것) | 지금 상태 줄에 들어가는 사용자 문자열은 뷰 이름 하나이고 그것은 `renderViews`가 그린다(다른 요소다). 넣게 되면 그 자리에서 이스케이프해야 한다 |

| **구도별 λ가 미측정이다** | D-L25의 정책 전제("미배치는 유예된다")가 **AS-L11에서 구도마다 다르다** — 3점 구도는 연쇄가 돌지만 **1점 구도는 연쇄가 0**이라 미배치가 영구다. 유예가 없으면 λ가 1에 가까워야 한다 | 구도별 λ를 정하려면 **구도별 회수율**과 **사용자가 각 구도에서 미배치를 얼마나 참는지**가 있어야 하는데 후자는 표본 0이다(AS-C1). 그리고 **구도별 임계 분기 자체가 이 프로젝트에서 아직 근거 없는 조치**다(같은 이유로 등급별 분기가 DEFERRED에 있다) |

## L-C.2 리뷰어 2회차가 남긴 것 (14건 중 대응 못 한 것)

| 항목 | 무엇 | 왜 미루나 |
|---|---|---|
| **`order_promote.seed_spread`에 정확히 1.00이 있다** | 시드별 (승격 후/전) 조용히 틀림 비가 [0.8, 0.833, 0.886, 0.946, 0.975, **1**]이고 `crosses_one: false`다. "1을 안 넘는다"는 참이지만 **한 시드에서 개선이 0**이라 "품질 개선은 시드 폭 밖"이 경계에 닿아 있다. 정확히 1.0은 selfcheck의 의심 대상이기도 하다(§5.1) | 다시 적으려면 그 시드의 전후 **개수**를 갈라 세야 하고(비가 아니라 분자/분모, #14) 하네스에 시드별 분자·분모를 더 넣어야 한다. 결론(승격이 나빠지지 않는다)은 안 바뀌고 **강도만 약해진다** — 표현은 "다섯 시드에서 개선, 한 시드에서 동일"이 정확하다 |
| **게이지 적합 모집단 차이의 몫을 안 갈랐다** | `dl25_rescore`의 두 팔이 **1122획과 954획**에서 각각 게이지를 적합한다. D-L22가 같은 종류의 비교에서 **부분집합 효과를 1.3pp**로 실측했고 여기 합산 여유는 **2.6pp**다 — 여유가 이미 알려진 편향의 **2배 안**이다 | 가르려면 승격 후 팔의 게이지를 **승격 전과 같은 획 집합**에서 적합해 봐야 하는데, 그 집합의 일부는 승격 후에 안 놓여 있다(`only_before` 570). 그러면 게이지를 못 낸다. **결론에는 영향이 없다** — 시드 부호가 이미 갈려 무결론이기 때문이다 |
| **`held_but_not_compared` 다섯을 대조에 안 넣었다** | `takeSnap`은 `{...s}`로 `id`·`userAxis`·`viewRef`·`color`·`width`를 담지만 `snapDiff`가 안 본다. **복원은 되고 복원 실패를 잡는 능력이 없다** | 지금 승격·되돌리기 경로가 이 다섯을 안 건드리므로 대조에 넣을 이유가 없었다. **건드리게 되면 그때 넣는다** — 넣는 비용은 `snapDiff` 몇 줄이고, 지금 넣으면 안 도는 검사가 늘 뿐이다 |
| **일반 `실행취소`의 파생 복원이 브라우저에서 미확인** | `l_c2`의 `undo_restores_camera`는 가이드가 6 → 6이라 `vps`·`f`·`fSource`의 복원을 시험하지 않는다. 확인된 것은 `revert_by_order` 경로 하나다 | 가이드를 바꾸는 조작을 `실행취소`로 되돌리는 브라우저 픽스처가 필요하다(가이드 끌기 → 실행취소). 노드 쪽 `trials`의 `guides`·`guides_add`가 같은 자리를 덮으므로 **기전은 확인돼 있다** — 없는 것은 앱 경로의 두 번째 확인이다. **L-D 종단 검증에서 함께 본다** |

## L-C.3이 남긴 것

| 항목 | 무엇 | 왜 미루나 |
|---|---|---|
| **참값과 신호 사이를 메우는 관측 가능한 양** | **고정 게이지 참값으로는 오승격 두 종류가 둘 다 크게 갈린다** — `spurious` **0.9694** · `wrong_vp3` **0.5446**(둘 다 등록 기준 0.5 위). `tiltDeg`는 0.24 이하다. ⚠ **자기 게이지로 보면 0.4979 · 0.1265로 작아 보이는데 그것은 배율 성분을 지운 수다**(AS-L11). **잴 것은 많고 관측 가능한 신호를 못 찾은 것이다** | 계획서 §6.2가 적은 후보는 **둘**이었고 둘 다 시험했다. 새 후보를 세우려면 **어디를 볼지**가 있어야 하는데, 남은 것은 전부 §5.3이 반증한 유형(입사 관계에서 유도한 양)이거나 참값이 필요한 양이다. **AS-L9(사람이 도달하는가)의 표본이 오면 함께 본다** |
| **`wrong_vp3`의 섭동 크기가 하나뿐이다** | VP3를 **(+400, +900)px** 옮긴 한 점만 쟀다. **축 각오차 중앙 5.59°**(AS-L9 임계의 11배)이고 고정 게이지 형태 오차가 **2.85배**다 — 작은 섭동에서는 어떻게 되는지 모른다 | 동작점 하나다(#12). 스윕하려면 오프셋 격자가 필요하고, **그보다 먼저 물어야 할 것은 "사람이 VP3를 얼마나 틀리게 놓는가"**인데 그것이 AS-L9이고 **표본 0**이다. 임의 격자를 스윕하면 답이 격자가 정한다 |
| **`no_signal` 장면의 성격을 안 갈랐다** | 참 팔 20/216(9.3%) · 가짜 팔 3/72(4.2%) · 틀린 VP3 팔 21/216(9.7%)로 **탈락 비율이 다르다**. ROC 분모가 **서로 다르게 정제된 집합**이다(#11). `no_signal_by`에 어느 셀인지는 남겼다 | 탈락 장면의 형태 오차를 재려면 **승격 후 배치가 0인 장면**의 참값 대조가 필요한데 그 장면은 놓인 획이 없어 게이지가 안 선다. 결론(무신호)이 탈락 방향에 안 달렸다 — 탈락한 장면이 전부 한쪽으로 갔다고 가정해도 판별력이 0.5에 못 닿는다 |
| **주점을 고정한 f² 편차** | 후보 ②가 죽은 이유는 **주점을 수심으로 잡는 것**이었다. 원장 자신이 "잴 수 있으려면 주점이 **독립으로 주어져야** 한다(예: 2점 경로의 이미지 중심 가정)"고 적었다 — 그것은 **관측 가능한 후보 하나를 명시한 것**이고, DEFERRED 초판이 "남은 것은 전부 참값이 필요하다"고 닫은 것은 **너무 넓었다**(2회차 리뷰어 [9]) | 주점을 이미지 중심에 고정하면 세 f²의 편차가 **VP 오차와 주점 가정 위반을 섞는다** — AS-C5(주점이 이미지 중심인가)가 **미검증**이라 그 둘을 못 가른다. 실획 표본이 오면 AS-C5와 함께 본다 |
| **`tiltDeg`의 f 성분을 빼면 남는가** | 실패 원인이 "f 오차와 축 오차를 한 수에 섞는다"로 특정됐다. f가 알려진 조건에서 그 몫을 빼면 축 오차만 남을 수 있다 | 앱에서 f는 **소실점에서 나오므로**(이론서 6.2·6.3) 소실점이 틀리면 f도 틀리다 — **뺄 기준 f가 없다.** 2점 경로의 렌즈 설정값이 그 자리일 수 있으나 그것은 사용자 입력이고 표본 0이다 |

## L-D.1이 남긴 것

| 항목 | 무엇 | 왜 미루나 |
|---|---|---|
| **축 지정이 획을 안 올린다**(사전 등록 ②의 답이 **0/2**) | 원인 둘을 특정했다: ① `solveInto`는 **대기 집합끼리만** 풀어 앵커가 없다(L-B.7이 일괄 **0/1904**로 이미 쟀다) ② `promoteChain` → `resolveLive`가 **축을 다시 추론**해서 `userAxis`를 무시한다 | 고치는 방향은 정해져 있다 — `resolveLive`가 **이미 축 고정 인자를 받으므로**(Shift가 쓰는 그것, D-L19) 사용자 지정 축을 거기로 넘긴다. **바깥에서 덮으면 안 된다**(D-L19). ⚠ 다만 그것은 **§7의 실시간 경로를 건드리는 변경**이라 **오배정이 함께 느는지 측정이 붙어야 한다**(#16 — 병목이 옮겨간 것을 없어진 것으로 읽지 않는다). 컨텍스트가 남지 않아 다음 세션으로 넘긴다 |
| **면 지정이 아니라 축 지정이다** | §9.5는 "'이 획은 저 면 위에 있다' 수준"이라 적었는데 **면 생성이 범위 밖**이라 방향만 줄 수 있다. 둘은 다르다 — 면 지정은 획을 **평면에 가두고** 축 지정은 그러지 않는다 | 면 생성(§6)이 L-A~L-D 범위 밖이다. 면이 생기면 `SnapCtx`에 넣기만 하면 되고(L-B.3이 같은 말을 적었다) 구조가 막지 않는다 |
| **지운 획에 붙어 있던 스냅** | 개수를 세어 알리지만 그 획들을 2D로 되돌리지 않는다 | 이미 놓인 3D는 좌표가 있고 그것을 지우는 것은 **사용자가 안 시킨 삭제**다. 되돌리려면 "스냅이 끊긴 획을 2D로 내린다"는 정책이 필요하고 그것은 §9.1의 대기와 구분이 필요하다(L-C.2가 같은 자리에서 같은 판단을 했다) |

## L-D.1 리뷰어가 남긴 것 — **결론이 철회됐다**

| 항목 | 무엇 | 왜 미루나 |
|---|---|---|
| ⚠⚠ **"0/2는 결함"을 철회했다 — 축 지정이 옳은 답이 아닐 수 있다** | `face_diagonal.json@54346ad1`가 **바로 이 픽스처**에 대해 "**축으로 배정된 사선이 놓인 것은 회수가 아니라 조용히 틀린 배치다**"라고 적었다(0.0216 대 **0.4483**, 20배). 같은 원장의 접근은 **"면은 축 둘이 편다 + 앵커가 위치를 정한다"**로 미분류 사선의 **84.16%**를 놓는다 | **다음 세션이 먼저 볼 것**: 축 지정을 고치기 전에 **`face_diagonal`의 면 경로를 §9.5에 붙일 수 있는지** 본다. ⚠ **`DEFERRED`가 적었던 수정 방향(사용자 축을 `resolveLive`에 넘긴다)은 위험하다** — 이 픽스처에서 2/2가 놓이지만 그 둘이 0.4483 자리일 수 있다. **고친 뒤 개수만 세면 안 된다**(#9·#15) |
| **원인 ①의 근거를 철회했다** | "일괄 0/1904"는 이 저장소가 **미실행**이라 철회한 수다(#32·D-L24). 같은 원장에 **승격 없이 대기 집합만 풀면 207/1904(10.9%)**가 있다 | 남는 사실은 **이 픽스처의 대기 집합이 2획**이라는 표본 크기이고, 그러면 원인은 기전이 아니라 픽스처다. **다시 재려면 대기 집합이 큰 픽스처**가 필요하다 |
| **사전 등록 ③이 미판정이다** | "차수 승격을 걸어도 `userAxis`가 유지된다"를 등록해 놓고 **승격을 한 번도 안 걸었다**. `promoteOrder`의 코드를 읽어 통과 처리했다 — **코드 읽기는 카운터가 아니다**(#7) | 판정하려면 `l_d1`에 승격 단계를 붙이면 된다(`l_c1`·`l_c2`가 그 절차를 갖고 있다). ⚠ 다만 **②가 0이라 사용자 축으로 놓인 기하가 0**이므로 ③이 관측 가능한 출력으로 확인될 수 있는 상태인지부터 봐야 한다 |
| **#35를 안 지켰다** — 오라클이 없다 | 등록 ②에 **"무엇이 이 기준을 넘을 수 있는가"**가 없어 **0/2가 축 지정의 성질인지 픽스처의 성질인지 안 갈린다**. L-C.3이 정확히 그 형태로 실패해서 #35를 만들었는데 **바로 다음 항목이 어겼다** | 오라클 팔은 "이 대각선 둘을 **어떤 경로로든** 놓을 수 있는가"이고 `face_diagonal`의 84.16%가 그 답에 가깝다 — **그 하네스를 §9.5 조건으로 다시 돌리는 것**이 다음 세션의 첫 일이다 |
| **원장 `l_d1`이 얇다** | 씬·뷰 소유 필드가 없는데 "통과"라 적었고(`snap_targets` 12→11이 `lifted` 12와 같은 수라 **독립 증거가 아닐 수 있다**), 등급·시드·jitter·dpr·`SIZE_HEAL.count`가 없고(#12·#14·#21·#22), **n = 2**이고, Esc 경로가 미측정이고, 축 번호 규약(0-기반)이 안 적혔다 | 필드를 더 내는 것은 싸다. **다시 돌릴 때 함께 넣는다** — 지금 넣으면 위의 결론 철회와 따로 돌게 된다 |

## PITFALLS #36 — **분모 0 보호가 12개 하네스에 남아 있다**(2026-08-16)

`scan_zero_denominator`가 `web/test`에서 **70곳**(파일 12개)을 찾았다:
`promote.test.ts` 35 · `axis_live.test.ts` 18 · 나머지 10개 파일에 17.

L-C.3에서 고친 것과 **같은 형태**다(#34: 한 자리에서 고친 것은 그 자리에서만 고쳐졌다).
전부 고치면 원장 12개를 다시 돌려야 하고 이번 항목의 범위를 넘는다(A-3).

**지금 하는 것**: 검사를 켜 두고 **파일당 한 건으로** 보고한다(70건을 그대로 내면 소음이 된다).
**다음에 그 하네스를 건드릴 때** `metrics.rate()`로 바꾼다 — 관측이면 `null`, 표시용 보호면
그렇게 적는다. 새 하네스는 처음부터 `rate()`를 쓴다.

⚠ **트리거를 하나 더 단다**(리뷰어 [3], 2026-08-16). "건드릴 때"는 기한이 아니라 조건인데,
`promote.test.ts`(35곳)와 `axis_live.test.ts`(18곳)는 **D-L21(D-L18 기각)의 근거 원장**이다 —
2976 대 3136도, cut 0.2의 0.4037 → 0.329도 거기서 나온다. 그 53곳이 **층화 비율(관측)**인지
**표시용 나눗셈 보호**인지 안 갈린 채이므로:

> **D-L21을 재개하기 전에 그 둘을 먼저 가른다.** 층에 분모 0이 있었다면 그 층의 비율은
> 없는 관측이고, D-L21의 두 팔 비교가 그만큼 흔들린다. 재개 없이 인용만 할 때도 같다 —
> **인용하는 칸의 분모가 0이 아닌지 그 자리에서 확인한다.**

## §9.5 지정 경로 — **면 지정은 §6과 함께 본다**(D-L28, 2026-08-16)

기존 원장으로 결론했다(새로 재지 않았다). `face_diagonal.json@54346ad1`:
축 배정 사선 **19/120**의 `corner_err_ratio`(끝점 거리 ÷ 상자 대각, **게이지 없음** —
D-L22의 형태 오차와 **다른 양이다**) 중앙 **0.4483**, 면 경로로 회수된 **85/101**의 중앙 **0.0216**.
⚠ **"20배"를 지웠다**(리뷰어 2회차). 두 수의 분모가 다르고, 더 중요하게는 두 집단이
**판정 대상인 그 분류기가 갈라 놓은 disjoint 부분집합**이다(19 = `by_reason.ok`가 **항등**이다).
짝지은 비교가 아니므로 배수로 읽지 않는다 — 남는 말은
**"축 경로는 자기가 가장 축답다고 고른 사선에서도 크게 틀린다"**이고 D-L28이 그 형태로 서 있다.
⚠ **비율보다 분자/분모로 적는다**(CLAUDE.md §5): 미분류 **101/120**, 그중 회수 **85/101**.
0.8417과 0.8416이 소수 넷째 자리까지 같아서 **비율로 적으면 다음 인용에서 안 갈린다**.

**그러므로 §9.5가 원하는 것은 면 지정이다.** 지금 넣지 않는 이유: 면 경로는 **평면(축 둘) +
앵커**를 요구하는데 L에는 면 개념이 없고(§6 범위 밖) 스냅이 앵커를 대신한다.
**다시 볼 조건**: §6(면)을 열 때. ⚠ **그대로 인용할 수 없다** — 그 측정은 **폐기된 배치 계층**(D-S14·S-6·S-8)의 것이고
`AS-5`는 지금 **무효**로 분류돼 있다(D-L1: 아카이브를 판단 근거로 인용하지 않는다).
§6을 열 때 **그 조건에서 다시 재야** 한다.


## L-D.0이 남긴 것 — **롤 0의 초안 효과는 안 쟀다**(2026-08-16)

| 항목 | 원래 목표 | 지금 | 트리거 |
|---|---|---|---|
| **롤 0 제약이 초안을 낫게 하는가** | 사람 지시: 게이트를 못 넘으면 "초안 품질이 얼마나 올랐는지 기록한다 — 조정 부담이 준 것만으로도 이득이다" | **안 쟀다.** `horizon.json@509615a1`이 잰 것은 **축 방향 오차**이고 그것은 초안의 지표가 아니다 — 초안이 하는 일은 **끌기 시작 위치**를 주는 것이고 그 지표는 `draft_recover.json`의 **확정률**(31/90 → 58/90, D-L16)과 **축 오차 중앙**(12.35 → 8.95°)이다. 축 오차로 대신 적으면 **#2**(대리값의 성질을 재는 것)다 | **`draft_recover` 하네스에 `roll_zero` 팔을 넣어 다시 돌릴 때.** 그 하네스는 `vpDraft`의 지지선 선택을 재므로, 롤 0을 **초안 적합 안에** 넣은 판이 확정률과 축 오차를 어떻게 바꾸는지가 직접 나온다. ⚠ 배치는 안 본다 — 초안은 놓는 경로가 아니다 |
| **롤 0의 손익분기가 5°인 것을 사람 표본으로 확인** | 종이를 기울여 그리는 사용자가 실제로 몇 도인가 | **표본 0**(AS-C1). 합성 픽스처는 롤 0으로 만든 것이다 | 실획이 `sessions/`에 들어올 때. `QUESTIONS.md` (d)와 같은 자리다 |
| **배치 붕괴의 자리를 특정 안 했다** | 소실점을 78~98px 옮기면 `liftAll`의 무엇이 거절하는가 | 미배치 사유별 카운터를 이 하네스가 안 냈다 | 롤 0을 다시 볼 때. `lift_grade`가 같은 종류의 분해를 이미 한다(미배치 사유별) |

## L-D.4가 남긴 것 — **이름과 실사용 취약점**(2026-08-16)

| 항목 | 원래 목표 | 지금 | 트리거 |
|---|---|---|---|
| **저장소 이름이 `SKETCH2SPACE`다** | 이름을 `brunelleschi`로 통일 | 코드·manifest·title·package·확장자(`.brnl`)는 **전부 바꿨다**. **저장소 디렉토리 이름만 못 바꿨다** — 그것은 git remote와 로컬 경로를 함께 옮기는 일이라 사람이 한다 | 사람이 리포지토리를 `brunelleschi`로 바꿀 때. 그때 README 1.5절의 배포 URL도 함께 정해진다 |
| **`PROTOTYPE.md` §5의 일곱** | 실사용 취약점을 고친다 | **기록만 했다**(사람 지시: 지금 고치지 말고 적을 것). ⚠ **수치가 없다** — 작도 과정을 따라가며 예상한 것이고 측정이 아니다 | 실획이 들어오거나 다음 계획을 세울 때. **수치가 붙으면 그 항목은 §1(알려진 결함)로 옮긴다** |
| **곡선이 카메라 입력이 될 수 있다** | 프리핸드와 함께 나중에 | 범위 밖 | ⚠ **우선순위가 다르다**: 이론서 8.7절 `d = (A/B)√(ab)`이 **타원 하나에서 시거리를 낸다**. 즉 곡선은 "표현"이 아니라 **카메라 확정의 입력**일 수 있다. §6(면)·프리핸드와 함께 볼 때 이것을 먼저 본다 |
| **v1 저장을 열 수단이 없어졌다** | 옛 문서 이전 | 옛 UI를 지웠으므로 **IndexedDB의 `current` 키를 읽는 코드가 없다** | 사람이 옛 문서를 꺼내 달라고 할 때. 데이터는 그대로 있으므로 읽는 스크립트를 한 번 짜면 된다(D-L29) |

## 옛 UI 삭제가 남긴 것 (2026-08-16, 리뷰어 지적)

| 항목 | 무엇이 빠졌나 | 트리거 |
|---|---|---|
| **#21의 진짜 회귀 팔이 없다** | `coords.spec`의 `[양성 채널]`은 **탐침의 눈**만 확인한다(캔버스에 직접 칠한다) — **앱의 좌표 변환 경로를 안 지난다.** "앱이 dpr을 두 번 적용해도 이 스펙이 실패한다"는 **미확인**이다 | 좌표 규약을 건드릴 때. 필요한 것은 **앱 쪽에 dpr 곱을 되살린 팔**이고, `canvasFrame.ts`에 시험용 배율 인자를 두면 된다(앱 기본값은 안 바꾼다) |
| **하네스와 앱이 같은 배치 옵션 출처를 쓰는지 아무도 안 본다** | 옛 `wiring.test.ts`가 그것을 봤는데 **대상(옛 `settle`)이 사라졌고**, "`stage.spec`으로 옮겨졌다"는 서술은 **철회했다**(D-L35) — 그 스펙은 참값 대조를 안 한다 | L의 배치 경로(`liftAll`·`placeLive`·`solveInto`)의 옵션을 늘릴 때. 그때 `appPlace.ts` 같은 단일 출처를 L용으로 다시 만든다 |
| **`view_vs_overlay`가 덮는 범위가 줄었는지 미확인** | 옛 UI는 **2D·3D 캔버스 둘**의 배율 불일치를 잡았고, 새 UI는 캔버스가 하나라 **잉크 ↔ three 렌더러 상자**를 본다. 옛 실패 모드(`setPixelRatio`가 잉크 규약과 갈리는 것)에 대응이 있는지 안 봤다 | dpr 규약을 다시 볼 때. `coords.json`의 `what_this_does_not_say`에 적어 두었다 |
| **옛 UI만 재던 것들** | `archive_W/e2e.json`의 고치기 조작 수·메쉬 크기·`fix_in_rotated_view`는 **새 UI에서 안 쟀다.** 그 표는 **회귀 감시용이 아니다**(다시 못 돌린다) | `고치기` 경로를 다시 볼 때 |

## 규칙 기반 전환이 미룬 것 (2026-08-16)

| 항목 | 무엇을 미뤘나 | 트리거 |
|---|---|---|
| **순서 가정이 미검증이다**(AS-L13) | 규칙 ⓑ가 "**처음** 두 깊이선의 교점"이라 **그리는 순서가 결과를 지배한다** — 축별로 몰아 그으면 축 오차 6.93°, 번갈아 그으면 32.57°였다(옛 실행 — ⚠ **[D-L59 주석]** 이후 값이 두 번 움직였다. 현행은 몰아 31.61°·번갈아 27.85°, 원장을 읽는다 #1). 사람이 어느 쪽으로 긋는지 **표본이 0이다** | `sessions/`에 실획이 들어오면 `real_ink.test.ts`가 AS-6과 함께 답을 낸다 |
| **8° 밖은 안 묻는다**(AS-L15) | 손으로 그은 수평선이 8°를 넘으면 **깊이선으로 조용히 읽힌다**(잡음 0.03~0.05에서 1점 구도에 가짜 소실점이 생겼다). 그 자리가 이 규칙의 조용히 틀리는 문이다 | 실획의 수평선 기울기 분포. 필요하면 `depth_min_deg`를 올리거나 **애매 구간을 넓힌다**(묻는 쪽이 안전하다) |
| **소실점을 지지선으로 다듬지 않는다** | "소실점은 확정 후 잠긴다"(CLAUDE.md §1)라 뒤에 온 선이 소실점을 안 고친다. 검출 팔은 4선 최소제곱이라 그만큼 유리하다. **잠금을 풀면 추정으로 되돌아가므로** 지금은 안 한다 | 사용자가 명시적으로 "다시 잡기"를 누르는 경로를 열 때. **자동으로는 안 연다** |
| **`stage_browser.json`의 `l_b2`가 안 갱신된다** | 그 스펙(가이드 조정)의 **대상이 사라졌다**(D-L37) — `test.skip`으로 두고 사유를 적었다. 계획서 §5.2의 핸들 예산 표는 **낡은 UI를 설명하는 것**이다 | 계획서를 규칙 기반으로 다시 쓸 때 §5.2를 통째로 갈아 끼운다 |
| **`vpDraft`·`vpDetect`·`vpSensitivity`·`vpHomog`가 앱에서 빠졌다** | 코드는 남겼다 — `camera_gate`·`draft_recover`·`guide_budget`·`vp_homog` 원장이 그 판정의 근거를 계속 재현한다. **배선은 없다** | 규칙 경로가 게이트를 통과한 뒤에 지운다(A-4: 폐기 코드를 지우기 전에 게이트를 통과시킨다) |
| **옛 저장본(`rules` 없음)이 안 열린다** | `Doc2.rules`가 없으면 규칙이 비어 카메라가 안 서고 획이 2D로 남는다. **조용히 틀린 카메라를 세우는 것보다 낫다**(A-3). `cam`(누산기 덤프)은 남아 있으므로 읽는 스크립트를 한 번 짜면 된다 | 옛 작업을 실제로 열어야 할 때(D-L29와 같은 자리) |

### ⚠ 미해결 — 종단 확인 두 스펙이 깨진 채로 남았다 (2026-08-16)

**A-4("기존 테스트가 깨진 채로 다음 항목에 가지 않는다")를 지키지 못했다.** 그대로 적는다.

| 스펙 | 증상 | 무엇까지 확인했나 |
|---|---|---|
| `stage.spec.ts` `단일 뷰포트 — 확정 시 3D가 잉크 자리에 그려진다` | `setup()` 뒤 `확정`을 눌러도 **배치 0**(12이어야 한다) | **같은 절차를 별도 스펙으로 재현하면 12가 나온다** — 규칙·축 판정·`liftAll`은 정상이다(`hasCtx` true · `order` 3 · 축 12개 전부 배정 · `lifted` 12). 즉 **하네스 쪽 문제**이고 앱 경로가 아니다. ⚠ 실행마다 갈린다(같은 명령이 한 번은 이 줄을 통과하고 다음 실행에서 못 통과했다) — **비결정이다** |
| `stage.spec.ts` `되돌리기 UI` | `snap_lost`가 0(0보다 커야 한다) | 위와 같은 픽스처를 쓰므로 **같은 원인일 가능성이 높다**. 갈라 보지 않았다 |

**의심하는 자리**(확인 안 함): 저장 복원이 비동기라 앞 시험의 문서가 늦게 도착하는 경로.
그 방향으로 한 번 고쳤고(`getDoc2` 복원이 **이미 그린 문서를 안 덮는다** — 그 자체는 실사용
결함이라 남긴다) **증상이 안 사라졌다.** 즉 **원인을 못 짚었다**(#7: 추측하지 말고 센다).

**다음 세션이 먼저 할 일**: `setup()`이 돌려주는 `hasCamera`·`strokes`를 실패 실행에서
찍어 본다. 그 둘이 정상이면 `확정` 버튼이 `disabled`인 경로를, 아니면 문서·카메라가
언제 갈아 치워지는지를 본다.

~~⚠ **`stage_browser.json`이 그래서 갱신되지 않았다** — 그 원장을 인용하는 문서의 수치는
`@34db7279`(옛 상수) 그대로이고 selfcheck가 STALE로 표시한다. **낡은 것이 맞다.**~~
**2026-08-17 해소 확인**: 그 원장은 그 뒤 재실행됐고 현재 해시 `69eba78d`이다. 남은 인용
셋(`progress.md`·`README.md`)도 전부 `stage_browser.json@54346ad1`로 현재 해시다 — 이 경고 자체가
낡아 있었다(두 세션 연속 이월 정리에서 닫음).

## 축 스냅·지평선이 미룬 것 (2026-08-16 2차)

| 항목 | 무엇을 미뤘나 | 트리거 |
|---|---|---|
| **축 스냅의 게이트 실패** | 조용히 틀린 배치가 **1094/1322(0.8275) → 1270/1322(0.9607)**(절단 0.2, 번갈아 긋기, `axis_snap.json@54346ad1` — 초판의 0.811 → 0.967은 재실행 전 값). 축 스냅이 **카메라 오차를 증폭**하기 때문이고, 이 픽스처의 카메라 축 오차가 임계의 **18~26배**다. **카메라가 정확할 때 부호가 뒤집히는지 안 쟀다** — 참 소실점을 넣으면 그것은 항등이라(#5) 그 조건을 만들 픽스처가 없다 | 카메라 정확도가 임계에 가까워지는 경로가 생길 때. 오스냅이 그 후보다(양 끝이 확정되면 축에 안 기댄다) ⚠ **[6차 주석]** 이 줄이 인용한 수치는 **6차 지시 11**(소실점은 **세 번째 선**이 정한다) **이전 실행**의 것이다 — 원장이 다시 움직였다(현행 값은 원장을 그 자리에서 읽는다, #1). |
| **모호 판정의 효과가 이 하네스로는 안 재진다**(#32) | 스윕 전 행에서 배치·조용히 틀림이 불변인 것은 **구성의 귀결**이다 — 하네스가 발동을 세기만 하고 배치는 발동 여부와 무관하게 `pick`으로 놓는다(발동이 배치 경로에 안 닿는다. 2026-08-17 리뷰어 [6]이 잡았고 AS-L18의 첫 결론 "안 바꾼다"를 철회했다). 남는 측정은 발동률(0 → 534/1100)과 발동분 정확도(최대 0.1037)뿐(`axis_snap.json@54346ad1`) | 발동이 배치를 실제로 바꾸는 팔(발동 시 묻기/보류)을 하네스에 넣는다. 그리고 카메라가 정확한 픽스처에서 다시 잰다 ⚠ **[6차 주석]** 이 줄이 인용한 수치는 **6차 지시 11**(소실점은 **세 번째 선**이 정한다) **이전 실행**의 것이다 — 원장이 다시 움직였다(현행 값은 원장을 그 자리에서 읽는다, #1). |
| **`고치기` 스펙의 축 지정 판정이 안 돈다** | 면 대각선이 `axisOfStroke`에서 어느 축으로 배정돼 놓이면서 **대기 획이 0**이 됐다. 그것이 회수인지 **조용히 틀린 배치**인지 안 갈랐다 — `face_diagonal.json`은 후자라 적었다(0.0216 대 0.4483) | 오스냅이 서면 면 대각선은 **양 끝 지정**으로 놓인다(사람 지시 6). 그때 이 자리가 없어진다 |
| ~~**초기 스케치가 1점·2점뿐이다**~~ **대상 소멸**(2026-08-17) | 이 항목의 기전(수심 유도가 지평선 높이에 걸려 `null`)이 **A-4에서 통째로 빠졌다**. 3점은 이제 기울어진 세로 모서리의 수직축 선언으로 초기부터 선다(D-L47) — "궤도 뒤"라는 제약 자체가 없어졌다. 사람 결정도 이미 왔다(D-L45가 지평선을 사용자가 끌게 했다) | 없음 — 닫힘 |
| **프리핸드 경로** | 삭제하지 않았다 — `pts2d`·`pts3d`가 그대로 있고 렌더 네 자리만 직선이다(D-L41). **모드 플래그는 아직 없다** | 프리핸드로 돌아갈 때 그 네 자리에 플래그를 건다 |

## 오스냅 (2026-08-16)

- **같은 꼭짓점이 획마다 다른 3D 점이다** — 모서리를 따라 그은 획의 두 앵커가 서로 다른 획에서 와서 그 방향이 축과 **6.3° 어긋난 실측**이 있다(종단 확인). 두 점 배치는 그 불일치를 그대로 물고, 그래서 축 라벨이 종종 미분류다. **우회**: 라벨을 억지로 안 붙인다(기하는 사용자가 지정한 두 점 그대로). **원래 목표**: 공유 꼭짓점을 병합해 기하가 한 점을 갖는 것. **트리거**: **두 점 배치 선분의 축 대비 방향 오차**(실측 6.3°가 그 값이다)가 배치 품질을 떨어뜨리는 것이 원장에 잡히면. ⚠ 초판은 트리거를 '축 미분류 비'로 적었는데 그것은 **설계상 상수에 가까운 양**이라 발화하지 않는다(리뷰어 지적으로 정정).
- **`osnap.json`은 대상을 참 3D로 준다**(상한). 앱의 대상은 **놓인 3D 레이어**이고 카메라 오차를 문다. **트리거**: 실획 표본이 들어오면 같은 지표를 그 위에서 다시 잰다.
- **지평선을 오스냅 대상으로 넣지 않았다** — 화면 직선이라 붙여도 3D가 안 정해진다. **트리거**: 사람 지시의 '지평선 근처점 경로 확장'이 다른 뜻이었으면.
- **정점(`vertex`) 스냅이 지금 기하에서 한 번도 안 뜬다** — 끝점이 안 합쳐지기 때문이다(상자 하나에서 끝점 후보 **24/12획**, 같은 꼭짓점 간 거리 **0.0305~0.1182·G** 대 병합 반경 **0.002·G**). **우회**: 종류를 남겨 두고 사실을 적는다. **원래 목표**: 공유 꼭짓점을 하나로 만드는 기하 계층. **트리거**: 위 항목(꼭짓점 병합)이 서면 자동으로 해소된다. ⚠ **병합 반경을 넓히는 것으로 대신하지 않는다** — 이웃 꼭짓점과 같은 자릿수라 다른 점끼리 합쳐진다.
- **`osnap.json`이 앱의 3D 레이어를 안 쓴다** — 참 3D를 쓰므로 위 불일치를 **구조적으로 못 본다**. **트리거**: 앱 기하를 대상으로 하는 하네스를 만들 때(그때 정점 0과 6.3°가 원장에 들어온다).
- **종단 확인의 약화를 메울 새 검사가 없다**(리뷰어 [2]) — 두 점 배치 단언이 `snapStart/snapEnd`를 되읽는 **항등**이다. **원래 목표**: 놓인 3D의 끝이 **대상 획의 실제 3D**와 같은지 대조하는 팔(앵커 기록이 아니라 기하와 대조). **트리거**: 다음 항목에서 그 스펙을 쓸 때.
- **근처점을 뺀 교환의 대가를 안 쟀다**(리뷰어 [13]) — 첫 판(근처점 포함)의 오붙음을 기록하지 않았다. **트리거**: 끝점 스냅의 대상 목록을 다시 만질 때 두 팔을 함께 돌린다.
- **꺾인 획 가드의 효력이 미측정이다** — 합성 대각선에 꺾인 것이 없어 `too_bent`가 0이다(덮는 대상 0, #38). **트리거**: 실획이 들어오면 그 대역에서 다시 잰다(Quick,Draw 낙서의 67.7%가 그 종류다).

## 2026-08-17 — 사람 지시 A~K, 그리고 리뷰어 20건

| 미해소 | 사실 | 트리거 |
|---|---|---|
| **3점 입구의 정확도가 미측정이다** | A-4가 3점의 유일한 입구를 **"기울어진 세로 모서리를 '수직축'이라 답한다"**로 바꿨다. 그 입구를 가르는 팔이 원장에 **없다** — `horizon_pitch.json`의 팔은 `center/true/placebo/err_±N`(전부 **지평선** 축)뿐이다. AS-L20이 인용한 3.39~4.88°는 **참 지평선 팔**의 값이고 다른 기전을 잰 것이다(리뷰어 [10]) | `tilted_vertical` 입구를 켜고 끈 두 팔을 `horizon_pitch` 또는 새 하네스에 넣는다. **측정 전에 게이트를 등록한다**(#26) |
| **`horizon_pitch.json`의 `passed`가 영원히 false다** | 등록 조건 ①("중앙 지평선 팔은 차수 3이 0")이 잰 기전(수심 유도)을 A-4가 없앴다. 조건은 만족될 수 없고 원장은 `passed:false`·`passed_orders:[]`로 남는다. **원장 안의 `registered`·`what_the_zeros_mean` 문장도 옛 기전을 사실로 적는다** — 다음 세션이 원장만 읽으면 틀린 문장을 읽는다(리뷰어 [7][8]) | 하네스의 등록문을 고쳐 ①을 **"대상이 사라졌다"**로 표시하고 ②·③만 판정하게 한다. **조건을 지우지 않는다**(왜 그 규칙이 생겼는지가 근거다) |
| **A·B·A-4가 같은 실행에 섞여 있다** | `rule_camera`·`axis_snap`의 변화를 어느 변경에 돌릴 수 없다. 팔이 `rule_drawn/rule_grouped/rule_wide_pair/detect`뿐이고 **A만 켠 팔도 B만 켠 팔도 없다**(리뷰어 [14]) | 각 변경을 끄는 `cfg` 손잡이를 하네스에 열고 팔을 나눈다 |
| **`min_vp_len_ratio`가 몇 획을 걸렀는지 세는 곳이 없다** | `rejected` 사건에 섞여 들어가고 사유별 분해가 없다(#38 덮는 대상 미상). 배수 2도 **스윕을 안 돌렸다**(#12) | 하네스가 거절 사유를 갈라 세고, `0.02~0.08` 스윕을 낸다 |
| **`first_pair_sep_deg_median`이 원장 40여 곳 전부 `null`이다** | AS-L13과 D-L37·README의 순서 설명이 **그 양에 대한 주장**인데 그 양이 한 번도 기록되지 않았다(리뷰어 [19]) | 하네스가 그 값을 실제로 채우거나, **덮는 대상 0**이라고 원장에 적는다 |
| **`axis_snap.json`의 `order_gap_off = 0`이 미실행이다** | `snap_off__horizon_off` 팔은 `drawn`과 `grouped`의 **모든 필드가 글자 그대로 같다** — 순서 knob이 안 돈다. 그 0을 게이트 ③의 기준선으로 쓰면 판정이 **다른 조건의 팔**과 견주는 것이다(리뷰어 [4], #32) | 게이트 ③의 기준선을 `snap_off__horizon_on`(1.118 — 2026-08-17 3차 재실행 값)로 바꾸거나, horizon_off 팔에서 순서가 왜 안 도는지 적는다 |
| **위약 팔의 조용히 틀림이 1.0으로 포화했다** | `placebo_wrong_horizon__drawn`: `silent_0_2_rate = 1`(**775/775**, `axis_snap.json@54346ad1` — 초판의 770/770은 원장과 달랐다, 2026-08-17 정정). 천장이라 "차이의 크기"를 못 읽는다(리뷰어 [20], #40 ②) | 위약의 세기를 줄인 중간 팔을 넣는다 | ⚠ **[D-L59 주석]** 이 행의 위약 수치도 D-L59 이후 원장이 움직였다(위약 팔 = 참 팔과 동일값 — 대조 자체가 죽었다, 항목 3 리뷰어 [4]).
| **"측정이 설계를 세 번 정정했다"의 수치가 원장에 없다** | 0.63 · 37.7° · 21/144 · 141/144는 **중간 실행**의 값이고 그 코드가 없으므로 **재현 불가능**하다(리뷰어 [9], #25). 그 셋이 설계 변경 셋의 근거다 | 초판 동작을 `cfg` 플래그로 되살려 팔로 남기거나, **원장 밖 값임을 명시**한 채 둔다(지금은 후자다) |
| ~~**D~K가 손도 안 갔다**~~ → **H·I·J·K가 남았다** | 그 세션은 A·B·C·F만 했고, 이어서 **D·E·G까지 끝났다**(2026-08-17). 남은 것은 H(오스냅 반경·수직점·거리점 스냅) · I(자동 분할·지우개) · J(그리드·패널) · K(실획 측정 준비)다 | `HANDOFF.md`의 "다음에 할 일" |
| **`SHARED_CONSTANTS`가 전역 해시 하나라 무관한 상수 추가가 전 원장을 STALE로 만든다** | G에서 `GESTURE_TOL`(제스처 감도)을 넣으면 그것에 **의존하지 않는 원장 40여 개**가 전부 STALE이 된다. 그래서 안 넣었고 대신 그 값을 원장 안에 적었다 — **STALE 검사의 사각지대를 하나 만든 것**이다(감도가 바뀌어도 `touch_route.json`이 안 걸린다) | **의존 집합별 해시** — 하네스가 자기가 읽는 상수만 스냅샷에 넣고 selfcheck가 그 부분집합만 대조한다. 지금은 전부 아니면 전무다 |
| **제스처의 실기 표본이 0이다** | `touch_route.json`은 chromium의 **합성 포인터 사건**이다. 실제 아이패드의 손바닥 접촉 형태·펜과 터치의 시간차·`pointercancel` 발생 시점은 안 재진다(AS-C1과 같은 자리) | K(실획 측정)와 같은 문 — 아이패드에서 한 세션을 받으면 `palm()` 카운터가 그대로 답이 된다 |
| **원장 밖 회귀 수치**(2026-08-17 항목 1~3) | D-L53의 회귀 크기(몰아 중앙 **31.6°** · `rule_camera` 몰아 **33.9°**)는 **커밋되지 않은 중간판의 실행 값**이라 재현 불가다(#25 — DEFERRED 307과 같은 유형, #34의 재발). 방향 증거는 직전 커밋 원장 대조(`git show 47da6c4:stage0/out/axis_snap.json`)로 남는다 | 초판 동작(소실점 있는 상태의 조용한 P1 선언)을 `cfg` 팔로 되살려 재면 닫힌다. 지금은 원장 밖 값임을 D-L53에 명시했다 |
| **`axis_snap.json`에 1pt 항등 제외 층이 없다** | D-L47이 "인용은 `headline_no_identity`에서 한다"고 정했는데 `axis_snap`의 `by_arm_order`는 전부 **5구도 혼합**(1pt 항등 포함)이다 — D-L42·53의 인용이 그 층 위에 있다. 항등 행은 두 팔에 똑같이 들어가 차이를 희석하는 방향이라 방향 판정은 살지만, 크기는 항등 지분만큼 눌려 있다 | 하네스에 `headline_no_identity` 층을 추가할 때. 그때 D-L42·53의 수치를 그 층으로 옮긴다 |
| **동결 리터럴이 세 원장의 인용 대조를 영구 통과로 만든다**(D-L54) | `horizon.json`·`draft_recover.json`·`guide_budget.json`은 하네스가 삭제돼 **다시는 안 움직인다** — 이 셋을 가리키는 인용 **9건**(DECISIONS 2 · DEFERRED 2 · assumptions 2 · line_plan 1 · progress 2)의 해시 대조 통과는 "현재와 같다"가 아니라 "영원히 못 움직인다"는 뜻이다(#40의 "값이 자명하면 같은 상태") | 그 인용들을 읽을 때 사람이 이 사실을 함께 읽는다. 의존 집합별 해시가 서면 동결 리터럴과 함께 정리한다 |
| **P1(임의 f)의 형태 오차가 미측정이다** | "형태는 정확하고 깊이만 무차원"(지시 1)의 근거는 `frontalWorld` 게이지 논증뿐이다 — 이론서 16.4의 잔여 자유도(깊이 아핀)에서 전단이 실제로 빠지는지(주점=소실점 조건), 형태 오차 지표로 잰 팔이 **원장에 없다**(#37 늦은 확인) | P1 픽스처(가로선+깊이선)에서 참 f 대비 임의 f의 형태 오차 팔을 하네스에 넣을 때 |
| **축 오차 분포가 오배정 점프에 지배될 가능성**(리뷰어 [11]) | `rule_camera`·`horizon_pitch`에서 서로 다른 팔·구도의 p90·max가 **글자 그대로 같은 값**(35 · 47.1001 · 31.6083)으로 반복된다 — 축이 인접 축으로 오배정되면 오차가 축 간 각 그대로 나오는 형태(추측). 맞으면 중앙값 변화가 각도 저하가 아니라 **오배정 건수의 이동**이다(#9) | 하네스가 `axes_measured` 옆에 **오배정 수**를 세면 갈린다. 다음에 그 원장을 만질 때 카운터를 넣는다 |
| **5-1(결과선 소실)의 원 조건 재현이 미확인이다**(지시 5) | 보고 조건은 **아이패드 배포본(터치·dpr 2)**인데 확인은 dpr 1·Playwright 합성 마우스다 — 그 조건에서 "재현 안 됨"이고 **원인 귀속은 못 한다**(#32의 변종). 회귀 팔(`basic_flow` ④ 차등 판정)은 앞으로의 재발을 잡지만 과거 원인은 특정 안 됐다. 표시 값(LINE_PX·표식 반지름)은 CSS px라 dpr에 시각 크기가 불변이지만 **실기 확인이 없다**(#21) | 아이패드 실기 시험(K와 같은 문)에서 같은 순서를 그려 본다 |
| **AS-L9·QUESTIONS d·e·f의 실획 직접 측정이 불가하다 — 대리 재적합 팔 미구현**(지시 K, 리뷰어 [9][10]) | 넷 다 **참값(지평선·카메라)이 실획에 없다**는 같은 구조다: AS-L9(±120px 창) · f(축 스냅 손익분기 배수 — 참 카메라 필요) · d(지평선 수평 — 스칼라 y라 항등) · e(롤 — 같은 자료구조). 대리는 재적합 팔인데 검출 경로의 부활이라 설계 검토가 필요하다. `real_ink.json`의 `what_this_cannot_measure`에 명시했다 | 실획이 들어와 `real_ink`가 처음 돌 때 함께 판단한다 |
| **그리는 시점의 자동 분할이 미배선이다**(지시 I, D-L56) | 분할 기하(`split.ts`)는 있고 **지우개가 조각을 집을 때만** 계산한다. 상시 분할은 획이 늘 때 O(n²)이고 **조각 폭주(획 수 증가)를 안 쟀다** — K의 획 단위(#11)도 조각(`pieceOf`)으로 갈라 두었다 | 그리는 즉시 분할이 필요한 사용례가 보고되면. 그때 조각 수 지표를 함께 잰다 |
| **오스냅 조리개 15px의 dpr 2·실기 확인이 없다**(D-L56, #21) | CSS px 임계라 시각 크기는 dpr 불변이지만, 지시의 출처(아이패드)에서 확인 안 됨 — DEFERRED 316(표시 값)과 같은 문이고 이쪽은 **동작 임계**다 | 아이패드 실기 시험(K의 문) |
| **인용 수치의 값 대조가 자동에 없다**(2026-08-17 항목 0) | `scan_citation_hashes`는 **해시 대조**다 — ① 해시 없는 인용은 덮는 대상 밖이고 ② 해시가 맞아도 문서에 옮겨 적은 **수치**가 낡으면 안 잡는다. 두 세션 연속 이월된 낡은 인용의 원인이 이 사각지대이고, 정리한 당일 리뷰어가 **같은 유형 셋을 더 찾았다**(순서 라벨 뒤집힘 · D-L43의 32~35° · DEFERRED의 770/770). 검사를 넓히는 쪽(#19 위험)보다 **인용할 때 원장을 그 자리에서 다시 읽는 규칙**(CLAUDE.md §5)이 현행 방어다 | 인용 문법을 `원장.json@해시/경로=값` 꼴로 굳혀 selfcheck가 값까지 대조할 수 있게 될 때. 그 전에는 사람이 본다 |

- **2D 오스냅의 겨냥 거리 기록**(2026-08-17 4차 지시 1): 2D 스냅은 `snapStart`(3D 점)를 못
  만들므로 `snapDistPx`에 적으면 K의 모집단(스냅 여부 = snapStart 유무)이 섞인다(#11).
  실획에서 2D 겨냥 분포가 필요해지면 별도 필드(`snap2DistPx` 등)로 갈라 담는다.
  ⚠ **이 미룸은 D-L56의 판정 범위를 좁힌다**(리뷰어 [2]): 오스냅 15px의 최종 판정자로 등록된
  `snapDistPx` 표본이 **확정 후 획으로만** 이뤄진다 — 보조선 단계(확정 전, 작도의 본체)의
  겨냥은 이 계기로 안 잡힌다. D-L56에 같은 사실을 적었다.
  트리거: `sessions/`에 실획이 들어와 K가 돌 때.

- ~~**`horizon_pitch` 하네스의 조작 변수가 죽었다**~~ **해소(5차 이월-3, 2026-08-17)**:
  horizon_pitch는 **은퇴**(원장 what에 표시 — 역사 기록, 인용 대상 유지 위해 파일은 남긴다.
  D-L54 동결과 같은 자리라 그 사각지대 목록에 이 원장도 더한다), AS-L19 폐기 표시,
  **찍기 경로 측정은 `pick_vp.test.ts` → `pick_vp.json`이 신설**로 잇는다(AS-L23):
  찍기 0 팔 2.67° 대 two_lines 31.61°, ±120px 평탄. ⚠ 남은 것: **배치 영향**(축 오차만 쟀다 —
  배치·조용히 틀림은 이 하네스 밖) · 사람 찍기 정확도 표본 0(AS-L9).
- ~~**스냅 반영 합성 하네스가 없다**~~ **rule_camera는 해소(5차 이월-2, 2026-08-17)**:
  `resolve2dCore` 추출(#17)로 `rule_*_snap` 팔이 앱의 확정 전 2D 판정을 그대로 통과한 선을
  먹는다. 판정: **스냅 입력이 31.6°를 구제하지 않는다**(물음 오라클 판 몰아 31.61→35.00·번갈아 27.85→29.66 = git show d724ac0 / **무물음 선언 판(현행) 둘 다 →35.00** — 8-R′ 정정 ·
  **위약(같은 크기 임의 방향)은 원시와 같다**, `rule_camera.json@54346ad1`) — D-L59의 회귀는 ⚠ **[6차 주석]** 이 줄이 인용한 수치는 **6차 지시 11**(소실점은 **세 번째 선**이 정한다) **이전 실행**의 것이다 — 원장이 다시 움직였다(현행 값은 원장을 그 자리에서 읽는다, #1).
  하네스 문제가 아니다. ⚠ 시드 폭 미확인(#14)·오배정 고원(DEFERRED 315) 위의 값이라
  1~3° 급 격차는 강도가 약하다 — 확실한 것은 "구제한다"의 부재다. **남은 것**:
  axis_snap 스냅 팔(트리거: **실획 K** — 항목 3이 3-c의 축 오차 축을 rule_camera로 닫았고 배치 축은 K로 미뤘다, 8-R′ 정합 정정) · horizon_pitch(이월-3 재목적). 실획(K)이
  최종 판정자라는 것은 그대로다.
- **지시 4-e — 확정 후 지평선 끌기(피치 조작) 미구현**(2026-08-17 4차, D-L60): 옳은 구현은
  수평 소실점·지평선의 동반 이동 + 전부 다시 풀기(승격 규약)인데, 그 재풀이의 배치 손실이
  미측정이다(D-L32의 실패 자리 — 사후 이동은 소실점을 지지선에서 뗀다). 그 전까지 D-L45의
  끌기는 휴면(확정 전 지평선 없음 · 확정 후 잠김). 트리거: 사람이 열거나 실획에서 피치 조작
  요구가 관측될 때.

- **옛 저장본의 가짜 뷰 대기 획은 마이그레이션이 없다**(D-L64의 유보, 재검 [2-8]):
  5차 항목 1 수리 전 저장된 문서에 자유 자세 뷰 소속 대기 획이 있으면 확정 뷰로 안 옮겨진다 —
  그 뷰를 열어 그리면 접근 가능하므로 데이터 손실은 아니다. 트리거: 옛 문서 열기 보고.
- **LINE_PX 회귀를 잡는 판정이 없다**(이월-2 재검 [30]): LINE_PX는 비등재 상수라 STALE이
  못 잡고(D-L62), basic_flow의 판정 임계 넷은 전부 굵기에 둔감하다(60 불변이 그 증거).
  굵기에 반응하는 필드는 `gl_painted_px`(2406→1849)인데 임계가 없다 — 값이 잘못 바뀌어도
  게이트가 통과한다. 대역 임계를 걸려면 GL AA·플랫폼 변동을 재야 한다. 트리거: LINE_PX를
  다음에 바꿀 때(그때 전후 대조를 원장 필드로 남기고 대역을 정한다).
- **접이식 막대의 백버퍼 정합(#22) 실측이 원장 밖이다**(이월-1 재검 [16]): 펼침/접힘 전중후
  `stretch=[1,1]`·`view_vs_overlay.same`·자가 치유 카운트 불변은 dev 서버 수동 실측(#25)뿐이다 —
  #22는 이 저장소에서 세 번 재발한 항목이라 재발이 관측 불가로 남는다. 트리거: basic_flow에
  접이식 펼침/접힘 팔을 넣을 때(6-b 모바일 프로젝트와 같은 문이면 함께).
- **손 정렬 자동 스냅(D-L67 hand_deg)의 종단 팔과 화면 이동량 실측이 없다**(2-R′ [B-5]·[B-6]):
  발화(궤도 종료 + 감쇠 정착 + 2° 안 → snapToDir 160ms)는 감쇠 대기 뒤라 결정론이 약해 e2e
  팔을 안 만들었고, 정렬이 일으키는 화면 이동량(기하 추정 수십 px 급)도 원장 밖이다 —
  D-L65가 같은 양(첫 프레임 튐)을 원장으로 잡은 선례(orbit_begin_invariance)가 있다.
  align_eps 변경 시 one_point_direct.json 재실행(2-R′ [B-3] — STALE이 못 잡는 자리).
  트리거: 실기(K) 또는 자동 정렬 관련 보고.
- **eraser_size 원장에 "팔 안 자체 대조가 되살림을 대신한다" 명시가 없다**(8-R′ [4] 낮음의
  이월, 재검 [L1]이 미등재를 잡음): 슬라이더 미배선이면 6px 팔이 실패하는 구조(view_cube와
  같은 판단)인데 그 판단이 원장 밖(#25 — progress에만)이다. 트리거: eraser_size 하네스를
  다음에 재실행할 때 what_this_does_not_say에 넣는다.
- **하단바 마우스 전용 숨김(6-b)의 회귀 방지 없음**(2026-08-17 4차, 리뷰어 [3]): pointer:coarse
  숨김은 CSS 미디어쿼리 한 줄이고 확인은 브라우저 수동(모바일 에뮬레이션 orbit_visible=false —
  progress 산문에만 있다, #25). Playwright의 emulateMedia가 pointer 미디어를 못 바꿔 e2e 잠금이
  바로 안 된다. 트리거: 모바일 프로젝트(deviceScaleFactor·hasTouch)를 e2e에 추가할 때 함께 잠근다.

## 2026-08-18 6차 지시 신규

| 미룬 것 | 왜 | 되살릴 조건 |
|---|---|---|
| **보조 소실점(45° 거리점 · 임의 비율 · 경사 소실점)** — 6차 지시 12가 **등재만** 하라고 명시했다 | 지시 12: "이번 세션에서 구현하지 않는다. 1~11이 끝나고 실사용이 안정된 뒤에 판단한다." 만드는 경로는 셋이다(직접 찍기 · f에서 45° 거리점 계산 · 비율 입력), 각도기 조작은 **두 소실점을 지름으로 하는 반원 위 직각점 = 평면상 카메라 위치**(탈레스, 이론서 7.5)에서 광선을 쏘는 것이고, 경사 소실점은 **수평 소실점 바로 위/아래**의 수직선 위다(이론서 15.1). ⚠ **소실점은 선으로 생기고 보조 소실점은 점으로 만든다** — 만드는 방식이 다르므로 앞서 거리점을 폐기했던 혼동(대각선이 거리점인지 2점 승격인지 기하로 구분 불가, D-L?/지시 2)이 **여기서는 없다** | 항목 1~11이 끝나고 실사용이 안정된 뒤 |
| **`RULE_TOL.concurrent_deg` 스윕** — 동작점 하나로 3°를 박았다(#12) | 근거는 **실획 보고**(Δ0.0~1.7°)이고 그 표본을 이 저장소에서 확인하지 못했다(`sessions/` 비어 있음). 값이 좁으면 진짜 지지선을 버리고, 넓으면 다른 축을 삼킨다 | `sessions/`에 `.brnl`이 들어오면 `real_ink`가 그 분포를 낸다 |
| **세 선이 두 축으로 갈리는 구도의 잔여 모호성** — 합성 120구도에서 3구도가 틀린다 | 대각선 셋과 축 둘이면 2+1 쪼개기가 기하적으로 둘 이상 성립하고, 어느 쪽인지는 **더 그어야** 정해진다. 등록 기준(틀림 0/N)은 **안 낮췄다**(#26) — `confirm_rules.json`의 `vp_rule_three.gate.status`가 미달을 든다 | 넷째 선까지 보는 판(대기를 한 번 더 늘린다)을 재거나, 실획이 이 구도를 실제로 만드는지 본다 |
| **그리드가 그린 선에 안 맞는다** — 방향은 맞고 **원점·간격이 임의**다(`grid.ts`의 `groundGrid`) | 실척이 없으므로 앵커가 없다(계획서 §3.8: 없는 정보를 있는 것처럼 보여 주지 않는다). 6차 지시 1이 이것을 "확정 뷰 자세 누락"으로 읽었으나 원인은 다르다(D-L66) | **항목 4**가 좌표를 연결로 정하면 그때 실제 앵커가 생긴다 |
| **`on_face`가 언제나 이겨 2D 대기 획 경로가 확정 뒤에 안 돈다** | 지면 스냅의 화면 거리가 **정의상 0**이라 다른 후보가 조리개 밖이면 반드시 붙는다 — 그래서 4차 지시 1-b의 2D 폴백(`snap2At`)이 카메라 확정 뒤 **한 번도 안 돈다**. 항목 2의 프로브가 그 사실을 관측 가능하게 만들었다 | **항목 4-5**(on_face 임의 스냅 제거)가 지운다 |

### 6-R 리뷰어 대응에서 새로 등재한 것

| 미룬 것 | 왜 | 되살릴 조건 |
|---|---|---|
| **`beyondSegment`의 위약 팔**(#39) | 그것은 후보를 **거르는** 제약이고 효과(묶음 틀림 15 → 3)를 "소실점은 연장선 위에 있다"(AS-L28)의 몫으로 귀속했다. #39의 나머지 절반은 **같은 크기의 무의미한 배제**를 위약으로 함께 돌리는 것인데 **안 돌렸다** — 위약도 3에 가까워지면 재던 것은 그 성질이 아니라 "후보를 그만큼 줄인 것"이다 | 다음 항목에서 같은 하네스를 다시 열 때 |
| **화각 게이트의 비대칭 동작점** | 대역이 이제 `f/W`라 비대칭(주점이 한 소실점에 붙는 배치)도 담지만, 쓸기는 **주점 정중앙 격자뿐**이다 — 비대칭이 커질 때 대역 경계가 어떻게 움직이는지 안 쟀다 | 실획 표본이 비대칭 구도를 실제로 내는지 본 뒤 |
| **그리드 방향이 맞다는 것의 측정** | 6차 항목 1은 "방향은 확정 카메라의 소실점에서 오므로 맞고 자리·간격만 임의"라 판정했는데, **확정 카메라의 그리드와 사용자 깊이선의 각차를 잰 값이 원장에 없다**(리뷰어 [4]). `vp_two_lines.json`의 `grid.maxSin`은 그 원장이 스스로 "보장 확인(#5)"이라 적은 값이라 근거로 못 쓴다 | 항목 4가 그리드에 실제 앵커를 주면 그때 함께 잰다 |
| **`rule_camera`의 `deg_p90` 포화 여부** | 여섯 팔 전부 40.66이고 `deg_median`이 넷에서 정확히 35다(리뷰어 [3]). 조작 변수를 넷 바꿔도 소수 둘째 자리까지 같으면 지표가 픽스처의 구도 각에 **포화**한 것일 수 있고, 그렇다면 "17.58 → 31.61"은 크기가 아니라 **굳은 구도 개수**의 변화다 | 그 하네스의 픽스처 각 격자를 확인할 때 |
| **중단 조건 게이트가 소실점 규칙을 안 지난다** | `camera_gate.test.ts`는 소실점을 **직접 주고** `recoverCamera`만 부른다 — `resolvePool`·`stepRule`을 안 지난다. 그 원장이 안 움직인 것은 **사정거리 밖**이라는 뜻이지 안전의 증거가 아니다(리뷰어 #41). 이 변경을 실제로 지나는 유일한 하네스는 `rule_camera`이고 거기서 축 오차가 나빠졌다 | 중단 조건을 규칙 경로 위로 옮길지 판단할 때(사람 지시가 필요하다 — 게이트를 바꾸는 것은 CLAUDE §2의 판정 주체를 바꾸는 것이다) |
| **새 규칙의 대조 팔 대가 24/120** | 같은 축 둘이 먼저 오는 구도에서 새 규칙이 24/120 묶음을 틀린다(옛 규칙 0/120). 겨냥 잡음 1.7°에서 세 선 묶기가 그만큼 흔들린다 | 실획 표본으로 실제 잡음 분포를 확인한 뒤 `concurrent_deg`를 다시 잡을 때 |

### 6-R 재검에서 새로 등재한 것

| 미룬 것 | 왜 | 되살릴 조건 |
|---|---|---|
| **겨냥 잡음·시드 스윕**(#12·#14) | `AIM_JITTER_DEG = 1.7°`(실획 보고 구간 0.0~1.7의 **끝**)와 `seed 20260818` **각각 동작점 하나**인데, 그 한 점이 ① 지표 교체의 근거 ② 새 규칙 7/120 ③ **대가 24/120** ④ D-L69의 채택 논거를 전부 낳는다. 잡음 0에서는 같은 하네스가 3/120이므로 **결과가 잡음에 강하게 반응한다**(6-R 재검) | 실획 표본이 실제 잡음 분포를 주면 그 분포로 다시 돌린다. 그전에 0.5°·1.0°·1.7°와 시드 셋을 돌려 **크기 순서가 유지되는지**만이라도 본다 |
| **위치를 구속하는 등록 기준이 없다** | 지표를 위치에서 묶음으로 옮기면서 위치가 게이트에서 빠졌는데 **카메라는 위치에서 나온다**. 새 규칙의 `first_vp_error_px`는 p90 2116 · max 16735 수준이다(6-R 재검) | 위치를 재는 게이트는 **조건수와 얽혀 있다** — 소실점을 **찍기**로 정하는 경로(이월-3에서 2.67°)와 견주는 팔을 만들 때 함께 세운다 |
| **D-L42의 표제 쌍이 현행 원장과 안 맞는다** | "1094/1322(0.8275) → 1270/1322(0.9607)"의 세 수가 지금 `axis_snap.json`에 없다. 현행의 대응 쌍으로 보이는 것은 `silent_denom 1403`에서 1277(0.9102) → 1358(0.9679)이고 **방향은 유지되고 크기가 다르다**(6-R 재검) | D-L42를 다시 열 때. 지금은 `[D-L59 주석]`·`[6차 주석]`이 "이전 실행의 것"이라 덮고 있다 |

## 2026-08-18 6차 지시 12 — 보조 소실점 (구현하지 않는다. 등재만)

지시 12가 명시했다: **"이번 세션에서 구현하지 않는다. 1~11이 끝나고 실사용이 안정된 뒤에 판단한다."**
아래는 지시문이 준 설계를 **잃지 않기 위한** 기록이고 결정이 아니다.

**왜 이제 만들 수 있게 됐나**: 소실점 확정 규칙이 정해졌으므로(D-L69) 경로가 갈린다 —
**소실점은 선으로 생기고 보조 소실점은 점으로 만든다.** 만드는 방식이 다르므로 앞서 거리점을
폐기했던 혼동(대각선이 거리점인지 2점 승격인지 기하로 구분 불가)이 **여기서는 없다**.

**용도**: 45° 거리점(정사각형 작도·깊이 등분) · 임의 비율(1:2 등) · 경사 소실점(지붕·계단 —
이론서 15.1: **지평선이 아니라 대응 수평 소실점 위의 수직선** 위에 있다).

**만드는 경로 셋**
```
a. 직접 찍기      지평선 위 원하는 자리를 탭한다
b. 계산해서 제시   f가 정해지면 45° 거리점은 **주점에서 좌우로 f만큼** 떨어진 지평선 위 점이다.
                 표시하고 누르면 확정 — 가장 자주 쓰이고 위치가 정확히 계산되므로 가장 유용하다
c. 비율 지정      "1:2" 같은 값을 입력하면 위치가 계산된다
```

**각도로 만드는 경로 — 카메라 위치에서 광선을 쏜다.** 두 소실점을 지름으로 하는 반원 위
직각점이 **평면상 카메라 위치**다(탈레스, 이론서 7.5). 주점에서 아래로 f만큼 내려간 점이고
`f² = |PV₁||PV₂|`로 계산된다(⚠ **`− h²`가 들어간다** — D-L68). 그 점에서 쏜 광선의 각도가
**곧 세계 각도**다(X축 소실점 방향이 0° · Z축이 90°) — "세계 각도를 화면에서 어떻게
짐작하나"가 여기서 풀린다. 조작: `[소실점 추가]` → 카메라 위치에 각도기 → 광선을 돌린다
(각도 실시간 표시) → 놓으면 광선 ∩ 지평선이 소실점. 각도 입력도 같은 것이다.
**화면 밖 처리**: f가 크면 카메라 위치가 화면 밖이다 — 가장자리 다이얼이나 축소 뷰를 잠깐
띄우고 **계산은 실제 위치로, 광선만 화면에 보이면 된다**(항목 7-e의 가장자리 표식과 같은 자리).

**경사 소실점**: 수평 소실점에서 수직선을 긋고 그 위에 점을 찍는다. 각도기 조작이 같고
**기준선만 바뀐다**(그 수평 소실점을 지나는 수직선을 지평선 대신 쓴다). 카메라 위치에서 그
수평 소실점을 향하는 방향이 0°이고 위아래로 쏘면 경사각이다. **표시를 구분한다** —
지평선 위는 "회전 30°", 경사는 "경사 30°"(다른 양이다). **개수 제한을 두지 않는다**
(계단 둘·지붕이 각각 다르면 소실점도 각각) — 이름이나 번호로 구분한다.

**표시**: 지평선 위의 짧은 수직선 틱(소실점 표식과 구분되고 지평선 위임이 명확하다 — CAD의
참조점 관행). **스냅**: 그 방향이 축 스냅 후보에 들어가되 **축이 아니다** — 그 방향 선은
3D에서 대각선이고 두 축의 조합이다(45°면 X와 Z가 같은 비율). **배치 계산이 축 방향과 다르므로
갈라서 처리한다.** 채널과 무관하다(사용자가 명시적으로 만드는 것이고 그냥 그은 선과 구분된다 —
f 추출을 채널 무관으로 정한 것과 일관된다).

**위계 — 공간 요소는 그린 획과 다른 축이다**
```
공간 요소  지평선 · 소실점 · 경사 소실점 · 보조 소실점 · 그것들을 담는 수직선
           채널 없음(보조선/결과선/주석 어느 것도 아니다) · 3D로 안 올라감 · 내보내기에 안 나감 ·
           지우개로 안 지워짐(소실점을 지우면 함께 사라진다) · 표시는 그리드·지평선과 같은 톤
그린 획    채널 있음(D의 셋) · 상태 있음(항목 4의 방향 미정 / 방향 확정 / 좌표 확정)
```
D의 채널은 **그린 획에만** 붙고 항목 4의 상태도 그린 획에만 해당한다 — 공간 요소는 그 축 밖이며
**카메라와 사용자 지정**이 정한다.

## 2026-08-18 7차 신규

- **대기 집합 안에서 "셋이 다 다르다"를 못 가른다**(7차 지시 2-3). 두 수평축이 다 선 뒤의
  셋째 방향은 `stepRule`이 거절하고 알리지만, **소실점이 아직 없는 대기 집합**에서 세 선이
  한 축으로도 두 축으로도 안 갈리는 경우는 `resolvePool` ④가 그냥 확정한다(대조 팔에서
  **24/120**이 그 자리다 — 알림 없음).
  ⚠⚠ **초판은 "그 임계를 정할 측정이 없다"고 적었는데 그것은 미실행을 부재로 처리한 것이었다**
  (7-R 리뷰어 [②-a] · #32). 기존 240구도가 이미 묶음 정오를 라벨링하고 있었고, **쟀다**:
  `confirm_rules.json`의 `far_distribution` — 묶음 맞음 n=113(중앙 1141.3 · min 293.9),
  묶음 틀림 n=3(중앙 204.5 · max 436.8). **두 분포가 겹치므로 `far` 하나로는 못 가른다**.
  그리고 분자가 3이라 임계를 고를 근거로도 못 쓴다(#14). **다른 신호가 필요하다** —
  리뷰어가 제안한 "2+1 쪼개기가 유일한가"(새 임계 없이 `concurrent_deg`만으로)를 다음에 본다.
- **`beyondSegment`가 참 짝을 4/120 떨어뜨린다**(7차 항목 2, `angle_vs_far.true_pair_gate`).
  무르면 AS-L28의 이득(무잡음 틀림 15 → 3)이 되돌아가므로 안 뭅다. 참 짝이 살아남고도
  잘못 고르는 3건과 함께, `resolvePool`의 남은 7/120이 이 둘로 나뉜다. 실획이 판정자다.
- **`order_lock` 팔이 앱의 `placeStroke` 경로를 안 탄다**(7-R 리뷰어 [5]). `liftAll` 일괄 풀이만
  쓰므로 시작점 스냅·축 되쓰기의 배치 효과가 그 원장에 안 잡힌다. 노드에서 그 경로를 돌리려면
  DOM 의존(`cssSize`·`stage`)을 걷어내야 한다. 지금은 `confirm_dir.json`(e2e)이 그 자리를 덮는다.
- **`RULE_TOL.screen_axis_deg`(4°) 스윕이 없다**(7-R 리뷰어 [7]). 입력 쪽 카운터
  (`order_lock.screen_h_operating_point`)는 냈지만 그 임계를 흔들어 차수 분포가 어떻게 바뀌는지는
  안 쟀다 — `CamState.feed`가 `cfg`를 안 받는다. **항목 3이 이 경로 자체를 바꾸므로 그 뒤에 잰다.**
- **P1 잠김의 원인은 여전히 열려 있다**(7차 항목 1·3). `classifyLine`이 화면 수평 4°
  (`RULE_TOL.screen_axis_deg`) 안의 깊이 모서리를 `screen_h`로 읽고, **소실점이 하나도 없을 때는
  묻지 않고 받는다**(`stepRule` 739~745행). 그래서 `first_declaration.screen_h`가 495/600
  (잡음 0 층 120/120)이고 차수가 P1에 갇힌다. **지시 2-1 다섯째 줄이 그 동작을 명시하므로**
  (수평선 → 화면 평행 축 존재 → 1점 확정) 바꾸는 것은 설계 변경이고 지시가 필요하다.
  후보 셋: ① 첫 획이 화면 수평이어도 **묻는다** ② `screen_axis_deg`를 줄인다(스윕 필요) ③
  P1을 가역으로 만든다(D-L53이 명시적으로 배제한 방향). 측정 준비는 돼 있다
  (`order_lock.screen_h_operating_point` — 2°/4°/8°의 입력 분포).
- **3점 스케치를 빈 캔버스에서 시작할 수 없다**(7차 지시 3의 귀결, AS-L33). 2점으로 시작해
  카메라를 기울여야 한다. 건축 투시도에서 3점으로 **처음부터** 그리는 관행이 흔하면 이 결정이
  되돌아온다 — 실획 표본이 판정자다.
- **지시 3-e(커서로 가르는 조작)가 미구현이다**(7차 항목 3, 7-R [4-D]). "물음이 남는 자리는
  모호 판정뿐 — 두 축이 화면에서 겹칠 때. **커서 위치로 가르므로 물음이 아니라 조작이다.**"
  항목 3은 문구만 고쳤고(3-g) 커서 가르기는 안 넣었다. **잇는 자리는 있다** —
  `resolveLive`의 `chooseAxis`가 이미 모호 구간에서 커서에 가까운 축을 고른다(사람 지시 3).
  그것을 **규칙 판정**(`stepRule`의 `screen_or_depth`)에도 쓰면 그 물음이 사라진다.
  ⚠ 그때까지 지시 3-f의 "asked가 0에 가까워진다"는 **미이행 상태의 값**이다(#32).
- **지시 3-a를 문자 그대로 이행하지 않았다**(7차 항목 3, 7-R [4-B]). 차수는 여전히
  `perspectiveOrder(rules)` = f(소실점 집합)이고 **피치의 함수가 아니다.** 귀결: 피치
  15°·25°·8°의 참 카메라를 그린 3점 구도 360실행에서 도구의 차수는 **P3이 0**이다.
  "3점은 시점의 성질"을 자료구조에 옮기려면 차수를 **확정 카메라의 자세**에서 계산해야 하고,
  그러면 뷰마다 차수가 달라진다(기울인 뷰는 3점 · 확정 뷰는 2점) — 그 뜻을 정하는 것이 먼저다.
- **기울인 시점에서 축 스냅이 셋을 고르는지 안 쟀다**(7차 항목 3, 7-R [4-C]).
  `axis_snap_table`의 "3점 확정" 시험은 **앱이 만들 수 없는 상태를 주입한다**(옛 저장본 형태로
  슬롯 2에 유한 소실점을 직접 넣는다) — 표가 상태의 함수임은 확인하지만 지시 3-b의
  "축 후보가 하나 늘 뿐 기전이 같다"의 근거는 아니다(#17·#32·#35).
  앱 경로(2점 확정 → 궤도로 기울임 → 그 시점에서 긋기)로 도달하는 팔이 필요하다.
- **`touch_route` dpr 2의 실패율을 원장에 안 남겼다**(7차 항목 3, 7-R [6-B] · #25).
  같은 코드에서 통과·실패가 갈린다. 지시문이 "환경 밴드다 · 그대로 둔다"고 했으므로 임계는
  안 건드리지만, **반복 실행 카운터가 없어 "간헐"이라는 말이 원장 밖이다.**
- **`camera_ok_rate`가 이 모집단에서 개입을 못 구분한다**(7차 항목 3, 7-R [3-B] · #40 ②).
  `rule_camera`의 `headline_no_identity` 열한 팔(위약 셋 포함)이 전부 **정확히 1**이다.
  그 지표로 다음 개입을 판정하면 아무것도 안 갈린다 — 1pt 구도를 포함한 층(`headline`)이나
  `order_lock`의 P0을 함께 읽어야 한다.
- **"예각 조건(6.5)이 3점 카메라를 막았다"를 세지 않았다**(7차 항목 3, 7-R [3-C] · #7).
  `camera_ok`만 있고 실패 사유별 카운터가 없다. 그 기전은 **가설**로 적었다.
## 2026-08-17 원격 갈래 (실획 첫 표본 대응) — 8차 항목 0에서 병합

⚠ 아래는 `origin/main`의 갈래(PR #1·#2)가 등재한 것이다. 그 갈래는 스스로를 "7차"라 불렀고
이 문서의 7차와 **다른 지시문**이다. 결정 번호도 옮겨 적었다(D-L66~L69 → **D-L76~L79**,
`DECISIONS.md` 8차 항목 0 절).

- **freeIntrinsics(궤도 렌즈 이어받기)의 P3 동작점 실측이 없다**(1-R′ [6]): 이어받는 값은
  확정 해의 주점 그대로라(2점 [W/2,지평선]=16.2+A-4 · 3점 수심=6.3) 분기는 없지만,
  `restore_pose.json`의 동작점은 2점(주점 중심·중심 밖 0.23H)까지다 — 3점(주점=수심,
  중심 밖 x·y 동시)은 안 쟀다. 트리거: P3 실획 표본 또는 승격 관련 다음 작업.
- **핀 상태에서 창 크기가 바뀌면 재핀이 없다**(1-R′ [9] 대응 중 파생 발견): `fit()`이
  `cam.resize`로 카메라(지평선 비례 유지 → 주점·f 갱신)를 다시 푸는데, 핀 투영 훅은
  **pinTo 시점의 옛 주점·f**를 계속 적용한다 — 2D 층(새 해)과 3D 층(옛 내적)이 크기 변경
  뒤 어긋날 수 있다. 복원 경로는 7차가 자가 치유 후 재핀으로 막았고, **런타임 리사이즈**가
  남은 자리다. 트리거: 창 크기 변경/기기 회전 관련 보고 또는 다음 뷰포트 작업
  (수리는 fit() 뒤 stage.isPinned면 cam.ctx()로 re-pinTo — 한 줄이나, 실측 원장과 함께).
- **오스냅 반경(기본 15px)의 적정성 판정**(7차 항목 2 — D-L56 "실획이 최종 판정한다"의
  실행): 판정 계기(snapDistPx)가 죽어 있어 첫 표본은 전부 0이었다. 정의 수리(aimDistPx, D-L79) 후 **다음 실획 표본의 snap_dist_px 분포**가 판정한다 — 그 전에 반경을 임의로
  넓히지 않는다(A-3). 트리거: 다음 .brnl 표본 도착(real_ink 재실행).
- **vpdir 물음 억제의 실획 효과 실측**(7차 항목 2): 합성(rule_camera)에서는 15px 팔 불변·
  r40 팔 asks만 감소라 실사용 효과(물음 6회/획 5가 주는가)는 미관측이다. 트리거: 다음
  .brnl 표본의 askStats(asked/획 비).
- **실획 첫 표본 파일이 저장소 밖이다**(7차 항목 4): 지시문은 관찰 수치를 인용했으나
  `.brnl` 파일 자체는 `sessions/`(작업 트리·origin 전 브랜치)·이슈 어디에도 없다.
  지시 4의 지표는 전부 정비돼 있다(snap_use·vp_dir_err_deg·legacy 0 분리 포함) —
  트리거: 파일을 `sessions/`에 넣고 `npx vitest run test/real_ink.test.ts` 한 번.
  ⚠ 그 표본은 7차 이전 앱의 저장본이라 snapDistPx가 전부 0(옛 정의)이다 —
  `snap_dist_legacy_zero`로 갈려 분포를 오염시키지 않는다(D-L79).
- **항목 3("같은 면 깊이 산포 — 별도 결함 없음")의 재개 트리거**(3·4-R [3]): 그 판정은
  "1·2의 귀결"이라는 인과 사슬(코드 읽기 + 사람 보고) 위에 있고 실측 확인은 다음 표본의
  몫이다. **다음 실획 표본에서 `snap_use.end_snapped`가 여전히 0에 머물거나
  `vertex_gap_ratio`가 안 줄면 항목 3을 다시 연다** — 그때는 별도 결함(면 지정·깊이 상속
  규약)의 후보를 본다. 트리거: 두 번째 표본(7차 수정 후 앱으로 그린 것)의 real_ink.
