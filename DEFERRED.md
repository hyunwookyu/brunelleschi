# DEFERRED.md — 우회·단순화·미룬 항목

A-2가 우회를 허용한 자리와, 계획서가 지정했으나 지금 하지 않는 것.
**사실과 원래 목표를 함께 적는다** — 나중에 되돌릴 수 있어야 한다.

## 2026-08-19 14차 항목 0 — 남긴 것 (리뷰어 1차 대응)

| 미룬 것 | 왜 | 되살릴 조건 |
|---|---|---|
| **on_edge·on_face 표식이 앵커 자격 없이 계속 뜬다**(14차 리뷰어 1차 [8]) | D-L92가 미리보기의 **기하**(3D 축 선)는 자격과 통일했지만 **표식**(hoverSnap)은 D-L83의 "표시·미리보기는 그대로" 결정대로 남겼다. 표식이 뜨는데 앵커가 안 잡히면 "보고 받아들인 것 ≠ 확정"의 잔여가 있다는 지적은 유효하다 | AS-L38(on_face가 실획 시작점의 5/5) 판정과 함께 — 다음 표본이 on_face 겨냥의 실사용을 보이면 D-L83의 표시 정책을 재개정한다 |
| **교차 앵커 방향 가드(crossStats.rejected_dir)의 발화 픽스처가 없다** | dir_state 팔 ⑨의 40° 획은 축 미분류(free)라 교차 경로에 못 들어간다(skipped_axis) — rejected_dir의 도달 가능성은 미상(#35 — 원장 산문에 등재) | angleWiden·rank_margin 완화로 축이 붙되 임계 밖인 교차 획 픽스처를 만들면 실측이 된다 |
| **얕은 2점 축이 스냅-선언으로 1점에 갇힐 수 있다**(14차 항목 3 · D-L96 — 선언 게이트의 잔여 위험) | 직교 스냅(≤4°)이 얕은 참-깊이선을 눕히면 그 수용이 D-L89 정의상 "선언"이라, 깊이선 둘 확정이 한 참 축의 이중 예약(화면 평행 + 유한 VP)을 굳힌다 — 합성 스냅 모사 팔에서 오배정 고원 재진입으로 실측(`rule_camera.json@f351839a`의 snap 팔들 — 중앙 31.6/33.3/35·p90 40.66은 **고원 상수**라 크기 아닌 범주로 읽고 #46, 분모 격차 axes 229 ↔ 원시 343을 함께 읽는다 #11 — 리뷰어 2차 [4][5]). ⚠ 원시 팔 복귀가 위안이 아니다(2차 [2]) — 등록문상 **스냅 팔이 앱 동작점**이고(앱 확정 전 경로가 같은 `resolve2dCore`를 지난다) 원시 팔은 "낡아 있던 조건의 연속 기록"이다. 방어는 AS-L25(합성 급송은 스냅이 눕힌 선을 사람처럼 안 되무른다)와 위약 팔 불붕괴(6.65~10.03 — #30 귀속 증거)다. 등록 게이트 둘째 조항 `passed_snapped`는 이 항목이 true → **false**로 뒤집었다(2차 [1]) | 관측 가능한 판정자(2차 [12] — DEFERRED 23행 규칙): ① 합성 — snap 팔 `screen_axes_total` 초과분(현행 347−227=120 — 이중 예약 규모의 대리)·`passed_snapped` 재역전 여부 ② 실획 채널(`real_ink` 축 방향 오차)이 열리면 그쪽이 정본. 나오면 선언 신호를 좁힌다(명시 토글·스냅 유지 확인 — unambiguous 폐기가 아니라) |
| **D-L96 초판(슬롯 존재 게이트)의 재현 스위치를 안 만들었다**(14차 항목 3 · 2차 [I] — A-2 ③ 기록) | 초판의 붕괴 실측(rule_camera 게이트 false·order_lock P2 급감·형태 배증)은 최종 판 재실행으로 덮어써져 **산문 기록**이다(#25). 재현하려면 선언 게이트를 슬롯-존재 게이트로 되돌리는 측정 스위치(#30)가 필요한데 범위로 기각했다(A-3 — 폐기 코드의 손잡이 유지는 확장) | 이 붕괴의 귀속(선언 게이트가 초판 결함을 실제로 막는가)을 다시 판정해야 할 때 — dir_state의 `setChainDirGuard` 선례대로 S2S 스위치 하나면 된다 |
| **진짜 같은쪽 몰림(두 소실점이 주점 x 한쪽) 구도는 여전히 거부된다**(14차 항목 2 · 2-R [1] — 지시 2-c의 부분 미이행) | 주점 x = W/2 가정(16.2)을 유지하는 한 f² ≤ 0이 원리다 — 두 소실점만으로는 x가 결정 불가(방정식 하나·미지수 둘)라 "x는 두 소실점에서 나온다"(지시 2-a)를 그대로 이행할 수 없다. 열린 것은 W/2를 사이에 둔 비대칭까지 | x를 그린 기하(예: 화면 밖으로 크게 치우친 구도의 프레이밍 선언·수직선 위치)에서 가져오는 설계가 서면 — 그때 16.2 가정의 사거리 축소로 함께 판정한다 |
| **가드 임계 대역(5~12°)의 발생 빈도가 미측정이다**(2차 리뷰어 [4·5] — 임계 논쟁의 잔여) | 가드의 물음("사용자가 안 본 채 얼마나 돌려도 되는가")에 답하는 등록 상수가 없다 — 5°(나란함)와 12°(축 인정) 사이 대역이 얼마나 자주 나는지, 5° 안 회전이 체감 무해한지 어느 원장에도 없다. A-3대로 엄격한 5°로 두었다(D-L92) | 실획 표본의 `chainDirGuardStats.rejected/attempts`(+거절된 획의 turn 분포를 원장에 추가)가 판정자 — 대기 잔류가 과도하면 임계 재검토 |

## 2026-08-19 13차 항목 0 — 남긴 것 (리뷰어 1차 대응)

| 미룬 것 | 왜 | 되살릴 조건 |
|---|---|---|
| **`snapDistPx`의 기록 시점 갈래(스냅 전/후)와 기록 시점 조리개가 저장본에 없다**(13차 리뷰어 [3] · #5·#34) | `radius8_revert_condition`의 p10이 **정확히 0**인데(near_zero 2/4 — `real_ink.json@f351839a`) 그 0이 겨냥의 측정인지 스냅 뒤 좌표의 구조 0인지 파일이 못 가른다. legacy-zero 배제 조건은 `on_face && 0`만 덮어 endpoint/vertex 스냅의 0을 안 걷는다(#34 — 서명이 그때도 p10=0이었다). 그리고 표본이 그려진 시점의 `OSNAP.radiusPx`가 저장 형식에 없어 조리개 8/15 어느 동작점의 겨냥인지 미상이다 | 저장 형식에 ① aimDistPx의 기록 시점 표식(스냅 적용 전 원시 시작점 기준임을 보장하는 갈래) ② 기록 시점 `OSNAP.radiusPx` 스냅샷을 넣는다 — 다음 표본부터 p10 판이 정보를 갖는다. 그 전까지 `radius8_revert_condition.reachability`가 이 한계를 원장에 든다 |
| **획별 배치 경로(placeBy)가 저장본에 없다**(13차 리뷰어 [4]) | 15-18-25의 시작점 앵커 없는 배치 셋(`lifted_no_snapstart_ids`)이 일괄 풀이 몫으로 **모순 없이** 설명되지만(`first_anchor.json@f351839a`의 `contrast_15_18_25.placed_ids`가 셋을 다 담는다) 증명이 아니다 — pathStats는 세션 합계뿐이라 획 단위 귀속이 안 된다. "스냅 6 = seg3d 6 — D-L83이 의도대로"(12차 보고)는 집합으로 깨졌고, 반증인지 미상인 채로 남는다 | 저장 형식에 획별 `placedBy`(batch/direct/lift/twoPoint/cross/ground)를 넣는다 — 다음 표본이 D-L83의 실측 판정자가 된다 |
| ~~**복원 경로가 화각 상한(FOV_GATE.reject_fov_deg) 밖이다**(13차 리뷰어 2차 [3] — 실측)~~ ⛔ **닫힘(2026-08-19 14차 항목 1 · D-L93)** — 화각 상한 자체가 제거됐다(거부는 f² ≤ 0뿐). 복원과 신규 확정이 같은 조건이 되어 비대칭이 소멸했다. 163° 저장본이 서는 것은 이제 명세다(넓은 화각은 의도일 수 있다) | (연혁 보존) 12차 상한은 신규 확정만 막았고 복원은 지나갔다 — 그 비대칭이 이 행의 내용이었다 | 해당 없음 — 상한이 없다 |
| **지면 첫 앵커의 대상 좁히기(소실점 기여 획으로)와 원거리 배치**(13차 항목 2 · D-L90 · 1·2 리뷰어 [6]) | 현행 대상은 확정 시점 수평 평면 축 대기 획 **전체**다 — 지시 2-a("소실점을 만든 두 선")보다 넓다. 좁히려면 stepRule이 vp_fixed 사건에 기여 선(RLine)을 실어야 하고(현행은 소비돼 사라짐 — ⚠ 13차 기각 ②의 초판 사유 "식별 불가"는 과대였다: `order_lock.pending_sep_deg`가 보이듯 대기 풀의 기하로 좌표 대조가 가능하다), 넓힘의 비용(전제 위반 깊이 배율·방향 보존)은 `first_anchor.json@f351839a` 참값 팔이 든다. 지평선 근처 끝점의 **원거리 배치**(상한 없음 — D-L90 잠정)도 같은 판정 대상이다 | ⛔ **되살릴 조건을 '실사용 보고'로 두면 영원히 안 돌아온다**(1·2 리뷰어 2차 [3] — 위반은 방향이 보존되어 사용자에게도 지표에도 안 보인다). 조건을 **관측 가능한 판정자**로 다시 쓴다: 지면 배치 획(placeBy.ground)이 이후 연결(교차 앵커·오스냅)로 다시 좌표를 받을 자리가 생겼을 때의 **깊이 불일치**를 원장이 세면(그 팔 신설이 선행), 불일치 > 0이 좁히기·상한의 재개 신호다. ⚠ 지면 앵커 뒤 연쇄의 실획 증거도 아직 n=1(s49 — `ground_recovery.crossable_after` 1/1)이다 — 다음 표본이 그 판정자다(획별 placedBy 저장 행과 같은 자리) |
| **연장선 스냅의 2D 단계(카메라 전) 후보와 안내선 픽셀 팔**(13차 항목 3 · D-L91 기각 ③) | 이번 구현은 3D 획의 연장(지시 3-c의 핵심)만이다 — 카메라 전 보조선 작도(2D 단계, 그리기의 절반 — snap_stage)의 연장 후보는 snap2d에 없다. 안내선 표시(옅음/발동 진해짐 — drawExtensionHint)도 픽셀 팔이 없다(dir_state의 INK_AT 방식이 선례) | 2D 연장 후보: snap2d에 방향 후보 계층이 생길 때(수선 발과 같은 질의 의존 후보). 픽셀 팔: 표시 회귀가 실제로 나면 |
| **대표 직선 프레임의 겨냥 각차가 없다 — 3°(concurrent_deg)가 버리는 참 지지선 비율 미측정**(13차 리뷰어 [5] · #24) | `vp_dir_err_deg`는 원시 첫·끝점 현이고 규칙이 재는 것은 `representative`다(9차가 같은 지표에서 가른 프레임 차이). 6차 사전 트리거("분포가 더 넓으면 3°가 진짜 지지선을 버린다")가 **발화했는데** 그 판정에 필요한 프레임의 값이 원장에 없다 | `real_ink`에 대표 직선 기준 겨냥 각차를 병기하고, 그 분포로 3° 안/밖 획 수를 낸다 — 그것이 "3°가 버리는 비율"의 분자·분모다. 임계 이동은 그 뒤에도 스윕이 선행이다(#26) |

## 2026-08-19 13차 리뷰어 2차-2 — 남긴 것 (2회 반복 규약으로 등재)

| 무엇 | 사실 | 되살릴 조건 |
|---|---|---|
| **`near_vp_degenerate` 표식이 기전량의 대리라 과잉 표식이 실측됐다**(2차-2 [5] · #49) | 조건(dVp < 현 길이)은 긴 획에서 소실점이 멀어도 참이 된다 — 15-16-18의 s44(Δ10.32·지렛대 0.16°)·s45(Δ7.11·0.21°)가 표식됐지만 Δ가 퇴화 산물이 아니다(`real_ink.json@f351839a`의 `per_doc[*].near_vp_lever`). 그 과잉 배제가 `vp_dir_err_deg_stable`의 분모(n=2)를 정했다 — AS-L26 판정의 표본 크기가 대리 조건의 산물이다 | 표식을 기전량 단위(예: 지렛대각 ≥ Δ의 절반)로 다시 긋고 stable을 재계산한다 — **관측 판정자**: 다음 실획 표본이 오면 그 개정과 함께(재정의만으로도 현행 세 파일에서 stable이 n=4로 는다 — near_vp_lever가 이미 값을 든다) |
| **camera_gate 단독 벽시계의 원장 자리가 없고 실행 간 변동폭이 미측정이다**(2차-2 [7] · #25·#14) | 단독 실행 vitest 보고 176.37s는 원장 밖이다(test_cost는 전량 타이밍 파일에서 나온다 — 현행 `by_file` 175.3s가 정본). "부하 몫 철회" 판정은 176↔108/124(수십 %)의 비교 위에 있고, 176↔175(0.9%)는 변동폭 미측정 안이라 판정에 안 쓴다 | testCost.mjs에 단독 실행 팔(예: `--solo` 기록 자리)을 넣거나, 같은 조건 반복 실행으로 변동폭을 낼 때 — 200s 분할선 판정이 그 폭을 필요로 하는 시점 |
| **①·③(widen·rank_margin)의 모집단 일치 개입 팔이 이 저장본으로는 불가능하다**(2차-2 [1]·[2] · #32·#40 ③) | 저장 획이 스냅 후 기하(misfit_raw 1e-16대)라 두 경로 다 구성상 발화 불가 — 개입 팔이 위약과 동일해진다. 현행 기각은 misfit_raw < 기본 임계(widen 불요)로 서고, 도달 가능성 팔(widen_positive·rank_positive)은 다른 픽스처다 | **스냅 전 원시 기하가 담긴 표본**(aimDistPx 기록 시점 갈래 행과 같은 저장 형식 개정)이 오면 같은 개입 팔이 정보를 갖는다 |

## 2026-08-18 11차 항목 3·5 — 남긴 것

| 미룬 것 | 왜 | 되살릴 조건 |
|---|---|---|
| **`p1_cross_arm`이 이 표본에서 기각을 못 낸다**(11차 리뷰어 2차 [3] · #35) | 기각 방향의 **유일한 관측은 0**인데 그때의 p가 이미 알파보다 크다 — `verdict`가 `underpowered`인 이유다. **AS-L35의 희소성 논증은 그래서 이 자료로 세울 수도 무너뜨릴 수도 없다** | `one_box` P1 실행을 `runs_needed_for_alpha`가 든 수 이상으로 늘린다(구도·시드를 넓히면 는다). ⚠ 늘리면 `order_lock`의 실행 수가 함께 늘어 비용이 는다 — `test_cost`에 그 증가가 보여야 한다(#38) |
| **`static_deploy`의 오프라인 양성 채널이 이 컨테이너에서 시간 초과한다**(11차 항목 5 · #25) | 전량 e2e 두 번 실행에서 같은 스펙(*캐시에서 번들을 지우면 오프라인 새로고침이 실제로 깨진다*)이 `timedOut`으로 두 번 다 실패했다. **이 세션의 변경과 무관하다**(서비스 워커·빌드본을 안 건드렸다). `touch_route` dpr 2와 함께 이 환경의 실패 둘이다 — `_e2e_timing.json`의 `stats.unexpected`가 그 수를 든다 | ⛔ **실패 지점을 정정한다**(11차 리뷰어 [14]): 오류는 본문이 아니라 **`afterAll` 훅의 120초 시간 초과**다(`_e2e_timing.json`이 그 자리를 든다). 그래서 원인 후보는 본문 셋이 아니라 **정리 훅**(서버 close·서비스 워커 해제)이고, 같은 이유로 **그 파일의 `seconds`가 0으로 기록된다** — 훅 시간은 테스트 결과 시간 밖이라 `e2e_by_file`이 이 갈래 최대 비용 항목을 놓친다(리뷰어 [4] — `totals.e2e.wall_seconds`·`unaccounted_seconds`가 그 크기를 든다). 원인 분리는 **훅에서 무엇이 안 끝나는가**부터다. **실패값을 원장에 먼저 쓰는 형태로** 고치는 것이 선행이다(`touch_route`의 그 교훈) |
| ⚠ **절반 해소(11차 항목 5)** — ~~개입 팔에 `missed`가 없다~~ → **`sweep_midpoint_off`를 넣었다**(같은 분모·같은 반경 축). 남는 것: **그 팔의 모집단이 중점 효용을 구조적으로 못 담는다** | 연결 팔의 `missed` 분모는 **이미 그린 획의 끝점을 겨냥한 시작점**이라 **중점을 겨냥한 연결이 아예 없다**(#11 — 분모가 전부인가). 그래서 "끄면 놓친 연결이 거의 안 는다"가 나와도 그것은 **끝점 겨냥에 한정된 사실**이고 **"중점을 꺼도 된다"로 읽으면 안 된다** | 중점을 겨냥하는 조작(선분 가운데에 맞물리게 긋기)을 픽스처에 넣는다 — 그 획을 만들면 분모가 생긴다. ⚠ 그때 `wrong`의 규약(떨어진 덩어리에 붙으면 틀렸다)과 충돌하지 않게 **같은 덩어리 안**에 둔다 |
| **r8에서 1점·2점 층을 재는 픽스처가 없다** | 분리 체제에서 그 두 구도의 `min_cand_px`가 53~66px이라 **조리개 8px이 닿을 수 없다** — 도달은 r25~r40의 지터 꼬리뿐이고, 앱 동작점의 비용은 계속 미상이다 | 후보를 **8~40px 사이**로 데려오되 덩어리는 안 겹치는 배치가 필요하다. 이 오프셋 계열(한 방향 광선)에는 없다 — **둘째 덩어리를 첫 덩어리의 연장선 위**에 두는 배치가 다음 후보다(교차·연장 후보가 그 자리를 만든다) |
| **보정 팔의 구도 비교가 짝지은 층이 아니다**(11차 항목 3 · D-L88 · #11·#24) | 배율이 구도마다 **0.2~0.8**로 다르고 `min_cand_px`도 **0.95~53.86**으로 두 자릿수 차이가 난다 — 그래서 "1점이 3점보다 이르게 샌다"는 **같은 인접도의 비교가 아니다**. 판정을 정하는 양이 `min_cand_px`인데 그것을 안 맞췄다 | `min_cand_px`를 목표값(예: 12px)에 맞추는 셋째 팔. ⚠ **너무 작게 맞추면 '붙는다'가 자명해져 판별력을 잃는다**(그래서 이번에 안 했다) — 목표값 자체를 스윕해야 한다(#12) |
| ✅ **해소(11차 항목 3 — 리뷰어 [3]·#43 대응)** — ~~`osnap_two_sided`가 후보의 뻗음을 개입으로 못 가른다~~ | (연혁) 보정 팔이 `min_cand_px`를 관측만 하고 개입이 없었다. **`midpoint_off_adjacent` 팔을 넣었다** — 인접 체제에서 **중점 토글 하나만** 끈 대조이고, r8의 `wrong`이 네 구도에서 크게 준다(2점은 불변 — 애초에 중점 누수가 없다) | (닫힘) **남는 것**: 교차점·수선발 토글의 같은 대조, 그리고 **끈 대가**(놓친 연결)를 이 원장이 안 잰다 — 두 곡선을 같은 축에 놓는 것이 이 원장의 존재 이유인데 개입 팔에는 `missed`가 없다 |
| **보정 팔의 `wrong_kinds` 칸이 후보 종류로만 갈리고 반경 두 점(r8·r40)뿐이다** | 종류별 분해를 **모든 반경**에 두면 원장이 커진다고 r8·r40만 냈다 — 그 사이에서 지배 종류가 바뀌는 자리(중점 → 끝점)를 못 본다 | 종류 × 반경 표를 한 구도에서만 전부 낸다(원장 크기와 맞바꾼다) |
| **보정 팔의 시드 폭이 미측정이다**(#14) | 본 팔의 `by_seed` 부재(이미 등재)와 같은 자리다. 2점 구도의 도달값이 **r25에서 1 · r30에서 2**라 시드 하나가 그 칸을 0으로 만들 수 있다 — 게이트 ③은 `max_wrong_at`(r40의 27)이 들어 안 깨지지만, **작은 칸을 결론에 쓰면 안 된다** | 시드별 분해를 두 팔에 함께 넣는다(본 팔의 미측정 행과 같은 작업) |
| ✅ **닫힘 — 중복·낡은 행이었다**(13차 항목 7 정정): ~~e2e 비용이 여전히 원장 밖이다~~ | 11차 항목 5가 이미 해소했다 — `test_cost.json`의 `totals.e2e`(파일·건수·seconds·**wall_seconds·unaccounted_seconds**·stats)가 현행이고 13차 전량 실행에도 든다. 이 행은 10차 등재분이 11차 해소 후에도 안 지워진 중복이었다 | (닫힘) |

## 2026-08-18 10차 항목 0 — 남긴 것

| 미룬 것 | 왜 | 되살릴 조건 |
|---|---|---|
| **`touch_route` dpr 2 임계(1e-11)가 9차 실행 환경의 밴드에 매였다** | 9차 항목 3이 임계를 그 세션 환경의 밴드(6.95e-14~5.72e-13) 상단 ×17.5로 맞췄는데, 이 세션 컨테이너(리눅스·사전 설치 크로뮴·캔버스 675px)에서는 잔차가 **~2.4e-6**으로 세 실행 연속 실패한다(**base 커밋에서도 실패 재현** — git stash 대조. 이 항목의 변경과 무관). ⚠ **10차 정정(리뷰어 2차 [11])**: 재생성·커밋된 원장이 실패 실행의 값을 담는다 — 그 스펙은 단언 **전에** 원장을 쓴다(`dpr2.rotate`의 두 필드 차가 그 값: 실행별 1.855e-6 · 2.753e-6. 첫 관측 ~2.4e-6은 커밋 전 실행의 테스트 출력값 — 잔차 자체가 실행마다 1.8~2.8e-6 대역이다). 같은 원장의 `az_tol_note`가 **환경 간 밴드 차이를 6차에 이미 기록해 뒀다**(chromium-1194 컨테이너에서 1e-10대 실패·변경 전 코드 재현). 원인을 "기계 밴드"로 좁힐 근거는 없다 — **브라우저 판·OS·캔버스 크기(674↔675)가 함께 다르다**. ⚠ 같은 스펙의 팜 리젝션 팔도 이 환경에서 **간헐 실패 1회**가 관측됐다(재실행 통과 — 미기록 관측, #25) | ~~① 실패 실행도 측정값을 남기게 값을 원장에 먼저 쓰고 단언한다~~(이미 그렇다 — 10차에 확인) ② 임계를 실행 환경의 밴드에서 유도하거나(런타임 캘리브레이션) 환경 간 분포를 재서 상한을 그 위로 둔다. ⚠ 판별력을 잃지 않는 값이어야 한다 — 9차가 1e-6을 "175만 배라 아무것도 안 가른다"고 내린 그 이유가 기준이다 |
| **4-3의 "놓지 않되 버리지 않는다"가 모집단 지표 없이 픽스처 하나 위에 있다**(10차 리뷰어 2차 [4] · #12·#16) | 9차 착수 표가 "#16이 가장 큰 위험 — 미배치로 옮겨갈 뿐인지 **미배치 수를 반드시 함께** 낸다"고 등록했는데, 항목 1이 낸 것은 `dir_state.json` 한 픽스처(9획·dpr 1)의 `placeBy`와 `basic_flow.waiting 2`뿐이다. **대기 획이 연쇄로 회수되는 비율**(모집단)과 조용히 틀림의 전후 대조가 없다 | `order_lock` 계열은 mainL을 안 지나므로(10차 실측 — 측정 갈래 전량 불변) 그 하네스로는 못 잰다. 앱 경로의 모집단 팔(합성 상자 여럿을 Playwright로 긋는 e2e 하네스)이 필요하고, 그것은 `elevation_flow`의 확장이 자연스러운 자리다 |
| ⛔ **닫는다(2026-08-19 14차 항목 5 · D-L94 — 부분: 좌표 규약은 닫히고 표시 규약 둘이 잔여 행으로 넘어간다)** ~~**핀 상태의 핀치(화면 줌)를 안 다뤘다**~~(10차 항목 5 · D-L86 · 리뷰어 4차 [14]) | 핀치·휠이 화면 줌으로 배선됐다(`view_pan.json@f351839a` ③″). 이 행이 금지한 "부분 도입(좌표계 혼합)"과의 관계(리뷰어 [7]): **좌표계에 닿는 둘(입력 되돌림·오스냅 조리개)은 한 번에 갈았다** — 입력 (표시−팬)/z와 조리개 radiusPx/z가 같은 `viewZoomNow` 훅을 지나고, 잉크 굵기도 ctx 배율로 표시 규약을 따른다(§6.1의 혼합 없음). 안 간 것은 **표시·고르기 규약 둘**(아래 잔여 행) — 좌표 혼합이 아니라 표시 결함/반경 규약이라 "한 번에"의 사거리 밖으로 판단했다(그 판단 자체를 여기 남긴다) | (닫힘 — 잔여는 아래 행) |
| **화면 줌의 잔여 — 3D 층 굵기·클리핑·PICK_TOL이 배율을 모른다**(14차 항목 5 · D-L94 알려진 표시 한계 · 리뷰어 [5][7]) | ① 잉크 층 선 굵기는 배율을 따르는데(종이 확대) 3D 층 화면 굵기는 불변 — 배율≠1에서 두 층 굵기가 갈린다 ② 가장자리 소실점 표식·잠정 그리드 클리핑이 팬·줌을 모른다(팬 행과 같은 문) ③ `PICK_TOL`(고르기·지평선 손잡이)은 문서 px 고정이라 **표시 반경이 z에 비례해 변한다**(줌인 과대·줌아웃 과소 — 초판 D-L94의 "체감 차가 작다"는 방향이 반대였다. 측정 없음) | 실기에서 배율≠1 사용이 확인될 때 — ③은 조리개와 같은 `viewZoomNow`를 걸면 되고, ①은 3D 층 라인 머티리얼 폭에 z를 곱하는 한 자리다 |
| **큰 화면 팬에서 소실점 가장자리 표식·잠정 그리드가 표시 밖으로 나간다**(10차 항목 5 · D-L86 · 리뷰어 4차 [14]) | 화면 밖 소실점의 가장자리 표식과 잠정 그리드의 클리핑이 **문서 좌표의 화면 사각형** 기준 그대로다 — viewPan이 크면 표식이 밀린 표시 영역 밖에 그려져 안 보일 수 있다. **표시 결함이지 좌표 결함이 아니다**(입력·배치는 viewPan을 되돌린다 — `view_pan.json` ③′) | 클리핑 사각형을 표시 기준(문서 사각형 − viewPan)으로 바꾸면 된다 — 화면 팬을 실사용에서 크게 쓰는 것이 확인될 때 |
| ⛔ **닫는다 — 다만 "해소"가 아니라 "되살릴 조건이 틀렸다"다**(2026-08-18 11차 항목 5 · 리뷰어 [6]): **`cube_frame`의 판별값이 크기로는 픽스처 상수다**(10차 항목 6 · 리뷰어 5차 [1] · #40). **막던 것은 걷었다** — `setupConfirmed`가 `yaw`·`pitch`를 받고 **둘째 구도**(요 20°·피치 25°)가 돈다. ⛔ **그런데 그 구도로도 크기는 정보를 못 싣는다**: 옛 배선의 시선이 정확히 세계 −z인 한 `old_frame.dot_gaze_depth`는 **깊이축의 −z 성분** 자체이고 그것은 픽스처가 요·피치로 만든 값이라 **cos(요)·cos(피치)와 같을 수밖에 없다**(실측 일치 1e-16 — 어떤 각을 넣어도 통과하는 **항등**). 즉 이 행이 적어 둔 되살릴 조건("둘째 구도에서 따라가면 예측 검증이 된다")은 **성립하지 않는 조건이었다.** 둘째 구도가 실제로 잡는 것은 **픽스처 매개변수화의 자기확인**이다. `_fixture_determined` 표시는 **영구 유지**하고, 이 원장이 재는 것은 그대로 **자리바꿈**이다 | (닫힘) 크기로 판별하려면 **다른 팔**이 필요하다 — 시선이 세계 −z로 고정되지 않는 개입(예: 기준계를 부분 회전) | (연혁 — 13차 5·6·7-R 7[5]로 칸 정리 · #42 ⑤) 옛 기준계 팔의 두 값은 항등 1(world_z)과 cos35°·cos15° ≈ 0.7912(depth — 이 픽스처에서 다른 값이 나올 수 없다)뿐이다. 원장이 실측하는 것은 **자리바꿈**(정확히 1이 어느 슬롯에 오는가)이고 크기는 정보량 0이다 — 원장·D-L87에 명시. ~~요·피치를 흔든 둘째 구도에서 depth가 cos(요)·cos(피치)를 따라가면 크기가 예측 검증이 된다~~(**폐기된 조건** — 행 머리가 적은 그대로 성립하지 않는 조건이었다. 초판 칸을 지우지 않고 남겨 뒀던 것을 제자리 정정) |
| ✅ **해소(2026-08-18 11차 항목 5)** — ~~**`test_cost.json`이 e2e(Playwright) 갈래를 안 덮는다**~~(리뷰어 5차 [14] · #38). `testCost.mjs`가 Playwright JSON 리포터(`_e2e_timing.json`)를 접어 `totals.e2e`·`e2e_by_file`을 낸다. ⚠ **두 갈래의 시간을 한 비율로 안 합친다**(실행기가 달라 분모가 다르다 — #11) — 그래서 e2e에는 `share`가 없다. ⚠ 파일이 없으면 `null`이고 그것이 "안 쟀다"의 표시다(0이 아니다 — #32) | (연혁) | 9차 지시 c("팔을 더하면 비용을 원장에 남긴다")를 vitest 갈래만 이행 중이다 — 10차가 더한 e2e 스펙 넷(dir_state·view_pan·cube_frame·osnap_config)의 비용(각 3~5초 + 전량 실행에 포함)이 원장 밖이다 | `testCost.mjs`가 Playwright의 JSON 리포터 출력도 합치게 확장 — e2e 팔을 다음에 더하는 세션이 함께 한다 |
| ⚠ **같은 항목이 아래에도 있다 — 그쪽이 현행이다**(11차 리뷰어 [8]): "위약 비교가 짝지은 층이 아니다" 행을 보라(절반 해소). 이 행은 **중복 등재**였고 내용을 그쪽으로 합친다. ~~**`rule_camera`의 스냅↔위약 비교에 짝지은 층이 없다**~~(리뷰어 5차 [15] · #11) | 위약 대조 넷(D-L85 ⑤·AS-L24)이 전부 비짝지음이다 — 팔 간 `axes_measured`가 최대 14축 다르고(341↔327 등) `paired_headline`은 raw↔snap에만 있다. 0.35~2.0° 격차가 층 구성 차일 가능성을 못 배제한다 | raw↔snap의 `paired_headline`과 같은 자리에 snap↔placebo 짝층을 넣는다 — 이 하네스를 다음에 여는 세션이 함께 |

> **2026-08-15 L 전환에서 새로 시작했다.** 이전 항목은 `docs/archive/DEFERRED_SG.md`.

---

## 2026-08-18 8차 2차 지시 항목 4 — **진단만 하고 구현을 이월한다**

| 미룬 것 | 왜 | 되살릴 조건 |
|---|---|---|
| ⛔⛔⛔ **사람이 보고한 "끝점 스냅 0/5"의 원인은 여전히 모른다**(8-2R‴ [2]) | 이 세션이 특정한 **2D 단계 배선 누락**은 그 관측을 **설명하지 못한다** — `real_ink.json@f351839a`의 `snap_use` 분모가 **3D 획**이라 그 획들은 확정 경로를 지나고 `endSnapAt`의 후보(`lifted`)도 있다. 그리고 그 0/5는 **사람 보고 · 표본 5 · 파일 미도착**이다(AS-C1 · #25). `confirm_rules.json@f351839a`의 `end_probe`는 **6동작점 중 둘에서 "붙었다"**를 낸다 — "언제나 null"이 아니다 | **`.brnl`이 오면 `snap_use`가 실측한다**(항목 8). 그 전에는 원인을 못 정한다 |
| **4-f·4-g를 손도 안 댔다** | 4-f(`snapStatic(segs, poseKey)` 캐시 수리와 충돌하는가) · 4-g(끝점 스냅이 돌면 `ambiguous` **1037**이 얼마나 주는가). 초판이 **누락으로 적지도 않았다** | 4′와 함께 본다. 4-g의 대상은 항목 2가 낸 1037이고 하네스(`order_lock`의 `ask_kinds`)가 이미 그 수를 낸다 |
| ⛔⛔ **2D 단계의 끝점 스냅이 `snapEnd`에 안 적힌다** — 원인을 특정했고 **안 고쳤다**. ⚠ **그것이 위 0/5의 원인이라는 귀속은 철회했다** — 구조적 사실로만 남는다. ⚠⚠ **미룬 사유가 약하다**(8-2R‴ [3]): `DEFERRED`의 4차 지시 1 절이 **같은 문제에 이미 답을 적어 뒀다**("별도 필드(`snap2DistPx` 등)로 갈라 담는다"). **그 선례를 안 봤다** — A-3("선례가 있으면 선례를 따른다")대로면 진행했어야 한다 | `resolve2dCore`가 `end2`를 계산하고 좌표를 옮기고 표식까지 그리는데(`mainL.ts` 1789행), **`applySnapToEnd`는 3D 경로 둘(643·2130행)에서만 불린다.** 그래서 "snapEnd 전부 null · snapDistPx 전부 0"이다 — **스냅은 걸렸고 기록만 안 된다.** ⚠ **항목 5-3의 연결 판정이 그 필드를 읽으므로 5가 안 선다** | `onStrokeEnd`에서 `r2d.end2`가 있으면 `applySnapToEnd`를 부르는 일이다. ⚠ **`snapEnd.at`이 3D 좌표(`Vec3`)라 2D 단계에는 넣을 값이 없다** — 자료구조를 2D 스냅도 담게 넓히거나(§6.1의 `pts2d` 보존과 같은 자리), 별도 필드를 두는 판단이 필요하다. **그것이 미룬 이유다** |
| **확정 뒤에도 대기 2D 획은 끝점 스냅 대상이 아니다**(지시 4-a) | `endSnapAt`의 후보가 `snapSegs()` = **`lifted(doc)`뿐**이다 | 위와 같은 자리에서 함께 본다 |
| **osnap_config ①(조리개 거부)의 통과 형태가 on_face 우승일 수 있다**(14차 항목 4 R2 [R7] · #46·#34) | measured `r15: "on_face"`·`r15_dist 0` — 22px 오조준이 "끝점으로 안 붙는" 것은 조리개 거부가 아니라 화면 거리 0인 on_face가 이긴 귀결일 수 있다(④는 같은 픽스처-보장 형태를 스스로 고쳤는데 ①은 아니다). 같은 원장에 실행 카메라 상태(standing/order) 필드도 없어 층 귀속이 코드 독해다(R2 [R11] · #25) | osnap_config를 다음에 여는 세션이 함께: ① r15 팔에서 on_face를 끄고도 서는지 ② 원장에 standing/order 필드 |
| ✅ **해소 — 마감 지연**(9차 항목 2-f가 넣었고 14차 항목 4가 닫는다 · 4-R1 [10]) ~~**2D에 수선 발(perp)이 없다**~~(지시 4-e) | `perp2dCandidates`(snap2d.ts)가 `resolve2d` 끝점 후보에 배선돼 있고 발화·토글 반례는 `perp2d.test.ts`의 `resolve2dCore` 팔이 잰다(앱 확정 전 판정과 같은 함수 #17). ⚠ **브라우저 종단 2D perp 팔은 없다**(snap2d_flow의 기록 kind는 endpoint뿐) — 그것이 잔여 강화 후보다 | (닫힘 — 잔여: 종단 2D perp 팔이 필요해지면 snap2d_flow에 한 팔) |
| **이 진단이 원장 밖이다**(#25) | 전부 **코드 독해**이고 카운터·하네스가 없다 | **다음 세션의 첫 일감**: `snapEnd`가 적히는 비율을 **2D 단계 ↔ 확정 뒤**로 갈라 세는 팔. 그것이 있어야 고친 뒤 **양성 채널**이 선다(#30) |
| **기본 오스냅 반경 축소에 측정이 반대한다**(지시 4-d) ⛔ 초판의 "근거가 없다"는 **틀렸다**(8-2R‴ [4]) ⛔⛔ **해소(10차 항목 4 · D-L85 — 리뷰어 4차 [13])**: **축소했다** — 기본 8px. 10차 지시가 지목한 근거는 이 행의 지표(틀린 붙음 비)가 아니라 **D-L80 모순 72 → 35**(`classify_leak.ablation`)다. 이 행의 관측(틀린 붙음이 반경과 함께 준다)은 그 시점 값으로 남고, 반경 8의 대가 여섯은 D-L85에 있다 | `snap.json@f351839a`의 `by_radius_all`에서 **틀린 붙음이 반경과 함께 단조 감소한다**(0.008 **0.2111** → 0.050 **0.0912** → 0.070 **0.0796**). `osnap_config.json@f351839a`도 "합성 측정은 반대를 가리킨다"를 이미 적는다. ⚠ 그 값은 **비**이고 하네스 58.6px ↔ 앱 72px이다(#24). **A-3대로면 축소는 기각 쪽**이고, 지시 4-d의 "너무 넓다는 보고"와 **어긋난다** | `real_ink`(표본 0)가 판정자다. ⚠ **`OSNAP.kinds`의 "7종 토글이 이미 있다"도 2D에서는 과대다** — `snap2d.ts`가 내는 것은 **셋**(endpoint·midpoint·intersection)이라 나머지 넷의 토글은 2D에서 아무것도 안 가른다 ⛔ **제자리 정정(2026-08-18 9차 항목 2 · 9-R″ [14])**: **셋이 아니라 넷**이었다 — `static2dCandidates`가 `vertex`(끝점이 둘 이상 만난 자리)도 낸다. 그리고 **항목 2-f가 수선 발을 넣어 이제 다섯**이다(`perp2dCandidates`). 2D에서 안 걸리는 토글은 **`on_edge`·`on_face` 둘**이다 |

## 2026-08-18 8차 2차 지시 항목 1·2 — 남긴 것

| 미룬 것 | 왜 | 되살릴 조건 |
|---|---|---|
| ⛔⛔ **P1의 배치를 재는 원장이 앱 경로를 안 탄다**(항목 2-a · #17) | P1 실행 **59개 전부**에서 `one_point_direct_open`이 참이다 — **앱은 `directSegment`로 놓고 `order_lock`은 `liftAll`로 놓는다.** 그러므로 **이 원장의 P1 중앙 1.6914는 앱을 서술하지 않는다.** ⚠ 하네스에 직접 경로를 넣는 것은 **앱 배선의 복제**라 #17을 반대 방향으로 어긴다 | 옳은 자리는 **`elevation_flow`**(직접 경로를 실제로 타는 e2e 원장)인데 그 하네스는 **참값 대조가 없다**. 참값을 든 픽스처를 그 e2e에 넣는 일이고, 그것은 "측정 경로와 앱 경로를 섞는다"(#17)의 반대편 함정이라 설계가 필요하다 |
| **깊이 획의 38%가 화면 축으로 샌다**(항목 1-b) ⛔⛔ **제자리 정정(2026-08-18 9차 항목 0 · D-L80 · #42 ⑥)**: 아래 수는 **가드 이전**이고 이제 `order_lock.json`의 **`guard_off` 팔**이 그 값이다. 현행(`fixed` 팔): `one_depth_line` **66실행 · 296 중 114** · `too_short` **116** · 풀에 드는 것 66. ⚠ **D-L85(반경 8) 후 다시 움직였다** — 현행은 원장을 읽는다(P0 88 · `p0_leak` leak 63/404 · too_short 206/404: 모순이 줄어 P0 자체가 줄었다). ⛔ **원인 귀속 "`classifyLine`의 4° 오독"은 철회했다** — 누수 1035/2880 중 **871이 참 투영선에서도 4° 안**이라 임계로는 못 가른다. 실제로 고칠 수 있었던 것은 **스냅이 판정을 뒤집는 모순**(108 → 72)이다 | `one_depth_line` 87실행에서 참 축이 깊이인 획 **392개 중 148**(`support` 137 + `screen_axis` 11)이 화면 축으로 읽히고 **157이 `too_short`**다. 대기 풀에 드는 것은 **87**(실행당 하나)뿐이다. ~~**`classifyLine`의 4° 오독은 안 고쳐졌고**~~, 그 귀결이 "나쁜 카메라"에서 **"카메라 없음"**으로 옮겨 갔다(#16) | 지시 1-a가 이미 그 자리를 지목했고(`stepRule` 739~745행) **8차 1차 지시 항목 1이 P1 잠김만 고쳤다.** 남은 것은 **애매 구간을 넓히거나**(4° → 더 좁게) **`min_vp_len_ratio`를 낮추는 것**이고 둘 다 조용히 틀린 배치를 늘릴 수 있다 — 스윕이 필요하다(#12) |
| **`stroke_budget`의 "상자 둘"은 실사용의 모형이지 실사용이 아니다**(항목 1-d · #32) | 사람은 상자를 하나 더 그리는 대신 **같은 상자에 선을 덧그을** 수도 있고, 그러면 획은 늘되 **새 정보는 안 는다.** 그때도 P0가 140 → 63으로 주는지는 **안 쟀다** ⛔ **제자리 정정(2026-08-18 9차 항목 1 · 9-R′ [R5] · #42 ⑥)**: 그 두 수는 가드 이전이다. ~~현행 `stroke_budget`은 `one_box` **122** → `two_box` **65**다~~ ⛔ **제자리 정정(10차 리뷰어 4차 [4] · #42 ⑥)**: 9차 정정문 자체가 틀려 있었다 — 122·65는 **P0 개수**인데 지표 이름 없이 적었고, 10차 D-L85(반경 8) 재실행으로 값도 낡았다. 현행(원장을 그 자리에서 읽었다): P0 `one_box` **88** → `two_box` **69**다 | `real_ink`(표본 0). ⚠ **위약이 없다** — "상자를 하나 더"는 정보를 늘리므로 위약이 아니고, 정보를 안 늘리는 획 증가(덧그리기) 팔이 그 자리다(#39) |
| **획을 더 주면 P1이 늘고 P1은 나쁘다**(항목 1-d·2-c) ⛔ **제자리 정정(2026-08-18 9차 항목 1 · 9-R′ [R5] · #42 ⑥)**: 아래 넷이 가드 이전 값이다. 현행 `stroke_budget`(그 자리에서 읽었다) — P1 **60 → 226** · P2 **418 → 309** · 형태 오차 중앙 **0.5901 → 0.8243** · `camera_ok` 478/600 → **535/600**. **결론의 방향은 그대로다**(획을 더 주면 P1이 늘고 형태 오차가 나빠진다). ⛔ **제자리 정정(10차 리뷰어 4차 [4] · #42 ⑥)**: 10차 D-L85(반경 8)로 그 넷도 낡았다. 현행(원장을 그 자리에서 읽었다): P1 **65 → 227** · P2 **447 → 304** · 형태 중앙 **0.5869 → 0.75** · `camera_ok` 512/600 → **531/600** · P1 절단 0.2 안 `one_box` **0/330** ↔ `two_box` **33/2642**(P2 `two_box` 214/3454 — P1이 P2보다 **5.0배** 나쁘다. 옛 "7배"도 그 시점 값이다). 방향은 셋 다 그대로다. ⚠ 그리고 **원인 귀속의 뒷부분이 낡았다** — "항목 1-b의 `classifyLine` 자리"는 9차 항목 0이 **철회한 귀속**이다(임계가 아니라 기하와 스냅이었다) | `two_box`에서 P1 **59 → 240** · P2 401 → 297이고, P1의 절단 0.2 안은 **2869 중 15**(0.5%)로 P2의 **3185 중 110**(3.5%)보다 **7배 나쁘다.** 형태 오차 중앙도 0.5731 → **0.8429**로 나빠진다 | **P1의 소실점을 고치는 것**이 답이고(2-b가 f를 기각했다) 그것은 항목 1-b의 `classifyLine` 자리와 같다 |

## 2026-08-18 8차 2차 지시 항목 0 — 남긴 것

| 미룬 것 | 왜 | 되살릴 조건 |
|---|---|---|
| ✅ **해소(2026-08-18 10차 항목 0)** — ~~D-L83(임의 깊이 앵커 가드)을 구현·측정하고 되돌렸다~~ **되살렸다.** 네 단언을 흐름별로 확인했고(D-L83 항목의 10차 절), p1_invariance 구도의 세 흐름 + 네 입면 확정은 **3이 옳았고**, 기본 흐름 ①~③은 **2가 옳았다**(연쇄의 `on_face` 임의 앵커까지 막으면 — 9차의 "넷이 3"은 연쇄를 안 막은 구현의 값). 카운터는 경로별(`start_rejected`·`unanchored_rejected`)로 걸었다 | (연혁) 가드를 켜면 배치가 준다(9차 실측 `lifted` 4 → 3 · `rejected` 1). `basic_flow`의 단언 넷이 깨져 되돌렸었다(#19) | (닫힘) 남는 것은 **4-3**: 대기로 남은 획을 연결이 놓아 주는 연쇄 — 10차 항목 1이 한다 |
| ⛔ **`unanchored_placed`를 0으로 만드는 것은 4-5가 아니라 4-3이다**(9차 항목 4-5의 실측 · ⚠ 10차 항목 0이 가드를 켰다 — 현행 값 **3**, 전부 일괄 풀이 경로) | 8차가 `p1_invariance`에 *"항목 4-5가 지우면 0이 된다 — 이 필드가 판정자다"*라고 미리 적었는데 **실측은 4 → 3**이다. `snapStart == null`인 배치를 만드는 경로가 **셋**이고 4-5는 둘만 막는다 — 셋째 **`solveInto` → `liftAll`**(카메라가 서면 대기 획을 일괄로 올린다, §6.1)은 `on_face`가 아니라 **카메라가 좌표를 정하는 것**이다 | ⛔⛔ **그 예측도 틀렸다(10차 항목 1 — 4-3 완료 뒤에도 3이다)**: 남은 셋은 확정 순간의 일괄 풀이(첫 앵커 — D-L84 ③이 정당한 배치로 못 박았다)라 **0이 될 수 없다.** 판정자 역할은 경로별 카운터(`place_by`)로 옮겨 갔다 — 임의 배치의 판정은 `place_by.unanchored`(가드 끔 팔 대조)와 가드 카운터다 |
| **`snap_stage`의 게이트에 실패할 수 있는 조항이 없다**(9-R″ [4][4b] · #26·#30·#40 ②) | 조항 ①은 원장이 스스로 "보장이지 측정이 아니다"라 적고, ②는 `reachability`가 그대로 다시 적는 문장이다. 그리고 **`no_record` 팔은 양성 채널이 아니라 항등**이다 — 기록 호출을 건너뛴 팔이 기록 0을 내는 것은 정의의 귀결이고, 판별력을 보이는 값(**그 팔의 `mismatch` = 1811**)은 원장에 없다 | 게이트를 다시 등록한다(예: `in_aperture_not_engaged`가 0이라는 조항 — 그것은 실패할 수 있다). `no_record` 팔에 **mismatch를 함께** 낸다 |
| **`osnap_two_sided`의 r=40 `missed 0`이 정의의 귀결이다**(9-R″ [5] · #5) | `missed`의 분모가 "**40px 안에** 이을 대상이 있던 시작점"이므로 **반경 40에서 0은 보장**이다. `selfcheck_notes`의 "겨냥 분포를 통째로 덮어서"는 그래서 설명이 아니라 순환이다 — 그 분포(`aim_px_when_connecting`) 자체가 40px 창으로 **절단**돼 있다 | 분모의 창(40px)을 반경과 **분리**한다(예: 창을 80px로 두고 반경을 4~40으로 훑는다). 그러면 r=40의 0이 측정이 된다 |
| **`snap_stage`의 빠진 45행이 안 적혔다**(9-R″ [6] · #10·#11) | 게이트가 "900실행 × 12획"이라 적는데 `rows`는 **10755**다(10800 − **45**). `representative()`가 `null`을 내는 획으로 보이지만 **안 셌다** — 뺄셈으로 만든 집합이라 #10이다 | 그 갈래를 카운터로 센다(`no_representative`). `order_lock`에 같은 이름의 선례가 있다 |
| **두 지표 판정의 시드 폭이 미측정이다**(9-R″ [13] · #14) | `osnap_two_sided`에 `by_seed`가 없다. 15 대 8의 격차는 `wrong` 123 ↔ 49이고 이 저장소는 "1건/400" 격차로 임계를 고른 것에 **두 번** 걸렸다 | 시드별로 갈라 낸다. 셀당 표본이 6분의 1이 되지만 겹침 여부는 볼 수 있다 |
| ⚠ **절반 해소(2026-08-18 11차 항목 3 · D-L88 — ⛔ 초판은 ✅ 해소로 적었고 리뷰어 [2]가 잡았다)**: **1점·2점에서 잘못된 연결을 만드는 픽스처가 없다**(9-R″ [3] · #35) | (연혁) 반경 **40에서도 0/1152**라 그 층의 비용을 못 매겼다. **되살릴 조건으로 적어 둔 그것**(두 덩어리를 40px 창 바로 밖에 둔다)을 보정 팔로 이행했다 — 다만 기준을 **세 판** 만들어야 했다(참 정점 거리 · 상자 겹침 둘 다 실패 — D-L88의 표). 현행 분리 기준(`min_cand_px` > 40px)에서 **다섯 구도 전부 도달 가능**하다(게이트 ③이 단언). ⛔ **그러나 앱 동작점(r8)에서는 1점·2점이 여전히 0**이고 도달은 r25~r40의 지터 꼬리다 — **그 층의 비용은 계속 못 매긴다** | **남는 것**: ① r8에서 그 층을 재는 픽스처(후보를 8~40px 사이로 데려오되 겹치지 않는 배치 — 이 오프셋 계열에는 없다. 둘째 덩어리를 **첫 덩어리의 연장선 위**에 두는 배치가 다음 후보다) ② 짝지은 층(아래 행) |
| **반경 결정이 두 원장으로 갈려 있다**(9-R″ [2]) | 축소를 지지하는 것은 `classify_leak.ablation`(모순 72 → 35), 반대하는 것은 `osnap_two_sided.missed`(29.1% → 50.6%)다. **분모도 단위도 다르다** | 한 원장에서 두 양을 함께 낸다 — `osnap_two_sided`에 `classify_leak`의 모순 지표를 더하는 것이 가장 짧다(같은 픽스처·같은 반경 축) |
| **반경 판정의 실획 근거가 아직 없다**(9차 항목 2-d) | `osnap_two_sided.json`은 **합성**이고(AS-L24·L25) 실획으로 대조할 것이 **`real_ink.snap_dist_px`인데 그 n이 0**이다 — 첫 표본 5획이 전부 D-L79 이전 저장본이라 `snap_dist_legacy_zero` 5/5다. 지시 2-d가 "실획이 오면 다시 본다"고 했고 **아직 못 본다** | D-L79 이후에 저장된 `.brnl`이 오면 `snap_dist_px`·`snap2d_use`가 처음으로 실측한다. 그때 이 스윕의 교차점이 실획 겨냥 분포의 어디인지 본다 |
| **"안 붙어야 할 곳에 붙는다"의 남은 후보 둘이 미측정이다**(9차 항목 2-c′) ⛔ **전제 정정(11차 항목 3 · D-L88)**: 아래 첫 문장의 근거가 **픽스처의 성질이었다** — 보정 팔에서 1점은 반경 4부터 샌다. **후보 둘(관계 스냅·`on_face`)이 미측정이라는 요청 자체는 그대로 살아 있다**. ⚠ 그리고 **셋째 후보가 생겼다**: 중점·교차점 **후보의 뻗음**(D-L88 — `min_cand_px`가 `min_cross_px`보다 훨씬 작다) | (연혁) 반경은 1점·2점에서 잘못된 연결을 **0/1152**로 만들지 않는다. 남는 것은 **관계 스냅**(`classify_leak`이 1점에서 79/240을 옮기는 것을 쟀다)과 **`on_face`**(실획 `start_kinds` 5/5)인데 **둘 다 "잘못된 연결"의 관점에서는 안 쟀다** | 같은 두 덩어리 픽스처에서 **관계 스냅을 켠 팔**을 돌린다(지금은 껐다). `on_face`는 **항목 4-5**가 지우므로 그 전후로 잰다 |
| **`osnap_two_sided`의 `wrong` 정의가 픽스처 규약이다**(#5) | "떨어진 덩어리에 붙으면 틀렸다"는 **이 픽스처가 만든 규약**이지 사용자의 의도가 아니다 — 실제로는 두 덩어리를 잇고 싶을 수도 있다. 그러면 `wrong`은 과대다 | 실획에서 **되돌리기 직후 다시 그은 획**을 "잘못된 연결"의 대리 신호로 쓸 수 있다(사용자가 스냅을 거부한 흔적). 계측기가 없다 |
| **`classify_leak`에 진짜 위약이 없다**(9-R [M9] · #39) | 제거 실험의 위약 팔(⚠ 10차 D-L85 후 앱 설정 그대로 = **`r8_rel_on`** — 팔 이름은 리터럴 반경이다)은 **앱 설정 그대로의 재실행**이라 본 팔과 값이 같은 것이 **보장**이고 정보량이 0이다 — `DEFERRED`가 `axis_snap`에 대해 이미 같은 판정을 내렸다. #39가 요구하는 위약은 **같은 크기·임의 방향의 좌표 이동**이고 이 원장에 없다 | `rule_camera`의 `placebo` 모드와 같은 형태의 팔을 넣는다 — 가드가 막은 이동량만큼 **임의 방향으로** 옮겨 같은 지표를 낸다 |
| **`true_angle_arm`의 분모가 60배 복제다**(9-R [L2] · #11·#14) | `leak_n`이 360·540·780·840·1080·1320으로 **전부 60의 배수**다 — 참 투영선은 시드·등급·잡음에 안 변하므로 한 기하 케이스가 60번 세어진다. **유효 케이스는 48**이고 도달 가능성 1320/2880은 실은 **22/48**이다 | 그 팔만 분모를 유효 케이스로 바꾼다. ⚠ 다른 팔과 분모가 달라지므로 **표를 나란히 못 놓는다** — 그것이 지금 안 고친 이유다 |
| **임계 스윕이 1차원이다**(9-R [L3] · #12) | `classify_leak.sweep`이 `screen_axis_deg`만 훑고 `depth_min_deg`는 8로 고정이다. "누수가 물음으로 옮겨 갈 뿐"이라는 0-a의 결론은 **애매 대역이 0.25~8°로 벌어진 결과**이기도 한데 그 성분을 안 갈랐다 | 두 임계를 격자로 훑는다(8 × 4 = 32점). 비용은 낮다 — 이 하네스는 1초 안에 돈다 |
| **0-b의 결론에 시드 분해가 없다**(9-R · #14) | P0 140 → 122 · 누수 351/772 → 271/658이 **시드 폭 안인지 모른다**. `order_lock`에 `by_seed`가 있는데 이 항목이 안 읽었다 | `guard_off` ↔ `fixed`를 시드별로 갈라 낸다. 셀당 100실행이라 쪼갤 만하다 |
| **실획 `ask`의 6 − 4 = 2가 안 잡힌다**(9-R [L5]) | 첫 표본의 `askStats`가 asked **6** · depth **4** · screen **0** · vertical **0** · skipped **0**이다. **취소인지 카운터 누락인지 모른다** | 물음 UI에서 닫기·바깥 탭을 `skipped`로 세는지 확인한다. 안 세면 그 자리를 만든다(#7 — 추측 대신 카운터) |
| **`classifyLine`의 누수 중 `too_short`가 절반이다** ⛔ **아무도 안 건드렸다**(2026-08-18 9차 항목 0-c) | P0의 깊이 획 **658** 중 **215**가 `min_vp_len_ratio`(0.04 × 화면 대각 ≈ 47px)에 걸려 **대기 풀에 못 든다**(`order_lock.json`의 `p0_leak.too_short`). 화면 축 누수 **271**과 **같은 크기**다 — 즉 소실점이 안 서는 원인의 **절반**이 "짧아서 안 받는다"이고, 그 임계의 근거는 실획으로 확인된 적이 없다. ⚠ 실획 첫 표본은 `below_min_vp_len` **0/5**(획 길이 중앙 0.3086 대각)이라 **표본이 이 구간을 안 만든다**(n=5, AS-C1) | 임계를 스윕해 누수와 오검출을 함께 낸다(#12·#30). ⚠ **낮추면 실수로 그은 짧은 획이 소실점을 정의한다** — 그 회귀가 이 임계를 만든 이유다(A-5) |
| **깊이 판정을 뒤집는 잔여 72획은 끝점 오스냅이 만든다**(2026-08-18 9차 항목 0 · D-L80) | 가드가 방향·관계 스냅만 막으므로 **위치 스냅이 만든 모순 72/2880은 남는다**(1점 55/240). `classify_leak.json`의 `ablation`이 지렛대를 보인다 — 반경 15 → 8이면 **35/2880**, 관계 스냅까지 끄면 **13/2880** | ⚠ **축소(10차 항목 4 · D-L85 — 해소가 아니다, 10차 리뷰어 3차 [12])** — 기본 반경 8로 모순이 72 → **35/2880**(1점 31/240)으로 줄었지만 **잔여 35는 남아 있다**(`classify_leak.osnap_arm.ablation`의 r8_rel_on = 현행 앱 팔). 관계 스냅까지 끄면 3 — 그 나머지는 안 끈다(관계 스냅의 효용). 잔여 35의 처리는 이 행이 계속 든다 |
| **`tube_render.json`에 비결정 값이 있다**(2026-08-18 9차 항목 0-e) | `build_ms_total`·`build_ms_per_stroke`가 **기계 시간**이라 아무 코드도 안 고쳐도 커밋마다 바뀐다(124.5 → 154.7). 그러면 `git status`의 "움직인 원장" 목록이 **가짜 항목을 안고** 나오고, #42 ⑨의 훑기 대상이 부풀려진다 | 시간 값을 원장에서 빼거나 별도 블록으로 격리한다(그 값을 인용하는 문서는 없다) |
| ⛔⛔ **`axis_snap`의 위약 팔이 본 팔과 글자 그대로 같다**(8-2R′ [1] · #40 ②·#5) | `placebo_wrong_horizon__drawn`이 `snap_on__horizon_on__drawn`과 **`deg_median` 6.4634 · `placed` 1381 · `silent_0_2` 1184 셋 다 동일**하다(몰아도 1405/1278로 동일 — 12차 D-L89 재실행 값·동일성 불변). **그 위약은 이 원장에서 정보를 하나도 안 나른다** — 0·1이 아니면서 자명한 값이라 #40의 자동 검사가 못 잡는 종류다. ⚠ **그 동일성이 실제로 오독을 만들었다**: 끔 행과 위약 행이 나란히 있고 위약이 켬과 같아 표를 세로로 훑으면 "켬 = 끔"으로 읽힌다(그 사건 당시 값 끔 948·위약 1091 — 12차 D-L89 재실행 현행은 끔 1040·위약 1184로 같은 구조다, 2차 [11]) | 그 위약의 정의(지평선을 참과 같은 크기·반대 방향으로 옮긴다)가 **D-L59 이후 소실점 확정에 안 쓰이므로 아무것도 안 바꾸는 것**으로 보인다 — `AS-L19`가 같은 이유로 폐기됐다. **위약을 소실점 자체에 거는 팔로 바꾸거나, 대상 소멸로 표시하고 지운다**(#40 — 정보 0인 필드를 남기지 않는다) |
| **`rule_camera`의 위약 팔이 판정 팔보다 좋다**(8-2R′ [3] · #39) ⚠ **절반 해소(2026-08-18 9차 항목 0 · D-L80)** — 가드 투입 후 `rule_grouped_snap` **6.9314** < `rule_grouped_placebo` **7.1615**로 **판정 팔이 위약을 이긴다**. 다만 위약이 **원시** 몰아(7.9567, 불변)보다 좋은 것은 그대로라 아래 요청은 **살아 있다** ⛔ **제자리 정정(2026-08-18 12차 항목 1 — 화각 상한 `FOV_GATE.reject_fov_deg` 등록 재실행 · #42 ⑥⑨, 12차 리뷰어 [3])**: 이 행 머리의 "판정 팔이 위약을 이긴다"는 **D-L80 시점의 값**이고, D-L85가 재역전을 기록했으며(DECISIONS D-L85 대가 ⑤) **12차 현행도 위약이 이긴다**(D-L89 재실행: 몰아 스냅 7.068 > 위약 6.6539 — `rule_camera.json@f351839a` `headline_no_identity`). 번갈아·넓은짝은 스냅이 이긴다(전 팔의 사실처럼 안 적는다, #12) | ⛔ **12차 정정(2차 리뷰어 [11] — 행 머리만 고치고 이 칸을 빼먹었었다, #42 ⑨)**: 현행(`rule_camera.json@f351839a` `headline_no_identity` — 12차 D-L89 재실행) `rule_grouped_placebo` **6.6539** < `rule_grouped_snap` **7.068** < `rule_grouped`(원시) **8.0056** < `detect` **9.1742**. 위약은 **같은 크기·임의 방향** 이동이다. ⚠ 이 위약은 **스냅에 대한 대조**이지 "규칙 대 검출"의 대조가 아니지만, 위약이 검출·원시·스냅 전부를 이긴다는 사실은 게이트 통과를 규칙의 품질에 묶기 어렵게 만든다(옛 수치 서사(7.116/7.9567/7.4653)는 D-L85 이전 실행의 것이라 지웠다 — 방향은 같다) | **"규칙 대 검출"의 위약**(규칙 소실점을 같은 크기·임의 방향으로 옮긴 팔)이 필요하다 — 지금 원장에 없다. #39가 규정한 "같은 크기의 무의미한 이동을 함께 돌린다"의 바로 그 자리 |
| ⚠ **절반 해소(2026-08-18 11차 항목 5 — ⛔ 초판은 ✅ 해소였고 리뷰어 [2]가 잡았다)**: 위약 비교가 짝지은 층이 아니다(10차 리뷰어 5차 [15] · #11·#39) | **`paired3_headline`을 넣었다**(셋 다 카메라가 선 픽스처 · `snap_beats_placebo` 팔별). ⛔ **그러나 "판정이 짝지음 부재의 산물이 아니다"는 이 층으로 못 세운다**: 짝짓기가 걷어낸 픽스처가 **팔당 1~3개**뿐이고 **중앙값이 안 움직인 칸이 여섯 중 넷**이다(3팔 × 2지표)(원장이 `dropped_by_pairing`·`medians_moved_by_pairing`으로 스스로 든다 — 몰아 팔은 스냅·위약 **둘 다 불변**이라 비짝지음 표의 재인용이다). 말할 수 있는 것은 **"이 짝짓기에 판정이 둔감하다"**까지다 | **남는 것 둘**: ① **축 수가 여전히 팔마다 다르다**(무한원 축 제외) — 픽스처는 맞췄고 축 분모는 못 맞췄다 ② 짝짓기가 거의 아무것도 안 걷어내므로 **층 구성 차 자체가 작다는 것**이지 그것이 결론을 지지하는 것은 아니다. 원래 우려(0.35~2.0° 격차가 층 구성 차일 가능성)는 **시드 폭 미측정**(#14)이 함께 풀려야 닫힌다. **되살릴 관측 판정자**(13차 5·6·7-R 7[6] — "다시 여는 항목에서"는 착수 의사이지 관측이 아니었다. 2차-2 [12]로 재정정 — "팔이 생기면"도 착수 사건이라 뺐다): `rule_camera.json`의 팔 간 `axes_measured` 차가 **10 이하**로 좁혀지는 실행이 관측되면 이 행을 연다 |
| **`pick_vp`의 두 팔이 같은 모집단이 아니다**(8-2R′ [10] · #9·#11) | AS-L23이 "찍기 ~~1.985°~~ 대 two_lines 7.4804° — ~~3.8배~~"를 낸다(⛔ 5차 [4]: 12차 D-L89 재실행 현행은 **2.1252° 대 7.1224° · 약 3.4배** — AS-L23은 고쳐졌는데 이 행 머리가 옛 인용을 들고 있었다, #34). 그러나 `pick_0`은 `camera_ok` **240**/`axes_measured` **425**(⚠ 10차 D-L85로 **411** · 12차 D-L89 재실행 현행 **410** — 3차 [10]), `two_lines`는 12차 D-L89 재실행에서 `axes_measured` **396**이고(camera_ok는 원장을 읽는다) `pick_0`의 중앙은 **`pick_failed` 44를 안 뺀다**(⚠ 10차 D-L85(반경 8)로 **70/240**이 됐다 — 찍기 조리개가 오스냅 반경과 한 값이라 함께 줄었다. 중앙도 2.1556으로 움직였다. 원장을 읽는다). 이 항목이 `factor_split`에 세운 규약("놓인 수가 다르면 중앙을 못 쓴다")을 **같은 절에서 안 지켰다** | 분모가 같은 지표(예: 두 팔에서 **둘 다 카메라가 선 픽스처**만 짝지은 층)를 그 원장에 넣는다 — `rule_camera`의 `paired_headline`이 같은 자리의 선례다 |
| **⑨의 "인용 없음" 여섯은 "못 찾았다"이지 "없다"가 아니다**(8-2R′ [7]) | 근거로 든 `scan_cited_values` 플래그 0은 **"줄에 원장 값이 하나라도 있으면 나머지는 안 본다"**는 검사다(같은 세션이 PITFALLS에 그렇게 적었다). 그리고 **②형(해시 없는 인용)은 모집단 밖**이다 — `touch_route`·`stage_browser` 둘이 실제로 그렇게 새어 결론을 들고 있었다 | ⑧의 기계화(절 경계 파싱)와 같은 자리다 — 그때 "이 원장을 인용하는 문서 줄" 목록을 기계가 낸다 |
| **#42 ⑧의 "결론이 인용하는 원장"이 여전히 손 판정이다**(8-2R [14]) | ⑦은 기계화됐는데(`pitfall_citations`) ⑧은 "그 항목의 결론이 어느 원장을 인용하는가"를 사람이 고른다. **세 세션 연속 틀린 자리가 정확히 그 손 판정**이었다 | 재료는 이미 있다 — `scan_citation_hashes`가 **줄 단위로** `원장.json@해시`를 안다. `progress.md`의 절 경계를 잡아 **그 절이 인용한 원장 목록**을 뽑고 `pitfall_citations`와 교차시키면 착수 표를 기계가 낸다. 절 경계 파싱이 새 일이라 이 항목의 범위 밖이다 |
| **`deg_p90`의 몰림이 안 풀렸다**(8-2R [6]) | 정확히 35인 팔이 열둘 중 **여섯**이다(옛 40.66은 열둘 중 열하나). `deg_median`의 포화만 풀렸다 | 위 "`rule_camera`의 `deg_p90` 포화 여부" 행에 있다 — 픽스처의 구도 각 격자를 확인할 때 |
| **게이트 뒤집힘의 시드 폭이 미측정이다**(8-2R [2]) | 여유가 **1.2175°**인데 같은 원장이 "1~3° 급 격차는 강도가 약하다"고 적는다(#14). 그리고 `rule_wide_pair`의 여유는 **0.1164°**, ~~`rule_grouped_snap_r40`은 **검출보다 나쁘다**~~ ⛔⛔ **제자리 정정(2026-08-18 9차 항목 0 · D-L80)**: **뒤집혔다** — `rule_grouped_snap_r40`이 검출보다 낫다(⛔ 12차 정정(2차 [11]): 현행 **6.9124 < 9.1742**, 여유 2.26° — 옛 6.9637/2.21°는 D-L85 실행의 값. 행 머리의 "여유 1.2175°"도 옛 실행의 것으로 현행 원시 몰아 8.0056 기준 1.1686°다). ⚠ **시드 폭이 미측정이라는 이 항목의 요청은 그대로 살아 있다** | `rule_camera`에 시드별 분해를 넣는 일이고, 셀당 표본이 6분의 1로 쪼개진다. 그 하네스를 다시 여는 항목에서 |
| **4배가 "축 오차 감소"인지 "측정 모집단 변화"인지 안 갈린다**(8-2R [9]) | `axes_measured` 246 → **346** · `screen_axes_total` 330 → **230**(`rule_grouped`). `detect` 불변은 픽스처 불변만 배제한다(#9) | 짝지은 축 집합(양쪽 판에서 **같은 축**만) 위의 중앙을 내는 팔이 필요하다. 판 사이 축 동일성을 잇는 키가 지금 없다 |

| **미리보기↔확정의 resolve2d 두 호출을 나란히 재는 팔이 없다**(12차 2차 리뷰어 [3]) | 무앵커 획의 미리보기(onLive — excludeId 없음·획이 문서에 없음)와 확정(placeStroke — excludeId)이 **같은 함수·다른 인자**다. 후보 집합이 구성상 같다는 것은 코드 항등 논증이고 측정이 아니다. 앱 수준 방향 일치는 `dir_state` ⑧이 들지만 두 호출의 **동일 좌표** 단언은 아니다 | live2d 결과를 확정 직전 캡처해 placeStroke 결과와 대조하는 e2e 팔 — §11 게이트를 이 가지로 넓힐 때 |
| **교차 앵커의 가림 교차 — ⚠ 절반 해소(13차 항목 7: 실측 도착 · 정책은 남음)**(12차 항목 3-a · #23) | 화면 교차가 3D 접촉이 아닌 **비율**은 합성 상자의 3/27이 유일하고, ⚙️ **실획이 새로 낸 것은 어긋남의 크기다**(`first_anchor.json@f351839a`의 `cross_occlusion` — 5·6·7-R 7[1]로 두 양을 가른다: **이 팔은 비율의 분자/분모를 안 낸다**, 실측 획 n=1(s49)·쌍 3): 세 교차가 시사하는 직선들이 서로 `pairwise_line_offset_ratio` 0.1563~0.6758 어긋난다(분모 "평균 앵커 거리"가 문서들이 '장면 규모'라 불러 온 그 양의 대리 — 원장 note에 명시). 참 접촉은 많아야 하나, 나머지는 가림이다. `crossAnchorOf`는 최근접 교차 하나로 놓는다(잔차 검증 없음). ⛔ 3차 리뷰어 [1] 정정 그대로: 재투영 검사는 구성의 항등이라 가림을 원리적으로 못 거른다 | **남는 것은 정책이다**: "2+ 교차 일치" 검증은 **허용치·술어 의존**(5·6·7-R 7[2]·2차-2 [8] · #13·#28 — 생존 경계는 술어별로 다르다: `verify_survival_boundary.any_pair_agrees` 0.1563(한 쌍 채택) / `all_pairs_agree` 0.6758(전 쌍 요구). 허용치가 앞 값 아래면 어느 술어에서도 s49 회복이 죽는다. 앱에 대응 임계는 아직 없다). **되살릴 관측 판정자**(7[6]): 다음 표본들에서 2+ 교차 획의 `pairwise_line_offset_ratio` 최솟값 분포가 나오면 — 참 접촉 쌍이 경계 아래로 모이면(예: p50 < 0.05) 그 값으로 검증을 켜고, 계속 0.15 위로 흩어지면 일치 검증 대신 규칙(최하단 교차 = 지면 접점 우선, SketchUp 선례 확인)으로 간다 — 이 실측(offset 분포)이 그 설계 입력이다 |

| **p1_guard 잔여 물음의 원 방향 내역 칸이 없다**(12차 3차 리뷰어 [4] · #43) | D-L89 뒤 합성(fixed 팔)의 잔여 물음은 `ask_kinds` 전부 `p1_guard` **45**인데(수정자·토글이 없는 하네스다), 그 내역 — ① 스냅 파이프라인 밖(원시 팔) ② **끝점 오스냅 좌표 이동으로 가로가 된 획**(D-L80 잔여 모순 — `classify_leak.r8_rel_on.contradiction` 35/2880) — 을 가르는 칸이 없다. ②는 hint가 안 붙어 가드가 잡는 것이 **옳은 동작**인데 그 수가 안 세어진다. D-L70 문의 개폐 판정이 이 내역에 걸려 있다 | `ask_kinds`를 site별에서 **원 방향 kind별**로 확장할 때 — order_lock 하네스를 다시 여는 항목에서 |
| **D-L89로 사라진 애매 물음의 참 축 분포(오라클 대조)가 없다**(3차 리뷰어 [5]) | fixed 팔에서 애매 물음 1121건(11차 말)이 자동 깊이 판정이 됐다 — 합성에는 참 축 오라클이 있으므로 그 자동 판정의 정오를 낼 수 있는데 칸이 없다. 지금 있는 것은 하류 지표(camera_ok_rate·축 오차 — 방향 유지)뿐이다 | 애매 대역 획의 (자동 판정, 참 축) 쌍을 세는 칸 — 같은 하네스 개정에서 |
| **교차 앵커 조항(dir_state cross_anchor=1)의 전용 대조 팔이 없다**(3차 리뷰어 [8] · #26·#35) | 조항이 **사후 등록**이고(관측 뒤 기대를 고쳤다 — 원장에 명시) 도달 가능성 오라클이 옛 end_anchor 팔의 값이다. 교차만 없앤 픽스처(s4가 dir로 남는 대조)가 없다 | dir_state에 setChainExt 세분(교차만 끄는 스위치)을 넣을 때 |

## 2026-08-18 8차 항목 2 — 남은 물음의 나머지

| 미룬 것 | 왜 | 되살릴 조건 |
|---|---|---|
| **애매 구간 1037 중 커서가 답을 갖는 것은 일부뿐이다** | `hint`는 **2D 스냅이 발동한 획**에서만 값이 있다(`ortho`·`vpdir`). 스냅이 안 걸린 자유 선은 커서에 답이 없어 여전히 묻는다 — 실측 감소가 팔당 **−2.5% 안팎**이고 없앨 수 있는 96%에 한참 못 미친다 | **`axisSnap.chooseAxis`의 커서 규칙(투영 이동량 최소)을 애매 구간 판정 자체에 잇는다** — 스냅 여부와 무관하게 커서가 답을 내게 하는 일이다. 바닥은 `p1_guard` **43**이다 |

## 2026-08-18 8차 리뷰어 15건 — **대응 못 하고 이월**(높음 6 · 중간 6 · 낮음 3)

리뷰어를 8차에 처음 정상 호출했고 항목 0·1을 걸었다. **[7]만 고쳤다**(스냅 팔 각도 서술이
거짓이었다). 나머지는 **다음 세션이 먼저 처리한다** — 특히 [1][2][3][5][6]이 높음이다.
✅ **2026-08-18 2차 지시 항목 0에서 남은 넷([5]·[11]·[13]·[14])을 닫았다**(취소선 표기).
⛔ **"이 표에 열린 행은 더 이상 없다"는 틀렸다**(8-2R′ [5]): 취소선이 붙은 것은
**[3][4][5][11][13][14] 여섯**이고 **[1][2][6][8][9][10][12][15] 여덟은 표기가 그대로다.**
그 여덟은 **8차 1차 지시 항목 0에서 대응했다고 기록된 것들**인데(`progress.md`의 그 절)
**이 표에만 반영이 안 됐다** — 아래에 표기를 맞춘다. ⚠ **[12]는 그 행 자체가 "부분 대응"이라
적으므로 닫지 않는다.**

| # | 지적 | 왜 아픈가 |
|---|---|---|
| ~~**[1]**~~ ✅ **해소**(8차 2차 지시 항목 0에서 `touch_route` 여덟 자리 정정)  높음 | **원장을 전부 재실행해 놓고 인용 재확인 모집단을 "원격 갈래 20곳"으로만 잡았다** — 이 갈래가 인용하던 `touch_route.json` 값이 낡은 채다: `progress.md:4265` 1.2551 → **1.0875938959012235** · `:4266` 1.0860/0.2327/1.2e-14/6.677/1.5441 → **1.0875938959011602/0.23305583/4.0856e-14/6.4704589/1.3377405** · `:4267` 0.5102/0.0707 → **0.5660261482031647/0.7106801334283791** · `:4274` 1.2551/0.4702 → **1.087593895901027/0.49648925709067077** · `:4275` 0.5430/1.0860 → **0.5437969/1.0875939** · `DECISIONS.md:250`(D-L51) 1.117 → **1.1186680** | **항목 1이 `#42 ⑥`을 박은 그 항목 안에서 ⑥을 어겼다**(리뷰어가 "세션 내 재발"로 적었다). 값은 리뷰어가 원장에서 읽어 줬으나 **그대로 옮기지 말고 그 자리에서 다시 읽는다** |
| ~~**[2]**~~ ✅ **해소**(원장 산문 동적화)  높음 | **`touch_route.json`의 자기 산문이 자기 값과 모순한다** — `app_gestures.reading`이 "방위각 1.2551 대 1.0860으로 13.5% 다르다"인데 실측 두 값이 12자리까지 같다. `two_finger.guarantee`는 "1/1.96 = 0.5102 — 항등이라 임계를 안 건다"인데 실측 **0.5660**이다 | **보장이 아니면 임계 없음의 근거가 사라진다**(#5 자기참조 3). 산문이 결론을 들고 있다 |
| ~~**[3]**~~ **해소** 높음 | **`basic_flow`의 `lifted` 4가 원장에 없다**(#25) — `basic_flow.json`의 `s3_depth.lifted`는 3이고, 브라우저 실측(`seg3d [-1.122,-0.295,3.499]…`)은 `stage0/out/` 어디에도 없다 | **해소(8차 항목 0)**: `p1_invariance.json`에 `unanchored_place`를 신설했다 — `placed`·`unanchored_placed`·`unanchored_on_ground`·`ground_y`·`unanchored_segs`. ⚠ 원장을 재니 **놓인 넷이 전부 미앵커**였다(3개가 같은 지면 y) — "넷째 획만"이라던 눈대중이 부정확했다 |
| ~~**[4]**~~ **해소** 높음 | **"항목 4-5를 하면 3으로 돌아간다"가 항목 4의 목표와 어긋날 수 있다** — 연결이 좌표를 정하면 그 획은 **놓인다**(4로 남는 경로가 있다). 개수는 "임의 깊이로 놓임"과 "연결이 정한 깊이로 놓임"을 **못 가른다** | **해소(8차 항목 0)**: 판정을 `lifted` 개수에서 **`unanchored_placed`**로 옮겼다. **항목 4-5가 지우면 0이 되어야 하고** 그것이 양성 채널이다 — 개수는 "임의 깊이"와 "연결이 정한 깊이"를 못 가르지만 이 필드는 가른다. `basic_flow`의 `lifted` 기대는 **더 이상 판정자가 아니다** |
| ~~**[5]**~~ ✅ **해소**(8차 2차 지시 항목 0) 높음 | **`scan_cited_values`의 모집단 서술이 문서마다 다르고, 1129 → 96이 판정 완화인지 모집단 축소인지 안 갈렸다** — 272/1129(24%)와 2/96(2%)은 **분모가 다른 두 비율**이다(#11·#40 ③) | 단위(수치인가 줄인가)·해시 없는 원장 포함 여부·**판정만 바꿨을 때의 플래그 수**를 갈라 적어야 한다 |
| ~~**[6]**~~ ✅ **해소**(실제 텍스트 반례 실증)  높음 | **되살리려던 6차 문장을 실제 텍스트로 넣어 보지 않았다** — 반례 아홉이 전부 합성 문자열이다. 이 저장소 문체는 한 줄에 원장 값을 여럿 싣기 때문에 완화된 판정에 **걸러질 수 있다** | CLAUDE.md A-4 "회귀 테스트는 **버그를 되살려** 실제로 잡는지 확인한다". `git show`로 그 문장을 꺼내 넣어 본다 |
| ~~**[8]**~~ ✅ **해소**(20과 13은 같은 양의 두 시점 — 20으로 통일)  중간 | 같은 양을 **20곳**과 **13곳**으로 적었다(값 재확인 대상 대 해시가 낡은 것) — 구분이 어느 문서에도 없다 | — |
| ~~**[9]**~~ ✅ **해소**(①형 판정 과대를 정정)  중간 | **#42 "세 형태" 표의 ①형 판정이 과대하다** — `scan_citation_hashes`는 값을 대조하지 않으므로 "상수가 같이 바뀌면 잡는다"가 아니라 **해시 불일치만** 잡고, 해시를 고치는 순간 침묵한다. 새 검사가 ①형을 **조건부로** 덮는다는 사실이 표에 없다 | — |
| ~~**[10]**~~ ✅ **해소**(⑥을 최근 다섯에 반영)  중간 | **⑥과 "세 형태"가 착수 시 읽히는 자리(최근 다섯)에 없다** — 본문에만 있다. #42 ⑤가 규정한 그 누락과 같은 구조다 | — |
| ~~**[11]**~~ ✅ **해소**(8차 2차 지시 항목 0 — `scan_pitfall_citations` 신설로 자동화) 중간 | **항목 0의 완료 대조가 재실행 원장 전문이 아니었다**(#42 ④) — 원장 45개를 다시 돌렸고 그 원장들의 `what_this_does_not_say`·`gate`가 인용하는 번호에 목록에 없는 것이 있다(`basic_flow` #41·#35·#25 · `snap_cache` #12·#21·#38·#25 · `one_point_direct` #39·#29 · `view_cube` #12·#21·#24·#38) | **항목 1(카메라)도 원장을 다시 돌렸으므로 같은 처리가 필요하다** |
| **[12]** 중간 | `HANDOFF.md`의 유일한 검증 블록이 7차 값(`cc46f39` 기준)이다 | 8차 절에 현행 한 줄을 넣었다(부분 대응) |
| ~~**[13]**~~ ✅ **해소**(8차 2차 지시 항목 0 — D-L77에 충돌 절 신설) 중간 | **병합이 받아들인 "앵커 없는 획을 임의 깊이로 놓는다"가 A-3과 정면으로 어긋나는데 그 충돌이 D-L77에 안 적혔다** | 전제 반증이 기록돼야 한다 |
| ~~**[14]**~~ ✅ **해소**(8차 2차 지시 항목 0 — 일곱 자리 제자리 정정. ⚠ 그 과정에서 `rule_camera`가 항목 1로 크게 움직인 것이 드러났다) 낮음 | 원격 갈래 기록의 낡은 값에 **제자리 표시가 없다**(#42 ⑤) — `progress.md:6339`의 0.253 · `:6216`의 "현행 값 35.00 / 519·277·237" | 6216은 문면이 "**현행 값**"이라 절 머리 배너가 그 주장을 못 무른다 |
| ~~**[15]**~~ ✅ **해소**(근거 번호 #19 → #38·#40 정정)  낮음 | ②형을 안 넓힌 근거로 든 **#19가 번호가 안 맞는다** — 덮는 수 부풀리기는 #38·#40 쪽이다 | — |

## 2026-08-18 8차 항목 1 — 미이행·대가

| 미룬 것 | 왜 | 되살릴 조건 |
|---|---|---|
| **1-c(남은 물음 분류)를 안 했다** | `asks_total` 1080이 전부 `screen_or_depth` 한 종류인 것까지만 확인했다. 지시 1-c의 "모호 판정이 아닌 것을 없애고, 모호 판정은 **커서로 가르므로 물음이 아니라 조작**"의 분해를 안 했다 | `resolveLive`의 `chooseAxis`가 이미 그 규칙을 갖고 있다 — **다음 세션의 첫 일감** |
| **잘 놓인 배치의 절대 개수가 176 → 46으로 줄었다** ⚠ **8차 항목 1이 갈랐다**: 잘 놓인 46은 **전부 P2**에서 나오고 **P1은 0**이다(중앙 1.69 — 임의 f의 배율 오차). 배치 감소의 지배항은 **p0 7 → 140실행**(1680획을 긋고 0을 놓는다)이다. ⚠ 옛 원장에 `by_order`가 없어 **커밋 사이 분해는 못 했다** | 중앙 형태 오차는 1.3329 → 0.5731로 **2.33배 좋아졌는데** 절단 0.2 안에 든 배치는 **줄었다**(#15·#16 — 개수만 보면 반대로 간다). 파국은 줄고 정확한 것도 줄었다. `camera_ok`도 583 → 460(`p0` 7 → 140) | **등록 게이트가 이 대가를 안 잡는다**(`floor_gate_discriminates: false`). 대가를 잡는 지표를 게이트에 넣거나, 셋째 선 요구가 `p0`를 늘리는 몫을 따로 센다 |

## 2026-08-18 8차 항목 0 — 원격 갈래 병합이 남긴 것

| 미룬 것 | 왜 | 되살릴 조건 |
|---|---|---|
| **`basic_flow`의 `lifted` 기대가 3 → 4로 늘었다** — 되돌리지 않고 **적어 두고 넘어갔다** | 병합된 원격 갈래의 **미앵커 배치**(`placeUnanchored`·`planeAnchor`, D-L77)가 앵커 없는 넷째 획을 **지면에 임의 깊이**로 놓는다(실측: `snapStart: null` · `seg3d` y가 앞 셋과 같은 지면 −0.295 · z 3.499~6.995). 이 갈래의 옛 동작(3)이 **8차 지시 항목 6이 요구하는 것**이고 원격의 동작(4)이 **지금 배포돼 있는 것**이다. 병합에서 배포 동작을 임의로 되돌리면 항목 6 이전에 사용자가 보는 것이 또 한 번 바뀐다 | **항목 6-5**(`on_face` 임의 스냅 제거)가 지운다. 그때 이 수는 **3으로 돌아가야 하고**, 돌아가지 않으면 항목 6이 미이행이다 — 그 줄이 **양성 채널**이다(#30) |
| **`elevation_flow.json`의 `gate.reachability_value`가 `[1]`이다**(#40 · `scan_gate_reachability` 플래그 1건) ⛔ 제자리 정정(14차 항목 6 착수 대조 · 6-R1 [11]): 현행 값은 **[2, 1]**이다(여전히 정확 0/1 포함 — 플래그 유지). 이 행의 "항목 6"은 **8차 항목 6**(on_face 제거 — 위 173행의 그것)이다 — 14차 항목 6은 이 하네스를 안 열었고(잉크 차단이지 입면 배치 규약이 아니다) 배정을 **안 닿음**으로 판정했다 | 원격 갈래가 신설한 게이트이고 도달 가능성 값이 **정확히 1**이라 #40이 잡는다 — 그 자리의 1은 대개 측정이 아니라 **구성의 귀결**이다. 병합 시점에 그 하네스의 팔을 다시 설계하는 것은 항목 0의 범위 밖이라 **플래그를 남긴 채 등재한다**(끄지 않았다 — #19) | `elevation_flow`를 다시 여는 항목에서 **1이 아닌 팔**(음성 대조의 실제 값)로 바꾼다. **항목 6**이 입면 흐름의 배치 규약을 바꾸므로 그때가 자연스러운 자리다 |
| **원격 갈래가 인용한 원장 해시 20곳이 낡았다**(`@509615a1` → 현재 `54346ad1`) ⚠ **20과 13의 구분**(8차 리뷰어 [8]): selfcheck가 처음 센 것이 13이었고 e2e 원장 셋(`elevation_flow`·`restore_pose`·`snap_cache`)을 재생성한 뒤 **20**이 됐다 — **같은 양의 두 시점**이지 다른 양이 아니다. 문서는 **20으로 통일한다** | 원격 갈래는 `web/test/constants.ts`를 안 건드렸으므로 그 갈래의 해시는 **분기점(`63e4831`)의 값**이다. 병합으로 이 갈래의 상수가 이겼고 원장을 다시 돌렸으므로 그 인용들은 **값부터 다시 읽어야 한다**(해시만 고치면 #42 ③ 그 자체다) | 항목 0에서 **원장을 다시 읽어 값과 해시를 함께 고쳤다**(아래 항목 0 절). 남은 것은 `n_stale` 은퇴 하네스 셋뿐이다 |

---

## L 전환이 명시적으로 미룬 것

| 항목 | 원래 목표 | 지금 | 사유 |
|---|---|---|---|
| **프리핸드 보존** | CLAUDE.md 핵심 원칙 "획을 버리지 않는다" | **획이 직선 세그먼트가 되고 손떨림은 버린다** | 계획서 §1.1. 미루면 떨림 평면 선택·굽은 획 미배치·획 파라미터화·미리보기와 확정의 차이가 **전부 없어진다**. 남는 것은 시작점과 끝점 둘뿐이고 그게 SketchUp이 다루는 것과 같다. **`pts2d`를 계속 보존하므로 구조가 막지 않는다** — 세그먼트가 확정된 뒤 원본 획을 그 위에 얹는 **표현 계층**으로 되돌아온다 |
| 튜브 메쉬의 곡선 보존 | 획 그대로를 3D 튜브로 | 직선 렌더 | 위와 같은 이유. `tube.ts`는 남기고 입력만 두 점이 된다 |
| 면 생성(§6) | 획 루프 → 면 | 범위 밖 | L-A~L-D에 없다. AS-4("면 없이 형태가 읽힌다")의 근거가 좁다는 것은 기록돼 있다 |
| 곡선 획 처리 | AS-C8 (실획 26.5%) | 현(弦)으로 접힌다 | 프리핸드와 함께 돌아온다 |

## 실획 표본에 걸린 것 (표본이 0이다)

| 항목 | 무엇이 필요한가 | 왜 지금 못 하나 |
|---|---|---|
| **스냅 반경** | 손으로 그은 시작점이 겨냥에서 몇 px 어긋나는가 | Quick,Draw `closure_gap` 환산 0.028~0.051이 유일한 자릿수인데 그것은 *자기 시작점*으로 돌아오는 오차이지 *다른 획의 끝점*을 겨냥하는 오차가 아니다. **상한이라는 근거가 없다**(하한일 수도). L-B는 합성으로 스윕만 하고 값은 잠정으로 둔다 |
| AS-C1(표본 대표성) | 스타일러스·숙련자 획 | 수집 경로가 없다. `sessionExport.ts`가 남아 있으므로 붙일 자리는 있다 |
| AS-C5(2점 주점 가정) | 실제 손 스케치에서 주점이 이미지 중심인가 | 미측정. f 전체가 이 가정 위에 있다 |

## S·G에서 넘어온 미해소 (대상이 남아 있는 것만)

| 항목 | 무엇이 문제인가 | 어디서 |
|---|---|---|
| **구도가 하나뿐이다** | 모든 배치 측정이 요 35°·피치 15° 3점, 직육면체 하나 위에 있다. "구도가 결과를 바꾼다"고 말하면서 구도는 하나만 쟀다 | L-A 측정 하네스가 **구도를 여러 개** 만든다(1점·2점·3점이 승격으로 자연히 생긴다) |
| `scan_unread_fields`가 문자열 리터럴 안의 필드명을 읽기로 센다 | selfcheck의 알려진 한계. `tests/test_selfcheck_scan.py`가 못 박아 뒀다 | 그대로 |
| 저장 슬롯이 하나다 | S-9 잔여 | **판단했다(L-D.2, 2026-08-16): 하나로 둔다.** 자동 저장 하나 + `JSON` 내보내기/불러오기가 여러 벌을 만드는 경로다(파일이 슬롯이다). 슬롯 UI를 따로 두면 저장소 관리 화면이 필요하고 그것은 §9의 범위 밖이다(A-3: 범위를 넓히지 않는다). ⚠ **README가 "슬롯 하나"를 그대로 적고 있고 그것이 맞다** |
| iPad 실측 지연이 없다 | 지연 수치가 전부 데스크톱·개발 서버 | L-D 종단 검증 |

## L-A.7이 남긴 것

| 항목 | 원래 목표 | 지금 | 사유 |
|---|---|---|---|
| **브라우저 확인의 형태 오차** | 앱에서 참값 대조 | 배치 개수만 본다 | 그 분모는 L-A.4가 이미 "공짜 통과"라 적었다(리뷰어 [13]). 참값 대조를 앱에서 하려면 측정 하네스를 앱에 넣어야 한다 — 측정 경로와 앱 경로를 섞는 일이라(PITFALLS #17) L-D 종단 검증에서 Playwright로 한다 |
| **AS-L9 사람 도달 가능성** | 사람이 축 오차 0.5°(핸들 1~4px)에 드는가 | 미측정 | 대리값만 있다. L-D 종단 시나리오로 재거나, 자동화가 어려우면 **실사용 대기**다 |
| **작은 캔버스의 핸들 예산** | **부분 해소(L-B.1)** | 단일 뷰포트에서 가이드 **607~1328px** — 그러나 **여섯 중 둘이 요구치 미달**이다 | 옛 좌우 분할(490×672)에서는 304~358px이 한계였다. 단일 뷰포트(1280×675)에서 `min_guide_ratio` 0.45가 요구하는 **651px**에 대해 실제 길이는 607.6 / 618.5 / 1233.3 / 1254.6 / 1256.8 / 1327.7px다 — **짧은 둘은 여전히 잘린다**(잘린 비율 0.81 → 0.93으로 줄었을 뿐이다). **개선 배수는 가장 짧은 가이드로 읽는다**: 600px 행 1.28~4.33px 대 옛 300px 행 0.63~2.16px = **약 2배**(가장 긴 것으로 읽으면 4배가 나오는데, **카메라 정확도는 가장 나쁜 축이 정한다**). ⚠ 그 예산도 **핸들 하나만 움직인다는 가정** 위에 있다(`vp_homog`의 섭동은 단일 핸들이다). 사람은 핸들 12개를 만진다. `stage_browser.json` · `vp_homog.json@f351839a` |
| **지렛대가 긴 획으로 획 수 늘리기** | AS-L10을 완전히 닫기 | 내부 격자선만 시험했다 | 늘린 획이 `1/k` 길이라 지렛대가 오히려 짧아졌다(리뷰어 [10]). 상자 여러 개·긴 모서리 경로는 **막히지 않았다**. 결론(자동 확정 없음)은 안 바뀌지만 반증의 범위가 좁다 |

## L-B.1에서 미룬 것

| 항목 | 무엇 | 왜 미루나 |
|---|---|---|
| **분포형 오차의 정확한 0을 selfcheck가 못 본다** | `lift_gate.json:seg_gap_px`는 min~max가 전부 0인데 새 "정확히 0" 규칙이 안 잡는다 — **꼬리(min·p10·p90·max) 제외 규약** 때문이다. `p90=0`이고 `max>0`인 분포는 두 규칙 어디에도 안 걸린다 | 꼬리 제외 규약을 건드리면 플래그가 폭증한 전례가 있다(**89 → 345**). "분포 블록 안에서 median이 아닌 자리가 0" 같은 좁은 규칙이 필요하고, 그 설계에 반례가 더 든다 |
| **핸들 예산의 다중 핸들 판** | `vp_homog`의 섭동은 **핸들 하나**를 옮긴다. 사람은 **가이드 6개 = 핸들 12개**를 만진다. 넷이 독립으로 어긋나도 축 오차 0.5°가 유지되는가 | 지금 수치(짧은 가이드 1.28~4.33px)는 **개별 핸들 예산**이고, 그 사실을 §5.2·§12·AS-L9·이 문서에 적었다. 다중 핸들 판은 AS-L9의 실사용 측정과 함께 보는 것이 맞다 |
| **`min_guide_ratio` 0.45가 여전히 잘린다** | 새 뷰포트(1280×675)에서도 요구 651px에 **가이드 여섯 중 둘(607.6·618.5)이 미달**이다 | **⚠ 원인 귀속이 틀렸다**(L-B.2가 정정). 늘리기를 넣은 뒤 길이는 **언제나 그 방향의 캔버스 안 현**이고 그림이 정하지 않는다. 캔버스 1280×675에서 가장 짧은 현(수직)은 **588.18px**(높이 675 − 여백 2×43.4)이라 **요구 651px에 원리적으로 못 닿는다** — 수직에 가까운 가이드는 **어떤 그림에서도** 미달이다. 미달 **164개 중 98개**가 정확히 그 588px 대역이다(`extend_ceiling`, 분모는 **카메라가 서는 58장면 × 6 = 348**이다 — `guides_below_required`의 540과 **다른 모집단**이다). 고치려면 `min_guide_ratio`를 캔버스 짧은 변 기준으로 다시 정하거나 세로를 키워야 한다 |
| ~~**참 카메라에서도 `coarse` 배치가 0.44**~~ **해소(원인 특정) 2026-08-16** | `lift_grade.json@f351839a`이 분해했다: **지배항은 축 판정**이다(미배치 사유 `축이 미분류다` coarse 533). 오라클 축이면 0.44 → 0.82이고 **등급 격차가 0.19 → 0.05로 거의 사라진다**. 그다음이 접합 반경(오라클 축 + `touch_ratio` 0.09면 1440/1440). **조건수는 "못 푸는" 문제가 아니라 "틀리게 푸는" 문제**다(`solveNull` 실패 0건, 형태 오차 중앙 0.10~0.18) | ⚠ **"L-B.3 스냅 뒤에 다시 잰다"는 전제가 틀렸다.** 스냅 대상은 3D 레이어인데 **초기 확정 순간에는 3D가 없다** — 스냅이 이 값에 닿지 않는다. 남은 일은 **L-B.4(확정 시점 축 판정)**과 **L-B.7(승격 연쇄)**이고, 고칠 때마다 조용히 틀림이 함께 느는 것을 봐야 한다(#16) |

## L-B.2에서 미룬 것

| 항목 | 무엇 | 왜 미루나 |
|---|---|---|
| **`min_guide_ratio` 0.45가 세로 가이드에 도달 불가** | 캔버스 1280×675에서 수직 현의 상한이 588.18px인데 요구는 651.2px다 | 임계를 **캔버스 짧은 변 기준**으로 다시 정하는 것이 맞아 보이나, 그 값을 정하는 근거가 `vp_homog`의 길이 스윕뿐이고 **길이만으로는 예산이 안 정해진다**(각차가 함께 정한다 — L-B.2 브라우저에서 1233px 축이 1226px 축보다 예산이 좁았다). 각차·길이 2차원 스윕이 필요하다 |
| ~~**초안이 카메라를 못 세우는 30/90이 `recover_failed`다**~~ **해소 2026-08-16** | `draft_recover.json@509615a1`이 특정했다: 지배항은 **지지선 선택**(가장 긴 둘이 하필 나란해 각차 최소 **0.03°**)이고, 무한원 스냅은 9/90만 설명한다. `len_x_sep` + 빈 축 중립 채우기로 **31/90 → 58/90**(D-L16). ⚠ **확정률만 보면 안 된다**(#16) — 어느 팔에서도 **축 오차 중앙 9~14°**로 임계 0.5°의 18~28배다. 초안 카메라는 어차피 못 쓰고 가치는 **끌기 시작 위치**에 있다. 중립 시작(13.83°)보다는 낫다는 것이 확인됐다 | — |
| **민감도 표시가 사용자의 조정을 실제로 돕는가** | 값을 화면에 냈으나 효과는 미측정 | 지표는 정의의 귀결이고(#3) "보여 주면 나아진다"는 별개의 주장이다. AS-L9의 실사용 측정과 함께 본다 |

## L-B.3에서 미룬 것

| 항목 | 무엇 | 왜 미루나 |
|---|---|---|
| **차수 승격에서 스냅이 조용히 풀린다** | 스냅은 `pts2d[0]`을 **대상의 상**으로 옮기는데, 그 상은 **그 카메라에서만** 그 대상이다. §6.1은 승격 시 `pts2d`를 보존해 전부 처음부터 다시 올리므로, 새 카메라에서는 옮겨 놓은 점이 대상의 상이 **아니다**. `snapStart.ofId`를 새 카메라로 **다시 풀지 않으면** 스냅이 소리 없이 풀린다 | **L-C(차수 승격)의 일이다.** `applySnapToStart`에 경고를 달아 두었다. 고치는 방향은 정해져 있다 — `snapStart`가 이미 `kind`·`ofId`를 들고 있으므로 승격 시 그 대상의 **새 상**으로 다시 옮기면 된다. 다만 대상 자체가 승격에서 다시 풀리므로 **순서**(어느 획을 먼저 올리는가)가 문제가 되고 그것이 L-C의 범위다 |
| **스냅으로 못 고치는 몫**(D-L15) | 겨냥 오차 / 대상 간격의 비가 1/2를 넘으면 **어떤 반경으로도** 이웃 꼭짓점을 문다. k3에서 15076/38880이다 | L-B.4·L-B.5·L-B.7을 지났는데 **아무것도 이 비를 안 바꾼다** — 축 판정도 고정도 승격도 **시작점이 어디에 붙는지**를 안 바꾼다. 남은 후보는 **이어 그리기 문맥**(직전 획의 끝점을 다음 획의 시작으로 우선)이고 그것은 계획서에 없다. **실획 표본이 오면 그때 본다** |
| **놓인 것의 19%가 조용히 틀린다** | 반경 0.05에서 배치 **2503/3240**(`end_jitter > 0`), cut 0.2 조용히 틀림 **474/2503** | **L-B.4가 답했다: 임계로는 못 고친다**(45조합에서 단조 맞바꿈). ⚠ **그리고 절단 0.5에서는 실시간이 확정 시점보다 나쁘다**(14.8% 대 7.9%) — 앵커는 작은 틀림을 줄이고 **큰 틀림을 늘린다**. 남은 몫은 **L-B.7 승격 연쇄**다 |
| **`LIVE_TOL.axis_deg = 12`의 근거가 약하다** | L-B.4가 4~35°를 쟀다. ⚠ **등록 규칙이 기준값만 고를 수 있는 구조**였으므로 이 항목은 **잠정값을 재확인했을 뿐**이다. 한계증분으로 보면 급변은 **18과 25 사이**이고(초판의 '무릎 12~18'은 희석된 평균이었다), **cut 0.5로 읽으면 4°가 뚜렷이 낫다**(9.0% 대 14.8%) | 절단을 무엇으로 읽을지가 **승격 연쇄의 회수율에 달렸다** — 큰 틀림을 줄이려 조이면 미배치가 늘고, 그것이 회수되는지가 L-B.7이다. **그 뒤에 다시 본다** |
| **`on_face`의 대상이 지면 하나다** | 면 생성이 범위 밖이라 다른 면이 없다. 그리고 지면 스냅의 화면 거리는 **정의상 0**이라 성공률 측정에 못 섞는다 | 면 생성(§6)이 L-A~L-D 범위 밖이다. 면이 생기면 `SnapCtx`에 넣기만 하면 된다 — 구조는 막지 않는다 |
| **`min_guide_ratio` 0.45 재정의**(L-B.2에서 이월) | 캔버스 1280×675에서 수직 현의 상한이 588.18px인데 요구는 651.2px다 | 여전히 미해소다. L-B.3(b)의 채우기가 **가이드 수**를 늘렸을 뿐 길이 상한은 캔버스가 정한다(요구 미달 347/540 → 249/540). 각차·길이 2차원 스윕이 필요하다는 판단은 그대로다 |

## L-B.7에서 미룬 것

| 항목 | 무엇 | 왜 미루나 |
|---|---|---|
| **L-B.6(뷰 시스템)이 남았다** | 계획서 §11의 순서는 B.6 → B.7인데 **B.7을 먼저 했다**(사유는 `progress.md`의 L-B.7 절: 열린 결정 셋이 전부 회수율에 달려 있었다) | `doc.ts`에 자료구조(`SView`·`deleteView`·`pending(viewId)`)는 이미 있고 **UI 배선만 남았다**. 다음 세션의 첫 항목이다 |
| **승격이 품질을 개선하지 않는다** | 회수된 720의 조용히 틀림이 cut 0.2에서 **392/720 = 0.544** 대 확정 **874/2416 = 0.362**다. ⚠ **격차의 대부분은 게이지 추정기다** — 자기 게이지로 재면 286/720 = 0.397이라 **같은 추정기끼리는 1.10배**다(리뷰어 [1]). 남는 ≈15pp가 **확정된 틀과 배율이 안 맞는 몫**이다. ⚠ **1점 구도는 90/93 = 0.968이다**(리뷰어 [2]) | 승격은 **개수 회수**의 기전이지 정확도의 기전이 아니다. 정확도는 카메라(축 오차)가 정하고 그것은 AS-L9(사람이 0.5°에 드는가)에 달려 있다 — **표본 0이다** |
| **오라클 축이 아니면 일괄 기전이 안 돈다** | 축을 주면 **1154/1904(60.6%)**를 회수하는데(D-L24), 연쇄가 실제로 붙인 축 라벨로는 **24/966**뿐이다. 그 사이가 곧 축 판정의 몫이다. ⚠ **그리고 일괄은 더 많이·더 틀리게 놓는다**(675/1154 = 0.585) | **축 판정을 고치는 것은 L-B 범위 밖이다** — L-B.4가 임계로는 못 고친다고 쟀고(45조합 단조 맞바꿈) 남은 후보는 카메라 정확도(AS-L9, 표본 0)다 |
| **`no_snap` 813이 승격의 상한이다** | 대기 획의 시작점이 확정 기하 근처에 **아예 없다**. 그 획들이 대기인 이유가 바로 안 이어졌기 때문이라 **구조적**이다 | 반경을 키우면 `unintended_snap`이 는다(D-L14). 다른 정보(이어 그리기 문맥)가 필요하고 계획서에 없다 |
| **품질 우선 목적함수에서는 조이기가 이긴다** | D-L18은 등록 규칙(**두 조건의 연언** — 개수 우선이 아니다, 리뷰어 [10])대로 **기각**했으나, 조이기가 조용히 틀림을 cut 0.2 **0.4037 → 0.329**로 낮춘다(고정 게이지) | **규칙을 사후에 바꾸지 않는다**(#28). 목적함수를 바꾸려면 **바꾸는 사유가 측정 밖에서** 와야 하고(예: 사용자가 조용히 틀린 배치를 얼마나 싫어하는가) 그것은 표본이 없다 |

| **연쇄 수렴이 픽스처의 성질일 수 있다**(리뷰어 [7]) | 장면당 획이 **12개**(상자 하나)라 연쇄 깊이 4는 그래프가 정하는 상한에 가깝다. §9.1의 "떨어진 덩어리도 연결되는 순간 전부 올라간다"는 **두 번째 덩어리가 없어 이 픽스처로 확인 불가**다 | 상자 둘 이상 픽스처가 필요하고 그것은 `scene3d`에 없다. 만들면 검출·확정 경로의 조건도 함께 바뀌므로 **L-B 게이트 재측정과 함께** 해야 한다 |
| **게이지 선택이 기계가 읽는 필드로 안 남는다**(리뷰어 [8]) | 정의 해시는 `metrics.ts` 소스만 덮는다. 어느 하네스가 `fit` 대신 `1`을 넘겨도 해시는 그대로다 — **이번 사고의 원인이 바로 그 인자였다** | 산출물에 `gauge: "fit"|"fixed"|"one"` 필드를 넣고 selfcheck가 대조하는 것이 맞다. 다만 하네스마다 게이지가 집합별로 다를 수 있어(확정 fit + 회수 fixed) **필드 하나로는 안 된다** — 설계에 반례가 더 든다 |
| **인용 규약이 정의 판을 못 가른다**(리뷰어 [9]) | `promote.json@f351839a`이 옛 `segErr` 판과 새 판을 **동시에 가리킨다**(상수 해시가 같았기 때문이다) | 규약을 `파일.json@상수+정의`로 넓히는 것이 맞아 보이나, 기존 인용 수백 곳을 한꺼번에 고쳐야 한다. **다음 원장 갱신에서 새 인용부터 적용한다** |
| **층별 수치에 시드 변동폭이 없다**(리뷰어 [16]) | 등급 셋의 차이(0.34~0.43)를 "안정"이라 적었는데 시드 폭을 안 쟀다 | 시드별 분해를 내려면 셀이 6배로 쪼개져 셀당 표본이 40 아래로 간다. **폭을 재려면 시드를 늘려야 하고** 그것은 하네스 실행 시간 문제다 |

## L-B.7 리뷰어가 남긴 것 — **12행 중 11행이 닫혔다**(2026-08-16, L-B.M)

닫힌 것은 `progress.md`의 **L-B.7 리뷰어 잔여 대응** 표에 결과와 함께 있다: **[1] [2][3] [4](원장 측) [5] [6] [10] [11] [12] [13] [15] [16]** — 표의 12행 중 11행이다.
아래에는 **남은 것만** 둔다. **L-B.M 자신에 대한 리뷰어 17건은 위 표들에 흡수했다.**

| # | 항목 | 무엇 |
|---|---|---|
| [14] | **`loosen` 팔이 사전 등록에 없다** | 등록은 기준·조이기 둘인데 원장은 셋이다. 그리고 늘리기의 앵커 결과(3343/4320 · cut 0.2 **0.4493**)가 본문에 없다 |

## L-B.M 리뷰어 2회차가 남긴 것 (17건 중 대응 못 한 것)

| # | 항목 | 무엇 |
|---|---|---|
| [11]의 [14] | **`loosen` 팔이 사전 등록에 없다**(1회차 [14]에서 이월) | 등록은 기준·조이기 둘인데 원장은 셋이다. 판정에 안 쓰므로 결론은 안 바뀌지만 **등록과 실행이 어긋난 자리**다. 지우면 늘리기 방향의 정보가 없어지고, 등록을 사후에 고치면 #26을 어긴다 — **다음 항목의 사전 등록에 셋을 다 적는다** |
| [16] | **시드 폭이 층별 수치에는 여전히 없다** | 표제 수치 둘(자기 게이지 배수 · 오라클 배수)은 이번에 쟀다. **층별**(등급·잡음·구도)은 셀당 표본이 85~800이라 시드 여섯으로 쪼개면 셀이 14~133이 된다 — **폭을 재려면 시드를 늘려야 하고** 그것은 하네스 실행 시간 문제다 |
| — | **배율을 확정 틀에 맞추는 기전이 없다** | 승격이 더 내는 오차가 **전부 배율**이라는 것이 이번에 확인됐다(형태는 시드 폭 안). 앵커 경로는 시작점만 확정 기하에 붙이고 **끝점의 깊이는 축 방향과 커서가 정한다** — 그 둘이 확정 틀의 배율을 안 본다 | 고치려면 회수 획의 배율을 확정 집합에 맞추는 단계가 필요한데, 그것은 **앵커 하나로 정해지지 않는다**(끝점이 자유롭다). 두 점이 붙는 경우에만 가능하고 그것은 다른 기전이다 |
| — | **1점 구도에서 승격이 무너진다** | 회수한 93획 중 **90획이 조용히 틀리고 연쇄가 0**이다. 확정도 268/422로 나쁘다 | 구도별 분기를 두려면 **어느 구도인지 앱이 알아야** 하는데 그것은 `Camera.order`로 있다. 다만 **1점 구도에서 아예 승격을 끄는 것**이 옳은지는 사용자 표본이 없다(미배치가 늘고 그것이 §9.1의 대기다) |
| — | **연쇄의 여러 회 반복을 브라우저에서 안 봤다** | 원장은 하네스에서 4회까지 봤고 `l_b8`은 앱에서 **1회 연쇄**만 본다 | 앱의 `promoteChain`은 8회 상한으로 돌지만 **여러 회가 실제로 도는 픽스처**를 브라우저에 넣으려면 상자 둘 이상이 필요하다(같은 이유로 [7]이 미뤄졌다) |

## 절단 곡선이 남긴 것 (리뷰어 3회차)

| 항목 | 무엇 | 왜 미루나 |
|---|---|---|
| ~~**앵커/확정의 우열이 비용비 λ에 달렸다**~~ **닫힘 2026-08-16 — 사람이 λ를 정했다(D-L25)** | **λ = 3 · 절단 0.5**를 사전 등록 목적함수로 박았다. 근거는 비용의 비대칭이다 — **미배치는 유예되고**(§9.1 대기) **틀린 배치는 영구이며 그 위에 구조가 쌓인다**. ⚠⚠ **그 칸에 측정 지지가 없다**(리뷰어 2회차): 여유 **0.0025**(8획)에 대해 **시드 부호가 3 대 3으로 갈리고 폭이 0.2166**이다(`axis_live.json@f351839a`의 `seed_spread_at_dl25_point`). "λ≥5에서는 뚜렷하다"·"λ=3은 하한이다"도 같은 열이라 함께 무너졌고 **철회했다**. **D-L25는 정책만으로 선다.** ⚠ 그 정책의 전제(미배치는 유예)도 **AS-L11에서 절반만 참**이다 — **1점 구도는 연쇄가 0이라 영구 미배치**이고 **구도별 λ는 미측정이다**(아래 새 행) | — |
| **등급별 임계 분기를 팔 *안에서* 안 쟀다** | 낸 것은 팔 **사이**의 교차점이고, 앵커 팔은 오라클 대상이라 **등급 효과가 구성상 안 나온다**(#31). 진짜 물음은 "`LIVE_TOL.axis_deg`의 최적점이 등급마다 다른가"다 | `live_time.rows`가 등급 합산이라 쪼개야 하고, 쪼개면 셀이 작아져 시드 폭이 결론을 삼킬 수 있다(#14). **실획 등급을 모르는 채로 분기를 두는 것 자체가 근거가 없다**(AS-C1) |
| **⚠ λ = 3이 D-L21(축 판정 임계)을 되열었다 — 아직 안 뒤집었다** | D-L25의 목적함수로 `promote.json@f351839a`의 세 팔을 다시 채점하면 **조이기가 이긴다**. 절단 0.5·λ=3에서 (놓였고 맞은 것 − 3×조용히 틀린 것)/4320이 기준 **(2742 − 3×394)/4320 = 1560/4320 = 0.361** · 조이기 **(2642 − 3×334)/4320 = 1640/4320 = 0.380** · 늘리기 **963/4320 = 0.223**이다. 절단 0.2에서도 조이기가 이긴다(−0.218 대 −0.446). 교차는 **λ=1과 λ=2 사이**다(λ=1: 기준 0.5435 대 조이기 0.5343으로 기준이 이긴다) | **시드 폭을 안 쟀다**(#14). `promote.json`에 시드별 분해가 없고 차이 80/4320은 L-B.4가 실측한 배치 시드 폭 **31**과 같은 자릿수다. 뒤집으려면 **하네스에 시드별 출력을 넣고 다시 돌려야** 하고, 뒤집으면 `AXIS_TOL` 상수가 바뀌어 그것에 의존한 산출물이 전부 STALE이 된다(D-C4). **L-D 종단 검증에서 함께 본다** — 그때가 상수 변경을 흡수할 유일한 자리다 |
| **`end_jitter = 0` 못박힘이 다른 원장에도 있는지 전수 확인 안 함** | `axis_live`의 실시간 팔에서 재발했고 `snap.json`이 서명(`p10 = 0`)을 규정해 놨다. **selfcheck가 이것을 못 본다** — 꼬리(min·p10) 제외 규약의 사각지대이고 DEFERRED에 이미 등록된 항목이다 | 규약을 건드리면 플래그가 폭증한 전례가 있다(89 → 345). "분포 블록에서 `p10 = 0`이고 `median > 0`" 같은 좁은 규칙이 필요하고 그 설계에 반례가 더 든다 |
| **부분집합 264획의 사유가 안 셌다** | 확정 2416 − 1979 = 437인데 카운터는 173(회수 0인 장면)이다. 나머지 264는 `kFix`가 없는 장면(확정 0)에서 앞 단계에 빠진 것이고 **따로 안 셌다** | 두 사유를 갈라 세려면 카운터를 하나 더 놓아야 한다(#10 — 뺄셈으로 만들지 않는다). 부분집합 효과 1.3pp의 해석에 영향이 있다 |
| **인라인 지표식 전수 확인** | `axis_live`에서 인라인 식 하나가 나왔다. **정의 해시로 못 잡힌다**(하네스가 `metrics.ts`를 아예 안 부르면 해시가 그대로다) | 30개 하네스의 모든 산술식을 사람이 읽어야 한다. 기계로 하려면 "오차류 이름의 지역 변수에 `/diag`가 들어간 식"을 정적 탐지해야 하고 그것은 `selfcheck`의 새 규칙이다 |

## L-C.1이 남긴 것

| 항목 | 무엇 | 왜 미루나 |
|---|---|---|
| **승격이 배치를 줄인다(−168)** | 소실점이 셋이 되면 `classifyStroke`가 더 까다로워진다 — 후보가 늘어 `ambiguity`가 더 자주 걸린다 | 임계를 승격 뒤에만 풀면 **차수마다 다른 임계**가 되고 그것은 L-B.4가 "임계로는 못 고친다"고 쟀던 자리다. 그리고 **미배치는 대기**이므로(§9.1) 줄어든 배치가 앵커로 회수될 수 있다 — 그것을 안 쟀다 |
| **`snapStart`가 매개변수를 안 들고 있다** | 승격 때 옛 스냅 점이 대상 위 어디였는지 되돌려야 하는데, **끝점·중점 말고는 한 대상만으로 안 정해진다**(교차점·수선 발·선 위는 두 선분의 관계다). `a`쪽 끝점으로 근사한다 | `snap.ts`의 `SnapCand`에 매개변수를 넣는 자료구조 변경이고, 교차점은 **두 대상의 매개변수 쌍**이라 필드 하나로 안 된다. 근사의 대가는 `drift_px`에 섞여 있고 **갈라 안 쟀다** |
| **못 살린 스냅 295개**(`order_undo.json@f351839a`. ⚠ **초판의 483은 가짜 승격 대조군이 섞인 값이다** — #9) | 대상이 새 카메라에서 안 놓이면 그 스냅은 옛 상 그대로 남는다 — **조용히 틀린 시작점**이다 | 대상이 안 놓이는 이유가 축 판정이고(위 항목과 같은 뿌리) 그것이 L-B.4의 미해소다. **그 획을 2D로 되돌리는 것**이 A-3에 맞을 수 있으나 §9.1의 대기와 구분이 필요하다 |
| **승격 뒤 뷰 카메라 갱신을 참값으로 안 쟀다** | `promoteOrderNow`가 뷰의 눈 위치를 기하 배율로 같이 옮긴다. 브라우저 확인은 있으나 **"상대적 시점이 유지되는가"를 참값으로 안 쟀다** | 뷰 자세의 참값이 없다 — 합성 장면에 "사용자가 보던 각도"라는 참값이 없기 때문이다. L-D 종단 시나리오에서 **화면 대조**로 볼 수는 있다 |

## L-C.2가 남긴 것

| 항목 | 무엇 | 왜 미루나 |
|---|---|---|
| **돌린 뷰에서는 승격 손실이 안 보인다** | `drawPromoteLoss`가 `stage.isPinned`일 때만 그린다 — `pts2d`는 **확정 카메라의 화면 좌표**라 돌린 뷰에 그리면 화면에 붙어 따라다니는 유령이 된다(`drawBelowInk` 머리말과 같은 이유) | 돌린 뷰에서 보이려면 **내려간 획의 옛 3D**를 들고 그 시점으로 되쏘아야 하는데, 내려간 획은 정의상 3D가 없다. 승격 **직전**의 3D를 따로 들고 다녀야 하고 그것은 자료구조 변경이다. 표시가 필요한 순간은 승격 직후이고 그때는 확정 시점이므로(`promoteOrderNow`가 `pinTo`한다) **지금 손실은 없다** |
| **차수 표식이 여럿일 때를 안 봤다** | 픽스처가 2 → 3 하나뿐이다. 1 → 2 → 3으로 두 번 승격하면 표식이 둘이 되고, `revertToOrder(1)` 뒤에 3점 표식이 남아 있는 것이 옳은지 안 정했다 | 두 번 승격하는 브라우저 픽스처가 필요하고 그것은 **상자 둘 이상**이 있어야 한다(L-B 게이트 3번·[7]과 같은 이유). **L-D 종단 검증에서 그 픽스처를 만들 때 함께 본다** |
| **끊긴 스냅의 재연결이 옳은 자리에 붙는지 안 쟀다** | `relinkLostSnaps`가 `snapAt`을 그대로 부르므로 **다른 대상에 붙는다**. 브라우저에서 3/4가 붙었지만 **참값 대조가 없다** | 참값 대조를 하려면 "그 획이 원래 붙어야 했던 3D 점"이 있어야 하는데, 대상이 새 카메라에서 **안 놓였기 때문에** 끊긴 것이라 그 점이 없다. 사용자가 누르는 명시 조작이고 되돌릴 수 있으므로 A-3을 안 어긴다 |
| **`md()`가 상태 줄 전체에 걸린다** | `**x**` → `<b>x</b>` 치환을 조립된 HTML에 한 번 건다. 넣는 문장이 전부 `mainL.ts` 안이라 지금은 안전하지만, **사용자 입력이 상태 줄에 들어오는 순간 위험해진다**(뷰 이름 같은 것) | 지금 상태 줄에 들어가는 사용자 문자열은 뷰 이름 하나이고 그것은 `renderViews`가 그린다(다른 요소다). 넣게 되면 그 자리에서 이스케이프해야 한다 |

| **구도별 λ가 미측정이다** | D-L25의 정책 전제("미배치는 유예된다")가 **AS-L11에서 구도마다 다르다** — 3점 구도는 연쇄가 돌지만 **1점 구도는 연쇄가 0**이라 미배치가 영구다. 유예가 없으면 λ가 1에 가까워야 한다 | 구도별 λ를 정하려면 **구도별 회수율**과 **사용자가 각 구도에서 미배치를 얼마나 참는지**가 있어야 하는데 후자는 표본 0이다(AS-C1). 그리고 **구도별 임계 분기 자체가 이 프로젝트에서 아직 근거 없는 조치**다(같은 이유로 등급별 분기가 DEFERRED에 있다) |

## L-C.2 리뷰어 2회차가 남긴 것 (14건 중 대응 못 한 것)

| 항목 | 무엇 | 왜 미루나 |
|---|---|---|
| **`order_promote.seed_spread`에 정확히 1.00이 있다** | 시드별 (승격 후/전) 조용히 틀림 비가 [0.8, 0.833, 0.886, 0.946, 0.975, **1**]이고 `crosses_one: false`다. "1을 안 넘는다"는 참이지만 **한 시드에서 개선이 0**이라 "품질 개선은 시드 폭 밖"이 경계에 닿아 있다. 정확히 1.0은 selfcheck의 의심 대상이기도 하다(§5.1) | 다시 적으려면 그 시드의 전후 **개수**를 갈라 세야 하고(비가 아니라 분자/분모, #14) 하네스에 시드별 분자·분모를 더 넣어야 한다. 결론(승격이 나빠지지 않는다)은 안 바뀌고 **강도만 약해진다** — 표현은 "다섯 시드에서 개선, 한 시드에서 동일"이 정확하다 |
| **게이지 적합 모집단 차이의 몫을 안 갈랐다** | `dl25_rescore`의 두 팔이 **1122획과 954획**에서 각각 게이지를 적합한다. D-L22가 같은 종류의 비교에서 **부분집합 효과를 1.3pp**로 실측했고 여기 합산 여유는 **2.6pp**다 — 여유가 이미 알려진 편향의 **2배 안**이다 | 가르려면 승격 후 팔의 게이지를 **승격 전과 같은 획 집합**에서 적합해 봐야 하는데, 그 집합의 일부는 승격 후에 안 놓여 있다(`only_before` 570). 그러면 게이지를 못 낸다. **결론에는 영향이 없다** — 시드 부호가 이미 갈려 무결론이기 때문이다 |
| **`held_but_not_compared` 다섯을 대조에 안 넣었다** | `takeSnap`은 `{...s}`로 `id`·`userAxis`·`viewRef`·`color`·`width`를 담지만 `snapDiff`가 안 본다. **복원은 되고 복원 실패를 잡는 능력이 없다** | 지금 승격·되돌리기 경로가 이 다섯을 안 건드리므로 대조에 넣을 이유가 없었다. **건드리게 되면 그때 넣는다** — 넣는 비용은 `snapDiff` 몇 줄이고, 지금 넣으면 안 도는 검사가 늘 뿐이다 |
| **일반 `실행취소`의 파생 복원이 브라우저에서 미확인** | `l_c2`의 `undo_restores_camera`는 가이드가 6 → 6이라 `vps`·`f`·`fSource`의 복원을 시험하지 않는다. 확인된 것은 `revert_by_order` 경로 하나다 | 가이드를 바꾸는 조작을 `실행취소`로 되돌리는 브라우저 픽스처가 필요하다(가이드 끌기 → 실행취소). 노드 쪽 `trials`의 `guides`·`guides_add`가 같은 자리를 덮으므로 **기전은 확인돼 있다** — 없는 것은 앱 경로의 두 번째 확인이다. **L-D 종단 검증에서 함께 본다** |

## L-C.3이 남긴 것

| 항목 | 무엇 | 왜 미루나 |
|---|---|---|
| **참값과 신호 사이를 메우는 관측 가능한 양** | **고정 게이지 참값으로는 오승격 두 종류가 둘 다 크게 갈린다** — `spurious` **0.9694** · `wrong_vp3` **0.5446**(둘 다 등록 기준 0.5 위). `tiltDeg`는 0.24 이하다. ⚠ **자기 게이지로 보면 0.4979 · 0.1265로 작아 보이는데 그것은 배율 성분을 지운 수다**(AS-L11). **잴 것은 많고 관측 가능한 신호를 못 찾은 것이다** | 계획서 §6.2가 적은 후보는 **둘**이었고 둘 다 시험했다. 새 후보를 세우려면 **어디를 볼지**가 있어야 하는데, 남은 것은 전부 §5.3이 반증한 유형(입사 관계에서 유도한 양)이거나 참값이 필요한 양이다. **AS-L9(사람이 도달하는가)의 표본이 오면 함께 본다** |
| **`wrong_vp3`의 섭동 크기가 하나뿐이다** | VP3를 **(+400, +900)px** 옮긴 한 점만 쟀다. **축 각오차 중앙 5.59°**(AS-L9 임계의 11배)이고 고정 게이지 형태 오차가 **2.85배**다 — 작은 섭동에서는 어떻게 되는지 모른다 | 동작점 하나다(#12). 스윕하려면 오프셋 격자가 필요하고, **그보다 먼저 물어야 할 것은 "사람이 VP3를 얼마나 틀리게 놓는가"**인데 그것이 AS-L9이고 **표본 0**이다. 임의 격자를 스윕하면 답이 격자가 정한다 |
| **`no_signal` 장면의 성격을 안 갈랐다** | 참 팔 20/216(9.3%) · 가짜 팔 3/72(4.2%) · 틀린 VP3 팔 21/216(9.7%)로 **탈락 비율이 다르다**. ROC 분모가 **서로 다르게 정제된 집합**이다(#11). `no_signal_by`에 어느 셀인지는 남겼다 | 탈락 장면의 형태 오차를 재려면 **승격 후 배치가 0인 장면**의 참값 대조가 필요한데 그 장면은 놓인 획이 없어 게이지가 안 선다. 결론(무신호)이 탈락 방향에 안 달렸다 — 탈락한 장면이 전부 한쪽으로 갔다고 가정해도 판별력이 0.5에 못 닿는다 |
| **주점을 고정한 f² 편차** | 후보 ②가 죽은 이유는 **주점을 수심으로 잡는 것**이었다. 원장 자신이 "잴 수 있으려면 주점이 **독립으로 주어져야** 한다(예: 2점 경로의 이미지 중심 가정)"고 적었다 — 그것은 **관측 가능한 후보 하나를 명시한 것**이고, DEFERRED 초판이 "남은 것은 전부 참값이 필요하다"고 닫은 것은 **너무 넓었다**(2회차 리뷰어 [9]) | 주점을 이미지 중심에 고정하면 세 f²의 편차가 **VP 오차와 주점 가정 위반을 섞는다** — AS-C5(주점이 이미지 중심인가)가 **미검증**이라 그 둘을 못 가른다. 실획 표본이 오면 AS-C5와 함께 본다 |
| **`tiltDeg`의 f 성분을 빼면 남는가** | 실패 원인이 "f 오차와 축 오차를 한 수에 섞는다"로 특정됐다. f가 알려진 조건에서 그 몫을 빼면 축 오차만 남을 수 있다 | 앱에서 f는 **소실점에서 나오므로**(이론서 6.2·6.3) 소실점이 틀리면 f도 틀리다 — **뺄 기준 f가 없다.** 2점 경로의 렌즈 설정값이 그 자리일 수 있으나 그것은 사용자 입력이고 표본 0이다 |

## L-D.1이 남긴 것

| 항목 | 무엇 | 왜 미루나 |
|---|---|---|
| **축 지정이 획을 안 올린다**(사전 등록 ②의 답이 **0/2**) | 원인 둘을 특정했다: ① `solveInto`는 **대기 집합끼리만** 풀어 앵커가 없다(L-B.7이 일괄 **0/1904**로 이미 쟀다) ② `promoteChain` → `resolveLive`가 **축을 다시 추론**해서 `userAxis`를 무시한다 | 고치는 방향은 정해져 있다 — `resolveLive`가 **이미 축 고정 인자를 받으므로**(Shift가 쓰는 그것, D-L19) 사용자 지정 축을 거기로 넘긴다. **바깥에서 덮으면 안 된다**(D-L19). ⚠ 다만 그것은 **§7의 실시간 경로를 건드리는 변경**이라 **오배정이 함께 느는지 측정이 붙어야 한다**(#16 — 병목이 옮겨간 것을 없어진 것으로 읽지 않는다). 컨텍스트가 남지 않아 다음 세션으로 넘긴다 |
| **면 지정이 아니라 축 지정이다** | §9.5는 "'이 획은 저 면 위에 있다' 수준"이라 적었는데 **면 생성이 범위 밖**이라 방향만 줄 수 있다. 둘은 다르다 — 면 지정은 획을 **평면에 가두고** 축 지정은 그러지 않는다 | 면 생성(§6)이 L-A~L-D 범위 밖이다. 면이 생기면 `SnapCtx`에 넣기만 하면 되고(L-B.3이 같은 말을 적었다) 구조가 막지 않는다 |
| **지운 획에 붙어 있던 스냅** | 개수를 세어 알리지만 그 획들을 2D로 되돌리지 않는다 | 이미 놓인 3D는 좌표가 있고 그것을 지우는 것은 **사용자가 안 시킨 삭제**다. 되돌리려면 "스냅이 끊긴 획을 2D로 내린다"는 정책이 필요하고 그것은 §9.1의 대기와 구분이 필요하다(L-C.2가 같은 자리에서 같은 판단을 했다) |

## L-D.1 리뷰어가 남긴 것 — **결론이 철회됐다**

| 항목 | 무엇 | 왜 미루나 |
|---|---|---|
| ⚠⚠ **"0/2는 결함"을 철회했다 — 축 지정이 옳은 답이 아닐 수 있다** | `face_diagonal.json@f351839a`가 **바로 이 픽스처**에 대해 "**축으로 배정된 사선이 놓인 것은 회수가 아니라 조용히 틀린 배치다**"라고 적었다(0.0216 대 **0.4483**, 20배). 같은 원장의 접근은 **"면은 축 둘이 편다 + 앵커가 위치를 정한다"**로 미분류 사선의 **84.16%**를 놓는다 | **다음 세션이 먼저 볼 것**: 축 지정을 고치기 전에 **`face_diagonal`의 면 경로를 §9.5에 붙일 수 있는지** 본다. ⚠ **`DEFERRED`가 적었던 수정 방향(사용자 축을 `resolveLive`에 넘긴다)은 위험하다** — 이 픽스처에서 2/2가 놓이지만 그 둘이 0.4483 자리일 수 있다. **고친 뒤 개수만 세면 안 된다**(#9·#15) |
| **원인 ①의 근거를 철회했다** | "일괄 0/1904"는 이 저장소가 **미실행**이라 철회한 수다(#32·D-L24). 같은 원장에 **승격 없이 대기 집합만 풀면 207/1904(10.9%)**가 있다 | 남는 사실은 **이 픽스처의 대기 집합이 2획**이라는 표본 크기이고, 그러면 원인은 기전이 아니라 픽스처다. **다시 재려면 대기 집합이 큰 픽스처**가 필요하다 |
| **사전 등록 ③이 미판정이다** | "차수 승격을 걸어도 `userAxis`가 유지된다"를 등록해 놓고 **승격을 한 번도 안 걸었다**. `promoteOrder`의 코드를 읽어 통과 처리했다 — **코드 읽기는 카운터가 아니다**(#7) | 판정하려면 `l_d1`에 승격 단계를 붙이면 된다(`l_c1`·`l_c2`가 그 절차를 갖고 있다). ⚠ 다만 **②가 0이라 사용자 축으로 놓인 기하가 0**이므로 ③이 관측 가능한 출력으로 확인될 수 있는 상태인지부터 봐야 한다 |
| **#35를 안 지켰다** — 오라클이 없다 | 등록 ②에 **"무엇이 이 기준을 넘을 수 있는가"**가 없어 **0/2가 축 지정의 성질인지 픽스처의 성질인지 안 갈린다**. L-C.3이 정확히 그 형태로 실패해서 #35를 만들었는데 **바로 다음 항목이 어겼다** | 오라클 팔은 "이 대각선 둘을 **어떤 경로로든** 놓을 수 있는가"이고 `face_diagonal`의 84.16%가 그 답에 가깝다 — **그 하네스를 §9.5 조건으로 다시 돌리는 것**이 다음 세션의 첫 일이다 |
| **원장 `l_d1`이 얇다** | 씬·뷰 소유 필드가 없는데 "통과"라 적었고(`snap_targets` 12→11이 `lifted` 12와 같은 수라 **독립 증거가 아닐 수 있다**), 등급·시드·jitter·dpr·`SIZE_HEAL.count`가 없고(#12·#14·#21·#22), **n = 2**이고, Esc 경로가 미측정이고, 축 번호 규약(0-기반)이 안 적혔다 | 필드를 더 내는 것은 싸다. **다시 돌릴 때 함께 넣는다** — 지금 넣으면 위의 결론 철회와 따로 돌게 된다 |

## PITFALLS #36 — **분모 0 보호가 12개 하네스에 남아 있다**(2026-08-16)

`scan_zero_denominator`가 `web/test`에서 **70곳**(파일 12개)을 찾았다:
`promote.test.ts` 35 · `axis_live.test.ts` 18 · 나머지 10개 파일에 17.

L-C.3에서 고친 것과 **같은 형태**다(#34: 한 자리에서 고친 것은 그 자리에서만 고쳐졌다).
전부 고치면 원장 12개를 다시 돌려야 하고 이번 항목의 범위를 넘는다(A-3).

**지금 하는 것**: 검사를 켜 두고 **파일당 한 건으로** 보고한다(70건을 그대로 내면 소음이 된다).
**다음에 그 하네스를 건드릴 때** `metrics.rate()`로 바꾼다 — 관측이면 `null`, 표시용 보호면
그렇게 적는다. 새 하네스는 처음부터 `rate()`를 쓴다.

⚠ **트리거를 하나 더 단다**(리뷰어 [3], 2026-08-16). "건드릴 때"는 기한이 아니라 조건인데,
`promote.test.ts`(35곳)와 `axis_live.test.ts`(18곳)는 **D-L21(D-L18 기각)의 근거 원장**이다 —
2976 대 3136도, cut 0.2의 0.4037 → 0.329도 거기서 나온다. 그 53곳이 **층화 비율(관측)**인지
**표시용 나눗셈 보호**인지 안 갈린 채이므로:

> **D-L21을 재개하기 전에 그 둘을 먼저 가른다.** 층에 분모 0이 있었다면 그 층의 비율은
> 없는 관측이고, D-L21의 두 팔 비교가 그만큼 흔들린다. 재개 없이 인용만 할 때도 같다 —
> **인용하는 칸의 분모가 0이 아닌지 그 자리에서 확인한다.**

## §9.5 지정 경로 — **면 지정은 §6과 함께 본다**(D-L28, 2026-08-16)

기존 원장으로 결론했다(새로 재지 않았다). `face_diagonal.json@f351839a`:
축 배정 사선 **19/120**의 `corner_err_ratio`(끝점 거리 ÷ 상자 대각, **게이지 없음** —
D-L22의 형태 오차와 **다른 양이다**) 중앙 **0.4483**, 면 경로로 회수된 **85/101**의 중앙 **0.0216**.
⚠ **"20배"를 지웠다**(리뷰어 2회차). 두 수의 분모가 다르고, 더 중요하게는 두 집단이
**판정 대상인 그 분류기가 갈라 놓은 disjoint 부분집합**이다(19 = `by_reason.ok`가 **항등**이다).
짝지은 비교가 아니므로 배수로 읽지 않는다 — 남는 말은
**"축 경로는 자기가 가장 축답다고 고른 사선에서도 크게 틀린다"**이고 D-L28이 그 형태로 서 있다.
⚠ **비율보다 분자/분모로 적는다**(CLAUDE.md §5): 미분류 **101/120**, 그중 회수 **85/101**.
0.8417과 0.8416이 소수 넷째 자리까지 같아서 **비율로 적으면 다음 인용에서 안 갈린다**.

**그러므로 §9.5가 원하는 것은 면 지정이다.** 지금 넣지 않는 이유: 면 경로는 **평면(축 둘) +
앵커**를 요구하는데 L에는 면 개념이 없고(§6 범위 밖) 스냅이 앵커를 대신한다.
**다시 볼 조건**: §6(면)을 열 때. ⚠ **그대로 인용할 수 없다** — 그 측정은 **폐기된 배치 계층**(D-S14·S-6·S-8)의 것이고
`AS-5`는 지금 **무효**로 분류돼 있다(D-L1: 아카이브를 판단 근거로 인용하지 않는다).
§6을 열 때 **그 조건에서 다시 재야** 한다.


## L-D.0이 남긴 것 — **롤 0의 초안 효과는 안 쟀다**(2026-08-16)

| 항목 | 원래 목표 | 지금 | 트리거 |
|---|---|---|---|
| **롤 0 제약이 초안을 낫게 하는가** | 사람 지시: 게이트를 못 넘으면 "초안 품질이 얼마나 올랐는지 기록한다 — 조정 부담이 준 것만으로도 이득이다" | **안 쟀다.** `horizon.json@509615a1`이 잰 것은 **축 방향 오차**이고 그것은 초안의 지표가 아니다 — 초안이 하는 일은 **끌기 시작 위치**를 주는 것이고 그 지표는 `draft_recover.json`의 **확정률**(31/90 → 58/90, D-L16)과 **축 오차 중앙**(12.35 → 8.95°)이다. 축 오차로 대신 적으면 **#2**(대리값의 성질을 재는 것)다 | **`draft_recover` 하네스에 `roll_zero` 팔을 넣어 다시 돌릴 때.** 그 하네스는 `vpDraft`의 지지선 선택을 재므로, 롤 0을 **초안 적합 안에** 넣은 판이 확정률과 축 오차를 어떻게 바꾸는지가 직접 나온다. ⚠ 배치는 안 본다 — 초안은 놓는 경로가 아니다 |
| **롤 0의 손익분기가 5°인 것을 사람 표본으로 확인** | 종이를 기울여 그리는 사용자가 실제로 몇 도인가 | **표본 0**(AS-C1). 합성 픽스처는 롤 0으로 만든 것이다 | 실획이 `sessions/`에 들어올 때. `QUESTIONS.md` (d)와 같은 자리다 |
| **배치 붕괴의 자리를 특정 안 했다** | 소실점을 78~98px 옮기면 `liftAll`의 무엇이 거절하는가 | 미배치 사유별 카운터를 이 하네스가 안 냈다 | 롤 0을 다시 볼 때. `lift_grade`가 같은 종류의 분해를 이미 한다(미배치 사유별) |

## L-D.4가 남긴 것 — **이름과 실사용 취약점**(2026-08-16)

| 항목 | 원래 목표 | 지금 | 트리거 |
|---|---|---|---|
| **저장소 이름이 `SKETCH2SPACE`다** | 이름을 `brunelleschi`로 통일 | 코드·manifest·title·package·확장자(`.brnl`)는 **전부 바꿨다**. **저장소 디렉토리 이름만 못 바꿨다** — 그것은 git remote와 로컬 경로를 함께 옮기는 일이라 사람이 한다 | 사람이 리포지토리를 `brunelleschi`로 바꿀 때. 그때 README 1.5절의 배포 URL도 함께 정해진다 |
| **`PROTOTYPE.md` §5의 일곱** | 실사용 취약점을 고친다 | **기록만 했다**(사람 지시: 지금 고치지 말고 적을 것). ⚠ **수치가 없다** — 작도 과정을 따라가며 예상한 것이고 측정이 아니다 | 실획이 들어오거나 다음 계획을 세울 때. **수치가 붙으면 그 항목은 §1(알려진 결함)로 옮긴다** |
| **곡선이 카메라 입력이 될 수 있다** | 프리핸드와 함께 나중에 | 범위 밖 | ⚠ **우선순위가 다르다**: 이론서 8.7절 `d = (A/B)√(ab)`이 **타원 하나에서 시거리를 낸다**. 즉 곡선은 "표현"이 아니라 **카메라 확정의 입력**일 수 있다. §6(면)·프리핸드와 함께 볼 때 이것을 먼저 본다 |
| **v1 저장을 열 수단이 없어졌다** | 옛 문서 이전 | 옛 UI를 지웠으므로 **IndexedDB의 `current` 키를 읽는 코드가 없다** | 사람이 옛 문서를 꺼내 달라고 할 때. 데이터는 그대로 있으므로 읽는 스크립트를 한 번 짜면 된다(D-L29) |

## 옛 UI 삭제가 남긴 것 (2026-08-16, 리뷰어 지적)

| 항목 | 무엇이 빠졌나 | 트리거 |
|---|---|---|
| **#21의 진짜 회귀 팔이 없다** | `coords.spec`의 `[양성 채널]`은 **탐침의 눈**만 확인한다(캔버스에 직접 칠한다) — **앱의 좌표 변환 경로를 안 지난다.** "앱이 dpr을 두 번 적용해도 이 스펙이 실패한다"는 **미확인**이다 | 좌표 규약을 건드릴 때. 필요한 것은 **앱 쪽에 dpr 곱을 되살린 팔**이고, `canvasFrame.ts`에 시험용 배율 인자를 두면 된다(앱 기본값은 안 바꾼다) |
| **하네스와 앱이 같은 배치 옵션 출처를 쓰는지 아무도 안 본다** | 옛 `wiring.test.ts`가 그것을 봤는데 **대상(옛 `settle`)이 사라졌고**, "`stage.spec`으로 옮겨졌다"는 서술은 **철회했다**(D-L35) — 그 스펙은 참값 대조를 안 한다 | L의 배치 경로(`liftAll`·`placeLive`·`solveInto`)의 옵션을 늘릴 때. 그때 `appPlace.ts` 같은 단일 출처를 L용으로 다시 만든다 |
| **`view_vs_overlay`가 덮는 범위가 줄었는지 미확인** | 옛 UI는 **2D·3D 캔버스 둘**의 배율 불일치를 잡았고, 새 UI는 캔버스가 하나라 **잉크 ↔ three 렌더러 상자**를 본다. 옛 실패 모드(`setPixelRatio`가 잉크 규약과 갈리는 것)에 대응이 있는지 안 봤다 | dpr 규약을 다시 볼 때. `coords.json`의 `what_this_does_not_say`에 적어 두었다 |
| **옛 UI만 재던 것들** | `archive_W/e2e.json`의 고치기 조작 수·메쉬 크기·`fix_in_rotated_view`는 **새 UI에서 안 쟀다.** 그 표는 **회귀 감시용이 아니다**(다시 못 돌린다) | `고치기` 경로를 다시 볼 때 |

## 규칙 기반 전환이 미룬 것 (2026-08-16)

| 항목 | 무엇을 미뤘나 | 트리거 |
|---|---|---|
| **순서 가정이 미검증이다**(AS-L13) | 규칙 ⓑ가 "**처음** 두 깊이선의 교점"이라 **그리는 순서가 결과를 지배한다** — 축별로 몰아 그으면 축 오차 6.93°, 번갈아 그으면 32.57°였다(옛 실행 — ⚠ **[D-L59 주석]** 이후 값이 두 번 움직였다. 현행은 몰아 31.61°·번갈아 27.85°, 원장을 읽는다 #1). 사람이 어느 쪽으로 긋는지 **표본이 0이다** | `sessions/`에 실획이 들어오면 `real_ink.test.ts`가 AS-6과 함께 답을 낸다 |
| **8° 밖은 안 묻는다**(AS-L15) | 손으로 그은 수평선이 8°를 넘으면 **깊이선으로 조용히 읽힌다**(잡음 0.03~0.05에서 1점 구도에 가짜 소실점이 생겼다). 그 자리가 이 규칙의 조용히 틀리는 문이다 | 실획의 수평선 기울기 분포. 필요하면 `depth_min_deg`를 올리거나 **애매 구간을 넓힌다**(묻는 쪽이 안전하다) |
| **소실점을 지지선으로 다듬지 않는다** | "소실점은 확정 후 잠긴다"(CLAUDE.md §1)라 뒤에 온 선이 소실점을 안 고친다. 검출 팔은 4선 최소제곱이라 그만큼 유리하다. **잠금을 풀면 추정으로 되돌아가므로** 지금은 안 한다 | 사용자가 명시적으로 "다시 잡기"를 누르는 경로를 열 때. **자동으로는 안 연다** |
| **`stage_browser.json`의 `l_b2`가 안 갱신된다** | 그 스펙(가이드 조정)의 **대상이 사라졌다**(D-L37) — `test.skip`으로 두고 사유를 적었다. 계획서 §5.2의 핸들 예산 표는 **낡은 UI를 설명하는 것**이다 | 계획서를 규칙 기반으로 다시 쓸 때 §5.2를 통째로 갈아 끼운다 |
| **`vpDraft`·`vpDetect`·`vpSensitivity`·`vpHomog`가 앱에서 빠졌다** | 코드는 남겼다 — `camera_gate`·`draft_recover`·`guide_budget`·`vp_homog` 원장이 그 판정의 근거를 계속 재현한다. **배선은 없다** | 규칙 경로가 게이트를 통과한 뒤에 지운다(A-4: 폐기 코드를 지우기 전에 게이트를 통과시킨다) |
| **옛 저장본(`rules` 없음)이 안 열린다** | `Doc2.rules`가 없으면 규칙이 비어 카메라가 안 서고 획이 2D로 남는다. **조용히 틀린 카메라를 세우는 것보다 낫다**(A-3). `cam`(누산기 덤프)은 남아 있으므로 읽는 스크립트를 한 번 짜면 된다 | 옛 작업을 실제로 열어야 할 때(D-L29와 같은 자리) |

### ⚠ 미해결 — 종단 확인 두 스펙이 깨진 채로 남았다 (2026-08-16)

**A-4("기존 테스트가 깨진 채로 다음 항목에 가지 않는다")를 지키지 못했다.** 그대로 적는다.

| 스펙 | 증상 | 무엇까지 확인했나 |
|---|---|---|
| `stage.spec.ts` `단일 뷰포트 — 확정 시 3D가 잉크 자리에 그려진다` | `setup()` 뒤 `확정`을 눌러도 **배치 0**(12이어야 한다) | **같은 절차를 별도 스펙으로 재현하면 12가 나온다** — 규칙·축 판정·`liftAll`은 정상이다(`hasCtx` true · `order` 3 · 축 12개 전부 배정 · `lifted` 12). 즉 **하네스 쪽 문제**이고 앱 경로가 아니다. ⚠ 실행마다 갈린다(같은 명령이 한 번은 이 줄을 통과하고 다음 실행에서 못 통과했다) — **비결정이다** |
| `stage.spec.ts` `되돌리기 UI` | `snap_lost`가 0(0보다 커야 한다) | 위와 같은 픽스처를 쓰므로 **같은 원인일 가능성이 높다**. 갈라 보지 않았다 |

**의심하는 자리**(확인 안 함): 저장 복원이 비동기라 앞 시험의 문서가 늦게 도착하는 경로.
그 방향으로 한 번 고쳤고(`getDoc2` 복원이 **이미 그린 문서를 안 덮는다** — 그 자체는 실사용
결함이라 남긴다) **증상이 안 사라졌다.** 즉 **원인을 못 짚었다**(#7: 추측하지 말고 센다).

**다음 세션이 먼저 할 일**: `setup()`이 돌려주는 `hasCamera`·`strokes`를 실패 실행에서
찍어 본다. 그 둘이 정상이면 `확정` 버튼이 `disabled`인 경로를, 아니면 문서·카메라가
언제 갈아 치워지는지를 본다.

~~⚠ **`stage_browser.json`이 그래서 갱신되지 않았다** — 그 원장을 인용하는 문서의 수치는
`@34db7279`(옛 상수) 그대로이고 selfcheck가 STALE로 표시한다. **낡은 것이 맞다.**~~
**2026-08-17 해소 확인**: 그 원장은 그 뒤 재실행됐고 현재 해시 `69eba78d`이다. 남은 인용
셋(`progress.md`·`README.md`)도 전부 `stage_browser.json@f351839a`로 현재 해시다 — 이 경고 자체가
낡아 있었다(두 세션 연속 이월 정리에서 닫음).

## 축 스냅·지평선이 미룬 것 (2026-08-16 2차)

| 항목 | 무엇을 미뤘나 | 트리거 |
|---|---|---|
| **축 스냅의 게이트 실패** | 조용히 틀린 배치가 **1094/1322(0.8275) → 1270/1322(0.9607)**(절단 0.2, 번갈아 긋기, `axis_snap.json@f351839a` — 초판의 0.811 → 0.967은 재실행 전 값). 축 스냅이 **카메라 오차를 증폭**하기 때문이고, 이 픽스처의 카메라 축 오차가 임계의 **18~26배**다. **카메라가 정확할 때 부호가 뒤집히는지 안 쟀다** — 참 소실점을 넣으면 그것은 항등이라(#5) 그 조건을 만들 픽스처가 없다 | 카메라 정확도가 임계에 가까워지는 경로가 생길 때. 오스냅이 그 후보다(양 끝이 확정되면 축에 안 기댄다) ⚠ **[6차 주석]** 이 줄이 인용한 수치는 **6차 지시 11**(소실점은 **세 번째 선**이 정한다) **이전 실행**의 것이다 — 원장이 다시 움직였다(현행 값은 원장을 그 자리에서 읽는다, #1). |
| **모호 판정의 효과가 이 하네스로는 안 재진다**(#32) | 스윕 전 행에서 배치·조용히 틀림이 불변인 것은 **구성의 귀결**이다 — 하네스가 발동을 세기만 하고 배치는 발동 여부와 무관하게 `pick`으로 놓는다(발동이 배치 경로에 안 닿는다. 2026-08-17 리뷰어 [6]이 잡았고 AS-L18의 첫 결론 "안 바꾼다"를 철회했다). 남는 측정은 발동률(0 → 534/1100)과 발동분 정확도(최대 0.1037)뿐(`axis_snap.json@f351839a`) | 발동이 배치를 실제로 바꾸는 팔(발동 시 묻기/보류)을 하네스에 넣는다. 그리고 카메라가 정확한 픽스처에서 다시 잰다 ⚠ **[6차 주석]** 이 줄이 인용한 수치는 **6차 지시 11**(소실점은 **세 번째 선**이 정한다) **이전 실행**의 것이다 — 원장이 다시 움직였다(현행 값은 원장을 그 자리에서 읽는다, #1). |
| **`고치기` 스펙의 축 지정 판정이 안 돈다** | 면 대각선이 `axisOfStroke`에서 어느 축으로 배정돼 놓이면서 **대기 획이 0**이 됐다. 그것이 회수인지 **조용히 틀린 배치**인지 안 갈랐다 — `face_diagonal.json`은 후자라 적었다(0.0216 대 0.4483) | 오스냅이 서면 면 대각선은 **양 끝 지정**으로 놓인다(사람 지시 6). 그때 이 자리가 없어진다 |
| ~~**초기 스케치가 1점·2점뿐이다**~~ **대상 소멸**(2026-08-17) | 이 항목의 기전(수심 유도가 지평선 높이에 걸려 `null`)이 **A-4에서 통째로 빠졌다**. 3점은 이제 기울어진 세로 모서리의 수직축 선언으로 초기부터 선다(D-L47) — "궤도 뒤"라는 제약 자체가 없어졌다. 사람 결정도 이미 왔다(D-L45가 지평선을 사용자가 끌게 했다) | 없음 — 닫힘 |
| **프리핸드 경로** | 삭제하지 않았다 — `pts2d`·`pts3d`가 그대로 있고 렌더 네 자리만 직선이다(D-L41). **모드 플래그는 아직 없다** | 프리핸드로 돌아갈 때 그 네 자리에 플래그를 건다 |

## 오스냅 (2026-08-16)

- **같은 꼭짓점이 획마다 다른 3D 점이다** — 모서리를 따라 그은 획의 두 앵커가 서로 다른 획에서 와서 그 방향이 축과 **6.3° 어긋난 실측**이 있다(종단 확인). 두 점 배치는 그 불일치를 그대로 물고, 그래서 축 라벨이 종종 미분류다. **우회**: 라벨을 억지로 안 붙인다(기하는 사용자가 지정한 두 점 그대로). **원래 목표**: 공유 꼭짓점을 병합해 기하가 한 점을 갖는 것. **트리거**: **두 점 배치 선분의 축 대비 방향 오차**(실측 6.3°가 그 값이다)가 배치 품질을 떨어뜨리는 것이 원장에 잡히면. ⚠ 초판은 트리거를 '축 미분류 비'로 적었는데 그것은 **설계상 상수에 가까운 양**이라 발화하지 않는다(리뷰어 지적으로 정정).
- **`osnap.json`은 대상을 참 3D로 준다**(상한). 앱의 대상은 **놓인 3D 레이어**이고 카메라 오차를 문다. **트리거**: 실획 표본이 들어오면 같은 지표를 그 위에서 다시 잰다.
- **지평선을 오스냅 대상으로 넣지 않았다** — 화면 직선이라 붙여도 3D가 안 정해진다. **트리거**: 사람 지시의 '지평선 근처점 경로 확장'이 다른 뜻이었으면.
- **정점(`vertex`) 스냅이 지금 기하에서 한 번도 안 뜬다** — 끝점이 안 합쳐지기 때문이다(상자 하나에서 끝점 후보 **24/12획**, 같은 꼭짓점 간 거리 **0.0305~0.1182·G** 대 병합 반경 **0.002·G**). **우회**: 종류를 남겨 두고 사실을 적는다. **원래 목표**: 공유 꼭짓점을 하나로 만드는 기하 계층. **트리거**: 위 항목(꼭짓점 병합)이 서면 자동으로 해소된다. ⚠ **병합 반경을 넓히는 것으로 대신하지 않는다** — 이웃 꼭짓점과 같은 자릿수라 다른 점끼리 합쳐진다.
- **`osnap.json`이 앱의 3D 레이어를 안 쓴다** — 참 3D를 쓰므로 위 불일치를 **구조적으로 못 본다**. **트리거**: 앱 기하를 대상으로 하는 하네스를 만들 때(그때 정점 0과 6.3°가 원장에 들어온다).
- **종단 확인의 약화를 메울 새 검사가 없다**(리뷰어 [2]) — 두 점 배치 단언이 `snapStart/snapEnd`를 되읽는 **항등**이다. **원래 목표**: 놓인 3D의 끝이 **대상 획의 실제 3D**와 같은지 대조하는 팔(앵커 기록이 아니라 기하와 대조). **트리거**: 다음 항목에서 그 스펙을 쓸 때.
- **근처점을 뺀 교환의 대가를 안 쟀다**(리뷰어 [13]) — 첫 판(근처점 포함)의 오붙음을 기록하지 않았다. **트리거**: 끝점 스냅의 대상 목록을 다시 만질 때 두 팔을 함께 돌린다.
- **꺾인 획 가드의 효력이 미측정이다** — 합성 대각선에 꺾인 것이 없어 `too_bent`가 0이다(덮는 대상 0, #38). **트리거**: 실획이 들어오면 그 대역에서 다시 잰다(Quick,Draw 낙서의 67.7%가 그 종류다).

## 2026-08-17 — 사람 지시 A~K, 그리고 리뷰어 20건

| 미해소 | 사실 | 트리거 |
|---|---|---|
| **3점 입구의 정확도가 미측정이다** | A-4가 3점의 유일한 입구를 **"기울어진 세로 모서리를 '수직축'이라 답한다"**로 바꿨다. 그 입구를 가르는 팔이 원장에 **없다** — `horizon_pitch.json`의 팔은 `center/true/placebo/err_±N`(전부 **지평선** 축)뿐이다. AS-L20이 인용한 3.39~4.88°는 **참 지평선 팔**의 값이고 다른 기전을 잰 것이다(리뷰어 [10]) | `tilted_vertical` 입구를 켜고 끈 두 팔을 `horizon_pitch` 또는 새 하네스에 넣는다. **측정 전에 게이트를 등록한다**(#26) |
| **`horizon_pitch.json`의 `passed`가 영원히 false다** | 등록 조건 ①("중앙 지평선 팔은 차수 3이 0")이 잰 기전(수심 유도)을 A-4가 없앴다. 조건은 만족될 수 없고 원장은 `passed:false`·`passed_orders:[]`로 남는다. **원장 안의 `registered`·`what_the_zeros_mean` 문장도 옛 기전을 사실로 적는다** — 다음 세션이 원장만 읽으면 틀린 문장을 읽는다(리뷰어 [7][8]) | 하네스의 등록문을 고쳐 ①을 **"대상이 사라졌다"**로 표시하고 ②·③만 판정하게 한다. **조건을 지우지 않는다**(왜 그 규칙이 생겼는지가 근거다) |
| **A·B·A-4가 같은 실행에 섞여 있다** | `rule_camera`·`axis_snap`의 변화를 어느 변경에 돌릴 수 없다. 팔이 `rule_drawn/rule_grouped/rule_wide_pair/detect`뿐이고 **A만 켠 팔도 B만 켠 팔도 없다**(리뷰어 [14]) | 각 변경을 끄는 `cfg` 손잡이를 하네스에 열고 팔을 나눈다 |
| **`min_vp_len_ratio`가 몇 획을 걸렀는지 세는 곳이 없다** | `rejected` 사건에 섞여 들어가고 사유별 분해가 없다(#38 덮는 대상 미상). 배수 2도 **스윕을 안 돌렸다**(#12) | 하네스가 거절 사유를 갈라 세고, `0.02~0.08` 스윕을 낸다 |
| **`first_pair_sep_deg_median`이 원장 40여 곳 전부 `null`이다** | AS-L13과 D-L37·README의 순서 설명이 **그 양에 대한 주장**인데 그 양이 한 번도 기록되지 않았다(리뷰어 [19]) | 하네스가 그 값을 실제로 채우거나, **덮는 대상 0**이라고 원장에 적는다 |
| **`axis_snap.json`의 `order_gap_off = 0`이 미실행이다** | `snap_off__horizon_off` 팔은 `drawn`과 `grouped`의 **모든 필드가 글자 그대로 같다** — 순서 knob이 안 돈다. 그 0을 게이트 ③의 기준선으로 쓰면 판정이 **다른 조건의 팔**과 견주는 것이다(리뷰어 [4], #32) | 게이트 ③의 기준선을 `snap_off__horizon_on`(1.118 — 2026-08-17 3차 재실행 값)로 바꾸거나, horizon_off 팔에서 순서가 왜 안 도는지 적는다 |
| **위약 팔의 조용히 틀림이 1.0으로 포화했다** | `placebo_wrong_horizon__drawn`: `silent_0_2_rate = 1`(**775/775**, `axis_snap.json@f351839a` — 초판의 770/770은 원장과 달랐다, 2026-08-17 정정). 천장이라 "차이의 크기"를 못 읽는다(리뷰어 [20], #40 ②) | 위약의 세기를 줄인 중간 팔을 넣는다 | ⚠ **[D-L59 주석]** 이 행의 위약 수치도 D-L59 이후 원장이 움직였다(위약 팔 = 참 팔과 동일값 — 대조 자체가 죽었다, 항목 3 리뷰어 [4]).
| **"측정이 설계를 세 번 정정했다"의 수치가 원장에 없다** | 0.63 · 37.7° · 21/144 · 141/144는 **중간 실행**의 값이고 그 코드가 없으므로 **재현 불가능**하다(리뷰어 [9], #25). 그 셋이 설계 변경 셋의 근거다 | 초판 동작을 `cfg` 플래그로 되살려 팔로 남기거나, **원장 밖 값임을 명시**한 채 둔다(지금은 후자다) |
| ~~**D~K가 손도 안 갔다**~~ → **H·I·J·K가 남았다** | 그 세션은 A·B·C·F만 했고, 이어서 **D·E·G까지 끝났다**(2026-08-17). 남은 것은 H(오스냅 반경·수직점·거리점 스냅) · I(자동 분할·지우개) · J(그리드·패널) · K(실획 측정 준비)다 | `HANDOFF.md`의 "다음에 할 일" |
| **`SHARED_CONSTANTS`가 전역 해시 하나라 무관한 상수 추가가 전 원장을 STALE로 만든다** | G에서 `GESTURE_TOL`(제스처 감도)을 넣으면 그것에 **의존하지 않는 원장 40여 개**가 전부 STALE이 된다. 그래서 안 넣었고 대신 그 값을 원장 안에 적었다 — **STALE 검사의 사각지대를 하나 만든 것**이다(감도가 바뀌어도 `touch_route.json`이 안 걸린다) | **의존 집합별 해시** — 하네스가 자기가 읽는 상수만 스냅샷에 넣고 selfcheck가 그 부분집합만 대조한다. 지금은 전부 아니면 전무다 |
| **제스처의 실기 표본이 0이다** | `touch_route.json`은 chromium의 **합성 포인터 사건**이다. 실제 아이패드의 손바닥 접촉 형태·펜과 터치의 시간차·`pointercancel` 발생 시점은 안 재진다(AS-C1과 같은 자리) | K(실획 측정)와 같은 문 — 아이패드에서 한 세션을 받으면 `palm()` 카운터가 그대로 답이 된다 |
| **원장 밖 회귀 수치**(2026-08-17 항목 1~3) | D-L53의 회귀 크기(몰아 중앙 **31.6°** · `rule_camera` 몰아 **33.9°**)는 **커밋되지 않은 중간판의 실행 값**이라 재현 불가다(#25 — DEFERRED 307과 같은 유형, #34의 재발). 방향 증거는 직전 커밋 원장 대조(`git show 47da6c4:stage0/out/axis_snap.json`)로 남는다 | 초판 동작(소실점 있는 상태의 조용한 P1 선언)을 `cfg` 팔로 되살려 재면 닫힌다. 지금은 원장 밖 값임을 D-L53에 명시했다 |
| **`axis_snap.json`에 1pt 항등 제외 층이 없다** | D-L47이 "인용은 `headline_no_identity`에서 한다"고 정했는데 `axis_snap`의 `by_arm_order`는 전부 **5구도 혼합**(1pt 항등 포함)이다 — D-L42·53의 인용이 그 층 위에 있다. 항등 행은 두 팔에 똑같이 들어가 차이를 희석하는 방향이라 방향 판정은 살지만, 크기는 항등 지분만큼 눌려 있다 | 하네스에 `headline_no_identity` 층을 추가할 때. 그때 D-L42·53의 수치를 그 층으로 옮긴다 |
| **동결 리터럴이 세 원장의 인용 대조를 영구 통과로 만든다**(D-L54) | `horizon.json`·`draft_recover.json`·`guide_budget.json`은 하네스가 삭제돼 **다시는 안 움직인다** — 이 셋을 가리키는 인용 **9건**(DECISIONS 2 · DEFERRED 2 · assumptions 2 · line_plan 1 · progress 2)의 해시 대조 통과는 "현재와 같다"가 아니라 "영원히 못 움직인다"는 뜻이다(#40의 "값이 자명하면 같은 상태") | 그 인용들을 읽을 때 사람이 이 사실을 함께 읽는다. 의존 집합별 해시가 서면 동결 리터럴과 함께 정리한다 |
| **P1(임의 f)의 형태 오차가 미측정이다** ✅ **부분 해소(2026-08-18 8차 2차 지시 항목 2 · 8-2R″ [4])**: `order_lock.json@f351839a`에 **f 팔 셋**을 넣었다 — `fixed_true_f`(참 f) · `fixed_f_half`(×0.5) · `fixed_f_double`(×2)와 개입 크기 `f_intervention`. **실측**: ×0.5에서 P1 중앙 1.6914 → **2.0866** · ×2에서 → **1.3061**이라 **형태 오차는 f에 반응한다**(자기 게이지가 지우지 않는다 — 비등방 오차다). **그러나 '형태는 정확하다'는 안 나왔다** — 참 f에서도 P1 중앙이 **1.6618**이다. ⚠ **완전 해소가 아니다**: 이 픽스처의 참 f(1000)와 임의 f(960)가 4%밖에 안 달라 **'임의 f 자체의 대가'는 여전히 미측정**이고, 동작점이 ×0.5·×1·×2 셋뿐이다(#12). | "형태는 정확하고 깊이만 무차원"(지시 1)의 근거는 `frontalWorld` 게이지 논증뿐이다 — 이론서 16.4의 잔여 자유도(깊이 아핀)에서 전단이 실제로 빠지는지(주점=소실점 조건), 형태 오차 지표로 잰 팔이 **원장에 없다**(#37 늦은 확인) | P1 픽스처(가로선+깊이선)에서 참 f 대비 임의 f의 형태 오차 팔을 하네스에 넣을 때 |
| **축 오차 분포가 오배정 점프에 지배될 가능성**(리뷰어 [11]) | `rule_camera`·`horizon_pitch`에서 서로 다른 팔·구도의 p90·max가 **글자 그대로 같은 값**(35 · 47.1001 · 31.6083)으로 반복된다 — 축이 인접 축으로 오배정되면 오차가 축 간 각 그대로 나오는 형태(추측). 맞으면 중앙값 변화가 각도 저하가 아니라 **오배정 건수의 이동**이다(#9) | 하네스가 `axes_measured` 옆에 **오배정 수**를 세면 갈린다. 다음에 그 원장을 만질 때 카운터를 넣는다 |
| **5-1(결과선 소실)의 원 조건 재현이 미확인이다**(지시 5) | 보고 조건은 **아이패드 배포본(터치·dpr 2)**인데 확인은 dpr 1·Playwright 합성 마우스다 — 그 조건에서 "재현 안 됨"이고 **원인 귀속은 못 한다**(#32의 변종). 회귀 팔(`basic_flow` ④ 차등 판정)은 앞으로의 재발을 잡지만 과거 원인은 특정 안 됐다. 표시 값(LINE_PX·표식 반지름)은 CSS px라 dpr에 시각 크기가 불변이지만 **실기 확인이 없다**(#21) | 아이패드 실기 시험(K와 같은 문)에서 같은 순서를 그려 본다 |
| **AS-L9·QUESTIONS d·e·f의 실획 직접 측정이 불가하다 — 대리 재적합 팔 미구현**(지시 K, 리뷰어 [9][10]) | 넷 다 **참값(지평선·카메라)이 실획에 없다**는 같은 구조다: AS-L9(±120px 창) · f(축 스냅 손익분기 배수 — 참 카메라 필요) · d(지평선 수평 — 스칼라 y라 항등) · e(롤 — 같은 자료구조). 대리는 재적합 팔인데 검출 경로의 부활이라 설계 검토가 필요하다. `real_ink.json`의 `what_this_cannot_measure`에 명시했다 | 실획이 들어와 `real_ink`가 처음 돌 때 함께 판단한다 |
| **그리는 시점의 자동 분할이 미배선이다**(지시 I, D-L56) | 분할 기하(`split.ts`)는 있고 **지우개가 조각을 집을 때만** 계산한다. 상시 분할은 획이 늘 때 O(n²)이고 **조각 폭주(획 수 증가)를 안 쟀다** — K의 획 단위(#11)도 조각(`pieceOf`)으로 갈라 두었다 | 그리는 즉시 분할이 필요한 사용례가 보고되면. 그때 조각 수 지표를 함께 잰다 |
| **오스냅 조리개 15px의 dpr 2·실기 확인이 없다**(D-L56, #21) | CSS px 임계라 시각 크기는 dpr 불변이지만, 지시의 출처(아이패드)에서 확인 안 됨 — DEFERRED 316(표시 값)과 같은 문이고 이쪽은 **동작 임계**다 | 아이패드 실기 시험(K의 문) |
| **인용 수치의 값 대조가 자동에 없다**(2026-08-17 항목 0) | `scan_citation_hashes`는 **해시 대조**다 — ① 해시 없는 인용은 덮는 대상 밖이고 ② 해시가 맞아도 문서에 옮겨 적은 **수치**가 낡으면 안 잡는다. 두 세션 연속 이월된 낡은 인용의 원인이 이 사각지대이고, 정리한 당일 리뷰어가 **같은 유형 셋을 더 찾았다**(순서 라벨 뒤집힘 · D-L43의 32~35° · DEFERRED의 770/770). 검사를 넓히는 쪽(#19 위험)보다 **인용할 때 원장을 그 자리에서 다시 읽는 규칙**(CLAUDE.md §5)이 현행 방어다 | 인용 문법을 `원장.json@해시/경로=값` 꼴로 굳혀 selfcheck가 값까지 대조할 수 있게 될 때. 그 전에는 사람이 본다 |

- **2D 오스냅의 겨냥 거리 기록**(2026-08-17 4차 지시 1): 2D 스냅은 `snapStart`(3D 점)를 못
  만들므로 `snapDistPx`에 적으면 K의 모집단(스냅 여부 = snapStart 유무)이 섞인다(#11).
  실획에서 2D 겨냥 분포가 필요해지면 별도 필드(`snap2DistPx` 등)로 갈라 담는다.
  ⚠ **이 미룸은 D-L56의 판정 범위를 좁힌다**(리뷰어 [2]): 오스냅 15px의 최종 판정자로 등록된
  `snapDistPx` 표본이 **확정 후 획으로만** 이뤄진다 — 보조선 단계(확정 전, 작도의 본체)의
  겨냥은 이 계기로 안 잡힌다. D-L56에 같은 사실을 적었다.
  트리거: `sessions/`에 실획이 들어와 K가 돌 때.

- ~~**`horizon_pitch` 하네스의 조작 변수가 죽었다**~~ **해소(5차 이월-3, 2026-08-17)**:
  horizon_pitch는 **은퇴**(원장 what에 표시 — 역사 기록, 인용 대상 유지 위해 파일은 남긴다.
  D-L54 동결과 같은 자리라 그 사각지대 목록에 이 원장도 더한다), AS-L19 폐기 표시,
  **찍기 경로 측정은 `pick_vp.test.ts` → `pick_vp.json`이 신설**로 잇는다(AS-L23):
  찍기 0 팔 2.67° 대 two_lines 31.61°, ±120px 평탄. ⚠ 남은 것: **배치 영향**(축 오차만 쟀다 —
  배치·조용히 틀림은 이 하네스 밖) · 사람 찍기 정확도 표본 0(AS-L9).
- ~~**스냅 반영 합성 하네스가 없다**~~ **rule_camera는 해소(5차 이월-2, 2026-08-17)**:
  `resolve2dCore` 추출(#17)로 `rule_*_snap` 팔이 앱의 확정 전 2D 판정을 그대로 통과한 선을
  먹는다. 판정: **스냅 입력이 31.6°를 구제하지 않는다**(물음 오라클 판 몰아 31.61→35.00·번갈아 27.85→29.66 = git show d724ac0 / **무물음 선언 판(현행) 둘 다 →35.00** — 8-R′ 정정 ·
  **위약(같은 크기 임의 방향)은 원시와 같다**, `rule_camera.json@f351839a` — ⛔⛔ **제자리 정정(2026-08-18 8차 2차 지시 항목 0 · 8-2R [4])**: 그 "현행 둘 다 →35.00"은 **8차 항목 1 이전**이다. 현행 짝지은 층은 **번갈아 7.3342→7.7742 · 몰아 7.8135→7.4653 · wide_pair 8.7129→8.0003**으로 **부호가 갈린다** — "구제하지 않는다"의 근거가 약해졌고 **다시 열어 둔다**) — D-L59의 회귀는 ⚠ **[6차 주석]** 이 줄이 인용한 수치는 **6차 지시 11**(소실점은 **세 번째 선**이 정한다) **이전 실행**의 것이다 — 원장이 다시 움직였다(현행 값은 원장을 그 자리에서 읽는다, #1).
  하네스 문제가 아니다. ⚠ 시드 폭 미확인(#14)·오배정 고원(DEFERRED 315) 위의 값이라
  1~3° 급 격차는 강도가 약하다 — 확실한 것은 "구제한다"의 부재다. **남은 것**:
  axis_snap 스냅 팔(트리거: **실획 K** — 항목 3이 3-c의 축 오차 축을 rule_camera로 닫았고 배치 축은 K로 미뤘다, 8-R′ 정합 정정) · horizon_pitch(이월-3 재목적). 실획(K)이
  최종 판정자라는 것은 그대로다.
- **지시 4-e — 확정 후 지평선 끌기(피치 조작) 미구현**(2026-08-17 4차, D-L60): 옳은 구현은
  수평 소실점·지평선의 동반 이동 + 전부 다시 풀기(승격 규약)인데, 그 재풀이의 배치 손실이
  미측정이다(D-L32의 실패 자리 — 사후 이동은 소실점을 지지선에서 뗀다). 그 전까지 D-L45의
  끌기는 휴면(확정 전 지평선 없음 · 확정 후 잠김). 트리거: 사람이 열거나 실획에서 피치 조작
  요구가 관측될 때.

- **옛 저장본의 가짜 뷰 대기 획은 마이그레이션이 없다**(D-L64의 유보, 재검 [2-8]):
  5차 항목 1 수리 전 저장된 문서에 자유 자세 뷰 소속 대기 획이 있으면 확정 뷰로 안 옮겨진다 —
  그 뷰를 열어 그리면 접근 가능하므로 데이터 손실은 아니다. 트리거: 옛 문서 열기 보고.
- **LINE_PX 회귀를 잡는 판정이 없다**(이월-2 재검 [30]): LINE_PX는 비등재 상수라 STALE이
  못 잡고(D-L62), basic_flow의 판정 임계 넷은 전부 굵기에 둔감하다(60 불변이 그 증거).
  굵기에 반응하는 필드는 `gl_painted_px`(2406→1849 — ⚠ 10차 현행 1385: D-L83으로 lifted가 줄며 함께 움직였다. 이 필드가 굵기 말고도 배치 수에 반응한다는 뜻이라 임계의 재료로 더 약해졌다)인데 임계가 없다 — 값이 잘못 바뀌어도
  게이트가 통과한다. 대역 임계를 걸려면 GL AA·플랫폼 변동을 재야 한다. 트리거: LINE_PX를
  다음에 바꿀 때(그때 전후 대조를 원장 필드로 남기고 대역을 정한다).
- **접이식 막대의 백버퍼 정합(#22) 실측이 원장 밖이다**(이월-1 재검 [16]): 펼침/접힘 전중후
  `stretch=[1,1]`·`view_vs_overlay.same`·자가 치유 카운트 불변은 dev 서버 수동 실측(#25)뿐이다 —
  #22는 이 저장소에서 세 번 재발한 항목이라 재발이 관측 불가로 남는다. 트리거: basic_flow에
  접이식 펼침/접힘 팔을 넣을 때(6-b 모바일 프로젝트와 같은 문이면 함께).
- **손 정렬 자동 스냅(D-L67 hand_deg)의 종단 팔과 화면 이동량 실측이 없다**(2-R′ [B-5]·[B-6]):
  발화(궤도 종료 + 감쇠 정착 + 2° 안 → snapToDir 160ms)는 감쇠 대기 뒤라 결정론이 약해 e2e
  팔을 안 만들었고, 정렬이 일으키는 화면 이동량(기하 추정 수십 px 급)도 원장 밖이다 —
  D-L65가 같은 양(첫 프레임 튐)을 원장으로 잡은 선례(orbit_begin_invariance)가 있다.
  align_eps 변경 시 one_point_direct.json 재실행(2-R′ [B-3] — STALE이 못 잡는 자리).
  트리거: 실기(K) 또는 자동 정렬 관련 보고.
- **eraser_size 원장에 "팔 안 자체 대조가 되살림을 대신한다" 명시가 없다**(8-R′ [4] 낮음의
  이월, 재검 [L1]이 미등재를 잡음): 슬라이더 미배선이면 6px 팔이 실패하는 구조(view_cube와
  같은 판단)인데 그 판단이 원장 밖(#25 — progress에만)이다. 트리거: eraser_size 하네스를
  다음에 재실행할 때 what_this_does_not_say에 넣는다.
- **하단바 마우스 전용 숨김(6-b)의 회귀 방지 없음**(2026-08-17 4차, 리뷰어 [3]): pointer:coarse
  숨김은 CSS 미디어쿼리 한 줄이고 확인은 브라우저 수동(모바일 에뮬레이션 orbit_visible=false —
  progress 산문에만 있다, #25). Playwright의 emulateMedia가 pointer 미디어를 못 바꿔 e2e 잠금이
  바로 안 된다. 트리거: 모바일 프로젝트(deviceScaleFactor·hasTouch)를 e2e에 추가할 때 함께 잠근다.

## 2026-08-18 6차 지시 신규

| 미룬 것 | 왜 | 되살릴 조건 |
|---|---|---|
| **보조 소실점(45° 거리점 · 임의 비율 · 경사 소실점)** — 6차 지시 12가 **등재만** 하라고 명시했다 | 지시 12: "이번 세션에서 구현하지 않는다. 1~11이 끝나고 실사용이 안정된 뒤에 판단한다." 만드는 경로는 셋이다(직접 찍기 · f에서 45° 거리점 계산 · 비율 입력), 각도기 조작은 **두 소실점을 지름으로 하는 반원 위 직각점 = 평면상 카메라 위치**(탈레스, 이론서 7.5)에서 광선을 쏘는 것이고, 경사 소실점은 **수평 소실점 바로 위/아래**의 수직선 위다(이론서 15.1). ⚠ **소실점은 선으로 생기고 보조 소실점은 점으로 만든다** — 만드는 방식이 다르므로 앞서 거리점을 폐기했던 혼동(대각선이 거리점인지 2점 승격인지 기하로 구분 불가, D-L?/지시 2)이 **여기서는 없다** | 항목 1~11이 끝나고 실사용이 안정된 뒤 |
| **`RULE_TOL.concurrent_deg` 스윕** — 동작점 하나로 3°를 박았다(#12) | 근거는 **실획 보고**(Δ0.0~1.7°)이고 그 표본을 이 저장소에서 확인하지 못했다(`sessions/` 비어 있음). 값이 좁으면 진짜 지지선을 버리고, 넓으면 다른 축을 삼킨다 | `sessions/`에 `.brnl`이 들어오면 `real_ink`가 그 분포를 낸다 |
| **세 선이 두 축으로 갈리는 구도의 잔여 모호성** — 합성 120구도에서 3구도가 틀린다 | 대각선 셋과 축 둘이면 2+1 쪼개기가 기하적으로 둘 이상 성립하고, 어느 쪽인지는 **더 그어야** 정해진다. 등록 기준(틀림 0/N)은 **안 낮췄다**(#26) — `confirm_rules.json`의 `vp_rule_three.gate.status`가 미달을 든다 | 넷째 선까지 보는 판(대기를 한 번 더 늘린다)을 재거나, 실획이 이 구도를 실제로 만드는지 본다 |
| **그리드가 그린 선에 안 맞는다** — 방향은 맞고 **원점·간격이 임의**다(`grid.ts`의 `groundGrid`) | 실척이 없으므로 앵커가 없다(계획서 §3.8: 없는 정보를 있는 것처럼 보여 주지 않는다). 6차 지시 1이 이것을 "확정 뷰 자세 누락"으로 읽었으나 원인은 다르다(D-L66) | **항목 4**가 좌표를 연결로 정하면 그때 실제 앵커가 생긴다 |
| **`on_face`가 언제나 이겨 2D 대기 획 경로가 확정 뒤에 안 돈다** | 지면 스냅의 화면 거리가 **정의상 0**이라 다른 후보가 조리개 밖이면 반드시 붙는다 — 그래서 4차 지시 1-b의 2D 폴백(`snap2At`)이 카메라 확정 뒤 **한 번도 안 돈다**. 항목 2의 프로브가 그 사실을 관측 가능하게 만들었다 | **항목 4-5**(on_face 임의 스냅 제거)가 지운다 |

### 6-R 리뷰어 대응에서 새로 등재한 것

| 미룬 것 | 왜 | 되살릴 조건 |
|---|---|---|
| **`beyondSegment`의 위약 팔**(#39) | 그것은 후보를 **거르는** 제약이고 효과(묶음 틀림 15 → 3)를 "소실점은 연장선 위에 있다"(AS-L28)의 몫으로 귀속했다. #39의 나머지 절반은 **같은 크기의 무의미한 배제**를 위약으로 함께 돌리는 것인데 **안 돌렸다** — 위약도 3에 가까워지면 재던 것은 그 성질이 아니라 "후보를 그만큼 줄인 것"이다 | 다음 항목에서 같은 하네스를 다시 열 때 |
| **화각 게이트의 비대칭 동작점** | 대역이 이제 `f/W`라 비대칭(주점이 한 소실점에 붙는 배치)도 담지만, 쓸기는 **주점 정중앙 격자뿐**이다 — 비대칭이 커질 때 대역 경계가 어떻게 움직이는지 안 쟀다 | 실획 표본이 비대칭 구도를 실제로 내는지 본 뒤 |
| **그리드 방향이 맞다는 것의 측정** | 6차 항목 1은 "방향은 확정 카메라의 소실점에서 오므로 맞고 자리·간격만 임의"라 판정했는데, **확정 카메라의 그리드와 사용자 깊이선의 각차를 잰 값이 원장에 없다**(리뷰어 [4]). `vp_two_lines.json`의 `grid.maxSin`은 그 원장이 스스로 "보장 확인(#5)"이라 적은 값이라 근거로 못 쓴다 | 항목 4가 그리드에 실제 앵커를 주면 그때 함께 잰다 |
| **`rule_camera`의 `deg_p90` 포화 여부** ⚠ **닫았다가 되돌렸다**(2026-08-18 8차 2차 지시 항목 0 · 8-2R [6]) | **옛 상태**: 여섯 팔 전부 40.66이고 `deg_median`이 넷에서 정확히 35다(리뷰어 [3]). ⛔ **그 "여섯"이 틀렸다** — `git show d85dca8:stage0/out/rule_camera.json`을 그 자리에서 세니 `deg_p90 = 40.66`인 팔은 **열둘 중 열하나**다(검출만 35). `deg_median = 31.6083`인 팔은 **일곱**이다. | ⚠⚠ **처음에 ✅ 해소로 닫았고 그것이 틀렸다** — 닫는 근거로 든 열한 값(7.3722…10.557)이 전부 **`deg_median`**인데 항목 이름은 **`deg_p90`**이다(#28 — 지표를 사유 없이 바꾸지 않는다. 리뷰어가 잡았다). **갈라 적는다**: ① **`deg_median` 포화는 풀렸다** — 일곱 팔이 31.6083을 공유하던 것이 이제 **열두 팔 전부 다른 값**(6.8811~10.557)이다. ② **`deg_p90` 포화는 안 풀렸다** — 정확히 **35**인 팔이 **열둘 중 여섯**이고(`rule_grouped`·`rule_wide_pair_snap`·`rule_drawn_placebo`·`rule_grouped_placebo`·`rule_grouped_snap_r40`·`detect`) 한 값에 몰린 팔 수는 11 → 6으로 **줄었을 뿐 사라지지 않았다**. 40.66도 안 없어졌다(`rule_wide_pair_snap.deg_max` = 40.66). **p90이 구도 각 상한에 붙어 있다는 의심은 그대로 열려 있다** |
| **중단 조건 게이트가 소실점 규칙을 안 지난다** | `camera_gate.test.ts`는 소실점을 **직접 주고** `recoverCamera`만 부른다 — `resolvePool`·`stepRule`을 안 지난다. 그 원장이 안 움직인 것은 **사정거리 밖**이라는 뜻이지 안전의 증거가 아니다(리뷰어 #41). 이 변경을 실제로 지나는 유일한 하네스는 `rule_camera`이고 거기서 축 오차가 나빠졌다. ⚠ **뒤 문장이 뒤집혔다**(8차 2차 지시 항목 0): 8차 항목 1 이후 `rule_camera`의 축 오차는 **31.6083 → 7.9567로 좋아졌고 그 하네스의 등록 게이트도 통과로 뒤집혔다**. **그래도 이 항목은 안 닫힌다** — 요지는 "축 오차가 나빠졌다"가 아니라 **중단 조건이 규칙 경로를 안 지난다**는 것이고, 그 사실은 그대로다 | 중단 조건을 규칙 경로 위로 옮길지 판단할 때(사람 지시가 필요하다 — 게이트를 바꾸는 것은 CLAUDE §2의 판정 주체를 바꾸는 것이다) |
| **새 규칙의 대조 팔 대가 24/120** | 같은 축 둘이 먼저 오는 구도에서 새 규칙이 24/120 묶음을 틀린다(옛 규칙 0/120). 겨냥 잡음 1.7°에서 세 선 묶기가 그만큼 흔들린다 | 실획 표본으로 실제 잡음 분포를 확인한 뒤 `concurrent_deg`를 다시 잡을 때 |

### 6-R 재검에서 새로 등재한 것

| 미룬 것 | 왜 | 되살릴 조건 |
|---|---|---|
| **겨냥 잡음·시드 스윕**(#12·#14) | `AIM_JITTER_DEG = 1.7°`(실획 보고 구간 0.0~1.7의 **끝**)와 `seed 20260818` **각각 동작점 하나**인데, 그 한 점이 ① 지표 교체의 근거 ② 새 규칙 7/120 ③ **대가 24/120** ④ D-L69의 채택 논거를 전부 낳는다. 잡음 0에서는 같은 하네스가 3/120이므로 **결과가 잡음에 강하게 반응한다**(6-R 재검) | 실획 표본이 실제 잡음 분포를 주면 그 분포로 다시 돌린다. 그전에 0.5°·1.0°·1.7°와 시드 셋을 돌려 **크기 순서가 유지되는지**만이라도 본다 |
| **위치를 구속하는 등록 기준이 없다** | 지표를 위치에서 묶음으로 옮기면서 위치가 게이트에서 빠졌는데 **카메라는 위치에서 나온다**. 새 규칙의 `first_vp_error_px`는 p90 2116 · max 16735 수준이다(6-R 재검) | 위치를 재는 게이트는 **조건수와 얽혀 있다** — 소실점을 **찍기**로 정하는 경로(이월-3에서 2.67°)와 견주는 팔을 만들 때 함께 세운다 |
| **D-L42의 표제 쌍이 현행 원장과 안 맞는다** | "1094/1322(0.8275) → 1270/1322(0.9607)"의 세 수가 지금 `axis_snap.json`에 없다. 현행의 대응 쌍으로 보이는 것은 `silent_denom 1403`에서 1277(0.9102) → 1358(0.9679)이고 **방향은 유지되고 크기가 다르다**(6-R 재검) | D-L42를 다시 열 때. 지금은 `[D-L59 주석]`·`[6차 주석]`이 "이전 실행의 것"이라 덮고 있다 |

## 2026-08-18 6차 지시 12 — 보조 소실점 (구현하지 않는다. 등재만)

지시 12가 명시했다: **"이번 세션에서 구현하지 않는다. 1~11이 끝나고 실사용이 안정된 뒤에 판단한다."**
아래는 지시문이 준 설계를 **잃지 않기 위한** 기록이고 결정이 아니다.

**왜 이제 만들 수 있게 됐나**: 소실점 확정 규칙이 정해졌으므로(D-L69) 경로가 갈린다 —
**소실점은 선으로 생기고 보조 소실점은 점으로 만든다.** 만드는 방식이 다르므로 앞서 거리점을
폐기했던 혼동(대각선이 거리점인지 2점 승격인지 기하로 구분 불가)이 **여기서는 없다**.

**용도**: 45° 거리점(정사각형 작도·깊이 등분) · 임의 비율(1:2 등) · 경사 소실점(지붕·계단 —
이론서 15.1: **지평선이 아니라 대응 수평 소실점 위의 수직선** 위에 있다).

**만드는 경로 셋**
```
a. 직접 찍기      지평선 위 원하는 자리를 탭한다
b. 계산해서 제시   f가 정해지면 45° 거리점은 **주점에서 좌우로 f만큼** 떨어진 지평선 위 점이다.
                 표시하고 누르면 확정 — 가장 자주 쓰이고 위치가 정확히 계산되므로 가장 유용하다
c. 비율 지정      "1:2" 같은 값을 입력하면 위치가 계산된다
```

**각도로 만드는 경로 — 카메라 위치에서 광선을 쏜다.** 두 소실점을 지름으로 하는 반원 위
직각점이 **평면상 카메라 위치**다(탈레스, 이론서 7.5). 주점에서 아래로 f만큼 내려간 점이고
`f² = |PV₁||PV₂|`로 계산된다(⛔ 14차 D-L95 정정: **`− h²`는 넣지 않는다** — 주점 y는 지평선이고 h = 0이다. 옛 D-L68의 그 항은 뒤집혔다). 그 점에서 쏜 광선의 각도가
**곧 세계 각도**다(X축 소실점 방향이 0° · Z축이 90°) — "세계 각도를 화면에서 어떻게
짐작하나"가 여기서 풀린다. 조작: `[소실점 추가]` → 카메라 위치에 각도기 → 광선을 돌린다
(각도 실시간 표시) → 놓으면 광선 ∩ 지평선이 소실점. 각도 입력도 같은 것이다.
**화면 밖 처리**: f가 크면 카메라 위치가 화면 밖이다 — 가장자리 다이얼이나 축소 뷰를 잠깐
띄우고 **계산은 실제 위치로, 광선만 화면에 보이면 된다**(항목 7-e의 가장자리 표식과 같은 자리).

**경사 소실점**: 수평 소실점에서 수직선을 긋고 그 위에 점을 찍는다. 각도기 조작이 같고
**기준선만 바뀐다**(그 수평 소실점을 지나는 수직선을 지평선 대신 쓴다). 카메라 위치에서 그
수평 소실점을 향하는 방향이 0°이고 위아래로 쏘면 경사각이다. **표시를 구분한다** —
지평선 위는 "회전 30°", 경사는 "경사 30°"(다른 양이다). **개수 제한을 두지 않는다**
(계단 둘·지붕이 각각 다르면 소실점도 각각) — 이름이나 번호로 구분한다.

**표시**: 지평선 위의 짧은 수직선 틱(소실점 표식과 구분되고 지평선 위임이 명확하다 — CAD의
참조점 관행). **스냅**: 그 방향이 축 스냅 후보에 들어가되 **축이 아니다** — 그 방향 선은
3D에서 대각선이고 두 축의 조합이다(45°면 X와 Z가 같은 비율). **배치 계산이 축 방향과 다르므로
갈라서 처리한다.** 채널과 무관하다(사용자가 명시적으로 만드는 것이고 그냥 그은 선과 구분된다 —
f 추출을 채널 무관으로 정한 것과 일관된다).

**위계 — 공간 요소는 그린 획과 다른 축이다**
```
공간 요소  지평선 · 소실점 · 경사 소실점 · 보조 소실점 · 그것들을 담는 수직선
           채널 없음(보조선/결과선/주석 어느 것도 아니다) · 3D로 안 올라감 · 내보내기에 안 나감 ·
           지우개로 안 지워짐(소실점을 지우면 함께 사라진다) · 표시는 그리드·지평선과 같은 톤
그린 획    채널 있음(D의 셋) · 상태 있음(항목 4의 방향 미정 / 방향 확정 / 좌표 확정)
```
D의 채널은 **그린 획에만** 붙고 항목 4의 상태도 그린 획에만 해당한다 — 공간 요소는 그 축 밖이며
**카메라와 사용자 지정**이 정한다.

### 2026-08-18 11차 항목 6 — **재확인**(구현하지 않는다. 등재만)

11차 지시 항목 6이 같은 설계를 다시 들었다 — **소실점은 선으로 생기고 보조 소실점은 점으로
만든다 · 각도기 방식(탈레스, 이론서 7.5) · 경사 소실점은 기준선만 수직선 · 위계가 그리는 선과
다르다**. 위 6차 기록이 그 넷을 **전부 담고 있고 고칠 것이 없다.** 다시 안 적고 이 줄로 잇는다.

**그 사이에 저장소에서 달라진 것 넷**(구현할 때 함께 봐야 한다):

| 무엇이 바뀌었나 | 이 설계에 닿는 곳 |
|---|---|
| **세 상태가 생겼다**(D-L84 — 방향 미정 / 방향 확정 / 좌표 확정) | 위 위계 표의 "항목 4의 상태"가 **이제 실재한다**. 공간 요소가 그 축 **밖**이라는 규정은 그대로 유효하다 — `perspectiveOrder`·`standing()`처럼 **계산으로 얻는 상태**이지 저장 필드가 아니라는 점도 같은 방향이다 |
| **화면 팬이 생겼다**(D-L86 — `stage.viewPan`) | "**화면 밖 처리**"(카메라 위치가 화면 밖일 때 가장자리 다이얼)가 **팬과 겹친다**. 이미 등재된 결함(화면 밖 소실점 가장자리 표식의 클리핑이 팬을 모른다)과 **같은 자리**다 — 하나로 고친다 |
| **오스냅 기본 반경이 8px이 됐다**(D-L85) | 위 "**스냅**: 그 방향이 축 스냅 후보에 들어가되 축이 아니다"의 조리개가 그 값이다. ⚠ 그리고 11차 항목 3이 낸 것: **작은 조리개에서 잘못된 연결을 만드는 것은 중점 후보**다(D-L88) — 보조 소실점 방향을 후보에 넣을 때 **어느 종류로 넣는가**가 그만큼 중요해졌다 |
| **찍기 조리개가 오스냅 반경과 한 값이다**(D-L85 대가 ③) | 만드는 경로 `a. 직접 찍기`가 그 조리개를 쓴다 — 반경을 줄인 뒤 `pick_vp`의 찍기 실패가 늘었다는 실측이 있다. 보조 소실점 찍기도 **같은 실패를 물려받는다** |

⚠ **여전히 구현하지 않는다.** 6차 지시 12의 조건("1~11이 끝나고 실사용이 안정된 뒤")이
그대로이고, 실사용 판정자인 실획 표본이 아직 D-L81 이후 저장본이 아니다(항목 4).

## 2026-08-18 7차 신규

- **대기 집합 안에서 "셋이 다 다르다"를 못 가른다**(7차 지시 2-3). 두 수평축이 다 선 뒤의
  셋째 방향은 `stepRule`이 거절하고 알리지만, **소실점이 아직 없는 대기 집합**에서 세 선이
  한 축으로도 두 축으로도 안 갈리는 경우는 `resolvePool` ④가 그냥 확정한다(대조 팔에서
  **24/120**이 그 자리다 — 알림 없음).
  ⚠⚠ **초판은 "그 임계를 정할 측정이 없다"고 적었는데 그것은 미실행을 부재로 처리한 것이었다**
  (7-R 리뷰어 [②-a] · #32). 기존 240구도가 이미 묶음 정오를 라벨링하고 있었고, **쟀다**:
  `confirm_rules.json`의 `far_distribution` — 묶음 맞음 n=113(중앙 1141.3 · min 293.9),
  묶음 틀림 n=3(중앙 204.5 · max 436.8). **두 분포가 겹치므로 `far` 하나로는 못 가른다**.
  그리고 분자가 3이라 임계를 고를 근거로도 못 쓴다(#14). **다른 신호가 필요하다** —
  리뷰어가 제안한 "2+1 쪼개기가 유일한가"(새 임계 없이 `concurrent_deg`만으로)를 다음에 본다.
- **`beyondSegment`가 참 짝을 4/120 떨어뜨린다**(7차 항목 2, `angle_vs_far.true_pair_gate`).
  무르면 AS-L28의 이득(무잡음 틀림 15 → 3)이 되돌아가므로 안 뭅다. 참 짝이 살아남고도
  잘못 고르는 3건과 함께, `resolvePool`의 남은 7/120이 이 둘로 나뉜다. 실획이 판정자다.
- **`order_lock` 팔이 앱의 `placeStroke` 경로를 안 탄다**(7-R 리뷰어 [5]). `liftAll` 일괄 풀이만
  쓰므로 시작점 스냅·축 되쓰기의 배치 효과가 그 원장에 안 잡힌다. 노드에서 그 경로를 돌리려면
  DOM 의존(`cssSize`·`stage`)을 걷어내야 한다. 지금은 `confirm_dir.json`(e2e)이 그 자리를 덮는다.
- **`RULE_TOL.screen_axis_deg`(4°) 스윕이 없다**(7-R 리뷰어 [7]). 입력 쪽 카운터
  (`order_lock.screen_h_operating_point`)는 냈지만 그 임계를 흔들어 차수 분포가 어떻게 바뀌는지는
  안 쟀다 — `CamState.feed`가 `cfg`를 안 받는다. **항목 3이 이 경로 자체를 바꾸므로 그 뒤에 잰다.**
- **P1 잠김의 원인은 여전히 열려 있다**(7차 항목 1·3). `classifyLine`이 화면 수평 4°
  (`RULE_TOL.screen_axis_deg`) 안의 깊이 모서리를 `screen_h`로 읽고, **소실점이 하나도 없을 때는
  묻지 않고 받는다**(`stepRule` 739~745행). 그래서 `first_declaration.screen_h`가 495/600
  (잡음 0 층 120/120)이고 차수가 P1에 갇힌다. **지시 2-1 다섯째 줄이 그 동작을 명시하므로**
  (수평선 → 화면 평행 축 존재 → 1점 확정) 바꾸는 것은 설계 변경이고 지시가 필요하다.
  후보 셋: ① 첫 획이 화면 수평이어도 **묻는다** ② `screen_axis_deg`를 줄인다(스윕 필요) ③
  P1을 가역으로 만든다(D-L53이 명시적으로 배제한 방향). 측정 준비는 돼 있다
  (`order_lock.screen_h_operating_point` — 2°/4°/8°의 입력 분포).
- **3점 스케치를 빈 캔버스에서 시작할 수 없다**(7차 지시 3의 귀결, AS-L33). 2점으로 시작해
  카메라를 기울여야 한다. 건축 투시도에서 3점으로 **처음부터** 그리는 관행이 흔하면 이 결정이
  되돌아온다 — 실획 표본이 판정자다.
- **지시 3-e(커서로 가르는 조작)가 미구현이다**(7차 항목 3, 7-R [4-D]). "물음이 남는 자리는
  모호 판정뿐 — 두 축이 화면에서 겹칠 때. **커서 위치로 가르므로 물음이 아니라 조작이다.**"
  항목 3은 문구만 고쳤고(3-g) 커서 가르기는 안 넣었다. **잇는 자리는 있다** —
  `resolveLive`의 `chooseAxis`가 이미 모호 구간에서 커서에 가까운 축을 고른다(사람 지시 3).
  그것을 **규칙 판정**(`stepRule`의 `screen_or_depth`)에도 쓰면 그 물음이 사라진다.
  ⚠ 그때까지 지시 3-f의 "asked가 0에 가까워진다"는 **미이행 상태의 값**이다(#32).
- **지시 3-a를 문자 그대로 이행하지 않았다**(7차 항목 3, 7-R [4-B]). 차수는 여전히
  `perspectiveOrder(rules)` = f(소실점 집합)이고 **피치의 함수가 아니다.** 귀결: 피치
  15°·25°·8°의 참 카메라를 그린 3점 구도 360실행에서 도구의 차수는 **P3이 0**이다.
  "3점은 시점의 성질"을 자료구조에 옮기려면 차수를 **확정 카메라의 자세**에서 계산해야 하고,
  그러면 뷰마다 차수가 달라진다(기울인 뷰는 3점 · 확정 뷰는 2점) — 그 뜻을 정하는 것이 먼저다.
- **기울인 시점에서 축 스냅이 셋을 고르는지 안 쟀다**(7차 항목 3, 7-R [4-C]).
  `axis_snap_table`의 "3점 확정" 시험은 **앱이 만들 수 없는 상태를 주입한다**(옛 저장본 형태로
  슬롯 2에 유한 소실점을 직접 넣는다) — 표가 상태의 함수임은 확인하지만 지시 3-b의
  "축 후보가 하나 늘 뿐 기전이 같다"의 근거는 아니다(#17·#32·#35).
  앱 경로(2점 확정 → 궤도로 기울임 → 그 시점에서 긋기)로 도달하는 팔이 필요하다.
- **`touch_route` dpr 2의 실패율을 원장에 안 남겼다**(7차 항목 3, 7-R [6-B] · #25).
  같은 코드에서 통과·실패가 갈린다. 지시문이 "환경 밴드다 · 그대로 둔다"고 했으므로 임계는
  안 건드리지만, **반복 실행 카운터가 없어 "간헐"이라는 말이 원장 밖이다.**
- **`camera_ok_rate`가 이 모집단에서 개입을 못 구분한다**(7차 항목 3, 7-R [3-B] · #40 ②).
  `rule_camera`의 `headline_no_identity` 열한 팔(위약 셋 포함)이 전부 **정확히 1**이다.
  그 지표로 다음 개입을 판정하면 아무것도 안 갈린다 — 1pt 구도를 포함한 층(`headline`)이나
  `order_lock`의 P0을 함께 읽어야 한다.
- **"예각 조건(6.5)이 3점 카메라를 막았다"를 세지 않았다**(7차 항목 3, 7-R [3-C] · #7).
  `camera_ok`만 있고 실패 사유별 카운터가 없다. 그 기전은 **가설**로 적었다.
## 2026-08-17 원격 갈래 (실획 첫 표본 대응) — 8차 항목 0에서 병합

⚠ 아래는 `origin/main`의 갈래(PR #1·#2)가 등재한 것이다. 그 갈래는 스스로를 "7차"라 불렀고
이 문서의 7차와 **다른 지시문**이다. 결정 번호도 옮겨 적었다(D-L66~L69 → **D-L76~L79**,
`DECISIONS.md` 8차 항목 0 절).

- **freeIntrinsics(궤도 렌즈 이어받기)의 P3 동작점 실측이 없다**(1-R′ [6]): 이어받는 값은
  확정 해의 주점 그대로라(2점 [W/2,지평선]=16.2+A-4 · 3점 수심=6.3) 분기는 없지만,
  `restore_pose.json`의 동작점은 2점(주점 중심·중심 밖 0.23H)까지다 — 3점(주점=수심,
  중심 밖 x·y 동시)은 안 쟀다. 트리거: P3 실획 표본 또는 승격 관련 다음 작업.
- **핀 상태에서 창 크기가 바뀌면 재핀이 없다**(1-R′ [9] 대응 중 파생 발견): `fit()`이
  `cam.resize`로 카메라(지평선 비례 유지 → 주점·f 갱신)를 다시 푸는데, 핀 투영 훅은
  **pinTo 시점의 옛 주점·f**를 계속 적용한다 — 2D 층(새 해)과 3D 층(옛 내적)이 크기 변경
  뒤 어긋날 수 있다. 복원 경로는 7차가 자가 치유 후 재핀으로 막았고, **런타임 리사이즈**가
  남은 자리다. 트리거: 창 크기 변경/기기 회전 관련 보고 또는 다음 뷰포트 작업
  (수리는 fit() 뒤 stage.isPinned면 cam.ctx()로 re-pinTo — 한 줄이나, 실측 원장과 함께).
- **오스냅 반경(기본 15px)의 적정성 판정**(7차 항목 2 — D-L56 "실획이 최종 판정한다"의
  실행): 판정 계기(snapDistPx)가 죽어 있어 첫 표본은 전부 0이었다. 정의 수리(aimDistPx, D-L79) 후 **다음 실획 표본의 snap_dist_px 분포**가 판정한다 — 그 전에 반경을 임의로
  넓히지 않는다(A-3). 트리거: 다음 .brnl 표본 도착(real_ink 재실행).
- **vpdir 물음 억제의 실획 효과 실측**(7차 항목 2): 합성(rule_camera)에서는 15px 팔 불변·
  r40 팔 asks만 감소라 실사용 효과(물음 6회/획 5가 주는가)는 미관측이다. 트리거: 다음
  .brnl 표본의 askStats(asked/획 비).
- **실획 첫 표본 파일이 저장소 밖이다**(7차 항목 4): 지시문은 관찰 수치를 인용했으나
  `.brnl` 파일 자체는 `sessions/`(작업 트리·origin 전 브랜치)·이슈 어디에도 없다.
  지시 4의 지표는 전부 정비돼 있다(snap_use·vp_dir_err_deg·legacy 0 분리 포함) —
  트리거: 파일을 `sessions/`에 넣고 `npx vitest run test/real_ink.test.ts` 한 번.
  ⚠ 그 표본은 7차 이전 앱의 저장본이라 snapDistPx가 전부 0(옛 정의)이다 —
  `snap_dist_legacy_zero`로 갈려 분포를 오염시키지 않는다(D-L79).
- **항목 3("같은 면 깊이 산포 — 별도 결함 없음")의 재개 트리거**(3·4-R [3]): 그 판정은
  "1·2의 귀결"이라는 인과 사슬(코드 읽기 + 사람 보고) 위에 있고 실측 확인은 다음 표본의
  몫이다. **다음 실획 표본에서 `snap_use.end_snapped`가 여전히 0에 머물거나
  `vertex_gap_ratio`가 안 줄면 항목 3을 다시 연다** — 그때는 별도 결함(면 지정·깊이 상속
  규약)의 후보를 본다. 트리거: 두 번째 표본(7차 수정 후 앱으로 그린 것)의 real_ink.
| **확정 뒤 대기 획에 축 라벨이 안 붙는다**(14차 항목 3 파급 — basic_flow 팔 ① 실측) | 확정 뒤 무연결 획은 D-L83대로 2D 대기하는데, 대기 분기(`onStrokeEnd`의 "2D로 대기")가 그릴 때의 `free` 라벨을 유지한다 — 소실점을 0.04° 차로 향하는 획도 free다. 14차 지시 0-b("스냅 안 걸린 획의 판정은 라벨 — 그은 대로 두고 어느 축인지만 기록한다")는 라벨 기록을 시사하지만, 라벨은 배치 때(`solveInto`의 `cam.axisOf`)만 붙는 것이 현행이고 병합 체크포인트에서 앱 거동을 안 넓혔다(A-3 — 확인된 비용은 대기 획의 축 색 표시 부재뿐) | 대기 획의 축 색 표시가 필요해질 때 — 대기 분기에 `cam.axisOf` 라벨 한 줄(#17 같은 판정자)이면 되고, 좌표는 안 움직인다(지시 0-b 그대로) |
| ⚙️ **절반 해소(15차 항목 8 · D-L101)** — **기운 그린 수직축(3점 구도)에서 작도 복귀 뷰의 그린 지평선이 화면에서 기운다**(14차 항목 6 · 6-R1 [1] — D-L97 한계) | 복귀·큐브 탭(`snapToDir`)의 up이 **세계 Y**라, 그린 수직축이 기운 구도에서는 "피치 0(그린 기준)" 복귀 뷰의 그린 지면 소실선이 화면 수평이 아니다 — 수평 소실점 둘의 y가 어긋나고 한 y의 지평선 표시는 근사가 된다(기운 픽스처의 어긋남은 **원장 밖 실행의 산문 기록**이다 #25 — 그 픽스처 판은 직립 판으로 대체돼 재현 팔이 없다. 직립 픽스처에서는 정합 판정 vps_y_gap<1이 선다 — `draft_return.json@f351839a`의 `returned_overlay` 필드가 정본) | ⚙️ **뒤 절반은 했다**(15차 항목 8 · D-L101): 지평선을 **두 소실점을 잇는 선분**으로 그린다(`horizonThrough`) — 그래서 "한 y의 표시가 근사가 된다"는 더 이상 표시의 문제가 아니다. **남은 것은 앞 절반**: `snapToDir`의 up이 여전히 세계 Y라 복귀 방향 자체가 기운 구도에서 어긋난다. 3점 구도 실사용에서 작도 복귀가 필요해질 때 |
| ⛔ **해소(14차 항목 6 R2 [N1])** ~~**`armOnePointAlign`이 세계 기준계를 쓴다**~~ | 이월 사유("1·2점 확정에서는 두 기준계가 같아 차이가 없다")가 **틀렸다** — 항등은 P1(화면 축 확정)뿐이고(D-L87의 자기 기록), 2점 직립 픽스처 실측에서 그린 축이 세계 축과 요 35° 어긋났다. 보통 상태(2점)가 위험 범위라 미루지 않고 `draftGazeDrawn`·`judgeDraftPose`(큐브·복귀와 같은 단일 출처 #17)로 갈아 끼웠다 | (해소 — 종단 팔 부재는 종전 그대로: basic_flow ws "손 정렬 자동 스냅의 종단 팔은 없다") |
| **확정된 끝점이 기록된 연결점의 화면 자리로 돌아오지 않는다**(15차 항목 1 · D-L98의 남은 몫) | 되맞춤은 연결점을 **대상 획의 대표 직선 위**에 둔다 — 그래서 두 획이 같은 점을 쓰는 것은 정확해도, 그 점의 **화면 자리**는 사용자가 찍은 자리에서 어긋난다(종단 `confirm_link_e2e.json@f351839a`의 `arms[2].rows[].break_px` = 9.79 · 17.78). 원인은 대상 획 자신의 끝점도 `rep`라서다. 전 끝점을 `pts2d`로 되맞추면 사라지지만 그것은 **프리핸드를 버린 대가(§1.1)를 되돌리는 결정**이라 범위 밖이다(A-3: 범위를 안 넓힌다) | §5.3의 "전환 무변화"를 화면 px로 재는 팔이 생길 때 — 그 팔이 이 값을 잴 수 있고, 그때 "모든 끝점 되맞춤"의 대가(축 방향 회전)를 함께 낸다 |
| **선언된 연결의 효과를 실획에서 못 쟀다**(15차 항목 1 · D-L98) | `confirm_link.json@f351839a`가 실획에서 잰 것은 **조건**(`real_ink_rep_offset`)뿐이고 **효과**(선언 제약이 놓인 수를 얼마나 늘리는가)는 합성 사슬에서만 쟀다 — 저장된 실획 셋에 이 사슬에 대응하는 `snap2d` 기록이 거의 없기 때문이다(옛 앱 저장본 · D-L81 이전) | 15차 이후 앱으로 그린 표본이 도착할 때 — 그 표본이면 `declared` 팔을 실좌표로 돌릴 수 있다. 트리거: `sessions/`에 새 파일을 넣고 `npx vitest run test/confirm_link.test.ts` |
| **끝점을 공유한 깊이선 짝의 완화가 `unambiguous`에 갇혀 있다**(15차 항목 1 · D-L99) | 가로축이 **선언되지 않은** 상태(NONE)에서 깊이선 둘이 한 점에서 만나면 여전히 안 선다 — 그 상태에서는 둘이 같은 축인지 다른 축인지 갈리지 않기 때문이다(지시 11-1의 원래 판정). 사용자 보고 ①의 두 획짜리 형태가 여기 남는다 | 실사용에서 "가로선 없이 깊이선만으로 시작한다"가 흔하다는 표본이 올 때 — 후보는 ① 셋째 깊이선을 기다리는 현행 유지 ② 공유점 방사(fan)를 선언으로 읽기(위험: 꼭짓점 별을 소실점으로 읽는다). 판정자는 실획이다 |
| **D-L99의 완화가 잘못된 선언의 결과를 넓힌다**(15차 항목 1 · 1차 리뷰어 [3]) | 선언(`declared` 가로축)이 틀린 상태 — DEFERRED 14차 행의 "얕은 2점 축이 스냅-선언으로 1점에 갇힌다" — 에서 서로 다른 축의 모서리 둘이 꼭짓점을 공유하면 **그 꼭짓점이 소실점으로 선다**(`shared_vertex_vp.test.ts`의 "완화의 대가" 팔이 실측). 옛 문에서는 `null`이었다. D-L3이 `min_reach`로 막은 실패(#23)와 같은 종류이고, **새 종류가 아니라 도달 범위의 확장**이다 | 잘못된 선언 자체가 해소될 때(D-L96의 물음 — 얕은 축의 스냅-선언 판정) 이 행도 함께 닫힌다. 그전까지의 판정자는 실획이다: 실사용에서 "닫힌 삼각형·꼭짓점 방사로 공간을 세우는" 손짓과 "선언이 틀린 채 모서리를 그리는" 손짓 중 어느 쪽이 흔한가 |
| **사용자 보고 그대로의 조건("2점 + 축 스냅 깊이선")에서 경고가 재현되지 않았다**(15차 항목 2 · 2차 리뷰어 [1]) | `axis_warn.json@f351839a`의 차수 둘 × ①② 팔이 전부 `misfit_min` **0** · `warned` false다(판정 임계 `vp_dist_ratio`는 **0.06**이고, 같은 실행에서 진짜 어긋난 획은 `genuinely_off_misfit` **0.25**다 — 축 스냅 획은 임계의 반대편 끝이다). 재현된 것은 **양 끝 스냅으로 놓인 획**이고, 그 획은 그리는 동안 축 색 미리보기를 보이다가 끝점이 붙는 순간 경로가 바뀐다 — 사용자가 "축 스냅"으로 읽었을 자리다. **그 동일시는 해석이다** | 사용자가 그 순간의 저장본(`.brnl`)을 주면 그 자리에서 판정된다 — 그 획의 `snapStart`/`snapEnd`가 있으면 양 끝 스냅이고, 없는데 축 라벨이 있으면 다른 기전이다. 트리거: `sessions/`에 파일을 넣고 `axis_warn` 팔을 그 기하로 다시 낸다 |
| **원장의 `pitfall_citations` 배열을 아무도 안 읽는다**(15차 항목 1·2 · 2차 리뷰어 [7][8] · #18) | 1차 리뷰어 [7] 대응으로 원장에 손 목록 필드를 넣었는데 `selfcheck.py`의 `scan_pitfall_citations`는 **원장 산문의 `#N`만** 긁는다 — 같은 이름의 두 양이 생겼고 값이 다르다(예: `confirm_link.json` 산문 [5,12,49] ↔ 필드 열셋) | selfcheck에 필드 우선 읽기를 넣거나 필드를 없애는 것 중 하나 — 둘 다 `selfcheck.py`를 건드리는 일이라 이 항목의 범위 밖이다. 그전까지 **완료 대조의 정본은 selfcheck의 표**이고 문서가 둘을 나란히 적는다 |
| **획으로 3점 카메라를 만들 수 없다**(15차 항목 3·8 · D-L102가 안 고친 자리) | 규칙에 P2 → P3 전이가 없다(7차 지시 3-d로 삭제 — "3점은 카메라를 기울인 시점의 성질이지 획이 만드는 전이가 아니다"). 그런데 CLAUDE.md §1은 여전히 **"남는 승격은 P2 → P3(기울어진 세로 모서리의 수직축 답) 하나다"**(D-L53)라고 적는다 — 문서와 코드가 어긋나 있다. 실측: 빈 화면에서 3점 상자를 그리면 기울어진 세로 모서리들이 깊이선으로 읽혀 **엉뚱한 수평 소실점**이 서고 지평선이 화면 밖으로 뛴다(`view_axes.json@f351839a`의 `stroke_built`) | 어느 쪽이 현행인지 사람이 정할 때. ① 7차 결정을 유지하면 CLAUDE.md §1의 그 줄을 고친다 ② D-L53을 되살리면 기울어진 세로선의 "수직축인가" 물음(불가역 P1 가드와 같은 계열)을 다시 만든다. **재현·판정 재료는 `stroke_built`가 이미 든다** |
| **격자의 지평선은 아직 스칼라 y다**(15차 항목 8 · D-L101) | 표시용 지평선은 두 수평 소실점을 잇는 **선분**이 됐는데(`horizonThrough`) `gridGuides`는 여전히 `horizonY`(스칼라)를 받는다 — 롤이 있는 시점에서 두 규약이 갈린다 | 격자가 롤 있는 시점에서 눈에 띄게 어긋난다는 관측이 올 때. `GridCam`에 선분을 넘기는 개정이고 `grid.ts`의 계산 전부가 그 y를 쓰므로 이 항목의 범위 밖이다(A-3) |
| **지시 3-c의 "소실점이 극단인 경우"를 안 좁혔다**(15차 항목 3 · D-L102) | 모델링을 "기준계 없음 + **소실점 극단**"으로 좁히라는 지시의 뒤 절반이다. 안 한 이유: ① 그 대역의 비용을 재는 팔이 없고 ② **실측한 자세 다섯 전부에서 그리기가 정상**이라(`view_axes.json@f351839a`의 `blocked_now` 전부 거짓) 어디부터 막아야 하는지의 문턱이 안 선다. ⚠ **"극단"은 화면 밖으로 멀리 나간 소실점을 뜻한다**(2차 리뷰어 [1] — 초판은 이것을 주점에 가까운 소실점으로 읽었고 그 읽기는 틀렸다). 실제 위험(시선이 축과 나란할 때 면 위 사선이 그 축에 오배정 — `view_plane.json`이 궤도 45°에서 79%를 실측)은 `angle_max_deg`(현재 0 = 꺼짐)의 자리다 | 그 오배정이 실사용에서 문제로 보고될 때, 또는 `angle_max_deg`를 켜는 결정이 설 때. **먼저 재야 하는 것**: 소실점이 **화면 밖으로 나간 거리**(화면 대각 대비 `dist`)별 오배정률 — 그 곡선이 "극단"의 문턱을 정한다. 주점 거리가 아니다 |
| **호버 잠금의 실기 확인이 없다**(15차 항목 4 · D-L103) | 합성 `PointerEvent`(`pointerType: "pen"` · 호버 중 `buttons: 0`·`pressure: 0`)로 잰다 — 애플펜슬의 실제 호버 표본(내려오는 동안의 좌표 흐름·마지막 호버와 착지의 실제 간격 분포)이 없다. 잠금 문턱(`PICK_TOL.radius_ratio` 재사용 — px 값은 `hover_lock.json@f351839a`의 `setup.lock_radius_px`가 든다, #47)이 그 분포에 맞는지는 **안 쟀다**(#46: 값이 픽스처가 아니라 상수에서 온다) | 아이패드 표본이 올 때 — `hover_lock.json`의 `land_gap_px` 자리에 실측 분포를 넣고 문턱을 다시 고른다 |
| **잠금이 가로챈 손짓의 비율을 못 쟀다**(15차 항목 4 · AS-L51) | "표식을 보고도 다른 곳에 대려 한" 손짓을 잠금이 가로채는 몫이다. 합성 팔로는 만들 수 없다(의도가 재료라서) — `stale`(21px 밖)이 그 갈래를 받지만 21px 안의 의도적 오조준은 안 갈린다 | 실획에서 "잠금이 붙인 획 중 무른 것"의 비율이 나올 때. 트리거: `sessions/`의 15차 이후 표본 + 무르기 기록 |
| **잠금 문턱의 «옳은 자리»를 안 쟀다**(15차 항목 4 · 1차 리뷰어 [9] · AS-L51) | 문턱이 상수 자리에 실제로 있다는 것은 쟀다(`summary.threshold_band_px`가 `setup.lock_radius_px`를 감싼다). **그 자리가 옳은가**는 안 쟀다 — 사람이 펜을 내리는 동안 실제로 미끄러지는 거리의 분포가 없기 때문이다 | 아이패드 표본이 올 때(위 행과 같은 트리거) — `land_gap_px`의 실측 분포가 오면 문턱을 그 분위수로 다시 고른다 |
| **호버 잠금의 2D 갈래가 동작점 하나뿐이다**(15차 항목 4 · #12) | `hover_lock.json`의 ⑤(카메라 전 · 조리개 밖 착지)만 든다 — 어긋남 곡선(0·6.7·12.5·낡음)은 3D 갈래만 돈다. 2D 판(`resolve2dCore`의 `pinStart`)은 단위 팔(`hover_pin.test.ts` 다섯)이 더 덮지만 그것은 **앱 배선이 아니다** | 2D 대기 상태가 실사용에서 오래 지속된다는 표본이 올 때 — 그때 ⑤를 네 동작점으로 편다 |
| **`snapDistPx`의 뜻이 호버 잠금으로 갈렸다**(15차 항목 4 · D-L103 파급 · #49) | `snapDistPx`는 **스냅 전 원시 시작점**에서 잰 겨냥 거리이고(7차 항목 2), "조리개를 넓혀야 하는가"의 재료였다 — 붙은 획은 그 값이 조리개 이하라는 전제가 있었다. 잠금 뒤에는 **붙었는데 조리개보다 큰** 획이 생긴다(`hover_lock.json@f351839a`의 ③: 12.5px에 붙는다). 분포를 그대로 읽으면 "조리개를 넓혀도 된다"로 잘못 읽힌다 | 실획 표본으로 반경을 다시 판정할 때 — 그전에 `snapDistPx` 옆에 **어느 경로로 붙었는가**(잠금/착지 질의)를 남기는 필드가 필요하다. 이 항목의 범위 밖이다(A-3: 저장 규약을 안 넓힌다) |
