# DEFERRED.md — 우회·단순화·미룬 항목

A-2(막히면 멈추지 말고 우회) A-3(가장 단순한 것을 고른다)에 따라 **의도적으로 줄인 것**을
전부 여기 적는다. 완주가 목적이고 완성도는 그 다음이다. 각 항목은 원래 목표와
지금 대신 하고 있는 것을 함께 적는다.

---

## W-A

| 항목 | 원래 목표 | 지금 | 사유 |
|---|---|---|---|
| 렌더 계층 재작성 | §2.1 "Three.js 씬 + sceneDiff 유지" | 삭제 후 W-4에서 재작성 | 압출 전용 자료구조에 묶여 있었다(DECISIONS D-A3) |
| 발화 입력 | plan §3.3 발화 앵커 | 범위 밖 | 카메라가 실척을 주므로 앵커의 역할이 줄었다. 코드 제거(stage2) |
| 다중 뷰 정합 | 프래그먼트 pose graph | 폐기 | 돌려서 계속 그리면 같은 세계(§2.3) |
| Rhino 출력 | stage1/rhino_out, stage3/rhino_view | 삭제 | 압출 IR 전용이었다. 필요해지면 Edge/Face에서 새로 |
| 세션 내보내기 | 지시 4.4 iPad 세션 수집 | 삭제(sessionExport.ts) | IR 의존. W-8 이후 실획 수집이 필요하면 새 자료구조로 다시 |

## W-0

| 항목 | 원래 목표 | 지금 | 사유 |
|---|---|---|---|
| Web Worker + 증분 | §2.1 유지 자산 | `worker/{incremental,parserWorker}.ts` 삭제 | IR 증분 파서였다(`parse.ts`·`normalize.ts` 의존). 워커 골격은 W-3/W-4에서 새 자료구조로 다시 세운다. 검증된 것은 **"증분 결과 == 전체 재계산"이라는 하드게이트 형태**이고 그것을 다시 쓴다 |
| 잉크 캡처 회귀 테스트 | V-5 검증(팜 리젝션·6튜플·coalesced) | **테스트 0** | 그 테스트들이 폐기 모듈과 같은 파일에 있었다. `inkCanvas.ts`는 DOM이 필요해 vitest 기본 환경에서 못 돈다 → **W-8 Playwright에서 덮는다**. 그때까지 유지 자산이 무검증 상태임을 기록한다 |
| 얇은 벽(길이의 5% 미만) | 나란한 두 줄을 각각 엣지로 | 덧그림으로 접힌다 | `OVERTRACE_TOL.dist_ratio=0.05`가 벽 두께와 덧그림을 가르는 띠다. 더 얇은 벽은 한 줄이 된다. W-2 스냅에서 "평행 오프셋" 의도를 따로 받으면 풀린다 |
| 거친 잉크의 덧그림 검출 | 전 등급 병합 | coarse 재현율 **0.69** | 거칠수록 되그은 선이 원선에서 멀어진다. 임계를 늘리면 벽 두께를 접기 시작한다 — 맞바꿈이라 지금은 놓치는 쪽을 택한다(놓치면 엣지가 둘이 되고, 잘못 접으면 벽이 사라진다) |

## W-0 2차 (리뷰어 잔여)

| 항목 | 원래 목표 | 지금 | 사유 |
|---|---|---|---|
| `straight_tol` 근거 산출물 | 판단 근거를 원장(JSON)에 | DECISIONS D-03 서술만 | 인용한 σ=0.031은 `archive_pre_W/ink_noise_model.json`의 `grades.precise.lf_bow`로 원장에 있다. 그러나 "0.05면 11% 탈락"이라는 **판단 자체를 재는 산출물**은 없다. W-2에서 제약 재적합을 잴 때 함께 낸다 |
| V-A(애플펜슬 대표성) 검증 경로 | 지시 4.4 세션 JSON 수집 | **경로가 삭제됨** | W-A에서 `sessionExport.ts`와 `stage0/10~14`를 지웠다. D-01의 유일한 되돌림 장치("실제 도구로 그린 획이 생기면 다시 잰다")가 지금 수집 수단을 갖고 있지 않다 → **W-8에서 최소 수집기를 다시 만들고 optimal_rate를 재측정한다** |
| 최소분할 DP의 정확성 | 엄밀 최소 | 근사(연속 초과 12회에서 중단) | O(n³)이라 실획 수백 장에 못 쓴다. 근사는 **한쪽으로만** 틀린다 — 최소분할을 과대평가할 수 있고 그러면 under_fit이 늘어 **검출기에 불리**하다 |

## W-2

| 항목 | 원래 목표 | 지금 | 사유 |
|---|---|---|---|
| 제약 재적합 | §4.1 "자유도 2→1, 손떨림 제거" | **기본 꺼짐. 분류만 쓴다** | 측정이 전제를 반증했다(코너 오차 중앙 0.4~2% 감소, p90 악화). 계획서 A-2 단계 2가 예상한 우회로다. 코드는 남기고 `{refit:true}`로 켤 수 있다 |
| 코너를 공유하는 선의 **동시** 해 | — | W-3으로 | 코너 오차의 지배항은 방향이 아니라 위치다. 선을 따로 고치면 못 준다. 정점을 명시적으로 만들고 거기에 선들을 붙이는 것이 W-3 앵커 체인이므로 그때 자연히 풀린다 |
| 비직교 회수율(W-5) 정량 | §1.1 "면 위 사선으로 상당 부분 회수" | 미측정 | 회수 여부는 **방향**이 아니라 **배치**로 정해진다 — 면 위 사선은 방향이 `free`지만 양 끝점이 기존 기하에 닿으면 배치된다. 그 판정이 W-3이므로 수치도 W-3에서 낸다 |
| 애매성 판정 | 비율 기준(1.4배) | 그대로 | 부적합도가 0 근처면 비율 기준이 무력해진다(짧은 선이 틀린 VP에도 잘 맞는다). 노이즈 스케일 기준으로 바꾸는 것이 옳으나 재적합을 끈 지금은 영향이 없다 — 재적합을 되살릴 때 함께 고친다 |

## W-1·W-0 리뷰어 2차 잔여 (**해소되지 않음 — W-3에서 처리**)

2회 대응 규칙에 따라 옮기되, **전부 유효한 지적이며 무시하지 않는다.**

| 항목 | 무엇이 문제인가 | W-3에서 할 것 |
|---|---|---|
| 게이트가 사실상 발동하지 않는다 | 격자 12개 중 0.5 미만이 양 극단 2개뿐. 구 지표는 항상 발동, 신 지표는 거의 발동 불가 — 척도가 다른데 같은 숫자를 썼다 | 발동 조건이 실재하는지 제시하거나, 임계를 **측정 기반으로** 다시 정한다 |
| 전제와 구현 품질이 섞여 있다 | optimal_rate는 검출기와 DP가 **같은 tol로** 얼마나 일치하는가다. 낮을 때 원인이 구분되지 않는다 | tol을 달리한 두 기준선을 비교해 성분을 분리한다 |
| 개수만 비교해 상쇄된다 | 없는 코너 1 + 놓친 코너 1 = optimal로 계수 | **위치 오차 기반 지표**를 병기(합성 정답에는 코너 위치가 있다) |
| 곡선 계수가 어긋난다 | 획 분류 곡선 28% vs out_of_scope 6~7% | optimal에서 곡선 획을 제외한 값을 낸다 |
| `straight_tol`만 스윕 안 됨 | 게이트 값에 직접 곱해지는 인자인데 한 점뿐 | 0.05/0.10/0.15에서 optimal_rate |
| DP 근사의 양방향 오차 | 과대평가는 **과분할한 검출기를 optimal로 위장**시키기도 한다("한쪽으로만 틀린다"는 반쪽 주장) | 소표본에서 엄밀 DP와 대조 |
| selfcheck가 "정확히 1.0"을 못 잡는다 | `exact = 1`(홀드아웃 n=200)이 플래그되지 않았다 — 키 이름 규칙(`*_rate`)에 걸려 놓친다 | 규칙을 값 기준으로 넓힌다 |
| W-1이 원장 산출물 0 | 카메라 수치가 전부 `stage0/out/` 밖 | W-3에서 카메라 측정도 원장에 낸다 |
| W-y 미측정 | 승격만 하고 재지 않았다. f와 화각 경고 전부가 이 가정 위에 있다 | W-6(궤도 회전) 이후 측정 |
| §3.2 게이지가 1점 전용 | 2·3점에서 폭이 f를 따라 움직이는지 미확인 | W-3 배치와 함께 잰다 |

## S-3

| 항목 | 원래 목표 | 지금 | 사유 |
|---|---|---|---|
| `place.ts` 신설 | HANDOFF가 새 파일을 예고 | `s3d/stroke.ts` 하나로 | 자료구조와 배치가 같은 것을 다룬다. 파일을 늘리지 않는다(A-3) |
| 접합 반경을 획 길이 기준으로 | `join_ratio`가 화면 대각 대비 | 그대로 | 끝점 오차가 커지면 접합이 반경 밖으로 나가 **미배치**가 된다(2%에서 23%). 반경을 키우면 엉뚱한 모서리에 붙기 시작하므로 맞바꿈이다. 실획 끝점 오차를 아는 S-10 이후에 정한다 |
| 떠 있는 획의 사용자 지정 앵커 | §4.2 "사용자 지정을 요구한다" | 미배치로 두고 화면에 알리기만 | 축 지정 UI는 넣었으나 **앵커 지정 UI는 없다**. 지금은 "기존 획에 닿게 그으면 놓입니다"로 안내한다. S-7(편집)에서 붙인다 |
| 축 밖 획(`free`)의 배치 | §4.3 면 위 사선·경사 VP | 배치하지 않는다 | S-6이 하는 일이다. 지금은 미분류로 두고 사용자가 축을 고르면 놓인다 |
| 미리보기 실시간 표시 | §4.5 "그리는 중 임시 평면에 놓고 미리보기" | `previewStroke`는 있으나 **화면에 안 쓴다** | 그리는 중에는 2D 잉크가 그대로 보이고, 3D는 획 종료 시 갱신된다. 두 평면의 화면 이미지가 같은 것은 **그린 카메라에서만**이고, 3D 뷰는 정의상 돌려 보는 화면이라 거기서는 다르다(회전 후 차이 중앙 0.29 획 길이). 그래도 미루는 이유는 **그리는 중에는 사용자가 2D 캔버스를 보고 있다**는 것뿐이다 — S-4에서 지연과 함께 다시 판단 |
| 획 끝점 ↔ 다른 획 끝점 거리 실측 | AS-8 스윕의 실획 기준점 | `closure_gap`(다른 양)으로 자릿수만 감각 | closure_gap은 *자기가 방금 그은 시작점*으로 돌아오는 오차이고 필요한 것은 *다른 획이 만든 모서리*를 겨냥하는 오차다. 상한이라는 근거가 없다. Quick,Draw 다획 그림에서 직접 재야 하며 **S-10 실획 수집과 함께** 한다 |
| 미배치율 임계 20%의 근거 | D-S7 중단 조건 | **사후에 정한 값**(그렇게 표시함) | 사전에 정할 수 없었던 이유는 원래 지정한 양이 항등이라 이 양의 존재를 몰랐기 때문이다. 지금은 "발동 가능한 조합이 있다"만 보인다. S-10에서 실획 끝점 오차를 재고 재정한다 |
| `sight` 평면 유지 | §4.4 비교 대상 | 코드에 남기되 기본 아님 | 측정 근거를 재현할 수 있어야 한다(D-S8). `WOBBLE_PLANES`로 셋 다 돌 수 있다 |

## S-3 리뷰어 2차 잔여 (2회 대응 규칙 — **전부 유효한 지적이며 무시하지 않는다**)

| 항목 | 무엇이 문제인가 | 어디서 처리하는가 |
|---|---|---|
| **구도가 하나뿐** | 전 결론이 요 35°·피치 15° 3점 카메라 하나, 직육면체 하나에서 나왔다. 그런데 S-2와 S-3의 오배정률 차이를 "구도가 다르다"로 설명한다 — 구도가 결과를 바꾼다고 말하면서 구도는 하나만 쟀다. 원장 `single_composition_warning`에 명시했다 | **S-5**(회전 후 이어 그리기)가 자연히 여러 구도를 만든다. 거기서 2점·정면 근접·넓은 화각을 함께 잰다 |
| `ray_axis_deg`가 위험 쪽 꼬리를 못 만진다 | AS-8이 지목한 증폭 항인데 표본의 최솟값이 27.0°다(위험은 0°에 가까울 때). 꼬리 통계(min·p10)는 넣었으나 **그 구간의 표본이 없다** — `bend_max`와 같은 종류의 한계 | 시선과 거의 나란한 축이 생기는 구도(소실점 정면)를 픽스처에 넣어야 한다. S-5 |
| 선분 보간 대 최근접 표본점 대조군 없음 | "≈4px가 앵커 오차가 된다"는 **추정**이었다 | **S-4에서 해소 — 추정이 반증됐다.** 깊이 오차 0.0116 대 0.0112로 차이가 없다(배치율만 0.886 대 0.869). 보간은 유지하되 근거는 "손해가 없다"까지다 |
| 두 원장의 같은 조건 수치가 미세하게 다르다 | `wobble_plane.judged_end_jitter.facing`(미분류 0.1972 / 미배치 0.2333)과 `preview_gap.jitter_0.02`(0.1764 / 0.2472)는 조건 표기가 같은데 시드가 달라 표본이 다르다. 두 자리까지는 같으므로 V-H 규칙 안이지만, 문서에서 인용할 때 어느 원장인지 병기해야 한다 | 조건 블록(`conditions`·`ink`)은 넣었다. **시드까지 통일하는 것**은 측정 재실행이 필요하므로 다음 측정 때 |
| `f_bias` 블록의 잉크 조건이 서술에만 있었다 | 조건 필드를 넣었으나, `f_bias_0`(깊이 0.0132)과 같은 조건이어야 할 `jitter_sweep_facing.jitter_0.01`(0.0116)이 다르다 — 시드가 다르다 | 위와 같다 |
| 등급 순서가 S-2와 반대인 이유 | `by_grade`에서 precise가 가장 나쁘다(판정된 획만으로도 0.139 / 0.132 / 0.123). 분모 효과만은 아니다 | **S-4에서 해소** — 끝점 오차를 0으로 두면 순서가 S-2로 돌아온다(0.054 / 0.105 / 0.110). 끝점 겨냥 오차가 등급 차이를 덮은 것이다 |

## S-2b

| 항목 | 원래 목표 | 지금 | 사유 |
|---|---|---|---|
| **(c) 획 간 일관성 재평가** | §S-2b c — 이웃의 국소 편향을 빼고 미판정 획을 구제 | **꺼 둔다**(D-S9). 코드는 `s3d/consensus.ts`에 그대로 | **기전 기각이 아니라 표본 부족이다.** 상자 하나에 축당 4획뿐이라 `min_support: 3`이 사실상 그 축 전체이고, 국소 편향이 자기 자신과 거의 같은 것을 본다. 그 조건에서 구제 29건 중 8건이 틀렸다(28%). **여러 볼륨이 있는 실획에서 재평가한다** — 축당 획이 많아지면 이웃 평균이 자기와 분리된다. 재평가 입력은 `sessions/`이고 측정은 `real_ink.json`에 붙인다. 판정선은 그대로: **구제 오류율이 기저 오류율(3~7%)을 넘으면 켜지 않는다** |
| (d) 왜곡 모델 추정 | §S-2b d — 전역 왜곡 모델 | 하지 않았다 | 지시가 "시간을 많이 쓰지 말 것"이라 했고 a~c로 충분한지가 먼저였다. c의 국소 편향장이 d의 가장 단순한 형태이며 그것이 28% 틀렸으므로, 전역 모델은 근거가 더 필요하다. 실획에서 c가 서면 그때 |
| 왜곡 세기·형태의 실측 | AS-15 | 대용 모델(성질만 맞춤) | `real_ink.json`의 `misfit_by_field_angle`이 그 자리다. 실획이 들어오면 채워진다 |

## S-2b·S-4 리뷰어 2차 잔여 (기록·재현성. 결론을 바꾸지 않는다)

| 항목 | 무엇이 문제인가 | 어디서 |
|---|---|---|
| 원장 조건 블록에 **판정 설정이 없다** | `wobble_plane`·`preview_gap`의 `conditions`에 a·b·c 설정이 없다. S-2b의 b가 켜지면서 두 원장이 바뀌었는데 **원장만 보고는 어느 판(版)인지 못 가린다** — 문서가 뒤처진 것을 늦게 안 이유이기도 하다 | S-5 |
| `join_radius_sweep`에 n·seed 없음 | 같은 조건 표기의 다른 블록과 값이 다른데(assigned 0.900 대 0.922) 표본을 가릴 수 없다 | S-5 |
| `build_failed = 0`이 selfcheck에 안 걸린다 | 키 이름 규칙(`n_`·`count`)을 비껴간다. DEFERRED에 있던 같은 유형을 S-3에서 한 번 넓혔는데 또 나왔다 — **규칙을 값 기준으로 더 넓힐지 판단이 필요하다**(넓히면 345건으로 터진 전례가 있다) | S-5 |
| 화각 표기 60.5 대 60.7 | `systematic_warp.json`의 `scenes`와 `key_finding`이 다르다. 그리고 `wobble_plane`에는 f·화각이 없어 "S-3 구도가 60.7°"를 원장으로 확인할 수 없다 | S-5 |
| selfcheck 분류 문장 부정확 | "신규는 전부 참 축 조건 정의상 0"이라 적었는데 `by_grade_no_end_jitter` 7건은 판정을 쓴다(끝점 오차 0에서의 **실측** 0이다) | S-5 |
| 좁은 구도의 `apparent_vp_ratio = 1` 해석 | "λ(t)=1이라 정의상"이라 적었으나 정확히는 "표본이 18.4 경계(tan 0.577) 밖으로 나가지 않았다"이다. 좁은 구도 `misfit_by_field_angle`에 tan 0.50~0.75 구간이 아예 없는 것이 근거 | S-5 |

## S-5

| 항목 | 원래 목표 | 지금 | 사유 |
|---|---|---|---|
| **혼합 시점 재투영** | §5 "카메라 재조정 시 전체 재투영" | 돌린 시점에서 그리면 **카메라를 잠근다**(D-S11) | 각 획의 `pts2d`가 어느 시점 것인지 알아야 하는데 자료구조를 늘리지 않기로 했다. 잠그면 그 상황이 안 생긴다. 필요해지면 그때 늘린다 |
| **가림 접합의 근본 해법** | 앵커를 깊이까지 맞게 고르기 | **S-6에서 대부분 해소** — 정합성 검사 + 깊이 타당성으로 p90 0.72~0.93 → 0.05~0.075 | **남는 것**: 두 검사는 **내부 정합성**을 강제할 뿐 참값 일치를 보장하지 않는다. 획이 **엉뚱한 두 모서리에 일관되게** 붙으면 통과한다(최댓값 0.88~1.06 잔존). 자동으로 더 풀 근거가 없다 — **남은 길은 사용자에게 보이고 고르게 하는 것**(S-7 편집) 또는 가림이 심한 시점에서 이어 그리지 않도록 안내하는 것 |
| 돌린 시점의 투시 가이드 | §3.6 격자·부챗살 | 3D 뷰에는 안 깐다 | 새 소실점을 계산하고 있으므로 그릴 수 있다. 다만 3D 씬 위에 2D 가이드를 겹치는 것이 읽히는지 미확인. **S-7(편집)에서 화면을 손볼 때** 함께 |
| `view_depth_guard` 0.05 | 측정 기반 값 | **잠정** | 0.05와 0.15가 같은 결과, 0.30은 무력. 띠 안이면 되고 정확한 값은 실획에서(AS-13) |

## S-6 (2b)

| 항목 | 원래 목표 | 지금 | 사유 |
|---|---|---|---|
| **축 오배정 되돌리기** | 정합성 검사 실패를 "이 축이 아니다"의 신호로 써서 오배정 사선을 회수 | **꺼 둔다**(D-S16). 코드는 `stroke.placeInto`의 `retryAsFace`, 상수는 `PLACE_TOL.retry_as_face = 0` | **되돌리기는 의도한 대상에서 정확했다** — 되돌린 사선은 3~6%만 틀린다. 막는 것은 **겨냥할 수단이 없다**는 것이다: 같은 신호가 정상 모서리를 3~4배 더 끌어들이고 그쪽이 30~62% 틀린다. 유리한 동작점에서는 시드 변동폭이 차이보다 커서 갈리지 않고, 실획에 가장 가까운 동작점(끝점 오차 0.02)에서는 전체 오류율 0.553이다. **판정선은 그대로**: 되돌림 오류율이 기저와 갈리지 않으면 켜지 않는다 |
| **판정 여유를 되돌리기 신호로** | 위 항목의 겨냥 수단 | 하지 않았다 | 오배정 사선은 판정이 **아슬아슬하게** 통과한 것이고 정상 모서리는 여유 있게 통과한 것일 가능성이 있다. `classifyStroke`가 이미 부적합도를 갖고 있으므로 되돌리기 조건에 넣으면 갈릴 수 있다. **다만 새 기전이고 새 임계다**(A-3). 실획 이후 |
| **되돌리기의 부작용** | — | 측정만 남긴다 | retry는 check가 놓던 획을 **잃기도 한다**(끝점 오차 0.01에서 11획, 그중 절단 0.2 기준 틀린 것 0·절단 0.1 기준 2획). 되돌린 획이 이후 앵커 후보 상태를 바꾸기 때문이다. 되돌리기를 켤 일이 생기면 이 순서 의존을 먼저 본다 — 원장 `vs.check.dropped`·`gained` |
| **첫 시점 배치율 0.82** | 검사를 켠 대가 | S-7이 받는다 | 검사가 떨어뜨리는 340획 중 **53%가 이미 틀려 있던 것**이다(절단 0.2. 절단 0.5로는 40%). 나머지 절반은 옳게 놓였던 배치도 함께 버리는 것이고, 그 대가가 미배치다. 나머지는 미배치로 남고 **사용자가 지정한다**(D-S15). S-7 전까지는 "그린 것의 다섯 중 하나가 3D에 없다"가 그대로 보인다 |
| **`preview_gap.json` 재측정** | D-S7 ②의 출하 값 | `misassign_retry.json`이 대신 낸다 | `preview_gap`은 검사를 끈 구성으로 돌았고 그 값(끝점 오차 0.01에서 0.0016)은 **더 이상 앱을 설명하지 않는다**. 출하 값은 0.153이다. 하네스를 고쳐 두 구성을 다 내게 할지, 인용만 갱신할지는 S-7에서 화면을 손볼 때 함께 |

## S-6 (3)

| 항목 | 원래 목표 | 지금 | 사유 |
|---|---|---|---|
| **경사 소실점** | §4.3 둘째 — 경사 VP를 만들어 지붕·계단을 축으로 다룬다 | **만들지 않는다**(D-S17) | 후보가 **점이 아니라 직선**이라 획 하나로는 못 가르고, 획 2·3·4·6의 최소제곱으로 묶으면 산포는 줄지만 **수평 VP 쪽으로 편향된 점에 수렴**한다(참 경사 VP 쪽에 서는 비율 획 6에서 1~4%). 원인은 AS-14 — 계통 왜곡이 획 간 공유라 평균이 못 지운다. **다시 볼 조건**: ① **사용자가 경사 VP를 직접 찍는다**(가장 단순하다. §3의 소실점 찍기를 그대로 쓰고 S-7의 사용자 지정에 얹을 수 있다) ② 이론서 15.4(계단의 등간격 반복)·15.5(박공의 대칭·용마루 검산)처럼 **추정이 아니라 구속**을 주는 구조를 쓴다 — 이번에 잰 것은 자유 추정뿐이다 |
| **자유 곡선 폴백** | §4.3 셋째 — 임시 평면에 놓고 나중에 조정 | **켰다, 곧은 획에만**(D-S17) | 굽은 획에서는 이 평면도 51%가 틀린다. 굽은 획으로 넓히려면 **가운데를 검증할 수단**이 필요하다 |
| **굽은 획 전체** | §4.3이 곡선을 1급으로 다룬다 | **평면 경로 둘 다에서 제외**(D-S17) | 끝점 검사가 굽음을 못 덮는다. **오라클 바닥이 0.078**이므로 답은 있고 두 방법이 못 고르는 것이다 — 굽은 획의 참 평면을 고르는 신호가 더 필요하다(예: 획 여럿이 같은 평면을 공유한다는 가정, 또는 사용자 지정). AS-7이 실획의 26.5%가 곡선이라 하지만 **그 값은 다른 임계(`straight_tol` 0.10)에서 나온 것**이고 여기 임계는 0.18이라 같은 집단이 아니다 — `bend > 0.18`인 실획 비율은 **아직 잰 적이 없다**(S-10) |
| `bend_max` 0.18의 근거 | — | 여전히 없다 | 이 표본은 이봉이라 임계를 시험하지 못한다(굽은 픽스처 85~98%가 위, 곧은 픽스처 대부분 아래). D-S2가 같은 상수에 대해 이미 같은 말을 적었다. **이제 이 값이 판정만이 아니라 배치 여부를 정한다** — S-10에서 재정할 때 두 역할을 함께 본다 |
| 등급 스윕 | — | medium 하나만 쟀다 | `coarse`의 저주파 휘어짐 0.0937은 임계 0.18의 절반이다. 곧은 획이 얼마나 잘려 나가는지 모른다 |
| **돌린 시점의 평면 경로** | — | **쟀고, 안 켠다**(D-S19) | 궤도 75°·110°에서는 회수 0.61~0.74에 오차도 작지만(0.2 초과 1~5%) **45°에서는 놓인 것의 82%가 0.2를 넘는다** — 가림이 가장 심한 각이고 S-6 (1)의 잔여 한계가 그대로 나온다. 깊이 타당성을 걸어도 안 듣는다(틀린 면도 앵커를 지난다). **다시 볼 조건**: 가림을 각도 없이 알아보는 신호가 생기면 — 후보 앵커의 **화면 거리 대비 깊이 차**(S-5의 `view_depth_guard`가 그 형태였다)를 평면 경로의 앵커 선택에 걸어 보는 것이 다음 후보다. `view_plane.json` |

## S-7 (0)

| 항목 | 원래 목표 | 지금 | 사유 |
|---|---|---|---|
| **배선 자동 탐지의 범위** | "측정에서만 켜지는 옵션"을 기계가 잡는다 | `PlaceOpts`만 잡는다 | 잠금이 덮는 것은 **배치 옵션**뿐이다. `AxisCfg`(판정 임계 override)·`CONSENSUS_TOL`·`TUBE_TOL`은 같은 형태의 위험이 있으나 지금은 앱이 기본값을 쓰고 하네스도 그렇다(대조군 제외). **판정 쪽에 옵션이 늘면 같은 표를 만든다** — `classifyStroke(cfg)`에 앱이 `{}`가 아닌 것을 넘기기 시작하는 순간이 그 시점이다 |
| 깊이 타당성을 평면 경로에 | 돌린 시점의 면 오선택을 막는다 | **안 듣는다**(D-S19) | 틀린 면도 앵커를 지나므로 깊이가 기존 범위 안이다(0.2 초과 33 → 33). 되돌렸고 코드에 그 사실을 주석으로 남겼다 |

## S-8

| 항목 | 원래 목표 | 지금 | 사유 |
|---|---|---|---|
| **B안 — 닫힌 루프에서 면 생성** | §6. 가림 처리로 형태를 읽히게 한다 | **만들지 않는다**(D-S20) | 가림은 **이미 있다**(튜브끼리 서로 가린다 — 뒤 모서리의 30%). 면은 획에서 추론한 새 기하이고 §1이 금지하는 쪽이다. B의 난점(곡선 루프의 비평면성)은 S-6 (3)이 이미 "평면을 고를 수 없다"로 답했다. **다시 볼 조건**: 실획으로 획 수십 개짜리 그림을 받았을 때 형태가 안 읽히면 |
| **깊이 단서**(면 대신) | — | 안 만들었다 | 형태가 안 읽힐 때 **면보다 먼저 볼 것**이다 — 안개(거리 페이드)·깊이에 따른 명도·바닥 그림자. 셋 다 **그리지 않은 기하를 만들지 않으므로** §1과 충돌하지 않는다. 지금은 지면 격자가 그 역할을 일부 한다 |
| 복잡한 그림의 판독성 | D-S20의 근거 | **구도 하나·물체 하나에서만 봤다** | 상자 하나를 돌려 본 것이 전부다. 획이 수십 개면 선이 엉켜 안 읽힐 수 있다 — 실획이 그 시험대다 |

## S-8 리뷰어 잔여 (D-S21)

| 항목 | 무엇이 남았나 | 어디서 |
|---|---|---|
| **돌린 시점 사선의 축 오배정** | 궤도 45°에서 사선의 **79%가 축으로 배정**되고 놓인 것은 전부 크게 틀린다. **자동으로 줄일 방법이 지금 없다** — 판정 임계를 건드리는 일이고 AS-13이 금지한다. **실획 대기 항목**이고, 그때까지는 README가 "사선은 첫 시점에서 긋는 편이 안전하다"로 안내한다 | S-10 |
| **고각 18°의 돌린 시점 모서리** | D-S13의 "해소"가 고각 35° 전용이었다. 고각 18°에서 p90 0.591이 남는다. **어느 검사가 왜 안 듣는지 안 쟀다** — 자유단 비율을 고각별로 다시 세는 것이 출발점이다 | S-10 |
| **가림 30%가 원장 밖이다** | 브라우저 픽셀 측정이라 `stage0/out/`에 없고, **유리한 자세를 골라 잰 값**이며 굵기 상수(`base_width_px`)에 비례한다. STALE이 못 잡는다. WebGL이 필요해 vitest로 옮기기 어렵다 — S-10 Playwright에서 원장화한다 | S-10 |
| **B안 기각의 (4)** | "루프의 근사 평면도 같은 문제"는 **재지 않았다**. S-6 (3)이 잰 것은 **끝점 둘로 평면을 고르는** 문제이고 루프는 여러 획의 모든 점을 쓰는 과결정 적합이라 자유도가 다르다. "같은 문제일 수 있다"까지가 근거다 | 필요해지면 |
| `bend_share`와 `one_stroke_one_axis`의 표본 차이 | 같은 코퍼스인데 낱말 수(7 대 6, `line` 포함 여부)와 표본 수(4785 대 1816)가 다르다. 두 원장을 나란히 인용할 때 주의해야 한다 | 기록만 |

## S-8b

| 항목 | 원래 목표 | 지금 | 사유 |
|---|---|---|---|
| **돌린 시점 사선 오배정** | 조용히 틀린 배치를 없앤다 | **완화만 됐다**(D-S22). 45°에서 139 → 99 | **투영의 애매성**이라 2D 증거로는 못 가른다(그 사선들은 축 VP에 2° 안으로 맞는다). **다시 볼 조건**: 2D 밖의 정보를 쓸 때 — **가림 정합성**이 후보다(그린 획이 기존 기하 뒤로 가면 화면에서 안 보였어야 하는데 사용자는 전체를 그렸다. 그 모순이 신호가 된다). 새 기전이고 새 임계다. **⚠ S-10에서 우선순위가 올라갔다** — 아래 참조 |
| **돌린 시점 배치율 0.25~0.46** | — | 그대로 둔다 | D-S22의 대가다. `고치기`가 받는다. **실획에서 이 거래를 다시 본다** — 오배정률이 낮으면 임계를 되돌린다 |
| `angle_max_deg` | 부적합도의 소실점 거리 의존을 없앤다 | **기본 0(꺼짐)** | 원인이 그것이 아니었다(2° 안으로 맞는다). 코드는 남긴다 — 소실점이 아주 가까운 구도에서는 다시 쓸모가 있을 수 있다 |

## S-9 저장·배포

| 항목 | 원래 목표 | 지금 | 사유 |
|---|---|---|---|
| **저장 슬롯 여럿** | 문서 여러 개 | **자동 저장 하나**(`current`) | A-3(가장 단순한 것). 여러 작업은 내보내기로 나눈다. IndexedDB 키만 늘리면 되므로 나중에 싸다 |
| **카메라 재조정 때 사용자 지정 배치 복원** | 손으로 붙인 것을 유지 | **다시 계산하고 알린다**(D-S23) | 앵커는 **다른 획 위의 3D 점**이고 카메라가 바뀌면 그 획도 움직인다(f 10%에 대각의 8%, 최대 32%). 옛 자리에 두면 붙어 있던 획에서 떨어진다. **다시 볼 조건**: 앵커를 "획 id + 매개변수 t"로 저장하면 재계산이 가능하다 — 자료구조를 늘리는 일이라 미룬다 |
| **돌린 시점 획의 재생성** | 카메라가 바뀌면 다시 만든다 | **경로 자체가 없다** | 그 획의 `pts2d`는 그 시점의 뷰 좌표다. 첫 카메라로 재투영할 대상이 아니다. 지금은 `pts3d`를 담아 두는 것으로 대신한다 |
| **glTF 굵기(튜브)** | 그린 굵기를 3D로 | **폴리라인만** | 튜브는 화면 표현이고 원본은 점열이다(D-S20의 연장). 받는 쪽에서 "모델링된 형상"으로 읽히지 않게 한다. 굵기가 필요하면 세션 JSON에 6튜플이 있다 |
| **오프라인 자동 검증** | 기내 모드 회귀 | **수동 절차**(README 2절 13번) | 개발 서버는 서비스 워커를 일부러 등록하지 않는다(HMR 충돌). 빌드본을 띄워 재려면 Playwright에 빌드 단계를 붙여야 한다 — S-10에서 그 사실만 확인하고(등록 0건) 자동화는 미뤘다 |
| `scan_unread_fields`의 문자열 리터럴 | 코드에서만 읽기를 센다 | **문자열 안의 필드명은 읽기로 센다** | 따옴표·템플릿·이스케이프를 파싱해야 한다. 지금 이득보다 복잡도가 크다. **한계를 테스트로 못 박았다**(`tests/test_selfcheck_scan.py`) — 바뀌면 깨져서 알려 준다 |

## S-10 종단 검증

| 항목 | 원래 목표 | 지금 | 사유 |
|---|---|---|---|
| **가림 30%의 원장화** | 브라우저 픽셀 측정을 `stage0/out/`으로 | **안 했다** | S-10의 스크린샷은 남지만(`stage0/out/e2e/`) **가림 비율을 세는 코드는 없다**. 세려면 뒤 모서리를 알고 픽셀을 대조해야 한다 — D-S20의 근거가 여전히 원장 밖이다(S-8 리뷰어 잔여 ③이 그대로 남는다) |
| **스크린샷 회귀의 범위** | 화면 전체 | **왼쪽 캔버스만** | 3D는 GPU·드라이버에 따라 픽셀이 흔들린다. 2D 캔버스는 같은 카메라·같은 획이면 같은 그림이라 회귀를 잡을 수 있다. 3D는 개수(정점·삼각형)로만 잠근다 |
| **지연 측정의 조건** | 실기(iPad) | **데스크톱 Chromium·개발 서버** | 번들이 없어 더 불리한 조건이므로 **상한**으로 읽는다. iPad 실측은 사람이 기기를 잡아야 한다(README 2절) |
| **손 모델링 대비 대조 실험** | "이 도구가 더 싼가"를 잰다 | **자릿수 판단만** | CAD에서 모서리 하나에 최소 두 번의 스냅 클릭이 필요하다는 것을 기준선으로 삼았다. 실제 대조 실험(같은 형상을 두 도구로)은 사람이 필요하다 |
| **고각 스윕** | 고각 18°/35° 둘 다 | 18°만(`fix_load`) | `view_plane`·`continue_after_rotate`가 각각 18°·35°다. 고치기 부하를 고각별로 가르지 않았다 — 1.3의 미해소 구간이 부하에 어떻게 나타나는지는 미측정 |

## ⬆ S-10 판정으로 **우선순위가 올라간 항목**

**가림 정합성** — 지시가 "S-10에서 고치기 부하가 감당 가능하면 DEFERRED로 두고, 감당 불가면
우선순위를 올린다"였다. **감당 불가 쪽이 나왔다**: 돌린 시점 궤도 75°·110°에서 **그린 획당
추가 조작이 2.5**로, 기준선(CAD 모서리 하나에 클릭 둘)을 넘는다(`fix_load.json@1671e540`).

부하의 원인이 곧 가림이다 — 돌린 시점의 배치율을 절반 아래로 떨어뜨린 것이 D-S22의 보수적
판정이고, 그렇게 한 이유가 **2D 증거로 옳고 그름을 못 가르기 때문**이다. 가릴 수 있는 신호가
생기면 그 판정을 되돌릴 수 있고, 그러면 부하가 함께 내려간다.

**착수 조건**: 실획 측정이 먼저다(임계 재조정이 이 거래를 바꿀 수 있다). 실획이 안 들어오면
가림 정합성이 다음 작업이다. 시작점은 `strokeView`가 이미 만드는 튜브 메쉬의 깊이 버퍼다.
